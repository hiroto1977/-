# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty. It contains only a placeholder `README.md` (whose sole content is the repo name `-`) and no source code, build configuration, dependency manifests, tests, or tooling.

There is therefore no codebase architecture to summarize and no build/lint/test commands to document yet. When future work introduces actual code, update this file to describe:

- The big-picture architecture (component boundaries, data flow, key abstractions) that spans multiple files.
- Commands for building, running, linting, and testing — including how to run a single test.
- Any project-specific conventions that are not obvious from reading individual files.

## Branching

Active development for Claude Code sessions happens on the branch designated in the task prompt (e.g. `claude/add-claude-documentation-F7HIa`). The default branch is `main`.
