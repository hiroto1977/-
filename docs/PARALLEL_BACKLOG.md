# 並列作業キュー & 待ち時間プロトコル

> 複数の継続タスク（ループ）を、CI 待ち時間を無駄にせず順次・前倒しで進めるための仕組み。
> セッション/`/loop`/「作業を再開」のたびに本キューの先頭から次アクションを実行する。

## 制約（正直な前提）

- 開発ブランチは **`claude/modest-noether-knlmJ` の1本**に限定（タスク指示）。
  複数 PR を同時に開くと差分が混ざるため、**PR は順次**（1本マージ→次を push）。
- 別ブランチ/worktree での真の同時並列は、**ユーザーの明示許可がある場合のみ**
  （`NEVER push to a different branch without explicit permission`）。

## 待ち時間プロトコル（CI 待ちの〜2分を活用）

現 PR の CI を待つ間、**リポジトリを変更しない読み取り作業**でキューの次アイテムを前倒しする:

1. **調査の先行**（コミット不要）: 確証ループ①なら次の制度の一次情報を WebSearch で裏取り、
   コード系②③なら対象コードを読み設計を確定。
2. **マージ後に即コミット**: 現 PR がマージされたら main へ同期し、前倒しで用意した変更を
   すぐコミット→push→PR。アイドル時間を最小化する。
3. **コミットは現 PR マージ後のみ**（単一ブランチを汚さない）。CI 待ち中はローカル編集も避け、
   読み取り/調査に限定する。
4. 真に同時並列が必要になったら、ユーザーに別ブランチ/worktree の許可を求める。

## キュー（順番に回す。完了で末尾へ）

| # | ループ | 直近の成果 | 次アクション |
|---|---|---|---|
| ① | 法務/税務/労務の確証 | 第7バッチ #306（確証**65件**: 税22/労23/法20） | 一次情報の裏取りを継続（残: 古物営業法・通信販売の返品・寄附金損金・生理休暇・安全配慮義務・インボイス登録番号 等）。「OK」まで継続 |
| ② | オーケストレーション思考精度 | #296（スコアリング+確信度+僅差判定） | 確信度/ambiguous をチャット返信ラベルに表示（現状 routeForService は採用済みだが UI 非表示） |
| ③ | DB/インフラのセキュリティ | #298（診断エンジン+バックアップ実状態アダプタ） | (a) SecurityPage の診断に実信号を配線（autoLock/recordCipher 整合/実バックアップ manifest）。現状は保守的 false 固定。(b) クラウドバックアップ連携の実装。(c) Linux×GitHub 統合ビュー |

## 棚卸し（2026-06-13 時点の残作業）

| 優先 | 項目 | 内容 | 規模 |
|---|---|---|---|
| ★高 | **A. 確証知識のアプリ露出** | 確証済み65件はデータ＋検証エンジン＋`COMPLIANCE_KNOWLEDGE.md` のみで**アプリUIに未表示**。分野別の制度一覧＋出典リンク＋確証サマリを表示する画面/セクションを追加 | 中（新ページ or 既存ページのセクション） |
| 中 | B. ③DB診断の実信号配線 | `SecurityPage` の `dbReport` が autoLock/integrity/backup を保守的 false 固定。`backupPosture` 等で実状態を反映 | 小〜中 |
| 中 | C. ③クラウドバックアップ連携 / Linux×GitHub 統合ビュー | 当初要望のうち未着手分（DB診断のみ実装済み） | 中〜大 |
| 低 | D. ②確信度のUI表示 | routeForService の confidence/ambiguous をチャット返信に表示 | 小 |
| 継続 | E. ①確証ループ | 「OK」まで6並列バッチを継続 | 反復 |
| 要判断 | F. 古い未マージPR | #256（head=main の誤構成・要クローズ判断）、#270（別セッションの古い draft）。**いずれも本ループ外でユーザー判断待ち** | — |

> 状態は各 PR マージ時に更新する。①はユーザーが「OK」と言うまで継続（最優先の標準指示）。

## 終了条件

- ①: ユーザーが「OK」と明言するまで継続。
- ②③: 各ロードマップ完了、またはユーザー指示で停止。
- 本キューが空＝「対応すべき作業なし」を1行で報告して停止（`/loop` の静穏時挙動）。
